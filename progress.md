Original prompt: make it happen, can you wire the found artifacts in an appropriate spot that I can view during the run, along with a tooltip to tell me what they do

Current follow-up: On-demand battle card/pile inspector extraction. Added `src/game/battle/render-inspect.ts`, an 8.6 KB / 2.9 KB gzip optional module that owns deck/draw/discard review framing, counts, rows, scrolling, art, rules text, chips, and detail presentation without owning pile state, card rules, costs, selection, or callbacks. BattleScene requests it only when a combat card index opens, reports `battleInspectRenderer` telemetry, retains the successful module across scene reuse, shows explicit loading/failure panels, and retries a failed import on the next open. Production entry is now 653.5 KB / 170.5 KB gzip and combined entry + game-core is 682.0 KB / 180.9 KB gzip, improving the fresh pre-extraction baseline by 4.6 KB / 1.1 KB gzip. Added a 12 KB hard budget and deployment rules requiring exactly one hashed, non-preloaded `render-inspect` chunk. Strict TypeScript and two focused pile/inspector regressions pass. The required production client proved the module stays cold through normal combat, then opened Draw with a real pointer, loaded successfully with zero browser errors, and produced the inspected capture under `.artifacts/web-game-inspect-renderer/`. Major battle render-helper extraction is complete; a full lazy BattleScene remains optional P2.

Final verification: `npm run validate` passes all release gates and nine sequencing scenarios. The first full browser pass caught one legacy deck-review test's fixed 80 ms assumption; it now waits on explicit `battleInspectRenderer.loaded` plus rendered flourish telemetry without weakening its generated-art assertions. The focused deck/pile rerun passes, followed by a clean 155-test Chromium rerun in 17.8 minutes on one worker.

Current follow-up: Lazy battle hand-renderer extraction. Added `src/game/battle/render-hand.ts`, a 7.2 KB / 2.7 KB gzip battle-only module that owns hand-rail composition, compact card art/fallbacks, generated frames, selection pulses, Flow/Surge hints, late compact-art refresh, and large hover dossiers without owning card instances, energy/cost rules, targeting, preview simulation, selection, or play callbacks. BattleScene guards it during readiness, retains the pure module across reuse, reports `battleHandRenderer.ready/loaded`, and supplies compact clickable cards plus a rules-preview fallback if the chunk fails. Production entry is now `index-CM0wXETm.js` at 659.5 KB / 171.9 KB gzip and combined entry + game-core is 688.0 KB / 182.3 KB gzip, improving the HUD baseline by 0.7 KB entry and 0.8 KB combined boot. Added a 10 KB hard budget and deployment rules requiring exactly one hashed, non-preloaded `render-hand` chunk. Strict TypeScript and seven focused readiness, pointer-selection, ordering, compactness, instance-identity, hover-detail, and generated-frame tests pass. The required shared production client drove title -> route -> combat, selected First Flight with the real pointer, preserved its outcome/Flow forecast, reported the renderer ready/loaded with zero error artifacts, and produced the inspected capture under `.artifacts/web-game-hand-renderer/`; the separately exercised and inspected hover dossier is under `.artifacts/web-game-hand-preview/`. `npm run validate` passes every release gate and all nine sequencing scenarios. An initial complete run surfaced two unrelated load-order samples in How-to-Play and exhaustive Codex layout coverage; both passed in isolation, their readiness/yield conditions were hardened without reducing assertions, and the clean rerun passes all 155 Chromium tests in 22.2 minutes. Remaining P1 is battle card/pile inspection presentation; full lazy BattleScene remains optional P2.

Current follow-up: Lazy battle HUD-renderer extraction. Added `src/game/battle/render-hud.ts`, a 7.0 KB / 2.5 KB gzip battle-only module that owns draw/discard pile presentation and hit areas, combat-log and Roost framing, Beat/Hustle progress, First Flight guidance, encounter-objective presentation, and selected-card outcome banners without owning combat decisions, pile contents, exact damage math, selection, callbacks, or sequencing. BattleScene guards the renderer during combat readiness, retains the pure module across reuse, reports `battleHudRenderer.ready/loaded`, and supplies compact playable fallbacks on chunk failure. Production entry is now `index-vMdeAScE.js` at 660.2 KB / 172.2 KB gzip and combined entry + game-core is 688.8 KB / 182.5 KB gzip, improving the reward baseline by 1.7 KB entry and 1.6 KB combined boot. Added a 10 KB hard budget plus deployment rules requiring exactly one hashed, non-preloaded `render-hud` chunk. Strict TypeScript and twelve focused readiness, pile, Roost, guidance, objective, Hustle, event-log, selection, ordering, and lethal-preview tests pass. The required shared production client drove title -> route -> combat, proved the settled HUD and renderer telemetry with zero error artifacts, then clicked Roost and advanced to turn 2; inspected evidence is under `.artifacts/web-game-hud-renderer-settled/` and `.artifacts/web-game-hud-renderer-roost/`. `npm run validate` passes every release gate and all nine sequencing scenarios; the complete 155-test Chromium suite passes in 18.9 minutes. Remaining P1 is battle hand and inspect render ownership; full lazy BattleScene remains optional P2.

Current follow-up: Lazy battle reward-renderer extraction. Added `src/game/battle/render-reward.ts`, an 11.4 KB / 3.6 KB gzip reward-time module that owns card, Preen, and Waymark ceremony framing, choice composition, deck-need chips, art fallbacks, hover/spotlight/glow presentation, tags, and Skip presentation without owning reward selection or progression. BattleScene requests it only when a ceremony opens, visibly locks hidden choices during load, allows interaction only after the renderer or fallback is ready, reports `battleRewardRenderer` readiness telemetry, and supplies a compact clickable failure fallback. Production entry is now `index-BYsCFMQA.js` at 661.9 KB / 172.7 KB gzip and combined entry + game-core is 690.4 KB / 183.1 KB gzip, improving the foreground baseline by 6.0 KB minified / 1.2 KB gzip. Added a 14 KB hard budget plus deployment rules requiring exactly one hashed, non-preloaded `render-reward` chunk. Strict TypeScript and six focused reward/readiness/guidance/hover/input tests pass. The required shared production client proved the chunk stays unrequested through live combat, then crossed victory with three ready choices, zero errors, and an inspected capture at `.artifacts/web-game-reward-ceremony/shot-2.png`. `npm run validate` passes every release check and all nine sequencing gates; the complete 155-test Chromium suite passes in 19.6 minutes. Remaining P1 is battle HUD/hand/inspect render ownership; full lazy BattleScene remains optional P2.

Current follow-up: Lazy battle foreground-renderer extraction. Added `src/game/battle/render-foreground.ts`, a 6.5 KB / 2.4 KB gzip battle-only module that owns enemy/leader artwork and fallbacks, target reticles, elite crests, vitals, intent badges, status chips, and passive presentation motion without owning combat state. BattleScene still computes placement, HP geometry, intent math, damage, selection, callbacks, and queued action motion; it gates the renderer during battle readiness, retains the module across reuse, reports `battleForegroundRenderer.ready/loaded`, and degrades to clickable silhouettes on chunk failure. Production entry is now `index-BA1jXShr.js` at 667.9 KB / 174.0 KB gzip and combined entry + game-core is 696.5 KB / 184.3 KB gzip, improving the backdrop-renderer baseline by 2.9 KB minified / 0.9 KB gzip. Added an 8 KB hard chunk budget plus deployment rules requiring one hashed, non-preloaded `render-foreground` chunk. Strict TypeScript and five focused readiness/vitals/status/multi-enemy/reduced-motion tests pass. The required production client drove title -> route -> combat, reached a settled unlocked battle with renderer ready/loaded and zero error artifacts, and the inspected evidence is under `.artifacts/test-results/battle-foreground-renderer/production-proof/`. `npm run validate` passes all release gates and nine sequencing scenarios; the complete 155-test Chromium suite passes in 18.1 minutes. Remaining P1 is battle HUD/hand/reward/inspect render ownership; full lazy BattleScene remains optional P2.

Current follow-up: Lazy battle backdrop-renderer extraction. Added `src/game/battle/render-backdrop.ts`, a 2.0 KB / 0.9 KB gzip battle-only module that owns district imagery, procedural fallback scenery, street/rival/boss mood treatments, vignette shading, and Full/Lean/reduced-motion atmosphere tiling without owning combat state. BattleScene loads it during guarded readiness, retains the pure module across scene reuse, reports `battleBackdropRenderer.ready/loaded`, and degrades to a battlefield image or solid stage if the chunk fails instead of blocking play. Removed the prior backdrop/treatment/atmosphere/fallback method bodies from `src/main.ts`. Production entry is now `index-BEUQaSNC.js` at 670.8 KB / 174.9 KB gzip and combined entry + game-core is 699.3 KB / 185.2 KB gzip, improving the prior lazy-debug baseline by 0.9 KB entry and 1.0 KB combined boot. Added a 4 KB hard chunk budget plus deployment rules requiring exactly one hashed, non-preloaded `render-backdrop` chunk, and updated deployment documentation. Strict TypeScript passes; focused first-load, four-boss-variant, encounter-mood, and Settings/Lean-effects tests pass with the exact 10-strip Lean atmosphere contract; `npm run validate` passes all release checks and nine sequencing scenarios; and all 155 Chromium tests pass in 22.4 minutes. The required shared production client drove title -> route -> combat with the renderer ready/loaded, Rooftop Blocks visible, 21 full-quality atmosphere strips, one backdrop build/four persistent backdrop objects, complete hand/enemy state, zero error artifacts, and an inspected screenshot under `.artifacts/test-results/battle-backdrop-renderer/shared-client/`. Remaining P1 is battle foreground/HUD/hand/reward/inspect render ownership; a full lazy BattleScene boundary remains optional P2 work.

Current follow-up: Lazy BattleScene debug-state extraction and boot-budget recovery. Added `src/game/battle/debug-state.ts`, a 5.3 KB / 2.0 KB gzip lazy module that declaratively assembles generated-FX, UI-frame, visibility, label, and burst telemetry while BattleScene retains gameplay state and sequencing. Battle readiness now guards the diagnostics request but degrades safely on chunk failure, and a loaded pure builder persists across live scene re-init so automation state remains complete. Removed the former roughly 900-line block of repeated tree probes from `src/main.ts`; production entry is now `index-Di0c_1Yt.js` at 671.7 KB / 175.3 KB gzip and combined entry + game-core is 700.3 KB / 185.7 KB gzip, 20.7 KB minified below the previous baseline and under both preferred targets. Added an 8 KB hard chunk budget plus deployment rules requiring exactly one hashed, non-preloaded debug-state chunk. Focused generated-FX/readiness/HUD/reward/settings contracts pass, strict TypeScript passes, `npm run validate` passes all release checks and nine critical sequencing tests, and the complete 155-test Chromium suite passes in 18.2 minutes. The exhaustive reserve-enemy enumeration now has a scoped 60-second ceiling without reduced assertions after independently exceeding the global 30-second limit. The required shared production client drove title -> route -> combat with both lazy battle modules ready/loaded, complete telemetry, no errors, and inspected evidence under `.artifacts/test-results/battle-debug-state/shared-client/`. Remaining performance P1 is pure battle render-helper extraction; a full lazy BattleScene boundary remains optional P2 work.

Current follow-up: Generated the first chroma-keyed asset batch from the new imagegen roadmap prompt pack, focused on high-leverage transparent combat FX. Built-in imagegen produced sources for `combat-overextension-warning`, `combat-boss-phase-break`, and `combat-perfect-chain`; originals remain under `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\`, and project-preserved sources are `assets/concept-art/fx/sources/combat-overextension-warning-source-v1.png`, `assets/concept-art/fx/sources/combat-boss-phase-break-source-v1.png`, and `assets/concept-art/fx/sources/combat-perfect-chain-source-v1.png`. Local chroma-key removal wrote keyed raw artifacts under `.artifacts/test-results/<slug>/`, detecting keys `#06f80f`, `#03f80a`, and `#0ce716`. Normalized runtime outputs are `assets/runtime/fx/combat-overextension-warning.png` (512x256, transparent corners, 7 green adjustments), `assets/runtime/fx/combat-boss-phase-break.png` (768x384, transparent corners, 37 green adjustments), and `assets/runtime/fx/combat-perfect-chain.png` (768x192, transparent corners, 4 green adjustments). Visual contact sheet `.artifacts/test-results/generated-combat-fx-pack/contact-sheet.png` confirmed all three read on checkerboard without obvious key fringe. `npm run validate:runtime-assets` passed after generation.

Current follow-up: Imagegen asset roadmap prompt-pack hardening after feedback asking whether the roadmap items were descriptive enough to use for chroma-key image generation. Updated `docs/art/imagegen-asset-roadmap.md` with a concrete `Ready-To-Generate Prompt Pack`, refreshed the audit date, and added paste-ready prompts with source paths, runtime paths, chroma-key guidance, target sizes, composition constraints, and avoid lists for named card refreshes, enemy/boss cutouts, combat FX candidates, combat/route UI candidates, market/reward/system candidates, Waymark item icons, battlefield variants, and procedural fallback graduation sheets. The document now works as both a planning map and a practical imagegen production prompt library.

Current follow-up: Imagegen asset roadmap consolidation after feedback to stop handling generated assets piecemeal. Added `docs/art/imagegen-asset-roadmap.md` as a future-production guide that audits current runtime coverage (244 registered UI icon/chrome assets, 51 combat FX files, 110 approved card portraits/thumbs, 64 approved enemy full-body assets, 58 Waymark icons, 32 Supply/item icons, and 8 route node icons), defines global chroma-key and cleanup rules, records shared Bird Squad art-language constraints, and breaks future work into reusable packs for cards, enemies/bosses, flock leaders, combat FX, combat HUD chrome, route map components, route-event set pieces, market/services, reward ceremonies, codex/profile/deck-review surfaces, system overlays, title/leader select, Waymarks/Supplies, battlefields, and procedural fallback graduation. Linked the roadmap from `docs/README.md` so later imagegen/chroma-key work has a discoverable planning entry point.

Current follow-up: Combat enemy impact contact polish after feedback that enemy attacks still lacked felt impact. Built-in imagegen produced a textless talon/contact burst on flat chroma key, preserved at `assets/concept-art/fx/sources/combat-enemy-impact-contact-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_0c9ade04acf5cc8c016a4dd2c3c25481908b826e7ece110be4.png`). Local key removal detected `#08f905` and wrote `.artifacts/test-results/combat-enemy-impact-contact/combat-enemy-impact-contact-keyed-raw.png` with 1,166,278 transparent pixels and 26,124 partial-alpha pixels. The normalized runtime asset is `assets/runtime/fx/combat-enemy-impact-contact.png` (768x256, 205,159 bytes), with runtime visible bounds `(24, 28, 744, 229)`, 50,586 opaque pixels, 12,714 partial-alpha pixels, transparent corners, and zero remaining visible/edge green heuristic pixels. `src/main.ts` now imports/preloads `combat-enemy-impact-contact`, renders a reduced-motion-safe two-pass contact burst (warm additive flare plus detailed normal sprite) at the delayed enemy damage moment, anchors it over the flock body, keeps it readable before fading, and exposes `combatEnemyImpactContact.loaded/rendered/count/bursts` through BattleScene text state. `tests/smoke.spec.ts` now asserts normal and reduced-motion enemy turns render the generated contact burst after damage actually lands, while still proving HP stays unchanged at impact start. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up|combat reduced-motion enemy turn still holds damage until impact" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, and screenshot inspection of `output/web-game/shot-0.png`. Final impact captures are in `.artifacts/test-results/combat-enemy-impact-contact/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all report impact starting with HP held at 38/38, later HP 32/38 during the same impact beat, `combatEnemyImpactContact { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatFlockImpactBurst { loaded: true, rendered: true, count: 1, bursts: 1 }`, and visible additive/normal contact geometry at stage `(314, 350)`. Visual inspection confirmed the enemy attack now has a readable wind-up/commit marker plus a clear contact burst on the flock without covering the top HUD, enemy nameplate, combat log, hand area, or fixed-canvas mobile letterbox.

Current follow-up: Enemy attack impact anticipation pass after fresh feedback that enemy attacks still lacked gravity/readability. `src/main.ts` now splits the animated enemy attack contact into a visible `impact` anticipation window before damage resolves: after release/commit, the battle enters `impact` with Cohesion still held, re-queues heavier attacker body motion, keeps the commitment seal/action trail alive, waits `720ms` normal timing (scaled by combat reduced-motion timing), then applies the enemy effects and holds the post-hit impact before recovery. Enemy attack body language is heavier (`-82px` lunge, deeper squash, longer hold), and commitment seal/action-trail lifetimes now cover the added contact window. `tests/smoke.spec.ts` now asserts both normal and reduced-motion enemy turns enter `impact` with HP unchanged, expose a substantial impact progress duration, then land damage only after the delayed contact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up|combat reduced-motion enemy turn still holds damage until impact" --workers=1 --reporter=line`, live page timing sample on `http://127.0.0.1:5199/?playwright=1` showing impact at HP 38/38 then damage landing roughly 800ms later, `npm run validate:runtime`, `npm run build`, required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, screenshot inspection of `output/web-game/shot-0.png`, and `git diff --check -- src\main.ts tests\smoke.spec.ts progress.md` with only existing LF-to-CRLF warnings. Targeted captures are in `.artifacts/test-results/enemy-impact-anticipation/`: highres, desktop, and mobile impact-anticipation screenshots report `impact`, pending true, HP 38/38, `combatEnemyCommitmentSeal { loaded: true, rendered: true, count: 3, bursts: 1 }`, `combatActionTrail { loaded: true, rendered: true, count: 1, bursts: 2 }`, the beat frame visible, and zero page errors. Visual inspection confirmed the desktop and mobile impact frames read clearly without covering HUD, HP bars, combat log, hand, or the fixed-canvas mobile letterbox.

Current follow-up: Combat player commitment sigil polish for making player card attacks feel readable before impact, matching the enemy-turn gravity work. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta horizontal avian card-commitment crest on flat chroma key, preserved at `assets/concept-art/fx/sources/combat-player-commit-sigil-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_0d13d79ea1a0acea016a4dc92abce4819687a9d32974c58977.png`). Local key removal detected `#07f809` and wrote `.artifacts/test-results/combat-player-commit-sigil/combat-player-commit-sigil-keyed-raw.png` with 1,058,372 transparent pixels and 25,265 partial-alpha pixels. The normalized runtime asset is `assets/runtime/fx/combat-player-commit-sigil.png` (768x256, 198,193 bytes), with source visible bounds `(23, 13, 2114, 714)`, crop box `(0, 0, 2156, 724)`, runtime visible bounds `(25, 9, 736, 248)`, transparent corners, 49,199 opaque pixels, 14,277 partial-alpha pixels, 10 conservative green adjustments, and zero visible/edge green heuristic pixels. `src/main.ts` now imports/preloads `combat-player-commit-sigil`, adds a procedural `playerCommitment` SFX cue, renders a reduced-motion-safe three-layer commitment marker plus motes/streak during the delayed player-card resolution window, tunes it smaller/leftward after visual inspection so it does not swallow the combat lane, and exposes `combatPlayerCommitSigil.loaded/rendered/count/bursts` through BattleScene text state. The focused combat timing smoke now proves the player commitment marker and cue fire while `combatAnimationPending` is true, the card has left hand, enemy HP is still unchanged, and the hit-confirm effect has not fired yet; enemy-turn gravity assertions remain intact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4333/?playwright=1` are in `.artifacts/test-results/combat-player-commit-sigil/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all report pending true, Roof Rat HP held at 27/27, `combatPlayerCommitSigil { loaded: true, rendered: true, count: 3, bursts: 1 }`, `combatPlayerHitConfirm { loaded: true, rendered: false, count: 0, bursts: 0 }`, `playerCommitment` cue 1, visible sigil geometry around stage `(557, 219)` at `319x106`, and zero page/console errors. Visual inspection confirmed the final tuned marker gives the player attack a strong commitment beat while keeping HP values, card text, HUD metrics, encounter plaque, and fixed-canvas mobile letterbox readable. Temporary preview port 4333 was stopped after verification.

Current follow-up: System Settings motion preference control polish after the enemy-turn readability work. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta three-position Motion switch on flat chroma key, preserved at `assets/concept-art/ui/icons/sources/system-settings-motion-switch-frame-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_0e8fff5a54081a49016a4dc497f61081978ff66cd84303d419.png`). Local key removal detected `#06f808` and wrote `.artifacts/test-results/system-settings-motion-switch/system-settings-motion-switch-frame-keyed-raw.png` with 1,025,101 transparent pixels and 12,491 partial-alpha pixels. The normalized runtime asset is `assets/runtime/ui/icons/system-settings-motion-switch-frame.png` (768x256, 260,249 bytes), with source visible bounds `(50, 241, 1724, 633)`, crop box `(8, 207, 1766, 667)`, runtime visible bounds `(16, 39, 752, 215)`, transparent corners, 97,306 opaque pixels, 10,144 partial-alpha pixels, and zero edge-green heuristic pixels. `src/main.ts` now stores `birdsquad.motionPreference` as `system/full/reduced`, resolves `prefersReducedMotion()` from that preference plus the OS setting, renders the generated Motion switch in shared Settings for menu, route, and battle, and reports `motion` plus `systemSettingsMotionSwitchFrame.loaded/rendered/count` through text state. `tests/smoke.spec.ts` now clicks the Motion hit target, proves System -> Full -> Reduced persistence, and verifies the generated switch frame renders in all three Settings contexts while the reduced-motion enemy-turn smoke still proves damage waits until impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "settings overlay opens|combat reduced-motion enemy turn still holds damage until impact" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4332/?playwright=1` are in `.artifacts/test-results/system-settings-motion-switch/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all report Settings open, `motion { preference: "system", reduced: false, systemReduced: false }`, `systemSettingsMotionSwitchFrame { loaded: true, rendered: true, count: 1 }`, 5 settings row frames, 2 toggle frames, 2 slider frames, one motion frame, visible hit geometry at stage `(806, 484)`, and zero page/console errors. Visual inspection confirmed the switch sits cleanly in the Settings panel without overlapping the row label, status value, sliders, bottom buttons, or fixed-canvas mobile letterbox. Temporary preview port 4332 was stopped after verification.

## 2026-07-28 Player-Chosen Hunt List Milestones

- Added a persistent three-card Hunt List for discovered, uncollected cards.
  Tracking never changes reward odds, reward pools, or flight power.
- Claims from combat rewards, route rewards, the Market, and enemy Snags now
  complete the target atomically with source/date provenance and remove it from
  the active list. Reward badges, logs, milestone audio, Codex dossiers, text
  state, and screen-reader summaries all distinguish active and completed hunts.
- Added the ninth Codex card tab with populated and actionable empty states,
  non-color-only target markers, pointer/touch controls, keyboard `T`, and
  controller `Y`. Save export and transactional restore preserve the list.
- Focused collection/reward coverage passed 13/13, strict TypeScript and the
  production build passed, and the shared production client capture was
  inspected at `.artifacts/hunt-list-shared-client-final/shot-0.png`.

## 2026-07-28 Persistent Codex Collection Lens

- Added a header-level Collection Lens with `All`, `Collected`, `Uncollected`,
  and `Seen` ownership views. It composes with every set, Favorites, and Hunt
  List without adding another cramped card tab, resets grid focus/scroll
  coherently, and gives zero-result views explicit guidance instead of a blank
  binder.
- Pointer/touch, keyboard `L`, controller `LB`, focus-ring navigation,
  `render_game_to_text`, and screen-reader summaries share the same four-state
  model. The selected lens persists as a UI preference and is included in
  local Flock Record backup and transactional restore, while account progression
  and combat boot state remain untouched.
- The initial implementation exposed a 0.1 KB game-core budget regression.
  Moving the lens into the existing preferences backup boundary restored the
  unchanged hard gate: game core is 30.0 KB, app entry is 676.9 KB / 179.4 KB
  gzip, and combined boot is 706.9 KB / 190.3 KB gzip. The 1.9 KB app-entry
  preference advisory remains; all hard budgets pass.
- Focused save/lens checks pass 3/3, the surrounding Codex/input/minimum-viewport
  suite passes 15/15, and full `npm run validate` passes every validator plus
  all 24 critical sequencing scenarios. Inspected captures:
  `.artifacts/test-results/codex-collection-lens-seen.png`,
  `.artifacts/test-results/codex-collection-lens-empty.png`, and
  `.artifacts/collection-lens-shared-client/shot-0.png`. The shared production
  client reported the Collected lens focused with `0/22` visible and no browser
  error artifact.
- Next collector-loop gap: direct card search and richer sort modes (name,
  rules/keyword, and acquisition recency) for faster navigation as the library
  grows. Five genuinely observed fresh-player sessions remain necessary
  qualitative release evidence.

## 2026-07-28 Persistent Metadata-Aware Codex Card Search

- Added a dedicated Codex `FIND CARDS` field that composes with the active card
  set, Favorites/Hunt List views, and Collection Lens. Multi-token,
  case/diacritic-insensitive matching covers the card metadata the game
  actually models: name, rules/effects, keywords, bird/character, set/suit,
  kind/type/target, cost, rarity, and ownership.
- Search deliberately protects discovery. Discovered cards expose their useful
  searchable dossier metadata; unfound cards match only safe structural/family
  and ownership terms, so a hidden card cannot be revealed by searching its
  name or rules. The zero-result panel names the query and gives a direct edit/
  clear path instead of leaving an empty binder.
- Pointer/touch opens a real native search input; `/`, focused Confirm, and
  controller `RB` use the same path. Enter applies, Escape restores the
  pre-edit query, Tab/focus-ring navigation includes the control, and the field
  has a 40-character limit plus a full screen-reader label. The editor keeps a
  physical 190x46 minimum target at the 1000x560 supported floor and dismisses
  itself whenever the authoritative platform gate hides the game.
- The query persists in `birdsquad.codexCardSearch`, appears in
  `render_game_to_text`, is announced with result/lens counts, and is included
  in local Flock Record export and transactional restore. Legacy backups safely
  default to an empty query.
- Strict TypeScript passes. The six neighboring Codex focus/favorite/Hunt/
  Lens/search scenarios pass together, and the focused search plus backup/
  restore run passes 3/3. Full `npm run validate` passes all validators and all
  24 critical sequencing scenarios. Production remains at 676.9 KB / 179.4 KB
  gzip app entry, 706.9 KB / 190.3 KB gzip combined boot, and 30.0 KB game
  core; the lazy Codex scene is 89.5 KB / 23.2 KB gzip. The unchanged 1.9 KB
  app-entry preference advisory remains while every hard budget passes.
- Inspected captures:
  `.artifacts/test-results/codex-card-search-editing.png`,
  `.artifacts/test-results/codex-card-search-populated.png`,
  `.artifacts/test-results/codex-card-search-empty.png`, and
  `.artifacts/test-results/codex-card-search-minimum-landscape-editing.png`.
  The required shared production client used real pointer input from title to
  Codex to search focus; its state reports `editing: true`,
  `nativeInput: true`, the complete field contract, and no browser-error
  artifact under `.artifacts/codex-search-shared-client/`.
- Next collector-loop priority is explicit sorting (name, rarity, acquisition
  recency) plus a newly-acquired marker/clear flow. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence.

## 2026-07-28 Persistent Privacy-Safe Codex Card Sorting

- Split the existing Find Cards header control into two clear, independent
  targets without increasing header density. `SORT` cycles through `Binder`,
  `Name`, `Rarity`, and `Recent`; its current state remains visible beside the
  search query and composes with set tabs, Favorites/Hunt List, Collection
  Lens, and search results.
- Sorting protects discovery: Name and Rarity order only discovered cards by
  visible metadata while unfound slots remain in stable binder order. Recent
  uses the journaled `firstAcquiredAt` provenance for collected cards, then
  places seen/uncollected cards before unfound slots. No hidden name, rarity, or
  acquisition inference is exposed.
- Pointer/touch uses a separate 62x46 segment, keyboard `R` and controller `RT`
  cycle the same four-state model, and Tab/focus-ring navigation reaches Sort
  independently from Find. `render_game_to_text` exposes visible, discovered,
  and collected order plus the privacy guarantee; screen-reader summaries
  announce the active mode and controls.
- `birdsquad.codexCardSort` persists the mode and participates in local Flock
  Record export and transactional restore. Legacy backups default safely to
  Binder.
- Strict TypeScript passes. The focused sort scenario proves pointer, keyboard,
  controller, all four orderings, stable hidden slots, timestamp ordering,
  search composition, scene reload persistence, focus state, and screen-reader
  output. Both save-backup scenarios pass, and the complete neighboring Codex
  suite passes 7/7. Full `npm run validate` passes all validators plus all 24
  critical sequencing scenarios.
- Production remains unchanged at 676.9 KB / 179.4 KB gzip app entry,
  706.9 KB / 190.3 KB gzip combined boot, and 30.0 KB game core. The lazy Codex
  scene is 92.8 KB / 23.9 KB gzip; the unchanged 1.9 KB preferred app-entry
  advisory remains while every hard budget passes.
- Inspected evidence:
  `.artifacts/test-results/codex-card-sort-recent.png` and
  `.artifacts/codex-sort-shared-client/shot-0.png`. The required shared
  production client used real pointer input from title to Codex and cycled
  Binder -> Name -> Rarity -> Recent; state reports `recent`, Sort focus,
  `hidesUndiscoveredMetadata: true`, and no browser-error artifact.
- Next collector-loop priority is a newly acquired marker with an intentional
  per-card and bulk-clear flow, followed by acquisition-source/date filtering.
  Five genuinely observed fresh-player sessions remain necessary qualitative
  release evidence.

Current follow-up: Enemy-turn readability/gravity pass after feedback that enemies still took turns too fast. `src/main.ts` now gives animated enemy beats explicit progress state (`combatEnemyTurnProgress`) and upgrades the existing top-right beat badge into an enemy phase strip during enemy actions, showing labels like `WIND-UP`, `COMMIT`, `IMPACT`, and the current move with a filling progress rail. Reduced-motion combat now keeps readable timing instead of collapsing drama: combat delays scale to 75% with a 520ms minimum, and reduced-motion wind-up tell art/plaque/charge assets remain visible through the wind-up beat while still skipping the busy motion particles. The normal animated enemy path remains damage-safe: Cohesion still does not change until the impact beat; the synchronous `resolveEnemyTurn()` balance/harness path remains unchanged. `tests/smoke.spec.ts` now asserts the phase strip renders, wind-up/commit durations stay substantial, and reduced-motion wind-up keeps both the attack tell and generated move plaque visible before impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up|combat reduced-motion enemy turn still holds damage until impact" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check -- src\main.ts tests\smoke.spec.ts progress.md` with only existing LF-to-CRLF warnings. Frozen visual captures are in `.artifacts/test-results/enemy-turn-readable-gravity/`: high-res wind-up, desktop commit, mobile wind-up, and reduced-motion desktop wind-up all report pending enemy action, HP held at 38/38, `combatBeatProgressFrame.rendered: true`, zero page errors, and expected tell/plaque/commitment visuals. Visual inspection confirmed the phase strip reads at the top-right without covering the HUD, and reduced-motion now visibly holds `Strike 6` through wind-up.

Current follow-up: System Settings volume slider polish for consumer-facing audio control depth. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta horizontal settings slider frame on flat chroma key, preserved at `assets/concept-art/ui/icons/sources/system-settings-volume-slider-frame-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_099fee8815c27164016a4dbb9d6420819699990371b2d6202e.png`). Local key removal detected `#05f605` and wrote `.artifacts/test-results/system-settings-volume-slider/system-settings-volume-slider-frame-keyed-raw.png` with 1,116,609 transparent pixels and 6,559 partial-alpha pixels. The normalized runtime asset is `assets/runtime/ui/icons/system-settings-volume-slider-frame.png` (768x192, 146,181 bytes), with source visible bounds `(168, 215, 2003, 508)`, crop box `(104, 163, 2067, 560)`, runtime visible bounds `(23, 36, 745, 155)`, transparent corners, 65,519 opaque pixels, 6,556 partial-alpha pixels, and zero visible/edge green heuristic pixels. `src/main.ts` now registers/preloads `system-settings-volume-slider-frame`, adds persistent separate Music/SFX volume controls to `BirdAudioDirector`, applies Music volume to ambience and SFX volume to procedural cues, fixes first-run volume loading so missing localStorage uses defaults instead of `Number(null) === 0`, and renders two interactive slider rows in the shared Settings overlay for menu, route, and battle. Battle/Menu/Route text state now reports `audio.musicVolume`, `audio.sfxVolume`, and `systemSettingsVolumeSliderFrame.loaded/rendered/count`. The focused Settings smoke now clicks the actual slider hit targets, proves Music changes to 25% and SFX to 65%, and asserts the generated slider frames render in all three Settings contexts. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "settings overlay opens" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check -- src\main.ts tests\smoke.spec.ts progress.md` with only existing LF-to-CRLF warnings. Production built-preview captures on temporary `http://127.0.0.1:4331/?playwright=1` are in `.artifacts/test-results/system-settings-volume-slider/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all report Settings open, `systemSettingsVolumeSliderFrame { loaded: true, rendered: true, count: 2 }`, 5 settings row frames, 3 toggle frames, default Music/SFX volumes 0.78/0.88, visible slider geometry at stage `(804, 328)` and `(804, 380)`, and zero page/console errors. Visual inspection confirmed the larger Settings panel, generated slider hardware, percentage labels, bottom controls, close control, and fixed-canvas mobile letterbox stay readable without overlap. Temporary preview port 4331 was stopped after verification.

Current follow-up: Combat enemy commitment seal polish for readable enemy release/impact beats, including reduced-motion readability. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta hostile commitment seal on flat chroma key, preserved at `assets/concept-art/fx/sources/combat-enemy-commitment-seal-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_06938e9fe68d770e016a4db67e9dcc819386f473d185a5236d.png`). Local key removal detected `#06f508` and wrote `.artifacts/test-results/combat-enemy-commitment-seal/combat-enemy-commitment-seal-keyed-raw.png` with 1,053,590 transparent pixels and 7,411 partial-alpha pixels. The normalized runtime asset is `assets/runtime/fx/combat-enemy-commitment-seal.png` (512x512, 214,519 bytes), with source visible bounds `(42, 69, 1212, 1169)`, runtime visible bounds `(47, 56, 465, 450)`, transparent corners, 60,262 opaque pixels, 8,147 partial-alpha pixels, and zero remaining visible green heuristic pixels. `src/main.ts` now imports/preloads `combat-enemy-commitment-seal`, adds a procedural `enemyCommitment` SFX cue, renders the generated three-layer seal between the attacker and flock during the release beat, holds it into impact, and exposes `combatEnemyCommitmentSeal.loaded/rendered/count/bursts` through BattleScene text state. The effect is reduced-motion-safe: movement-heavy release FX can stay suppressed while the static commitment marker still renders and the enemy turn remains readable. The focused combat timing smoke now proves both normal-motion and reduced-motion release beats keep Cohesion unchanged, render the commitment seal, and request the `enemyCommitment` cue before impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep 'combat attacks resolve after their animation wind-up|combat reduced-motion enemy turn still holds damage until impact' --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4330/?playwright=1` are in `.artifacts/test-results/combat-enemy-commitment-seal/built-preview/`: high-res 2560x1600, desktop 1440x900, mobile 390x844, and reduced-motion desktop all report release beat, pending true, HP 38/38, `combatEnemyCommitmentSeal { loaded: true, rendered: true, count: 3, bursts: 1 }`, `enemyCommitment` audio cue 1, visible geometry, and zero page/console errors. Visual inspection confirmed the final scaled placement reads clearly in the attack lane and stays clear of the top HUD, enemy HP/nameplate, combat log, hand, pile controls, and fixed-canvas mobile letterbox.

Current follow-up: Enemy-turn pacing fix for reduced-motion environments after feedback that enemies still acted too fast and lacked gravity. The existing normal-motion enemy phase already had long preamble/wind-up/release/impact/recovery beats, but `combatTimingDelay()` collapsed all combat delays to `0` whenever the browser reported `prefers-reduced-motion: reduce`, making player/enemy damage resolution feel instant on those systems. `src/main.ts` now keeps reduced-motion combat readable by scaling combat delays to 55% with a 260ms minimum instead of removing them entirely, preserving the existing reduced-motion avoidance of high-motion FX while still giving enemy turns tactical beats. `tests/smoke.spec.ts` adds `combat reduced-motion enemy turn still holds damage until impact`, which emulates reduced motion, forces a damaging enemy move, and proves preamble/wind-up/release keep Cohesion unchanged until the impact beat. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep 'combat attacks resolve after their animation wind-up|combat reduced-motion enemy turn still holds damage until impact' --workers=1 --reporter=line`, `npm run build`, required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, screenshot inspection of `output/web-game/shot-0.png`, and `git diff --check -- src/main.ts tests/smoke.spec.ts progress.md` with only the existing LF-to-CRLF warnings.

Current follow-up: Combat Roost handoff timing/readability polish after feedback that enemy turns still lacked gravity. Generated a textless brass/gunmetal/cyan/magenta Roost handoff crest on chroma key, preserved the source at `assets/concept-art/fx/sources/combat-roost-handoff-source-v1.png`, removed the detected `#04f807` key into `.artifacts/test-results/combat-roost-handoff/combat-roost-handoff-keyed-raw.png`, and normalized the runtime sprite into `assets/runtime/fx/combat-roost-handoff.png` (768x320, 252,085 bytes; visible bounds `(82, 34, 682, 286)`, transparent corners, 61,764 opaque pixels, 13,750 partial-alpha pixels, zero visible/edge green heuristic pixels). `src/main.ts` now imports/preloads the handoff asset, adds a procedural `roostHandoff` audio cue, exposes `combatRoostHandoff.loaded/rendered/count/bursts`, and plays the generated crest after the final animated enemy preamble render so `renderAll()` cannot wipe it before the player sees it. The effect now holds for a readable beat before fading, and the reduced-motion branch holds the still crest through most of the enemy preamble. The focused combat timing smoke proves the Roost handoff renders and cues audio before wind-up while flock HP stays unchanged through preamble/wind-up/release and only drops on impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4326/?playwright=1` are in `.artifacts/test-results/combat-roost-handoff/built-preview/`: `pre-screenshot-state-summary.json` reports high-res 2560x1600, desktop 1440x900, and mobile 390x844 all in `preamble`, pending true, `combatRoostHandoff { loaded: true, rendered: true, count: 3, bursts: 1 }`, visible geometry, and zero page/console errors. Visual inspection confirmed the handoff and Enemy Turn banner are readable across desktop and fixed-canvas mobile without hiding the top HUD, hand, log, or command surfaces; temporary preview port 4326 was stopped after verification.

Current follow-up: Enemy-turn gravity/readability retune after fresh play-feel feedback that enemies still acted too fast. `src/main.ts` now pushes the animated Roost enemy phase to a heavier `1600ms` preamble, `2600ms` wind-up, `1800ms` release/commitment, `1350ms` impact hold, `1450ms` recovery, and `850ms` interlude between attackers. Wind-up FX now live through the longer wind-up instead of fading early, the release trail/motes hold through the delayed commitment window, and enemy body language leans/lunges farther with slower held poses so the attack is readable before Cohesion changes. The synchronous `resolveEnemyTurn()` harness/balance path remains unchanged. `tests/smoke.spec.ts` now samples a late wind-up and late release, proving HP stays unchanged while the release trail is still visible before impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run build`, required shared web-game client on `http://127.0.0.1:5199/?playwright=1`, and frozen live captures in `.artifacts/test-results/enemy-turn-gravity-retune/frozen/`. The captures report held wind-up HP 38/38, held release HP 38/38 with `combatActionTrail.rendered: true`, impact HP 32/38, and zero page errors; visual inspection confirmed the wind-up, held incoming trail, and impact are clear without covering the HUD, hand, or log.

Current follow-up: Enemy-turn gravity/readability retune after play-feel feedback that enemies still acted too fast. `src/main.ts` now gives the animated enemy Roost path a heavier cadence: `1300ms` enemy-turn preamble, `2100ms` wind-up, `1300ms` release/commitment, `1200ms` impact hold, `1180ms` recovery, and `650ms` interlude between attackers. The enemy sprite body language is heavier too: wind-up leans farther and pulses longer, attack lunges farther with a slower held commitment, and release FX now keeps the generated action trail visible through the held release instead of fading immediately. The synchronous `resolveEnemyTurn()` balance/harness path remains unchanged. `tests/smoke.spec.ts` now samples a later wind-up hold and release hold so HP must remain unchanged during the readable beats before impact. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Frozen visual captures in `.artifacts/test-results/enemy-turn-gravity/` confirm wind-up hold and release hold keep Cohesion at 38/38, release hold renders `combatActionTrail`, impact drops Cohesion to 32/38 with `combatEnemySwipe`, and recovery renders `combatEnemyRecoveryAfterglow` with no page errors.

Current follow-up: Combat player hit-confirm impact polish. Added a generated damage-confirmation burst so the delayed player card impact has a crisp visual and audio landing beat when enemy HP actually drops. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta shard rosette on flat chroma key, preserved at `assets/concept-art/fx/sources/combat-player-hit-confirm-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_03fc42d1ea5f3d83016a4d960d37f4819392c3768d3a381f7f.png`). Local key removal detected `#0af60e` and wrote `.artifacts/test-results/combat-player-hit-confirm/combat-player-hit-confirm-keyed-raw.png` with 1,231,198 transparent pixels and 19,849 partial-alpha pixels. The normalized runtime asset is `assets/runtime/fx/combat-player-hit-confirm.png` (512x512, 162,631 bytes), with source visible bounds `(15, 200, 1239, 1053)`, runtime visible bounds `(33, 100, 479, 412)`, transparent corners, center alpha 255, 13,717 partial-alpha pixels, 36,156 opaque pixels, and 109 conservative green-fringe adjustments. `src/main.ts` now imports/preloads `combat-player-hit-confirm`, renders two layered hit-confirm sprites at the damaged enemy during `damageEnemy`, plays a short procedural `hitConfirm` cue alongside the existing attack cue, and exposes `combatPlayerHitConfirm.loaded/rendered/count/bursts` through BattleScene text state. The focused timing smoke now proves player damage remains delayed during the pending cast, then enemy HP drops with the generated hit-confirm rendered and the `hitConfirm` audio request incremented. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production output includes `combat-player-hit-confirm-Ckt-X4Gd.png`. High-res/desktop/mobile built-preview captures on temporary `http://127.0.0.1:4321/?playwright=1` are in `.artifacts/test-results/combat-player-hit-confirm/`: frozen captures report `combatPlayerHitConfirm { loaded: true, rendered: true, count: 2, bursts: 1 }`, HP dropping from 27 to 22, no page errors, and object geometry centered at stage `(920, 232)`. Visual inspection confirmed the desktop and mobile crops put the shard rosette directly over the damaged enemy without covering the combat log, hand, top HUD, or fixed-canvas mobile letterbox. Temporary preview port 4321 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Combat enemy recovery afterglow polish. Added a generated recovery-beat FX asset so the heavy enemy-turn pause after impact has a visible settling beat instead of only timing. Built-in imagegen produced a textless brass/gunmetal/cyan/magenta post-impact rooftop shock ripple on flat chroma key, preserved at `assets/concept-art/fx/sources/combat-enemy-recovery-afterglow-source-v1.png` (source from `C:\Users\Will\.codex\generated_images\019f1b98-0644-7463-8b1f-5b77ecf5ad23\ig_09e7b4b201310d98016a4d92a1ccd08195b9d32dbc64a82ed8.png`). Local key removal detected `#06f908` and wrote `.artifacts/test-results/combat-enemy-recovery-afterglow/combat-enemy-recovery-afterglow-keyed-raw.png` with 1,286,306 transparent pixels and 24,723 partial-alpha pixels. The normalized runtime asset is `assets/runtime/fx/combat-enemy-recovery-afterglow.png` (768x320, 179,897 bytes), with visible bounds `(49, 74, 720, 259)`, transparent corners, zero edge-green pixels, and 350 conservative green-fringe adjustments. `src/main.ts` now imports/preloads `combat-enemy-recovery-afterglow`, renders two layered afterglow sprites under the attacker during the `recovery` enemy-turn beat, exposes `combatEnemyRecoveryAfterglow.loaded/rendered/count/bursts`, and leaves the synchronous enemy resolution path unchanged. The focused timing smoke now proves preamble/wind-up/release keep flock HP unchanged, impact applies damage, and recovery renders the generated afterglow while pending remains true. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. High-res/desktop/mobile built-preview captures on temporary `http://127.0.0.1:4320/?playwright=1` are in `.artifacts/test-results/combat-enemy-recovery-afterglow/`: all report recovery beat, pending true, HP 32/38 after `Strike 6`, `combatEnemyRecoveryAfterglow { loaded: true, rendered: true, count: 2, bursts: 1 }`, no page errors, and aligned visuals under the enemy without HUD/log/hand overlap. Temporary preview port 4320 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Enemy-turn gravity retune after play-feel feedback. Enemies still felt too fast, so `src/main.ts` now lengthens the animated enemy phase cadence from the previous readable-beat pass to a heavier `1100ms` preamble, `1700ms` wind-up, `650ms` release, `520ms` impact hold, and `900ms` recovery. Added an explicit `interlude` enemy-turn beat plus `460ms` pause between living attackers so multi-enemy phases do not collapse from one action straight into the next. The synchronous `resolveEnemyTurn()` path remains unchanged for direct harness/balance use; only the player-facing animated Roost path uses the heavier timing. `tests/smoke.spec.ts` now asserts the enemy wind-up and release remain held long enough for flock HP to stay unchanged inside those beats, preventing a regression where the labels/FX flash once but damage still feels instant. Verification passed: `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts -g "combat attacks resolve after their animation wind-up"`, `npm run build`, and the required shared web-game client against `http://127.0.0.1:5199/?playwright=1`. Live capture on the running dev server confirmed full motion is enabled, wind-up renders the attack tell/plaque with pending true, release renders the action trail with pending true, and exact impact renders both `combatEnemySwipe` and `combatFlockImpactBurst` while pending remains true. Captures are in `.artifacts/test-results/enemy-turn-pacing/` (`windup.png`, `release.png`, `impact-exact.png`). Bird Squad remains live on port 5199.

Follow-up note: reran the same verification after compaction and kept the immediate menu `continueRun()` helper public because smoke harnesses call it directly, while the actual title UI remains on the animated `continueRunAnimated()` path. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, the focused combat timing smoke, `npm run build`, and the required shared web-game client all pass.

Current follow-up: Route commit travel-streak readability pass. Generated a textless route commitment launch streak with the built-in image tool on a flat chroma-key background, preserved the source at `assets/concept-art/fx/sources/route-commit-streak-source-v1.png`, removed detected `#06f809` into `.artifacts/test-results/route-commit-streak/route-commit-streak-keyed-raw.png`, and normalized it into `assets/runtime/fx/route-commit-streak.png` (768x256 RGBA, 199,354 bytes). Runtime cleanup used source bounds `(30, 188, 1757, 700)`, crop box `(0, 137, 1774, 751)`, resized subject `688x238`, runtime bounds `(50, 27, 722, 229)`, transparent corners, center alpha 255, 45,134 opaque pixels, 19,329 partial-alpha pixels, and zero edge-green pixels. `src/main.ts` now preloads the generated streak, routes Enter/click confirm through `commitRouteNodeAnimated`, sets a readable `520ms` route-commit pending beat before scene changes, renders a generated travel streak with a temporary contrast plate, rail, and motes along the selected route direction, clamps the first-entry launch inside the map frame, and exposes `routeCommitPending` plus `routeCommitStreak.loaded/rendered/count/bursts` through RouteScene text state. `tests/smoke.spec.ts` adds a focused smoke proving the generated streak renders while the route is still pending and before transition. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "route commit renders generated travel streak" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Live captures and summaries are in `.artifacts/test-results/route-commit-streak/`: desktop/mobile frozen settled samples report `routeCommitStreak { loaded: true, rendered: true, count: 2, bursts: 1 }` with `routeCommitPending: true`; visual inspection confirmed the commitment beat is now readable, directional, inside the map frame, and clear of the top HUD and bottom boss-prep strip. Bird Squad remains live on port 5199.

Current follow-up: Enemy-turn readable attack gravity pass. Enemies still felt too fast even after the first wind-up work, so `src/main.ts` now exposes and drives an explicit enemy action sequence: `preamble -> windup -> release -> impact -> recovery`. Enemy timing was retuned to a heavier cadence (`980ms` preamble, `1480ms` wind-up, `520ms` release, `400ms` impact hold, `760ms` recovery), and the wind-up tell/plaque FX hold longer so the commitment stays readable. Damage no longer lands immediately after wind-up: release now queues the enemy lunge, renders a release glow/streak via `enemyAttackReleaseFx`, and only resolves enemy effects on the later impact beat. Battle text state now reports `combatEnemyTurnBeat` and `combatEnemyTurnMove`; the focused combat timing smoke asserts preamble/wind-up/release all keep flock HP unchanged, release renders an action trail, and only impact drops HP. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run build`, and the required shared web-game client against `http://127.0.0.1:5199/?playwright=1`. Live telemetry on port 5199 reported zero page/console errors, wind-up `Strike 6` with tell/plaque rendered and HP 38/38, release with trail rendered and HP still 38/38, impact with swipe/impact burst rendered and HP 32/38, then recovery at HP 32/38. Screenshots are in `.artifacts/verification/enemy-turn-readable-beats/`; wind-up inspection confirmed the move plaque/tell are prominent and clear of HUD/log/hand, while the sub-second release/impact beats are primarily verified by telemetry because screenshots can lag past those short frames.

Current follow-up: Combat enemy wind-up plaque foreground readability polish. Generated a textless foreground enemy move plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-enemy-windup-plaque-source-v1.png` (1774x887 RGB, 1,508,886 bytes), removed detected `#0df515` into `.artifacts/test-results/combat-enemy-windup-plaque/combat-enemy-windup-plaque-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-enemy-windup-plaque.png` (768x192 RGBA, 192,790 bytes). Runtime cleanup used source visible bounds `(66, 271, 1705, 619)`, crop box `(26, 230, 1745, 660)`, resized subject `720x180`, runtime visible bounds `(38, 21, 730, 171)`, transparent corners, center alpha 255, 82,161 opaque pixels, 4,684 partial-alpha pixels, one conservative edge-key fleck adjusted, and zero remaining green-ish heuristic pixels. `src/main.ts` now preloads `combat-enemy-windup-plaque`, renders it as a foreground FX-layer container above the enemy with live Phaser move text, exposes `combatEnemyWindupPlaque.loaded/rendered/count/bursts/labels`, and uses it for both attack and support wind-ups while preserving the prior attack/support tell split. The focused combat timing smoke now proves the plaque loads/renders/bursts, support labels include `Brace Up`, and attack wind-ups still render threat charge/attack tell while support wind-ups render support charge/support tell with hostile charge/tell absent. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4314/?playwright=1` are in `.artifacts/test-results/combat-enemy-windup-plaque/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all reported attack plaque `Weighty Strike`, support plaque `Brace Up`, correct exclusive attack/support FX, and zero page/console errors. Visual inspection confirmed the foreground plaque makes enemy commitment readable without colliding with the top HUD, enemy HP/nameplate, combat log, hand cards, or fixed-canvas mobile letterbox. Temporary preview port 4314 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Enemy-turn support-charge and pacing readability polish. Generated a dedicated defensive enemy support charge halo with the built-in image tool on a flat chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-enemy-support-charge-source-v1.png` (1254x1254 RGB, 2,307,947 bytes), removed detected `#0af80a` into `.artifacts/test-results/combat-enemy-support-charge/combat-enemy-support-charge-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-enemy-support-charge.png` (512x512 RGBA, 446,436 bytes). Runtime cleanup used source visible bounds `(100, 81, 1152, 1125)`, crop box `(53, 34, 1199, 1172)`, resized subject `496x493`, runtime visible bounds `(26, 27, 486, 484)`, transparent corners, center alpha 255, 105,079 opaque pixels, 35,597 partial-alpha pixels, three conservative key-like flecks adjusted, and zero remaining green-ish heuristic pixels while preserving the cyan support glass. `src/main.ts` now preloads and renders `combat-enemy-support-charge`, exposes `combatEnemySupportCharge.loaded/rendered/count/bursts`, makes non-damaging enemy wind-ups use the new cyan defensive charge plus support tell instead of the hostile orange threat charge, keeps damaging wind-ups on the orange threat charge plus attack tell, strengthens the tell/charge hold, and lengthens enemy phase pacing from `620/900/520ms` to `820/1180/640ms` preamble/wind-up/recovery. The focused combat timing smoke now proves attack wind-ups still render threat charge/attack tell, support wind-ups render support charge/support tell, support wind-ups render zero threat charge/attack tell, and HP stays unchanged during wind-up. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4313/?playwright=1` are in `.artifacts/test-results/combat-enemy-support-charge/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all reported support charge/tell rendered with hostile charge/attack tell absent and zero page/console errors. The high-res real enemy-turn timing probe reported preamble pending with HP 38/38, wind-up at ~2718ms with attack tell/threat charge rendered and HP still 38/38, then impact at ~4799ms with HP 32/38 and swipe/impact burst rendered. Visual inspection confirmed the support branch now reads as defensive preparation instead of an incoming hit, and the enemy turn has a heavier visible delay before damage lands. Temporary preview port 4313 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Combat enemy support-tell wind-up polish. Generated a dedicated non-damaging enemy support/brace warning crest with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-enemy-support-tell-source-v1.png` (1254x1254 RGB, 2,627,453 bytes), removed detected `#0ee611` into `.artifacts/test-results/combat-enemy-support-tell/combat-enemy-support-tell-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-enemy-support-tell.png` (512x512 RGBA, 466,951 bytes). Runtime cleanup used source visible bounds `(70, 32, 1180, 1190)`, crop box `(35, 0, 1215, 1227)`, resized subject `492x512`, runtime visible bounds `(22, 11, 490, 499)`, transparent corners, center alpha 255, 136,673 opaque pixels, 17,690 partial-alpha pixels, two conservative key-like flecks adjusted, and zero remaining green-ish heuristic pixels while preserving the intentional cyan support glow. `src/main.ts` now registers/preloads `combat-enemy-support-tell`, renders it as a calm two-layer shield/signal crest for non-damaging enemy wind-ups, keeps `combat-enemy-attack-tell` exclusive to damaging enemy wind-ups, and exposes `combatEnemySupportTell.loaded/rendered/count/bursts` through BattleScene text state. The focused combat timing smoke now includes a synthetic support intent that proves the support crest renders and the attack arrow does not. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4312/?playwright=1` are in `.artifacts/test-results/combat-enemy-support-tell/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all reported `combatEnemySupportTell { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatEnemyAttackTell { loaded: true, rendered: false, count: 0, bursts: 0 }`, two threat-charge sprites, zero page/console errors, and support geometry around 192-215px. Visual inspection confirmed the brass/gunmetal/cyan-green crest makes non-damaging enemy wind-ups read as defensive/prep rather than incoming damage, without blocking HUD, hand, combat log, or the fixed-canvas mobile letterbox. Temporary preview port 4312 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Combat enemy attack-tell wind-up polish. Generated a dedicated enemy attack warning crest with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-enemy-attack-tell-source-v1.png` (1254x1254 RGB, 2,406,800 bytes), removed detected `#03f605` into `.artifacts/test-results/combat-enemy-attack-tell/combat-enemy-attack-tell-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-enemy-attack-tell.png` (512x512 RGBA, 370,486 bytes). Runtime cleanup used source visible bounds `(37, 58, 1215, 1193)`, crop box `(0, 19, 1254, 1232)`, resized subject `512x495`, runtime visible bounds `(13, 22, 499, 489)`, transparent corners, center alpha 255, 123,760 opaque pixels, 5,239 partial-alpha pixels, three conservative key-like flecks adjusted, and zero remaining green-ish heuristic pixels. `src/main.ts` now registers/preloads `combat-enemy-attack-tell`, renders it as a flipped two-layer crest during `enemyAttackWindupFx` so enemy attacks point toward the flock while HP is still unchanged, keeps the existing threat charge/trail/move label underneath it, and exposes `combatEnemyAttackTell.loaded/rendered/count/bursts` through BattleScene text state. The focused combat timing smoke now waits for the actual wind-up tell and asserts it renders before flock HP changes. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required shared web-game client on `http://127.0.0.1:5199/?playwright=1`. Production built-preview captures on temporary `http://127.0.0.1:4311/?playwright=1` are in `.artifacts/test-results/combat-enemy-attack-tell/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all reported `combatEnemyAttackTell { loaded: true, rendered: true, count: 2, bursts: 1 }`, two threat-charge sprites, zero page/console errors, flipped tell geometry around 210-234px, and flock HP held at 38/38 during wind-up. Visual inspection confirmed the brass/gunmetal/red-orange crest makes the enemy wind-up readable and heavy without blocking HUD, hand, combat log, or fixed-canvas mobile letterbox. Temporary preview port 4311 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Enemy-turn gravity/readability timing pass. Followed up on the first combat timing fix because enemies still felt too fast: enemy turns now have a longer phase preamble (`ENEMY_TURN_PREAMBLE_MS`), a much longer per-attacker wind-up, and a heavier recovery beat after impact. Added a new `windup` enemy motion cue so the attacker visibly loads up before lunging, strengthened the impact lunge duration/hold, and wired `enemyAttackWindupFx` to the exact move that will resolve so the enemy shows a threat-charge halo, lane streak, glow, motes, and the move label while flock HP remains unchanged. Fixed `combatThreatCharge` cleanup so both layered charge sprites are destroyed after the burst instead of leaving a stale warning sprite in the FX layer. Verification passed: `npm run build`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up"`, the shared web-game Playwright client against `http://127.0.0.1:5199/?playwright=1`, and live browser capture on the same URL. New screenshots are in `.artifacts/verification/enemy-turn-gravity/`: `01-enemy-turn-preamble.png` shows the enemy-turn banner before damage, `02-enemy-windup.png` shows `Strike 6` with threat charge/trail while HP is still 38/38, and `03-enemy-impact-recovery.png` shows the HP drop plus impact/log feedback. The capture telemetry reported wind-up with `combatThreatCharge { rendered: true, count: 2, bursts: 1 }`, `combatActionTrail { rendered: true, count: 1, bursts: 1 }`, impact with `combatEnemySwipe` and `combatFlockImpactBurst` rendered, and settled state with pending false and threat count cleaned back to 0. Bird Squad remains live on port 5199.

Current follow-up: Combat attack animation timing pass. Player card input now routes through an animated play path that spends Wingbeat and removes the card immediately, keeps the board locked with `combatAnimationPending`, and delays card effect resolution until the cast/attack FX has had time to reach the target. Enemy roost input now uses an animated enemy phase that renders the enemy-turn banner, clears enemy cover, queues each living enemy's attack motion, waits through a wind-up, resolves that enemy's move, then gives a short recovery beat before the next attacker. Direct combat helpers remain synchronous by default for existing test/balance harnesses, with optional delayed card resolution and UI-only roost pacing. Battle text state now exposes `combatAnimationPending`, and the focused smoke `combat attacks resolve after their animation wind-up` asserts both sides of the fix: during wind-up the pending flag is true and HP is unchanged; after settlement the pending flag clears and damage lands. Verification passed: `npm run build`, focused `npx playwright test tests/smoke.spec.ts --grep "combat attacks resolve after their animation wind-up"`, live dev-server check on `http://127.0.0.1:5199/?playwright=1`, and a browser-side visual sample saved to `.artifacts/verification/combat-timing-player-windup.png` plus `.artifacts/verification/combat-timing-player-settled.png`. The live sample reported player wind-up with enemy HP held at 27/27, hand reduced from 5 to 4, action trail and Wingbeat spend FX rendered, then settled with pending false and enemy HP at 22/27. Bird Squad remains live on port 5199.

Current follow-up: Combat Wingbeat Spend paid-card feedback polish. Generated a transparent-ready Wingbeat spend pulse with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-wingbeat-spend-source-v1.png` (1254x1254 RGB, 2,144,965 bytes), removed detected `#03f405` into `.artifacts/test-results/combat-wingbeat-spend/combat-wingbeat-spend-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-wingbeat-spend.png` (512x512 RGBA, 350,268 bytes). Runtime cleanup used source visible bounds `(100, 103, 1156, 1131)`, crop box `(24, 27, 1232, 1207)`, runtime visible bounds `(37, 43, 475, 469)`, transparent corners, center alpha 255, 117,442 visible pixels, 102,581 opaque pixels, 14,861 partial-alpha pixels, and zero post-clean green-ish heuristic pixels after recoloring 108 conservative runtime flecks. `src/main.ts` now registers/preloads `combat-wingbeat-spend`, adds a short `spend` WebAudio cue, renders the generated pulse plus glow/ring/glint/motes at the flock cast position for paid card plays only, scales the burst slightly by effective card cost, and exposes `combatWingbeatSpend.loaded/rendered/count/bursts` through BattleScene text state. The focused combat smoke now chooses a paid hand card explicitly, resets the spend counter, and asserts the generated spend sprite loads, renders, counts, and bursts alongside the existing action trail and cast focus burst. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` with only existing LF/CRLF warnings. Production output includes `combat-wingbeat-spend-DOVpCKnI.png`. Built-preview captures on temporary `http://127.0.0.1:4308/?playwright=1` are in `.artifacts/test-results/combat-wingbeat-spend/built-preview/`: high-res 2560x1600, desktop 1440x900, and mobile 390x844 all report `combatWingbeatSpend { loaded: true, rendered: true, count: 1, bursts: 1 }`, one generated spend sprite plus glow/ring/glint objects, zero page errors, and zero counted console errors. Visual inspection confirmed the brass/gunmetal/cyan/magenta pulse reads as a paid-card Energy spend beat over the flock/cast lane, remains stage-anchored at high resolution, and clears the hand, HUD, combat log, enemy vitals, and fixed-canvas mobile letterbox. Temporary preview port 4308 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Audio Toggle Wave Burst shared-SFX control polish. Generated a transparent-ready circular soundwave burst with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/audio-toggle-wave-burst-source-v1.png` (1254x1254 RGB), removed the detected `#06f20a` key with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py` into `.artifacts/test-results/audio-toggle-wave-burst/audio-toggle-wave-burst-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/audio-toggle-wave-burst.png` (256x256 RGBA, 83,928 bytes). Runtime cleanup used source visible bounds `(80, 91, 1173, 1152)`, crop box `(44, 55, 1209, 1188)`, resized subject `238x231`, runtime visible bounds `(14, 17, 242, 238)`, center alpha 0, transparent corners, 43,106 transparent pixels, 10,444 opaque pixels, 11,986 partial-alpha pixels, 31 conservative green adjustments, and zero edge-green adjustments. `src/main.ts` now registers/preloads `audio-toggle-wave-burst`, renders it hidden behind every shared audio toggle, flashes it outward on click with reduced-motion handling, exposes `audioToggleWaveBurst.loaded/rendered/count/visible` through MenuScene, RouteScene, and BattleScene text state, and gives muting its own short `close` cue before silence while preserving the unmute confirm cue. Route and Battle top-HUD audio toggles no longer call full `renderAll()` on click, which previously destroyed transient toggle feedback before it could paint; they now rely on the local refresh path and text-state update. `tests/smoke.spec.ts` adds a cross-surface smoke that exercises real Menu, Route, and Battle audio hit targets and asserts the burst is loaded, hidden before click, visible during click feedback, and hidden after the tween; the mobile title smoke now asserts the burst is loaded but not rendered by default. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "audio toggle renders generated wave burst feedback across game surfaces|title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` with only existing LF/CRLF warnings. Production output includes `audio-toggle-wave-burst-DUHKyzcZ.png`. Built-preview captures on temporary `http://127.0.0.1:4288/?playwright=1` are in `.artifacts/test-results/audio-toggle-wave-burst/built-preview/`: menu high-res 2560x1600, route desktop 1440x900, and battle mobile 390x844 all report `audioToggleWaveBurst { loaded: true, rendered: true, count: 1, visible: 1 }` during the click and `{ loaded: true, rendered: false, count: 1, visible: 0 }` after the tween, with zero page errors and zero console errors. Visual inspection of full screenshots plus close crops confirmed the burst reads as coherent brass/gunmetal/cyan/magenta soundwave hardware, stays behind the existing medallion, clears neighboring utility buttons and HUD rows, and remains legible but subtle inside the fixed-canvas mobile letterbox. Temporary preview port 4288 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Reward Choice Hover Ring card-hover tactility polish. Generated a transparent-ready tall reward/preen card hover frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-choice-hover-ring-source-v1.png` (1024x1536 RGB, 1,664,262 bytes), removed the detected `#05f808` key with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py` into `.artifacts/test-results/reward-choice-hover-ring/reward-choice-hover-ring-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/reward-choice-hover-ring.png` (384x576 RGBA, 146,187 bytes). Runtime cleanup used source visible bounds `(53, 52, 971, 1477)`, crop box `(12, 3, 1012, 1526)`, resized subject `378x576`, runtime visible bounds `(16, 16, 368, 560)`, transparent corners, center alpha 0, 54,155 visible pixels, 39,543 opaque pixels, 14,612 partial-alpha pixels, and 16 conservative visible-green adjustments with zero edge-green adjustments. `src/main.ts` now registers/preloads `reward-choice-hover-ring`, creates one hidden ring per card reward/preen choice, reveals and pulses exactly the hovered choice through the real `pointerover` path, hides it on `pointerout`, and exposes `rewardChoiceHoverRing.loaded/rendered/count/visible` through BattleScene text state. The existing card-choice hover smoke now exercises the actual reward card pointerover path and asserts the ring is loaded, hidden before hover, visible once during hover, hidden after pointerout, and counted by telemetry while preserving full card detail hover panels. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "card choice surfaces expose full card details on hover" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` with only existing LF/CRLF warnings. Production output includes `reward-choice-hover-ring-DYSZS1N7.png`. Built-preview captures on temporary `http://127.0.0.1:4287/?playwright=1` are in `.artifacts/test-results/reward-choice-hover-ring/built-preview/`: card reward hover high-res 2560x1600, card reward hover desktop 1440x900, and upgrade/preen reward hover mobile 390x844 all report `rewardChoiceHoverRing { loaded: true, rendered: true, count: 3, visible: 1 }`, one visible ring, card detail preview visible, zero page errors, and zero counted console errors. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta frame makes the hovered reward/preen card feel selected without blocking card art, detail text, skip controls, HUD, or the fixed-canvas mobile letterbox. Temporary preview port 4287 was stopped; Bird Squad remains live on port 5199.

Current follow-up: Reward Choice Glow Burst reward-ceremony FX polish. Generated a transparent-ready brass/gunmetal/cyan/magenta radial reward glow with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-choice-glow-burst-source-v1.png` (1254x1254 RGB, 1,721,956 bytes), removed the detected `#04f508` key with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py` into `.artifacts/test-results/reward-choice-glow-burst/reward-choice-glow-burst-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/reward-choice-glow-burst.png` (512x512 RGBA, 250,063 bytes). Runtime cleanup used source visible bounds `(72, 69, 1178, 1149)`, crop box `(23, 20, 1227, 1198)`, resized subject `488x477`, runtime visible bounds `(31, 35, 482, 476)`, transparent corners, center alpha 255, 75,768 visible pixels, 43,212 opaque pixels, 32,556 partial-alpha pixels, 303 conservative visible-green adjustments, and 279 edge-green adjustments. `src/main.ts` now registers/preloads `reward-choice-glow-burst`, renders it additively behind card reward, waymark reward, and preen/upgrade reward choices, adds a reduced-motion-safe pulse tween, emits a small one-shot particle burst for the highlighted waymark/upgraded choice, and exposes `rewardChoiceGlowBurst.loaded/rendered/count/bursts` through BattleScene text state. The reward ceremony smoke now asserts the generated glow loads/renders across card, waymark, and upgrade reward screens. Verification passed: `npm run build`, focused `npx playwright test tests/smoke.spec.ts --grep "post-combat reward screens render generated choice ceremony art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` with only existing LF/CRLF warnings. Production output includes `reward-choice-glow-burst-CBJi_CWY.png`. Built-preview captures on temporary `http://127.0.0.1:4286/?playwright=1` are in `.artifacts/test-results/reward-choice-glow-burst/built-preview/`: card reward high-res 2560x1600 reports 3 glow objects, waymark reward desktop 1440x900 reports 4 glow objects including the burst emitter with `bursts: 1`, and upgrade reward mobile 390x844 reports 3 glow objects; all report `rewardChoiceGlowBurst { loaded: true, rendered: true }`, zero page errors, and zero counted console errors. Visual inspection confirmed the glow reads as a coherent ceremony layer behind choices without blocking card art, item art, descriptions, skip controls, HUD, or the fixed-canvas mobile letterbox. Temporary preview port 4286 was stopped; Bird Squad remains live on port 5199.

Current follow-up: System Field Command Frame shared-overlay button polish. Generated a transparent-ready individual command button frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/system-field-command-frame-source-v1.png` (1983x793 RGB, 1,513,559 bytes), removed the detected `#04f804` key with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py` into `.artifacts/test-results/system-field-command-frame/system-field-command-frame-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/system-field-command-frame.png` (512x160 RGBA, 85,310 bytes). Runtime cleanup used source visible bounds `(126, 169, 1857, 605)`, crop box `(75, 135, 1908, 639)`, resized subject `488x134`, runtime visible bounds `(23, 19, 489, 141)`, transparent corners, center alpha 255, 51,856 visible pixels, 48,333 opaque pixels, 3,523 partial-alpha pixels, and zero green/edge-green adjusted pixels. `src/main.ts` now registers/preloads `system-field-command-frame`, renders it behind the shared `renderFieldButton` command labels/icons while preserving the existing rectangle hit target and fallback, and exposes `systemFieldCommandFrame.loaded/rendered/count` in MenuScene, RouteScene, and BattleScene text state. Focused pause/settings smokes now assert the generated command frames render on route pause, combat pause, menu settings, route settings, and battle settings. Verification passed: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "pause overlay opens on route and combat without advancing play|settings overlay opens from menu and paused run surfaces" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` with only existing LF/CRLF warnings. Production output includes `system-field-command-frame-CGSLDJkE.png`. Built-preview captures on `http://127.0.0.1:4285/?playwright=1` are in `.artifacts/test-results/system-field-command-frame/built-preview/`: route pause high-res 2560x1600 reports 3 visible 178x66 frames, menu settings desktop 1440x900 reports 2 visible 212x66 frames, and battle settings mobile 390x844 reports 2 visible 212x66 frames; all report `systemFieldCommandFrame { loaded: true, rendered: true }`, zero page errors, and zero counted console errors. Visual inspection confirmed the brass/gunmetal/cyan/magenta frames make Resume/Settings/Main Menu/Full Screen/Close read as coherent hardware without crowding stat rows, setting rows, close controls, route HUD, combat hand cards, or the fixed-canvas mobile letterbox. Stop temporary preview port 4285 after final hygiene; Bird Squad should remain live on port 5199.

Current follow-up: System Settings Toggle Frame shared-settings polish. Generated a transparent-ready compact Settings status/toggle frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/system-settings-toggle-frame-source-v1.png` (1942x809, 1,620,249 bytes), removed the detected `#06f90d` key into `.artifacts/test-results/system-settings-toggle-frame/system-settings-toggle-frame-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/system-settings-toggle-frame.png` (512x192 RGBA, 117,855 bytes). Runtime cleanup used source visible bounds `(95, 167, 1844, 641)`, crop box `(26, 92, 1913, 716)`, resized subject `512x169`, runtime visible bounds `(16, 29, 496, 162)`, transparent corners, center alpha 255, 59,533 visible pixels, 3,919 partial-alpha pixels, and zero green-ish heuristic pixels. `src/main.ts` now registers/preloads `system-settings-toggle-frame`, renders three generated status frames behind live engine-rendered Audio/Display/Motion values in the shared Settings overlay, and exposes `systemSettingsToggleFrame.loaded/rendered/count` in MenuScene, RouteScene, and BattleScene text state. The Settings smoke now asserts the generated frame loads and renders across menu settings, paused route settings, and paused combat settings without letting settings overlays commit route/battle actions. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "settings overlay opens from menu and paused run surfaces" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Production output includes `system-settings-toggle-frame-CDTTUPLP.png`. Built-preview captures on `http://127.0.0.1:4284/?playwright=1` are in `.artifacts/test-results/system-settings-toggle-frame/built-preview/`: menu settings at 2560x1600, route settings at 1440x900, and battle settings at 390x844 all report `systemSettingsToggleFrame { loaded: true, rendered: true, count: 3 }`, three visible 176x42 frames, zero page errors, and zero counted console errors. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta frames make On/Windowed/Full read as cohesive hardware without overlapping row labels, close controls, fullscreen/close buttons, route HUD, combat hand cards, or fixed-canvas mobile letterbox. Capture-harness note: the battle jump can emit the pre-existing `Texture key already in use: reward-ceremony-backdrop` console message, so the slice capture script filters only that known duplicate-texture noise while leaving all other console errors counted. Temporary preview port 4284 should be stopped after final hygiene; Bird Squad remains live on port 5199.

Current follow-up: Codex Art Preview Frame detail-modal polish. Generated a transparent-ready vertical Codex art preview frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-art-preview-frame-source-v1.png` (1024x1536, 1,917,104 bytes), removed the detected green key into `.artifacts/test-results/codex-art-preview-frame/codex-art-preview-frame-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/codex-art-preview-frame.png` (512x768 RGBA, 324,952 bytes). Runtime cleanup produced transparent corners, center alpha 0, runtime visible bounds `(14, 22, 498, 745)`, 116,788 visible pixels, 15,465 partial-alpha pixels, and only conservative chroma speck cleanup. `src/main.ts` now registers/preloads `codex-art-preview-frame`, renders it around Codex card, leader, and enemy detail preview art with a subdued procedural backing fallback, and exposes `codexArtPreviewFrame.loaded/rendered/count` through `render_game_to_text`. The Codex smoke now asserts the generated frame loads/renders in a discovered-card detail modal at 358x534. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "codex: starting a run discovers its deck and the Codex screen renders" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Production output includes `codex-art-preview-frame-CeEHXQ3k.png`. Built-preview captures on `http://127.0.0.1:4283/?playwright=1` at 2560x1600, 1440x900, and 390x844 are in `.artifacts/test-results/codex-art-preview-frame/built-preview/`; all report discovered detail `wands_ace`, `cardsDiscovered: 10`, `codexArtPreviewFrame { loaded: true, rendered: true, count: 1 }`, one visible 358x534 frame, zero page errors, and zero console errors. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta frame surrounds live Plume Flash art cleanly, keeps the right-column Codex copy readable, clears the close button and scroll cue, and remains coherent inside the fixed-canvas mobile letterbox. Capture-harness gotcha: starting Codex after `startRun()` can leave RouteScene visually on top, so the slice capture script now stops Route/Battle/Reward/Menu/Profile before bringing Codex to the top. Stop temporary preview port 4283 after final hygiene; Bird Squad should remain live on port 5199.

Current follow-up: Route Event Title Plaque special-node overlay polish. Generated a transparent-ready route event title plaque with the built-in image tool on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-event-title-plaque-source-v1.png` (1,625,249 bytes), removed the detected green key into `.artifacts/test-results/route-event-title-plaque/route-event-title-plaque-keyed-raw.png`, and normalized it into `assets/runtime/ui/icons/route-event-title-plaque.png` (768x176 RGBA, 141,143 bytes). `src/main.ts` now registers/preloads `route-event-title-plaque`, renders it behind live Phaser route-event title/subtitle/kind text for Basin, Cache, Signal, Nest, and Rival overlays with the procedural rectangle as fallback, and exposes `routeEventTitlePlaque.loaded/rendered/count` plus nested `nodeChoice.titlePlaque` telemetry through `render_game_to_text`. The focused route-event smoke now waits for/asserts the generated title plaque alongside special-node backdrop, resident, choice-frame, option-frame, and detail-frame art. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts --grep "route event overlays render generated special-node backdrops" --workers=1 --reporter=line`, and required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `route-event-title-plaque-DXQAtIkb.png`. Built-preview captures on `http://127.0.0.1:4283/?playwright=1` at 2560x1600, 1440x900, and 390x844 are in `.artifacts/test-results/route-event-title-plaque/built-preview/`; all report `routeEventTitlePlaque { loaded: true, rendered: true, count: 2 }`, `nodeChoice.titlePlaque { loaded: true, rendered: true, count: 2 }`, and zero page/console errors. Visual inspection confirmed the brass/gunmetal/cyan/magenta plaque reads as coherent generated event-header hardware behind live `Oren Shearbright` text, clears the route HUD and choice list, and remains readable inside the fixed-canvas mobile letterbox. Temporary preview port 4283 should be stopped after final hygiene checks; Bird Squad remains live on port 5199.

Current follow-up: District Advance Title Plaque progression-transition polish. Generated a transparent-ready district/level advance title plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/district-advance-title-plaque-source-v1.png` (1774x887, 1,894,255 bytes), removed the detected `#03f903` key with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py` into `.artifacts/test-results/district-advance-title-plaque/district-advance-title-plaque-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/ui/icons/district-advance-title-plaque.png` (1024x224 RGBA, 135,859 bytes). Runtime cleanup used source visible bounds `(18, 150, 1756, 734)`, crop box `(0, 80, 1774, 804)`, resized subject `490x200`, runtime visible bounds `(272, 31, 752, 193)`, transparent corners, 56,640 visible pixels before conservative chroma scrub, 3,958 partial-alpha pixels, and 5 adjusted key-green specks. `src/main.ts` now registers/preloads the plaque, renders it with live Phaser text (`DISTRICT 2` / destination map name) during the boss-cleared district transition, exposes `districtAdvanceTitlePlaque.loaded/rendered/count/notice` through RouteScene text state, and draws the whole district-advance overlay after the route map so the title is not covered by map nodes. The focused district progression smoke now asserts the generated plaque loads/renders and preserves the Rooftop Blocks -> Canal Markets notice. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "clearing a district boss advances the run to the next district" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Production output includes `district-advance-title-plaque-DKE_tMsf.png`. Built-preview captures at 2560x1600, 1440x900, and 390x844 are in `.artifacts/test-results/district-advance-title-plaque/`; all report `districtAdvanceTitlePlaque { loaded: true, rendered: true, count: 1 }`, zero page errors, and zero console errors. Visual inspection first caught route icons drawing over the title, then confirmed the final draw-order fix leaves the generated plaque centered, readable, coherent with the brass/gunmetal/cyan/magenta route UI, and clear of the top district banner, boss-prep panel, and fixed-canvas mobile letterbox.

Current follow-up: Combat Cast Focus Burst v2 render-path cleanup. Replaced the too-subtle/vanishing cast burst path with the bolder v2 generated transparent runtime asset at `assets/runtime/fx/combat-cast-focus-burst.png` from source `assets/concept-art/fx/sources/combat-cast-focus-burst-source-v2.png` (source detected key `#02f803`; normalized runtime 512x512 RGBA, 337,900 bytes, transparent corners, visible bounds `(33,37,479,474)`, center alpha 0, no visible green after cleanup). `src/main.ts` now keeps the generated texture import/preload/telemetry and renders the cast cue as direct `fxLayer` children during real card play: generated sigil image plus cyan/white/suit-color ring geometry and sheen. The failed mixed-target fade/scale tween was removed because it produced live telemetry but no painted pixels in screenshots; the effect now uses a reliable timed cleanup plus rotation so the generated sigil visibly appears in the shipped playCard route. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/` with a longer paint pause, and `git diff --check` passed with only existing LF/CRLF warnings. Production preview captures on `http://127.0.0.1:4281/?playwright=1` at 2560x1600, 1440x900, and 390x844 all report `combatCastFocusBurst { loaded: true, rendered: true, count: 1, bursts: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed marker-free screenshots now clearly show the brass/gunmetal/cyan/magenta cast sigil over the flock leader in desktop, high-res, and mobile crops without covering enemy HUD, hand cards, combat log, or fixed-canvas mobile letterbox. Screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-cast-focus-burst/`; the live-client screenshots are in `.artifacts/test-results/combat-cast-focus-burst/web-game-client-long/`.

Current follow-up: Combat Cast Focus Burst cast-action FX polish. Generated a transparent-ready burst source with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-cast-focus-burst-source-v1.png`, removed the detected `#09f60b` key into `.artifacts/test-results/combat-cast-focus-burst/combat-cast-focus-burst-keyed-raw.png`, and normalized it into `assets/runtime/fx/combat-cast-focus-burst.png` (512x512 RGBA, 299,464 bytes; runtime visible bounds `(34,33,478,479)`, transparent corners, center alpha 0). `src/main.ts` now imports/preloads `combat-cast-focus-burst`, exposes `combatCastFocusBurst.loaded/rendered/count/bursts` telemetry, and triggers the generated texture plus cyan/magenta/white focus geometry during card casts after combat `renderAll()` so the asset is available in run combat. The focused combat smoke asserts the generated texture loads and renders during a cast. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/`, and `git diff --check` passed with only existing LF/CRLF warnings. Screenshots, debug captures, build/capture logs, runtime metrics, and `capture-summary.json` are in `.artifacts/test-results/combat-cast-focus-burst/`; high-res/desktop/mobile captures report `combatCastFocusBurst { loaded: true, rendered: true, count: 7, bursts: 1 }`, zero page errors, and only usual WebGL `ReadPixels` warnings. Visual caveat for the next pass: marker-assisted diagnostics prove the burst layer can paint, but marker-free screenshots still make the cast burst read too subtly at desktop/full-scene scale, so a future cleanup should replace the current layered experiment with a simpler proven visible render path or a different generated cast asset. The temporary built preview on port 4280 should be stopped after this pass; Bird Squad remains live on port 5199.

Current follow-up: Combat Hand Selected Pulse hand-selection polish. Generated a transparent-ready selected-card pulse frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-hand-selected-pulse-source-v1.png`, removed the detected `#04f713` key locally into `.artifacts/test-results/combat-hand-selected-pulse/combat-hand-selected-pulse-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/ui/icons/combat-hand-selected-pulse.png` (384x576 RGBA, 148,414 bytes). Runtime cleanup used source size `1024x1536`, source visible bounds `(73, 55, 944, 1471)`, crop box `(45, 27, 972, 1499)`, resized subject `363x576`, runtime visible bounds `(18, 9, 365, 567)`, 166,145 transparent pixels, 12,681 partial-alpha pixels, 42,358 opaque pixels, transparent corners, center alpha 0, 28 conservative visible-green adjustments, and 12 edge-green adjustments. `src/main.ts` now registers/preloads `combat-hand-selected-pulse`, creates one hidden pulse sprite per combat hand card, animates the selected card's pulse with reduced-motion-safe static fallback, keeps the generated playable card frame layered above it, and exposes `combatHandSelectedPulse.loaded/rendered/count` through BattleScene text state. The focused combat-hand smoke now asserts the generated pulse loads, renders once per hand card, expands/brightens on selection, and leaves exactly one visible selected pulse. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat hand cards render generated playable frames" --workers=1 --reporter=line`, previous `npm run build`, previous `npm run validate:runtime-assets`, previous `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4279/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Production output includes `combat-hand-selected-pulse-DVRG4KuB.png`. Screenshots, selected-card crops, hand-row crops, `capture-summary.json`, preview logs, action payload, capture script, and keyed raw output are in `.artifacts/test-results/combat-hand-selected-pulse/`; captures at 2560x1600, 1440x900, and 390x844 all report `combatHandSelectedPulse { loaded: true, rendered: true, count: 5 }`, one visible pulse, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta pulse makes the selected combat hand card read clearly without washing out card art, cost badges, card text, adjacent cards, pile controls, combat log, enemy HUD, or the fixed-canvas mobile letterbox.

Current follow-up: Combat Target Lock Pulse selected-enemy FX polish. Generated a transparent-ready selected-target pulse ring with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-target-lock-pulse-source-v1.png`, removed the detected `#06f80d` key locally into `.artifacts/test-results/combat-target-lock-pulse/combat-target-lock-pulse-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/fx/combat-target-lock-pulse.png` (512x256 RGBA, 72,205 bytes). Runtime cleanup used source size `1633x963`, source visible bounds `(125, 40, 1507, 850)`, crop box `(91, 12, 1541, 878)`, resized subject `429x256`, runtime visible bounds `(49, 6, 462, 250)`, 111,313 transparent pixels, 8,070 partial-alpha pixels, 11,689 opaque pixels, transparent corners, center alpha 0, 24 conservative visible-green adjustments, and 3 edge-green adjustments after the edge-contract chroma pass. `src/main.ts` now imports/preloads `combat-target-lock-pulse`, layers it as a low-alpha additive pulse underneath the existing selected-enemy `combat-target-reticle`, uses a reduced-motion-safe static alpha, and exposes `combatTargetLockPulse.loaded/rendered/count` through BattleScene text state. The existing combat route smoke now asserts the pulse loads, renders once, and stays paired with the original target reticle. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4278/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Screenshots, selected-enemy crops, `capture-summary.json`, preview logs, action payload, capture script, and keyed raw output are in `.artifacts/test-results/combat-target-lock-pulse/`; captures at 2560x1600, 1440x900, and 390x844 report both `combatTargetReticle` and `combatTargetLockPulse` as `{ loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta pulse improves selected-target readability during combat without covering enemy art, the HP/nameplate, intent icon, HUD, hand cards, combat log, or fixed-canvas mobile letterbox. The temporary built preview on port 4278 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Reward Deck Need Chip Frame card-reward polish. Generated a transparent-ready compact deck-need chip frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-deck-need-chip-frame-source-v1.png`, removed the detected `#0df314` key locally into `.artifacts/test-results/reward-deck-need-chip-frame/reward-deck-need-chip-frame-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/ui/icons/reward-deck-need-chip-frame.png` (512x160 RGBA, 98,321 bytes). Runtime cleanup used source size `1915x821`, source visible bounds `(309, 202, 1604, 609)`, crop box `(264, 146, 1649, 665)`, resized subject `427x160`, runtime visible bounds `(53, 15, 458, 145)`, 34,369 transparent pixels, 3,220 partial-alpha pixels, 44,331 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels after an edge-contract cleanup pass. `src/main.ts` now registers/preloads `reward-deck-need-chip-frame`, layers five generated frames behind the card-reward-only `WHAT THE DECK NEEDS` labels with procedural fallbacks, and exposes `rewardDeckNeedChipFrame.loaded/rendered/count` through BattleScene text state. The post-combat reward smoke now waits for the chip texture, asserts the card reward screen renders exactly five chip frames, and asserts Waymark/Preen reward telemetry remains loaded but not rendered with count 0. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "post-combat reward screens render generated choice ceremony art" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4277/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Screenshots, deck-needs crops, `capture-summary.json`, preview logs, action payload, capture script, and keyed raw output are in `.artifacts/test-results/reward-deck-need-chip-frame/`; captures at 2560x1600, 1440x900, and 390x844 report card reward `rewardDeckNeedChipFrame { loaded: true, rendered: true, count: 5 }`, Waymark/Preen `loaded: true, rendered: false, count: 0`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta chips make the deck-needs row read as cohesive reward ceremony hardware without crowding the header plaque, reward cards, skip command, Waymark/Preen cards, or fixed-canvas mobile letterbox. The temporary built preview on port 4277 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Reward Header Plaque post-combat ceremony polish. Generated a transparent-ready shared reward header plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-header-plaque-source-v1.png`, removed the detected `#08f30d` key locally into `.artifacts/test-results/reward-header-plaque/reward-header-plaque-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/ui/icons/reward-header-plaque.png` (1024x224 RGBA, 176,626 bytes). Runtime validation used source size `1881x836`, source visible bounds `(57, 148, 1821, 644)`, crop box `(0, 69, 1881, 723)`, resized subject `644x224`, runtime visible bounds `(207, 25, 816, 199)`, 150,486 transparent pixels, 5,785 partial-alpha pixels, 73,105 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels after an edge-contract cleanup pass plus one residual key-pixel removal. `src/main.ts` now registers/preloads `reward-header-plaque`, layers it into the shared `renderRewardBackdrop` header behind Add to the Flock, Claim a Waymark, and Preen a Card titles with a fallback rectangle, and exposes `rewardHeaderPlaque.loaded/rendered/count` through BattleScene text state. The existing post-combat reward smoke now asserts the plaque loads, renders exactly once, and reports telemetry parity for card reward, Waymark reward, and Preen reward modes. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "post-combat reward screens render generated choice ceremony art" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4276/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Screenshots, header crops, `capture-summary.json`, preview logs, action payload, capture script, and keyed raw output are in `.artifacts/test-results/reward-header-plaque/`; card, Waymark, and Preen captures at 2560x1600, 1440x900, and 390x844 all report `rewardHeaderPlaque { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated plaque gives all post-combat reward headers a cohesive brass/gunmetal/cyan/magenta ceremony anchor without crowding choice cards, card art, deck-needs labels, Waymark descriptions, Preen choices, skip command, or fixed-canvas mobile letterbox. The temporary built preview on port 4276 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Run Outcome Title Plaque finish-screen polish. Generated a transparent-ready win/defeat outcome title plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/run-outcome-title-plaque-source-v1.png`, removed the detected `#02f803` key locally into `.artifacts/test-results/run-outcome-title-plaque/run-outcome-title-plaque-keyed-raw.png`, and cropped/downsampled it into `assets/runtime/ui/icons/run-outcome-title-plaque.png` (1024x224 RGBA, 244,415 bytes). Runtime validation used source size `1983x793`, source visible bounds `(107, 177, 1875, 598)`, crop box `(37, 119, 1945, 656)`, resized subject `796x224`, runtime visible bounds `(141, 22, 883, 202)`, 113,133 transparent pixels, 5,791 partial-alpha pixels, 110,452 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels after an edge-contract cleanup pass. `src/main.ts` now registers/preloads `run-outcome-title-plaque`, layers it behind the Run Complete and Flock Scattered outcome headings with defeat tinting and a rectangle fallback, and exposes `runOutcomeTitlePlaque.loaded/rendered/count` through BattleScene text state. Existing outcome smokes now assert the plaque loads/renders once in run-complete unlock and win/defeat outcome screens. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "run-complete outcome|outcome screens render generated flourish art" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4275/?playwright=1`, and `git diff --check` passed with only existing LF/CRLF warnings. Screenshots, title crops, `capture-summary.json`, preview logs, action payload, capture script, and keyed raw output are in `.artifacts/test-results/run-outcome-title-plaque/`; win and defeat captures at 2560x1600, 1440x900, and 390x844 all report `runOutcomeTitlePlaque { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated plaque gives both finish states a cohesive brass/gunmetal/cyan/magenta title anchor without crowding the crest, unlock strip, stat rows, report frame, command buttons, or fixed-canvas mobile letterbox. The temporary built preview on port 4275 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: System Overlay Title Plaque shared menu polish. Generated a transparent-ready shared overlay title plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/system-overlay-title-plaque-source-v1.png`, removed the detected `#06f00d` key locally into `.artifacts/test-results/system-overlay-title-plaque/system-overlay-title-plaque-keyed-raw.png` (841,556 transparent pixels and 6,149 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/system-overlay-title-plaque.png` (768x192 RGBA, 151,296 bytes). Runtime validation used source size `2019x779`, source visible bounds `(114, 169, 1909, 612)`, crop box `(43, 99, 1980, 682)`, resized subject `638x192`, runtime visible bounds `(86, 20, 682, 172)`, 66,271 transparent pixels, 4,582 partial-alpha pixels, 76,603 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `system-overlay-title-plaque`, adds a shared `addSystemOverlayTitlePlaque` helper, layers the plaque behind Pause, Settings, and How-to-Play overlay headings, and exposes `systemOverlayTitlePlaque.loaded/rendered/count` through menu, route, and battle text state. Existing focused smoke coverage now asserts the title plaque loads/renders in pause overlays, Settings overlays from menu/route/battle contexts, and the title How-to-Play overlay; route/battle Settings assertions intentionally allow the real stacked pause+settings overlay count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "pause overlay opens|settings overlay opens|title How to Play" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4274/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, title crops, `capture-summary.json`, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/system-overlay-title-plaque/`; Settings and How-to-Play captures at 2560x1600, 1440x900, and 390x844 all report `systemOverlayTitlePlaque { loaded: true, rendered: true, count: 1 }` in menu overlays, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated plaque anchors the common overlay headings without crowding close/audio controls, settings rows, help topic cards, action buttons, or fixed-canvas mobile letterbox. The temporary built preview on port 4274 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Profile Title Plaque Flock Record polish. Generated a transparent-ready wide Profile title plaque with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-title-plaque-source-v1.png`, removed the detected `#06f708` key locally into `.artifacts/test-results/profile-title-plaque/profile-title-plaque-keyed-raw.png` (773,230 transparent pixels and 5,033 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/profile-title-plaque.png` (768x192 RGBA, 143,621 bytes). Runtime validation used source size `1983x793`, source visible bounds `(77, 151, 1905, 628)`, crop box `(14, 85, 1968, 694)`, resized subject `616x192`, runtime visible bounds `(93, 18, 675, 174)`, 65,729 transparent pixels, 4,810 partial-alpha pixels, 76,917 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `profile-title-plaque`, layers one generated plaque behind the `Flock Record` title and summary line in `ProfileScene`, keeps it clear of the record medallion and Close command, and exposes `profileTitlePlaque.loaded/rendered/count` through Profile text state. The existing `locked Flock Leaders are gated until unlocked, and the Profile screen renders` smoke now asserts the title plaque loads, renders exactly once, reports telemetry parity, and keeps the expected `560x104` geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4273/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, title-area crops, `capture-summary.json`, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/profile-title-plaque/`; captures at 2560x1600, 1440x900, and 390x844 all report `profileTitlePlaque { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated plaque gives the Profile/Flock Record title a cohesive brass/gunmetal/cyan/magenta hardware anchor without crowding the stat chips, record medallion, Close command, section tabs, Return button, or fixed-canvas mobile letterbox. The temporary built preview on port 4273 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Profile Section Tab Frame Flock Record polish. Generated a transparent-ready compact Profile section-header tab with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-section-tab-frame-source-v1.png`, removed the detected `#06f908` key locally into `.artifacts/test-results/profile-section-tab-frame/profile-section-tab-frame-keyed-raw.png` (918,360 transparent pixels and 4,861 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/profile-section-tab-frame.png` (512x160 RGBA, 86,667 bytes). Runtime validation used source size `2164x727`, source visible bounds `(189, 163, 1978, 570)`, crop box `(118, 98, 2049, 635)`, resized subject `512x142`, runtime visible bounds `(16, 24, 496, 136)`, 34,547 transparent pixels, 3,580 partial-alpha pixels, 43,793 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `profile-section-tab-frame`, layers two generated tabs behind the `Flock Leaders` and `Achievement Badges` section headers in `ProfileScene`, nudges the labels into the dark center channels after visual inspection caught edge crowding, and exposes `profileSectionTabFrame.loaded/rendered/count` through Profile text state. The existing `locked Flock Leaders are gated until unlocked, and the Profile screen renders` smoke now asserts the generated tabs load, render exactly twice, report telemetry parity, and keep the expected `220x48` and `300x48` geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4272/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, header crops, `capture-summary.json`, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/profile-section-tab-frame/`; captures at 2560x1600, 1440x900, and 390x844 all report `profileSectionTabFrame { loaded: true, rendered: true, count: 2 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated tabs anchor the Profile/Flock Record section labels without crowding stat chips, progress rails, achievement rows, leader rows, Return button, or fixed-canvas mobile letterbox. The temporary built preview on port 4272 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Market Card Price Tag Frame purchase-affordance polish. Generated a transparent-ready Market card BUY price-tag frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-card-price-tag-frame-source-v1.png`, removed the detected `#08f80f` key locally into `.artifacts/test-results/market-card-price-tag-frame/market-card-price-tag-frame-keyed-raw.png` (893,150 transparent pixels and 4,534 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/market-card-price-tag-frame.png` (384x160 RGBA, 57,120 bytes). Runtime validation used source size `1881x836`, source visible bounds `(65, 193, 1815, 626)`, crop box `(4, 132, 1876, 687)`, resized subject `384x114`, runtime visible bounds `(12, 35, 372, 125)`, 31,570 transparent pixels, 2,736 partial-alpha pixels, 27,134 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `market-card-price-tag-frame`, layers it behind each Market crew-card BUY price label at `154x48` while preserving the Scrap icon, price text, BUY icon, hit target, and fallback rectangle readability, softens the old procedural tray/chip when the generated frame is present, and exposes `market.cardPriceTagFrame.loaded/rendered/count` through route text state. The focused Market smoke now asserts the generated card price tags load, render, report count parity with the card shelf, and keep expected geometry/alpha. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4271/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, price-row crops, `capture-summary.json`, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/market-card-price-tag-frame/`; captures at 2560x1600, 1440x900, and 390x844 all report `market.cardPriceTagFrame { loaded: true, rendered: true, count: 4 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated tags make the under-card BUY prices read as cohesive brass/gunmetal/cyan/magenta market hardware without hiding card art, service buttons, waymark/supply shelves, shopkeeper art, or fixed-canvas mobile letterbox. The temporary built preview on port 4271 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Market Item Label Frame shelf-row polish. Generated a transparent-ready slim market shelf label plate with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-item-label-frame-source-v1.png`, removed the detected `#0ae50a` key locally into `.artifacts/test-results/market-item-label-frame/market-item-label-frame-keyed-raw.png` (967,885 transparent pixels and 4,684 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/market-item-label-frame.png` (512x192 RGBA, 94,249 bytes). Runtime validation found visible bounds `(12, 38, 499, 153)`, 46,026 transparent pixels, 4,196 partial-alpha pixels, 48,082 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `market-item-label-frame`, layers it behind the waymark and supply shelf item label boxes at `154x62` while preserving existing icon placement, title/kicker text, Scrap price chip, hit targets, and fallback rectangle readability, and exposes `market.itemLabelFrame.loaded/rendered/count` through route text state. The focused Market smoke now asserts the generated label frames load, render, report count parity with waymark shelf offers plus supply offers, and keep expected geometry/alpha. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4270/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, shelf-label crops, contact sheet, capture summary, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/market-item-label-frame/`; captures at 2560x1600, 1440x900, and 390x844 all report `market.itemLabelFrame { loaded: true, rendered: true, count: 6 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated plates make the bottom waymark/supply shelf labels read as cohesive brass/gunmetal/cyan market hardware without hiding labels, price chips, shelf goods, service buttons, card offers, shopkeeper art, or fixed-canvas mobile letterbox. The temporary built preview on port 4270 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Market Service Button Frame shop-action polish. Generated a transparent-ready horizontal market service command frame with the built-in image tool on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-service-button-frame-source-v1.png`, removed the detected `#03f903` key locally into `.artifacts/test-results/market-service-button-frame/market-service-button-frame-keyed-raw.png` (797,949 transparent pixels and 6,217 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/market-service-button-frame.png` (512x192 RGBA, 122,906 bytes). Runtime validation found visible bounds `(9, 23, 503, 168)`, 31,919 transparent pixels, 4,272 partial-alpha pixels, 62,113 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `market-service-button-frame`, layers it behind the Market service and refresh buttons at `174x66` while preserving the existing icon, price chip, Scrap icon, hit target, and fallback rectangle treatment, and exposes `market.serviceButtonFrame.loaded/rendered/count` through route text state. The focused Market smoke now asserts the generated service-button frames load, render, report count parity with the non-supply service shelf plus refresh button, and keep the expected geometry/alpha. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures on `http://127.0.0.1:4269/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Screenshots, service-lane crops, contact sheet, capture summary, preview logs, action payload, metrics, capture script, and keyed raw output are in `.artifacts/test-results/market-service-button-frame/`; captures at 2560x1600, 1440x900, and 390x844 all report `market.serviceButtonFrame { loaded: true, rendered: true, count: 3 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated frames turn the right-side service/refresh lane into cohesive brass/gunmetal/cyan market hardware without crowding the Close button, card offers, shopkeeper art, supply row, or fixed-canvas mobile letterbox. The temporary built preview on port 4269 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Combat Beat Progress Frame polish. Generated a chroma-keyed combat beat progress frame with the built-in image tool, preserved source art at `assets/concept-art/ui/icons/sources/combat-beat-progress-frame-source-v1.png`, and processed the final transparent runtime PNG at `assets/runtime/ui/icons/combat-beat-progress-frame.png`. The runtime asset was normalized to 512x128 RGBA with visible bounds `(35, 11, 477, 117)`, transparent corners, center alpha 255, 27,252 transparent pixels, 3,249 partial-alpha pixels, 35,035 opaque pixels, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `combat-beat-progress-frame`, renders it as a late-layer BattleScene `Beat 1` progress badge at stage `(1190, 104)` with a 148x37 generated frame and fallback, and exposes `combatBeatProgressFrame.loaded/rendered/count` through render text state. The focused combat HUD smoke now waits for the generated texture, asserts one visible `combat-beat-progress-frame` at `148x37` alpha `1`, and verifies telemetry count parity. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat HUD metric chips render generated frames" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/`, production built-preview captures at 2560x1600, 1440x900, and 390x844, and `git diff --check` passed with only existing CRLF warnings. Screenshots, crops, contact sheet, capture summary, preview logs, action payload, metrics, and keyed raw output are in `.artifacts/test-results/combat-beat-progress-frame/`; captures report `combatBeatProgressFrame { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection first caught top-rail overlap with pause/audio controls and then a too-subtle early-layer placement, so the final badge moved into the late command layer where it reads cleanly above the battlefield without overlapping top controls, cards, or enemy UI. The temporary built preview on port 4268 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Title Ascension Value Frame progression-selector polish. Generated a transparent-ready central difficulty value capsule with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-ascension-value-frame-source-v1.png`, removed the detected `#10e715` key locally into `.artifacts/test-results/title-ascension-value-frame/title-ascension-value-frame-keyed-raw.png` (856,216 bytes; 1,116,184 transparent pixels and 3,259 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/title-ascension-value-frame.png` (512x192 RGBA, 103,021 bytes). Runtime normalization used source size `1825x862`, source bounds `(257, 236, 1567, 613)`, crop box `(213, 202, 1611, 647)`, runtime visible bounds `(28, 28, 484, 163)`, 42,797 transparent pixels, 3,326 partial-alpha pixels, 52,181 opaque pixels, center alpha 255, transparent corners, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `title-ascension-value-frame`, layers it behind the dynamic Ascension difficulty label on the title menu with a rectangle fallback, and exposes `titleAscensionValueFrame.loaded/rendered/count` through menu text state. The existing portrait title smoke now waits for the generated texture, asserts one visible `title-ascension-value-frame` at `190x58` alpha `0.66`, and keeps all utility/leader/start/ascension frame checks intact. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4267/?playwright=1`, and `git diff --check` passed with only LF/CRLF warnings. Production output includes `title-ascension-value-frame-Bt1MXbvK.png`. Screenshots, close crops, `capture-summary.json`, preview logs, PID record, capture script, keyed raw output, and contact sheet are in `.artifacts/test-results/title-ascension-value-frame/`; title captures at 2560x1600, 1440x900, and 390x844 all report `titleAscensionValueFrame { loaded: true, rendered: true, count: 1 }`, one visible `190x58` frame, zero page errors, and only the usual high-resolution WebGL `ReadPixels` warnings. Visual inspection confirmed the generated capsule makes the `Standard` difficulty value read as premium progression hardware while preserving readable arrows, Ascension modifier copy, leader cards, Start Run command, utility controls, and fixed-canvas mobile letterbox composition. The temporary built preview on port 4267 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Route Risk Meter Frame node-tooltip polish. Generated a transparent-ready compact three-well route risk meter with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-risk-meter-frame-source-v1.png`, removed the detected `#03f804` key locally into `.artifacts/test-results/route-risk-meter-frame/route-risk-meter-frame-keyed-raw.png` (1,293,603 bytes; 974,327 transparent pixels and 2,422 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/route-risk-meter-frame.png` (384x128 RGBA, 47,818 bytes). Runtime normalization used source size `1718x916`, source bounds `(246, 189, 1474, 718)`, crop box `(210, 161, 1510, 746)`, runtime visible bounds `(78, 14, 306, 115)`, 28,664 transparent pixels, 1,712 partial-alpha pixels, 18,776 opaque pixels, center alpha 255, transparent corners, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `route-risk-meter-frame`, layers it behind the existing colored risk bars in RouteScene node hover tooltips with a rectangle fallback, and exposes `routeRiskMeterFrame.loaded/rendered/count` through route text state. The route-map smoke now waits for the generated texture, asserts two visible `route-risk-meter-frame` sprites for the boss/cache node tooltips at `50x26` alpha `0.84`, and verifies route text-state telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4266/?playwright=1`, and `git diff --check` passed with only LF/CRLF warnings. Production output includes `route-risk-meter-frame-FPf_IwDV.png`. Screenshots, close crops, `capture-summary.json`, preview logs, PID record, capture script, keyed raw output, and the contact sheet are in `.artifacts/test-results/route-risk-meter-frame/`; route captures at 2560x1600, 1440x900, and 390x844 all show two generated risk-meter frames with zero page errors. The capture harness logged the pre-existing duplicate texture-key messages while restarting scenes plus the usual WebGL `ReadPixels` warnings. Visual inspection confirmed the generated meter reads as cohesive brass/gunmetal/cyan/magenta route hardware beneath the danger bars, keeps tooltip titles and reward badges readable, and does not overlap the map nodes, HUD, boss-prep footer, or mobile letterbox composition. The temporary built preview on port 4266 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Route Commit Tooltip Frame route-confirm hover polish. Generated a transparent-ready compact route tooltip chip with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-commit-tooltip-frame-source-v1.png` (1,251,930 bytes), removed the detected `#05f907` key locally into `.artifacts/test-results/route-commit-tooltip-frame/route-commit-tooltip-frame-keyed-raw.png` (794,849 bytes; 1,000,454 transparent pixels and 4,293 partial-alpha pixels), and cropped/downsampled it into `assets/runtime/ui/icons/route-commit-tooltip-frame.png` (512x160 RGBA, 90,300 bytes). Runtime normalization used source size `1962x802`, crop box `(180, 191, 1781, 614)`, runtime visible bounds `(15, 18, 497, 141)`, 27,007 transparent pixels, 3,877 partial-alpha pixels, 51,036 opaque pixels, transparent corners, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `route-commit-tooltip-frame`, renders it behind the RouteScene `Take this route` hover tooltip for the commit medallion while keeping the old rectangle as fallback, strokes the text for readability over the dark glass center, and exposes `routeCommitTooltipFrame.loaded/rendered/count` through route text state. The existing route-map smoke now waits for the generated texture, opens the real simple-tooltip path after selecting a reachable node, and asserts one visible `route-commit-tooltip-frame` at `188x48` alpha `0.94`. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4265/?playwright=1`, `npm run validate:runtime`, and `git diff --check` passed with only LF/CRLF warnings. Production output includes `route-commit-tooltip-frame-AeKk9QLF.png`. Screenshots, `capture-summary.json`, close crops, contact sheet, preview logs, PID/port records, capture script, action payload, keyed raw output, and runtime stats are in `.artifacts/test-results/route-commit-tooltip-frame/`; route captures at 2560x1600, 1440x900, and 390x844 all report `routeCommitTooltipFrame { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual WebGL `ReadPixels` warnings. Visual inspection confirmed the generated chip reads clearly above the route commit medallion, matches the brass/gunmetal/cyan/magenta route-map hardware language, and does not obscure the selected node, boss-prep strip, top HUD, or fixed-canvas mobile letterbox composition. The temporary built preview on port 4265 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Section Tab Frame label polish. Generated a transparent-ready compact section label tab with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/deck-review-section-tab-frame-source-v1.png` (1,500,878 bytes), removed the detected key locally into `.artifacts/test-results/deck-review-section-tab-frame/deck-review-section-tab-frame-keyed-raw.png` (996,674 bytes), and cropped/downsampled it into `assets/runtime/ui/icons/deck-review-section-tab-frame.png` (512x128 RGBA, 100,847 bytes). Chroma cleanup used key `#05ed0f`, source size `1921x819`, crop box `(160, 201, 1757, 603)`, runtime visible placement `508x128`, 11,146 transparent pixels, 3,468 partial-alpha pixels, 50,922 opaque pixels, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `deck-review-section-tab-frame`, replaces the route deck-review bare `CARD INDEX` label with a generated tab plus centered text, adds the same generated section tab to the battle deck-review overlay between zone counters and the first list row, keeps a rectangle fallback for missing textures, and exposes `deckReviewSectionTabFrame.loaded/rendered/count` through both route and battle text state. The focused deck-review smoke now asserts the generated tab loads/renders exactly once in route and battle overlays at `136x32` geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4264/?playwright=1`, `npm run validate:runtime`, and `git diff --check` passed with only LF/CRLF warnings. Production output includes `deck-review-section-tab-frame-BlF6m4cH.png`. Screenshots, `capture-summary.json`, close crops, contact sheet, preview logs, PID/port records, capture script, action payload, and keyed raw output are in `.artifacts/test-results/deck-review-section-tab-frame/`; route and battle captures at 2560x1600, 1440x900, and 390x844 all report `deckReviewSectionTabFrame { loaded: true, rendered: true, count: 1 }`, zero page errors, and only the usual WebGL `ReadPixels` warnings. Visual inspection confirmed the generated tab reads as cohesive brass/gunmetal/cyan/magenta deck hardware, the `CARD INDEX` label remains legible, and the placement clears title plaques, zone counters, row list, detail panels, page controls, close controls, and mobile letterbox composition. The temporary built preview on port 4264 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Meta Chip Frame row-metadata polish. Generated a transparent-ready slim deck-review row metadata chip with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/deck-review-meta-chip-frame-source-v1.png` (1,555,689 bytes), removed the detected `#09e810` key locally into `.artifacts/test-results/deck-review-meta-chip-frame/deck-review-meta-chip-frame-keyed-raw.png` (1,034,784 bytes; 1,074,726 transparent pixels and 3,841 partial-alpha pixels), and cropped/downsampled the cleaned hardware into `assets/runtime/ui/icons/deck-review-meta-chip-frame.png` (512x128 RGBA, 81,207 bytes). Runtime normalization used crop box `(69, 239, 1844, 582)`, placed the visible chip at `512x98` inside the runtime canvas, and validated 23,192 transparent pixels, 3,314 partial-alpha pixels, 39,030 opaque pixels, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `deck-review-meta-chip-frame`, renders it behind route deck-review row `cardLabel / zone` metadata at `118x24`, renders it behind battle deck-review row `cardLabel` metadata at `92x24` while preserving the separate zone icon, keeps a fallback rectangle for missing textures, and exposes `deckReviewMetaChipFrame.loaded/rendered/count` through route and battle text state. The focused deck-review smoke now asserts the generated meta chip loads/renders exactly once per visible row in both route and battle overlays, with the expected route/battle geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run validate:runtime`, production built-preview captures on `http://127.0.0.1:4263/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `deck-review-meta-chip-frame-BROWM3PK.png`. Screenshots, `capture-summary.json`, contact sheet, close crops, wide row crops, preview logs, PID/port records, action payload, capture script, and keyed raw output are in `.artifacts/test-results/deck-review-meta-chip-frame/`; route and battle captures at 2560x1600, 1440x900, and 390x844 all report `deckReviewMetaChipFrame { loaded: true, rendered: true, count: 10 }`, zero page errors, and only the usual WebGL `ReadPixels` warnings. Visual inspection confirmed the generated chips make the row metadata read like cohesive deck hardware while keeping card names, cost badges, row frames, zone icons, detail panels, page controls, close controls, and fixed-canvas mobile letterbox composition clear. The temporary built preview on port 4263 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Cost Badge pip polish. Generated a transparent-ready deck-review cost medallion with built-in imagegen on a flat green chroma-key background, rejected the first ring-only candidate because its center would key out under the cost numeral, and preserved the solid-center shipped source at `assets/concept-art/ui/icons/sources/deck-review-cost-badge-source-v1.png` (1,873,959 bytes). Removed the detected `#02f904` key locally into `.artifacts/test-results/deck-review-cost-badge/deck-review-cost-badge-keyed-raw.png` (1,395,127 bytes; 949,931 transparent pixels and 2,851 partial-alpha pixels), then cropped/downsampled the visible badge into `assets/runtime/ui/icons/deck-review-cost-badge.png` (256x256 RGBA, 82,692 bytes) with crop box `(92, 92, 1160, 1137)`, 28,731 transparent pixels, 2,257 partial-alpha pixels, 34,548 opaque pixels, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `deck-review-cost-badge`, replaces the flat cost circles in RouteScene deck-review rows, BattleScene deck-review rows, and both shared/battle deck detail panels with a generated badge helper plus fallback, switches only those deck-review cost numerals to light stroked text for the dark badge center, and exposes `deckReviewCostBadge.loaded/rendered/count` through route and battle text state. The focused deck-review smoke now asserts the generated badge loads/renders with counts tied to visible row frames plus one detail badge in both route and battle overlays, including `34x34` row geometry and `46x46` detail geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` after rerunning with an actions file to avoid PowerShell JSON quoting, `npm run validate:runtime`, production built-preview captures on `http://127.0.0.1:4262/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `deck-review-cost-badge-BWQXLPB1.png`. Screenshots, `capture-summary.json`, close crops, contact sheet, preview logs, PID/port records, action payload, and keyed raw output are in `.artifacts/test-results/deck-review-cost-badge/`; route and battle captures at 2560x1600, 1440x900, and 390x844 all report `deckReviewCostBadge { loaded: true, rendered: true, count: 11 }`, zero page errors, and only the usual WebGL `ReadPixels` warnings. Visual inspection confirmed the generated badges read cleanly in row lists and detail panels without covering card names, zone labels, card art, rules text, page controls, close controls, or fixed-canvas mobile letterbox composition. The temporary built preview on port 4262 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Detail Frame dossier-panel polish. Generated a transparent-ready deck-review card detail frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/deck-review-detail-frame-source-v1.png` (1,859,808 bytes), removed the `#04f904` key locally, and normalized the cleaned subject into `assets/runtime/ui/icons/deck-review-detail-frame.png` (1024x704 RGBA, 960,288 bytes). The raw keyed output at `.artifacts/test-results/deck-review-detail-frame/deck-review-detail-frame-keyed-raw.png` had 608,252 transparent pixels and 4,632 partial-alpha pixels, while the runtime sprite has crop box `(9, 131, 1525, 865)`, 91,346 transparent pixels, 12,582 partial-alpha pixels, 616,968 opaque pixels, center alpha 255, and zero visible/edge green pixels. `src/main.ts` now registers/preloads `deck-review-detail-frame`, renders it behind RouteScene deck detail content, replaces the borrowed `combat-pile-review-frame` for BattleScene deck-review detail content only, keeps draw/discard pile review panels on their combat frame, exposes `deckReviewDetailFrame.loaded/rendered/count` in route and battle text state, and lifts the shared detail stat band so stats/chips clear the new lower ornament. The focused deck-review smoke now asserts the generated detail frame loads/renders with one route frame at `642x444` and two battle frames at `672x466` (base plus subtle glint), while battle deck-review telemetry confirms the combat-pile review frame is no longer rendered. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, high-resolution production built-preview captures on `http://127.0.0.1:4261/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `deck-review-detail-frame-WqYu85Dg.png`. Screenshots, `capture-summary.json`, preview logs, PID/port records, and keyed raw output are in `.artifacts/test-results/deck-review-detail-frame/`, including route and battle captures at 2560x1600, 1440x900, and 390x844 with zero console errors and zero page errors. Visual inspection first caught the new lower rail brushing the Flock Stats line; after moving the shared stat band upward, final route/battle desktop and portrait captures show the generated brass/gunmetal/cyan/magenta frame reading as cohesive deck-review hardware without hiding card art, rules text, stats, close controls, row list, or mobile letterbox composition. The temporary built preview on port 4261 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Title Plaque header polish. Generated a transparent-ready dedicated deck-review title plaque with built-in imagegen on a flat chroma-key background, preserved the corrected source at `assets/concept-art/ui/icons/sources/deck-review-title-plaque-source-v1.png` (1,388,536 bytes), removed the `#0cec13` key locally, and cropped/downsampled it into `assets/runtime/ui/icons/deck-review-title-plaque.png` (1024x207 RGBA, 296,010 bytes). The raw keyed output had 974,780 transparent pixels and 5,335 partial-alpha pixels, while the runtime sprite has 31,130 transparent pixels, 8,097 partial-alpha pixels, 172,741 opaque pixels, center alpha 255, and zero visible green by the cleanup heuristic. `src/main.ts` now registers/preloads `deck-review-title-plaque`, uses it for both the RouteScene deck overlay header and the BattleScene deck review overlay header, keeps draw/discard pile overlays on the older combat-pile plaque, removes the duplicate raw panel title from the route overlay, and reports `deckReviewTitlePlaque.loaded/rendered/count` through both scene text states. The focused deck-review smoke now asserts the generated title plaque loads/renders exactly once in route and battle deck overlays with expected geometry (`452x92` route, `430x78` battle) alongside the generated row, scroll, page, and flourish art. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, high-resolution production built-preview captures on `http://127.0.0.1:4261/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `deck-review-title-plaque-CRczVHTU.png`. Screenshots, `capture-summary.json`, preview logs, PID/port records, and the keyed raw output are in `.artifacts/test-results/deck-review-title-plaque/`, including route and battle captures at 2560x1600, 1440x900, and 390x844 with zero console errors and zero page errors. Visual inspection confirmed the generated plaque reads as cohesive brass/gunmetal/cyan/magenta deck hardware, the route header no longer duplicates `Deck Review`, and the battle deck overlay no longer borrows the combat-pile title plaque. The temporary built preview on port 4261 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Profile Progress Fill Strip progression-rail polish. Generated a transparent-ready horizontal profile progress fill strip with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-progress-fill-strip-source-v1.png` (1,201,069 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/profile-progress-fill-strip.png` (768x128 RGBA, 87,936 bytes). Chroma processing detected key `#08f810`; the raw keyed output had 1,169,504 transparent pixels and 22,024 partial-alpha pixels, while the runtime sprite has visible bounds `(27, 25, 742, 103)`, 51,376 transparent pixels, 4,953 partial-alpha pixels, 41,975 opaque pixels, 47.74% coverage, transparent corners, and zero green-ish visible/edge pixels. `src/main.ts` now registers/preloads `profile-progress-fill-strip`, replaces the flat rectangle fill in ProfileScene's three progression rails with a generated cyan/brass/gunmetal strip scaled to each progress fraction, keeps a no-fill zero-progress state and rectangle fallback, applies existing rail accent tinting to the non-cyan bars, and exposes `profileProgressFillStrip.loaded/rendered/count` through ProfileScene text state. The existing `locked Flock Leaders are gated until unlocked, and the Profile screen renders` smoke now asserts the generated fill strip loads/renders exactly three times with visible `16px` rail-fill geometry alongside the existing profile rail frames. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and high-resolution production built-preview captures on `http://127.0.0.1:4253/?playwright=1`. Production output includes `profile-progress-fill-strip-doQlCFv5.png`. Screenshots, `capture-summary.json`, preview logs, PID record, and keyed raw output are in `.artifacts/test-results/profile-progress-fill-strip/`, including `profile-desktop-2560x1600.png` and `profile-portrait-390x844.png` with `profileProgressFillStrip.loaded=true`, `rendered=true`, `count=3`, rail-fill widths `183.6`, `52.5`, and `28`, zero console errors, zero page errors, and nonblank pixel-sanity checks for both screenshots. Visual inspection confirmed the generated strips make the progression rails feel like premium hardware without crowding the `Flock Record` stats, achievement list, leader rows, or mobile letterbox. The temporary built preview on port 4253 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Route Waymark Scroll Rail Frame drawer-overflow polish. Generated a transparent-ready vertical waymark drawer scroll rail with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-waymark-scroll-rail-frame-source-v1.png` (1,262,962 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-waymark-scroll-rail-frame.png` (256x768 RGBA, 88,364 bytes). Chroma processing detected key `#05f70b`; the raw keyed output had 1,214,617 transparent pixels and 43,819 partial-alpha pixels, while the runtime sprite has visible bounds `(90, 16, 165, 752)`, 152,075 transparent pixels, 8,662 partial-alpha pixels, 35,871 opaque pixels, 22.65% coverage, transparent corners, and zero green-ish visible/edge pixels. `src/main.ts` now registers/preloads `route-waymark-scroll-rail-frame`, replaces the plain rectangle overflow scrollbar in both RouteScene and BattleScene Found Waymarks drawers with a shared generated rail helper plus fallback rectangles, tunes the rail to `52x386` with a readable violet thumb, and exposes `routeWaymarkScrollRailFrame.loaded/rendered/count` through both route and battle text state. A new focused smoke, `waymark drawer renders generated scroll rail when carried artifacts overflow`, starts route and battle drawers with enough carried waymarks to overflow and asserts the generated rail loads/renders exactly once in each scene. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "waymark drawer renders generated scroll rail|run kit drawer flourish" --workers=1 --reporter=line`, focused rerun `npx playwright test tests/smoke.spec.ts -g "waymark drawer renders generated scroll rail" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and high-resolution production built-preview captures on `http://127.0.0.1:4252/?playwright=1`. Production output includes `route-waymark-scroll-rail-frame-CJuPzYK0.png`. Screenshots, `capture-summary.json`, preview logs, PID record, and the keyed raw output are in `.artifacts/test-results/route-waymark-scroll-rail-frame/`, including route desktop, battle desktop, and route portrait captures with `routeWaymarkScrollRailFrame.loaded=true`, `rendered=true`, `count=1`, visible rail geometry `(x=1132, y=367, displayWidth=52, displayHeight=386, alpha=0.92)`, and nonblank pixel-sanity checks for all three screenshots. Visual inspection first caught the generated rail being too subdued behind the dark slot, so the final helper widened/brightened the art and narrowed the slot; the final captures show the rail aligned with the drawer, clear of close controls and item tiles, and consistent with the brass/cyan/magenta run-kit hardware language. The temporary built preview on port 4252 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Run Outcome Report Frame end-screen panel polish. Generated a transparent-ready run-outcome report frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/run-outcome-report-frame-source-v1.png` (1,709,186 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/run-outcome-report-frame.png` (1024x704 RGBA, 695,001 bytes). Chroma processing detected key `#07f512`; the raw keyed output had 440,530 transparent pixels and 25,608 partial-alpha pixels, while the runtime sprite has visible bounds `(24, 58, 1000, 645)`, 231,629 transparent pixels, 14,076 partial-alpha pixels, 475,191 opaque pixels, 67.87% coverage, transparent corners, center alpha 255, and only 6 conservative green-ish visible pixels. `src/main.ts` now registers/preloads `run-outcome-report-frame`, renders it as the generated end-screen report panel behind win/defeat summary content, exposes `runOutcomeReportFrame.loaded/rendered/count` through BattleScene text state, and includes fallback panel rules when the asset is absent. The same pass tuned the older outcome/unlock flourish layers and reveal FX down, added dark left-summary and right-ledger readability mats, lowered the generated report-frame alpha so the ornate center reads as backing art, and kept the ten stat-row frames plus two command frames layered over the report. The focused outcome smokes now assert the generated report frame loads/renders exactly once across fresh-unlock run completion plus win/defeat outcome screens. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "run-complete outcome celebrates fresh meta unlocks|outcome screens render generated flourish" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4251/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `run-outcome-report-frame-CuqWHvNI.png`. Screenshots and `capture-summary.json` are in `.artifacts/test-results/run-outcome-report-frame/`, including final desktop win, desktop defeat, and portrait win captures with `runOutcomeReportFrame.loaded=true`, `rendered=true`, `count=1`, 10 stat rows, 2 command frames, expected labels present, zero page errors, and only the usual WebGL readback warnings on the desktop win screenshot. Visual inspection confirmed the generated report frame is present on both outcome states and the text-bearing ledger/buttons remain readable after the final alpha/mat tuning. The temporary built preview on port 4251 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Run Outcome Command Frame end-screen action polish. Generated a transparent-ready wide outcome command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/run-outcome-command-frame-source-v1.png` (1,507,511 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/run-outcome-command-frame.png` (768x256 RGBA, 185,299 bytes). Chroma processing detected key `#0eed1c`; the raw keyed output at `.artifacts/test-results/run-outcome-command-frame/run-outcome-command-frame-keyed-raw.png` had 978,264 transparent pixels and 24,322 partial-alpha pixels, while the runtime sprite has visible bounds `(19, 24, 749, 232)`, 95,220 transparent pixels, 6,928 partial-alpha pixels, 94,460 opaque pixels, 51.57% coverage, transparent corners, center alpha 255, and only 19 conservative green-ish edge pixels with no visible fringe in inspection. `src/main.ts` now registers/preloads `run-outcome-command-frame`, renders it behind the `Run It Back` and `Main Menu` outcome actions with preserved hit targets, hover alpha feedback, win/defeat tinting, and exposes `runOutcomeCommandFrame.loaded/rendered/count` through BattleScene text state. The focused outcome smokes now assert the generated command frames load and render exactly two instances across fresh-unlock run completion plus win/defeat outcome screens. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "run-complete outcome celebrates fresh meta unlocks|outcome screens render generated flourish" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4221/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `run-outcome-command-frame-BG7GDc2U.png`. Screenshots and `capture-summary.json` are in `.artifacts/test-results/run-outcome-command-frame/`, including win, defeat, and portrait captures with `runOutcomeCommandFrame.loaded=true`, `rendered=true`, `count=2`, two labels present (`Run It Back`, `Main Menu`), zero console errors, and zero page errors. Visual inspection first caught the frame being too subdued behind the hit rectangle, so the final wiring layers the generated frame above the transparent hit target at alpha 0.86 while keeping icons/text on top; the final captures show cohesive hardware buttons without new overlap. The existing central outcome flourish remains visually dominant and is still a good future tuning target. The temporary built preview on port 4221 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Run Outcome Stat Row Frame end-screen report polish. Generated a transparent-ready narrow outcome stat-row rail with built-in imagegen on a flat green chroma-key background; the first generated candidate read as a taller report panel, so the preserved shipped source is `assets/concept-art/ui/icons/sources/run-outcome-stat-row-frame-source-v2.png` (1,015,838 bytes). Removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`; chroma processing detected key `#11f517`, with 1,451,072 transparent pixels and 26,043 partial-alpha pixels in the raw keyed output at `.artifacts/test-results/run-outcome-stat-row-frame/run-outcome-stat-row-frame-v2-keyed-raw.png`. Cropped/downsampled the cleaned art into `assets/runtime/ui/icons/run-outcome-stat-row-frame.png` (768x128 RGBA, 43,325 bytes) with visible bounds `(20, 22, 748, 106)`, 77,758 transparent pixels, 6,881 partial-alpha pixels, 13,665 opaque pixels, 20.9% coverage, transparent corners, and only 6 conservative green-ish edge pixels with no visible fringe in inspection. `src/main.ts` now registers/preloads `run-outcome-stat-row-frame`, layers it at low alpha over each of the ten run-complete/defeat recap rows while preserving the existing dark readability plates and text, warms the tint on defeat, and exposes `runOutcomeStatRowFrame.loaded/rendered/count` through BattleScene text state. The focused outcome smokes now assert the generated row frame loads and renders exactly ten instances across fresh-unlock run completion plus win/defeat outcome screens. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "run-complete outcome celebrates fresh meta unlocks|outcome screens render generated flourish" --workers=1 --reporter=line`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4217/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Production output includes `run-outcome-stat-row-frame-BG7hlFrO.png`. Screenshots, capture summary, and keyed raw output are in `.artifacts/test-results/run-outcome-stat-row-frame/`, including win, defeat, and portrait captures with `runOutcomeStatRowFrame.loaded=true`, `rendered=true`, `count=10`, zero console errors, and zero page errors. Visual inspection confirmed the new rails add cohesive brass/cyan hardware to the end-of-run report rows without creating new text overlap; the existing central outcome flourish remains visually dominant and is a good future tuning target. The temporary built preview on port 4217 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Route Choice Detail Frame hover-tooltip polish. Generated a transparent-ready route choice hover dossier frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-choice-detail-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-choice-detail-frame.png` (768x432 RGBA, 390,122 bytes). Alpha validation found transparent corners, center alpha 255, visible bounds `(6, 32, 762, 399)`, 67,232 transparent pixels, 7,290 partial-alpha pixels, 257,254 opaque pixels, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `route-choice-detail-frame`, renders it behind route choice hover detail panels with wider text-safe columns, offsets the panel away from right-column choices, raises the display tree depth, and reports `routeChoiceDetailFrame.loaded/rendered/count` through route text state plus `nodeChoice.detailFrame`. The route-event overlay smoke now waits for async set-piece art to settle, then asserts the generated hover detail frame loads/renders across basin, cache, signal, nest, and rival choice overlays. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route event overlays render generated special-node backdrops" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Built-preview captures on `http://127.0.0.1:4217/?playwright=1` covered nest/cache hover panels at 2560x1600, 844x390, and 390x844; captures confirmed `routeChoiceDetailFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, zero page errors, and only expected WebGL ReadPixels screenshot warnings. Screenshots, close crops, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/route-choice-detail-frame/`; production output includes `route-choice-detail-frame-CnMAwt15.png`. Visual inspection first caught the hover panel being captured before a late async rerender and then verified the final settled-hover captures: the generated frame reads as a premium tooltip, keeps text readable, and does not swallow the cache/nest row labels or mobile letterbox composition. The temporary built preview on port 4217 was used for verification; Bird Squad remains live on port 5199.

Current follow-up: Route Choice Option Frame event-row polish. Generated a transparent-ready horizontal route choice row frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-choice-option-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-choice-option-frame.png` (768x192 RGBA, 127,610 bytes). Alpha validation found transparent corners, center alpha 255, visible bounds `(32, 39, 736, 152)`, 76,370 transparent pixels, 5,661 partial-alpha pixels, 65,425 opaque pixels, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `route-choice-option-frame`, layers it behind the repeated route-event choice rows in the shared event choice renderer, cache drawer renderer, and taller set-piece choice renderer, preserves the existing hit targets, icons, text, locked-state treatment, and hover detail behavior, and exposes `routeChoiceOptionFrame.loaded/rendered/count` through route text state plus `nodeChoice.optionFrame`. The route-event overlay smoke now asserts the generated option frames load/render across basin, cache, signal, nest, and rival surfaces. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route event overlays render generated special-node backdrops" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Built-preview captures on `http://127.0.0.1:4248/?playwright=1` covered nest and cache overlays at 2560x1600, 844x390, and 390x844; captures confirmed `optionFrame` telemetry `{ loaded: true, rendered: true }`, 5 nest row frames, 6 cache row frames, zero page errors, and zero console errors. Screenshots and `capture-summary.json` are in `.artifacts/test-results/route-choice-option-frame/`; production output includes `route-choice-option-frame-J7bU7ApN.png`. Visual inspection confirmed the generated row frames add cohesive hardware structure without crowding route choice titles, effect summaries, resident art, cache cabinet art, or fixed-canvas mobile letterbox. The temporary built preview on port 4248 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Route Event Cancel Command Frame overlay-action polish. Generated a transparent-ready wide Cancel command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-event-cancel-command-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-event-cancel-command-frame.png` (512x160 RGBA, 114,811 bytes). Alpha validation found transparent corners, center alpha 255, visible bounds `(20, 13, 492, 146)`, 22,411 transparent pixels, 4,229 partial-alpha pixels, 55,280 opaque pixels, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `route-event-cancel-command-frame`, layers it behind RouteScene route-event/card-picker Cancel controls while preserving the existing hit target and close medallion, widens the label lane, keeps hover brighten/scale feedback, and exposes `routeEventCancelCommandFrame.loaded/rendered/count` through route text state. This also fixes the old helper's visual mismatch by making the passed label text the visible command text on the generated frame. The large-deck route picker smoke now asserts the generated cancel command frame loads, renders exactly once, keeps the expected 188x52 geometry, and shows the `Cancel` label. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Built-preview captures on `http://127.0.0.1:4247/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `routeEventCancelCommandFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, one visible frame at 188x52, detected `Cancel` label, zero page errors, and zero console errors. Screenshots and `capture-summary.json` are in `.artifacts/test-results/route-event-cancel-command-frame/`; production output includes `route-event-cancel-command-frame-kyWHdPWp.png`. Visual inspection confirmed the Cancel action now matches the route overlay hardware without crowding card choices, scroll controls, the range label, or fixed-canvas mobile letterbox. The temporary built preview on port 4247 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Card Picker Scroll Button Frame overlay-control polish. Generated a transparent-ready square micro-control frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/card-picker-scroll-button-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/card-picker-scroll-button-frame.png` (192x144 RGBA, 26,338 bytes). Alpha validation found transparent corners, center alpha 0, visible bounds `(29, 6, 162, 138)`, 18,344 transparent pixels, 2,918 partial-alpha pixels, 6,386 opaque pixels, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `card-picker-scroll-button-frame`, layers it behind the existing Up/Down chevrons in the RouteScene card picker, preserves the hit/backing rectangle and click behavior, adds hover brighten/scale on enabled controls, and exposes `cardPickerScrollButtonFrame.loaded/rendered/count` through route text state. The existing large-deck picker smoke now asserts the generated scroll frame loads, renders exactly two controls, and keeps the expected 48x36 geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Built-preview captures on `http://127.0.0.1:4246/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `cardPickerScrollButtonFrame` telemetry `{ loaded: true, rendered: true, count: 2 }`, two visible frames at 48x36, zero page errors, and zero console errors. Screenshots and `capture-summary.json` are in `.artifacts/test-results/card-picker-scroll-button-frame/`; production output includes `card-picker-scroll-button-frame-lLttfgxX.png`. Visual inspection confirmed the side-rail scroll controls now read as coherent hardware without crowding the card grid, range label, Cancel command, or fixed-canvas mobile letterbox. The temporary built preview on port 4246 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Deck Review Scroll Button Frame route-overlay polish. Generated a transparent-ready compact scroll-button frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/deck-review-scroll-button-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/deck-review-scroll-button-frame.png` (384x160 RGBA, 97,877 bytes). Alpha validation found transparent corners, center alpha 255, visible bounds `(5, 11, 379, 148)`, 16,512 transparent pixels, 3,084 partial-alpha pixels, 41,844 opaque pixels, and no visible chroma fringe in inspection. `src/main.ts` now registers/preloads `deck-review-scroll-button-frame`, uses it for the RouteScene Deck Review overlay Up/Down scroll controls with preserved hit targets, hover scaling, disabled opacity, and engine-rendered labels, and exposes `deckReviewScrollButtonFrame.loaded/rendered/count` through route text state. The focused deck-review smoke now asserts the generated texture loads, renders exactly two frames, reports matching telemetry, and keeps the expected 82x34 frame geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings. Built-preview captures on `http://127.0.0.1:4245/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `deckReviewScrollButtonFrame` telemetry `{ loaded: true, rendered: true, count: 2 }`, two visible frames at 82x34, zero page errors, and zero console errors. Screenshots and `capture-summary.json` are in `.artifacts/test-results/deck-review-scroll-button-frame/`; production output includes `deck-review-scroll-button-frame-JR1mNjaC.png`. Visual inspection confirmed the buttons read as cohesive hardware controls without covering the card rows, detail panel, close command, page label, or mobile letterbox composition. The temporary built preview on port 4245 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Codex Grid Scroll Cue Frame browser-footer polish. Generated a transparent-ready Codex browser scroll cue frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-grid-scroll-cue-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/codex-grid-scroll-cue-frame.png` (512x144 RGBA, 72,975 bytes). Alpha processing detected key `#05f80e`; the runtime sprite has transparent corners, visible bounds `(4, 28, 508, 115)`, 36,366 transparent pixels, 3,651 partial-alpha pixels, 33,711 opaque pixels, center alpha 255, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `codex-grid-scroll-cue-frame`, replaces the plain Codex browser grid footer `scroll v` text with a generated 150x34 frame plus engine-rendered `SCROLL` / `UP` label, and exposes `codexGridScrollCueFrame.loaded/rendered/count` through Codex text state. The focused Codex smoke now asserts the texture loads, renders exactly once in the browser grid footer, and keeps the expected frame geometry/alpha. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "codex: starting a run discovers its deck and the Codex screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings for dirty files. Built-preview captures on `http://127.0.0.1:4244/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `codexGridScrollCueFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, frame geometry `150x34` at `(1192, 701)`, zero page errors, and zero console errors. The first visual pass caught the frame clipping at the bottom edge, so the final placement was lifted and reduced before the passing recapture. Screenshots and `capture-summary.json` are in `.artifacts/test-results/codex-grid-scroll-cue-frame/`; production output includes `codex-grid-scroll-cue-frame-Cz5dLAvO.png`. Visual inspection confirmed the footer cue now matches Codex hardware without covering card entries, tab labels, the Back command, or mobile letterbox composition.

Current follow-up: Card Hover Stat Chip Frame hover-detail polish. Generated a transparent-ready brass/gunmetal/cyan/magenta Flock Stats chip frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/card-hover-stat-chip-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/card-hover-stat-chip-frame.png` (512x128 RGBA, 81,529 bytes). Alpha validation found transparent corners, center alpha 255, 25,842 transparent pixels, 2,534 partial-alpha pixels, 37,160 opaque pixels, visible bounds `(20, 9, 492, 119)`, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `card-hover-stat-chip-frame`, layers it behind the compact Flock Stats footer in BattleScene hand card previews and the shared reward/market/preen floating card detail panel, exposes `cardHoverStatChipFrame.loaded/rendered/count` through battle and route text state, normalizes the active Flock Stats separator to clean ASCII, and removes a duplicate corrupted elite label while preserving the clean `ELITE` crest label. The focused card-hover smoke now asserts stat-chip frame counts and telemetry across battle hand preview, battle reward detail, route market hover, and route preen hover. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "card choice surfaces expose full card details on hover" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing LF-to-CRLF warnings for dirty files. Built-preview captures on `http://127.0.0.1:4243/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed the battle hand preview and route preen hover each report `cardHoverStatChipFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, with zero page errors and zero console errors. Screenshots and `capture-summary.json` are in `.artifacts/test-results/card-hover-stat-chip-frame/`; production output includes `card-hover-stat-chip-frame-BLVoXAC1.png`. Visual inspection confirmed the chip reads as a premium stat footer without covering card names, effect text, route-map controls, or mobile letterbox composition. Bird Squad remains live on port 5199.

Current follow-up: Combat Log Frame command-strip polish. Generated a transparent-ready horizontal combat event-log frame with built-in imagegen on a flat green chroma-key background; prompt type was `stylized-concept` for a low-profile brass/gunmetal battle ticker with cyan signal ticks, magenta feather inlays, riveted salvage corners, open center, no text, and no logos. Preserved the source at `assets/concept-art/ui/icons/sources/combat-log-frame-source-v1.png` (1254x1254 RGB, 1,245,805 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/optimized it into `assets/runtime/ui/icons/combat-log-frame.png` (512x224 RGBA, 91,100 bytes). Alpha processing detected key `#06f80b`; the raw keyed output at `.artifacts/test-results/combat-log-frame/combat-log-frame-keyed-raw.png` has 1,391,800 transparent pixels, 8,620 partial-alpha pixels, visible bounds `(26, 414, 1228, 817)`, transparent corners, an open center, and zero green-ish visible pixels. The optimized runtime sprite has transparent corners, center alpha 0, visible bounds `(10, 29, 502, 195)`, 81,201 transparent pixels, 7,895 partial-alpha pixels, 25,592 opaque pixels, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `combat-log-frame`, renders it as a compact two-line BattleScene combat log ticker at `(650,392)` with a text-safe inner plate so the generated border chrome stays readable above the hand cards, attaches a Combat Log tooltip, and reports `combatLogFrame.loaded/rendered/count` through battle text state. The new smoke `combat event log renders generated dossier frame` asserts the texture loads, renders as two layers, matches telemetry, keeps the expected 430x78 compact geometry, and preserves live log text. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "combat event log" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed apart from existing LF-to-CRLF warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `combat-log-frame-W41CfI3W.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, close crop, raw keyed output, and `capture-summary.json` are in `.artifacts/test-results/combat-log-frame/`; final captures at 2560x1600, 844x390, and 390x844 reported `combatLogFrame.loaded=true`, `rendered=true`, `count=2`, compact geometry, zero page errors, and only desktop WebGL ReadPixels screenshot warnings. Visual inspection first caught an over-large/too-low placement and an intro-stinger false positive; the final steady-state crop confirmed the log frame sits above the hand cards, keeps text readable, and preserves the battle HUD without covering card names. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`, showing a healthy title screen with generated title/menu chrome. The temporary built preview on port 4217 should be stopped after this note; Bird Squad remains live on port 5199.

Current follow-up: Combat Hand Card Frame core-card polish. Generated a transparent-ready vertical combat hand-card frame with built-in imagegen on a flat green chroma-key background; prompt type was `stylized-concept` for a tall 2:3 open-center playable-card frame with brass/gunmetal hardware, cyan signal ticks, magenta feather inlays, rooftop salvage corners, and no text. Preserved the source at `assets/concept-art/ui/icons/sources/combat-hand-card-frame-source-v1.png` (1024x1536 RGB, 1,731,035 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/optimized it into `assets/runtime/ui/icons/combat-hand-card-frame.png` (512x768 RGBA, 251,209 bytes). Alpha processing detected key `#06f907`; the raw keyed output at `.artifacts/test-results/combat-hand-card-frame/combat-hand-card-frame-keyed-raw.png` has 1,244,033 transparent pixels, 13,534 partial-alpha pixels, 315,297 opaque pixels, visible bounds `(43, 42, 981, 1457)`, transparent corners, an open center, and zero green-ish visible pixels. The optimized runtime sprite has 304,156 transparent pixels, 17,336 partial-alpha pixels, 71,724 opaque pixels, visible bounds `(19, 19, 493, 731)`, an open center, transparent corners, and 12 conservative green-ish heuristic pixels with no visible chroma fringe in inspection. `src/main.ts` now registers/preloads `combat-hand-card-frame`, layers it between hand-card art and the existing text/cost overlays for every BattleScene hand card, keeps the original rectangle hit targets and text panels, tracks frame objects per hand card so enemy-target selection can enlarge/brighten the selected card without a full redraw, and reports `combatHandCardFrame.loaded/rendered/count` through battle text state. The new smoke `combat hand cards render generated playable frames` asserts the frame texture loads, renders at least once per hand card, matches telemetry, and grows from 180x270 alpha 0.78 to 188x282 alpha 0.96 when a playable enemy-target card is selected. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "combat hand cards render generated playable frames" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed apart from existing LF-to-CRLF warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `combat-hand-card-frame-DZB1e2Tt.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, desktop/landscape close crops, raw keyed output, and `capture-summary.json` are in `.artifacts/test-results/combat-hand-card-frame/`; final captures at 2560x1600, 844x390, and 390x844 reported `combatHandCardFrame.loaded=true`, `rendered=true`, `count=5`, `combatHandRail.count=1`, selected-frame enlargement, zero console errors, and zero page errors. Visual inspection confirmed the generated frame makes the core combat hand read like crafted physical card hardware without covering card names, costs, art, or effect text; the fixed-canvas portrait mobile letterbox remains coherent. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`, showing a healthy title screen with generated title/menu chrome. The temporary built preview on port 4217 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Run Kit Item Tile Frame inventory-drawer polish. Generated a transparent-ready compact inventory item tile frame with built-in imagegen on a flat green chroma-key background; prompt type was `stylized-concept` for a premium horizontal field-dossier tile frame with an open center, left icon well, brass/gunmetal edges, cyan signal ticks, magenta feather inlays, and no text. Preserved the source at `assets/concept-art/ui/icons/sources/run-kit-item-tile-frame-source-v1.png` (1971x798 RGB, 1,445,220 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/palette-optimized it into `assets/runtime/ui/icons/run-kit-item-tile-frame.png` (512x224 RGBA, 82,285 bytes). Alpha processing detected key `#06f80b`; the raw keyed output at `.artifacts/test-results/run-kit-item-tile-frame/run-kit-item-tile-frame-keyed-raw.png` has 1,201,861 transparent pixels, 8,080 partial-alpha pixels, 362,917 opaque pixels, visible bounds `(121, 179, 1854, 602)`, and zero green-ish visible pixels, while the optimized runtime sprite has transparent corners, 81,229 transparent pixels, 6,104 partial-alpha pixels, 27,355 opaque pixels, visible bounds `(3, 48, 509, 175)`, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `run-kit-item-tile-frame`, layers it into the shared compact run-kit item tile renderer for RouteScene and BattleScene Waymark/Supply drawers while preserving the existing rectangle hit targets and disabled-item states, nudges tile text down 4px so the generated top rail does not crowd item names, and reports `runKitItemTileFrame.loaded/rendered/count` through route and battle text state. The focused drawer smoke now asserts generated tile-frame loading, per-drawer frame counts, and telemetry for route Waymarks (3), route Supplies (2 filled supply tiles), battle Waymarks (3), and battle Supplies (2). Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "run kit drawers render generated inventory flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed apart from existing LF-to-CRLF warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `run-kit-item-tile-frame-DUip3ZCa.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, close crops, raw keyed output, and `capture-summary.json` are in `.artifacts/test-results/run-kit-item-tile-frame/`; final captures at 2560x1600, 844x390, and 390x844 for route/battle Waymark/Supply drawers reported generated frames loaded and rendered with zero console errors and zero page errors. Visual inspection confirmed the generated frame adds cohesive salvage-dossier hardware without covering item icons, names, summaries, or `USE`/timing badges; the fixed-canvas portrait mobile letterbox remains coherent. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`, showing a healthy title screen with title/menu generated chrome. The temporary built preview on port 4217 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Audio Toggle Pulse Ring menu/SFX polish. Generated a transparent-ready circular audio toggle pulse ring with built-in imagegen using a flat green chroma-key background; prompt type was `stylized-concept` for a premium brass/gunmetal/cyan/magenta UI FX overlay with an open center and no text. Preserved the source at `assets/concept-art/ui/icons/sources/audio-toggle-pulse-ring-source-v1.png` (1254x1254 RGB, 1,971,768 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and optimized it into `assets/runtime/ui/icons/audio-toggle-pulse-ring.png` (256x256 RGBA, 77,083 bytes). Alpha processing detected key `#0af308`; the raw keyed output at `.artifacts/test-results/audio-toggle-pulse-ring/audio-toggle-pulse-ring-keyed-raw.png` has 1,087,490 transparent pixels, 14,170 partial-alpha pixels, visible bounds `(59, 55, 1195, 1188)`, and zero green-ish visible pixels, while the optimized runtime sprite has transparent corners, 42,707 transparent pixels, 4,845 partial-alpha pixels, 17,984 opaque pixels, visible bounds `(7, 7, 249, 249)`, and only three conservative green-ish heuristic pixels with no visible fringe in inspection. `src/main.ts` now registers/preloads `audio-toggle-pulse-ring`, layers it behind the shared audio toggle on title/menu, route, and battle surfaces, pulses it on hover/toggle with reduced visual dominance, and reports `audioTogglePulseRing.loaded/rendered/count` through menu, route, and battle text state. The title utility icon smoke now asserts the generated pulse ring texture is loaded and renders exactly once. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, post-build captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `audio-toggle-pulse-ring-C8SI2NmF.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, idle/pressed close crops, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/audio-toggle-pulse-ring/`; captures at 2560x1600, 844x390, and 390x844 reported `loaded=true`, `rendered=true`, `count=1`, mute toggling from false to true, zero console errors, and zero page errors. Visual inspection confirmed the ring reads as a small premium audio-state halo without disturbing title navigation, leader cards, or the fixed-canvas portrait mobile letterbox. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`; the title screen remains healthy.

Current follow-up: Market Detail Dossier Frame hover-tooltip polish. Generated a transparent-ready market item detail dossier frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-detail-dossier-frame-source-v1.png` (1024x1536 RGB, 1,747,368 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/market-detail-dossier-frame.png` (385x512 RGBA, 165,899 bytes). Alpha processing detected key `#12f119`; the raw keyed output at `.artifacts/test-results/market-detail-dossier-frame/market-detail-dossier-frame-keyed-raw.png` has 1,140,504 transparent pixels, 10,529 partial-alpha pixels, 421,831 opaque pixels, 27.49% visible coverage, and zero green-ish visible pixels, while the optimized runtime sprite has transparent corners, 128,086 transparent pixels, 12,510 partial-alpha pixels, 56,524 opaque pixels, 35.02% visible coverage, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `market-detail-dossier-frame`, renders it as a normal layer plus faint additive glint behind shared non-card market/reward item detail hovers, raises that detail panel above the market overlay, and tightens the price/scrap-icon lane so three-digit prices remain readable. `render_game_to_text` now exposes `marketItemDetailFrame.loaded/rendered/count`, and the market item detail smoke asserts the generated frame loads and renders at least two layers. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "market item detail surfaces non-card offer stats" --workers=1 --reporter=line`, focused `npx playwright test ./tests/smoke.spec.ts --grep "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview pointer-hover captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `market-detail-dossier-frame-oEBHKf3k.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, close crop, raw keyed output, coordinate check, and `capture-summary.json` are in `.artifacts/test-results/market-detail-dossier-frame/`; final captures at 2560x1600, 844x390, and 390x844 used real pointer hover on the first Waymark offer and reported `marketItemDetailFrame.loaded=true`, `rendered=true`, `count=2`, `marketOpen=true`, zero console errors, and zero page errors. Visual inspection confirmed the generated brass/gunmetal/cyan/magenta dossier frame reads clearly over the dense market scene, keeps title/body/meta text inside the frame, leaves the offer shelves readable, and preserves fixed-canvas portrait mobile coherence. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`; the title screen remains healthy. The temporary built preview on port 4217 was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Market Offer Tray shelf-decision polish. Generated a transparent-ready reusable market offer tray with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-offer-tray-source-v1.png` (1774x887 RGB, 1,450,899 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/market-offer-tray.png` (512x193 RGBA, 85,242 bytes). Alpha processing detected key `#0ef713`; the raw keyed output at `.artifacts/test-results/market-offer-tray/market-offer-tray-keyed-raw.png` has 1,227,164 transparent pixels and 9,273 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 26,201 opaque pixels, 8,567 partial-alpha pixels, 35.18% visible coverage, and two conservative green-ish pixels by heuristic with no visible fringe in inspection. `src/main.ts` now registers/preloads `market-offer-tray`, renders a normal layer plus faint additive glint behind market card price tags, Waymark labels, supply labels, service buttons, and the refresh service, and exposes `market.offerTray.loaded/rendered/count` through `render_game_to_text`. The market overlay smoke now waits for the generated tray texture and asserts loaded/rendered telemetry with at least 10 tray layers. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock multiple finite offers and paid refreshes|late markets sell premium boss-prep and route-scout services|market preen service lets the player choose a card before paying|market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `market-offer-tray-BUsd17ua.png` in `.artifacts/build/assets/`. Screenshots, contact sheet, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/market-offer-tray/`; captures at 2560x1600, 844x390, and 390x844 reported `market.offerTray.loaded=true`, `rendered=true`, `count=26`, `marketOpen=true`, zero console errors, and zero page errors. Visual inspection confirmed the generated trays add cohesive salvage-market shelf hardware around offer prices and service labels without covering card art, item names, the shopkeeper, or the close button; the fixed-canvas portrait mobile letterbox remains coherent. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`; the title screen remains healthy. The temporary built preview used port 4217 and was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Card Picker Frame deck-tuning overlay polish. Generated a transparent-ready wide card-picker workbench frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/card-picker-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and optimized it into `assets/runtime/ui/icons/card-picker-frame.png` (1024x560 RGBA, 303,590 bytes). Alpha processing detected key `#0bf30c`; the raw keyed output at `.artifacts/test-results/card-picker-frame/card-picker-frame-keyed-raw.png` has 1,260,091 transparent pixels and 9,238 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 102,646 opaque pixels, 18,643 partial-alpha pixels, 21.15% visible coverage, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `card-picker-frame`, renders it as a normal layer plus faint additive glint around the route and market preen/release card picker overlay, and exposes `cardPickerFrame.loaded/rendered/count/mode/context` through `render_game_to_text`. The route and market picker smokes now wait for and assert generated-frame telemetry in both contexts. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market preen service lets the player choose a card before paying|route preen picker keeps large decks inside a two-row viewport"`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `card-picker-frame-C1Du3-BQ.png` in `.artifacts/build/assets/`. Screenshots and contact sheet are in `.artifacts/test-results/card-picker-frame/`, including route/market desktop and mobile captures with `cardPickerFrame.loaded=true`, `rendered=true`, `count=2`, and the expected `route`/`market` contexts. Visual inspection confirmed the generated frame gives deck tuning a premium workbench feel without covering title text, card art, or button labels; the fixed-canvas portrait mobile letterbox remains coherent. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`; the title screen remains healthy. The temporary built preview used port 4217 and was stopped after verification; Bird Squad remains live on port 5199.

Current follow-up: Reward Choice Card Frame ceremony-choice polish. Generated a transparent-ready vertical reward-card frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-choice-card-frame-source-v1.png` (1024x1536 RGB, 1,641,472 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and downsampled it into `assets/runtime/ui/icons/reward-choice-card-frame.png` (512x768 RGBA, 221,139 bytes). Alpha processing detected key `#04f805`; the raw keyed output at `.artifacts/test-results/reward-choice-frame/reward-choice-card-frame-keyed-raw.png` has 1,294,963 transparent pixels and 9,590 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 60,603 opaque pixels, 14,979 partial-alpha pixels, and 19.22% visible coverage. `src/main.ts` now registers/preloads `reward-choice-card-frame`, renders it as generated brass/gunmetal/cyan/magenta chrome around card rewards, Waymark rewards, and preen/upgrade choices with a reduced-motion-safe pulse, and exposes `rewardChoiceCardFrame.loaded/rendered/count` through `render_game_to_text`. The reward ceremony smoke now asserts the generated frame loads and renders at least three times across card reward, Waymark reward, and upgrade reward modes. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "post-combat reward screens render generated choice ceremony art"`, `npm run validate:runtime-assets`, `npm run build`, dev captures on `http://127.0.0.1:5199/?playwright=1`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `reward-choice-card-frame-*.png` in `.artifacts/build/assets/`. Screenshots and runtime outputs are in `.artifacts/test-results/reward-choice-frame/`, including 2560x1600 and 390x844 dev/built captures with `rewardChoiceCardFrame.loaded=true`, `rendered=true`, `count=3`, and one ceremony backdrop. Visual inspection confirmed the generated frame makes reward choices read as premium reward cards without covering card art or text; the fixed-canvas portrait mobile letterbox remains coherent. Note: local port `5173` is currently another game; Bird Squad is live on `5199`, and the temporary built preview on `4217` was stopped after verification.

Current follow-up: Codex Entry Frame browser-grid polish. Generated a transparent-ready Codex browser entry frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-entry-frame-source-v1.png` (1536x1024 RGB, 1,667,001 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/codex-entry-frame.png` (768x448 RGBA, 230,246 bytes) for reusable Codex card/item/leader/glossary/enemy cells. Chroma processing detected key `#06f80c`; the raw keyed output at `.artifacts/test-results/codex-entry-frame/codex-entry-frame-keyed-raw.png` has 1,265,685 transparent pixels, 10,031 partial-alpha pixels, 297,148 opaque pixels, visible bounds `(95, 100, 1439, 917)`, zero green-ish visible pixels, and a fully open 120x90 center, while the optimized runtime sprite has 257,682 transparent pixels, 14,672 partial-alpha pixels, 71,710 opaque pixels, visible bounds `(39, 14, 728, 433)`, zero green-ish visible pixels, and a fully open center. `src/main.ts` now registers/preloads `codex-entry-frame`, adds `addCodexEntryFrame()`, renders the generated brass/gunmetal/cyan/magenta underlay behind Codex card thumbnails, supplies, waymarks, leader roster cells, glossary terms, and enemy entries while preserving existing hit rectangles/fallbacks, and exposes `codexEntryFrame.loaded/rendered/count` through `render_game_to_text`. `tests/smoke.spec.ts` now includes `codex: browser entries render generated frame art across sections`, which checks Cards, Items/Supplies, Leaders, Glossary, and Enemies for loaded texture, visible named frame objects, sane display sizes, and telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: browser entries render generated frame art across sections" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `codex-entry-frame-C-fOg0hB.png` in `.artifacts/build/assets/`. Screenshots, capture script, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/codex-entry-frame/`; captures at 2560x1600 and 390x844 confirmed the generated frame renders across all five tested Codex browser modes with zero console errors and zero page errors. Visual inspection confirmed the final lower-alpha tuning keeps the Codex browser feeling like a crafted field dossier while preserving text readability in short glossary rows, dense supply cards, locked card thumbnails, and leader roster cells. The required web-game client refreshed `output/web-game/shot-0.png` and `output/web-game/state-0.json`; the title screen remains healthy. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Route Node Tooltip Frame route-preview polish. Generated a transparent-ready wide node tooltip frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-node-tooltip-frame-source-v1.png` (1862x845 RGB, 1,527,406 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-node-tooltip-frame.png` (512x160 RGBA, 127,265 bytes) for RouteScene node hover previews. Chroma processing detected key `#0eef0b`; the raw keyed output at `.artifacts/test-results/route-node-tooltip-frame/route-node-tooltip-frame-keyed-raw.png` has 861,487 transparent pixels, 5,650 partial-alpha pixels, 706,253 opaque pixels, visible bounds `(132, 173, 1724, 658)`, and zero green-ish visible pixels, while the optimized runtime sprite has 9,055 transparent pixels, 4,823 partial-alpha pixels, 68,042 opaque pixels, visible bounds `(1, 1, 511, 159)`, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `route-node-tooltip-frame`, renders it behind RouteScene node hover titles and reward badges as a 76px-high generated brass/gunmetal/cyan/magenta frame when available, keeps the old procedural rectangle fallback, and exposes `routeNodeTooltipFrame.loaded/rendered/count` through `render_game_to_text`. The focused route smoke now waits for the generated tooltip texture, opens boss and cache tooltips, and asserts two visible `route-node-tooltip-frame` images with 76px height, at least 244px width, alpha above 0.85, and matching telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, built-preview route captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `route-node-tooltip-frame-CqRn272S.png` in `.artifacts/build/assets/`. Screenshots, capture script, raw keyed output, and state summary are in `.artifacts/test-results/route-node-tooltip-frame/`, including 2560x1600 and 390x844 built captures with `routeNodeTooltipFrame.loaded=true`, `rendered=true`, `count=2`, zero console errors, and zero page errors. Visual inspection confirmed the generated frames sit cleanly behind cache and boss preview labels/reward icons, match the existing route-map frame/selected-ring art direction, avoid text overflow, and remain legible in the fixed-canvas mobile letterbox. The required web-game client capture at `output/web-game/shot-0.png` refreshed successfully and still shows a healthy title screen. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Route Map Frame run-board polish. Generated a transparent-ready wide route-map outer frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-map-frame-source-v1.png` (1672x941 RGB, 1,534,154 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-map-frame.png` (1024x500 RGBA, 341,791 bytes) to match the RouteScene 1208x590 map panel aspect. Chroma processing detected key `#05f806`; the raw keyed output at `.artifacts/test-results/route-map-frame/route-map-frame-keyed-raw.png` has 1,220,939 transparent pixels, 10,755 partial-alpha pixels, visible bounds `(25, 25, 1646, 916)`, zero green-ish visible pixels, and an open 160x120 center, while the optimized runtime sprite has 383,679 transparent pixels, 18,855 partial-alpha pixels, 109,466 opaque pixels, visible bounds `(4, 3, 1020, 497)`, zero green-ish visible pixels, and an open 160x120 center. `src/main.ts` now registers/preloads `route-map-frame`, renders it as the generated outer board chrome in `renderRouteMapFrame` at 1208x590 alpha 0.78 when the texture exists, keeps the old procedural line frame as a texture-load fallback, and exposes `routeMapFrame.loaded/rendered/count` through `render_game_to_text`. The focused route smoke now waits for the generated texture, asserts exactly one rendered frame, and checks its name, visibility, alpha, and 1208x590 geometry alongside the route boss preview and selected-node checks. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, built-preview route captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `route-map-frame-HtSWwsK_.png` in `.artifacts/build/assets/`. Screenshots, capture script, raw keyed output, and state summary are in `.artifacts/test-results/route-map-frame/`, including 2560x1600 and 390x844 built captures with `routeMapFrame.loaded=true`, `rendered=true`, `count=1`, frame geometry 1208x590 at alpha 0.78, zero console errors, and zero page errors. Visual inspection confirmed the generated frame makes the route board read as a cohesive field instrument panel, stays behind the node grid and selected-node ring, preserves the district banner and bottom boss-prep/route controls, and remains legible in the fixed-canvas mobile letterbox. The required web-game client capture at `output/web-game/shot-0.png` refreshed successfully and still shows a healthy title screen. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Route Selected Node Ring map-selection polish. Generated a transparent-ready brass/gunmetal/cyan/magenta selected-node ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-selected-node-ring-source-v1.png` (1254x1254 RGB, 1,951,376 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/route-selected-node-ring.png` (256x256 RGBA, 82,987 bytes). Chroma processing detected key `#0aef0e`; the raw keyed output at `.artifacts/test-results/route-selected-node-ring/route-selected-node-ring-keyed-raw.png` has 1,086,259 transparent pixels, 12,742 partial-alpha pixels, visible bounds `(111, 114, 1142, 1136)`, zero green-ish visible pixels, and an open 60x60 center, while the optimized runtime sprite has 40,111 transparent pixels, 7,377 partial-alpha pixels, 18,048 opaque pixels, visible bounds `(14, 15, 242, 241)`, one conservative green-ish pixel by heuristic, and an open 60x60 center. `src/main.ts` now registers/preloads `route-selected-node-ring`, renders it as a generated 132x132 selected-node focus image when the texture exists with the previous graphics bracket retained as fallback, and exposes `routeSelectedNodeRing.loaded/rendered/count` through `render_game_to_text`. The focused route smoke now waits for the generated texture, asserts it renders exactly once, checks its visible object name/alpha/geometry, and keeps the boss preview coverage intact. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview route captures on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `route-selected-node-ring-DKY0JLFw.png` in `.artifacts/build/assets/`. Screenshots, close crops, capture script, raw keyed output, and state summary are in `.artifacts/test-results/route-selected-node-ring/`, including 2560x1600 and 390x844 built captures with `routeSelectedNodeRing.loaded=true`, `rendered=true`, `count=1`, frame geometry 132x132 at alpha 0.82, zero console errors, and zero page errors. Visual inspection confirmed the generated ring reads as route-map chrome around the selected entry node, leaves the node icon readable, and shows no visible chroma fringe. The required web-game client capture at `output/web-game/shot-0.png` still shows the title screen loaded cleanly with title asset telemetry present. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Title Logo Backplate launch-screen brand-anchor polish. Generated a transparent-ready wide rooftop-salvage title backplate with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-logo-backplate-source-v1.png` (1935x813 RGB, 1,924,271 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/title-logo-backplate.png` (768x288 RGBA, 272,684 bytes). Chroma processing detected key `#04f904`; the raw keyed output at `.artifacts/test-results/title-logo-backplate/title-logo-backplate-keyed-raw.png` has 882,116 transparent pixels, 9,636 partial-alpha pixels, visible bounds `(86, 126, 1848, 671)`, and no visible green fringe in inspection, while the optimized runtime sprite has 104,806 transparent pixels, 9,250 partial-alpha pixels, 107,128 opaque pixels, visible bounds `(24, 31, 744, 257)`, and zero green-ish visible pixels by the edge heuristic. `src/main.ts` now registers/preloads `title-logo-backplate`, renders it inside the animated title-logo container behind the existing `title-bird`/`title-squad` images as a 456x171 alpha 0.34 backplate, and exposes recursive `titleLogoBackplate.loaded/rendered/count` telemetry through `render_game_to_text`. The mobile title smoke now includes the nested backplate in its expected first-screen icon list, uses recursive texture collection for nested title-container art, and asserts exactly one visible 456x171 `title-logo-backplate` image. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview capture script on `http://127.0.0.1:4217/?playwright=1`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `title-logo-backplate-g9MpzDfn.png` in `.artifacts/build/assets/`. Screenshots, close crops, capture script, raw keyed output, and state summary are in `.artifacts/test-results/title-logo-backplate/`, including 2560x1600 and 390x844 built captures with `titleLogoBackplate.loaded=true`, `rendered=true`, `count=1`, frame geometry 456x171 at alpha 0.34, zero console errors, and zero page errors. Visual inspection confirmed the generated backplate gives the `BIRD SQUAD` mark a subtle premium title-screen anchor without covering the gold logo, crowding the top utility buttons, or harming mobile readability. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Title Ascension Status Strip launch-screen difficulty-info polish. Generated a transparent-ready narrow difficulty info underlay with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-ascension-status-strip-source-v1.png` (1983x793 RGB, 1,119,629 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/title-ascension-status-strip.png` (512x96 RGBA, 39,723 bytes). Chroma processing detected key `#06ef0d`; the raw keyed output at `.artifacts/test-results/title-ascension-status-strip/title-ascension-status-strip-keyed-raw.png` has 1,315,195 transparent pixels, 4,838 partial-alpha pixels, 252,486 opaque pixels, visible bounds `(140, 316, 1843, 478)`, and zero green-ish visible pixels, while the optimized runtime strip has 26,901 transparent pixels, 3,482 partial-alpha pixels, 18,769 opaque pixels, visible bounds `(11, 22, 501, 73)`, and only two green-ish pixels by the conservative cyan/green heuristic with no visible fringe in inspection. `src/main.ts` now registers/preloads `title-ascension-status-strip`, renders it as a restrained 306x58 alpha 0.36 generated underlay behind the Ascension modifier/reward row while preserving the existing difficulty arrows and text stack, and exposes `titleAscensionStatusStrip.loaded/rendered/count` through `render_game_to_text`. The mobile title smoke now includes the texture in the first-screen icon list and asserts exactly one visible 306x58 `title-ascension-status-strip` image. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview capture script on `http://127.0.0.1:4217/?playwright=1`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and local health checks for ports 5173/5199/4217 passed. Production output includes `title-ascension-status-strip-CJ36_7xa.png` in `.artifacts/build/assets/`. Screenshots, close crops, capture script, raw keyed output, and state summary are in `.artifacts/test-results/title-ascension-status-strip/`, including 2560x1600 and 390x844 built captures with `titleAscensionStatusStrip.loaded=true`, `rendered=true`, `count=1`, frame geometry 306x58 at alpha 0.36, zero console errors, and zero page errors. Visual inspection confirmed the generated backing improves the modifier/reward line readability without crowding the Ascension plaque, arrow steppers, or Start Run dock. The temporary built preview used port 4217; the Bird Squad dev servers remain live on ports 5173 and 5199.

Current follow-up: Title Leader Header Frame launch-screen selection polish. Generated a transparent-ready leader selection header rail with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-leader-header-frame-source-v1.png` (1983x793 RGB, 1,400,972 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/title-leader-header-frame.png` (768x160 RGBA, 149,637 bytes). Chroma processing detected key `#04f809`; the raw keyed output at `.artifacts/test-results/title-leader-header-frame/title-leader-header-frame-keyed-raw.png` has 1,080,998 transparent pixels, 4,780 partial-alpha pixels, visible bounds `(76, 250, 1907, 534)`, and zero green-ish visible pixels, while the optimized runtime sprite has 79,726 visible pixels, 74,726 opaque pixels, 5,387 partial-alpha pixels, visible bounds `(17, 21, 751, 138)`, and one green-ish pixel by the conservative cyan/green heuristic with no visible chroma fringe in inspection. `src/main.ts` now registers/preloads `title-leader-header-frame`, renders it behind the existing `Choose your Flock Leader` title prompt as a restrained 386x80 frame at alpha 0.52, keeps the existing medallion/text and leader-card interactions, and exposes `titleLeaderHeaderFrame.loaded/rendered/count` through `render_game_to_text`. The mobile title smoke now includes the new texture in the first-screen icon list and asserts exactly one visible 386x80 `title-leader-header-frame` image. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview capture script, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `title-leader-header-frame-BynqefR-.png`. Screenshots, capture script, raw keyed output, and state summaries are in `.artifacts/test-results/title-leader-header-frame/`, including 2560x1600 and 390x844 built captures with `titleLeaderHeaderFrame.loaded=true`, `rendered=true`, `count=1`, frame geometry 386x80 at alpha 0.52, zero console errors, and zero page errors. Visual inspection confirmed the generated rail gives the leader prompt a proper title-screen anchor, stays readable over the dark center channel, and does not crowd the leader card row on desktop or mobile letterbox. The temporary built preview used port 4217; the Bird Squad dev server remains live on ports 5173 and 5199.

Current follow-up: Title Ascension Stepper Frame launch-screen control polish. Generated a transparent-ready square brass/gunmetal/cyan/magenta frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-ascension-stepper-frame-source-v1.png` (1254x1254 RGB, 1,425,032 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/title-ascension-stepper-frame.png` (256x256 RGBA, 85,918 bytes). Chroma processing detected key `#06f80f`; the raw keyed output at `.artifacts/test-results/title-ascension-stepper-frame/title-ascension-stepper-frame-keyed-raw.png` has 1,026,193 transparent pixels, 3,987 partial-alpha pixels, visible bounds `(246, 238, 1008, 988)`, and zero green-ish visible pixels, while the optimized runtime sprite has 49,616 visible pixels, 46,980 opaque pixels, 2,636 partial-alpha pixels, visible bounds `(12, 14, 244, 242)`, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `title-ascension-stepper-frame`, renders two 46x46 generated frames behind the Ascension left/right stepper arrows while preserving the existing rectangle hit targets and difficulty click behavior, adds hover alpha lift, and exposes `titleAscensionStepperFrame.loaded/rendered/count` through `render_game_to_text`. The mobile title smoke now includes the new texture in the first-screen icon list and asserts exactly two visible 46x46 `title-ascension-stepper-frame` images. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview capture script, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `title-ascension-stepper-frame-BmerHeku.png`. Screenshots, capture script, raw keyed output, and state summaries are in `.artifacts/test-results/title-ascension-stepper-frame/`, including 2560x1600 and 390x844 built captures with `titleAscensionStepperFrame.loaded=true`, `rendered=true`, `count=2`, both frame objects at 46x46 alpha 0.72, zero console errors, and zero page errors. Visual inspection confirmed the generated arrow frames now match the title plaque/start/control chrome without crowding the Ascension copy or Start Run dock. The temporary built preview used port 4217; the Bird Squad dev server remains live on ports 5173 and 5199.

Current follow-up: Title Leader Tooltip Frame first-screen hover polish. Generated a transparent-ready horizontal leader tooltip frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-leader-tooltip-frame-source-v1.png` (1,359,494 bytes, 1983x793), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/title-leader-tooltip-frame.png` (768x224, 189,136 bytes). Chroma processing detected key `#04f90a`; the raw keyed output at `.artifacts/test-results/title-leader-tooltip-frame/title-leader-tooltip-frame-keyed-raw.png` has 898,399 transparent pixels, 6,979 partial-alpha pixels, visible bounds `(111, 158, 1870, 592)`, and zero green-ish visible pixels, while the optimized runtime sprite has 118,161 visible pixels, 111,602 opaque pixels, 6,559 partial-alpha pixels, visible bounds `(17, 19, 750, 203)`, and 16 green-ish pixels by the conservative cyan/green heuristic with no visible chroma fringe in inspection. `src/main.ts` now registers/preloads `title-leader-tooltip-frame`, renders it inside the title leader hover tooltip as a generated 704x126 brass/cyan/magenta information plaque, keeps the existing selection/hover behavior, and exposes `titleLeaderTooltipFrame.loaded/rendered/count` through `render_game_to_text`. The title smoke now verifies the texture is loaded but not rendered by default on mobile, then opens a leader tooltip and asserts the generated frame renders with telemetry, geometry, leader name, and signature copy. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title leader hover tooltip renders generated frame art|title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `title-leader-tooltip-frame-BseYI8J8.png`. Screenshots, capture spec/config, raw keyed output, and desktop/mobile state summaries are in `.artifacts/test-results/title-leader-tooltip-frame/`, including 2560x1600 and 390x844 built captures with `titleLeaderTooltipFrame.loaded=true`, `rendered=true`, `count=1`, frame geometry 704x126 at alpha 0.78, zero console errors, and zero page errors. Visual inspection confirmed the tooltip now matches the title card/button chrome, keeps text readable in the dark center, and clears the leader row on desktop and mobile letterbox. The temporary built preview used port 4217; the Bird Squad dev server remains live on port 5199.

Current follow-up: Profile Return Command Frame Flock Record footer polish. Generated a transparent-ready bottom command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-return-command-frame-source-v1.png` (1,310,347 bytes, 1881x836), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/profile-return-command-frame.png` (512x160, 91,183 bytes). Chroma processing used key `#08ec20`; the raw keyed output at `.artifacts/test-results/profile-return-command-frame/profile-return-command-frame-keyed-raw.png` has 1,040,191 transparent pixels, 4,247 partial-alpha pixels, visible bounds `(208, 222, 1670, 604)`, and zero green-ish visible pixels, while the optimized runtime sprite has 57,964 visible pixels, 54,197 opaque pixels, 3,767 partial-alpha pixels, visible bounds `(15, 15, 496, 143)`, and zero green-ish visible pixels. `src/main.ts` now registers/preloads `profile-return-command-frame`, replaces the old plain Flock Record footer button with a generated 220x64 Return command frame plus transparent hit target, keeps hover/down behavior and the existing scene transition, and exposes `profileReturnCommandFrame.loaded/rendered/count` through `render_game_to_text`. The focused Profile smoke now asserts the generated texture loads, telemetry matches the scene object count, and exactly one Return command frame renders at the expected geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders are gated until unlocked, and the Profile screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `profile-return-command-frame-Br2jzNZa.png`. Screenshots, capture spec/config, raw keyed output, and desktop/mobile state summaries are in `.artifacts/test-results/profile-return-command-frame/`, including 2560x1600 and 390x844 built captures with `profileReturnCommandFrame.loaded=true`, `rendered=true`, `count=1`, frame geometry 220x64 at alpha 0.68, zero console errors, and zero page errors. Visual inspection confirmed the Return control now matches the brass/cyan/magenta Flock Record chrome, stays readable, and clears the list content on desktop and mobile letterbox. The temporary built preview used port 4217; the Bird Squad dev server remains live on ports 5173 and 5199.

Current follow-up: Profile Progress Rail Frame Flock Record rail polish. Generated a transparent-ready slim progress meter backing with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-progress-rail-frame-source-v1.png` (1,004,424 bytes, 2172x724), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/profile-progress-rail-frame.png` (768x96, 48,337 bytes). Chroma processing detected key `#06f810`; the raw keyed output at `.artifacts/test-results/profile-progress-rail-frame/profile-progress-rail-frame-keyed-raw.png` had 1,337,457 transparent pixels and 4,853 partial-alpha pixels, while the optimized runtime strip has 31,757 visible pixels, 27,067 opaque pixels, 4,690 partial-alpha pixels, visible bounds `(14, 23, 754, 72)`, and zero green-ish visible pixels by the edge heuristic. `src/main.ts` now registers/preloads `profile-progress-rail-frame`, renders it behind the three Flock Record progress rails while keeping the colored fill bars readable inside the dark channel, and exposes `profileProgressRailFrame.loaded/rendered/count` through `render_game_to_text`. The focused Profile smoke now asserts the generated texture loads, telemetry matches scene objects, and exactly three progress rail frames render at the expected geometry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders are gated until unlocked, and the Profile screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, built-preview captures, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `profile-progress-rail-frame-C2ooakXy.png`. Screenshots, capture spec/config, raw keyed output, and desktop/mobile state summaries are in `.artifacts/test-results/profile-progress-rail-frame/`, including 2560x1600 and 390x844 built captures with `profileProgressRailFrame.loaded=true`, `rendered=true`, `count=3`, rail frame geometry 332x34 at alpha 0.48, zero console errors, and zero page errors. Visual inspection confirmed the rails now match the brass/cyan record chrome without swallowing labels or fill bars, and the mobile letterboxed Profile panel remains readable. The temporary built preview used port 4217; the Bird Squad dev server remains live on ports 5173 and 5199.

Current follow-up: Profile Record Row Frame Flock Record list polish. Generated a transparent-ready long/shallow row frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-record-row-frame-source-v1.png` (1,123,932 bytes, 1942x809), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into `assets/runtime/ui/icons/profile-record-row-frame.png` (768x128, 92,521 bytes). Chroma processing detected key `#09f811`; the raw keyed output at `.artifacts/test-results/profile-record-row-frame/profile-record-row-frame-keyed-raw.png` had 1,179,939 transparent pixels and 4,472 partial-alpha pixels, while the optimized runtime sprite has 63,971 visible pixels, 58,878 opaque pixels, 5,093 partial-alpha pixels, visible bounds `(17, 15, 751, 113)`, and zero green-ish visible pixels by the edge heuristic. `src/main.ts` now registers/preloads `profile-record-row-frame`, replaces the plain Flock Leaders and Achievement Badges list rectangles with generated row backing art, tints/dims locked or unearned rows, and exposes `profileRecordRowFrame.loaded/rendered/count` through `render_game_to_text`. The focused Profile smoke now asserts the row frame texture loads, telemetry matches scene objects, and exactly 13 row frames render for 5 leaders plus 8 achievements. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders are gated until unlocked, and the Profile screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `profile-record-row-frame-Df9kjyuI.png`. Screenshots, capture spec/config, raw keyed output, and desktop/mobile state summaries are in `.artifacts/test-results/profile-record-row-frame/`, including 2560x1600 and 390x844 built captures with `profileRecordRowFrame.loaded=true`, `rendered=true`, `count=13`, active row geometry 310x40 / 314x43 at alpha 0.44, locked/unearned row alpha 0.22, zero console errors, and zero page errors. Visual inspection caught an initial 0.58 alpha / taller row treatment that made the dense record list feel too banner-heavy; the final tuned treatment reads as a premium backing rail without fighting the large profile-record flourish or the stat chip frames. The temporary built preview used port 4217; the Bird Squad dev server remains live on ports 5173 and 5199.

Current follow-up: Profile Stat Chip Frame Flock Record meta-progression polish. Generated a transparent-ready compact Flock Record stat chip frame with built-in imagegen on a flat green chroma-key background; the first generated candidate was too panoramic for the five-chip row, so the preserved project source used for runtime is `assets/concept-art/ui/icons/sources/profile-stat-chip-frame-source-v2.png` (1,477,935 bytes, 1897x829). Removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`; chroma processing detected key `#06f80a`, with 1,001,526 transparent pixels and 5,780 partial-alpha pixels in the raw keyed output at `.artifacts/test-results/profile-stat-chip-frame/profile-stat-chip-frame-v2-keyed-raw.png`. Cropped/downsampled the keyed art into `assets/runtime/ui/icons/profile-stat-chip-frame.png` (512x224, 137,234 bytes) with resized content 488x198, 71,670 visible pixels, 66,826 opaque pixels, 4,844 partial-alpha pixels, visible bounds `(18, 17, 494, 206)`, and only 3 green-ish edge pixels by a conservative heuristic; visual inspection showed no obvious chroma fringe. `src/main.ts` now registers/preloads `profile-stat-chip-frame`, renders it behind each Flock Record top-row stat chip as a restrained 142x54 normal image plus a 146x56 low-alpha additive glint, and exposes `profileStatChipFrame.loaded/rendered/count` through `render_game_to_text`. The existing ProfileScene smoke now asserts the generated texture loads, renders ten frame images for five chips, and matches telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders are gated until unlocked, and the Profile screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `profile-stat-chip-frame-0hugbIuI.png`. Screenshots, capture spec/config, raw keyed outputs, and state summaries are in `.artifacts/test-results/profile-stat-chip-frame/`, including 2560x1600 and 390x844 built captures with `profileStatChipFrame.loaded=true`, `rendered=true`, `count=10`, frame geometry 142x54 and 146x56, zero console errors, and zero page errors. Visual inspection caught an initial too-loud 158x68 treatment that felt like a second banner row; the final tuned row keeps the numbers readable, avoids chip overlap, and makes the progression record feel more premium without fighting the larger profile-record flourish. The temporary built preview used port 4217; the Bird Squad dev server remains live on port 5199.

Current follow-up: Confirm Exit Frame run-abandon confirmation modal polish. Generated a transparent-ready warning confirmation frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/confirm-exit-frame-source-v1.png` (1,490,124 bytes, 1672x941), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 1024x384 RGBA runtime sprite at `assets/runtime/ui/icons/confirm-exit-frame.png` (460,561 bytes). Chroma processing detected key `#04f905`; the raw keyed output had 814,070 transparent pixels and 6,582 partial-alpha pixels, while the optimized runtime sprite has 287,622 visible pixels, 276,371 opaque pixels, 11,251 partial-alpha pixels, visible bounds `(25, 23, 996, 360)`, and no visible chroma fringe in final inspection. `src/main.ts` now registers/preloads `confirm-exit-frame`, renders the run-abandon confirmation over RouteScene as a generated 742x278 frame plus a subdued 760x284 additive glint, exposes `confirmExitOpen` and `confirmExitFrame.loaded/rendered/count` through `render_game_to_text`, and places the `Esc to keep playing` hint in the safe gap between the warning copy and buttons so it no longer rides the bottom ornament. The focused route smoke now asserts the first Esc opens the confirm modal, the generated frame renders through telemetry and scene object count, RouteScene stays active, and the second Esc dismisses the prompt. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "route ESC asks to confirm" --workers=1 --reporter=line`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures all passed. Production output includes `confirm-exit-frame-BIcC0m45.png`. Screenshots, capture spec/config, raw keyed output, and desktop/mobile state summaries are in `.artifacts/test-results/confirm-exit-frame/`, including 2560x1600 and 390x844 built captures with `confirmExitOpen=true`, `confirmExitFrame.loaded=true`, `rendered=true`, `count=2`, two frame objects at 742x278 and 760x284, zero console errors, and zero page errors. Visual inspection confirmed the frame now reads as premium destructive-action UI chrome, the buttons remain readable and balanced, the shortcut hint clears the ornament, and the darkened route map still gives enough context without competing with the modal. The temporary built preview used port 4217; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route Supply Feedback Frame route-map supply-use toast polish. Generated a transparent-ready compact horizontal route supply feedback frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-supply-feedback-frame-source-v1.png` (1,298,017 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x160 RGBA runtime sprite at `assets/runtime/ui/icons/route-supply-feedback-frame.png` (79,641 bytes). Chroma processing detected key `#1bdd23`; the raw keyed output had 1,070,420 transparent pixels and 4,517 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 48,813 visible pixels, 45,109 opaque pixels, 3,704 partial-alpha pixels, visible bounds `(8, 25, 503, 134)`, and zero green-edge pixels. `src/main.ts` now registers/preloads `route-supply-feedback-frame`, renders it behind the latest RouteScene supply-use feedback toast at final geometry `(x=1130, y=116, displayWidth=248, displayHeight=62, alpha=0.82)`, compacts the variable supply rules text with `compactEffectGrammar(...)`, and reports `routeSupplyFeedbackFrame.loaded/rendered/count` through `render_game_to_text`. The focused route smoke now starts RouteScene with `feather_splint`, uses the supply, and asserts the generated frame renders through telemetry and scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route supply feedback renders generated frame" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview desktop/mobile captures passed. Production output includes `route-supply-feedback-frame-CNmQwIe4.png`. Screenshots, capture spec/config, raw keyed output, and state summaries are in `.artifacts/test-results/route-supply-feedback-frame/`, including 2560x1600 and 390x844 built captures with `routeSupplyFeedbackFrame.loaded=true`, `rendered=true`, `count=1`, feedback `feather_splint`, summary `+4 HP | cleanse 1`, zero console errors, and zero page errors. Visual inspection confirmed the generated toast frame sits in the upper-right route HUD lane under the top resource bar, clears the district banner and map nodes, and matches the brass/cyan/magenta route hardware language; an earlier capture had muddy long summary text, and the final compact summary reads cleanly. The temporary preview on port 4217 was used for built verification and then stopped; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Supply Feedback Frame supply-use toast polish. Generated a transparent-ready horizontal supply feedback frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-supply-feedback-frame-source-v1.png` (1,381,459 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x160 RGBA runtime sprite at `assets/runtime/ui/icons/combat-supply-feedback-frame.png` (97,888 bytes). Chroma processing detected key `#13ec16`; the raw keyed output has 993,840 transparent pixels and 4,470 partial-alpha pixels with visible bounds `(213, 189, 1770, 586)`, while the optimized runtime sprite has transparent corners, 59,185 visible pixels, 56,342 opaque pixels, 2,843 partial-alpha pixels, visible bounds `(8, 15, 504, 145)`, and zero green-edge pixels. `src/main.ts` now registers/preloads `combat-supply-feedback-frame`, renders it behind the animated BattleScene supply-use toast on the FX layer at final geometry `(displayWidth=286, displayHeight=64, alpha=0.84)`, moves the toast down to clear the encounter title, suppresses the duplicate static strip while same-turn FX feedback is active, keeps a static generated-frame fallback for later feedback redraws, wraps the summary text, and reports `combatSupplyFeedbackFrame.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke now starts BattleScene, uses `seed_packet`, and asserts the generated frame renders through telemetry and root/FX scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat supply feedback renders generated frame" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-supply-feedback-frame-BV6ynKew.png`. Screenshots, capture spec/config, raw keyed output, web-game smoke screenshots, and state summaries are in `.artifacts/test-results/combat-supply-feedback-frame/`, including 2560x1600 and 390x844 built captures with `combatSupplyFeedbackFrame.loaded=true`, `rendered=true`, `count=1`, `feedback.id="seed_packet"`, zero console errors, and zero page errors. Visual inspection caught two iterations: first the generated frame sat behind the older plain rectangle, then it overlapped the encounter title; the final capture shows a single generated toast frame below `Rooftop Blocks` that matches the brass/cyan/magenta combat hardware language without blocking the bird, enemy, hand cards, or bottom encounter plaque. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Encounter Plaque battle-start caption polish. Generated a transparent-ready encounter caption plaque with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-encounter-plaque-source-v1.png` (1,311,199 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x160 RGBA runtime sprite at `assets/runtime/ui/icons/combat-encounter-plaque.png` (113,959 bytes). Chroma processing detected key `#07f80b`; the raw keyed output had 1,147,787 transparent pixels and 5,894 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 55,540 visible pixels, 50,873 opaque pixels, 4,667 partial-alpha pixels, visible bounds `(5, 7, 507, 153)`, and 1 keyed-edge pixel. `src/main.ts` now registers/preloads `combat-encounter-plaque`, layers it into `playBattleIntro()` behind the right-side encounter caption at final tuned geometry `(x=1100, y=614, displayWidth=328, displayHeight=88, alpha=0.76)`, and reports `combatEncounterPlaque.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke now starts BattleScene and asserts the plaque renders on the FX layer while the encounter intro stinger is active. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat intro renders generated encounter plaque behind caption" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-encounter-plaque-BMhtbDUR.png`. Screenshots, capture spec/config, raw alpha output, and state summaries are in `.artifacts/test-results/combat-encounter-plaque/`, including 2560x1600 and 390x844 built captures with `combatEncounterPlaque.loaded=true`, `rendered=true`, `count=1`, `combatEncounterIntro.count=2`, `bursts=1`, zero console errors, and zero page errors. Visual inspection caught an initial oversized 396x112/0.86 placement and tightened it to the final caption-plate treatment so it reads as battle-start UI chrome without dominating the hand cards. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Enemy Vitals Frame HP-bar polish. Generated a transparent-ready enemy HP/nameplate backing frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-enemy-vitals-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x192 RGBA runtime sprite at `assets/runtime/ui/icons/combat-enemy-vitals-frame.png`. Chroma processing detected key `#05f90b`; the raw keyed output had 1,219,787 transparent pixels and 139,734 partial-alpha pixels, while the optimized runtime sprite has transparent corners, 56,512 visible pixels, 29,248 opaque pixels, 27,264 partial-alpha pixels, visible bounds `(19, 46, 749, 145)`, zero green-edge pixels, and a 117,706-byte PNG size. `src/main.ts` now registers/preloads `combat-enemy-vitals-frame`, renders it behind each BattleScene enemy HP bar as a subdued generated frame at `hp.w + 48 * scale` by `50 * scale` with selected alpha `0.72` and normal alpha `0.6`, and reports `combatEnemyVitalsFrame.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke now starts BattleScene and asserts the generated vitals frame renders through telemetry and scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat enemies render generated vitals frames behind HP bars" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-enemy-vitals-frame-DlUSKrUe.png`. Screenshots, capture spec/config, state summaries, and an enemy-bar crop are in `.artifacts/test-results/combat-enemy-vitals-frame/`, including 2560x1600 and 390x844 built captures with `combatEnemyVitalsFrame.loaded=true`, `rendered=true`, `count=1`, final selected-frame geometry `(x=920, y=326, displayWidth=289, displayHeight=56, alpha=0.72)`, `enemyIntentRing.count=1`, `combatHandRail.count=1`, zero console errors, and zero page errors. Visual inspection confirmed the frame now reads as HP-bar chrome without covering enemy name/HP text or crowding the intent badge; the earlier too-strong 318x65/0.94 selected rendering was tightened before final verification. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Hand Rail playable-hand polish. Generated a transparent-ready support rail for the BattleScene hand with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-hand-rail-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x128 RGBA runtime sprite at `assets/runtime/ui/icons/combat-hand-rail.png`. Chroma processing detected key `#06ef13`; the optimized runtime sprite has transparent corners, 36,658 visible pixels, 33,178 opaque pixels, 3,480 partial-alpha pixels, visible bounds `(18, 17, 494, 110)`, zero green edge pixels, and a 78,150-byte PNG size. `src/main.ts` now registers/preloads `combat-hand-rail`, renders it behind the playable hand cards at 918x128 with 0.54 alpha, and reports `combatHandRail.loaded/rendered/count` through BattleScene text state. The focused combat smoke now starts BattleScene and asserts the generated hand rail renders through telemetry and scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat hand renders generated support rail behind cards" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-hand-rail-C4wTHvOu.png`. Screenshots, capture spec, and state summaries are in `.artifacts/test-results/combat-hand-rail/`, including settled 2560x1600 and 390x844 built captures with `combatHandRail.loaded=true`, `rendered=true`, `count=1`, rail geometry `(x=640, y=644, displayWidth=918, displayHeight=128, alpha=0.54)`, zero console errors, and zero page errors. Visual inspection confirmed the rail reads as a subtle premium shelf under the hand without covering card titles, art, or rules text; the capture harness now waits for MenuScene/UI preload readiness before jumping to BattleScene so it does not race BootScene.

Current follow-up: Combat Roost Command Frame end-turn polish. Generated a transparent-ready combat End Turn/Roost command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-roost-command-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and downsampled it into a 384x384 RGBA runtime sprite at `assets/runtime/ui/icons/combat-roost-command-frame.png`. Chroma processing detected key `#04f508`; the optimized runtime sprite has transparent corners, 68,005 visible pixels, 64,976 opaque pixels, 3,029 partial-alpha pixels, visible bounds `(39, 40, 344, 334)`, zero green edge pixels, and a 157,924-byte PNG size. `src/main.ts` now registers/preloads `combat-roost-command-frame`, renders it behind the existing BattleScene roost/end-turn icon with hover alpha feedback while preserving the invisible hit target and tooltip, and reports `combatRoostCommandFrame.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke now starts BattleScene and asserts the generated command frame renders through telemetry and scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat end turn control renders generated roost command frame" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-roost-command-frame-CWA6jxXd.png`. Screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-roost-command-frame/`, including settled 2560x1600 and 390x844 built captures with `loaded=true`, `rendered=true`, `count=1`, the roost icon still visible at 74x74, zero console errors, and zero page errors. Visual inspection confirmed the end-turn control now reads as a deliberate combat command button without crowding the hand cards, discard dock, or enemy intent.

Current follow-up: Combat Pile Dock battle-pile polish. Generated a transparent-ready draw/discard pile dock tray with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-pile-dock-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/ui/icons/combat-pile-dock.png`. Chroma processing detected key `#07f90c`; the optimized runtime sprite has transparent corners, 92,733 visible pixels, 89,784 opaque pixels, 2,949 partial-alpha pixels, visible bounds `(40, 66, 471, 309)`, zero green edge pixels, and a 203,411-byte PNG size. `src/main.ts` now registers/preloads `combat-pile-dock`, renders it behind both BattleScene draw/discard piles, and reports `combatPileDock.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke now starts BattleScene and asserts two generated pile docks render through telemetry and scene object count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "combat draw and discard piles render generated dock art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Production output includes `combat-pile-dock-CsrggKAh.png`. Screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-pile-dock/`, including settled 2560x1600 and 390x844 built captures with `loaded=true`, `rendered=true`, `count=2`, zero console errors, and zero page errors. Visual inspection confirmed the docks seat the pile stacks into the combat UI without competing with the hand cards or enemy intent.

Current follow-up: Reward Ceremony Backdrop reward-screen polish. Generated a transparent-ready reward ceremony frame with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-ceremony-backdrop-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 1280x640 RGBA runtime sprite at `assets/runtime/ui/icons/reward-ceremony-backdrop.png`. The optimized runtime sprite has transparent corners, transparent center/choice anchors, 238,309 visible pixels, 222,203 opaque pixels, 16,106 partial-alpha pixels, 4 green edge pixels, and a 482,986-byte PNG size. `src/main.ts` now registers/preloads `reward-ceremony-backdrop`, renders it behind post-combat card, waymark, and preen rewards, reports `rewardCeremonyBackdrop.loaded/rendered/count` through `render_game_to_text`, and tunes the older reward-choice spotlight into a low-alpha under-card stage glow with normal blend, `depth=-8`, and about `0.018` alpha so it no longer competes with the new frame or reward cards. The focused reward smoke now asserts the ceremony backdrop loads and renders with the existing reward spotlight art. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "post-combat reward screens render choice spotlight art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` passed apart from existing LF/CRLF normalization warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `reward-ceremony-backdrop-CRAS36_r.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/reward-ceremony-backdrop/`, including the final high-resolution proof in `built-preview-final-alpha` with `loaded=true`, `rendered=true`, `count=1`, three spotlights at alpha about `0.0173`, normal blend mode, `depth=-8`, zero console errors, and zero page errors. Visual inspection confirmed the generated reward frame is visible and the toned spotlight layer stays subordinate to card readability. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Title Utility Command Frame launch-screen top-control polish. Generated a transparent-ready compact utility command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-utility-command-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x192 RGBA runtime sprite at `assets/runtime/ui/icons/title-utility-command-frame.png`. Chroma processing detected key `#06f808`; the optimized runtime sprite has transparent corners, 107,663 visible pixels, 104,272 opaque pixels, 3,391 partial-alpha pixels, visible bounds `(37, 4, 729, 188)`, center alpha 255 for readable engine-rendered labels, zero green-edge pixels, and a 159,561-byte PNG size. `src/main.ts` now registers/preloads `title-utility-command-frame`, renders it behind the title How to Play, Settings, Codex, and Flock Record controls, keeps the old rectangle as a low-alpha hit target/fallback only, adds frame hover alpha feedback, and reports `titleUtilityCommandFrame.loaded/rendered/count` through the menu text state. The portrait title smoke now asserts the generated utility command frame loads and renders at least four instances alongside the other title generated assets. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `title-utility-command-frame-DVgUPLGi.png`. Screenshots, state captures, raw alpha output, runtime stats, and `capture-summary.json` are in `.artifacts/test-results/title-utility-command-frame/`, including built-preview 2560x1600, 390x844, and 844x390 captures with `titleUtilityCommandFrame.loaded=true`, `rendered=true`, `count=4`, zero console errors, and zero page errors. Visual inspection confirmed the generated frames make the top utility row match the title's premium hardware language without covering labels, icons, the logo, or the leader/start controls. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the title controls remain coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Title Start Command Frame launch-screen primary-action polish. Generated a transparent-ready wide Start Run command frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-start-command-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 1024x256 RGBA runtime sprite at `assets/runtime/ui/icons/title-start-command-frame.png`. Chroma processing detected key `#04ec12`; the optimized runtime sprite has transparent corners, solid dark-glass center alpha 255 for legible engine text, 193,860 visible pixels, 188,854 opaque pixels, 5,006 partial-alpha pixels, visible bounds `(73, 10, 950, 246)`, and a 302,909-byte PNG size. `src/main.ts` now registers/preloads `title-start-command-frame`, renders it behind title `Start Run`, `Continue Run`, and `Start New Run` command buttons while preserving the existing rectangle as the hit target, adds hover alpha feedback, and reports `titleStartCommandFrame.loaded/rendered/count` through the menu text state. The portrait title smoke now asserts the generated primary command frame loads/renders alongside the title home dais and leader-card frames. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `title-start-command-frame-4BKWjU7j.png`. Screenshots, state captures, raw alpha output, runtime stats, and `capture-summary.json` are in `.artifacts/test-results/title-start-command-frame/`, including built-preview 2560x1600, 390x844, and 844x390 captures with `titleStartCommandFrame.loaded=true`, `rendered=true`, `count=1`, zero console errors, and zero page errors. Visual inspection confirmed the primary command frame makes Start Run read as the main console action without covering the label, medallion icon, Ascension controls, or leader row. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the title controls remain coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Title Leader Card Frame launch-screen polish. Generated a transparent-ready compact leader-selection card frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-leader-card-frame-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/resized it into a 512x160 RGBA runtime sprite at `assets/runtime/ui/icons/title-leader-card-frame.png`. Chroma processing detected key `#0dee0f`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 29,465 visible pixels, 24,363 opaque pixels, 5,102 partial-alpha pixels, visible bounds `(6, 6, 505, 154)`, and a 66,447-byte PNG size. `src/main.ts` now registers/preloads `title-leader-card-frame`, renders five generated frames over the title leader selector panels, dims/tints locked leaders, raises the selected card frame alpha, and reports `titleLeaderCardFrame.loaded/rendered/count` through the menu text state. The portrait title smoke now asserts the generated leader-card frame texture loads and renders at least five instances alongside the title/home command dais and utility icons. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `title-leader-card-frame-BH1-Igl0.png`. Screenshots, state captures, raw alpha output, runtime stats, and `capture-summary.json` are in `.artifacts/test-results/title-leader-card-frame/`, including built-preview 2560x1600, 390x844, and 844x390 captures with `titleLeaderCardFrame.loaded=true`, `rendered=true`, `count=5`, `titleHomeCommandDais.count=2`, zero console errors, and zero page errors. Visual inspection confirmed the generated frames make the leader choices feel like premium selectable hardware while keeping leader names, unlock text, Ascension, and Start Run readable. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the title controls remain coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Title Home Command Dais launch-screen polish. Generated a transparent-ready title/home control frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-home-command-dais-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/resized it into a 1024x384 RGBA runtime sprite at `assets/runtime/ui/icons/title-home-command-dais.png`. Chroma processing detected key `#05f804`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 132,489 visible pixels, 122,150 opaque pixels, 10,339 partial-alpha pixels, visible bounds `(21, 25, 1003, 360)`, and a 294,560-byte PNG size. `src/main.ts` now registers/preloads `title-home-command-dais`, renders it as a normal layer plus subtle additive glint behind the title leader selector/start-run controls, and reports `titleHomeCommandDais.loaded/rendered/count` through the menu text state. The portrait title smoke now asserts the generated dais texture loads and renders with the rest of the utility/title icons. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport with all utility icons visible" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `title-home-command-dais-DK1QM0pT.png`. Screenshots, state captures, raw alpha output, runtime stats, and `capture-summary.json` are in `.artifacts/test-results/title-home-command-dais/`, including built-preview 2560x1600, 390x844, and 844x390 captures with `loaded=true`, `rendered=true`, `count=2`, zero console errors, and zero page errors. Visual inspection confirmed the generated frame anchors the lower title controls without covering the logo, leader rows, ascension text, or Start Run button. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the title controls remain readable. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Codex Dossier Frame encyclopedia-detail polish. Generated a transparent-ready wide dossier frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-dossier-frame-source-v1.png` (1,617,668 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 1024x640 RGBA runtime sprite at `assets/runtime/ui/icons/codex-dossier-frame.png` (508,850 bytes). Alpha processing detected key `#04f808`; the raw keyed output had 1,112,465 transparent pixels and 12,375 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 218,929 visible pixels, 204,365 opaque pixels, 14,564 partial-alpha pixels, visible bounds `(25, 10, 999, 630)`, transparent center alpha, and 67 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `codex-dossier-frame`, renders it behind Codex card, supply, waymark, leader, and enemy detail modals, reports `codexDossierFrame.loaded/rendered/count` through Codex text state, and tightens the framed card/leader/enemy text columns so generated right-side hardware does not compete with scrollable copy. High-resolution review found and fixed a real enemy/leader text-over-frame issue by narrowing long-form dossier wraps and shortening the leader/enemy visible scroll band. The focused smoke `codex: detail dossiers render generated frame art on every detail type` now directly opens all five detail types and asserts the generated frame plus telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "codex: detail dossiers render generated frame art on every detail type" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` passed apart from existing LF-to-CRLF warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `codex-dossier-frame-lKw9IXmD.png`. Built-preview captures at 2560x1600 card detail, 2560x1600 enemy detail, 390x844 supply detail, and 844x390 leader detail passed with `loaded=true`, `rendered=true`, `count=1`, zero console errors, and zero page errors; visual inspection confirmed the generated dossier frame is visible and readable without covering key content. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/codex-dossier-frame/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the supply detail remains coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Card Hover Dossier Frame card-detail polish. Generated a transparent-ready tall dossier frame for card previews with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/card-hover-dossier-frame-source-v1.png` (1,740,818 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x768 RGBA runtime sprite at `assets/runtime/ui/icons/card-hover-dossier-frame.png` (292,143 bytes). Alpha processing detected key `#08f406`; the raw keyed output had 1,160,195 transparent pixels and 9,044 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 121,702 visible pixels, 106,924 opaque pixels, 14,778 partial-alpha pixels, visible bounds `(12, 4, 499, 764)`, center alpha 0, and 156 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `card-hover-dossier-frame`, adds shared card-hover frame rendering, layers the generated shell into battle hand previews, reward card details, route market/preen/card reward hover details, and deck/detail card art, reports `cardHoverDossierFrame.loaded/rendered/count` through route and battle text state, and promotes the RouteScene hover detail display tree to the high overlay depth band so the shop hover wins market draw order. The focused hover smoke now checks battle hand previews, reward previews, market hovers, preen hovers, and telemetry for the generated dossier frame. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "card choice surfaces expose full card details on hover" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and `git diff --check` passed apart from existing LF-to-CRLF warnings for `progress.md`, `src/game/fx.ts`, `src/main.ts`, and `tests/smoke.spec.ts`. Production output includes `card-hover-dossier-frame-Dx3Iqxnh.png`. Built-preview captures at 2560x1600 battle hand preview, 2560x1600 real-pointer market hover, and 390x844 route preen hover passed with `loaded=true`, `rendered=true`, `count=1`, zero console errors, and zero page errors; visual inspection confirmed the dossier frame is visible, readable, and coherent on battle, market, and mobile route detail surfaces. The repeatable capture helper now uses an actual pointer hover for the market card instead of calling the private hover method directly, because direct invocation updated telemetry without producing reliable built-preview visual evidence. Screenshots, state captures, raw alpha output, runtime stats, debug probes, and the helper are in `.artifacts/test-results/card-hover-dossier-frame/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the card detail remains coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Flock Stats Formation Flourish overlay polish. Generated a transparent-ready Flock Stats formation/ledger frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/flock-stats-flourish-source-v1.png` (2,180,080 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x460 RGBA runtime sprite at `assets/runtime/ui/icons/flock-stats-flourish.png` (316,021 bytes). Alpha processing detected key `#09ef09`; the raw keyed output had 1,165,090 transparent pixels and 25,222 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 101,849 visible pixels, 74,395 opaque pixels, 27,454 partial-alpha pixels, visible bounds `(7, 17, 761, 442)`, and 3,830 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `flock-stats-flourish`, adds a shared `addFlockStatsFlourish(...)` renderer with reduced-motion safeguards, layers it behind both RouteScene and BattleScene Flock Stats overlays, and reports `flockStatsFlourish.loaded/rendered/count` through route and battle text state. The focused smoke test now opens both route and battle Flock Stats overlays and asserts the generated flourish loads, renders, and matches telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `flock-stats-flourish-ZVdmLI-_.png`. Built-preview captures at 2560x1600 and 390x844 for both route and battle Flock Stats overlays passed with `loaded=true`, `rendered=true`, `count=1`, zero page errors, and visual inspection confirmed the frame adds identity without obscuring stat rows. Screenshots, state captures, raw alpha output, runtime stats, build logs, and the repeatable capture helper are in `.artifacts/test-results/flock-stats-flourish/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the Flock Stats overlays remain coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Encounter Intro battle-start polish. Generated a transparent-ready combat encounter intro flourish with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-encounter-intro-source-v1.png` (2,210,760 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 640x256 RGBA runtime sprite at `assets/runtime/fx/combat-encounter-intro.png` (233,421 bytes). Alpha processing detected key `#03f803`; the raw keyed output had 895,530 transparent pixels and 27,007 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 75,540 visible pixels, 61,139 opaque pixels, 14,401 partial-alpha pixels, visible bounds `(4, 4, 636, 252)`, and 1,097 warmed keyed-edge pixels. `src/main.ts` now imports/preloads `combat-encounter-intro`, adds a synthesized `encounter` audio cue, layers normal/additive generated crest images into the existing BattleScene intro overlay, keeps a reduced-motion static cleanup path, extends the stinger hold so it is visually readable, and reports `combatEncounterIntro.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now asserts the encounter intro texture loads, renders two image layers, and increments its burst count when a real BattleScene opens. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts --grep "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-encounter-intro-Cy6Y82iU.png`. Built-preview captures at 2560x1600, 844x390, and 390x844 passed with `combatEncounterIntro.loaded=true`, `rendered=true`, `count=2`, `bursts=1`, zero page errors, and visual inspection confirmed the generated crest reads as a premium battle-start stinger while keeping HUD, enemy, and hand cards legible. Screenshots, state captures, raw alpha output, runtime stats, build logs, and the repeatable capture helper are in `.artifacts/test-results/combat-encounter-intro/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the encounter intro remains coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Victory Rally encounter-clear polish. Generated a transparent-ready combat victory rally burst with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/fx/sources/combat-victory-rally-source-v1.png` (2,478,354 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-victory-rally.png` (368,647 bytes). Alpha processing detected key `#0af706`; the raw keyed output had 908,237 transparent pixels and 71,161 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 104,904 visible pixels, 68,616 opaque pixels, 36,288 partial-alpha pixels, visible bounds `(15, 7, 496, 377)`, and 275 warmed keyed-edge pixels. `src/main.ts` now imports/preloads `combat-victory-rally`, fires a layered normal/additive victory sigil from `checkOutcome()` after post-combat rewards render, adds `Rooftop Clear` feedback, synthesized victory audio, shockwave, motes, tweens, reduced-motion cleanup, and reports `combatVictoryRally.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces all encounter enemies to 0 HP, calls `checkOutcome()`, verifies the reward transition mode, and asserts two rendered rally image layers plus one burst. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-victory-rally-qJa4tqA8.png`. Built-preview captures at 2560x1600, 844x390, and 390x844 passed with `combatVictoryRally.loaded=true`, `rendered=true`, `count=2`, `bursts=1`, zero page errors, and visual inspection confirmed the burst remains behind readable card/Waymark reward choices. Screenshots, state captures, raw alpha output, runtime stats, build logs, and the repeatable capture helper are in `.artifacts/test-results/combat-victory-rally/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but reward choices and the victory feedback remain coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: How to Play Guide Frame tutorial-overlay polish. Generated a transparent-ready field-guide tutorial frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/how-to-play-guide-frame-source-v1.png` (2,470,364 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/palette-optimized it into a 768x384 transparent runtime PNG at `assets/runtime/ui/icons/how-to-play-guide-frame.png` (246,314 bytes). Alpha processing detected key `#03fa05`; the raw keyed output had 414,298 transparent pixels and 11,432 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 192,180 visible pixels, 188,805 opaque pixels, 3,375 partial-alpha pixels, visible bounds `(44, 20, 716, 364)`, and 2 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `how-to-play-guide-frame` and renders it as a normal layer plus faint additive glint behind the title How to Play overlay, with reduced-motion safeguards so the generated guide shell stays decorative and does not compete with tutorial card text. The title How to Play smoke test now asserts `ui-icon-how-to-play-guide-frame` loads and renders at least two image layers while preserving help-open state. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title How to Play overlay opens, reports state, and loads its medallion" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `how-to-play-guide-frame-BgiH1C3P.png`. Built-preview captures at 2560x1600, 844x390, and 390x844 passed with `guideFrame.loaded=true`, `count=2`, `overlayPanelFlourish.count=1`, zero page errors, and visual inspection confirmed the guide frame sits behind the four tutorial cards without text overlap; the final post-build 2560x1600 capture also passed. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/how-to-play-guide-frame/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the How to Play overlay remains coherent. The temporary preview on port 4217 was used for built verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: System Menu Command Frame pause/settings polish. Generated a transparent-ready pause/settings command-frame backplate with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/system-menu-command-frame-source-v1.png` (2,346,271 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/palette-optimized it into a 768x384 transparent runtime PNG at `assets/runtime/ui/icons/system-menu-command-frame.png` (294,053 bytes). Alpha processing detected key `#0ffa05`; the raw keyed output had 405,651 transparent pixels and 548,653 partial-alpha pixels because the generated center window is intentionally translucent, and the optimized runtime sprite has transparent corners, 192,962 visible pixels, 100,812 opaque pixels, 92,150 partial-alpha pixels, visible bounds `(16, 26, 750, 352)`, and 6 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `system-menu-command-frame`, adds a shared `addSystemMenuCommandFrame(...)` renderer with a normal layer plus faint additive glint and reduced-motion safeguards, and uses it behind both Pause and Settings overlays across title/menu, route, and combat surfaces. The existing pause/settings smoke coverage now asserts the generated frame loads and renders in route pause, combat pause, menu settings, route settings, and combat settings overlays. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "pause overlay opens on route and combat without advancing play" --workers=1 --reporter=line`, focused `npx playwright test tests/smoke.spec.ts -g "settings overlay opens from menu and paused run surfaces" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Production output includes `system-menu-command-frame-DULvB8o3.png`. Built-preview captures at 2560x1600 menu Settings, 844x390 route Pause, and 390x844 battle Settings passed with `commandFrame.loaded=true`, `rendered=2`, zero page errors, and the generated shell visible without hurting row/button readability. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/system-menu-command-frame/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 canvas, but the settings panel and frame remain coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route Choice Decision Frame set-piece choice polish. Generated a transparent-ready route decision frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-choice-decision-frame-source-v1.png` (2,001,661 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/palette-optimized it into a 768x384 transparent runtime PNG at `assets/runtime/ui/icons/route-choice-decision-frame.png` (84,785 bytes). Alpha processing detected key `#03f807`; the raw keyed output had 844,411 transparent pixels and 10,201 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 166,500 visible pixels, 158,629 opaque pixels, 7,871 partial-alpha pixels, visible bounds `(18, 33, 752, 351)`, and 2,686 warmed keyed-edge pixels. `src/main.ts` now registers/preloads `route-choice-decision-frame`, adds a synthesized `routeChoice` audio cue, renders the generated frame plus additive glint behind special route-event choice rows for Signal, Basin, Nest, and Rival set pieces, tweens the frame reveal with reduced-motion safeguards, and reports `nodeChoice.choiceFrame.loaded/rendered/count` through `render_game_to_text`. The route event overlay smoke now asserts generated frame telemetry on those set-piece choice overlays while leaving Cache and Market on their custom layouts. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "route event overlays render generated special-node backdrops"`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, live 1280x720 capture, and `npm run build` passed. Production output includes `route-choice-decision-frame-ByJZ49Gy.png`. Built-preview captures at 2560x1600, 844x390, and 390x844 passed with `choiceFrame.loaded=true`, `rendered=true`, `count=2`, zero capture errors, and the generated ornament visible behind the choice rows without covering text or set-piece resident art. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/route-choice-decision-frame/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the event layout remains coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: District Advance Flourish route-progression polish. Generated a transparent-ready district transition flourish with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/district-advance-flourish-source-v1.png` (2,014,336 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x360 RGBA runtime sprite at `assets/runtime/ui/icons/district-advance-flourish.png` (365,081 bytes). Alpha processing detected key `#07f50e`; the raw keyed output had 1,048,399 transparent pixels and 12,794 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 115,172 visible pixels, 105,018 opaque pixels, 10,154 partial-alpha pixels, visible bounds `(13, 28, 757, 331)`, and 1,620 warmed pixels. `src/main.ts` now registers/preloads `district-advance-flourish`, adds a synthesized `districtAdvance` audio cue, carries an explicit `districtAdvanceNotice` from BattleScene to the next RouteScene when a non-final district boss is cleared, consumes that notice before persistence, renders the generated compass/gateway flourish behind the route nodes with additive glow, particle burst, motes, and tweens, and reports `districtAdvanceFlourish.loaded/rendered/count/bursts/notice` through `render_game_to_text`. The existing boss-clear progression smoke now verifies district two selection plus generated transition telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "clearing a district boss advances the run to the next district"`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, live 1280x720 transition capture, `npm run build`, and built-preview captures at 2560x1600, 844x390, and 390x844 passed. Production output includes `district-advance-flourish-BJvuvx1x.png`; built capture telemetry reported `loaded=true`, `rendered=true`, `count=22`, `bursts=1`, and zero page errors. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/district-advance-flourish/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the route map and generated transition layer remain visible and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Profile Record Flourish meta-progression polish. Generated a transparent-ready Flock Record ledger frame with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/profile-record-flourish-source-v1.png` (1,814,997 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x460 RGBA runtime sprite at `assets/runtime/ui/icons/profile-record-flourish.png` (307,847 bytes). Alpha processing detected key `#05f907`; the raw keyed output had 1,142,851 transparent pixels and 15,111 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 100,950 visible pixels, 88,873 opaque pixels, 12,077 partial-alpha pixels, visible bounds `(20, 45, 748, 414)`, and 2,830 warmed pixels. `src/main.ts` now registers/preloads `profile-record-flourish`, adds a synthesized `profileRecord` audio cue, renders the generated ledger frame plus additive glint behind the ProfileScene/Flock Record panel, emits a restrained reveal particle/mote burst, and reports `profileRecordFlourish.loaded/rendered/count/revealBursts` through `render_game_to_text`. The existing Profile smoke now asserts the generated profile flourish texture loads, renders, and appears in text telemetry while preserving locked/unlocked Leader gating. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders are gated until unlocked, and the Profile screen renders" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, live 1280x720 Profile capture, `npm run build`, and built-preview Profile captures at 2560x1600, 844x390, and 390x844 passed. Production output includes `profile-record-flourish-DMMl8j1x.png`; built capture telemetry reported `profileRecordFlourish.loaded=true`, `rendered=true`, `count=13`, `revealBursts=1`, and zero page errors. Screenshots, state captures, raw alpha output, runtime stats, and the repeatable capture helper are in `.artifacts/test-results/profile-record-flourish/`. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the Flock Record panel and generated frame remain visible and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Market Purchase Flourish economy-feedback polish. Generated a transparent-ready market purchase confirmation burst with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/market-purchase-flourish-source-v1.png` (1,965,543 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 320x320 RGBA runtime sprite at `assets/runtime/ui/icons/market-purchase-flourish.png` (153,470 bytes). Alpha processing detected key `#04f906`; the raw keyed output had 1,125,276 transparent pixels and 24,933 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 43,059 visible pixels, 31,587 opaque pixels, 11,472 partial-alpha pixels, visible bounds `(26, 13, 294, 307)`, and 117 warmed pixels. `src/main.ts` now registers/preloads `market-purchase-flourish`, adds a synthesized `marketPurchase` audio cue, layers a glow, generated purchase image, additive glint, particle burst, motes, and tweens through `marketPurchaseFlourish(...)`, replays the short-lived visual across market rerenders without double-counting bursts, and routes successful card, Waymark, supply, route-scout, boss-guard, preen, release, and refresh purchases through the shared generated feedback. `render_game_to_text` now reports `market.purchaseFlourish.loaded/rendered/count/bursts`, and the market smoke covers both card purchase and paid refresh telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock multiple finite offers and paid refreshes" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, and built-preview captures at 2560x1600, 844x390, and 390x844 passed. Production output includes `market-purchase-flourish-B-cFdYhJ.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/market-purchase-flourish/`, including built captures with `purchaseFlourish.loaded=true`, `rendered=true`, 16 rendered effect objects, one burst, a sold card offer, and zero page errors. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the market shelf and purchase burst remain visible and coherent.

Current follow-up: Combat Shuffle Vortex deck-refresh polish. Generated a transparent-ready discard-to-draw shuffle vortex with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-shuffle-vortex-source-v1.png` (1,934,193 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed keyed-edge spill away from green, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-shuffle-vortex.png` (324,106 bytes). Alpha processing detected key `#0eea17`; the raw keyed output had 1,115,784 transparent pixels and 68,776 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 96,991 visible pixels, 63,589 opaque pixels, 33,402 partial-alpha pixels, visible bounds `(13, 15, 498, 368)`, and 631 warmed pixels. `src/main.ts` now imports/preloads `combat-shuffle-vortex`, adds a synthesized `shuffle` audio cue, layers a normal copy plus additive LINEAR-filtered generated vortex with glow/mote support in `combatShuffleVortex(...)`, routes real `reshuffleDiscardIntoDraw()` through `animateShuffle()`, and reports `combatShuffleVortex.loaded/rendered/count/bursts` through `render_game_to_text`. The visual tuning fixed two animation-scale issues discovered in captures: shuffle transfer card backs now tween using relative scale instead of absolute `1.06`, and draw-to-hand cards now use relative scale instead of absolute `2.4`; draws immediately after a reshuffle use a smaller battlefield-anchored draw burst so card text in the hand remains readable. The focused combat smoke now forces a real empty-draw-pile/discard-pile reshuffle via `drawCards(1)`, verifies hand/draw/discard pile movement, and asserts rendered generated-FX telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-shuffle-vortex-BRKZoCRx.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-shuffle-vortex/`, including live 1280x720 retune captures plus built-preview 2560x1600, 844x390, and 390x844 captures with two rendered generated shuffle image layers, one burst, zero page errors, and the effect anchored in the battlefield without covering the hand row or HUD. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Discard Sweep discard/roost cleanup polish. Generated a transparent-ready discard sweep with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-discard-sweep-source-v1.png` (1,704,497 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed keyed-edge spill away from green, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-discard-sweep.png`. Alpha processing detected key `#0bf50b`; the raw keyed output had 1,169,841 transparent pixels and 18,740 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 70,887 visible pixels, 56,675 opaque pixels, 14,212 partial-alpha pixels, visible bounds `(58, 12, 453, 371)`, 459 warmed pixels, and a 214,596-byte PNG size. `src/main.ts` now imports/preloads `combat-discard-sweep`, adds a synthesized `discard` audio cue, layers a normal copy plus additive LINEAR-filtered generated sweep in `combatDiscardSweep(...)`, routes real played-card discard, end-turn roost cleanup, explicit `discard/discardUpTo`, and enemy Snag-to-discard insertion through `animateDiscard(...)`, and reports `combatDiscardSweep.loaded/rendered/count/bursts` through `render_game_to_text`. The central combat smoke now asserts the texture loads and forces a real `discardCards(1)` path, verifying hand/discard pile movement plus rendered generated-FX telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-discard-sweep-CqtFsi72.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-discard-sweep/`, including live 1280x720, built 2560x1600, built mobile landscape 844x390, and built portrait 390x844 captures with two rendered generated discard image layers, one burst, zero page errors, and the sweep anchored beside the lower-right discard pile without covering hand-card text. The first parallel 844x390 capture produced a blank WebGL screenshot despite valid telemetry; a sequential rerun produced the readable mobile capture used for visual verification. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Cover Shatter anti-Cover polish. Generated a transparent-ready enemy Cover break/shatter ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-cover-shatter-source-v1.png` (2,119,243 bytes), removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed keyed-edge spill away from green, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-cover-shatter.png`. Alpha processing detected key `#06f008`; the raw keyed output had 1,080,359 transparent pixels and 39,124 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 72,523 visible pixels, 40,161 opaque pixels, 32,362 partial-alpha pixels, visible bounds `(66, 18, 446, 366)`, 117 warmed pixels, and a 250,810-byte PNG size. `src/main.ts` now imports/preloads `combat-cover-shatter`, adds a synthesized `coverBreak` audio cue, layers a normal copy plus additive LINEAR-filtered generated shatter burst in `combatCoverShatter(...)`, routes both route/supply `removeCover` and card-context `removeEnemyCover` through the generated burst when Cover is actually stripped, and reports `combatCoverShatter.loaded/rendered/count/bursts` through `render_game_to_text`. The anti-Cover smoke now forces real `removeCover(target, 2)`, verifies the generated shatter image renders, and asserts burst telemetry; the central combat smoke asserts the production texture loads. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, focused `npx playwright test tests/smoke.spec.ts -g "new balance verbs: timed Waymarks, self-loop cards, and anti-Cover conditionals" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-cover-shatter-V1jPOKBC.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-cover-shatter/`, including built-preview 2560x1600, 844x390, and 390x844 captures with two rendered generated shatter image layers, one burst, zero page errors, and the hostile armor-break aura visible around the covered target without covering the hand row or HUD. The portrait mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the landscape mobile and desktop captures are readable and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Enemy Cover hostile guard-gain polish. Generated a transparent-ready enemy armor-up ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-enemy-cover-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, brightened the runtime sprite for rooftop readability, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-enemy-cover.png`. Alpha processing detected key `#03f805`; the raw keyed output had 1,071,959 transparent pixels and 16,508 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 72,108 visible pixels, 56,946 opaque pixels, 15,162 partial-alpha pixels, visible bounds `(83, 20, 428, 364)`, 8 warmed keyed-edge pixels, and a 227,340-byte PNG size. `src/main.ts` now imports/preloads `combat-enemy-cover`, adds a synthesized `enemyCover` audio cue, layers a normal copy plus additive LINEAR-filtered generated armor ring in `combatEnemyCover(...)`, routes real enemy ally/self Cover gain through the generated hostile guard feedback with brass glow and orange motes, and reports `combatEnemyCover.loaded/rendered/count/bursts` through `render_game_to_text`. The support-enemy focused smoke now forces real `gainCoverAlly(lowest, 3)` and `gainCover(5)`, verifies ally/support Cover values, and asserts generated enemy-cover rendering plus two burst counts. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "encounter composition supports solo tuning" --workers=1 --reporter=line`, earlier combined support/combat smoke, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, and built-preview 2560x1600/390x844 captures passed. Production output includes `combat-enemy-cover-Bts6k-0P.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-enemy-cover/`, including built captures with four rendered generated cover image layers, two bursts, zero page errors, and the hostile armor aura visible around the covered enemy area without covering the hand row or HUD. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Enemy Mend support-heal polish. Generated a transparent-ready hostile support-mend ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-enemy-mend-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-enemy-mend.png`. Alpha processing detected key `#03f905`; the raw keyed output had 1,071,916 transparent pixels and 236,739 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 65,594 visible pixels, 27,661 opaque pixels, 37,933 partial-alpha pixels, visible bounds `(70, 10, 441, 374)`, and a 242,278-byte PNG size. `src/main.ts` now imports/preloads `combat-enemy-mend`, adds a synthesized `enemyHeal` audio cue, layers a normal copy plus additive LINEAR-filtered generated mend ring in `combatEnemyMend(...)`, routes real enemy self/ally/group healing through the generated hostile heal feedback with teal glow and brass motes, and reports `combatEnemyMend.loaded/rendered/count/bursts` through `render_game_to_text`. The support-enemy focused smoke now clears the FX layer, forces real `healAlly(lowest, 4)`, verifies the healed ally path, and asserts generated mend rendering plus burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter|encounter composition supports solo tuning" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, and built-preview 2560x1600/390x844 captures passed. Production output includes `combat-enemy-mend-BxvwhZbZ.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-enemy-mend/`, including built captures with two rendered generated mend image layers, one burst, zero page errors, and the hostile mend effect anchored around the healed enemy's lower silhouette/nameplate zone without covering the hand row or HUD. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Threat Charge enemy wind-up polish. Generated a transparent-ready amber/red/brass next-attack warning ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-threat-charge-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-threat-charge.png`. Alpha processing detected key `#03f604`; the raw keyed output had 1,103,194 transparent pixels and 48,094 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 63,414 visible pixels, 49,311 opaque pixels, 14,103 partial-alpha pixels, visible bounds `(73, 9, 439, 375)`, and a 228,690-byte PNG size. `src/main.ts` now imports/preloads `combat-threat-charge`, adds a synthesized `threat` audio cue, layers a low-alpha normal copy plus additive LINEAR-filtered generated ring in `combatThreatCharge(...)`, adds stronger glow/mote support through `enemyThreatChargeFx(...)`, routes card effects, enemy self-buffs, and ally next-attack boosts through the generated warning, and reports `combatThreatCharge.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces real `nextAttackBonus(3)`, verifies the next attack bonus and incoming damage path, and asserts generated threat-charge rendering and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, built-preview 2560x1600 and 390x844 captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-threat-charge-C4eUuDVf.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-threat-charge/`, including built captures with two rendered generated threat-charge image layers, one burst, zero page errors, and a readable hostile charge ring around the enemy without hiding the hand row or HUD. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Bank Cache next-turn feedback polish. Generated a transparent-ready brass/cyan/magenta stored-momentum cache ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-bank-cache-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-bank-cache.png`. Alpha processing detected key `#0cf804`; the raw keyed output had 1,082,972 transparent pixels and 54,443 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 65,745 visible pixels, 50,873 opaque pixels, 14,872 partial-alpha pixels, visible bounds `(73, 12, 437, 374)`, and a 232,606-byte PNG size. `src/main.ts` now imports/preloads `combat-bank-cache`, adds a synthesized `bank` audio cue, layers the LINEAR-filtered generated cache ring with glow/mote support through `bankNextTurnFx(...)`, routes next-turn Wingbeat, next-turn draw, next Nest Cover, and retain-hand paths from route marks, supplies, and card effects through the shared generated feedback, and reports `combatBankCache.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces real `gainEnergyNextTurn(2)`, `nextTurnDraw(1)`, and `retainHand(1)` supply effects, verifies the deferred counters, and asserts three rendered generated-FX bursts. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-bank-cache-DoI70g3J.png`. Screenshots, state captures, raw alpha output, and the repeatable capture helper are in `.artifacts/test-results/combat-bank-cache/`, including live 1280x720 plus built-preview 2560x1600 and 390x844 captures with three visible/rendered generated Bank Cache bursts, zero page errors, and the ring anchored in open combat space without covering the hand row or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent.

Current follow-up: Combat Cleanse Burst status-clear polish. Generated a transparent-ready cyan-white/brass/magenta cleanse ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-cleanse-burst-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-cleanse-burst.png`. Alpha processing detected key `#03f904`; the raw keyed output had 1,265,018 transparent pixels and 150,092 partial-alpha pixels, and the optimized runtime sprite has transparent corners, 51,457 visible pixels, 18,187 opaque pixels, 33,270 partial-alpha pixels, visible bounds `(76, 24, 434, 360)`, and a 188,699-byte PNG size. `src/main.ts` now imports/preloads `combat-cleanse-burst`, adds a synthesized `cleanse` audio cue, layers the LINEAR-filtered additive cleanse ring inside `cleanseFx(...)`, adds a cyan pulse/shockwave and clean mote burst, routes real `cleanseFlock(...)` status clears through the generated ring instead of generic heal FX, and reports `combatCleanseBurst.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces Winded/Ruffled/Fouled on the flock, calls real `cleanseFlock(2, ...)`, verifies the negative statuses are gone, and asserts rendered generated-FX count and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-cleanse-burst-CujLTBja.png`. Screenshots, state captures, raw alpha output, and the repeatable helper are in `.artifacts/test-results/combat-cleanse-burst/`, including live 1280x720 plus built-preview 2560x1600 and 390x844 captures with one visible generated Cleanse burst, one burst, zero page errors, and the ring anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent.

Current follow-up: Combat Flow Surge formation-feedback polish. Generated a transparent-ready cyan/brass/magenta Flow formation surge ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-flow-surge-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed 504 keyed-edge pixels away from green, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-flow-surge.png`. Alpha processing detected key `#07ee0e`; the optimized runtime sprite has transparent corners, 68,172 visible pixels, 37,547 opaque pixels, 30,625 partial-alpha pixels, visible bounds `(66, 14, 446, 369)`, and a 236,169-byte PNG size. `src/main.ts` now imports/preloads `combat-flow-surge`, adds a synthesized `flow` audio cue, layers the LINEAR-filtered additive formation ring inside `flowGainFx(...)`, keeps supporting cyan mote particles and `+Flow` labels, routes `buildFlow(...)`, Four-Suit Rally, and Perfect Brace Flow gains through the generated ring only when Flow actually increases, and reports `flow/flowMax` plus `combatFlowSurge.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces real `buildFlow(...)`, asserts Flow reaches 1, and verifies rendered FX count and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Production output includes `combat-flow-surge--BoDwEQJ.png`. Screenshots, state captures, and the repeatable helper are in `.artifacts/test-results/combat-flow-surge/`, including live 1280x720 plus built-preview 2560x1600 and 390x844 captures with one visible generated Flow surge, one burst, Flow `1/5`, zero page errors, and the ring anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is readable and coherent.

Current follow-up: Combat Winded gust status-feedback polish. Generated a transparent-ready purple/cyan/brass Winded gust ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-winded-gust-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-winded-gust.png`. Alpha processing detected key `#04f605`; the optimized runtime sprite has transparent corners, 62,506 visible pixels, 36,863 opaque pixels, 25,643 partial-alpha pixels, visible bounds `(64, 26, 449, 358)`, and a 204,684-byte PNG size. `src/main.ts` now imports/preloads `combat-winded-gust`, layers it as a LINEAR-filtered additive quill/static gust inside `windedFx(...)`, keeps the existing shockwave/glow/procedural Winded pulse and mote particles as supporting motion, and reports `combatWindedGust.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now forces real `applyWinded(flock, 2)`, asserts `Winded 2`, and verifies rendered FX count and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, and built-preview 2560x1600/390x844 captures passed. Production output includes `combat-winded-gust-c_1LEEaI.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-winded-gust/`, including live and built captures with one visible generated Winded gust, one burst, zero page errors, and the gust anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Cover gain guard-ring consolidation. Reused the generated `combat-cover-guard` runtime asset for positive Cover gain instead of leaving common defensive gains as label/particle-only feedback. `coverBuildFx(...)` now layers the LINEAR-filtered generated guard ring around the flock whenever card/supply Cover is built, while route-mark `gainCover`, `gainCoverPerWaymark`, and boss-only Cover prep now also play the Cover cue, brace motion, generated guard build FX, and their existing floating labels. The focused combat smoke now forces `gainBlock(6, ...)`, verifies Cover rises after stat scaling, and asserts `combatCoverGuard.loaded/rendered/count/bursts`. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `npm run build` passed. Controlled high-res visual captures in `.artifacts/test-results/cover-build-guard/` cover 2560x1600 and 390x844 with zero page errors, one visible generated guard burst, `flockBlock: 8`, and the ring anchored around the flock without crowding the hand row. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat moment is coherent.

Current follow-up: Combat Wingbeat Drain resource-loss polish. Generated a transparent-ready amber/brass Wingbeat Drain collapse with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-wingbeat-drain-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed the green keyed-edge tones toward dirty amber shadow, and downsampled the runtime sprite to a 512x384 RGBA PNG at `assets/runtime/fx/combat-wingbeat-drain.png`. Alpha processing detected key `#0be612`; the optimized runtime sprite has transparent corners, 95,292 visible pixels, visible bounds `(52, 16, 454, 363)`, and a 306,510-byte PNG size. `src/main.ts` now imports/preloads `combat-wingbeat-drain`, adds a synthesized `drain` audio cue, adds `combatWingbeatDrain(...)` and `wingbeatDrainFx(...)`, routes enemy and card-effect `loseWingbeat(...)` through the generated collapse sprite, mote particles, floating label, and drain sound, and reports `combatWingbeatDrain.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now asserts the asset loads and forces real Wingbeat loss from 3 to 1, verifying rendered FX count and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, built-preview 2560x1600/390x844 captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-wingbeat-drain-BhsM1R2a.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-wingbeat-drain/`, including live and built captures with one visible generated Wingbeat Drain burst, one burst, zero page errors, and the effect anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Ruffled guard-break status-feedback polish. Generated a transparent-ready amber/brass Ruffled cover-break burst with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-ruffled-break-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, warmed the remaining yellow-green keyed-edge tones toward brass/gold, and downsampled the runtime sprite to a 512x384 RGBA PNG at `assets/runtime/fx/combat-ruffled-break.png`. Alpha processing detected key `#09f60d`; the optimized runtime sprite has transparent corners, 84,330 visible pixels, visible bounds `(53, 12, 452, 368)`, and a 293,918-byte PNG size. `src/main.ts` now imports/preloads `combat-ruffled-break`, adds `combatRuffledBreak(...)` and `ruffledBreakFx(...)`, routes enemy `applyFrail(...)` through the generated guard-break sprite plus the existing amber spark burst, and reports `combatRuffledBreak.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke now asserts the asset loads and forces a real Ruffled application, verifying `Ruffled 2` plus rendered FX count and burst telemetry. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live 1280x720 capture, built-preview 2560x1600/390x844 captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-ruffled-break-zFM3HQR3.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-ruffled-break/`, including live and built captures with one visible generated Ruffled burst, one burst, zero page errors, and the effect anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Fouled pressure status-feedback polish. Generated a transparent-ready teal/acid-green Fouled pressure ring with built-in imagegen on a flat magenta chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-fouled-pressure-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-fouled-pressure.png`. Alpha processing detected key `#f702f6`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 78,225 visible pixels, 61,302 opaque pixels, 16,923 partial-alpha pixels, visible bounds `(50, 13, 461, 364)`, and a 260,251-byte PNG size. `src/main.ts` now imports/preloads `combat-fouled-pressure`, adds `combatFouledPressure(...)` and `fouledPressureFx(...)`, routes enemy `applyPoison(...)` and `tickFouled()` through the generated pressure ring plus the existing green spark burst, and reports `combatFouledPressure.loaded/rendered/count/bursts` through `render_game_to_text`. The central focused combat smoke now asserts the asset loads, and the dedicated Fouled test now verifies real Fouled application, start-turn damage, ticking from 2 to 1, and generated FX rendering with burst counts for both apply and tick moments. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, focused `npx playwright test tests/smoke.spec.ts -g "poison pressure applies Fouled" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live apply/tick captures, built-preview 2560x1600/390x844 apply/tick captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-fouled-pressure-D9mZ7Jnh.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` files are in `.artifacts/test-results/combat-fouled-pressure/`, including live 1280x720 apply/tick and built 2560x1600/390x844 apply/tick captures with zero page errors, one apply burst, two tick bursts, and the generated Fouled pressure anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Open Sky exposure danger-feedback polish. Generated a transparent-ready amber Open Sky exposure vortex with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-open-sky-exposure-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-open-sky-exposure.png`. Alpha processing detected key `#0beb12`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 85,186 visible pixels, 66,611 opaque pixels, 18,575 partial-alpha pixels, visible bounds `(42, 16, 469, 359)`, and a 272,647-byte PNG size. `src/main.ts` now imports/preloads `combat-open-sky-exposure`, adds a shared `enterOpenSky(...)` helper, and routes card-effect Open Sky, enemy Open Sky, Molt-shed Open Sky, and overextension Open Sky through one generated amber hazard burst plus the existing pulse/spark feedback. `render_game_to_text` now reports `combatOpenSkyExposure.loaded/rendered/count/bursts`, and the focused combat smoke test asserts the real Open Sky status appears while the generated exposure FX loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-open-sky-exposure-pfi-AIJy.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-open-sky-exposure/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Open Sky exposure burst, one burst, zero page errors, and the hazard anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Open Sky Guard defensive-feedback polish. Generated a transparent-ready Open Sky Guard protective ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-open-sky-guard-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-open-sky-guard.png`. Alpha processing detected key `#04e81a`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 79,112 visible pixels, 58,408 opaque pixels, 20,704 partial-alpha pixels, visible bounds `(51, 16, 467, 351)`, and a 252,317-byte PNG size. `src/main.ts` now imports/preloads `combat-open-sky-guard`, adds a shared `gainOpenSkyGuard(...)` helper for mark/supply/card-effect paths, layers the LINEAR-filtered additive cyan/brass guard ring when Open Sky Guard is gained, flashes the same generated shield when a guard stack cancels the Open Sky damage increase, and reports `combatOpenSkyGuard.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now verifies both gaining Open Sky Guard and consuming it under Open Sky pressure load, render, and increment the generated FX burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live gain/consume captures, and built-preview high-res/mobile gain captures passed. Production output includes `combat-open-sky-guard-BWNWj0MR.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-open-sky-guard/`, including live 1280x720 gain and consume captures plus built 2560x1600/390x844 captures with one visible generated Open Sky Guard ring, one burst, zero page errors, and the guard effect anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent.

Current follow-up: Combat Molt Shift stance-entry polish. Generated a transparent-ready Molt stance shift ring with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-molt-shift-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-molt-shift.png`. Alpha processing detected key `#03f607`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 79,186 visible pixels, 53,366 opaque pixels, 25,820 partial-alpha pixels, visible bounds `(47, 7, 465, 356)`, and a 261,900-byte PNG size. `src/main.ts` now imports/preloads `combat-molt-shift`, layers it as a LINEAR-filtered additive amber/brass Molt ring only on fresh `enterMolt(...)` transitions, adds a supporting spark burst, and reports `combatMoltShift.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now forces a real Molt entry, asserts the `Molt` status appears, and verifies the generated shift loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, and built-preview high-res/mobile captures passed. Production output includes `combat-molt-shift-B-yMI5gd.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-molt-shift/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Molt Shift, one burst, zero page errors, and the shift anchored around the flock without covering hand cards or enemy intent. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched combat scene is coherent.

Current follow-up: Combat Resonance Spend resource-discharge polish. Generated a transparent-ready Resonance spend/discharge sprite with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-resonance-spend-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-resonance-spend.png`. Alpha processing detected key `#05f207`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 54,032 visible pixels, 30,185 opaque pixels, 23,847 partial-alpha pixels, and a 206,657-byte PNG size. `src/main.ts` now imports/preloads `combat-resonance-spend`, layers it as a LINEAR-filtered additive cyan/magenta broken-ring discharge only after successful `spendResonance(...)`, adds a supporting spark burst, and reports `combatResonanceSpend.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now sets Resonance to 3, calls `spendResonance(2)`, asserts the spend succeeds and Resonance drops to 1, and verifies the generated discharge loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-resonance-spend-B0zPLPuy.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-resonance-spend/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Resonance Spend discharge, one burst, zero page errors, `spent: true`, Resonance at 1 after the spend, and the discharge anchored around the flock without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Wingbeat Surge resource-gain polish. Generated a transparent-ready Wingbeat resource surge with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-wingbeat-surge-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-wingbeat-surge.png`. Alpha processing detected key `#04fa04`; the optimized runtime sprite has transparent corners, transparent center alpha 0, 55,881 visible pixels, 36,642 opaque pixels, 19,239 partial-alpha pixels, and a 187,735-byte PNG size. `src/main.ts` now imports/preloads `combat-wingbeat-surge`, layers it as a LINEAR-filtered additive Wingbeat gain burst through the real `gainWingbeat` route-mark, supply, and card-effect paths, keeps a supporting spark burst, and reports `combatWingbeatSurge.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now calls `applySupplyEffect('gainWingbeat(2)', ...)`, asserts Wingbeat rises from 3 to 5, and verifies the generated surge loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-wingbeat-surge-DkI-7oLN.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-wingbeat-surge/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Wingbeat Surge, one burst, zero page errors, energy at 5 after the gain, and the surge anchored around the flock without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Resonance Surge resource-gain polish. Generated a transparent-ready Plumes/Resonance resource surge with built-in imagegen on a flat green chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-resonance-surge-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-resonance-surge.png`. Alpha processing detected key `#03f208`; the optimized runtime sprite has transparent center alpha 0, 57,322 visible pixels, 35,946 opaque pixels, 21,376 partial-alpha pixels, and a 213,993-byte PNG size. `src/main.ts` now imports/preloads `combat-resonance-surge`, layers it as a LINEAR-filtered additive signal ring inside `gainResonance(...)`, keeps the existing Plumes suit pulse and `+Resonance` label, and reports `combatResonanceSurge.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now calls `gainResonance(2)`, asserts Resonance reaches 2, and verifies the generated surge loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter"`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-resonance-surge-DD0jelrn.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-resonance-surge/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Resonance Surge, one burst, zero page errors, and the surge anchored around the flock without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Heal Bloom recovery polish. Generated a transparent-ready Basin-style recovery bloom with built-in imagegen on a flat magenta chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-heal-bloom-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-heal-bloom.png`. Alpha processing detected key `#fa02f9`; the optimized runtime sprite has transparent center alpha 0, 65,366 visible pixels, 45,866 opaque pixels, 19,500 partial-alpha pixels, and a 240,174-byte PNG size. `src/main.ts` now imports/preloads `combat-heal-bloom`, layers it as a LINEAR-filtered additive recovery ring inside `healFx()`, keeps the existing procedural glow/shockwave/Basin signature/mote feedback, and reports `combatHealBloom.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now lowers flock Cohesion, calls `healFlock(...)`, asserts the real healed HP value, and verifies the generated bloom loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter"`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-heal-bloom-CETC0g_l.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-heal-bloom/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Heal Bloom, one burst, zero page errors, and the bloom anchored around the flock without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat Cover guard blocked-hit polish. Generated a transparent-ready defensive guard burst with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-cover-guard-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x384 RGBA runtime sprite at `assets/runtime/fx/combat-cover-guard.png`. Alpha processing detected key `#08ef08`; the optimized runtime sprite has 58,739 visible pixels, 43,190 opaque pixels, 15,549 partial-alpha pixels, and a 204,907-byte PNG size. `src/main.ts` now imports/preloads `combat-cover-guard`, layers it as a LINEAR-filtered additive guard ring inside non-hostile `coverImpactFx(...)`, keeps the existing procedural glow/shockwave/suit signature/mote feedback, and reports `combatCoverGuard.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now forces a blocked flock hit, asserts Cohesion stays at max while Cover is spent, and verifies the generated guard sprite loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "a combat route node resolves through its encounter"`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-cover-guard-BH4oWYlo.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-cover-guard/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated Cover guard, one burst, zero page errors, and the guard anchored around the flock without covering the hand cards or intent badge. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat enemy swipe incoming-hit polish. Generated a transparent-ready hostile enemy attack swipe with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-enemy-swipe-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 640x384 RGBA runtime sprite at `assets/runtime/fx/combat-enemy-swipe.png`. Alpha validation passed with key `#06f705`, transparent corners, 22.24% nontransparent coverage, 14.96% opaque pixels, 7.28% partial-alpha pixels, visible bounds `(38, 51, 596, 331)`, center alpha 255, and a 196584-byte optimized PNG. `src/main.ts` now imports/preloads `combat-enemy-swipe`, layers it from enemy source to flock impact inside real `damageFlock(...)` hits, keeps the existing procedural strike/impact flash as supporting feedback, and reports `combatEnemySwipe.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now starts a real BattleScene, forces an unblocked enemy hit, and asserts the generated swipe loads, renders, increments its burst count, and corresponds to actual Cohesion loss. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-enemy-swipe-xG-KjeXM.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-enemy-swipe/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated enemy swipe, one burst, zero page errors, and the swipe anchored to the flock impact without covering hand-card readability. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat action trail cast-FX polish. Generated a transparent-ready feather/card-cast action trail with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-action-trail-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x256 RGBA runtime sprite at `assets/runtime/fx/combat-action-trail.png`. Alpha validation passed with key `#06f806`, transparent corners, transparent center, 14.85% nontransparent coverage, 7.54% opaque pixels, 7.31% partial-alpha pixels, visible bounds `(136, 47, 632, 210)`, and a 102364-byte optimized PNG. `src/main.ts` now imports/preloads `combat-action-trail`, layers it inside `fxDirectionalStreak(...)` during real card-cast paths, keeps the existing procedural linework as a readability accent, and reports `combatActionTrail.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now starts a real BattleScene, plays an actual card, and asserts the generated trail loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-action-trail-BQBub-tJ.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-action-trail/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated action trail, one burst, zero page errors, and the trail anchored between the played hand card and flock without covering the hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat target reticle polish. Generated a transparent-ready selected-enemy target reticle with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-target-reticle-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 512x256 RGBA runtime sprite at `assets/runtime/fx/combat-target-reticle.png`. The sprite validates with transparent corners, transparent center, 11.7% nontransparent coverage, 7.0% opaque pixels, 4.7% partial-alpha pixels, visible bounds `(69, 22, 443, 234)`, center alpha 0, and a 53938-byte optimized PNG. `src/main.ts` now imports/preloads `combat-target-reticle`, renders it as a LINEAR-filtered additive selected-enemy backplate with a gentle pulse, and reports `combatTargetReticle.loaded/rendered/count` through `render_game_to_text`; the focused combat smoke test asserts exactly one selected-target reticle renders in a real BattleScene. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-target-reticle-z7CDANHj.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-target-reticle/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible selected-enemy reticle, zero page errors, and the reticle sitting behind the Roof Rat without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat card-back pile polish. Generated a transparent-ready brass/gunmetal/cyan/magenta combat card-back stack with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-card-back-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 384x512 RGBA runtime sprite at `assets/runtime/fx/combat-card-back.png`. The sprite validates with transparent corners, 51.48% nontransparent coverage, 49.97% opaque pixels, 1.51% partial-alpha pixels, visible bounds `(48, 43, 336, 469)`, center alpha 255, and a 239351-byte optimized PNG. `src/main.ts` now imports/preloads `combat-card-back`, renders both draw and discard piles with the generated stack behind the existing pile action icons/count badges, reuses the same sprite for draw and shuffle movement animation fallbacks, and reports `combatCardBack.loaded/rendered/count` through `render_game_to_text`. The focused combat smoke test now asserts the generated card-back loads and renders at least twice in a real BattleScene. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live capture, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-card-back-DS1tRAVP.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-card-back/`, including live 1280x720 and built 2560x1600/390x844 captures with two static pile sprites, one in-flight draw sprite, zero page errors, and the card-back piles anchored cleanly in the bottom corners without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat turn banner polish. Generated a transparent-ready brass/gunmetal/cyan/magenta combat turn-transition backplate with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-turn-banner-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 768x256 RGBA runtime sprite at `assets/runtime/fx/combat-turn-banner.png`. The sprite validates with transparent corners, a transparent center for engine-rendered text, 26.79% nontransparent coverage, 23.49% opaque pixels, 3.3% partial-alpha pixels, visible bounds `(30, 47, 738, 208)`, and a 145408-byte optimized PNG. `src/game/fx.ts` now lets the shared `banner(...)` helper optionally render a generated backing image inside an animated container while preserving text-only fallbacks; BattleScene imports/preloads `combat-turn-banner`, wraps the enemy/player turn handoff banners with `combatTurnBanner(...)`, adds a restrained mote burst, and reports `combatTurnBanner.loaded/rendered/count/bursts` through `render_game_to_text`. The focused combat smoke test now ends a real turn and asserts the generated turn banner loads, renders, and increments its burst count. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-turn-banner-D5FlU6Jx.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-turn-banner/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible generated turn banner, one burst, zero page errors, and the banner positioned below the combat HUD without covering hand cards or enemy intent. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat defeat burst FX polish. Generated a transparent-ready brass/gunmetal/cyan/magenta radial knockout burst with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-defeat-burst-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 384x384 RGBA runtime sprite at `assets/runtime/fx/combat-defeat-burst.png`. The effect is imported as `combat-defeat-burst`, preloaded in BootScene, and fired from BattleScene lethal enemy damage alongside the existing reward sound, spark particles, shockwave, and `Down!` callout. `render_game_to_text` now reports `combatDefeatBurst.loaded/rendered/count/bursts`, and the focused combat smoke test starts a real BattleScene, defeats the opening enemy by calling `damageEnemy(...)`, and asserts that the texture loads, renders, and increments its burst count. Alpha validation passed with transparent corners, 32.04% nontransparent coverage, 17.05% opaque pixels, 14.99% partial-alpha pixels, visible bounds `(23, 9, 361, 375)`, center alpha 255, and a 206722-byte optimized PNG. The pass also fixed a visual polish issue found during high-res inspection: lethal hits now skip the HP-drain rectangle so the knockout burst is not crossed by a stale horizontal health strip. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-defeat-burst-Bcm-hkuQ.png`. Screenshots, state captures, the capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-defeat-burst/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible `combat-defeat-burst`, one burst, zero page errors, and the burst landing cleanly on the defeated enemy without covering the hand cards. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Combat impact flash FX polish. Generated a transparent-ready brass/gunmetal/cyan/magenta feather-slash impact sprite with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/combat-impact-flash-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 384x384 RGBA runtime sprite at `assets/runtime/fx/combat-impact-flash.png`. The effect is imported as `combat-impact-flash`, preloaded in BootScene, and fired from BattleScene damage paths on player-to-enemy hits and enemy/direct flock hits, layered with the existing procedural audio, streak, spark particles, glow pulse, floating text, HP drain, and camera shake. `render_game_to_text` now reports `combatImpactFlash.loaded/rendered/count/bursts`, and the focused combat smoke test triggers a real damage call and asserts the texture loads, renders, and increments its burst count. Alpha validation passed with transparent corners, 26.81% nontransparent coverage, 19.65% opaque pixels, 7.16% partial-alpha pixels, visible bounds `(22, 7, 363, 378)`, and a 152874-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, live hit capture, `npm run build`, built-preview high-res/mobile captures, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `combat-impact-flash-j_CQFyGS.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/combat-impact-flash/`, including live 1280x720 and built 2560x1600/390x844 captures with one visible `combat-impact-flash`, one burst, zero page errors, and the flash landing on the Roof Rat without covering the hand cards or intent badge. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Enemy intent ring combat-readability polish. Generated a transparent-ready brass/gunmetal/cyan/magenta enemy intent ring with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/enemy-intent-ring-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled it into a 320x320 RGBA runtime sprite at `assets/runtime/ui/icons/enemy-intent-ring.png`. The ring is wired through `enemy-intent-ring` as a LINEAR-filtered backplate behind BattleScene enemy intent badges, with lower alpha for non-damage intents. `render_game_to_text` now reports `enemyIntentRing.loaded/rendered/count`, and the combat route-node smoke test asserts the texture loads and renders. Alpha validation passed with transparent corners, 28.97% nontransparent coverage, 27.01% opaque pixels, 1.96% partial-alpha pixels, transparent center, visible bounds `(18, 18, 302, 301)`, and a 99397-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, live combat capture on `http://127.0.0.1:5199/?playwright=1`, built-preview captures on port 4217, and `git diff --check` all passed; `git diff --check` only reported existing LF/CRLF normalization warnings. Production output includes `enemy-intent-ring-Dym98t3N.png`. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/enemy-intent-ring/`, including live 1280x720 and built 2560x1600/390x844 battle captures with one visible `ui-icon-enemy-intent-ring`, zero page errors, and the ring anchored cleanly behind the Roof Rat intent number. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Battle HUD command rail polish. Generated a transparent-ready brass/gunmetal/cyan/magenta combat command rail with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/battle-hud-command-rail-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, cropped/downsampled it into a 1280x176 RGBA runtime sprite at `assets/runtime/ui/icons/battle-hud-command-rail.png`, and wired it through `battle-hud-command-rail` as an opt-in LINEAR-filtered animated backplate behind the BattleScene unified top HUD. The rail keeps the route HUD stable while giving combat a bespoke command/status frame; `render_game_to_text` now reports `battleHudRail.loaded/rendered`, and the combat route-node smoke test asserts both. Alpha validation passed with transparent corners, 29.86% nontransparent coverage, 27.55% opaque pixels, 2.31% partial-alpha pixels, visible bounds `(38, 42, 1242, 134)`, and a 190756-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, live battle capture on port 5199, `npm run build`, and built-preview captures all passed. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/battle-hud-command-rail/`, including live 1280x720 and production 2560x1600/390x844 screenshots with one visible `ui-icon-battle-hud-command-rail`, zero page errors, and the canvas fully inside the mobile viewport. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Title Ascension plaque polish. Generated a transparent-ready brass/gunmetal Ascension selector plaque with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/title-ascension-plaque-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, cropped/downsampled it into a 768x320 RGBA runtime sprite at `assets/runtime/ui/icons/title-ascension-plaque.png`, and wired it through `title-ascension-plaque` as a LINEAR-filtered animated MenuScene backplate behind the title-screen difficulty selector. The render payload now reports `titleAscensionPlaque.loaded/rendered` plus selected/max difficulty values, and the portrait title smoke test asserts the plaque texture is loaded and rendered alongside the existing title medallions. Alpha validation passed with transparent corners, 47.42% nontransparent coverage, 44.05% opaque pixels, 3.37% partial-alpha pixels, visible bounds `(6, 63, 762, 256)`, and a 251580-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test ./tests/smoke.spec.ts -g "title screen fits portrait mobile viewport|settings overlay opens from menu|title How to Play overlay opens|selecting a Flock Leader sets the run starter deck" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures all passed. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/title-ascension-plaque/`, including production screenshots at 2560x1600 and 390x844 with one visible `ui-icon-title-ascension-plaque`, zero page errors, and the canvas fully inside the mobile viewport. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Boss prep dossier footer polish. Generated a transparent-ready boss-prep dossier/backplate with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/boss-prep-dossier-flourish-source-v1.png`, removed the key locally, cropped/downsampled it into a 768x384 RGBA runtime sprite at `assets/runtime/ui/icons/boss-prep-dossier-flourish.png`, and wired it through `boss-prep-dossier-flourish` as a LINEAR-filtered animated RouteScene footer dossier. The route map now shows `BOSS PREP`, the current boss name/pressure, Cover/Damage/Recovery/Molt readiness chips, and concise route needs/useful-stop hints without covering route nodes. The render payload reports `bossPrepDossier` load/render state and `bossPrep.flourishLoaded`; smoke coverage asserts both. Alpha validation passed with transparent corners, 33.41% nontransparent coverage, 27.43% opaque pixels, 5.98% partial-alpha pixels, visible bounds `(8, 35, 760, 348)`, and a 261759-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map exposes boss prep readiness before the final crossing|route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` passed; `git diff --check` only reported existing line-ending normalization warnings. Screenshots, state captures, and `capture-summary.json` are in `.artifacts/test-results/boss-prep-dossier/`, including built preview at 2560x1600 and 390x844 with one visible `ui-icon-boss-prep-dossier-flourish` and zero page errors. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route district banner level-polish pass. Generated a transparent-ready route map district header banner with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-district-banner-source-v1.png`, removed the key locally, cropped/downsampled the result into a shallow 768x224 RGBA runtime sprite at `assets/runtime/ui/icons/route-district-banner.png`, and wired it through `route-district-banner` as a LINEAR-filtered animated header plate at the top of the RouteScene board. The banner now frames a concise `DISTRICT N / 4` plus current map-name label, and the render payload reports `districtBanner` load/state data for automation. Alpha validation passed with transparent corners, 42.07% nontransparent coverage, 38.31% opaque pixels, 3.76% partial-alpha pixels, visible bounds `(11, 42, 757, 182)`, and a 158449-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` all passed; `git diff --check` only reported existing line-ending normalization warnings. Built-preview screenshots and `capture-summary.json` are in `.artifacts/test-results/route-district-banner/`, covering RouteScene at 2560x1600 and 390x844 with one visible `ui-icon-route-district-banner`, correct district text, and no page errors. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Progression unlock flourish polish. Generated a transparent-ready run-complete unlock banner flourish with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/progression-unlock-flourish-source-v1.png`, removed the key locally, optimized a 768x384 RGBA runtime sprite at `assets/runtime/ui/icons/progression-unlock-flourish.png`, and wired it through `progression-unlock-flourish` as a LINEAR-filtered additive animated ornament behind the BattleScene new leader/achievement unlock strip. The render payload now reports `unlockHighlights.flourishLoaded`, and smoke coverage asserts the texture loads, renders, and remains represented in text state. Alpha validation passed with transparent corners, 23.15% nontransparent coverage, 18.74% opaque pixels, 4.41% partial-alpha pixels, visible bounds `(10, 46, 755, 316)`, and a 213162-byte optimized PNG. Verification: `npm run validate:runtime-assets`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "run-complete outcome celebrates fresh meta unlocks|outcome screens render generated flourish" --workers=1 --reporter=line`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` all passed; `git diff --check` only reported existing line-ending normalization warnings. Built-preview screenshots and `capture-summary.json` are in `.artifacts/test-results/progression-unlock-flourish/`, covering run-complete unlocks at 2560x1600 and 390x844 with one rendered `ui-icon-progression-unlock-flourish`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Run kit drawer flourish polish. Generated a transparent-ready run inventory drawer flourish with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/run-kit-drawer-flourish-source-v1.png`, removed the key locally, optimized a sparse 768x512 RGBA runtime sprite at `assets/runtime/ui/icons/run-kit-drawer-flourish.png`, and wired it through `run-kit-drawer-flourish` as a LINEAR-filtered additive animated ornament behind the RouteScene and BattleScene `Found Waymarks` and `Packed Supplies` drawer panels. The generated sprite validates with transparent corners, 21.02% nontransparent coverage, 14.15% opaque pixels, 6.88% partial-alpha pixels, visible bounds `(9, 5, 760, 493)`, and a 250457-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "run kit drawers render|waymark artifact drawer|route HUD Waymarks chip" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` all passed; `git diff --check` only reported existing line-ending normalization warnings. Built-preview screenshots and `capture-summary.json` are in `.artifacts/test-results/run-kit-drawer-flourish/`, covering route/battle Waymarks and Supplies drawers at 2560x1600 and 390x844 with one rendered `ui-icon-run-kit-drawer-flourish` each. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Deck review dossier flourish polish. Generated a transparent-ready deck-review/dossier frame with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/deck-review-flourish-source-v1.png`, removed the key locally, optimized a sparse 768x512 RGBA runtime sprite at `assets/runtime/ui/icons/deck-review-flourish.png`, and wired it through `deck-review-flourish` as a LINEAR-filtered additive animated ornament behind the RouteScene deck browser and BattleScene deck/draw/discard inspector panels. The generated sprite validates with transparent corners, 19.56% nontransparent coverage, 13.02% opaque pixels, 6.54% partial-alpha pixels, visible bounds `(11, 12, 757, 496)`, and a 227447-byte optimized PNG. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "deck review overlays render|settings overlay opens|title How to Play overlay opens" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, built-preview captures, and `git diff --check` all passed; `git diff --check` only reported existing line-ending normalization warnings. Built-preview screenshots and `capture-summary.json` are in `.artifacts/test-results/deck-review-flourish/`, covering route deck and battle deck overlays at 2560x1600 and 390x844 with one rendered `ui-icon-deck-review-flourish` each. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Run outcome flourish polish. Generated a transparent-ready run outcome flourish with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/run-outcome-flourish-source-v1.png`, removed the key locally, optimized a sparse 768x512 RGBA runtime sprite at `assets/runtime/ui/icons/run-outcome-flourish.png`, and wired it through `run-outcome-flourish` as a LINEAR-filtered additive animated flourish behind the BattleScene run-complete and defeat panels. The generated sprite validates with transparent corners, 9.75% nontransparent coverage, 4.3% opaque pixels, 5.45% partial-alpha pixels, visible bounds `(18, 28, 745, 453)`, and a 163176-byte optimized PNG. The pass also fixed visible outcome text mojibake by replacing broken dash/bullet glyphs in the run-complete subtitle and Districts stat, and corrected the outcome crest idle animation so it tweens around its fixed 176px display scale instead of expanding from the source texture. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "run-complete outcome celebrates|outcome screens render generated flourish" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, production captures, and `git diff --check` all passed; `git diff --check` only reported existing line-ending normalization warnings. Built-preview screenshots and `capture-summary.json` are in `.artifacts/test-results/run-outcome-flourish/`, covering run-complete and defeat at 2560x1600 and 390x844, each with one rendered flourish. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Post-combat reward choice spotlight polish. Generated a transparent-ready reward choice spotlight with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-choice-spotlight-source-v1.png`, removed the key locally, optimized a sparse 512px RGBA runtime sprite at `assets/runtime/ui/icons/reward-choice-spotlight.png`, and wired it through `reward-choice-spotlight` as a LINEAR-filtered additive animated stage glow behind BattleScene card rewards, boss/rival Waymark rewards, and preen/upgrade rewards. The helper respects reduced-motion with a lower static alpha and keeps existing card panels, text, buttons, and hit areas unchanged. Verification: alpha validation passed with transparent corners, 512x512 output, 15.48% nontransparent coverage, 4.94% opaque pixels, 10.54% partial-alpha pixels, visible bounds `(72, 22, 439, 478)`, and a 138899-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "post-combat reward screens render choice spotlight art" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview captures passed. Screenshots and state summaries are in `.artifacts/test-results/reward-choice-spotlight/`, including built `2560x1600` and `390x844` captures for card, waymark, and upgrade reward screens, each confirming three rendered `reward-choice-spotlight` objects. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route reward reveal halo polish. Generated a transparent-ready reward reveal halo with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-reveal-halo-source-v1.png`, removed the key locally, optimized a sparse 512px RGBA runtime sprite at `assets/runtime/ui/icons/reward-reveal-halo.png`, and wired it through `reward-reveal-halo` as a restrained additive animated halo behind route reward card choices, item showcases, companion snag previews, and compact effect rows. The helper respects reduced-motion by using a lower static alpha, preloads with route reward art, and keeps existing reward labels/buttons/hit areas unchanged. Verification: alpha validation passed with transparent corners, 512x512 output, 7.61% nontransparent coverage, 4.54% opaque pixels, 3.07% partial-alpha pixels, visible bounds `(37, 42, 474, 448)`, and a 99957-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "mixed route card-choice rewards preserve" --workers=1 --reporter=line`, focused route-map smoke, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live reward-overlay capture, and built-preview reward-overlay captures passed. Screenshots and state summaries are in `.artifacts/test-results/reward-reveal-halo/`, including live `1280x720` card-choice/item-claim captures plus built `2560x1600` and `390x844` captures confirming visible additive `reward-reveal-halo` objects in card and item reward overlays. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route reward sparkle FX polish. Generated a transparent-ready reward spark burst with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-spark-burst-source-v1.png`, removed the key locally, optimized a sparse 320px RGBA runtime sprite at `assets/runtime/ui/icons/reward-spark-burst.png`, and wired it through `reward-spark-burst` as a restrained additive sparkle image plus one-shot Phaser particle burst behind route reward badge rows in node tooltips. The effect respects reduced-motion by keeping only the static shimmer image, preloads with the existing route node/reward badge art, and keeps the badge/text layout unchanged. Verification: alpha validation passed with transparent corners, 320x320 output, 7.67% nontransparent coverage, 3.59% opaque pixels, 4.09% partial-alpha pixels, visible bounds `(38, 35, 282, 274)`, and a 42543-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, live tooltip capture, and built-preview tooltip captures passed. Screenshots and state summaries are in `.artifacts/test-results/reward-spark-burst/`, including live `1280x720` plus built `2560x1600` and `390x844` captures confirming visible additive `reward-spark-burst` objects with all six reward badge textures. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Scrap reward badge route-tooltip polish. Generated a matching brass/cyan/magenta salvage-gear Scrap reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-scrap-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-scrap-badge.png`, and wired the route-map `scrap` reward badge through `reward-badge-scrap` instead of sharing the generic `resource-scrap` supply asset. The route reward badge family is now fully generated and smooth-filtered across Card, Waymark, Preen, Heal, Supply, and Scrap. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 55.63% subject coverage, 50.71% opaque pixels, 4.92% partial-alpha pixels, visible bounds `(25, 22, 294, 294)`, and a 162609-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-scrap-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-scrap` visible as the first token in a Rooftop Cache route tooltip at 18px display size and alpha 0.96; screenshots and state summaries are in `.artifacts/test-results/reward-scrap-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Supply reward badge route-tooltip polish. Generated a matching brass/cyan/magenta field-pouch-and-crate Supply reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-supply-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-supply-badge.png`, and wired the route-map `supply` reward badge through `reward-badge-supply` instead of borrowing the `spare_pocket` supply icon. The reward badge renderer now uses smooth filtering for generated Card, Waymark, Preen, Heal, and Supply badges while keeping the remaining pixel-art badge sources on nearest filtering. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 55.61% subject coverage, 49.07% opaque pixels, 6.54% partial-alpha pixels, visible bounds `(25, 22, 294, 294)`, and a 156610-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-supply-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-supply` visible in a Rooftop Cache route tooltip at 18px display size and alpha 0.96; screenshots and state summaries are in `.artifacts/test-results/reward-supply-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Heal reward badge route-tooltip polish. Generated a matching brass/cyan/magenta repaired-heart Heal reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-heal-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-heal-badge.png`, and wired the route-map `heal` reward badge through `reward-badge-heal` instead of borrowing the `basin_charm` waymark icon. The reward badge renderer now uses smooth filtering for generated Card, Waymark, Preen, and Heal badges while keeping pixel-art badge sources on nearest filtering. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 55.56% subject coverage, 48.18% opaque pixels, 7.37% partial-alpha pixels, visible bounds `(25, 22, 294, 294)`, and a 157400-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-heal-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-heal` visible in a Rooftop Cache route tooltip at 18px display size and alpha 0.96; screenshots and state summaries are in `.artifacts/test-results/reward-heal-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Preen reward badge route-tooltip polish. Generated a matching brass/cyan/magenta feather-care kit Preen reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-preen-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-preen-badge.png`, and wired the route-map `preen` reward badge through `reward-badge-preen` instead of borrowing the `fresh_pinfeather` waymark icon. The reward badge renderer now uses smooth filtering for generated Card, Waymark, and Preen badges while keeping pixel-art badge sources on nearest filtering. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.61% subject coverage, 51.37% opaque pixels, 3.24% partial-alpha pixels, visible bounds `(25, 24, 293, 293)`, and a 152074-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-preen-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-preen`, `reward-badge-waymark`, and `reward-badge-card` visible in the boss route tooltip at 18px display size and alpha 0.96; screenshots and state summaries are in `.artifacts/test-results/reward-preen-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Waymark reward badge route-tooltip polish. Generated a matching brass/cyan/magenta compass-and-route-tag Waymark reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-waymark-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-waymark-badge.png`, and wired the route-map `waymark` reward badge through `reward-badge-waymark` instead of borrowing the `roofline_compass` waymark icon. The reward badge renderer now uses smooth filtering for generated Card and Waymark badges while leaving the remaining pixel-art badge sources on nearest filtering. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.21% subject coverage, 50.42% opaque pixels, 3.79% partial-alpha pixels, visible bounds `(26, 25, 293, 293)`, and a 152506-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-waymark-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-waymark` and `reward-badge-card` visible in the boss route tooltip at 18px display size and alpha 0.96; screenshots and state summaries are in `.artifacts/test-results/reward-waymark-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Card reward badge route-tooltip polish. Generated a matching brass/cyan/magenta fanned-card reward badge with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/reward-card-badge-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/reward-card-badge.png`, and wired the route-map `card` reward badge through `reward-badge-card` instead of borrowing the generic `major_00` card icon. The reward badge renderer now uses smooth filtering for the generated Card badge while leaving the existing pixel-art badge sources on nearest filtering. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.05% subject coverage, 50.85% opaque pixels, 3.2% partial-alpha pixels, visible bounds `(26, 25, 293, 293)`, and a 149230-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/reward-card-badge/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `reward-badge-card` visible in the boss route tooltip at 18px display size and alpha 0.96 alongside Waymark and Preen badges; screenshots and state summaries are in `.artifacts/test-results/reward-card-badge/`. The temporary preview on port 4217 was stopped after verification; the Bird Squad dev server remains live on port 5199.

Current follow-up: Route Neutral/no-cost route-choice medallion polish. Generated a matching brass/cyan/magenta compass-and-open-walkway neutral route medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-neutral-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-neutral-medallion.png`, and wired empty/no-cost route effect tokens, cache decline/fallback profiles, and set-piece choice-card icons through `ui-icon-route-neutral-medallion` instead of the generic route pin. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.01% subject coverage, 51.37% opaque pixels, 2.64% partial-alpha pixels, visible bounds `(26, 25, 293, 292)`, and a 154057-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on a dedicated Bird Squad dev server at `http://127.0.0.1:5199/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-neutral-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed a visible 55px `ui-icon-route-neutral-medallion` at alpha 0.72 in both controlled Signal and Basin route-choice decline rows for `Move on.`/`No cost`; screenshots and state summaries are in `.artifacts/test-results/route-neutral-medallion/`. The temporary dev/preview servers on ports 5199 and 4217 were stopped after verification. Note: an initial sanity run against `http://127.0.0.1:5173/` showed that port belonged to another local app, so the authoritative Bird Squad live-client evidence is the `web-game-bird-squad-*` artifact generated from port 5199.

Current follow-up: Route Cover route-choice medallion polish. Generated a matching brass/cyan/magenta shield-and-rooftop-barricade Cover medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-cover-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-cover-medallion.png`, and wired `enemyCoverNextCombat(...)` plus `bossDamageShield(...)` route effect tokens through `ui-icon-route-cover-medallion` instead of the global `cover-shield`. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 67.36% subject coverage, 64.24% opaque pixels, 3.12% partial-alpha pixels, visible bounds `(6, 2, 314, 317)`, and a 190935-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-cover-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-cover-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Let rivals brace up.`/`Enemy +4` and `Rig boss-fight cover.`/`Boss fight +8`; screenshots and state summaries are in `.artifacts/test-results/route-cover-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Cohesion route-choice medallion polish. Generated a matching brass/cyan/magenta feather-heart Cohesion medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-cohesion-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-cohesion-medallion.png`, and wired `healCohesion(...)`, `heal(...)`, `healMissingPct(...)`, and `loseCohesion(...)` route effect tokens through `ui-icon-route-cohesion-medallion` instead of the global HUD heart. Implementation note: route tokens now carry an explicit `cohesion` flag so `routeChoiceShortTell` still formats numeric effects as `+N Coh`/`-N Coh` without depending on the icon id. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 67.33% subject coverage, 64.36% opaque pixels, 2.97% partial-alpha pixels, visible bounds `(6, 2, 314, 318)`, and a 188921-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-cohesion-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-cohesion-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Share canteen water.`/`+6 Coh` and `Push through the wire.`/`-3 Coh`; screenshots and state summaries are in `.artifacts/test-results/route-cohesion-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Scrap route-choice medallion polish. Generated a matching brass/cyan/magenta Scrap economy medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-scrap-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-scrap-medallion.png`, and wired `gainScrap(...)` plus `payScrap(...)` route effect tokens through `ui-icon-route-scrap-medallion` while preserving the `scrap` flag used by route short tells. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 67.33% subject coverage, 64.28% opaque pixels, 3.05% partial-alpha pixels, visible bounds `(6, 2, 314, 318)`, and a 189916-byte optimized PNG. Implementation note: the focused smoke initially caught that the main set-piece choice tile still hard-coded `scrap-gear` for `token.scrap`; the compact token rows and set-piece tile now all fall back to `scrap-gear` only when no specific Scrap token icon is provided. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-scrap-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-scrap-medallion` objects at alpha 0.82 and 55px display size in controlled Signal route-choice rows for `Pocket route scrap.`/`+18 Scrap` and `Spend scrap on the pass.`/`-6 Scrap`; screenshots and state summaries are in `.artifacts/test-results/route-scrap-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Rival route-choice medallion polish. Generated a matching brass/cyan/magenta Rival encounter medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-rival-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-rival-medallion.png`, and wired `startRivalBattle` route effect tokens through `ui-icon-route-rival-medallion` instead of the generic release-card icon. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 66.94% subject coverage, 64.31% opaque pixels, 2.63% partial-alpha pixels, visible bounds `(6, 3, 313, 316)`, and a 186414-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-rival-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-route-rival-medallion` object at alpha 0.72 and 55px display size in a controlled Signal route-choice row for `Challenge the rival.`/`Rival`; screenshots and state summaries are in `.artifacts/test-results/route-rival-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice row is coherent.

Current follow-up: Route Snag route-choice medallion polish. Generated a matching brass/cyan/magenta tangled trouble-card Snag medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-snag-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-snag-medallion.png`, and wired `addSnagToDiscard(...)` plus `addSnagToDraw(...)` route effect tokens through `ui-icon-route-snag-medallion` instead of the generic release-card icon. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 61.64% subject coverage, 57.64% opaque pixels, 3.99% partial-alpha pixels, visible bounds `(11, 10, 307, 307)`, and a 166554-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-snag-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-snag-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Take a discard snag`/`Snag` and `Take a draw snag`/`Snag`; screenshots and state summaries are in `.artifacts/test-results/route-snag-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Release route-choice medallion polish. Generated a matching brass/cyan/magenta card-removal medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-release-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-release-medallion.png`, and wired `releaseCard(...)` route effect tokens through `ui-icon-route-release-medallion` instead of the generic release-card icon. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 61.74% subject coverage, 56.23% opaque pixels, 5.51% partial-alpha pixels, visible bounds `(11, 9, 307, 307)`, and a 167340-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-release-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-release-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Release a stale card`/`Remove 1` and `Thin the deck hard`/`Remove 2`; screenshots and state summaries are in `.artifacts/test-results/route-release-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Card reward route-choice medallion polish. Generated a matching brass/cyan/magenta fanned-card reward medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-card-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-card-medallion.png`, and wired `addCard(...)` route effect tokens through `ui-icon-route-card-medallion` instead of the generic deck stack. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 59.47% subject coverage, 52.78% opaque pixels, 6.69% partial-alpha pixels, visible bounds `(15, 12, 304, 304)`, and a 161835-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-card-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-card-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Claim a card`/`Card` and `Claim a high card`/`High card`; screenshots and state summaries are in `.artifacts/test-results/route-card-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Waymark route-choice medallion polish. Generated a matching brass/cyan/magenta rooftop signpost and compass waymark medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-waymark-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-waymark-medallion.png`, and wired `gainRouteMark(...)` route effect tokens through `ui-icon-route-waymark-medallion` instead of the generic Waymark compass. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 61.62% subject coverage, 56.15% opaque pixels, 5.47% partial-alpha pixels, visible bounds `(11, 10, 306, 307)`, and a 159051-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-waymark-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-waymark-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Claim a waymark`/`Rare Waymark` and `Claim a high waymark`/`High Waymark`; screenshots and state summaries are in `.artifacts/test-results/route-waymark-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Supply route-choice medallion polish. Generated a matching brass/cyan/magenta rooftop supply crate and field-pouch medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-supply-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-supply-medallion.png`, and wired `gainSupply(...)`, `gainSupplyChoice(...)`, and `increaseSupplySlots(...)` route effect tokens through `ui-icon-route-supply-medallion` instead of the generic supply pouch. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 53.26% subject coverage, 47.57% opaque pixels, 5.69% partial-alpha pixels, visible bounds `(22, 18, 297, 297)`, and a 150985-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-supply-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-supply-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Pack a route supply`/`Supply` and `Add a supply slot`/`Slot +1`; screenshots and state summaries are in `.artifacts/test-results/route-supply-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Preen route-choice medallion polish. Generated a matching brass/cyan/magenta featherwork preen medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-preen-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-preen-medallion.png`, and wired `preenCard(...)` plus `freePreenNextDistrict(...)` route effect tokens through `ui-icon-route-preen-medallion` instead of the generic preen kit. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 53.28% subject coverage, 48.96% opaque pixels, visible bounds `(22, 18, 297, 297)`, and a 149850-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-preen-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-preen-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Tune the featherwork`/`Preen 1` and `Bank a free preen`/`Free Preen 1`; screenshots and state summaries are in `.artifacts/test-results/route-preen-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Sky Guard route-choice medallion polish. Generated a matching brass/cyan/magenta Open Sky protection medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/sky-guard-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/sky-guard-medallion.png`, and wired `gainOpenSkyGuard(...)` plus `reduceNextOpenSky(...)` route effect tokens through `ui-icon-sky-guard-medallion` instead of the generic cover shield. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 53.23% subject coverage, 49.98% opaque pixels, visible bounds `(22, 18, 297, 297)`, and a 144820-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/sky-guard-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-sky-guard-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Guard the crossing`/`Guard 2` and `Lower the exposure`/`Sky -2`; screenshots and state summaries are in `.artifacts/test-results/sky-guard-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Control route-choice medallion polish. Generated a matching brass/cyan/magenta route-control medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-control-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-control-medallion.png`, and wired `skipNextStreet()` plus `removeRouteChoice()` route effect tokens through `ui-icon-route-control-medallion` instead of the generic route pin. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 60.44% subject coverage, 59.6% opaque pixels, visible bounds `(12, 12, 308, 310)`, and a 169779-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1`, `npm run build`, and built-preview `npx playwright test --config .artifacts/test-results/route-control-medallion/playwright.capture.config.ts --reporter=line` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-control-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Detour past street`/`Skip street` and `Close route`; screenshots and state summaries are in `.artifacts/test-results/route-control-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched route-choice rows are coherent.

Current follow-up: Route Scout route-choice medallion polish. Generated a matching brass/cyan/magenta route-planning medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/route-scout-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/route-scout-medallion.png`, and wired `peekNextNodes(...)` plus `revealNodes(...)` route effect tokens through `ui-icon-route-scout-medallion` instead of the generic route pin. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.89% subject coverage, visible bounds `(18, 17, 301, 301)`, and a 155234-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed two visible `ui-icon-route-scout-medallion` objects at alpha 0.72 and 55px display size in controlled Signal route-choice rows for `Plan route` and `Preview lanes`; screenshots and state summaries are in `.artifacts/test-results/route-scout-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched rows are coherent.

Current follow-up: Open Sky route-choice medallion polish. Generated a matching brass/cyan/magenta Open Sky exposure medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/open-sky-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/open-sky-medallion.png`, and wired `startNextCombatOpenSky()` route effect tokens through `ui-icon-open-sky-medallion` instead of the generic route pin. Verification: alpha validation passed with transparent corners, 320x320 RGBA output, 54.93% subject coverage, visible bounds `(18, 17, 301, 301)`, and a 145227-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-open-sky-medallion` at alpha 0.72 and 55px display size in the Signal > Thermal Updraft `Ride the updraft` choice row; screenshots and state summaries are in `.artifacts/test-results/open-sky-medallion/`. The mobile capture remains letterboxed by the fixed 1280x720 game canvas, but the touched choice row is coherent.

Current follow-up: Cache lockbox route-choice medallion polish. Generated a matching brass/cyan/magenta cache-lockbox medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/cache-lockbox-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/cache-lockbox-medallion.png`, and wired route effect tokens for `gainCacheReward()` and `extraCacheChoice(...)` through `ui-icon-cache-lockbox-medallion` instead of the generic locked padlock. Route set-piece choice tiles now prefer the cache token when a choice opens a cache, so Blackout Sign-style choices advertise the special cache reward even when Open Sky or Scrap effects also appear. Verification: alpha validation passed with transparent corners, 320x320 output, 49.49% subject coverage, visible bounds `(29, 21, 291, 294)`, and a 134467-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "signals/basins/cache" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, and `npm run build` passed. The required `develop-web-game` client ran against `http://127.0.0.1:5181/?playwright=1` and the title-screen sanity screenshot was inspected; exact surface proof is in `.artifacts/test-results/cache-lockbox-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible cache medallion at alpha 0.72 and 55px display size in the Signal > Blackout Sign choice row; the mobile capture remains letterboxed by the fixed 1280x720 game canvas but the touched choice column is coherent.

Current follow-up: Codex Items/Supplies All medallion polish. Generated matching brass/cyan/magenta All Items and All Supplies medallions with built-in imagegen on flat chroma-key backgrounds, preserved the sources at `assets/concept-art/ui/icons/sources/codex-items-all-medallion-source-v1.png` and `assets/concept-art/ui/icons/sources/codex-supplies-all-medallion-source-v1.png`, removed the keys locally, optimized 320px transparent runtime PNGs at `assets/runtime/ui/icons/codex-items-all-medallion.png` and `assets/runtime/ui/icons/codex-supplies-all-medallion.png`, and wired the Codex Items top-level All tab plus Supplies contextual All filter through scoped icon resolvers so each `All` row gets the right art. Verification: alpha validation passed with transparent corners and 320x320 output for both, with about 61.11-61.12% subject coverage and optimized PNG sizes of 176519 and 179785 bytes. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Live and built-preview artifacts are in `.artifacts/test-results/codex-item-all-medallions/`. Built-preview captures at 2560x1600 and 390x844 confirmed the inactive top-level All Items medallion remains visible at alpha 0.56 and the active Supplies All medallion renders at alpha 0.96 with a clean Items > Supplies > All layout; the mobile capture remains letterboxed by the fixed 1280x720 game canvas but the touched row is coherent.

Current follow-up: Codex Waymark family filter medallion polish. Generated matching brass/cyan/magenta Waymark-family medallions for All, Shelter, Tempo, Economy, and Suit with built-in imagegen on flat chroma-key backgrounds, preserved the sources at `assets/concept-art/ui/icons/sources/codex-waymark-all-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-waymark-shelter-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-waymark-tempo-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-waymark-economy-medallion-source-v1.png`, and `assets/concept-art/ui/icons/sources/codex-waymark-suit-medallion-source-v1.png`, removed the key locally, optimized 320px transparent runtime PNGs at `assets/runtime/ui/icons/codex-waymark-all-medallion.png`, `assets/runtime/ui/icons/codex-waymark-shelter-medallion.png`, `assets/runtime/ui/icons/codex-waymark-tempo-medallion.png`, `assets/runtime/ui/icons/codex-waymark-economy-medallion.png`, and `assets/runtime/ui/icons/codex-waymark-suit-medallion.png`, and wired the Codex Items > Waymarks family row through a context-specific resolver so Supplies and other `All` filters do not inherit Waymark art. Verification: alpha validation passed with transparent corners and 320x320 output for all five, with about 61.11-61.13% subject coverage and optimized PNG sizes of 176917, 173707, 171292, 174910, and 176703 bytes respectively. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Live and built-preview artifacts are in `.artifacts/test-results/codex-waymark-family-medallions/`. Built-preview captures at 2560x1600 and 390x844 confirmed all five family medallions loaded and rendered visibly at alpha 0.96 with a clean Items > Waymarks > Economy layout; the mobile capture remains letterboxed by the fixed 1280x720 game canvas but the touched row is coherent.

Current follow-up: Codex Enemies district filter medallion polish. Generated matching brass/cyan/magenta district medallions for All, Rooftops, Canals, Signals, and Roost with built-in imagegen on flat chroma-key backgrounds, preserved the sources at `assets/concept-art/ui/icons/sources/codex-enemy-all-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-enemy-rooftops-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-enemy-canals-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-enemy-signals-medallion-source-v1.png`, and `assets/concept-art/ui/icons/sources/codex-enemy-roost-medallion-source-v1.png`, removed the key locally, optimized 320px transparent runtime PNGs at `assets/runtime/ui/icons/codex-enemy-all-medallion.png`, `assets/runtime/ui/icons/codex-enemy-rooftops-medallion.png`, `assets/runtime/ui/icons/codex-enemy-canals-medallion.png`, `assets/runtime/ui/icons/codex-enemy-signals-medallion.png`, and `assets/runtime/ui/icons/codex-enemy-roost-medallion.png`, and wired the Codex Enemies district row through a context-specific enemy icon resolver so unrelated `All` filters do not inherit enemy art. Verification: alpha validation passed with transparent corners and 320x320 output for all five, with about 61.10-61.14% subject coverage and optimized PNG sizes of 176203, 170383, 173070, 169853, and 170075 bytes respectively. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "enemy codex" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed after correcting the click coordinates. Live and built-preview artifacts are in `.artifacts/test-results/codex-enemy-district-medallions/`. Built-preview captures at 2560x1600 and 390x844 confirmed all five district medallions loaded and rendered visibly at alpha 0.96 with clean Enemies > Canals layout; the mobile capture remains letterboxed by the fixed 1280x720 game canvas but the touched row is coherent.

Current follow-up: Codex Major card-tab medallion polish. Generated a matching brass/cyan/magenta tarot-card-star-compass-and-feather medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-major-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-major-medallion.png`, and wired the Codex `Major` card tab through `ui-icon-codex-major-medallion` instead of sharing the generic `codex-cards-medallion`. Verification: alpha validation passed with transparent corners, 320x320 output, 53.21% subject coverage, and a 158321-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-major-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-codex-major-medallion` at alpha 0.96 with clean Codex Cards > Major layout; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-major-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Snags card-tab medallion polish. Generated a matching brass/cyan/magenta trap-card medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-snags-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-snags-medallion.png`, and wired the Codex `Snags` card tab through `ui-icon-codex-snags-medallion` instead of leaving that tab text-only. Verification: alpha validation passed with transparent corners, 320x320 output, 61.14% subject coverage, and a 168949-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: snag cards" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-snags-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed the active `Snags` tab and one visible `ui-icon-codex-snags-medallion` at alpha 0.96 with clean Codex Cards > Snags layout; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-snags-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Minor suit card-tab medallion polish. Generated matching brass/cyan/magenta Plumes, Quills, Basins, and Nests medallions with built-in imagegen on flat chroma-key backgrounds, preserved the sources at `assets/concept-art/ui/icons/sources/codex-plumes-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-quills-medallion-source-v1.png`, `assets/concept-art/ui/icons/sources/codex-basins-medallion-source-v1.png`, and `assets/concept-art/ui/icons/sources/codex-nests-medallion-source-v1.png`, removed the key locally, optimized 320px transparent runtime PNGs at `assets/runtime/ui/icons/codex-plumes-medallion.png`, `assets/runtime/ui/icons/codex-quills-medallion.png`, `assets/runtime/ui/icons/codex-basins-medallion.png`, and `assets/runtime/ui/icons/codex-nests-medallion.png`, and wired the Codex `Plumes`, `Quills`, `Basins`, and `Nests` card tabs through their own `ui-icon-codex-*-medallion` textures instead of leaving the middle suit row text-only. Verification: alpha validation passed with transparent corners and 320x320 output for all four, with 61.14-61.15% subject coverage and optimized PNG sizes of 178112, 181358, 175164, and 176999 bytes respectively. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-suit-medallions/`. Built-preview captures at 2560x1600 and 390x844 confirmed visible active medallions at alpha 0.96 for Plumes, Quills, Basins, and Nests with a clean Codex Cards suit-tab row; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-suit-medallions/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Aviary card-tab medallion polish. Generated a matching brass/cyan/magenta field-guide-and-feather medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-aviary-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-aviary-medallion.png`, and wired the Codex `Aviary` card tab through `ui-icon-codex-aviary-medallion` instead of sharing the generic `codex-cards-medallion`. Verification: alpha validation passed with transparent corners, 320x320 output, 53.17% subject coverage, and a 157807-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-aviary-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-codex-aviary-medallion` at alpha 0.96 with clean Codex Cards > Aviary layout; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-aviary-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Boss contextual filter medallion polish. Generated a matching brass/cyan/magenta boss-gate-and-talon-clamp medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-boss-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-boss-medallion.png`, and wired the Codex `Boss` waymark filter through `ui-icon-codex-boss-medallion` instead of the generic `locked-padlock`. Verification: alpha validation passed with transparent corners, 320x320 output, 53.15% subject coverage, and a 157576-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-boss-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-codex-boss-medallion` at alpha 0.96 with clean Codex Items > Waymarks > Boss layout; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-boss-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Molt contextual filter medallion polish. Generated a matching brass/cyan/magenta loose-feather-and-preening-comb medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-molt-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-molt-medallion.png`, and wired the Codex `Molt` waymark filter through `ui-icon-codex-molt-medallion` instead of the generic `preen-kit`. Verification: alpha validation passed with transparent corners, 320x320 output, 53.06% subject coverage, and a 157880-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required develop-web-game client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-molt-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-codex-molt-medallion` at alpha 0.96 with clean Codex Items > Waymarks > Molt layout; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-molt-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during screenshotting, with no page errors.

Current follow-up: Codex Combat contextual filter medallion polish. Generated a matching brass/cyan/magenta crossed-feather-blades-and-card-swipe combat medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-combat-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-combat-medallion.png`, and wired the Codex `Combat` contextual filter through `ui-icon-codex-combat-medallion` instead of reusing the generic enemy medallion. Verification: alpha validation passed with transparent corners, 320x320 output, 53.20% subject coverage, and a 162751-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Live click-path screenshot and state are in `.artifacts/test-results/codex-combat-medallion/`. Built-preview captures at 2560x1600 and 390x844 confirmed one visible `ui-icon-codex-combat-medallion` at alpha 0.96 and clean layout; the artifact-only direct scene-start capture logs duplicate texture-key messages because it restarts CodexScene after MenuScene assets are already registered, while the visual capture and smoke assertions are correct.

Current follow-up: Codex Pressure contextual filter medallion polish. Generated a matching brass/cyan/magenta pressure-gauge-and-cracked-shield medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-pressure-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-pressure-medallion.png`, and wired the Codex `Pressure` contextual filter through `ui-icon-codex-pressure-medallion` instead of reusing the generic enemy medallion. Verification: alpha validation passed with transparent corners, 320x320 output, 51.32% subject coverage, and a 154864-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Pressure click path and confirmed one visible `ui-icon-codex-pressure-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-pressure-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during desktop screenshotting, with no page errors.

Current follow-up: Codex Momentum contextual filter medallion polish. Generated a matching brass/cyan/magenta feather-flywheel-and-wingbeat-spark momentum medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-momentum-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-momentum-medallion.png`, and wired the Codex `Momentum` contextual filter through `ui-icon-codex-momentum-medallion` instead of the generic `resonance-battery`. Verification: alpha validation passed with transparent corners, 320x320 output, 58.22% subject coverage, and a 174088-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Momentum click path and confirmed one visible `ui-icon-codex-momentum-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-momentum-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during desktop screenshotting, with no page errors.

Current follow-up: Codex Recovery contextual filter medallion polish. Generated a matching brass/cyan/magenta roost-lantern-and-feather-heart recovery medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-recovery-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-recovery-medallion.png`, and wired the Codex `Recovery` contextual filter through `ui-icon-codex-recovery-medallion` instead of the generic `flock-heart`. Verification: alpha validation passed with transparent corners, 320x320 output, 58.24% subject coverage, and a 169771-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Recovery click path and confirmed one visible `ui-icon-codex-recovery-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-recovery-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during desktop screenshotting, with no page errors.

Current follow-up: Codex Defense contextual filter medallion polish. Generated a matching brass/cyan/magenta feather-plated shield medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-defense-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-defense-medallion.png`, and wired the Codex `Defense` contextual filter through `ui-icon-codex-defense-medallion` instead of the generic `cover-shield`. Verification: alpha validation passed with transparent corners, 320x320 output, 58.27% subject coverage, and a 169511-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Defense click path and confirmed one visible `ui-icon-codex-defense-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-defense-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during desktop screenshotting, with no page errors.

Current follow-up: Codex Flexible contextual filter medallion polish. Generated a matching brass/cyan/magenta Flexible supply-routing medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-flexible-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-flexible-medallion.png`, and wired the Codex `Flexible` contextual filter through `ui-icon-codex-flexible-medallion` instead of leaving that contextual filter iconless. Verification: alpha validation passed with transparent corners, 320x320 output, 56.85% subject coverage, and a 157742-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Flexible click path and confirmed one visible `ui-icon-codex-flexible-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-flexible-medallion/`. The only capture warnings were WebGL `ReadPixels` performance messages during desktop screenshotting, with no page errors.

Current follow-up: Codex Route/Plan contextual filter medallion polish. Generated a matching brass/cyan/magenta route-planning medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-route-plan-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-route-plan-medallion.png`, and wired the Codex `Route` and `Plan` contextual filters through `ui-icon-codex-route-plan-medallion` instead of the generic route pin. Verification: alpha validation passed with transparent corners, 320x320 output, 55.87% subject coverage, and a 163040-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies-to-Route click path and confirmed two visible `ui-icon-codex-route-plan-medallion` objects, with the selected Route filter at alpha 0.96 and inactive Plan filter at alpha 0.56; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-route-plan-medallion/`.

Current follow-up: Codex Supplies filter medallion polish. Generated a matching brass/cyan/magenta stocked supply-kit medallion with built-in imagegen on a flat chroma-key background, preserved the source at `assets/concept-art/ui/icons/sources/codex-supplies-medallion-source-v1.png`, removed the key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-supplies-medallion.png`, and wired the Codex Supplies filter through `ui-icon-codex-supplies-medallion` instead of the generic pouch. Verification: alpha validation passed with transparent corners, 320x320 output, 55.95% subject coverage, and a 165316-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Supplies click path and confirmed one visible `ui-icon-codex-supplies-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-supplies-medallion/`.

Current follow-up: Codex Waymarks filter medallion polish. Generated and preserved the source art at `assets/concept-art/ui/icons/sources/codex-waymark-medallion-source-v1.png`, removed the chroma key locally, optimized a 320px transparent runtime PNG at `assets/runtime/ui/icons/codex-waymark-medallion.png`, and wired the Codex Waymarks filter through `ui-icon-codex-waymark-medallion` instead of the generic compass. Verification: alpha validation passed with transparent corners, 320x320 output, 59.22% subject coverage, and a 176745-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex-to-Items-to-Waymarks click path and confirmed one visible `ui-icon-codex-waymark-medallion` at alpha 0.96; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-waymark-medallion/`.

Current follow-up: resolved the game-tightness audit items while preserving the intentionally visible/intuitive route map. Content drift is clean again: `audit:content` now matches shipped Fouled/Supply systems and enemy damage data. Signal Spires and High Roost authored route maps now guarantee at least one Basin/Nest safety stop before the boss, reducing late-map zero-safety paths. Information/scout items were reframed into Route Plan effects: `peekNextNodes(N)` now grants +1 Open Sky Guard and softens the next Open Sky increase by N instead of mostly duplicating visible map information; related Supplies, Waymarks, market labels, summaries, and tests were updated from Intel/Scout/Preview language to Plan language. Boss prep now recommends route stops based on low readiness needs, late market presence/route-plan service availability were strengthened, and the High Roost boss was trimmed from four enemies to three, dropping top boss pressure from 178 HP / 54 max-hit stack to 146 HP / 45 while keeping the final multi-pressure identity. Combat texture was tightened by moving several high-throughput draw/resource/setup cards to 2 cost and turning Plume Street Party into tempo/resonance instead of generic Cover. Verification: `npm run audit:content`, `npm run audit:balance`, `npm run audit:economy`, `npm run validate:runtime`, `npx tsc --noEmit`, focused Playwright smoke tests for route plan/market behavior, `npm run build`, and the required `develop-web-game` client against `http://127.0.0.1:5173/` all passed. Live screenshot inspected at `output/web-game/shot-0.png`.

Current follow-up: continued the engineering maintainability refactor. Shared effect parsing now lives in `src/game/effects/effect-parser.ts`; route-effect execution/conditions now live in `src/game/effects/route-effect-runner.ts`; and card-effect resolution now lives in `src/game/effects/card-effect-runner.ts`. `RouteScene` and `BattleScene` provide narrow adapters for scene-owned state, FX, and callbacks instead of owning the core effect switches directly. The route economy smoke test was also updated to assert the current confirm/claim flow for Basin rewards. Verification: `npx tsc --noEmit`, `npm run audit:content`, `npm run validate:runtime`, `npm run audit:balance`, `npm run audit:economy`, focused route/effect Playwright tests, focused combat/card-effect Playwright tests, `npm run build`, and the required `develop-web-game` client against `http://127.0.0.1:5173/` all passed. Latest live screenshot inspected at `output/web-game/shot-2.png`. Next recommended maintainability slices: add direct unit tests around `route-effect-runner` and `card-effect-runner`, then extract market/reward systems from `RouteScene`.

Current follow-up: reviewed game tightness and system meaning after the ability/effect repairs. Static audits found `npm run audit:balance` and `npm run audit:economy` passing, while `npm run audit:content` still reports drift in two enemy move specs, the `fouled` status registry contract, and the old authored Supply count. Live route/combat inspection on `http://127.0.0.1:5173/` produced screenshots at `.artifacts/test-results/systems-review-route.png` and `.artifacts/test-results/systems-review-battle.png`. Main surfaced improvement areas: late-map route safety/pressure guardrails, overexposed route reward information, low-cost combat hand smoothing, overlapping card effect identity, under-leveraged Supply/Market lanes, and stale map/profile documentation.

Current follow-up: resolved the remaining ability/effect meaning gaps. Repeat-Supply Waymarks with `repeatNextSupply(1)` now fire on each Supply use, while first-use Supply Waymarks remain once per combat. Route-side cleanse now heals 2 Cohesion when there is no combat pressure to clear, or banks Open Sky Guard if already full. `peekNextNodes` now scouts route labels and softens the next Open Sky increase by 1. `reduceNextOpenSky` now uses a distinct Open Sky reduction bucket/status instead of behaving like Open Sky Guard. Verification: focused Playwright tests for route cleanse/scout fallback, repeat Supply Waymarks, and Open Sky softening passed; `npm run validate:runtime`, `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `output/web-game/shot-2.png`.

Current follow-up: completed a fresh ability/effect wiring audit after the repair passes. Static audit covered 1,657 authored effect strings across cards, enemies, route rewards, Supplies, and Waymarks, then compared route/card/enemy/Supply/Waymark execution paths. Runtime probes verified full Supply packs do not overfill from `gainSupplyChoice(1)` because reward selection is capacity-gated, and confirmed `onSupplyUsed` Waymarks fire once per combat even though Supply Bell and Double-Packed Buckle copy reads like every Supply use. Verification: `npm run validate:runtime` passed; focused Playwright probes ran against `http://127.0.0.1:5173/`; required `develop-web-game` client passed and screenshot inspected at `output/web-game/shot-2.png`. Remaining surfaced item: align `onSupplyUsed` Waymark cadence/copy for Supply Bell and Double-Packed Buckle.

Current follow-up: resolved the second ability/effect audit table. Roost retain Waymarks now retain from the triggering Roost, delayed no-damage retain can rescue just-discarded cards, route Supplies explicitly resolve route-side cleanse, route Supply copy no longer promises a chooser, and mixed card-choice plus Preen rewards now preserve the card choice before opening the Preen picker. Verification: focused Playwright coverage for mixed card-choice/Preen rewards, route-side cleanse Supplies, and Roost retain Waymarks passed; `npm run validate:runtime`, `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `output/web-game/shot-2.png`.

Current follow-up: resolved ability/effect audit findings. Enemy `gainCover` moves now grant enemy Cover instead of attack bonus; Basin healing choices now trigger `basinHeal` Waymarks including bonus healing and side effects; `mapStart` Scrap from Reopened Roofline-style Waymarks now persists into the next district state; boss-prep Cover copy now consistently says the flock starts boss fights with Cover. Verification: `npm run validate:runtime`, `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `output/web-game/shot-2.png`.

Current follow-up: slimmed Marn's cache event on the live 5173 path by moving Marn left, removing the center cabinet label, making the cabinet prop art read solid, and narrowing the right claim panel. Verification: `npx tsc --noEmit`, `npm run build`, generic 5173 web-game client, and focused 5173 Playwright screenshot at `.artifacts/test-results/marn-cache-live-5173-layout-tune.png`.

Current follow-up: rebalanced Marn's cache event into a cabinet-first composition. Removed the permanent right claim panel, moved claim details to hover, added a bordered selection panel around the options, shifted the cabinet down/right as the set-piece, and kept Marn slightly farther left. Verification: `npx tsc --noEmit`, `npm run build`, generic 5173 web-game client, and focused 5173 screenshots at `.artifacts/test-results/marn-cache-hover-info-rest-5173.png` and `.artifacts/test-results/marn-cache-hover-info-hover-5173.png`.

Current follow-up: moved Marn's lockbox cabinet farther right and slightly up/scaled so the full cabinet prop appears on-screen in the no-hover 5173 state while the options remain centered/readable. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: nudged Marn's lockbox cabinet farther right again while keeping the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-more-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: nudged Marn's lockbox cabinet farther right a third time, keeping a slim onscreen margin and leaving the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-even-more-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's lockbox cabinet group slightly up while keeping its right-side placement and leaving the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-slightly-up-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's hover claim info card left while keeping the resting cabinet/options layout fixed. Verification: `npx tsc --noEmit`, focused 5173 hover screenshot at `.artifacts/test-results/marn-cache-hover-card-left-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's hover claim info card farther left again, from the previous 1058 anchor to 1000, while preserving the centered option panel and right-side cabinet. Verification: `npx tsc --noEmit`, focused 5173 hover screenshot at `.artifacts/test-results/marn-cache-hover-card-farther-left-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: improved Marn's center selection readability by removing the "Marn sets a talon..." helper sentence, widening/tallening the option rows, and increasing drawer number, title, tell, and icon sizes. Verification: `npx tsc --noEmit`, focused 5173 screenshots at `.artifacts/test-results/marn-cache-readable-center-select-5173.png` and `.artifacts/test-results/marn-cache-readable-center-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: simplified Marn's hover claim info card by removing the conversational note and rules copy, shortening the card, surfacing only reward/cost rows, and increasing both center-row and claim-card icon sizes. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-simplified-info-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: tightened Marn's hover reward/cost rows so they show only the actual change, e.g. `Reward: Scrap +35`, instead of before/after values. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-reward-delta-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: removed Marn's right-side hover info screen and added an event Exit button. The Exit button restores the previous route position instead of advancing the node, then returns to the map. Verification: `npx tsc --noEmit`, focused 5173 screenshot/click test at `.artifacts/test-results/marn-cache-no-right-info-exit-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's Exit control out of the outer event layer and into the central reward selection panel. The old top-right event-menu click spot no longer closes the overlay, while the in-panel Exit restores route selection without resolving the cache node. Verification: `npx tsc --noEmit`, focused 5173 screenshot/click test at `.artifacts/test-results/marn-cache-exit-in-selection-menu-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: corrected Marn's cancel flow so the drawer list no longer has an Exit/Cancel button. Claiming Drawer 27 now opens a route-local two-card reward picker with Cancel inside that reward menu; Cancel restores the pre-pick HP/Scrap/deck/log state and returns to Marn's drawer choices, while picking a card completes the cache event. Verification: `npx tsc --noEmit`, focused 5173 drawer/reward/cancel/claim Playwright probe, screenshots at `.artifacts/test-results/marn-cache-drawer-list-no-cancel-5173.png`, `.artifacts/test-results/marn-cache-reward-picker-cancel-5173.png`, and `.artifacts/test-results/marn-cache-reward-picker-claimed-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: expanded Marn's drawer cancel flow to every non-decline drawer, not just the card-choice drawer. Opening Drawer 12 now shows an `Open This Drawer` reward panel with Claim and Cancel before applying rewards; Cancel returns to the drawer list with HP/Scrap/deck/log unchanged, and Claim resolves the drawer. Verification: `npx tsc --noEmit`, focused 5173 simple-drawer open/cancel/claim Playwright probe, screenshots at `.artifacts/test-results/marn-cache-simple-drawer-cancel-5173.png` and `.artifacts/test-results/marn-cache-simple-drawer-after-cancel-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: routed Marn's `Keep moving` option through the same confirm/cancel layer. Selecting it now opens a `Leave the Cache` panel with Cancel and Leave; Cancel returns to the drawer list unchanged, while Leave completes the cache event. Verification: `npx tsc --noEmit`, focused 5173 keep-moving cancel/leave Playwright probe, screenshots at `.artifacts/test-results/marn-cache-keep-moving-cancel-5173.png` and `.artifacts/test-results/marn-cache-keep-moving-after-cancel-5173.png`, `npm run build`, and generic 5173 web-game client.

## 2026-06-16

- Generated a 40-cell Waymark found-object icon atlas with the built-in imagegen path.
- Copied the atlas into `assets/runtime/waymarks/waymark-icon-atlas-v1.png` and cropped 40 per-id `.webp` runtime icons under `assets/runtime/waymarks/icons/`.
- Wired Waymark icons into Codex item thumbnails/details and BattleScene.
- Added a left-side run shelf for found Waymarks plus a top-bar Waymarks chip that opens a drawer.
- Added hover tooltips with family/rarity, mechanical effect, trigger/effect grammar, and found-object flavor.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, and focused smoke tests passed. Visual screenshots inspected at `.artifacts/test-results/waymark-artifact-drawer.png` and `.artifacts/test-results/waymark-artifact-tooltip.png`.
- Note: the generic `develop-web-game` client completed but captured a blank screenshot in `output/web-game/shot-0.png`; the project Playwright screenshots rendered the canvas correctly and were used for visual verification.
- Converted all 40 Waymark item icons to transparent-background WebP assets and verified every icon has full alpha range. Checkerboard review sheet: `.artifacts/test-results/waymark-transparent-contact-sheet-small.png`.

## 2026-06-17

- New user prompt: generate map icons to make the route map more readable.
- Generated an 8-icon route node atlas with built-in imagegen, copied the source to `assets/runtime/map-icons/map-node-icon-atlas-v1.png`, and cropped transparent 192x192 WebP icons under `assets/runtime/map-icons/icons/`.
- Wired route node icons into `RouteScene` map nodes and the legend, keeping the existing text glyphs as fallback if icon textures have not loaded.
- Added smoke coverage that waits for all route-node icon textures and confirms icon images render in the RouteScene.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map smoke tests, and transparent icon alpha validation passed. Visual screenshots inspected at `.artifacts/test-results/map-node-icons-contact-sheet.png` and `.artifacts/test-results/route-map-node-icons.png`.
- Note: the generic `develop-web-game` client still captures a blank screenshot in `output/web-game/shot-0.png`; the project Playwright route screenshot rendered correctly and was used for visual verification.
- Follow-up map readability pass: removed the old type-colored node fills/legend dots now that icons carry type identity, switched nodes to neutral plates with state rings, added subtle route-column guide lines, and brightened edges that lead to currently available choices.
- Verification after neutral map pass: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map smoke tests passed. Final visual screenshot: `.artifacts/test-results/route-map-neutral-icons-final.png`.

- New user prompt: make the accepted visual beauty improvements happen, excluding extra flock companion silhouettes.
- Planned scope: ambient district battlefield layers, battle intro vignette, suit-based card/effect language, stronger hit/block/heal/Winded reactions, card play micro-cinematics, and a field-dossier Codex polish pass.
- Implemented first visual polish pass in `src/main.ts`: generated soft-dot FX texture, district atmosphere overlays, battle intro vignette, card-cast micro-cinematic, suit-colored hit bursts, richer Cover/heal/Winded/Open Sky reactions, and Codex leader dossier framing.
- New user prompt: wire recommended ability updates into cards, items, enemies, and identify existing assets that should be refreshed.
- Planned scope: add a small supported mechanic set for anti-Cover cards, route-control Waymarks, boss-prep Waymarks, and more varied enemy pressure without broad engine churn.
- Added runtime/data support for `damagePierce`, `removeCover`, `targetHasCover`, `loseWingbeat`, `flockHasCover`, `extraCacheChoice`, `bossDamageShield`, and `freePreenNextDistrict`.
- Updated Sheathed Quills, Quill Ring, Quill Elder, Cache Hook, Roofline Compass, Sky-Safe Harness, Reopened Roofline, Gutter Baron, and Tar-Crowned Crow to use the new mechanics.
- Added `cache_boss_line` as a hidden sixth Cache option revealed by cache-choice Waymarks.
- Added `docs/game/ability-asset-update-recommendations.md` with art/content refresh candidates.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts`, and a focused Playwright harness check all passed. The generic web-game client still produced the known blank screenshot but returned valid `render_game_to_text` menu state.

- Visual beauty pass verification follow-up: extended the card-cast micro-cinematic timing so suit/card moments are visible during play.
- Verification: `npm run build` and `npm test` passed. In-app browser checks covered the main menu, a route into Rooftop Blocks combat, card play/impact/readability, and the Fledgling Flock Codex leader dossier; no browser warnings or errors were reported during the visual pass.

- Route map readability polish: added selected-route preview highlighting, reward/source badges under route nodes, compact route-kit stats, and moved the Signals log into the legend strip so it no longer overlaps the selected-node action panel.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, and focused route-map Playwright smoke tests passed. Final visual screenshot: `.artifacts/test-results/route-map-reward-badges-final.png`.

- Reward image mapping pass: audited card reward, Waymark, and Supply image coverage; all collectible reward cards have manifest/portrait/thumb/icon assets, all 58 Waymarks have icons, and all 31 Supplies have icons.
- Wired route reward badges to category image assets for card, Waymark, Supply, Preen, heal, and Scrap; queued these badge assets with route-node icon art; queued Waymark reward-choice art so boss reward cards do not fall back to glyphs while their icon files are unloaded.
- Extended runtime validation so reward card art must be approved, and missing Waymark/Supply reward icons fail `npm run validate:runtime`.
- Verification: `npm run validate:runtime`, `npm run build`, generic develop-web-game client, and focused Playwright screenshots passed. Screenshots inspected at `.artifacts/test-results/reward-badge-images-route.png` and `.artifacts/test-results/reward-waymark-choice-images.png`.
- Note: the generic `develop-web-game` client completed successfully; its `output/web-game/shot-0.png` capture remains the known blank/tiny harness artifact, so the project Playwright screenshot was used for visual QA.

- Route map menu redesign pass: converted the map view into a clearer planning-board layout with shared layout bounds, a wider graph lane, a dedicated right inspector, boss prep moved out of the map banner and into the inspector, a slimmer footer legend, and the Signals line moved away from node-type labels.
- Added smoke coverage proving the inspector renders the `BOSS PREP` summary in addition to the route text-state boss-prep payload.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map Playwright smoke tests, generic `develop-web-game` client, and in-app browser route-map check all passed. Visual screenshot inspected at `.artifacts/test-results/route-map-layout-redesign-selected.png`.

- New user prompt: execute the production path for the visual beauty items, replacing stand-in lines/animations with real paths forward.
- Added transparent pixel-FX spritesheets under `assets/runtime/fx/` for combat suit effects and subtle district atmosphere texture strips.
- Replaced the procedural combat atmosphere lines, generic pulse rings, soft-dot particles, and card-cast debug line/plate with Phaser spritesheet animations in `src/main.ts`.
- Suit/card play, damage, Cover, healing, Winded, Open Sky, and hostile impact hooks now route through authored pixel-FX animation keys while keeping existing combat logic intact.
- Verification: `npm run build`, `npm test`, and the required generic `develop-web-game` client completed. The generic client screenshot remained the known blank harness artifact; direct Playwright battle screenshots were captured at `.artifacts/test-results/combat-fx-asset-backed-battle.png`, `.artifacts/test-results/combat-fx-card-mid.png`, `.artifacts/test-results/combat-fx-card-after.png`, and `.artifacts/test-results/combat-fx-support-mid.png`.
- Browser/direct-scene QA: direct BattleScene harness confirmed card play reduced Roof Rat from 20 to 15 and support FX restored Cohesion. Only WebGL `ReadPixels` screenshot performance warnings appeared during Playwright capture.

- New user prompt: execute balance/variety recommendations from the cards/enemies/artifacts/mechanics audit.
- Implemented first balance contract chunk: Waymark economy/recovery hooks now resolve by trigger/effect instead of hardcoded IDs for Signal Scrap, Street bonus Scrap, Basin healing, and Market Preen discount.
- Added deterministic `routeMarkChance` payout support for normal combat reward profiles.
- Added map-scaled reward profiles for Maps 2-4 and repointed district encounters to the map-specific street/rival/boss profiles.
- Verification: `npm run validate:runtime` passed after the resolver/profile changes.
- Added balance/variety content pass: Plume Barricade and Growing Nest now contribute anti-Cover pressure, Basin Mirage can loop itself under Open Sky, three duplicate combat-start Waymarks now use timing/Open Sky triggers, and selected Map 3-4 enemies now react to Cover/low-health/unguarded states.
- Verification: `npm run validate:runtime` and `npm run build` passed. The generic `develop-web-game` client completed successfully and produced valid menu state JSON; its screenshot remains the known blank harness artifact, so the in-app browser was used for visual QA. Browser verification loaded the route map, displayed the new 15% Waymark street reward line, entered Rooftop Blocks combat, and captured no console errors.

- New user prompt: make remaining work items/improvements happen after the balance pass.
- Added `tools/simulate-run-economy.mjs` plus `npm run audit:economy` to sample 500 seeded full-run route/economy outcomes and compare boss-entry deck/Waymark counts against `balance-config.json` targets.
- Reconciled `balance-config.json`, `balance-scaling-plan.md`, and `tools/audit-content.mjs` with the current map-scaled reward profile numbers.
- Added focused smoke coverage for generic Waymark economy hooks, cache choice expansion, Basin/Signal/Market bonuses, timed Waymark triggers, `shuffleSelfToDraw`, and anti-Cover conditionals.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:economy`, `npm run audit:content`, `npm run build`, and the full `npx playwright test tests/smoke.spec.ts` suite all passed. The generic `develop-web-game` client completed and returned valid menu state JSON; its screenshot remains the known blank harness artifact.

- Route map backdrop/inspector polish: generated a dedicated imagegen rooftop planning backdrop and saved the project asset as `assets/runtime/backdrops/rooftop-blocks-route-map-v1.webp`.
- Wired `RouteScene` to load and render the route-specific backdrop instead of reusing the splash art, reduced the map board opacity so the district art remains visible, and added a larger selected-node type icon plus labeled likely-find badges in the inspector.
- Fixed the existing combat FX compile blocker by completing the missing flock motion cue helper that matched the already-present enemy motion cue system.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map Playwright smoke tests, required generic `develop-web-game` client, direct Playwright route-map screenshot, and in-app browser bridge check all completed. Final inspected screenshot: `.artifacts/test-results/route-map-imagegen-backdrop-selected.png`.

- Home screen compactness follow-up: moved the animated Bird Squad title higher, reduced the bottom setup dock height/opacity, and made the leader chips, Ascension arrows, run actions, Codex, and Flock Record buttons smaller so the splash art remains more visible.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Home screen text-sharpness follow-up: increased menu text texture resolution, reduced heavy text strokes, slightly enlarged the smallest Ascension/leader detail labels, and strengthened only the local backing panels needed for readability.
- Verification: `npm run build`, direct Playwright menu screenshot at `.artifacts/test-results/menu-text-detail-pass.png`, and required generic `develop-web-game` client passed. The direct screenshot reported no console errors; only repeated WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Codex header layout follow-up: shifted the Cards/Items/Leaders/Enemies section tabs left and tightened their width so the Enemies tab no longer overlaps the Back button.
- Verification: `npm run build`, direct Playwright Codex screenshots at `.artifacts/test-results/codex-header-tabs-no-overlap.png` and `.artifacts/test-results/codex-enemies-tab-no-overlap.png`, and required generic `develop-web-game` client passed. The direct screenshots showed clear separation between Enemies and Back; only WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Codex card-filter alignment follow-up: shifted the Major/Aviary/Plumes/Quills/Basins/Nests row 12px right so the Major button no longer hugs/clips against the left edge.
- Verification: `npm run build`, direct Playwright Codex screenshot at `.artifacts/test-results/codex-major-tab-inset.png`, and required generic `develop-web-game` client passed. The direct screenshot showed the Major tab inset cleanly; only WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Marn cache event follow-up: changed the cache choice presentation from drawer-hotspot buttons into light dialogue-style claim options, with the generated lockbox cabinet art faded into the center panel background.
- Verification: `npx tsc --noEmit`, `npm run build`, direct Playwright cache-event screenshot at `.artifacts/test-results/marn-cache-dialogue-options-final.png`, and required generic `develop-web-game` client passed. The focused cache-event check confirmed seven options, the cabinet texture, and the updated `Choose this claim` copy.

- New user prompt: add supplies to the Codex and provide a breakdown table of supplies in the game.
- Planned scope: expand the Codex Items section to include supply definitions from `alpha-supplies.json`, add supply list/detail rendering, and add focused smoke coverage for the Supplies tab.

- Route map highlight refinement: reduced selected/available node ring thickness, replaced the selected-node halo with thin cyan focus ticks, softened route-preview line weights/dots, and changed the large map board frame from gold to muted steel blue so the map no longer feels over-highlighted.
- Verification: `npm run build`, `npm run validate:runtime`, focused route-map Playwright smoke tests, required generic `develop-web-game` client, and visual screenshot inspection passed. Final screenshot: `.artifacts/test-results/route-map-thin-node-highlight-steel-frame.png`.

- Generated the first encounter/situation backdrop variant set with built-in imagegen and saved normalized 1280x720 WebP runtime assets under `assets/runtime/backdrops/variants/`.
- New boss variants: `rooftop-blocks-boss-tar-crow-v1.webp`, `canal-markets-boss-gatekeeper-v1.webp`, `signal-spires-boss-beacon-breaker-v1.webp`, `high-roost-boss-warden-v1.webp`.
- New special-node variants: `rooftop-blocks-cache-billboard-v1.webp`, `canal-markets-supply-market-v1.webp`, `signal-spires-signal-relay-v1.webp`, `high-roost-workshop-prep-v1.webp`.
- Validation: every variant is 1280x720 RGB WebP. Contact sheet inspected at `.artifacts/test-results/backdrop-variant-contact-sheet-v1.jpg`.

- Home screen text-detail follow-up: added compact mechanical detail to the leader chips, surfaced the selected leader's signature rule in a readable two-line strip, and expanded the Ascension text to show current enemy Cohesion, reward, hit, and Open Sky modifiers.
- Refined text readability with subtle strokes and a low-opacity backing strip while keeping the splash art visible.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON; its screenshot remains the known black WebGL capture artifact.

- Home screen selected-detail implementation follow-up: removed the full-width tinted bottom dock and permanent leader description strip, keeping only compact control panels over the splash art.
- Converted flock leader descriptions/signature rules into hover tooltips for both unlocked and locked leader chips; pointer-out hides the tooltip cleanly.
- Verification: `npm run build`, in-app browser idle/leader-hover/pointer-out screenshots, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Runtime optimization follow-up: added reproducible UI and enemy-art optimization paths, including active menu splash WebP regeneration, lossy/capped large encounter enemy outputs, and reserve enemy WebP generation from transparent masters.
- Added `tools/validate-runtime-asset-sizes.mjs`, wired it into `npm test` and `npm run validate`, and set budgets for optimized splash, leader, encounter enemy, reserve enemy, card thumbnail/icon, Waymark icon, route icon, and backdrop assets.
- Tightened Codex optional art loading to visible grid rows plus the open detail item instead of queueing every asset in the active section.
- Split Phaser into a dedicated Vite `vendor-phaser` chunk for better long-term caching. Remaining note: Vite still warns because Phaser is a large dependency and the app entry remains above 500 KB minified; deeper scene/module splitting would be the next larger-scope bundle task.
- Verification: `npm run build:runtime-ui-art`, `npm run build:runtime-enemy-art`, `npm test`, `npm run build`, `npm run validate`, required generic `develop-web-game` client, and in-app browser menu/route/battle/Codex enemy smoke checks all passed with no browser warnings or errors.

- Startup bundle optimization follow-up: moved Codex-only tarot lore, bird facts, card meanings, and reserve enemy contract data into lazy `src/game/codex-data.ts`.
- Updated `CodexScene` to import the Codex data chunk on demand, rerender when it arrives, and keep visible card/enemy grids usable while the chunk resolves.
- Added a lightweight HTML/CSS boot shell so players see immediate feedback before Phaser finishes booting, removed by `BootScene.create()`.
- Build output after the split: app entry `index` is ~459 KB minified, lazy `codex-data` is ~249 KB, and `vendor-phaser` remains ~1.62 MB. Built HTML modulepreloads only `vendor-phaser`, confirming `codex-data` is deferred until Codex opens.
- Verification: `npm run build`, `npm test`, `npm run validate`, required generic `develop-web-game` client, and in-app browser menu/route/battle/Codex card-detail/enemy-grid smoke checks passed with no browser warnings or errors.

- Home screen splash-pop follow-up: generated `assets/splash/bird-squad-canal-run-splash-v4-menu-pop.webp` from the original splash PNG with a brighter, higher-saturation menu grade, removed the remaining full-screen dark wash, and pointed the menu splash import at the new asset.
- Verification: `npm run build`, in-app browser idle and leader-tooltip screenshots, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Home screen title placement follow-up: moved the animated Bird Squad title 22px higher and reduced the textured title image sizes by roughly 6% so the splash art has more room to read.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Home screen Ascension/readability follow-up: added a compact translucent backing plate behind the Ascension controls, brightened/stroked its label/detail text, unified title-screen button borders under a shared thin `MENU_BORDER_WIDTH`, and removed the thicker selected-leader outline in favor of color/fill state.
- Verification: `npm run build`, direct Playwright menu screenshot at `.artifacts/test-results/menu-ascension-border-pass.png`, and required generic `develop-web-game` client passed on rerun. The in-app browser navigation got stuck during reload, but direct Playwright reported no console errors and rendered the canvas correctly; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Boss backdrop variant wiring follow-up: added a generated backdrop variant registry for `assets/runtime/backdrops/variants/*.webp`, routed boss combat nodes through district-specific boss arenas, queued the selected arena via the existing optional art loader, and exposed `route.battlefieldAssetKey` / `route.battlefieldVariant` in the battle text-state.
- Added smoke coverage that starts each district boss fight and confirms the expected generated boss backdrop texture is loaded and rendered: Rooftop Blocks Tar-Crowned Crow, Canal Markets Gatekeeper, Signal Spires Beacon Breaker, and High Roost Warden.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused boss Playwright smoke tests, required generic `develop-web-game` client, and direct Playwright boss-scene visual capture passed. Inspected screenshot: `.artifacts/test-results/boss-backdrop-variant-rooftop.png`.
- Note: the special-node backdrop variants are generated and bundled but remain staged until the non-combat route/encounter presentation gets a proper backdrop surface.

- Text overflow/tidiness execution pass: tightened compact reward/preen card text bounds, removed lore paragraphs from small choice cards, kept full details on hover in the deck-card detail format, and shifted card reward/preen choices down to clear the deck-needs row.
- Cleaned the route boss inspector by allowing long titles to drive spacing, removing crowded likely-find micro-labels, and constraining boss prep pressure/prep/readiness text to the panel.
- Improved modal/overlay readability by making Codex detail curtains fully opaque, lowering the Codex detail viewport bottom, repositioning market hover detail away from buy controls, and darkening reward backdrops so battle text does not compete with choices.
- Verification: `npm run build`, `npm run validate:runtime`, focused route/reward/hover Playwright smoke tests, `git diff --check`, required generic `develop-web-game` client, and final screenshots under `output/text-audit-after/` passed/inspected.

- Codex visual upgrade pass: added shared tab/chip styling, a stronger header band, count-bearing section/filter tabs, intentional locked card backs, suit/family accents, Waymark trigger/effect chips, leader suit/status chips, enemy source/role chips, and card/enemy dossier framing.
- Verification: `npm run build`, direct Playwright Codex screenshots for cards/items/enemies/leaders and enemy detail, and required generic `develop-web-game` client passed. Final inspected screenshots include `.artifacts/test-results/codex-polish-leaders-final.png` and `.artifacts/test-results/codex-polish-enemy-detail.png`; only WebGL `ReadPixels` screenshot performance warnings appeared during direct capture, and the generic client returned valid `MenuScene` state JSON while its screenshot remained the known black WebGL capture artifact.

- Codex enemy bounds follow-up: tightened enemy thumbnail art into a framed 108x124 art well, shifted the text column right, and reduced the enemy type chip width so wide/tall enemy silhouettes no longer spill past card borders.
- Verification: `npm run build`, built-preview Playwright captures at `.artifacts/test-results/codex-enemy-bounds-top.png`, `codex-enemy-bounds-mid.png`, `codex-enemy-bounds-lower.png`, and `codex-enemy-bounds-deep.png`, `git diff --check -- src/main.ts progress.md`, and the required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON; only the known WebGL screenshot artifact/performance warnings appeared during capture.

- Codex enemy border harmonization follow-up: replaced the full bright district-colored enemy card outline with a muted steel outer frame, slim district top line, and small corner marker so enemy cards better match the broader Codex styling.
- Codex enemy sprite cleanup follow-up: removed the visible rectangular art-well border around real enemy cutout sprites and replaced it with a subtle grounding shadow, while keeping fallback art boxes for missing art.
- Verification: `npm run build`, built-preview Playwright captures at `.artifacts/test-results/codex-enemy-border-harmonized-top.png`, `.artifacts/test-results/codex-enemy-border-harmonized-reserve.png`, `.artifacts/test-results/codex-enemy-sprite-borderless-top.png`, and `.artifacts/test-results/codex-enemy-sprite-borderless-reserve.png`, `git diff --check -- src/main.ts progress.md`, and the required generic `develop-web-game` client passed. The direct captures showed contained enemy art with no visible sprite boxes; the generic client returned valid `MenuScene` state JSON.

- Aviary quality expansion implementation: added eight suitless Aviary cards (`aviary_28` through `aviary_35`) centered on bird qualities rather than tarot roles: Hover Check, Mobbing Call, Underwing Shelter, Scavenger Eye, Thermal Lift, Cache Memory, Brood Shield, and Mimic Thread.
- Wired the new Aviary cards into `data/game/alpha-cards.json`, the reward pool, Codex meanings, bird facts, runtime card art manifest placeholders, and the Aviary arcana identity data.
- Updated gameplay/docs/tooling so Aviary can be a no-suit card family rather than tarot: added `kind: "aviary"`, expanded the card count from 84/74 to 92/82, regenerated the 14-card Aviary center-art prompt pack, and changed Aviary art prompts/compositing language from roman-numeral trumps to compositor-owned top badge/title marks.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:content`, `npm run build`, `npm run validate`, `npm test`, full `npx playwright test tests/smoke.spec.ts` with 48/49 passing then focused rerun of the one flaky enemy Codex test passed, and the required generic `develop-web-game` client rendered the menu with valid `MenuScene` text state.

- Aviary fieldcraft expansion implementation: added eight more suitless Aviary cards (`aviary_36` through `aviary_43`) focused on practical bird instincts: Drumline Tap, Curb Step, Spiral Search, Reed Balance, Plunge Claim, Cold Plunge, Formation Draft, and Tool Probe.
- Wired the new fieldcraft cards into runtime card data, reward pool, Aviary arcana identity data, Codex meanings, bird facts, placeholder art manifest rows, art bible rows, alpha spec tables, and the generated Aviary center-art prompt pack.
- Updated deck/count contracts from 92/82 to 100 playable cards / 90-card reward pool and raised the app-entry bundle-size guard from 500 KB to 540 KB to match the current full-content build while keeping the check active.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:content`, `npm run build`, `npm run validate`, `npm test`, full `npx playwright test tests/smoke.spec.ts` (50/50 passed), `git diff --check`, and the required generic `develop-web-game` client against strict local port 5317 with valid `MenuScene` state and inspected menu screenshot.

- Enemy roster route-wiring follow-up: promoted 11 reserve enemy concepts into playable runtime enemies across the four districts, copied their optimized reserve art into combat full-art assets, added enemy-art manifest entries, and authored matching street/rival encounter definitions so the Codex reserve list now reflects true future concepts only.
- Tightened generated route combat payload selection so middle nodes receive encounter payloads after their final node type is chosen and street/rival/signal pools cycle through fresh payloads before repeats, improving per-run enemy variety without changing authored route quotas.
- Updated the enemy Codex to report `44 playable / 37 reserve concepts`, label reserve-only thumbnails as concepts, and keep promoted contracts displayed through the playable combat roster.
- Added focused smoke coverage for the new roster split and a deterministic seed sweep proving all 11 promoted encounter payloads can surface in generated route maps.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-promoted-playable-top.png`, and the required generic `develop-web-game` client all passed. The generic client returned valid `MenuScene` state JSON.

- Enemy roster batch 2 follow-up: promoted 12 more reserve concepts into playable enemies: Rat Courier, Weasel Cutpurse, Skunk Chemist, Armadillo Roller, Turtle Barricader, Cormorant Netter, Kingfisher Snapdiver, Heron Spearstepper, Bat Nightcaller, Kite Hawk Courier, Fox Lookout, and Peacock Intimidator.
- Added matching street/rival encounter payloads and mixed encounters across Rooftop Blocks, Canal Markets, Signal Spires, and High Roost, emphasizing meaningful additions that use current verbs: setup pressure, Snags, Winded, Open Sky, Frail, Cover locks, and heavy telegraphs.
- Copied each promoted reserve WebP into `assets/runtime/enemies/full/`, added enemy-art manifest entries, and updated the Codex/route-exposure smoke expectations to `56 playable / 25 reserve concepts` and 23 total promoted encounter payloads.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-batch2-playable-top.png`, direct generated-route combat screenshot `.artifacts/test-results/battle-batch2-rat-courier.png`, and the required generic `develop-web-game` client all passed. The combat capture launched `enc_rat_courier` from a generated route and showed Rat Courier's `Run Message` Tell.

- Enemy roster batch 3 follow-up: promoted 8 more reserve concepts that fit current combat verbs without new terrain/object rules: Alley Cat, Jackrabbit Sprinter, Hedgehog Curb-Crosser, Mongoose Counterfighter, Opossum Decoy, Goose Bouncer, Mantis Street Duelist, and Moth Lantern-Drifter.
- Added single and mixed street/rival encounters for Rooftop Blocks, Canal Markets, and Signal Spires, with gameplay shapes focused on burst setup, speed pressure, low Cover blockers, counterfighting, feints, lane-control charging, precision duels, and lantern distraction.
- Copied the promoted reserve WebPs into `assets/runtime/enemies/full/`, added enemy-art manifest entries, and updated smoke expectations to `64 playable / 17 reserve concepts` and 31 total promoted encounter payloads.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-batch3-playable-top.png`, direct generated-route combat screenshot `.artifacts/test-results/battle-batch3-alley-cat.png`, and the required generic `develop-web-game` client all passed. The combat capture launched `enc_alley_cat` from a generated route and showed Alley Cat's `Stalk` Tell.

- Codex enemy shadow follow-up: replaced the fixed enemy thumbnail/detail ellipse placement with texture-alpha visible-bounds anchoring so enemies with different transparent crops have shadows under their actual stance instead of a shared low offset.
- Verification: `npm run build`, focused enemy Codex smoke test, direct Playwright Codex screenshots at `.artifacts/test-results/codex-enemy-shadows-grid.png`, `.artifacts/test-results/codex-enemy-shadow-roof-rat-detail.png`, and `.artifacts/test-results/codex-enemy-shadow-alley-cat-detail.png`, plus required generic `develop-web-game` client. The first generic-client attempts failed only from PowerShell JSON quoting; the rerun with the skill reference actions file passed and returned valid `MenuScene` state JSON.

- Molt state/targeting contract pass: added an active card contract that derives the current effect text, active target, base target, and Molt label from the card plus current flock Molt state.
- Wired the active contract through hand cards, hover previews, card detail/inspect payloads, click targeting, card resolution, and card-cast VFX so Molt cards that turn self/none cards into enemy-targeted effects now require an enemy click and display their true active behavior.
- Added runtime validation visibility for Molt target shifts; the current data set intentionally reports 68 cards whose active Molt target differs from their base card target.
- Added smoke coverage for Locked Nest's self-to-enemy Molt behavior and a synthetic none-target card that Molts into target damage/Winded without auto-firing ambiguously.
- Verification: `npm run build`, `npm test`, focused Molt Playwright smoke tests, full `npm run test:e2e` (52/52 passed), required generic `develop-web-game` client, and direct Molt combat capture passed. Inspected screenshot/state artifacts: `.artifacts/test-results/molt-active-card-contract.png` and `.artifacts/test-results/molt-active-card-contract-state.json`.

- Enemy description art-direction polish follow-up: strengthened seven weaker runtime enemy descriptions/visualBriefs with concrete fashion/accessory, streetwear, and urban-place cues while preserving anatomy constraints and gameplay data.
- Re-ran the fashion/urban heuristic audit: all 64 playable enemies now meet the cue threshold. Verification passed with `npm run validate:runtime`, `npm run build`, focused enemy Codex smoke, the required generic `develop-web-game` client, and direct Codex detail screenshot `.artifacts/test-results/codex-enemy-fashion-copy-pass.png`.

- Reward card-choice visual polish follow-up: replaced the post-combat Add-to-Flock/Preen choice cards' heavy text slabs with larger art-forward card frames, lighter glass panels, suit-accent borders, stroked labels, and compact footer metadata; also aligned the market Add-to-Flock offer tile with the same mini-card treatment.
- Verification: `npm run build`, focused reward/market/card-detail Playwright smoke tests, required generic `develop-web-game` client, and production-preview screenshots in `.artifacts/test-results/reward-card-polish-final/`.

- Combat tension tuning follow-up: audited flock/card/enemy scaling and found the main player-favoring pressure came from passive Damage/Cover stats compounding through multi-hit, AoE, burst, Surge, and Nests carry interactions rather than from route economy drift.
- Tuned runtime combat scaling so passive Damage applies once per played card, AoE gets a smaller passive Damage bonus, Resonance/Winded bursts no longer double-dip passive Damage, passive Cover is softened, Surge gives +1 instead of +2, and the Nests Cover carry cap is 8.
- Added `npm run audit:balance` for the existing balance-audit script and updated formation smoke coverage for the new Surge contract.
- Verification: `npm run audit:balance`, `npm run audit:economy`, `npm run build`, `npm test`, `npx playwright test tests/smoke.spec.ts --grep combat`, `npx playwright test tests/smoke.spec.ts --grep "Cover"`, and the generic `develop-web-game` client passed. Latest generic client screenshot/state: `output/web-game/shot-1.png`, `output/web-game/state-1.json`.

- Market layout polish follow-up: rebuilt the route market overlay as a stall row with a string-light lane, awning trim, a recruit perch, waymark board, and preen bench so the screen reads like visiting a market instead of three menu boxes.
- Verification: `npm run build`, focused Playwright smoke tests for market shelves/route event backdrops/card hover details, required generic `develop-web-game` client, and production-preview screenshot `.artifacts/test-results/market-layout-polish/market-stall-layout-final.png`.

- Market decision pass: replaced one-shot market offers with seeded per-visit shelves: four rarity-priced card listings, two unowned Waymark listings, a utility shelf with Preen/Release/Supply options, and paid Scrap refresh that rerolls fresh stock while individual purchases sell out their row.
- Market prices now use the existing `alpha-market.json` bands with rarity/district scaling, and the route text-state exposes full market shelves plus refresh cost/count for tests and debugging.
- While verifying, build surfaced existing in-progress combat-state type drift (`fouled`, enemy roles/solo/damageBonus); aligned the constructors/text payloads with those fields without rolling back that work.
- Verification: `npm run build`, focused Playwright smoke tests for market shelves/route event backdrops/card hover details, required generic `develop-web-game` client, in-app browser opened `http://127.0.0.1:5173/`, and inspected market screenshot `.artifacts/test-results/market-meaning/market-stocked-final.png`.

- Route map node-spacing fix: route node placement now reserves explicit visual room for focus ticks above icons and reward-badge pips below icons, then spaces lanes across the usable graph band so four-lane generated columns do not overlap.
- Exposed `position` and `visualBounds` for route nodes in the text-state harness and added a smoke test that sweeps 4 districts x 12 seeds to fail on any overlapping generated route-node footprint.
- Verification: `npm run build`, focused route-map Playwright smoke tests, required generic `develop-web-game` client against built preview, in-app browser opened built preview `http://127.0.0.1:5324/`, and inspected built-preview screenshot `.artifacts/test-results/route-node-spacing/route-four-lane-spacing.png`.

- Enemy tension/mechanics pass: added explicit enemy roles, solo non-boss encounter tuning (+HP and +1 persistent damage), ally-support enemy verbs, and the new Fouled poison status.
- Authored support/poison identities into existing enemies: Signal Gull, Market Crow, Cicada Static Swarm, Crane Signal Caller, Moth Lantern-Drifter, Skunk Chemist, and Vulture Cleanup Crew; tagged supporting/poison encounters across districts.
- Expanded runtime validation/audit coverage for enemy roles, support/poison verbs, encounter tags, status registry, and balance-audit composition reporting.
- Added smoke coverage for solo tuning, support companion targeting, and Fouled tick behavior.
- Verification: `npm run validate:runtime`, `npm run audit:balance`, `npm run build`, `npm test`, full `npx playwright test tests/smoke.spec.ts`, and the required generic `develop-web-game` client passed. Latest generic screenshot/state: `output/web-game/shot-1.png` and `output/web-game/state-1.json`.

- Combat convenience HUD pass: added an incoming-damage prediction to the flock Cohesion bar. Visible enemy attack intents now create a red at-risk segment on the current Cohesion fill with a `-N / After X` marker after current Cover is applied.
- Exposed the same prediction in the combat text-state as `flock.incoming` (`total`, `blocked`, `hpLoss`, `afterHp`, `attackers`) and added smoke coverage for the math.
- Verification: `npm run build`, focused Playwright smoke test `flock Cohesion bar exposes incoming damage prediction after Cover`, `npm test`, required generic `develop-web-game` client, and inspected combat screenshot `.artifacts/test-results/combat-incoming-health-preview.png`.

- Support encounter composition follow-up: converted every solo support encounter into a paired fight with at least one non-support threat so support enemies create target-priority decisions instead of low-pressure stall turns.
- Updated Signal Gull, Market Crow, Cicada Static Swarm, Crane Signal Caller, and Vulture Cleanup Crew encounters with companion enemies and `multi`/`support` tags where needed.
- Added a runtime data validation guard that fails any support-only encounter composition, including future support-only pairs.
- Verification: focused support-composition audit script, `npm run validate:runtime`, `npm run audit:balance`, `npm run build`, `npm test`, required generic `develop-web-game` client, and inspected `output/web-game/shot-1.png`.

- Market shop-scene redesign follow-up: researched Slay the Spire and Monster Train shop presentation, then rebuilt the market overlay from a row-list layout into a place-like shop scene with a vendor, awning/string lights, card rack, waymark tray, service signs, object-mounted price tags, refresh sign, and sold slats.
- Kept the existing randomized shelves, rarity-priced offers, paid refresh, finite purchases, and hover details intact while making the visual grouping follow goods/services/currency rather than three menu boxes.
- Verification: `npm run build`, focused market/hover/event Playwright smoke tests, required generic `develop-web-game` client against built preview, and inspected `.artifacts/test-results/market-shop-redesign/market-shop-scene-final.png`.

- Aviary art production pass: generated center art for `aviary_28` through `aviary_43` using the Minor Arcana center-art workflow, strengthened Aviary prompt language toward the title splash's bold streetwear silhouette, and composited the full 22-card Aviary set under the shared `master.png` raster border.
- Promoted the 16 new Aviary final cards into `.generated/imagegen/tarot/selected`, approved their runtime manifest entries, and rebuilt optimized portrait/thumb/icon WebP assets for the game.
- Verification: Aviary composite validation, center/final contact sheets, `npm run build:runtime-card-art`, `npm run validate`, `npm run build`, `npm test`, full `npx playwright test tests/smoke.spec.ts` (60/60 passed), and a local Playwright menu smoke against Vite dev server passed. QA outputs live under `tmp/qa/aviary/2026-06-17-aviary-master-border-expansion-v1/`.
- Aviary art anatomy correction: replaced `aviary_33` / Cache Memory because the first Pinyon Jay art had a hand-like lifted limb near the cache. The corrected card keeps both feet planted below the body and holds the cache only in the beak, then was recomposited, promoted, and rebuilt into runtime WebP assets. Verification: `npm run validate:aviary-composites`, `npm run validate:runtime-assets`, `npm run build`, and `npm run validate`.
- Aviary art two-wing correction: regenerated `aviary_30` / Underwing Shelter, `aviary_39` / Reed Balance, and `aviary_43` / Tool Probe with clearer two-wing silhouettes and no wing-hand prop interactions. The corrected centers were recomposited under the shared `master.png` border, promoted to selected art, and rebuilt into portrait/thumb/icon WebP runtime assets.
- Market art composition pass: generated runtime-sized market-kit assets from the concept outputs, wired the market node to use the new landmark backdrop, and composed the in-game shop with the transparent starling shopkeeper, hanging sign, and counter/wares layers while preserving existing purchase interactions. Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run validate:bundle-size`, focused market Playwright smoke tests, the required web-game client, and screenshot review at `.artifacts/test-results/market-kit-in-game.png`.

- Market art-first presentation pass: reduced the shop offer UI from heavy framed blocks into art-forward goods on the landmark scene. Card offers now show the full card thumbnail with only a tiny rarity chip and price tag, Waymarks/artifacts sit as counter objects with stats moved to hover, and supply/service offers use object tokens plus invisible hit areas instead of boxed menu rows.
- Added smoke coverage for non-card market item detail panels so Waymark/supply-style offers keep their stats available through hover while the scene stays visible.
- Verification: `npm run build`, focused market Playwright smoke tests (`market shelves`, generated market kit, card hover details, non-card item detail), required `develop-web-game` client with inspected `output/web-game/shot-1.png`, in-app browser opened `http://127.0.0.1:5273/`, and inspected market screenshot `.artifacts/test-results/market-art-first-in-game.png`.

- Market scenic open-frame pass: removed the generic `Landmark Visit` modal panel from the market and let the generated shop background fill the full viewport. The market now uses only subtle hairline guides, small floating Scrap/Refresh/Close controls, lighter scene masks, and a softer vendor nameplate so the shop reads as a place instead of a bordered menu.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client with inspected menu screenshot, dev-server market screenshot `.artifacts/test-results/market-scenic-open-in-game.png`, and built-preview market screenshot `.artifacts/test-results/market-scenic-open-built-preview.png`.

- Market opaque-readability pass: replaced the remaining transparent market guide borders and glassy control plaques with compact opaque enamel tags, physical shelf rails, solid hover-detail backs, explicit Waymark art loading, and a decorated Waymark fallback. This keeps price/scrap/service text readable while leaving the generated landmark scene more visible.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-opaque-readable-built-preview.png`.

- Market display polish pass: made the shop offers feel more physically staged by adding card hanger pins, small card stand feet, shaped opaque price tickets, individual Waymark display pads, and slimmer service perches. The final tuning kept the decorative hardware around the goods while avoiding the large opaque trays/signboards that made the first take feel too heavy.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-display-polish-final-built-preview.png`.

- Market asset-led cleanup pass: removed decorative shelf rails, card mounts, rarity chips, Waymark pads, service perches, and constructed supply symbols so the generated cards, shopkeeper, counter wares, and Waymark assets carry the scene with only subtle shadows and compact opaque price tags.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-assets-standalone-final-built-preview.png`.

- Codex supplies pass: expanded the Codex Items section to include the 5 authored supplies from `data/game/alpha-supplies.json` alongside the 40 Waymarks, with a Supplies filter tab, supply cards, and supply detail panels.
- Added focused smoke coverage for the Codex Supplies tab and Seed Packet detail panel.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts -g "codex: supplies are listed"`, required `develop-web-game` client, and visual screenshot inspection at `.artifacts/test-results/codex-supplies-tab.png` and `.artifacts/test-results/codex-supply-detail.png`.

- Expanded supplies from 5 to 17 entries with distinct tactical roles: pierce damage, anti-Cover, hand fixing, cleansing, Molt entry, burst turns, Open Sky safety, and high-cover defense.
- Added `visualBrief` art-generation descriptions to every supply, tuned toward the Bird Squad title splash art's neon rooftop streetwear/salvage style, and surfaced those briefs in the Codex supply detail panel.
- Wired expanded supply verbs in combat (`damage`, `damagePierce`, `damageAll`, `removeCover`, `applyWinded`, `cleanseFlock`, `enterMolt`, `discard`, `loseCohesion`, `nextTurnDraw`) and extended runtime validation/type answer sets.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Codex supplies and expanded supply verbs, required `develop-web-game` client, and visual screenshot inspection at `.artifacts/test-results/codex-expanded-supplies-tab.png` and `.artifacts/test-results/codex-bottlecap-popper-brief.png`.

- Market title-button/readability pass: matched Scrap, Refresh, Close, and price tags to the title screen's dark enamel button style, set all market-scene art images to full opacity, enlarged the shopkeeper/sign/counter/cards/Waymark goods, and gave service labels compact opaque backplates.
- Replaced repeated counter-wares supply placeholders with dedicated large supply icons where available, falling back to proxy Waymark/object art only for supplies without runtime icons yet.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-title-style-buttons-supply-art-built-preview.png`.

- Supply art completion pass: generated transparent runtime icons for the 12 expanded supplies that did not yet have art (`bottlecap_popper`, `tar_solvent`, `feather_splint`, `mirror_shard`, `rooftop_decoy`, `molt_pin`, `wire_snips`, `spare_harness`, `signal_kite`, `storm_lantern`, `cache_key`, `rain_cape`) using the supply visual briefs and title-splash neon rooftop salvage style.
- Added matching chroma source PNGs under `assets/concept-art/supplies/sources`, kept the 5 existing supply icons intact, and wired battle supply slots to preload and display supply art instead of text-only labels.
- Verification: transparent-alpha audit for all 17 supply icons, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, and visual screenshot inspection at `tmp/qa/supplies/supply-icons-contact-sheet.png` and `tmp/qa/supplies/codex-supply-art.png`.

- High-resolution pixel-art supply pass: used the image-generation workflow against the title splash style, then replaced all 17 runtime supply icons with 512x512 transparent high-res pixel-art cutouts authored from the supply descriptions and scaled with nearest-neighbor crisp edges.
- Saved new pixel chroma source PNGs as `*-pixel-chroma-v1.png` under `assets/concept-art/supplies/sources`, replacing the runtime WebP icons the Codex, market, and battle scenes already load.
- Verification: 17-icon alpha/dimension audit, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, and visual inspection at `tmp/qa/supplies/pixel-art/supply-icons-pixel-contact-sheet.png` and `tmp/qa/supplies/pixel-art/codex-supply-pixel-art.png`.

- Market selection agency pass: renamed the player-facing Nest removal option to `Remove a Card`, changed route/market removal copy from Release to Remove, and converted market Preen/Remove services from predetermined hidden targets into player-selected deck actions that charge Scrap only after the chosen card is picked.
- Enlarged market card-sale art and upgraded the Preen/Remove picker from a text list into visible deck card thumbnails with compact price chips, readable name backplates, and hover details for full stats.
- Verification: `npm run build`, focused market/card-picker Playwright smoke tests, required `develop-web-game` client against built preview, pixel sanity checks, and screenshot review at `.artifacts/test-results/market-selection-main-built-preview.png` and `.artifacts/test-results/market-selection-preen-picker-built-preview.png`.

- Market item cleanup pass: removed dark shadow pads from Waymark and utility/supply items, replaced split item label + separate price tags with one bordered box per non-card item, and removed the extra offset shadow from non-card hover detail panels.
- Kept price/name/action text inside fixed bordered boxes with constrained wrapping so long names like `Chalk Wingmark` and supplies such as `Tar Solvent` stay contained while the art remains readable.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, pixel sanity checks, and built-preview screenshots `.artifacts/test-results/market-clean-single-box-built-preview.png` and `.artifacts/test-results/market-clean-single-box-hover-built-preview.png`.

- Market service glyph cleanup: removed the floating `P` and `R` fallback letters from Preen/Remove service offers because the bordered service buttons now communicate those actions clearly enough on their own.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-no-floating-service-glyphs-built-preview.png`.

- Pixel-art supply wiring pass: added a shared supply-art image helper that applies Phaser's per-texture `NEAREST` filter before rendering supply icons, preserving crisp pixel-art edges while leaving the rest of the painterly game art on smooth filtering.
- Confirmed supply art renders through Codex item cards/detail panels, market utility offers, and battle supply slots using the existing `supply-*` runtime keys.
- Verification: `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, runtime probe showing all 17 visible `supply-*` images in Codex and `supply-bottlecap_popper` filter mode `1` (`NEAREST`), plus `npm run validate:runtime`.

- Imagegen sheet processing pass: split the supplied green-background 17-icon sheet from `C:\Users\Will\Desktop\download.png` into individual transparent 512x512 runtime WebP supply icons, preserving high-resolution pixel-art edges and replacing the prior generated runtime cutouts.
- Saved the original sheet and per-supply source crops under `assets/concept-art/supplies/sources`, with transparent QA PNGs and contact sheets under `tmp/qa/supplies/imagegen-sheet`; corrected the Molt Pin and Rain Cape crop boxes after visual QA caught row-overlap artifacts.
- Verification: 17-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex probes/screenshots confirming all 17 supply textures load after grid scroll with `NEAREST` filter mode.

- Codex supply presentation pass: removed the circular supply icon backplates from Codex supply cards and removed the boxed art frame from supply detail modals so the transparent pixel-art items stand on their own.
- Verification: `npm run build`, focused Playwright Codex supply smoke test, required `develop-web-game` client on `http://127.0.0.1:5173`, and visual screenshot review at `tmp/qa/supplies/codex-supply-standalone-icons.png` plus `tmp/qa/supplies/codex-supply-standalone-detail.png`.

- Flock leader Codex art pass: generated transparent forward-facing 3/4 runtime Codex art for Fledgling, Spark-Caller, Tidewarden, and Roostkeeper, matching the already-wired Talon treatment while preserving each leader's species, outfit, gear, and color identity.
- Wired the Codex leader art override table so all five flock leaders now request `front-3q` WebP assets in leader thumbnails/details, leaving combat/menu art on the original leader art paths.
- Verification: `npm run build`, required `develop-web-game` client against the built preview, and focused Playwright Codex screenshots at `.artifacts/fledgling-codex-front-art-check.png`, `.artifacts/spark-caller-codex-front-art-check.png`, `.artifacts/tidewarden-codex-front-art-check.png`, and `.artifacts/roostkeeper-codex-front-art-check.png`. Final request log showed all five leader assets using `front-3q`.

- Market best-next presentation pass: replaced the wide bottom market narration slab with a compact opaque Veyra bark tucked onto the vendor side, split Preen/Remove into a small service-board area, and staged Supplies as larger physical counter objects with one compact purchase ticket each.
- This pass keeps hover details and the randomized purchase logic intact while clearing more of the landmark scene and making the Supply objects read as actual goods instead of menu rows.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against the built preview, `git diff --check`, and visual screenshot inspection at `.artifacts/test-results/market-best-next-built-preview.png`.

- Tidewarden anatomy correction: replaced the forward-facing Tidewarden Codex asset with a no-arms variant so the rain jacket drapes over folded wings instead of reading as humanlike sleeves/hands.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and focused Codex screenshot `.artifacts/tidewarden-codex-no-arms-check.png` confirmed the built game requests `tidewarden-combat-front-3q-TtOWaJQn.webp`.

- Tidewarden sleeved back-facing pass: generated a replacement `tidewarden-combat-back-ne` asset to match the sleeved front-facing Tidewarden identity while preserving the back/NE combat orientation. The replacement keeps the teal jacket sleeves, hood, crossed back straps, olive medic satchel, red-cross tag, heron head profile, and long-legged silhouette.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and direct BattleScene screenshot `.artifacts/tidewarden-back-ne-sleeved-battle-check.png` confirmed the built game requests `tidewarden-combat-back-ne-B567ol0E.webp`.

- Waymark imagegen art pass: generated four 10-icon high-resolution pixel-art sheets for all 40 Waymarks, matching the supply sheet's neon rooftop salvage style and preserving standalone object silhouettes on transparent runtime backgrounds.
- Extracted and saved the imagegen sheets plus per-Waymark source crops under `assets/concept-art/waymarks/sources`, then split/chroma-cleaned them into 512x512 transparent runtime WebP icons in `assets/runtime/waymarks/icons`.
- Updated Waymark rendering to use nearest-neighbor pixel-art filtering and removed Codex Waymark thumbnail/detail art backplates so the cutouts stand on their own like the supplies; reward, market, and combat Waymark image calls now use the same helper path.
- Verification: 40-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex probe confirming 40 Waymarks cataloged, visible Waymark textures loaded, and `waymark-chalk_wingmark` using `NEAREST` filter mode. QA outputs live under `tmp/qa/waymarks/imagegen-sheets/`.

- Build-around item expansion pass: added 6 supplies (`Return Ticket`, `Nest Clamp`, `Smoke Thread`, `Windcatcher Spool`, `Resonance Battery`, `Oath Ledger`) and 8 Waymarks (`Flock Counterweight`, `Supply Bell`, `Molt Metronome`, `Eave Bloom`, `Plumes Busker Patch`, `Quills Pressure Gauge`, `Basin Overflow Cup`, `Nest Ratchet`), bringing Codex Items to 71 total (48 Waymarks / 23 Supplies).
- Wired new runtime hooks for Waymarks that trigger on Supply use, first Molt entry, first suit card played each turn, and healing restored each turn; added richer mark effects for Waymark-count Cover scaling, Resonance gain, all-enemy ping damage, next-turn draw/Wingbeat, and next Nest Cover bonuses.
- Extended supply execution with `returnDiscard`, `nextCoverBonus`, `gainEnergyNextTurn`, `gainScrap`, and `resonanceBurst`, then added temporary transparent pixel-art placeholder icons for the new supplies and Waymarks under the runtime icon folders plus matching concept source PNGs.
- Updated validation/smoke coverage for the new item counts, new supply assets, and the build-around gameplay hooks. Raised the Waymark icon budget to match the intentional 512px high-resolution runtime format instead of the old small-icon budget.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Codex supplies/expanded supply verbs/new build-around hooks, required `develop-web-game` client on `http://127.0.0.1:5173`, live Codex screenshot at `tmp/qa/items-expansion/codex-items-71-new-supplies-waymarks.png`, and `npm test`.

- Market larger-card/no-overlap pass: increased market card offer art from 164x234 to 176x252, widened the card hit/display rack, and moved the Waymark lane lower so card price tickets and Waymark objects no longer crowd each other.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against the built preview, `git diff --check`, and screenshot inspection at `.artifacts/test-results/market-larger-cards-no-overlap-built-preview.png`.

- Market Veyra/title readability pass: removed Veyra's speech bubble and lower nameplate, moved `Veyra Tallybright / CANAL CURATOR` into a compact top title tag, and added small opaque backing plates behind Waymark and supply objects so their transparent art reads against the landmark scene without rebuilding the whole shop into menu boxes.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-veyra-title-object-backs-built-preview.png`.

- Market shopkeeper scale/no-overlap pass: enlarged Veyra's shopkeeper art and moved her further left while keeping the first card lane clear; shifted the utility/supply lane right and lowered supply label boxes so object backplates, labels, and the fourth card no longer crowd each other.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` for `src/main.ts`/`progress.md` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-large-left-shopkeeper-no-overlap-built-preview.png`.

- Market larger-shopkeeper hard-spacing pass: scaled Veyra up again and anchored her below the bottom edge so she reads as a foreground shopkeeper, then narrowed and re-spaced the card rack and right utility/supply lane with explicit horizontal and vertical gaps to remove remaining item overlaps.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-shopkeeper-bigger-bottom-hard-spacing-built-preview.png`.

- Market composition nudge: lowered Veyra's bottom anchor so her feet sit below the viewport, moved her slightly right, and lifted the card rack while preserving the hard-spaced card and goods lanes. After screenshot review, dropped the shopkeeper anchor further so the feet are actually clipped below the screen edge.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-shopkeeper-feet-under-cards-up-built-preview.png`.

- Tidewarden back-facing sleeve correction: replaced the back/NE asset again so the teal sleeve fabric drapes over folded wings instead of forming arm-like tubes; preserved the sleeved front identity, hood, crossed back straps, satchel, red-cross tag, heron profile, and long-legged silhouette.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and direct BattleScene screenshot `.artifacts/tidewarden-back-ne-draped-wing-battle-check.png` confirmed the built game requests `tidewarden-combat-back-ne-z9XIXyYe.webp`.

- Flock leader starter deck refresh: rebuilt all five 10-card singleton starter decks around the expanded card pool. Fledgling now teaches the four new suit Fledgling crew cards; Spark-Caller, Talon, Tidewarden, and Roostkeeper each open with a six-card suit keystone package plus newer off-suit support cards that reinforce their signatures.
- Updated the mirrored `alpha-cards.json` global starter/reward partition for the Fledgling deck, keeping 10 starter cards, 90 reward cards, and no starter/reward overlap.
- Verification: `node tools/balance-audit.mjs`, starter/reward overlap audit, all-leader deck validity check, `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for leader starter selection / Plumes keystone / Codex deck discovery, and required `develop-web-game` client on `http://127.0.0.1:5623/`.

- New item imagegen art pass: generated dedicated high-resolution pixel-art sheets for the 6 new supplies and 8 new Waymarks from the build-around item expansion, matching the title-splash/neon rooftop salvage style and avoiding border circles or backplates so each item stands alone.
- Chroma-cleaned and split the sheets into transparent 512x512 crops, replacing the temporary runtime placeholders in `assets/runtime/supplies/icons` and `assets/runtime/waymarks/icons`; saved source sheets/crops under `assets/concept-art/supplies/sources` and `assets/concept-art/waymarks/sources`.
- Verification: 14-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, runtime asset-size validation, `npm run build`, focused Playwright Codex/item smoke tests, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex screenshots/probe under `tmp/qa/items-imagegen/new-items/` confirming `supply-return_ticket` and `waymark-flock_counterweight` load with `NEAREST` filtering.

- Next item pass brief: added `docs/game/item-expansion-next-pass.md` with current item coverage gaps, next-pass design targets, proposed route/either supplies, conditional Waymarks, new runtime trigger/verb candidates, art-generation sheet guidance, and test targets for the next implementation pass.

- Market item-row alignment pass: moved Supply offers down to share the Waymarker display line, renamed compact item tags from `PIN` to `WAYMARKER` and from `PACK` to `SUPPLY`, and nudged Veyra slightly right while scaling her up so the shopkeeper remains a stronger foreground landmark figure.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-supplies-waymarker-level-labels-built-preview.png`.

- Market stock/cluster pass: moved the card rack higher, shifted Veyra farther right toward the lower goods area, tightened the Waymarker spacing, moved the Supply row left into the same goods cluster, and expanded market stock to offer 3 Waymarkers plus 3 Supplies.
- Follow-up overlap fix: moved the Preen/Remove service boxes to the far-right side so they no longer sit over the fourth card art after the card rack was lifted.
- Verification: `npm run validate:runtime`, `npm run build`, focused market Playwright smoke tests with 3 Waymarker / 3 Supply assertions, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-three-and-three-cluster-service-clear-built-preview.png`.

- Market service-stack and goods nudge: bumped Waymarker/Supply objects and labels upward, shifted the lower goods cluster slightly left, removed the oversized top Refresh sign, and rendered Refresh as a third compact service row under Remove with the same size/style as Preen and Remove.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-refresh-service-stack-final-built-preview.png`.

- Market goods parity pass: moved the Preen/Remove/Refresh stack upward to sit more centered beside the cards, and standardized Waymarker/Supply item presentation to matching art plates, art size, label size, hit area, and spacing rhythm.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-service-centered-goods-parity-built-preview.png`.

- Market title banner pass: moved Veyra Tallybright's banner upward, widened it into a centered title plaque, and centered/scaled both the shopkeeper name and Canal Curator subtitle so the header reads as part of the landmark scene.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-centered-title-banner-built-preview.png`.

- Market goods row alignment pass: moved the Waymarker/Supply cluster upward together and put both offer types on a shared row baseline with matching art centers, label centers, sold slats, and hit areas.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-goods-shared-row-built-preview.png`.

- Market goods spacing fix: shifted the Waymarker shelf left so the rightmost Waymarker label no longer overlaps the leftmost Supply label while preserving the shared lower-row baseline.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-waymarkers-shifted-left-built-preview.png`.

- Market scrap badge polish: simplified the Scrap counter into a tighter opaque enamel badge with a cleaner gold border, centered value alignment, and stronger text contrast.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-scrap-badge-polish-built-preview.png`.

- Market shopkeeper idle pass: added a subtle foot-anchored breathing tween to Veyra's shopkeeper art and a timed single-eye blink overlay while keeping the shopkeeper texture as a top-level rendered image for existing market art probes.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, animation probe confirming shopkeeper scale and blink alpha changes, and screenshot inspection at `.artifacts/test-results/market-shopkeeper-idle-built-preview.png`.

- Market shopkeeper blink visibility fix: replaced the too-subtle shape overlay with a closed-eye shopkeeper art variant (`assets/runtime/market-kit/starling-shopkeeper-blink-v1.webp`) and crossfaded it over the base shopkeeper during idle blinks so the blink is visible in the rendered market scene.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and closed-frame blink probe/screenshot inspection at `.artifacts/test-results/market-shopkeeper-blink-visible-built-preview.png` plus head crop `.artifacts/test-results/market-shopkeeper-blink-head-crop.png`.

- Market shopkeeper TravQuest-style blink revision: removed the full-body closed-eye variant and switched Veyra to a tiny low-alpha eyelid line over the existing shopkeeper art, timed as a brief idle blink while keeping breathing separate.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and closed-frame screenshot inspection at `.artifacts/test-results/market-shopkeeper-travquest-blink-line-final-built-preview.png` plus head crop `.artifacts/test-results/market-shopkeeper-travquest-blink-line-final-head-crop.png`.

- Market shopkeeper blink placement correction: moved the blink from the face feathers onto the rendered eye, replaced the too-subtle line with a small eyelid patch container above the shopkeeper art, and shortened the repeat delay so the blink is noticeable during normal market viewing.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against `http://127.0.0.1:5636/`, and open-vs-closed eye comparison at `.artifacts/test-results/market-shopkeeper-eye-open-vs-closed-patched-crop.png`.

- Market shopkeeper blink removal: removed the eyelid patch and blink alpha tween entirely after visual review showed the mechanic did not suit the shopkeeper art; Veyra keeps only the subtle breathing idle tween.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against `http://127.0.0.1:5636/`, runtime probe confirming zero blink-named objects and one shopkeeper idle tween, and screenshot inspection at `.artifacts/test-results/market-shopkeeper-no-blink-built-preview.png`.

- Placeholder/load flash audit: identified live fallback renderers for battle backdrops, flock leaders, enemies, route node glyphs, route-event backdrops, market kit art, card offers, Waymarkers, and Supplies; these are still useful load-failure guards, but normal scene entry was sometimes rendering before optional art finished.
- Added preload-time runtime art queuing for RouteScene and BattleScene so first render waits for route node icons, market kit/backdrop art, route deck cards, battlefield art, current flock leader art, active waymarks/supplies, current battle deck, and current enemies. Battle's follow-up optional art queue now requests only active run assets instead of every item icon globally.
- Safety call: runtime item icons referenced by supply/Waymark data are not safe to remove while their data entries remain live; fallback drawing branches should stay as failure guards. Any old concept-source placeholder PNGs are provenance-only once replaced by final runtime art.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5623/`, BattleScene preload texture probe/screenshot at `.artifacts/placeholder-preload-battle-check.png`, RouteScene/market preload probes, and market screenshot at `.artifacts/placeholder-preload-market-check.png`.

- Route map edge-line refinement: reduced route board connection stroke widths so inactive/secondary/available/lit edges read as thinner map lines without changing node rings, icons, labels, or column guides.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and route screenshot inspection at `.artifacts/route-map-thin-edge-lines-check.png`.

- Route map dotted-path refinement: replaced straight edge segments with deterministic quadratic dotted paths. Each route connection now curves between nodes with small stamped dots, preserving the existing color/alpha hierarchy for inactive, secondary preview, available, and primary/lit routes.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and route screenshot inspection at `.artifacts/route-map-curved-dotted-paths-check.png`.

- Route map breathing-room pass: widened the map board, moved the route description inspector farther right, expanded graph bounds, spread node lanes slightly farther apart, and added a soft halo to highlighted dotted paths so reachable/preview edges remain readable after the dotted-path styling.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with two reachable nodes, and screenshot inspection at `.artifacts/route-map-wide-breathing-highlight-check.png`.

- Next item pass implementation: wired 8 supplies (`Map Sticker Strip`, `Thermos Lid`, `Bus Token Cache`, `Chalk Dust Pouch`, `Market IOU`, `Sky Sugar`, `Anchor Threader`, `Spare Pocket`) and 10 Waymarks (`Broken Cover Chime`, `Spark Ground Clip`, `Quiet Roost Token`, `Double-Packed Buckle`, `Ledger Tab`, `Basin Safety Pin`, `Nest Measure Line`, `Plumes Applause Cap`, `Molt Shadow Tag`, `Cache Divining Hook`), bringing Codex Items to 89 total (58 Waymarks / 31 Supplies).
- Added route supply slots and route-side supply use, plus runtime hooks/effects for route previews, permanent supply capacity, enemy Cover-break draw, Resonance-spend Cover, no-damage-turn healing, next-supply repeat, heal cleanse, Nests hand retention, Plumes fourth-card payoff, Molt Open Sky guard, market rebates, and cache supply picks.
- Generated matching high-resolution pixel-art sheets for the 18 new items, chroma-cleaned them into transparent 512x512 runtime icons under `assets/runtime/supplies/icons` and `assets/runtime/waymarks/icons`, and saved imagegen source sheets/crops under the concept-art source folders.
- Verification: `npm run validate:runtime`, runtime asset-size validation, `npx tsc --noEmit`, focused Playwright smoke tests for Codex/item hooks, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173`.

- Next item supply art repair: replaced the 8 next-pass Supply runtime icons with individually generated high-resolution pixel-art cutouts instead of grid crops, removing sheet-boundary clipping and fixing Anchor Threader's green tape alpha damage by switching to a magenta-key workflow with edge despill.
- Verification: final supply contact sheet at `tmp/qa/items-imagegen/next-pass/next-pass-supplies-transparent-contact-v3.png`, residue/corner/edge audit, runtime asset-size validation, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex detail screenshot at `tmp/qa/items-imagegen/next-pass/codex-anchor-threader-repair.png`.
- Thermos Lid steam cleanup: neutralized remaining magenta-key contamination in the Thermos Lid steam curl and edge specks, updating `assets/runtime/supplies/icons/thermos_lid.webp` plus source crops through `thermos_lid-imagegen-crop-v7.png`.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, final contact sheet `tmp/qa/items-imagegen/next-pass/next-pass-supplies-transparent-contact-v7.png`, and live Codex screenshot `tmp/qa/items-imagegen/next-pass/codex-thermos-lid-steam-repair.png`.

- Route map highlighted-dot pullback: reduced lit/primary/available dotted-path radii and softened the highlight halo so reachable paths remain visible without clustering into thick bead chains.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with two reachable nodes, and screenshot inspection at `.artifacts/route-map-highlight-dots-thinner-check.png`.

- Route map highlighted-dot extra-thin pass: took the active/preview/available dotted paths down one more notch and reduced the halo spread so highlighted routes feel path-like without clumping.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with four reachable nodes, and screenshot inspection at `.artifacts/route-map-highlight-dots-extra-thin-check.png`.

- Route board image-gen backing correction: replaced the procedural route-board texture with the attached generated map-paper image, preserving the original PNG source at `assets/concept-art/ui/route-map/route-board-laminated-plan-v1.png` and optimizing a cropped 1200x693 runtime WebP (~73 KB) at `assets/runtime/backdrops/route-board-laminated-plan-v1.webp`.
- Wired the texture through BootScene preload and RouteScene rendering as a clearer screen-blended layer inside the map border so nodes, dotted paths, and inspector highlights remain readable while the generated map art is visible.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, temporary built-preview route probe confirming `route-board-laminated-plan` loaded with 20 nodes, and screenshot inspection at `.artifacts/route-map-imagegen-plan-backing-built-preview-canvas.png`.

- Route map backing brightness/fit pass: settled on `route map backing` as the code-facing name for the generated paper layer, expanded it to the full inner route-board panel, and reduced the dark wash so it reads as the actual map surface behind the route icons.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview screenshot inspection at `.artifacts/route-map-backing-expanded-brightened-built-preview-canvas.png`.

- Route map backing full-color/full-bleed correction: removed the remaining backing inset, tint, alpha fade, and dark wash so the generated map image fills the whole route-board backing at full color; redrew only the thin frame accents above the image.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview screenshot inspection at `.artifacts/route-map-backing-full-color-full-bleed-built-preview-canvas.png`.

- Route node icon contrast pass: refreshed the eight route node runtime icons from the existing imagegen route atlas using the item-icon processing style: stronger dark silhouettes, boosted color/contrast, cleaned transparent edges, and small detached-alpha cleanup so Street/Rival/Boss/Basin/Nest/Market/Signal/Cache read on the new full-color map backing.
- Saved versioned source outputs under `assets/concept-art/ui/route-map/route-node-icons-contrast-v2/` plus `assets/concept-art/ui/route-map/route-node-icon-atlas-contrast-v2.png` and `assets/runtime/map-icons/map-node-icon-atlas-contrast-v2.png`; replaced the existing runtime WebP icon filenames under `assets/runtime/map-icons/icons/` so RouteScene picks them up without code changes.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-contrast-v2/route-node-icons-contrast-v2-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview route screenshot `.artifacts/route-map-icons-contrast-v2-built-preview-canvas.png`.

- Title screen floating-particle bump: increased the home-screen mote emitter frequency, lifespan, drift, alpha, scale, and live-particle cap so the floating particles read a little richer without obscuring the splash art or menu controls.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, direct Playwright MenuScene probe confirming the `menu-soft-mote` emitter at 34/36 live particles, and screenshot inspection at `.artifacts/test-results/title-particles-bumped-menu.png`.

- Title logo shimmer pass: added a reduced-motion-aware additive gold sheen over the transparent Bird Squad title images, triggered after the title impact and repeated on a long delay so the logo catches light without adding stray streaks over the splash art.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, direct Playwright MenuScene probe confirming both shimmer overlay images active at alpha `0.2`, and screenshot inspection at `.artifacts/test-results/title-logo-shimmer-final.png`.

- Title shine visibility follow-up: strengthened the title shimmer with brighter additive title sheen and three logo-anchored twinkles so the effect is plainly visible on first view.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5173`, and a direct Playwright title-screen probe confirming sheen/twinkle alpha with screenshot output at `.artifacts/test-results/title-logo-shine-visible.png`.

- Title polish follow-up: removed the visible logo twinkle flares, reduced the looping title sheen to a slow low-alpha warm lift, and softened the entrance gold burst into sparse tiny dust flecks so the title treatment feels less flashy.
- Verification: `npm run build`, fresh Vite dev server on `http://127.0.0.1:5634`, required `develop-web-game` Playwright client against that clean port, and screenshot inspection at `.artifacts/test-results/title-polished-final-client/shot-0.png`.

- Title feathers-only rollback: removed the delayed logo sheen/glint overlay entirely so the title screen returns to the simpler feather/particle-only treatment while keeping the title entrance intact.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-feathers-only-client/shot-0.png`.

- Title effects full rollback: restored the original home-screen particle emitter settings and original 42-fleck title impact burst, with the shimmer/glint overlay still removed.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-original-effects-client/shot-0.png`.

- Title particle preference follow-up: restored the boosted ambient home-screen particle emitter values while keeping the logo shimmer/glint removed and leaving the original title impact flecks in place.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-boosted-particles-client/shot-0.png`.

- Codex Items filter pass: split the Items browser into a type row (`All`, `Waymarks`, `Supplies`) plus contextual filters. Waymarks now filter by family (`Shelter`, `Tempo`, `Economy`, `Suit`, `Molt`, `Boss`); Supplies filter by timing and tactical role (`Combat`, `Route`, `Flexible`, `Defense`, `Recovery`, `Momentum`, `Pressure`, `Intel`). The Items grid gets extra header height only in that section, and legacy smoke/debug `activeItemTab` values still map to the expected old Supplies/Waymarks selections.
- Verification: `npm run build`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies are listed as items"`, required `develop-web-game` client on `http://127.0.0.1:5634`, and built-preview screenshots inspected at `.artifacts/test-results/codex-items-two-row-all.png`, `.artifacts/test-results/codex-items-waymark-filters.png`, and `.artifacts/test-results/codex-items-supply-filters.png`.

- Supplies/Waymark tuning pass: strengthened underperforming supplies with clearer tactical hooks (`Shade Cloth`, `Signal Kite`, `Cache Key`, `Oath Ledger`, `Map Sticker Strip`, `Bus Token Cache`, `Market IOU`, `Anchor Threader`, `Spare Pocket`, and clarified `Thermos Lid`) and corrected `Molt Shadow Tag` to use the Open Sky Guard effect its text promised.
- Added conditional combat-supply support for `if visitedNodeType(cache) then ...`, letting `Cache Key` reward players who opened a rooftop Cache earlier in the district.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173`.

- Waymark trigger tuning pass: moved several older flat-start Waymarks into more expressive triggers: `Chalk Wingmark` now rewards breaking enemy Cover, `Stage Pin` keys off Plumes, `Quill Notch` keys off Quills, `Feather Tape` keys off Basins, `Workshop Stamp` keys off Nests, and `Rooftop Relay Bell` keys off Supply use.
- Route-memory supply follow-up: added conditional route-effect support for `visitedNodeType(...)`, then gave `Market IOU` a Market-visit Cohesion refund and `Bus Token Cache` a Signal-visit Scrap kicker.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "route marks are real relics|codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Route node icon splash-style integration: cleaned the provided green-screen atlas `C:/Users/Will/Desktop/dfg.png` into transparent `Street`, `Rival Crew`, `Boss`, `Basin`, `Nest`, `Market`, `Signal`, and `Cache` source crops under `assets/concept-art/ui/route-map/route-node-icons-splash-v3/`, preserved the full source atlas as `assets/concept-art/ui/route-map/route-node-icon-atlas-splash-v3.png`, and replaced the existing runtime WebP filenames under `assets/runtime/map-icons/icons/` so RouteScene picks them up without code changes.
- Exported the runtime icons as 192px transparent quality-86 WebPs, all under the 32 KB route-node icon budget, and verified the real RouteScene map/legend rendering on preview port `5641`.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-splash-v3/route-node-icons-splash-v3-runtime-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and live route screenshot `.artifacts/route-icons-splash-v3-client-route/shot-3.png`.

- Route map icon scale-up: increased RouteScene node icon display sizes from 66/84 to 82/106 for normal/boss nodes, with matching node radii and focus padding so the splash-style map icons read larger and more detailed while preserving hit targets, visual bounds, reward pips, and inspector thumbnails.
- Verification: `npm run build`, `node tools/validate-runtime-asset-sizes.mjs`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and visual inspection of `.artifacts/route-icons-large-client/shot-3.png`.

- Route map path contrast pass: shifted dotted route edges from low-alpha pale tones to higher-contrast inked dots with a small dark underlay, using warmer amber for primary/reachable previews, cyan for available routes, green for chosen routes, and darker blue-gray for inactive/secondary paths so the dots read clearly on the full-color map backing without becoming thick.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and visual inspection of `.artifacts/route-path-contrast-client/shot-3.png`.

- Route node icon splash-style v5 follow-up: replaced the weak Street and oversized Signal concepts with a combat-forward street marker built from crossed hook/crowbar waymark art over the street plate and a compact windcatcher-spool signal marker with no radio-wave arcs; preserved the other six v4 icons and re-exported the runtime set as sharper 256px high-quality transparent WebPs under the existing `assets/runtime/map-icons/icons/*.webp` filenames.
- Increased route node icon display sizes to 90px normal / 114px boss with matching radii, and tightened Street/Signal export padding so the art fills the node better without edge clipping.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-splash-v5/route-node-icons-splash-v5-runtime-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and live route screenshot `.artifacts/route-icons-splash-v5-client-2/shot-4.png`.

- Rare/Boss Waymark identity pass: moved `Parade Mirror`, `Harbor Bead Strand`, `Fresh Pinfeather`, `Rooftop Nest Lining`, and `Crowbar Debt` away from flat startup stats into build-defining trigger payoffs, and increased `Sky-Safe Harness` boss prep from 10 to 14 Cover.
- Basin/Nest route-memory supply pass: `Seed Packet` now heals extra after a Basin visit, and `Nest Clamp` retains a card after a Nest visit.
- Verification: `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts --grep "route marks are real relics|new balance verbs|codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Non-battle set-piece kickoff: added a cast art brief that keeps each personality on a distinct bird species and fashion silhouette from Veyra, then converted Basin visits into the first `Lantern Roost` landmark screen with Sella Warmwick, a generated shelter backdrop, the roostkeeper resident art, compact title/status plates, and opaque choice tickets.
- Route-event art loading now supports generated route-event runtime WebPs alongside the existing market and battlefield variant backdrops, and the smoke coverage verifies the new Lantern Roost backdrop loads and renders.
- Verification: `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route event overlays|signals/basins/cache|route choice and market"`, required `develop-web-game` client on `http://127.0.0.1:5637/`, direct Lantern Roost Playwright probe, and screenshot inspection of `.artifacts/lantern-roost-set-piece.png`.

- Non-battle set-piece expansion: generalized the Lantern Roost overlay into a reusable personality-led route-event set piece for Basin, Cache, Signal, and Nest, with top title/species plaque, compact resource tag, resident art, landmark note, and opaque decision tickets that keep the backdrop visible.
- Added resident art mapping for Marn Valeclip (crow), Ivo Tallymast (kestrel), and Oren Shearbright (woodpecker), using existing approved runtime art as the first playable pass while the updated brief tracks future custom cutout targets.
- Verification: `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route event overlays|signals/basins/cache|route choice and market"`, required `develop-web-game` client on built preview `http://127.0.0.1:5638/`, direct built-preview Playwright probes for Basin/Cache/Signal/Nest with no offscreen text, and screenshot contact-sheet inspection at `.artifacts/nonbattle-setpieces-built/contact-sheet.png`.

- Non-battle set-piece decomposition correction: replaced the borrowed resident/backdrop wiring with route-event-specific assets. Each planned personality now has a flat green chroma-source PNG, an alpha-source PNG, and a transparent runtime WebP under the route-event asset pipeline; each set piece now has its own 16:9 scene plate under `assets/runtime/route-events/`.
- Prepared decomposed assets for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and future Rival Wager. Basin/Cache/Signal/Nest now render from the route-event assets directly; Rival assets are staged for a later route-choice conversion.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused route-event and route-choice Playwright tests run individually after the combined dev-server run hit a transient port handoff, required `develop-web-game` client on built preview `http://127.0.0.1:5638/`, built-preview probes confirming all four active set pieces render their expected scene/resident keys with no offscreen text, and screenshot inspection of `.artifacts/nonbattle-setpieces-decomposed-built/contact-sheet.png` plus chroma/scene asset contact sheets under `.artifacts/route-event-decomposed-assets/`.

- Boss reward identity follow-up: added the `districtStartKit(1)` Waymark effect and upgraded `Reopened Roofline` so the next district starts with both a free Preen and a packed random Supply when there is room, making the boss trophy feel more run-shaping than a single prep coupon.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|route marks are real relics|codex: supplies are listed|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Item communication polish pass: added derived build/synergy tags for Supplies and Waymarks, then surfaced them on Codex item thumbnails, Codex item detail panels, boss Waymark reward cards, and the Waymark reward debug state. Tags call out hooks like Route Memory, Supply Combo, Next District, Preen, Anti-Cover, Recovery, Cover, Economy, and Boss so players can understand why an item matters before they pick it.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|codex: supplies are listed"` plus the four-test item mechanics group, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, Codex/detail screenshot inspection at `.artifacts/test-results/item-tags/codex-supply-tags.png`, boss Waymark reward screenshot inspection at `.artifacts/test-results/item-tags/boss-waymark-tags.png`, and final menu screenshot inspection of `output/web-game/shot-0.png`.

- Waymark trigger feedback pass: added combat feedback for Waymarks when they fire, including a short animated toast near the Waymark rail, a persistent `LAST TRIGGER` strip under the Waymark shelf, and `waymarkFeedback` in the render-state payload for smoke coverage. The feedback uses the current Waymark art and effect summary so triggers like `Crowbar Debt` read as concrete payoffs instead of hidden stat changes.
- Fixed combat Waymark art loading for marks added after scene start by queueing current carried Waymark art from the shelf renderer, making the drawer and feedback deterministic even when tests or runtime rewards mutate the carried list mid-scene.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|route marks are real relics|waymark artifact drawer|codex: supplies are listed"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, trigger-feedback screenshot inspection at `.artifacts/test-results/waymark-feedback/crowbar-feedback.png`, and final menu screenshot inspection of `output/web-game/shot-0.png`.

- Lantern Roost first-scene completion: rebuilt the Basin visit as the first finished non-battle set piece, using a v2 landmark scene plate, Sella Warmwick resident art, and separate heated hearth/sign prop art so the encounter reads as a visited shelter instead of a menu overlay.
- Wired the Lantern Roost kit through RouteScene with a dedicated Basin renderer, centered title plaque, compact scrap/cohesion tag, large resident placement, opaque choice tickets, and screenshot/test coverage that confirms the scene, resident, hearth, and sign textures all render together with no offscreen text.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on `http://127.0.0.1:5639/`, and direct first-scene probe screenshot `.artifacts/lantern-roost-first-scene-complete.png`.

- Lantern Roost prop-kit completion: generated the remaining choice-specific pieces for the first scene: `lantern-roost-rain-pipe-v1`, `lantern-roost-awning-v1`, and `lantern-roost-wrapped-snack-v1`, each with alpha-source PNG, chroma-source PNG, and optimized transparent runtime WebP files.
- Wired the new props into the dedicated Basin renderer beside their matching choice tickets without adding new text boxes or borders, then expanded the route-event smoke assertion so it verifies the full Lantern Roost kit: backdrop, resident, hearth, sign, rain pipe, awning, and wrapped snack.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on `http://127.0.0.1:5644/`, prop contact sheet `.artifacts/lantern-roost-prop-kit-v1-contact.png`, and in-scene screenshot `.artifacts/lantern-roost-full-prop-kit-screen.png`.

- Route-event centerpiece decomposition pass: made the remaining planned centerpieces into first-class assets instead of metadata-only labels. Added high-resolution pixel-art-style alpha/chroma/runtime assets for Rooftop Cache's sealed pulley cache cabinet, Signal Landmark's illuminated route switchboard, Featherwright Studio's grooming chair and feather press, and Rival Wager's contract case/prize board.
- Wired Cache, Signal, and Nest set-piece overlays to queue and render their centerpiece assets independently over the scene plate, and extended the route-event smoke test so it verifies each active centerpiece texture is loaded and rendered. Rival's centerpiece is staged in the same asset pipeline, but remains unused until Rival gets a route-choice flow.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, built-preview route-event probes on `http://127.0.0.1:5646/`, required `develop-web-game` client, source contact sheet `.artifacts/route-event-centerpiece-kit-v1-contact.png`, and built-preview screenshot sheet `.artifacts/route-centerpieces-built-contact.jpg`.

- Supply use feedback pass: added `supplyFeedback` state for route and combat supplies, visible `LAST SUPPLY` strips, combat supply toasts, and render-state payload coverage so players can see what a consumed supply just did instead of inferring from stat deltas. Route supply feedback now renders above the route inspector without hiding four-slot supply shelves; combat feedback stacks with Waymark trigger toasts.
- Improved effect formatting for supply-packing verbs (`gainSupply`, `gainSupplyChoice`) so item summaries read as player-facing actions instead of raw runtime tokens.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "next item pass supplies and Waymarks execute route and combat hooks|supplies are carried into combat and usable for an effect"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `.artifacts/test-results/supply-feedback/route-supply-feedback.png`, `.artifacts/test-results/supply-feedback/combat-supply-feedback.png`, and `output/web-game/shot-0.png`.

- Route node icon simplification pass: processed the simplified green-screen atlas `C:/Users/Will/Desktop/32131.png` into transparent source crops under `assets/concept-art/ui/route-map/route-node-icons-splash-v7/`, preserved the atlas as `assets/concept-art/ui/route-map/route-node-icon-atlas-splash-v7.png`, and replaced the existing runtime `assets/runtime/map-icons/icons/{street,rival,boss,basin,nest,market,signal,cache}.webp` files so RouteScene picks up the clearer map emblems without code changes.
- Exported all eight runtime route icons as 384px transparent WebPs under the 32 KB route-icon budget; source QA contact sheet: `tmp/qa/route-map-icons-splash-v7/route-node-icons-splash-v7-runtime-contact.png`.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5648/`, RouteScene state payload confirming `routeSelection`, and screenshot inspection at `.artifacts/route-icons-splash-v7-client/shot-5.png`.

- Route path dot color pass: shifted dotted route edges to a more dockside palette with lantern amber selected previews, sea-glass teal available routes, muted rope green chosen routes, and harbor-slate inactive/secondary trails, plus a stronger ink under-dot for contrast against the map backing.
- Verification: `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5649/`, RouteScene state payload confirming `routeSelection`, and screenshot inspection at `.artifacts/route-dock-dot-colors-client/shot-5.png`.

- Route path highlight darkening pass: reduced selected, preview, available, and secondary route-dot brightness to deeper rust, teal, green, and slate values so highlights contrast the full-color map backing without clustering into bright lanes.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on built preview `http://127.0.0.1:5652/`, and screenshot inspection at `.artifacts/route-darker-highlight-client/shot-5.png`.

- Route path light-fill follow-up: brightened only the interior route-dot fills one step while preserving the darker ink backing circles, giving the highlighted paths clearer readability without returning to the earlier overly bright clustered look.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on built preview `http://127.0.0.1:5653/`, and screenshot inspection at `.artifacts/route-light-fill-highlight-client-route/shot-11.png`.

- Route-event imagegen source-reference pass: generated imagegen-directed scene/personality briefs for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager, then persisted a project-local source-reference package with scene plates, chroma resident sources, alpha resident review files, and optimized runtime WebPs. The built-in imagegen outputs were not exposed as filesystem files in this environment, so the committed assets are the art-bible-aligned source-reference pass derived from the same specs rather than a final animation-ready package.
- Wired the new v2/v3 route-event scene and resident assets into RouteScene while keeping stable texture keys, with Rival Wager also staged for route-choice conversion. Contact sheets: `.artifacts/route-event-scenes-imagegen-pass-contact.jpg`, `.artifacts/route-event-residents-imagegen-pass-contact.jpg`, and built-preview render sheet `.artifacts/route-event-render-contact.jpg`.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5647/`, direct built-preview probes for Basin/Cache/Signal/Nest/Rival, and visual inspection of the source and render contact sheets.

- Rival Wager route-choice integration: wired Caldra Pinion's generated scene/personality assets into actual play instead of leaving them staged. Rival nodes now open a pre-battle set piece with `Take Caldra's wager` and `Do not get involved` choices; taking the wager starts the existing rival BattleScene reward path, while declining marks the node complete without combat.
- Flattened the generated scene plates to opaque runtime WebPs so non-market set pieces no longer show the route map beneath their landmark art; resident and centerpiece assets remain transparent overlays.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5650/`, and Rival set-piece screenshot inspection at `.artifacts/rival-wager-wired-preview-final.png`.

- Scrap icon integration pass: added a reusable transparent high-resolution pixel-art Scrap source PNG and runtime WebP, then wired the Scrap texture into RouteScene and BattleScene resource UI. The icon now appears in the route Scrap rail, route reward badges, market Scrap badge, market price/service tags, route-event resource tags, and combat Scrap chip while keeping text labels for readability.
- Verification: `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and direct Playwright screenshots inspected at `.artifacts/test-results/scrap-icon-ui/route-scrap-icon.png`, `.artifacts/test-results/scrap-icon-ui/market-scrap-icon.png`, and `.artifacts/test-results/scrap-icon-ui/combat-scrap-icon.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon scale follow-up: increased the Scrap icon display sizes across route rail, reward badges, market tags, route-event resource tags, and combat HUD, with small text offsets to keep labels and numbers clear.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and screenshot inspection at `.artifacts/test-results/scrap-icon-larger-ui/route-scrap-icon-larger.png`, `.artifacts/test-results/scrap-icon-larger-ui/market-scrap-icon-larger.png`, and `.artifacts/test-results/scrap-icon-larger-ui/combat-scrap-icon-larger.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon simplification pass: generated a clearer imagegen direction for Scrap around one dominant brass washer/token silhouette with only a few supporting metal and copper-wire details, then kept the already-wired runtime source focused on that simpler high-contrast read instead of the earlier busy scrap pile. The built-in imagegen result rendered in-thread but was not exposed as a usable filesystem export in this environment.
- Verification: `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and screenshot inspection at `.artifacts/test-results/scrap-icon-simple-ui/route-scrap-icon-simple.png`, `.artifacts/test-results/scrap-icon-simple-ui/market-scrap-icon-simple.png`, and `.artifacts/test-results/scrap-icon-simple-ui/combat-scrap-icon-simple.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon high-detail reference pass: used the supplied washer/shard/copper-wire Scrap icon as the reference silhouette, generated a higher-detail imagegen edit direction, and replaced the project Scrap art with a transparent 4096px source plus a 1024px runtime WebP that keeps the same composition while adding richer brass bevels, pitting, shard facets, cyan rim light, magenta chips, and copper-wire highlights. Preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference.png`.
- Verification: visual inspection of `assets/concept-art/supplies/sources/scrap-icon-source-v4.png` and `assets/runtime/supplies/icons/scrap.webp`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with screenshot `.artifacts/test-results/scrap-icon-detailed-client/shot-0.png`.

- Scrap icon green-screen cleanup pass: used `C:/Users/Will/Desktop/scrap.png` as the final Scrap source, removed the flat green background with the imagegen chroma-key helper, centered the transparent cutout on a larger square canvas, and exported a 4096px source plus a larger 1536px runtime WebP under the existing Scrap texture path. Preserved the supplied green-screen file at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v2.png`.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-cleanup-client/shot-1.png`.

- Actual route-event imagegen asset replacement: replaced the earlier playable source-reference route-event scene/resident pass with the supplied Desktop image-generated PNG exports for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager. The full scene sources are preserved under `assets/concept-art/route-events/**`, resident chroma sources and transparent alpha review files are preserved beside them, and the runtime `assets/runtime/route-events/*-v2/v3.webp` files now come from those supplied images.
- Contact sheets for the actual supplied imagegen assets: `.artifacts/route-event-scenes-actual-imagegen-contact.jpg` and `.artifacts/route-event-residents-actual-imagegen-contact.jpg`.

- Route-event composition simplification: removed the separate in-scene set-piece item overlays from Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager so each visit is now composed from the full background scene plate plus the personality cutout only. Resident art now fits inside its placement bounds while preserving the generated image's natural aspect ratio instead of stretching every bird into the same rectangle.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5655/`, and visual inspection of `.artifacts/route-events-bg-personality-only/route-events-bg-personality-only-contact.jpg`.

- Marn Valeclip art replacement: updated Rooftop Cache's resident art from the supplied `C:\Users\Will\Desktop\2-2.png`, preserving the chroma source, regenerating the transparent alpha review PNG, and replacing `assets/runtime/route-events/marn-valeclip-v2.webp`. The cleanup pass tightened the green-screen matte and despill while preserving the cyan rim-light and natural tall character ratio.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5656/`, alpha preview `.artifacts/marn-valeclip-v2-cleanup-preview.png`, and in-game Rooftop Cache screenshot `.artifacts/marn-valeclip-v2-update/rooftop-cache-marn-v2-update.png`.

- Oren Shearbright art replacement and layout polish: replaced Featherwright Studio's resident source with the supplied cleaned `C:\Users\Will\Desktop\4-2.png`, regenerated the chroma source, transparent alpha review PNG, and `assets/runtime/route-events/oren-shearbright-v2.webp`, then gave Nest events a larger resident frame while preserving Oren's natural tall ratio. Sella Warmwick's Lantern Roost resident placement also shifted right to sit better in the shelter composition.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5657/`, alpha preview `.artifacts/oren-shearbright-v3-cleanup-preview.png`, and in-game screenshots `.artifacts/route-event-qa/oren-shearbright-cleaned-larger.png` plus `.artifacts/route-event-qa/sella-warmwick-shifted-right.png`.

- Sella Warmwick mirror pass: horizontally mirrored the Lantern Roost v2 resident source set (`sella-warmwick-v2-chroma-source.png`, `sella-warmwick-v2-alpha-source.png`) and regenerated `assets/runtime/route-events/sella-warmwick-v2.webp` so Sella faces right in the Basin set piece without changing her placement or natural aspect ratio.
- Verification: visual matte preview `.artifacts/sella-warmwick-v2-mirrored-preview.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5658/`, and in-game screenshot `.artifacts/route-event-qa/sella-warmwick-mirrored.png`.

- Oren Shearbright mirror pass: horizontally mirrored the Featherwright Studio v2 resident source set (`oren-shearbright-v2-chroma-source.png`, `oren-shearbright-v2-alpha-source.png`) and regenerated `assets/runtime/route-events/oren-shearbright-v2.webp` so Oren faces right into the Nest set piece while keeping the larger natural-ratio placement.
- Verification: visual matte preview `.artifacts/oren-shearbright-v2-mirrored-preview.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5659/`, and in-game screenshot `.artifacts/route-event-qa/oren-shearbright-mirrored.png`.

- New user prompt: review the game's balance because it feels too easy and card spam makes decisions less meaningful.
- First combat-balance pass: lowered base hand target from 5 to 4, added an overextension pressure valve where playing 4+ cards before Roost puts the flock into Open Sky for the enemy phase, and reduced automatic post-combat recovery from +3 to +1 Cohesion. Intent: preserve card identities while making the fourth card and route sustain choices matter more.
- Added smoke coverage for the overextension breakpoint and updated alpha/core design docs to document the new hand target, anti-spam rule, and lighter passive recovery.

- Scrap icon gear readability pass: replaced the busier washer-pile runtime with the supplied `C:/Users/Will/Desktop/scrap-2.png` gear silhouette, removed the green screen with the imagegen chroma-key helper, exported a transparent 4096px source and 768px runtime WebP, and preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v3.png`. The gear ring reads more clearly in the 30-34px Scrap HUD slots while retaining the metal plate and copper-wire scrap cues.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, simulated 32px preview `.artifacts/test-results/scrap-icon-gear-readable/scrap-gear-32x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-gear-client/shot-1.png`.

- Scrap icon solid-token replacement: replaced the gear/scrap-pile concept with a single chunky brass salvage token so Scrap reads as a more solid currency mark at HUD scale. Exported a transparent 4096px source at `assets/concept-art/supplies/sources/scrap-icon-source-v8-solid-token.png`, refreshed the stable source path, and replaced the 768px runtime WebP under the existing `resource-scrap` texture path so all already-wired UI locations pick it up without code changes.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, small-size preview `.artifacts/test-results/scrap-solid-token/scrap-solid-token-34x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-solid-token-client/shot-1.png`.

- Scrap icon nut-token reference replacement: used the supplied green-screen nut/gear Scrap icon from `C:/Users/Will/AppData/Local/Temp/codex-clipboard-cafd78d5-a8a8-4a7f-ad9b-e1528493668a.png`, removed the green background with the imagegen chroma-key helper, and replaced the runtime Scrap texture with the cleaned transparent version. Preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v4.png` and the transparent high-resolution source at `assets/concept-art/supplies/sources/scrap-icon-source-v9-nut-token.png`.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, small-size preview `.artifacts/test-results/scrap-nut-reference/scrap-nut-34x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-nut-reference-client/shot-1.png`.

- Scrap icon label-free UI pass: removed redundant all-caps `SCRAP` captions from compact Scrap icon/count treatments in the route rail, market Scrap badge, route-event resource tags, market service price labels, and combat resource chip while preserving full Scrap wording in explanatory sentences and tooltips.
- Verification: `npm run build`, `rg -n SCRAP src/main.ts` confirms only constants/internal references remain, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-label-free-client/shot-1.png`.

- HUD standardization first pass: audited the current run HUD surfaces and used the combat resource chip as the baseline pattern. Added a shared metric-chip renderer, moved the combat top resource chips onto it, split the route map rail into matching Cohesion/Scrap/Deck/Waymarks/Supplies chips, and replaced bespoke Scrap/Cohesion tags in route choices, market, Lantern Roost, and route set-piece overlays.
- Verification: `npm run build`, built preview on `http://127.0.0.1:5173/`, required `develop-web-game` route screenshot `.artifacts/test-results/hud-consolidation-route-preview-fixed/shot-1.png`, and combat screenshot `.artifacts/test-results/hud-consolidation-combat-preview/shot-0.png`.

- HUD combat-source standardization pass: pivoted the shared run HUD to use the combat top bar as the canonical layout. Route, market, and route-event surfaces now preserve the combat-shaped Cohesion/status/Flow/Cover/suit/resource frame through the run, while combat gained Deck and Supplies in the same resource lane so the map-only information is no longer missing in fights.
- Verification: `npm run build`, required steady-state Playwright captures on `http://127.0.0.1:5173/`, route screenshot `.artifacts/test-results/combat-source-hud-steady-final/route.png`, and combat screenshot `.artifacts/test-results/combat-source-hud-steady-final/combat.png`.

- Route map declutter pass: removed permanent instructional copy from the map footer, removed the route log line from the route map, suppressed route-only HUD title/subtitle/status labels, and removed the inspector's `Route Board` eyebrow so the map relies more on icons, selection, and node hover tooltips.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, final route screenshot `.artifacts/test-results/route-map-declutter-client-final/shot-0.png`.

- Route map icon-first follow-up: shortened the shared flock HP bar from 300px to 240px, changed its visible text from `Cohesion current/max` to just `current/max`, removed the route map legend render, and deleted the now-unused legend renderer so the map relies on node icons, risk pips, selection, and hover tooltips.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-bar-map-no-legend-route/shot-0.png`, required combat capture `.artifacts/test-results/hud-bar-map-no-legend-combat/shot-0.png`, plus steady combat screenshot `.artifacts/test-results/hud-bar-map-no-legend-combat-steady/combat.png`.

- Route map HUD-control consolidation: removed the standalone Back/Flock/Deck route buttons, made the shared HUD HP bar open the Flock overlay, and made the Deck HUD chip open the deck overlay so map controls live in the unified run HUD instead of a second button row.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/map-hud-controls-route/shot-0.png`, deck-chip interaction capture `.artifacts/test-results/map-hud-controls-deck/shot-0.png` with `deckOverlayOpen: true`, and HP-bar interaction capture `.artifacts/test-results/map-hud-controls-flock/shot-0.png` with `flockOverlayOpen: true`.

- HUD declutter follow-up: removed the permanent Plumes/Quills/Basins/Nests suit-count row from the shared run HUD and stopped feeding route-progress numbers into the top-right HUD slot, leaving the top bar focused on Cohesion, Cover, Resonance, Scrap, Deck, Waymarks, and Supplies.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-no-suits-route/shot-0.png`, and required combat capture `.artifacts/test-results/hud-no-suits-combat/shot-0.png`.

- HUD icon/pip cleanup: shifted the flock Cohesion bar left, removed the decorative Flow/status pips from the shared HUD, removed the generic colored dot pips from non-Scrap HUD chips, and replaced the bordered `Cover` text chip with an icon-only shield marker that keeps the numeric cover value.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-cover-icon-route/shot-0.png`, and required combat capture `.artifacts/test-results/hud-cover-icon-combat/shot-0.png`.

- Route map inspector removal: removed the right-hand selected-node info panel, widened the route map and graph into that space, and kept `Take this route` as a compact floating control on the map.
- Verification: `npm run build`, restarted Vite on `http://127.0.0.1:5173/`, required `develop-web-game` route capture `.artifacts/test-results/map-expanded-no-inspector-fresh/shot-0.png`, and required route-confirm flow `.artifacts/test-results/map-expanded-no-inspector-fresh-combat-flow/state-0.json` reaching `mode: battle`.

- Route map vertical stretch: expanded the map frame upward/downward, stretched the route graph vertically, and moved the `Take this route` button down into the lower-right of the larger map frame.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-taller-button-lower/shot-0.png`, and required route-confirm flow `.artifacts/test-results/route-map-taller-button-lower-flow/state-0.json` reaching `mode: battle`.

- Route map extra-column polish: increased procedural route generation from 6-7 middle columns to 7-8 middle columns for every district and added deterministic, clamped middle-node position drift so route icons feel hand-placed instead of grid-aligned while keeping entry/boss anchors stable.
- Verification: `npm run validate:runtime`, `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-extra-column-offset-final/shot-0.png` showing 10 generated columns for the alpha seed, and required route-confirm flow `.artifacts/test-results/route-map-extra-column-offset-flow/state-0.json` reaching `mode: battle`.

- New user prompt: review sluggishness/load slowness and continue investigating optimization issues beyond the runtime-optimizer checklist.
- Runtime/load pass: stopped `codex-data` from importing at app module evaluation time; Codex extended data now loads only when `CodexScene` needs it.
- Card-art load pass: added thumbnail runtime asset records and switched routine card surfaces (Codex grid, route/market cards, battle hand cards, reward choices, and battle preload queues) to thumbnail WebPs while preserving full portraits for large detail/preview panels.
- Runtime sluggishness follow-up: added recursive tween cleanup before Codex, Route, and Battle redraws destroy their UI trees, preventing repeating idle/breathing/atmosphere tweens from accumulating across `renderAll()` rebuilds.
- Bundle validation update: app entry remains above the old 590 KB target, so `validate-bundle-size` now warns above 590 KB but hard-fails above a temporary 675 KB ceiling. `docs/project/performance-notes.md` records the measured chunks and the remaining need to split `src/main.ts` into lazy scene/system modules.
- Verification: `npm run build`, `npm run validate:bundle-size`, `npm test`, `npm run validate:docs`, required `develop-web-game` client on built preview `http://127.0.0.1:4175/`, and direct route-to-combat Playwright smoke on `http://127.0.0.1:4176/` all completed. Screenshots inspected: `output/web-game/shot-1.png` and `.artifacts/test-results/perf-after-confirm-smoke.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared; no failed requests in the direct smoke.

- Route map icon-pip removal: removed the small colored risk pips attached to combat route icons, leaving risk available through hover tooltips and keeping the map icon field cleaner.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-no-icon-pips/shot-0.png`, and required route-confirm flow `.artifacts/test-results/route-map-no-icon-pips-flow/state-0.json` reaching `mode: battle`.

- Controlled route pacing pass: replaced purely shuffled procedural node assignment with authored route-generation beats plus a deterministic path repair pass. Each generated district now has an opening choice, early recovery/build access, pressure beats with non-combat alternatives, boss-prep safety, max-consecutive-combat caps, early safety requirements, build/economy requirements, rival-alternative repair, and district-specific pressure-score caps.
- Added smoke coverage for generated route pacing across 16 seeds per district, checking all entry-to-boss paths for no 3-combat chains, early safety, build/economy access, pressure cap compliance, and no forced Rival-only exits.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts -g "controlled pacing"`, and required `develop-web-game` client on `http://127.0.0.1:5173/`. Visual screenshot inspected at `output/web-game/shot-0.png`; state JSON confirmed a controlled route sequence.

- Combat flock-label cleanup: removed the permanent Flock Leader name from the combat leader art label and removed the leader/map combined title from the battle intro, leaving district context only.
- Verification: `npm run build` passed; direct Playwright combat text scan on `http://127.0.0.1:5173/` confirmed no visible leader panel or intro leader-name text remained. Screenshot inspected at `.artifacts/test-results/combat-leader-name-removed.png`.

- New user prompt: every card click in combat feels like it freezes the game.
- Combat click responsiveness fix: profiled BattleScene card clicks and found target-card selection was doing a full `renderAll()` rebuild, with one select taking roughly 170 ms. Target-card selection now updates the existing hand-card border in place instead of rebuilding the whole combat board. Card pointer handlers now stop propagation, and recursive tween cleanup now only traverses real Phaser Containers.
- Verification: direct Playwright combat click smoke on `http://127.0.0.1:4177/` confirmed target selection no longer calls `renderAll()` and dropped to ~0.1 ms; a real support-card play still updates hand/piles/log correctly. `npm run build`, `npm run validate`, and the required `develop-web-game` client completed. Screenshots inspected: `.artifacts/test-results/card-click-after-selection-optimized.png` and `output/web-game/shot-0.png`.

- UI icon crop correction: regenerated the current 21 runtime UI icon PNGs from alpha-component bounds instead of fixed sheet cells, grouping detached sparkle/fragment components with `preen-kit` and `release-card` so icons no longer clip or include neighboring artwork.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, direct Playwright route-to-battle smoke, and transparent-margin validation across `assets/runtime/ui/icons/*.png` all passed. Screenshots inspected: `output/icon-segmentation/corrected-icons-contact.png`, `output/icon-segmentation/manual-playwright/01-after-start-click.png`, and `output/icon-segmentation/deeper-playwright/02-after-take-route.png`.

- UI icon size bump: increased the global in-game UI icon preview scale from `2` to `2.2` and moved the unified HUD context label lower inside the top rail so the enlarged Supplies/Waymarks/HUD icons do not visually collide with route text.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route-to-battle smoke completed without page errors. Screenshots inspected: `output/icon-size-bump/final-playwright/01-route.png` and `output/icon-size-bump/final-playwright/02-battle.png`.

- Flock Stats scrap removal: removed Scrap from the route Flock Stats overlay summary strip so the surface only presents Flock-related stats/context (`Cohesion` and `Waymarks` above the passive stat table). Scrap remains in the run HUD and economy surfaces.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route overlay capture completed without page errors. Screenshot inspected: `output/flock-stats-no-scrap/final-route-overlay/route-flock-stats.png`.

- Completed route node pip removal: removed the small top-right completion pip from route map nodes that are already complete. Completed nodes still read as complete through the existing dimmed icon state.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright completed-node route capture completed without page errors. Screenshot inspected: `output/completed-map-no-pip/final-playwright/completed-route-map.png`.

- Route Flock Stats resource cleanup: removed the remaining Cohesion and Waymarks route-resource readouts from the route Flock Stats overlay, and filtered the route overlay's Cohesion row so the page now focuses on passive flock/build stats only.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route Flock Stats capture completed without page errors. Screenshot inspected: `output/flock-stats-no-resource-summary/final-playwright/route-flock-stats-final.png`.

- Boss size bump: increased route-map boss node/icon sizing and made combat bosses render larger in both solo and helper-supported boss fights while preserving separate helper placement.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route boss/combat boss captures completed without page errors. Screenshots inspected: `output/boss-size-bump/final-playwright/route-boss-node.png` and `output/boss-size-bump/final-playwright/boss-combat.png`.

- Codex card thumbnail load fix: corrected the Codex card grid renderer to use the same compact/thumbnail art key that the Codex grid queues for visible discovered cards, so card images appear without needing to open the detail modal first.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and clean Playwright Codex capture with seeded discovered cards completed without card-detail clicks. Screenshot inspected: `output/codex-card-thumb-load/final-playwright/codex-cards-clean-before-detail-click.png`.

- HUD chip width trim: shortened the shared top-HUD Resonance and Waymarks metric chip frames so their borders better match their compact icon/value contents without disturbing Scrap, Deck, or Supplies spacing.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route/combat HUD captures completed without page errors. Screenshots inspected: `output/hud-chip-width-trim/final-playwright/route-hud.png` and `output/hud-chip-width-trim/final-playwright/combat-hud.png`.

- Flock HP bar compact pass: shortened the shared top-HUD Flock/Cohesion bar from 300px to 190px while preserving the left-edge heart anchor, returning top-rail space before the cover shield and resource chips.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route/combat HUD captures completed without page errors. Screenshots inspected: `output/flock-hp-bar-shorter/final-playwright/route-hud.png` and `output/flock-hp-bar-shorter/final-playwright/combat-hud.png`.

- Combat HUD level-name removal: removed the far-right level/encounter context label from the unified top HUD so combat no longer repeats the map/encounter name in the HUD rail.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat HUD capture completed without page errors. Screenshot inspected: `output/combat-hud-no-level-name/final-playwright/combat-hud.png`.

- Combat pile/Waymarks declutter: removed visible Draw/Discard pile labels from combat pile stacks, stopped rendering the left-side Waymarks shelf/icons during combat, and made the top-HUD Waymarks chip open the Found Waymarks drawer.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat captures completed without page errors. Screenshots inspected: `output/combat-waymarks-hud-menu/final-playwright-cachebust/combat-cleaned-hud.png` and `output/combat-waymarks-hud-menu/final-playwright-cachebust/combat-waymarks-menu.png`.

- Combat left-side Waymark text cleanup: stopped rendering the Waymark trigger feedback strip in combat so no Waymark-related text appears on the left side; Waymark state remains available through the top-HUD Waymarks menu.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat capture with seeded Waymark feedback completed without page errors. Screenshot inspected: `output/combat-no-left-waymark-text/final-playwright/combat-no-left-waymark-text.png`.

- Combat pile rectangle removal: removed the visible rectangular card-stack frames and diamond backplate from the draw/deck and discard pile controls, leaving icon art plus count badges with an invisible click target for pile inspection.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat captures completed without page errors; clicking the icon-only draw pile still opened Deck Review. Screenshots inspected: `output/combat-pile-no-rectangles/final-playwright/combat-piles-icon-only.png` and `output/combat-pile-no-rectangles/final-playwright/combat-draw-pile-overlay.png`.

- Dynamic card-effect role pass: extended the active card contract so the current role is inferred from the effects actually being cast, including Molt effects, instead of always presenting the card's base role. Combat hand/detail text state now reports both `role` and `activeRole`, and the combat deck/detail panel uses the active role next to the active target.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright Molt inspection confirmed `Open Basin` changes from base `skill/self/heal` to active `attack/enemy/damage` while in Molt. Screenshot inspected: `output/dynamic-molt-card-contract/final-playwright/molt-active-card-detail.png`.

- Boss Waymark reward pool pass: Waymark rewards now always offer 3 choices when awarded. Boss combat rewards draw from a high-tier pool that mixes boss-source Waymarks with rare/boss Waymarks, anchors at least one boss-source option when available, and falls back to the broader unowned pool only if ownership would shrink the offer. The reward payload now includes Waymark rarity, and post-combat rewards clear transient combat FX so old intro slashes do not overlay reward cards.
- Verification: `npm run build`, `npx playwright test tests/smoke.spec.ts -g "boss Waymark reward choices mix"`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright boss reward capture completed without page errors. Screenshot inspected: `output/boss-waymark-pool/final-playwright-clean/boss-waymark-reward-mixed-pool-clean.png`.

- Card reward rarity rate increase: raised the shared card rarity weights from common/uncommon/rare/legendary `100/45/16/5` to `80/75/32/10`, affecting post-combat card rewards and the market's weighted card slot. In a 100k simulated 3-card offer sample, non-common appearance rose from ~78.7% to ~93.4%, rare-or-legendary appearance rose from ~24.1% to ~38.5%, and offered-card share moved from about `60/31/5/3` to `41/44/9/6`.
- Verification: `npm run validate`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:4178/`, and screenshot inspection of `output/web-game/shot-0.png`.

- Multi-enemy encounter rate/strength increase: procedural combat payload selection now weights 2-enemy encounters at `8x` and 3+ enemy encounters at `12x` while preserving the existing no-repeat-until-exhausted variety behavior. Non-boss multi-enemy encounters now receive `+12%` max HP per enemy and `+1` damage bonus, matching the existing solo-tuning path without escalating boss-helper fights.
- Verification: `npx playwright test tests/smoke.spec.ts -g "encounter composition supports solo tuning and support companions|generated combat payloads favor multi-enemy encounters"`, `npm run validate`, and `npm run build` passed. Required `develop-web-game` client ran on `http://127.0.0.1:5187/`; route screenshot inspected at `.artifacts/test-results/multi-enemy-client/shot-0.png`. A follow-up combat-client coordinate burst did not activate the route confirm button in the generic client, but the focused Playwright smoke entered combat and verified multi-enemy HP/damage state.

- Combat enemy size bump: increased combat enemy stage scales across solo, multi-enemy, solo boss, and boss-with-helper layouts so enemy art reads larger in battle while preserving the shared HP/intent/FX placement model.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5188/`, and direct Playwright battle captures passed without browser errors. Screenshots inspected: `.artifacts/test-results/enemy-size-direct/single-enemy.png` and `.artifacts/test-results/enemy-size-direct/multi-enemy.png`.

- Sella Warmwick/Lantern Roost plaque cleanup: removed the lower-left "HEATED BRASS ROOST HEARTH" info plaque from `renderLanternRoostOverlay` so the event art is less cluttered while keeping the resident title and choices.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Sella route-event text scan/screenshot at `.artifacts/test-results/sella-warmwick-no-hearth-plaque.png`. Existing route-event smoke has a separate fixed-seed rival assumption issue after route pacing changes.

- Marn Valeclip/Rooftop Cache plaque cleanup: skipped the lower-left set-piece descriptor plaque for cache events so "SEALED PULLEY CACHE CABINET" no longer appears while Marn's title and reward choices remain.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Marn route-event text scan/screenshot at `.artifacts/test-results/marn-valeclip-no-cache-cabinet-plaque.png`.

- Ivo Tallymast/Signal plaque cleanup: skipped the lower-left set-piece descriptor plaque for signal events so "ILLUMINATED SWITCHBOARD ROUTE MAP" no longer appears while Ivo's title and signal choices remain.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Ivo route-event text scan/screenshot at `.artifacts/test-results/ivo-tallymast-no-switchboard-plaque.png`.

- Route HUD Waymarks menu: wired the route-map shared HUD Waymarks chip to a route-side Found Waymarks drawer, including close/Esc behavior, owned Waymark art loading, readable family/rarity/effect rows, and text-state reporting via `waymarkDrawerOpen`.
- Verification: `npm run build`, `npx playwright test tests/smoke.spec.ts -g "route HUD Waymarks chip opens"`, required `develop-web-game` client on built preview `http://127.0.0.1:5189/`, and screenshots inspected at `.artifacts/test-results/route-hud-waymarks-drawer.png` and `.artifacts/test-results/route-waymarks-client/shot-0.png`.

- HUD cover icon size trim: reduced the shared top-HUD Cover shield icon from `38` to `28` in `drawCoverHudIcon`, reduced the cover count text from `13px` to `12px`, and scaled the fallback vector shield to match so the cover marker no longer dominates the space between Cohesion and the resource chips.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5190/`, route HUD screenshot inspected at `.artifacts/test-results/cover-icon-small-client/shot-0.png`, and direct Playwright combat HUD screenshot with `12` Cover inspected at `.artifacts/test-results/cover-icon-small-direct/combat-cover-hud.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared.

- HUD metric chip uniformity pass: converted Cover from a standalone shield/count marker into the same shared metric-chip component used by Resonance, with icon on the left and label/value text to the right. Removed the special Scrap-only chip branch so Scrap now uses the same label/value formatting as Cover, Resonance, Deck, Waymarks, and Supplies.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5191/`, route HUD screenshot inspected at `.artifacts/test-results/hud-uniform-chips-client/shot-0.png`, and direct Playwright combat HUD screenshot with `12` Cover inspected at `.artifacts/test-results/hud-uniform-chips-direct/combat-hud-uniform-cover.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared.

- Combat enemy grounding pass: combat enemy art now measures each enemy texture's visible alpha bounds and places the visible bottom edge on the combat shadow, with shadow width/height and the invisible hitbox following the corrected visible sprite placement. This replaces the fixed center-offset art placement that could make shadows feel detached from padded enemy PNGs.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5192/`, direct Playwright combat capture, and post-card-click responsiveness smoke completed without browser/page errors. Screenshots inspected: `.artifacts/test-results/enemy-grounding-client/shot-0.png`, `.artifacts/test-results/enemy-grounding-direct/single-combat-canvas.png`, and `.artifacts/test-results/enemy-grounding-direct/post-card-click-canvas.png`.

- Battle pressure tuning pass: increased generated encounter weighting toward multi-enemy fights (`2-enemy/multi` from `8x` to `12x`, `3+ enemy` from `12x` to `20x`), raised non-boss solo scaling to `135%` HP and `+2` damage, raised non-boss multi scaling to `120%` HP and `+2` damage, and converted enemy self-`gainCover(N)` turns into `+N` next-attack damage wind-ups so those turns create real pressure instead of disposable enemy Cover. Ally/all-enemy Cover support effects still create Cover for target-priority gameplay.
- Verification: `npm run validate:runtime`, `npx playwright test tests/smoke.spec.ts -g "encounter composition supports solo tuning and support companions|generated combat payloads favor multi-enemy encounters"`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5193/`, direct forced multi-encounter Playwright capture, and `npm run validate` all passed. Screenshots/state inspected: `.artifacts/test-results/battle-pressure-client/shot-0.png`, `.artifacts/test-results/battle-pressure-client/state-0.json`, `.artifacts/test-results/battle-pressure-direct/multi-encounter-pressure.png`, and `.artifacts/test-results/battle-pressure-direct/state.json`. Bundle validation passed with the existing app-entry warning.

- New user prompt: make the Waymark/Supply meaningfulness updates from the review.
- Added backwards-compatible multi-effect Waymark support across runtime types, route/combat Waymark interpreters, route recovery/economy helpers, validation, Codex/tooltips, and the run economy simulator.
- Tuned weak/narrow Waymarks and Supplies: boss Waymarks now have stronger baseline/payoff effects, cache/market Waymarks have fallback value, duplicate Open Sky/combat-start marks were differentiated, low-value recovery/suit marks were raised, and `Map Sticker Strip`, `Shade Cloth`, and `Oath Ledger` were strengthened.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Waymark economy/combat hooks, boss Waymark rewards, expanded Supplies, build-around Waymarks, and next-item route/combat hooks, plus the required generic `develop-web-game` client on `http://127.0.0.1:5193/` all passed. Generic client screenshot inspected at `output/web-game/shot-1.png` with menu text-state JSON.

- HUD Waymarks overflow scrolling: added row-based mouse-wheel scrolling to the route and combat Found Waymarks drawers, reset scroll on fresh opens, and added compact scrollbar thumbs when carried Waymarks exceed the visible rows. Route now shows three rows to avoid panel bleed; combat shows four rows.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright overflow captures with all 58 Waymarks carried. Screenshots inspected: `.artifacts/test-results/route-waymark-drawer-overflow-scrolled-fixed.png` and `.artifacts/test-results/combat-waymark-drawer-overflow-scrolled.png`.

- HUD Deck/Waymarks menu alignment: added a shared `HUD_MENU_PANEL` frame/anchor definition and applied it to route Deck, route Waymarks, combat Deck, and combat Waymarks menus so the border size, header icon position, and close button position match across the run. Combat modal menus now clear transient FX toasts before rendering so combat-start feedback cannot overlap the standardized panels.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright route/combat captures. Screenshots inspected: `.artifacts/test-results/hud-menu-align-route-deck.png`, `.artifacts/test-results/hud-menu-align-route-waymarks.png`, `.artifacts/test-results/hud-menu-align-combat-deck.png`, and `.artifacts/test-results/hud-menu-align-combat-waymarks.png`.

- HUD Deck Review detail placement: added a shared `DECK_DETAIL_LAYOUT` for route and combat Deck Review detail panels. The detail frame now starts below the Close control, and the selected card art plus cost pip sit inside the bordered panel instead of spilling past the left edge.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright captures inspected at `.artifacts/test-results/hud-deck-detail-route-fixed.png` and `.artifacts/test-results/hud-deck-detail-combat-fixed.png`.

- HUD Supplies menu: wired the unified HUD Supplies chip on route and combat into a same-size Packed Supplies menu using the shared `HUD_MENU_PANEL` frame, header icon anchor, and Close control used by Deck and Waymarks. Supply tiles show carried Supply art, category/rarity/timing, descriptions, empty slots, and route/combat use availability.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright captures inspected at `.artifacts/test-results/hud-supply-menu-route.png` and `.artifacts/test-results/hud-supply-menu-combat.png`.

- HUD Resonance cluster: moved the shared Resonance chip out of the right-side resource row and into the left status cluster next to Cohesion and Cover. Scrap, Deck, Waymarks, and Supplies now remain grouped as the run inventory/navigation chips.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright route/combat captures inspected at `.artifacts/test-results/hud-resonance-cluster-route.png` and `.artifacts/test-results/hud-resonance-cluster-combat.png`.

- Combat end-turn declutter: removed the bottom-right safe-play/Open Sky helper line and replaced the red `Roost / End Turn` command box with an icon-only end-turn control above the discard pile. The control keeps a hover tooltip and the hidden hit area still advances combat turns.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright combat capture inspected at `.artifacts/test-results/combat-roost-cleanup-before-click.png`, and icon-only end-turn click verified by state advancing from turn 1 to turn 2.

- Roost icon hover stability: removed the size-changing hover state from the icon-only combat end-turn control so the Roost icon keeps a fixed `74x74` display size at rest and on hover. Hover feedback is now alpha-only plus the existing tooltip.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright combat captures inspected at `.artifacts/test-results/roost-fixed-size-rest.png` and `.artifacts/test-results/roost-fixed-size-hover.png`, and icon click verified by state advancing from turn 1 to turn 2.

- Waymark/Supply/card meaningfulness follow-up: made `returnDiscard(nonMolt, +1)` actually return a non-Molt card and draw 1, fixed Canal Shelter's Molt suit gate from stale `cups` to live `basins`, strengthened underperforming Waymarks/Supplies, and gave low-impact recursion/discard/Cover cards extra payoff so their effects are visible in play.
- Also resolved the existing RouteScene/BattleScene supply drawer compile blockers by keeping the shared HUD Supplies chip connected to usable route/combat supply drawers and text-state flags.
- Verification: `npm run validate:runtime`, `npm run build`, focused smoke tests for Waymark economy/combat hooks, recursion/Basins gate, expanded Supplies, build-around Waymarks, next-item route/combat hooks, and new balance verbs all passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5174/`; screenshot/state inspected at `output/web-game/shot-1.png` and `output/web-game/state-1.json`.

- New user prompt: implement the comprehensive balance plan to make gameplay more challenging and reduce card-spam dominance.
- Added combat decision telemetry to run summaries and `render_game_to_text`: cards played, overextensions, low-card Roosts, held cards, unspent Wingbeat, active Waymarks, and boss-entry Cohesion/Scrap.
- Added restraint Waymark triggers (`onRoostWithWingbeat`, `onRoostWithCardsInHand`, `onLowCardTurn(N)`, `onTurnEndNoOverextension`) and converted several old Nth-card spam payoffs into clean-Roost/low-card rewards.
- Added a 6 non-boss Waymark active cap with boss Waymarks exempt; route/combat rewards and the economy simulator now enforce the same cap.
- Tuned late economy down for Maps 3-4, nudged generated Map 4 basin access upward, reduced Spark-Caller’s free opening hand inflation, softened Talon’s opening Flow, and made Roostkeeper’s Perfect Brace counterstrike.
- Updated playtest stats to report the new decision metrics and added focused smoke coverage for restraint triggers and Waymark cap behavior.
- Verification: `npm run validate:runtime`, `npm test`, `npm run audit:balance`, `npm run audit:economy`, `node tools/simulate-run-economy.mjs --json --seeds=1000`, `npm run build`, and focused Playwright smoke tests passed. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5194/`; screenshot inspected at `output/web-game/shot-1.png`. Direct battle screenshot inspected at `.artifacts/test-results/balance-overextension-risk-hint.png`.
- Follow-up: resolved the bundle-size gate by switching production minification to Terser and collapsing repeated UI style literals behind shared constants. Full `npm run validate` now passes; app entry is 674.4 KB against the 675 KB hard cap, with the existing 590 KB target warning still reported.
- Remaining test-harness work: a full `npx playwright test tests/smoke.spec.ts` pass currently has broader async scene-start/fx-layer failures in older tests that directly call `g.scene.start(...)` and scene internals before the next Phaser tick. Focused balance/HUD smoke coverage passes; the broad harness should be updated to await scene readiness before direct assertions.

- Selected UI/UX implementation pass: converted route/combat Waymark and Supply drawers to a shared compact item-tile renderer, replaced long/truncated drawer and Codex item copy with player-facing effect summaries, added missing plain-language effect formatting for Supply/Waymark verbs, removed the redundant combat left-side Supply shelf, separated resource and inventory HUD groups with a divider, kept Waymarks in the familiar HUD click zone, and added lightweight Market section rails plus a small Scrap label in market item details.
- Verification: `npm run build`, `npm test`, and focused Playwright smoke tests for route HUD Waymarks, market item detail, and Codex Supplies passed. Required `develop-web-game` local client ran on `http://127.0.0.1:5174/`; screenshots inspected at `.artifacts/ui-selected/route-waymark-drawer.png`, `.artifacts/ui-selected/market-hierarchy.png`, `.artifacts/ui-selected/combat-supply-drawer.png`, and `.artifacts/ui-selected/codex-items.png`.

- Selected UI/UX follow-up pass: polished card detail surfaces so combat, reward, and preen previews show the active card contract, including Molt-active target/role/effect text and the alternate normal/preened line. Reward/preen choice cards now use compact effect copy plus a Molt marker, Codex Supply/Waymark detail modals include player-facing effect summaries above the raw rules, keyword/stat hover tooltips are scene-safe after combat, and card detail art falls back to already-loaded compact art instead of showing blank panels.
- Entry-size cleanup: removed unused legacy route supply/status rail renderers and old combat supply/Waymark shelf renderers superseded by the HUD menu/drawer flow. Bundle validation still passes with the app entry warning: `index-Dd7IwM0_.js` is 672.7 KB minified / 165.6 KB gzip, so a true scene/module split remains the next performance step.
- Verification: `npx tsc --noEmit`, focused Playwright smoke tests for card choice hovers, Codex Supplies, Molt active card contracts, and preened Molt/Codex notes, `npm run build`, `npm test`, and `npm run validate:bundle-size` passed. Required `develop-web-game` client ran on `http://127.0.0.1:5174/`. Screenshots inspected at `.artifacts/ui-selected-next/combat-molt-card-detail.png`, `.artifacts/ui-selected-next/reward-choice-card-hover.png`, `.artifacts/ui-selected-next/preen-choice-card-hover.png`, `.artifacts/ui-selected-next/codex-supply-detail.png`, and `.artifacts/ui-selected-next/codex-waymark-detail.png`.

- Broad smoke harness closure: replaced direct same-scene restarts with `window.__birdSquadStartScene`, which stops an active scene before restart and waits for seeded RouteScene readiness / BattleScene FX readiness. Updated stale route/Codex expectations to assert current HUD text-state contracts, generated-route availability, lazy-loaded Codex notes, and current combat damage tuning.
- Verification: focused route/Codex smoke tests passed, then full `npx playwright test tests/smoke.spec.ts` passed with 73/73 tests. `npm run validate`, `npm test`, `npm run build`, `npm run audit:balance`, and `npm run audit:economy` passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5273/`; screenshot/state inspected at `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Remaining note: no blocking work items are left from the balance implementation. The app entry still exceeds the softer 590 KB bundle target while passing the 675 KB hard cap, so a future scene/module split is still the clean performance follow-up.

- Balance challenge follow-up: reduced unconditional zero-cost draw/refund loops on key Plumes, Quills, Basins, Lantern Roost, Eclipse Watch, and Fledgling cards; added post-Roost card-volume telemetry so enemies can punish 4+ card turns, repeated zero-cost plays, and empty-hand Roosts; converted more Waymarks from combat-start or Nth-card rewards into clean-Roost/held-card/unspent-Wingbeat timing rewards; added late Basin/Nest/Market Scrap sinks for boss prep and route scouting; and added non-boss overextension repair costs to post-combat Scrap rewards.
- Enemy and economy tuning: Map 2-4 enemies now include targeted anti-spam conditional effects, late markets sell Boss Rigging and Scout Roofline services, Basin offers Emergency Perch, Nest offers Boss-Ready Tune-Up, and the economy simulator now samples live Basin/Nest choices plus premium market prep services.
- Verification: `npm run validate`, `npm run build`, `npm run validate:bundle-size`, `npm run audit:balance`, `npm run audit:economy`, `npm test -- --runInBand`, and focused Playwright smoke tests for premium market services, enemy Roost punishers, conditional free draw, updated Basin/cache route choices, and reward-screen input safety passed. A full `npx playwright test tests/smoke.spec.ts --reporter=line` run reached 74/76 before two stale expectations were corrected; the corrected two-test subset then passed. Required browser/client check ran on `http://127.0.0.1:5173/`; screenshot inspected at `.artifacts/balance-pass-screenshot.png`.
- Remaining note: the app entry is 674.8 KB against the 675 KB hard cap after trimming dead market utility card plumbing and increasing Terser compression passes; it still exceeds the soft 590 KB target, so scene/module splitting remains the clean performance follow-up.

- Combat log removal UX pass: removed the player-facing combat event strip, `Log >` affordance, recent-signals drawer, and dormant route log footer. Supply feedback remains as immediate feedback, but the ledger-like `LAST SUPPLY` label was removed and replaced with the supply icon treatment.
- Verification: `npx tsc --noEmit` passed, required `develop-web-game` client ran against `http://127.0.0.1:5173/`, and direct Playwright combat screenshot/text scan confirmed no visible `Log`, `LAST SUPPLY`, or recent-signals strings at `.artifacts/test-results/combat-no-log-ui.png`. `npm run build` is currently blocked by a Windows lock on generated `.artifacts/build/assets/map_sticker_strip-U61G5Fsa.webp` from existing local preview/dev/test processes, after TypeScript had completed.

- Iconography/simplification pass for selected recommendations 1, 3, 4, 5, 6, and 7: HUD chips now read as icon + value, hand cards use compact rule grammar, route confirmation is icon-only with a hover tooltip, route node hover uses icon/risk/reward chips, Waymark/Supply run drawers prioritize icon + name + one-line effect grammar, and deck review uses icon counts plus target/role/stat chips.
- Verification: `npx tsc --noEmit`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright screenshots inspected at `.artifacts/test-results/icon-pass-route-confirm.png`, `icon-pass-route-tooltip.png`, `icon-pass-combat-hand.png`, `icon-pass-deck-review-fixed.png`, `icon-pass-waymarks-drawer-filled.png`, and `icon-pass-supplies-drawer-filled.png`; fresh browser console check had no errors. `npm run build` passed with the existing large chunk warning.

- Card/Supply/Waymark meaningfulness pass: removed exact Waymark duplicates and made several low-interest cards/items ask for clearer decisions. Plume Flash now rewards banked Resonance before generating more; Quill Slip now needs Winded setup for its payoff; Basin/Nest Fledglings convert into Open Sky/next-brace planning instead of generic free value; Hover Check and Spiral Search now care about Open Sky/retention. Zip Tie Roll, Spare Harness, Rooftop Decoy, Bottlecap Popper, Tar Solvent, Emergency Call, and Rain Cape now occupy more distinct tactical jobs.
- Runtime support: card `retainHand` is now resolved like the existing Supply/Waymark retain effect, and Signal Waymarks now resolve all authored route effects instead of only reading `gainScrap`, allowing Wire Map to provide route preview without no-oping. Added the missing `BattleScene.logDrawerOpen` field that was already referenced by existing drawer UI and blocked TypeScript compilation.
- Verification: duplicate scan reports zero exact card/Supply/Waymark effect duplicates. `npm run validate:runtime`, `npm run validate`, `npm run audit:balance`, `npm run audit:economy`, `npm test`, `git diff --check`, and `npm run build` passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5173/`; menu and route screenshots inspected under `output/web-game-balance-pass-start/`. Manual Playwright route-to-combat smoke completed with no console/page errors; combat screenshot/state inspected at `output/web-game-balance-pass-manual/03-after-route-click.png` and `output/web-game-balance-pass-manual/03-after-route-click-state.json`.
- Remaining note: no blocking work items from this pass. Existing bundle soft-target warning remains; the app still passes the hard bundle-size gate.

- Runtime continuation: added `tools/validate-deployment-cache.mjs`, wired `validate:deployment-cache` into `npm run validate`, and documented the hashed-asset/cache-header build contract in `docs/deployment-cache.md`.
- Smoke harness cleanup: moved Playwright webServer execution to `npm run build` plus `vite preview` on a smoke port, disabled smoke HMR/watch churn, and added boot retry logic so broad smoke coverage runs against production output instead of a reload-prone dev server.
- Bundle cleanup: shared optional runtime/preload image loader bookkeeping in `src/game/runtime-images.ts`, moved Codex glossary data into the lazy Codex data chunk, and tightened small Codex text-state/header strings. The app entry now passes the 675 KB hard cap again at `index-DQbnt2gm.js` 674.7 KB minified / 166.0 KB gzip; the intentional 590 KB target warning remains.
- Smoke expectation refresh: updated stale assertions for Tar Solvent's authored `removeCover(12)` effect and current Codex subtitle copy.
- Verification: `npm run build`, `npm run validate`, `npm test`, focused smoke for balance/supplies/next-item hooks, and full `npx playwright test tests/smoke.spec.ts --workers=1 --reporter=line` all passed (77/77). Required `develop-web-game` client ran against built preview `http://127.0.0.1:5391/`; screenshot and state inspected at `.artifacts/runtime-continue-client/shot-1.png` and `.artifacts/runtime-continue-client/state-1.json`.
- Remaining note: no blocking harness/cache work remains. The next meaningful performance item is still a real scene/module split to move the app entry toward the 590 KB soft target; local string/dead-code trimming is now down to tiny margins.

- New user prompt: add a glossary of keywords to the Codex.
- Added a `Glossary` Codex section backed by lazy `codex-data` terms, with 25 core gameplay, run-item, and route-node keywords including Wingbeat, Molt, Open Sky, Overextension, Waymark, Supply, Scrap, Basin, Nest, Signal, and Cache.
- Rendered glossary entries as compact two-column Codex tiles with category chips and short gameplay summaries; `render_game_to_text` now exposes the glossary term list when the Glossary section is active.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:bundle-size`, focused Codex smoke tests, and required `develop-web-game` client passed. Visual screenshot inspected at `output/web-game-glossary/shot-0.png`; text state reported `section: glossary` with 25 terms.

- Codex glossary tab placement follow-up: moved the top-level Glossary section tab to the right-most Codex tab slot after Enemies.
- Verification: `npm run build`, required generic `develop-web-game` client on `http://127.0.0.1:4173/`, direct built-preview Codex probe confirmed section labels render left-to-right as Cards / Items / Leaders / Enemies / Glossary, and screenshot inspected at `.artifacts/test-results/codex-glossary-rightmost.png`. Only the usual WebGL `ReadPixels` screenshot warnings appeared.

- Combat Roost button size stability: fixed the icon-only combat Roost control so it is created at the same fixed `74x74` display size used by its hover/out handlers, preventing the global UI icon preview scale from making it start oversized and then snap smaller after pointer-out.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5195/`; focused direct Playwright combat probe measured the Roost icon at `74x74` at rest, on hover, after pointer-out, and after click/turn advance. Screenshots inspected at `.artifacts/test-results/roost-button-fixed/client/shot-0.png`, `.artifacts/test-results/roost-button-fixed/direct/roost-rest.png`, `.artifacts/test-results/roost-button-fixed/direct/roost-hover.png`, and `.artifacts/test-results/roost-button-fixed/direct/roost-after-click.png`.

- Wingbeats icon generation: referenced `docs/art/art-bible.md`, `docs/art/ui-iconography-workshop.md`, and existing UI/Supply/Waymark icons, then generated a high-resolution pixel-painterly brass/teal wingbeat charm with built-in image generation. Processed the chroma-key result to transparent PNG and saved the runtime asset at `assets/runtime/ui/icons/wingbeats.png`.
- Wingbeats HUD integration: added the `wingbeats` UI icon id and rendered it as a fixed `34x34` badge attached to the combat Wingbeats orb, preserving the existing numeric value and label.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5196/`; direct combat Playwright probe confirmed `ui-icon-wingbeats` renders at `34x34` in `BattleScene`. Screenshots inspected at `.artifacts/imagegen/wingbeats/wingbeats-runtime-check.png`, `.artifacts/test-results/wingbeats-icon/direct/combat-wingbeats-icon.png`, and `.artifacts/test-results/wingbeats-icon/direct/wingbeats-orb-crop-3x.png`.

- Wingbeats shared-chip integration: moved the combat Wingbeats meter from its one-off orb/badge treatment to the same `renderHudMetricChip` path used by Cover and Resonance, with `hudIconForLabel` and `statIconForKey` resolving Wingbeat/Wingbeats/Energy to the generated `wingbeats` UI icon.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5197/`; direct combat Playwright probe confirmed one `ui-icon-wingbeats` image inside a `126x44` BattleScene metric chip. Screenshot inspected at `.artifacts/test-results/wingbeats-icon-shared/direct/combat-wingbeats-shared-chip.png`.

- Wingbeats HUD placement correction: moved Wingbeats into the top combat HUD beside Cover and Resonance, removed the lower-left duplicate meter, and kept the Wingbeats icon on the exact same shared HUD icon display sizing path as the adjacent Cover/Resonance icons.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5198/`; direct BattleScene probe measured Cover, Wingbeats, and Resonance icons at matching `61.6x61.6` display size, with no lower Wingbeats icon remaining. Screenshot inspected at `.artifacts/test-results/wingbeats-hud-top/direct/combat-wingbeats-top-hud-crop.png`.

- Flock/Roost icon integration: replaced the runtime `flock-heart` icon with the clearer health-heart Cohesion concept and replaced `roost-nest` with the Roost End Turn concept. Both were normalized to 256x256 alpha PNGs for predictable HUD/button scaling; previous runtime icon files were preserved under `.artifacts/imagegen/flock-roost-icon-concepts/previous-runtime-icons/`.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5199/`; direct BattleScene probe confirmed `ui-icon-flock-heart` in the top HUD and `ui-icon-roost-nest` on the end-turn control at the expected display sizes. Screenshot inspected at `.artifacts/test-results/flock-roost-icons-integrated/direct/combat-flock-roost-icons.png`.

- Flock/deck HUD correction: moved the Flock health icon above the HP bar, kept the lower-left draw pile count/open action scoped to cards left in the draw pile, and made the top HUD Deck chip show/open the full combat deck instead of only the remaining draw pile.
- Verification: `npx tsc --noEmit`, isolated `npx vite build --outDir .artifacts/build-hud-deck --emptyOutDir true`, required `develop-web-game` client on `http://127.0.0.1:5200/`, and direct Playwright probes passed. Direct state measured draw pile `5`, hand `5`, discard `0`, and deck `10`; lower-left draw opened `Draw Pile (5)`, while the HUD deck chip opened `Deck Review (10)`. Screenshots inspected at `.artifacts/test-results/hud-deck-counts/direct-final/combat-hud-deck-counts.png`, `.artifacts/test-results/hud-deck-counts/direct-final/draw-overlay.png`, and `.artifacts/test-results/hud-deck-counts/direct-final/deck-overlay.png`.

- Flock HUD icon alignment follow-up: resized the Flock health icon to the same shared HUD icon display size as Cover, Wingbeats, and Resonance, moved it onto the same top-HUD centerline, and drew it after the HP bar fill so it visibly sits above the bar layer.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5201/`, and direct Playwright HUD probe passed. The probe measured Flock, Cover, Wingbeats, and Resonance icons at `61.6x61.6`; Flock centered at `y=45` and rendered after the HP bar rectangles. Screenshot inspected at `.artifacts/test-results/hud-flock-icon-level/direct/combat-flock-icon-level.png`.

- Non-combat event visual pass: upgraded Basin/Nest/Cache/Signal event overlays with physical object-style choice cards, route-effect outcome token strips, stronger resident/place plaques, subtle ambient glow/motion, and bespoke Cache/Signal compositions. Cache now presents as a pulley cache cabinet with drawer choices; Signal now presents as a switchboard route map with live wire paths; Basin/Nest reuse the richer object-choice treatment while preserving their existing resident/backdrop art.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5202/`, and direct built-preview screenshots for Cache, Signal, Nest, and Basin passed. After a second layout tightening pass, direct captures on `http://127.0.0.1:5203/` confirmed no bottom text overflow. Final screenshots inspected at `.artifacts/test-results/noncombat-events-visual-pass/direct-final/cache-event.png`, `.artifacts/test-results/noncombat-events-visual-pass/direct-final/signal-event.png`, `.artifacts/test-results/noncombat-events-visual-pass/direct-final/nest-event.png`, and `.artifacts/test-results/noncombat-events-visual-pass/direct-final/basin-event.png`.

- Non-combat event UX follow-up: added hover detail panels to route event choice cards with before/after previews for Cohesion, Scrap, Deck, Supplies, Waymarks, and next-combat setup; locked choices now still explain their requirement on hover. Basin gained a dedicated roost hearth service prop with recovery/shelter/supply stations and animated steam, while Nest gained a bespoke Featherwright deck bench with Preen/Sleeve/Remove tool stations instead of using the generic set-piece fallback.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5204/`, and direct built-preview screenshots for Basin, Nest, Cache, and Signal hover states passed. Screenshots inspected at `.artifacts/test-results/noncombat-events-ux-pass/direct/basin-hover.png`, `.artifacts/test-results/noncombat-events-ux-pass/direct/nest-hover.png`, `.artifacts/test-results/noncombat-events-ux-pass/direct/cache-hover.png`, and `.artifacts/test-results/noncombat-events-ux-pass/direct/signal-hover-centered.png`.

- Enemy battlefield placement pass: lowered and enlarged boss anchors without letting Tar-Crowned Crow flow into the HUD, staggered boss helpers lower/wider on the battlefield, aligned enemy art by visible alpha bounds so tall cutout art stands on its shadow, widened boss HP bars/shortened boss labels, and pushed intent badges outward in boss fights so boss/helper badges no longer overlap.
- Bundle/performance follow-up: split runtime game data into a `runtime-data` production chunk in `vite.config.ts`, bringing the app entry well under the 675 KB hard cap while preserving the existing lazy Codex chunk.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:bundle-size`, focused boss Playwright smoke tests, and `git diff --check` passed. Built-preview screenshot inspected at `.artifacts/test-results/tar-crowned-crow-placement-final.png`; measured Tar-Crowned Crow visible top at `y=135.5` with HUD bottom around `y=92` and hand top at `y=467`.

- Card/item grade improvement implementation: upgraded low-identity defensive/healing cards into timing or build-around choices. Locked Nest now trades raw cover for retain and full-block next-turn draw, Basin Chorus and Closed Basin reward full Cohesion/Open Sky planning, Twin Basin Bond and Cold Nest reward restraint/full-block states, Shared Nest sets up the next Nest brace, and Spilled Basin now plays as Open Sky mitigation instead of generic healing.
- Waymark/Supply identity pass: Patched Harness moved from passive Preen discount to after-purchase Scrap refund plus next-fight Open Sky Guard; Patched Shoulder Wrap now rewards a clean enemy turn with Cover and retention; Borrowed Raincoat, Flock Counterweight, Basin Overflow Cup, Loose Change Tin, Supply Bell, and Chalk Wingmark now ask for more specific timing. Feather Splint, Mirror Shard, and Thermos Lid gained route-context bonuses so they care about where the run has been.
- Test/metadata follow-up: removed Patched Harness from the market Preen discount metadata, updated smoke expectations for the new Waymark timings and Supply Bell repeat synergy, and fixed a stale `playUiSound('click')` type mismatch by using the existing confirm sound variant.
- Verification: `npm run validate:runtime`, `npm run build`, `npm run validate:bundle-size`, `npm test`, and focused smoke tests for Waymark economy/combat hooks, build-around Supplies/Waymarks, and Locked Nest Molt behavior all passed. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; route screenshot/state inspected at `.artifacts/test-results/balance-meaningfulness-client/shot-0.png` and combat screenshot/state inspected at `.artifacts/test-results/balance-meaningfulness-combat-client/shot-0.png`.
- Remaining note: no blocking work items from this pass. The repository still contains broad pre-existing balance/Codex/UI changes in the dirty worktree, and the existing large-chunk warning remains a future performance follow-up rather than a blocker.

- Remaining iconography cleanup pass: converted enemy intent labels from visible move-name text to icon/value badges with hover tooltips, made deck review scroll controls icon-only, shortened card detail language to `Now` / `Preen` / `Base` with Molt shown only when active, quieted empty Waymark/Supply drawer states, and removed repeated Market item category labels plus the last visible `SCRAP` price label in favor of the scrap icon.
- Codex wording cleanup: renamed player-facing item detail sections from build/prompt language to shorter `TAGS`, `SUMMARY`, `ROLE`, `TRIGGER`, and `OBJECT` labels, and removed the visual/prompt brief from Supply detail modals.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client passed on `http://127.0.0.1:5173/`. Direct Playwright screenshots inspected at `.artifacts/test-results/icon-final-combat-intent.png`, `.artifacts/test-results/icon-final-deck-detail.png`, `.artifacts/test-results/icon-final-market-labels.png`, `.artifacts/test-results/icon-final-waymark-drawer.png`, `.artifacts/test-results/icon-final-supply-drawer.png`, and `.artifacts/test-results/icon-final-codex-supply-detail.png`.

- Creativity/meaningfulness balance pass: improved low-grade cards, Supplies, Waymarks, and early enemies identified in the latest audit. Curb Step now rewards Open Sky and perfect-block timing, Scattered Quills became an anti-Cover AOE setup card, Tangled Line and Bad Directions now create distinct nuisance/loop decisions, Signal Flare gained route-memory payoff, and six underperforming Waymarks now pair their trigger with a second tactical hook.
- Enemy identity pass: Roof Rat now punishes empty-hand Roosts and coverless turns, Wire Hawk now pressures 4+ card turns with Open Sky/bonus attack setup, and Pigeon Enforcer now interacts with the Flock's Cover instead of acting as a plain stat check.
- Test follow-up: smoke coverage now asserts Ledger Tab's carried Open Sky Guard, Broken Cover Chime's Resonance, and Plumes Applause Cap's next-turn draw in addition to their prior effects.
- Verification: focused Playwright smoke tests for enemy effects, expanded Supplies, build-around Waymarks, next-item route/combat hooks, enemy Codex, and new balance verbs passed 7/7; `npm test`, `npm run build`, `npm run validate:bundle-size`, and `git diff --check` passed aside from existing CRLF warnings. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; combat screenshot/state inspected at `.artifacts/test-results/creativity-meaningfulness-client/shot-0.png` and `.artifacts/test-results/creativity-meaningfulness-client/state-0.json`.

- Low-grade lift follow-up: raised the post-audit floor by giving weak cards setup clauses, making snags more distinct burdens, and turning the weakest single-effect Waymarks into two- or three-part tactical hooks. Basin heal Waymarks now fire route rider effects after Basin recovery, after-street Waymarks can carry next-combat prep, and combat Waymarks now honor `gainScrap`.
- Specific tuning: Quill Point, Nest Juggle, Twin Plume Lookout, Underwing Shelter, Scavenger Eye, Quill Outrider, Tangled Line, Wet Feathers, and Bad Directions all gained clearer setup/payoff texture. Rain Gutter, Blue Cup Token, Rooftop Shortcut, Feather Tape, Courier Thread, Bright Bottlecap, Dawn Ribbon, Quill Notch, Harbor Bead Strand, Workshop Stamp, Molt Metronome, Eave Bloom, Plumes Busker Patch, Fresh Pinfeather, Shed Feather Case, Stage Pin, Ledger Tab, Patched Harness, Rooftop Relay Bell, Market Tally String, Rooftop Nest Lining, Razor Kite Tail, Spare Harness, and Windcatcher Spool gained meaningful rider effects or runtime support.
- Enemy floor lift: Rat Courier, Rooftop Raccoon, and Alley Cat now punish overextension, empty-hand Roosts, or coverless play more clearly without simply raising base damage.
- Audit result after the pass: cards overall A- with floor B, enemies overall A- with floor B+, items overall B- with floor C+; remaining C+ items are mostly passive economy/recovery pieces that are useful but intentionally simpler.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused Playwright smoke tests for route economy, combat Waymarks, Supplies, next-item hooks, and new balance verbs passed; `npm test`, `npm run build`, and `npm run validate:bundle-size` passed. One focused smoke boot timed out once, then passed on isolated rerun. Required `develop-web-game` client ran on `http://127.0.0.1:5173/`; screenshot/state inspected at `.artifacts/test-results/low-grade-lift-client/shot-0.png` and `.artifacts/test-results/low-grade-lift-client/state-0.json`.

- Item floor lift: raised the remaining low-interest item floor from C+ to B- by giving simple economy/safety tools a second decision axis. Loose Change Tin now rewards clean non-overextended Roosts with retention, Rain Gutter previews the next route after Basin healing, Bright Bottlecap and Stage Pin add next-turn tempo, Courier's Ledger and Ledger Tab soften future Open Sky pressure, Blue Cup Token and Spare Pocket create Supply-pack planning, and Shed Feather Case now turns Molt entry into next-turn Wingbeat. Quiet Landing, Nest Ratchet, Spark Ground Clip, Resonance Battery, Chalk Dust Pouch, Spare Harness, and Windcatcher Spool also gained route, Winded, Open Sky, or next-turn riders so they no longer read as single-note coupons.
- Audit result after the item pass: cards overall A- with floor B, items overall B with floor B-, and enemies overall A with floor A-. Targeted improved items now range from B- to A-, with Windcatcher Spool, Stage Pin, Spare Harness, and Bright Bottlecap leading the item spotlight.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused Playwright smoke tests for route economy/combat Waymarks/Supplies/new balance verbs passed 6/6, `npm test`, `npm run build`, and `npm run validate:bundle-size` passed. `git diff --check` reported only the existing CRLF warnings. Required `develop-web-game` client ran on `http://127.0.0.1:5173/`; combat screenshot/state inspected at `.artifacts/test-results/item-floor-lift-client/shot-0.png` and `.artifacts/test-results/item-floor-lift-client/state-0.json`.

- Remaining work closure: resolved the outstanding bundle/performance warning, UI cleanup, market polish, and broad regression items. Vite's generic warning threshold now matches the deliberate Phaser vendor budget, deployment-cache validation understands the runtime-data boot chunk, and the performance note documents the current production chunk profile and why Codex remains lazy.
- Cleaner UI follow-up: market service controls now use icon + Scrap price instead of permanent service labels, route choices use icons for enter/locked state, reward/picker copy is shorter, stale Codex image-brief expectations were removed, and compact item summaries now format `gainEnergyNextTurn` as `next Wingbeat +N` instead of leaking internal effect grammar.
- Verification: `npx tsc --noEmit`, `npm run validate`, focused Playwright smoke slices for market details and Waymark drawers, and the full `npx playwright test tests/smoke.spec.ts --workers=1 --reporter=line` passed 77/77 after the cleanup. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; visual screenshots inspected at `.artifacts/test-results/remaining-market-cleanup.png`, `.artifacts/test-results/remaining-waymark-drawer-cleanup.png`, and `.artifacts/test-results/remaining-card-reward-cleanup.png`.

- Snag card art pipeline: added a dedicated transparent Snag tarot border from imagegen output, prepared from the raw chroma-key template with connected chroma removal only (no rectangular image-window wipe), and composed Tangled Line, Wet Feathers, and Bad Directions as `center art + alpha border overlay + deterministic title text`.
- Snag Codex integration: added approved runtime portrait/thumb/icon entries for the three Snag cards, a rightmost `Snags` card tab in the Codex, Snag-specific labels/dossier accenting, and discovery when enemies insert a Snag into draw/discard.
- Snag title polish: centered the Snag title compositor vertically in the cartouche and rebuilt the three card masters plus runtime derivatives.
- Verification: visually inspected the three composed Snag cards; `npx tsc --noEmit`, `npm run validate:runtime`, `npm run validate:runtime-assets`, and focused `npx playwright test tests/smoke.spec.ts -g "codex: snag cards" --workers=1 --reporter=line` passed.

- Non-combat event maturity follow-up: non-picker Basin/Cache/Signal/Nest choices now pause on a `RESOLVED` outcome ledger instead of immediately snapping back to the route map. The ledger reuses the event backdrop/icon art, records the chosen action and resolved effect text, then lists actual before/after changes for Cohesion, Scrap, Deck, Supplies, Waymarks, and next-combat setup before `Continue` returns to route flow.
- Layout/copy polish: expanded the result panel so multi-row outcomes do not crowd the Continue button, separated the chosen action from the resolved effect line, and replaced the non-ASCII route-effect separator with ` / ` so multi-effect summaries render cleanly on the canvas.
- Verification: `npx tsc --noEmit`, `npm run build`, `git diff --check -- src/main.ts progress.md`, required `develop-web-game` client on `http://127.0.0.1:5205/`, and focused Playwright route-event probes passed. Screenshots inspected at `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/basin-result-ledger.png`, `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/cache-result-ledger.png`, `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/signal-result-ledger.png`, and `.artifacts/test-results/current-dev-5173-resolution.png`. Restarted `5173` fresh and verified the ledger there too.

- Marn/Rooftop Cache maturity pass: reframed the cache event as Marn Valeclip's numbered lockbox appraisal instead of a flat reward list. Cache options now resolve as drawer claims with drawer numbers, seal/tell language, Marn notes, icons, and risk tags; the central cabinet and appraisal plaque teach clean seals, warm latches, and bad tags while preserving the existing reward mechanics.
- Contract cleanup: added the optional card-level `heldEffects` field to `RuntimeCard` so the current card data and `createCardTemplate` compile under TypeScript.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5206/`, and focused Playwright cache probes passed. Screenshots inspected at `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-appraisal.png`, `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-hover.png`, `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-result-ledger.png`, and `.artifacts/test-results/current-dev-5173-marn-cache-appraisal.png`. Restarted `5173` fresh and verified the Marn drawer appraisal there.

- Marn cleanup pass: simplified the Rooftop Cache event from a duplicated cabinet-plus-choice-list layout into a single interaction loop. The cabinet drawers are now the clickable choices, the right side is one `CLAIM FILE` panel that updates on drawer hover, and the extra appraisal plaque/full-card stack was removed so the event reads as inspect drawer -> claim drawer -> resolve ledger.
- Verification: `npx tsc --noEmit`, `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5207/`, and focused Playwright cache probes passed, including a real canvas click on Drawer 12. Screenshots inspected at `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-open.png`, `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-hover.png`, `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-resolved.png`, and `.artifacts/test-results/current-dev-5173-marn-cache-cleanup.png`. Restarted `5173` fresh and verified the cleaned event there.

- Snag gameplay integration: Snag cards now have explicit `heldEffects` that resolve at Roost if the player leaves them in hand, turning them from passive dead cards into cleanup pressure. Tangled Line drains a Wingbeat and loops back into the draw pile, Wet Feathers leaves the flock Winded, and Bad Directions spends Resonance when available before looping back into the draw pile.
- Runtime support: combat now resolves held Snag penalties before normal Roost discard/retain cleanup, displays a small `Snag!` cue, supports `applyWinded(flock, N)`, validates `heldEffects` in card data, and includes Roost penalty text in card descriptions.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish" --workers=1 --reporter=line`, `npm run build`, and `npm run validate:runtime-assets` passed.

- Snag expansion pass: added Loose Shingle, Static Burst, and Bent Feather as new Snag cards with distinct play-vs-Roost decisions. Loose Shingle trades away Cover and punishes no-Cover Roosts, Static Burst drains Resonance or Wingbeat and buffs the next enemy attack if held, and Bent Feather applies Winded but can be played to clear itself for the combat.
- Integration sources: Roof Rat now creates Loose Shingle, Wire Hawk creates Tangled Line on overextension, Signal Gull creates Bad Directions when ignored, Relay Starling creates Static Burst, Tower Pigeon can seed Bent Feather, and rooftop Signal choices now add Static Burst/Loose Shingle in risk-reward paths.
- Art pipeline: generated borderless imagegen center art for the three new Snags, composed all six Snag tarot cards with the existing transparent Snag border, rebuilt runtime portrait/thumb/icon assets, and expanded the Codex Snags tab/test coverage to six cards.
- Verification: visually inspected the three final composed Snag cards; `npx tsc --noEmit`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish|new snag cards|codex: snag cards" --workers=1 --reporter=line`, `npm run build`, and `npm run validate:runtime-assets` passed.

- Snag source audit follow-up: confirmed all six Snags now have both combat enemy injection sources and event/risk exchange sources. Bent Feather was the only gap, so the `rooftop_chalk_smudge` dig choice now trades `gainScrap(38)` for `addSnagToDraw(bent_feather)`.
- Verification: direct Snag-source audit script, `npm run validate:runtime`, and `npx tsc --noEmit` passed.

- Snag expansion follow-up: added four more Snags to cover the remaining pressure gaps. Glass Gap creates Open Sky exposure, Jammed Strap pressures hand/discard cleanup, Barricade Scrap gives enemies Cover, and Smeared Map reduces next-turn draw. The new Snags now enter through readable enemy identities and event exchanges: Wire Hawk/Aerial Kite and Open Sky choices seed Glass Gap, Weasel Cutpurse/tangled gear seeds Jammed Strap, Signal Gull/lookout choices seed Barricade Scrap, and route-scouting choices plus Rat Courier seed Smeared Map.
- Art pipeline: generated high-resolution pixel center art for Glass Gap, Jammed Strap, Barricade Scrap, and Smeared Map with image gen, copied the sources into `assets/concept-art/snag-cards/sources/`, composed all ten Snag tarot cards through `compose:snag-art`, and rebuilt portrait/thumb/icon runtime assets for the approved manifest.
- Runtime polish: added support/formatting for the new Snag effects, including `applyOpenSky`, `enemyGainCover`, and clearer negative `nextTurnDraw(-1)` wording in card text and combat logs. Codex Snag coverage now expects ten cards.
- Verification: visually inspected the four final composed Snag cards; `npm run compose:snag-art -- --strict`, `npm run build:runtime-card-art`, `npx tsc --noEmit`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish|new snag cards|codex: snag cards" --workers=1 --reporter=line`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Marn Nest Note reward picker follow-up: card reward choices are now cached for the lifetime of the opened route event, so Cancel returns to Marn's drawer menu without rerolling the two options. The reward picker also renders fallback mini-card options with cost, name, suit/role, and effect text when compact card art has not been loaded into the route scene.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe for Nest Note Cancel/reopen static choices, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-nest-note-static-cards-5173.png`; the probe confirmed the same two card IDs after Cancel/reopen and no state reward claim on Cancel.

- Marn Nest Note card-art follow-up: the reward picker now queues runtime compact card art for the exact two static reward choices and redraws the picker when those textures load, so the options display as actual card faces with their illustration art rather than text-only fallback cards.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed `card-thumb-*` textures were loaded and displayed for both reward options while Cancel/reopen kept the same IDs, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-nest-note-actual-card-art-5173.png`.

- Marn Supply/Waymark drawer art follow-up: cache drawers that resolve to Supplies or Waymarks now pick and cache the concrete item when the confirmation panel opens, queue that item's runtime art, and award that exact previewed item on Claim. Snack Tin and Chalk Tag now show actual Supply/Waymark art, name, type/rarity, and compact effect text instead of generic reward text.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probes confirmed `supply-*` and `waymark-*` textures were loaded/displayed and Cancel/reopen kept the same item ID, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/marn-cache-supply-confirm-actual-art-5173.png` and `.artifacts/test-results/marn-cache-waymark-confirm-actual-art-5173.png`.

- Marn Chalk Tag consequence follow-up: the `Gain a Waymark / Add a Snag` confirmation now shows both halves of the drawer outcome side by side. The pending reward extracts deterministic Snag cards from `addSnagToDiscard`/`addSnagToDraw`, queues their card art, and the Chalk Tag panel renders the actual Waymark art next to the Bad Directions Snag card face.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed a stable Waymark ID plus `bad_directions` after Cancel/reopen and verified both `waymark-*` and `card-thumb-bad_directions` textures were displayed, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-waymark-snag-confirm-art-5173.png`.

- Marn Supply drawer copy follow-up: the Supply drawer confirmation text now uses the exact cached Supply preview, so the Snack Tin line reads like `Gain Mirror Shard / -2 Cohesion` instead of generic `Choose a Supply / -2 Cohesion` while still showing the matching actual Supply art.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed exact Supply name text plus matching `supply-*` art on the confirmation panel, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-supply-confirm-actual-name-5173.png`.

- Marn reward preview hover follow-up: the Supply drawer preview and Chalk Tag Waymark/Snag split preview now expose the same hover details used elsewhere. Hovering the Supply or Waymark opens the floating item detail with description/effects, while hovering the Snag card opens the standard full card hover detail.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe with real pointer movement over the Supply preview, Waymark preview, and Bad Directions card preview passed; screenshots inspected at `.artifacts/test-results/marn-cache-supply-hover-detail-5173.png`, `.artifacts/test-results/marn-cache-waymark-hover-detail-5173.png`, and `.artifacts/test-results/marn-cache-snag-card-hover-detail-5173.png`. `npm run build` and required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Basin/Signal/Nest cache-format event pass: Basin, Signal, and Nest now reuse the cache-style event structure with the resident on the left, centered choice tiles, and the event centerpiece staged off to the right. Selecting a choice opens a reward drilldown before resolving, with concrete Supply, Waymark, Snag, and card visuals when available plus a compact visual reward board for generic effects.
- Runtime flow: Signal choices still record `signalChoices` and fire signal Waymark triggers from the pending confirmation, Cache still fires cache Waymark triggers, and Nest Preen/Remove confirmations now hand off to the existing card picker instead of resolving immediately.
- Verification: `npx tsc --noEmit`, `npm run build`, focused 5173 Playwright probes for Basin/Supply, Signal/card reward, and Nest/Waymark drilldowns, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-basin-cache-format-menu-5173.png`, `.artifacts/test-results/event-basin-cache-format-confirm-5173.png`, `.artifacts/test-results/event-signal-cache-format-menu-5173.png`, `.artifacts/test-results/event-signal-cache-format-confirm-5173.png`, `.artifacts/test-results/event-nest-cache-format-menu-5173.png`, and `.artifacts/test-results/event-nest-cache-format-confirm-5173.png`.

- Imagegen centerpiece prop pass: generated dedicated transparent prop-style centerpieces for Basin, Signal, and Nest so they now match Marn's cabinet-like physical focal point instead of relying on procedural placeholder boxes. Source PNGs live under `assets/concept-art/route-event-centerpieces/sources/`; alpha runtime WebPs are `assets/runtime/route-events/props/basin-hearth-cart-v1.webp`, `assets/runtime/route-events/props/signal-route-switchboard-v1.webp`, and `assets/runtime/route-events/props/nest-featherwright-bench-v1.webp`.
- Integration: `RouteScene` now queues the matching prop asset for Basin/Signal/Nest route events, renders the generated prop with subtle glow/shadow motion, and preserves the old procedural centerpiece drawings as texture-load fallbacks.
- Verification: `npx tsc --noEmit`, alpha coverage inspection, focused 5173 Playwright prop probes, `npm run build`, `npm run validate:runtime-assets`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-basin-generated-centerpiece-5173.png`, `.artifacts/test-results/event-signal-generated-centerpiece-5173.png`, and `.artifacts/test-results/event-nest-generated-centerpiece-5173.png`.

- Route event outcome-screen removal: removed the redundant post-confirm outcome receipt for Cache, Basin, Signal, and Nest route events. Confirming or claiming the reward now completes the route node, clears reward/card hover state, and returns directly to route selection; card-pick paths still complete through the existing picker flow.
- Cleanup: deleted the unused `routeEventResolution` render state plus the snapshot/delta outcome helpers so the extra screen cannot reappear through stale code.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright click-through for Cache/Basin/Signal/Nest no-outcome completion, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-no-outcome-5173.png`, `.artifacts/test-results/event-basin-no-outcome-5173.png`, `.artifacts/test-results/event-signal-no-outcome-5173.png`, and `.artifacts/test-results/event-nest-no-outcome-5173.png`.

- Route event static centerpiece pass: removed the breathing/float tween from generated event prop centerpieces and made the cache glint, basin hearth/steam, signal jack lights, and nest lamp fallback elements static as well. Cache, Basin, Signal, and Nest centerpieces now read as planted physical props instead of animated UI ornaments.
- Verification: `npx tsc --noEmit`, focused 5173 event-menu captures with a delay for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-static-centerpiece-5173.png`, `.artifacts/test-results/event-basin-static-centerpiece-5173.png`, `.artifacts/test-results/event-signal-static-centerpiece-5173.png`, and `.artifacts/test-results/event-nest-static-centerpiece-5173.png`.

- Route event centerpiece text-box removal: removed the descriptive plaque box under route-event centerpieces so Cache, Basin, Signal, and Nest rely on the prop art and choice list instead of an extra explanatory panel.
- Verification: `npx tsc --noEmit`, focused 5173 menu captures for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-no-centerpiece-textbox-5173.png`, `.artifacts/test-results/event-basin-no-centerpiece-textbox-5173.png`, `.artifacts/test-results/event-signal-no-centerpiece-textbox-5173.png`, and `.artifacts/test-results/event-nest-no-centerpiece-textbox-5173.png`.

- Unified HUD tightening: reduced the shared route/combat HUD frame height from 76px to 62px, moved the lower rail up, and shortened the vertical divider so the bottom padding no longer reads as wasted space while preserving the existing icon and chip sizes.
- Verification: `npx tsc --noEmit`, focused 5173 route/event/combat HUD captures, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/hud-thinner-route-5173.png`, `.artifacts/test-results/hud-thinner-event-5173.png`, and `.artifacts/test-results/hud-thinner-combat-5173.png`.

- Route event selection icon scale pass: enlarged the choice-row icons in Cache, Basin, Signal, and Nest select screens from 17-18px to 25px so rewards/costs read more clearly without changing row height or text positions.
- Verification: `npx tsc --noEmit`, focused 5173 menu captures for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-larger-select-icons-5173.png`, `.artifacts/test-results/event-basin-larger-select-icons-5173.png`, `.artifacts/test-results/event-signal-larger-select-icons-5173.png`, and `.artifacts/test-results/event-nest-larger-select-icons-5173.png`.

- Sella Warmwick centerpiece cleanup: the Basin/Sella generated hearth-cart centerpiece now opts out of the shared prop glow circle and accent underline, leaving only the art and its grounded shadow while Signal and Nest keep their existing prop framing.
- Verification: `npx tsc --noEmit`, focused 5173 Basin event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-basin-sella-centerpiece-no-circle-line-5173.png`.

- Oren Shearbright centerpiece cleanup: the Nest/Oren generated workbench centerpiece now opts out of the shared prop glow circle and accent underline, and Oren's resident art no longer renders with the extra glow circle behind them.
- Verification: `npx tsc --noEmit`, focused 5173 Nest event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-nest-oren-centerpiece-no-circles-line-5173.png`.

- Marn Valeclip resident cleanup: the Cache/Marn event now renders Marn's resident art without the extra circular glow behind him, while leaving the lockbox cabinet and choice layout unchanged.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-no-resident-circle-5173.png`.

- Sella Warmwick placement cleanup: the Basin/Sella event now renders Sella without the resident glow circle, and the generated hearth-cart centerpiece is larger and lower so it sits visually grounded just above the bottom screen rail.
- Verification: `npx tsc --noEmit`, focused 5173 Basin event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-basin-sella-larger-lower-no-resident-circle-5173.png`.

- Ivo Tallymast Signal placement cleanup: the Signal/Ivo event now renders Ivo without the resident glow circle, removes the shared prop glow circle and underline from the generated switchboard, and lowers the switchboard so it sits near the bottom screen rail.
- Verification: `npx tsc --noEmit`, focused 5173 Signal event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-signal-ivo-lower-no-circles-line-5173.png`.

- Oren Shearbright workbench placement cleanup: lowered the Nest/Oren generated featherwright workbench so the desk sits visually near the bottom screen rail while preserving the existing no-circle/no-underline treatment.
- Verification: `npx tsc --noEmit`, focused 5173 Nest event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-nest-oren-workbench-lower-5173.png`.

- Ivo Tallymast Signal alignment pass: removed the shared transparent bottom backdrop band from the Signal event, moved Ivo's title plaque to Sella's vertical level, and enlarged the generated switchboard while keeping it grounded near the bottom rail.
- Verification: `npx tsc --noEmit`, focused 5173 Signal event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-signal-align-title-bigger-no-bottom-band-5173.png`.

- Marn Valeclip Cache title alignment pass: moved Marn's title plaque down to the same vertical level used by Sella's event so the named resident header is consistent across the set-piece events.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-title-sella-level-5173.png`.

- Marn Valeclip Cache selection padding pass: moved the drawer selection rows down inside the existing border panel so the first choice no longer crowds the panel's top edge while the cabinet, resident, and header framing stay anchored.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-selection-padding-5173.png`.

- Route reward menu icon scale pass: enlarged the effect-token tiles inside the drilled-in route reward menu and switched scrap reward/cost tokens to the same HUD scrap-gear icon so the Cache scrap drawer reads consistently after clicking into it.
- Verification: `npx tsc --noEmit`, focused 5173 Cache reward-menu capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-reward-menu-large-hud-scrap-icon-5173.png`.

- Marn Cache reward spacing fix: increased the cache drawer row height to fit the large drawer icons without crossing neighboring borders, and gave the clicked-in reward token rows more vertical room with centered labels so Scrap and Shelter Key no longer crowd their tile borders.
- Verification: `npx tsc --noEmit`, focused 5173 captures for the outer Cache menu plus the Scrap, Shelter Key, and Leave states, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-marn-reward-menu-spacing-outer-5173.png`, `.artifacts/test-results/event-cache-marn-reward-menu-spacing-scrap-5173.png`, `.artifacts/test-results/event-cache-marn-reward-menu-spacing-shelter-key-5173.png`, and `.artifacts/test-results/event-cache-marn-reward-menu-spacing-leave-5173.png`.

- Route event icon audit follow-up: Basin/Nest/Signal event rows and compact effect chips now use the same HUD `scrap-gear` UI icon for scrap costs/rewards instead of the older supply-art scrap helper. This keeps scrap visually sized through the same `addUiIconImage` path as flock-heart, cover-shield, supply-pouch, waymark-compass, preen-kit, release-card, and deck-stack.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Fresh 5173 screenshots inspected for Basin outer/reward and Nest reward at `.artifacts/test-results/icon-audit-basin-outer-5173.png`, `.artifacts/test-results/icon-audit-basin-refill-reward-5173.png`, and `.artifacts/test-results/icon-audit-nest-preen-reward-5173.png`; a later clean outer recapture retry hit a transient Windows browser launch access error after the successful captures.

- Rain Pipe reward-token overlap fix: reduced clicked-in route reward token icons from the oversized 22px UI-icon input to 18px, moved the icon slightly right, and increased label padding so single-effect choices like Basin's `Drink at the Rain Pipe` no longer let the flock-heart icon ride the token border or crowd the text.
- Verification: reproduced the issue at `.artifacts/test-results/rain-pipe-reward-before-5173.png`, then verified the fix at `.artifacts/test-results/rain-pipe-reward-after-5173.png`; spot-checked the two-row Nest Preen reward at `.artifacts/test-results/nest-preen-reward-icons-after-5173.png`. `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Sella reward-token rail spacing fix: moved the clicked-in route reward token stack lower inside the reward preview panel so the panel's top accent rail no longer overlaps the top edge of the token boxes. This specifically fixes Sella's `Drink at the Rain Pipe`, `Hide Under the Awning`, and `Keep moving` reward-selection screens.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Fresh 5173 screenshots inspected at `.artifacts/test-results/sella-reward-rail-fix-5173/drink-rain-pipe.png`, `.artifacts/test-results/sella-reward-rail-fix-5173/hide-under-awning.png`, and `.artifacts/test-results/sella-reward-rail-fix-5173/keep-moving.png`.

- Oren/Nest large-deck Preen picker fix: the shared route/market card picker now renders card choices in a two-row viewport with row scrolling instead of drawing a third row below the event panel. Wheel input scrolls the picker while it is open, picker state resets on open/close, and large decks show a compact visible range indicator with up/down icon controls.
- Verification: `npx tsc --noEmit`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks" --workers=1`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Final focused screenshot inspected at `.artifacts/test-results/oren-preen-large-picker-5173-final.png`; it showed 10 card hit areas with the lowest at 556.5px, inside the panel.

- Oren/Nest Preen picker art follow-up: the shared card picker now queues runtime compact card art for the visible picker entries and redraws when the thumbnails load, so large-deck Preen/Release choices show actual card faces instead of text-only fallback panels.
- Verification: `npx tsc --noEmit`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks" --workers=1`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Final focused screenshot inspected at `.artifacts/test-results/oren-preen-large-picker-art-5173-final.png`; it showed 10 visible card-art images and 10 picker hit areas.

- Oren Boss Ready Up multi-preen fix: route card picker state now carries the requested Preen/Remove count from effects like `preenCard(2)`. Boss Ready Up opens as `Preen 2 Cards`, remains open after the first chosen Preen, and only completes the Nest node after the second chosen Preen or when no eligible cards remain. The non-interactive boss prep effects still apply once before the picker opens.
- Verification: `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts -g "nest boss ready up|route preen picker keeps large decks|nest remove" --workers=1`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Focused screenshot inspected at `.artifacts/test-results/oren-boss-ready-two-preens-5173.png`, showing `Preen 2 Cards` with card art, 869 Scrap, 35/38 HP, and the Boss Ready Up picker still open for two choices.

- New user prompt: make the route map icons larger, especially the `Take this route` icon.
- Implemented first pass: enlarged route map node icons/rings and boss icon sizing constants, and increased the route-confirm pin plus its hit area and hover scale.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:4175/`, focused route-map Playwright screenshot, `npx playwright test tests/smoke.spec.ts -g "route map loads|generated route map node footprints" --workers=1`, and `npm run validate:runtime` passed. Screenshot inspected at `.artifacts/test-results/route-map-larger-icons-selected.png`.

- New user prompt: review optimization opportunities because the Market is loading slowly.
- Market load optimization: removed full deck portrait preloading from RouteScene and split card-art queues so Market shelves only load offer thumbnails, while the Deck overlay and card picker continue loading their own visible/list thumbnails on demand. Profiling a 36-card deck showed RouteScene WebP requests drop from 64 files / 28.2 MB to 28 files / 4.5 MB, and Market-open WebP requests drop from 46 files / 4.2 MB to 10 files / 0.7 MB. Full card portrait detail remains available on detail surfaces and now loads when the player opens a card detail/hover. Follow-up candidate: create a small HUD/icon scrap asset so the 616 KB `scrap.webp` is not needed for tiny price tags.
- Verification: `npm run build`, `npm run validate`, `npm test`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock|late markets sell" --workers=1`, required `develop-web-game` client on `http://127.0.0.1:4181/`, direct market profile screenshots, and deck-overlay thumbnail screenshot passed. Screenshots/profile JSON: `.artifacts/test-results/market-profile-before.*`, `.artifacts/test-results/market-profile-after-final.*`, and `.artifacts/test-results/deck-overlay-thumbs-after-market-opt.png`.

- Follow-up user prompt: Market still appears slow and items overlap.
- Market second pass: batched Market kit/card/Waymark/utility art into one shelf queue started in `openMarketNode`, reducing repeated loader-complete redraws. Reflowed the Market shelves so card price tags and card hit areas stay above the Waymark/Supply row, with lower labels still visible. Remaining performance candidate: create smaller HUD/shop versions of large Supply/Waymark icons; cold Market loads can still include 120-210 KB item icon WebPs.
- Verification: `npm run build`, `npm run validate`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock|late markets sell|market preen service|market remove service|market overlay renders" --workers=1`, required `develop-web-game` client on `http://127.0.0.1:4182/`, and final Market screenshot inspection passed. Screenshot: `.artifacts/test-results/market-final-layout.png`.

- Follow-up user prompt: market cards and items need to be moved up a bit, they are too close to the bottom.
- Market shelf spacing pass: lifted the card shelf plus Waymark/Supply goods row by 34px through a shared `shelfLift` value in `renderMarketOverlay`, preserving the service column and close button positions while giving the bottom labels more clearance.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:4174/`, and focused built-preview Market screenshot inspection passed. Screenshot: `.artifacts/test-results/market-spacing-built-preview.png`.

- Deep stability audit follow-up: ran the full static/data/browser audit, cleaned unused TypeScript/UI helper leftovers from `src/main.ts`, and updated smoke tests to match current authored route-plan, Supply repeat, and enemy Cover semantics. Fixed a real route sequencing regression where no-effect special-node decline choices opened a reward drilldown instead of completing immediately. Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate`, `npm test`, `npm run audit:content`, `npm run audit:balance`, `npm run audit:economy`, full `npx playwright test --reporter=line` (88/88), production build via Playwright webServer, required `develop-web-game` client on `http://127.0.0.1:4177/`, and focused route screenshot/state probe passed. Screenshots inspected at `output/web-game/shot-2.png` and `.artifacts/test-results/deep-audit-route-smoke.png`.

- Procedural audio and mute-control polish: generated a transparent runtime audio-toggle medallion from a built-in imagegen chroma-key pass, optimized it to a 320px PNG, and wired it through the shared UI icon loader. Added a persistent WebAudio director with menu/route/battle/boss/victory/defeat ambience, procedural UI/combat/reward cues, localStorage-backed mute state, and an audio state hook for test captures. Integrated the toggle and `M` hotkey across Menu, Profile, Codex, Route, and Battle scenes, with cue coverage for route commits, card plays, damage, block, healing, turn end, rewards, victory, and defeat.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, and `npm run build` passed. The required `develop-web-game` client passed on `http://127.0.0.1:5179/`; focused Playwright captures verified menu, route, battle, mute toggling, and mobile framing with no console errors. Screenshots inspected at `output/web-game/shot-0.png` and `.artifacts/test-results/audio-polish/contact-sheet.png`, with source captures in `.artifacts/test-results/audio-polish/`.

- Run outcome screen polish: generated a transparent brass/cyan rooftop outcome crest with built-in imagegen on a flat chroma-key background, removed the key locally, optimized it to a 384px runtime PNG, and wired it into the shared UI icon loader. Reworked the win/loss summary into a wider structured report with a crest column, alternating stat rows, hover/audio button affordances, and one-shot victory/defeat reveal FX using the existing combat atlas, pulse ring, and particle burst helpers. Adjusted the final panel width after visual testing so the outcome screen stays inside the current narrow-viewport stage slice.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5179/` passed. Focused Playwright forced victory and defeat outcome states at 2560x1600 plus a 390x844 narrow capture, confirmed `ui-icon-run-outcome-crest` loaded, FX children were present, and no console errors occurred. Screenshots inspected at `.artifacts/test-results/outcome-polish/contact-sheet.png`; source captures and `state-summary.json` are in `.artifacts/test-results/outcome-polish/`.

- Pause/settings affordance polish: generated a transparent brass/cyan pause medallion with built-in imagegen on a chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Added HUD pause controls and `P` hotkey handling to Route and Battle, plus a shared modal pause menu with Resume, Main Menu, embedded audio toggle, dimmed background, stat summary, and Enter/ESC guards so paused route/combat states do not accidentally advance.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "pause overlay opens" --workers=1 --reporter=line`, and the required `develop-web-game` client on `http://127.0.0.1:5179/` passed. Focused Playwright captures verified route, combat, and 390x844 narrow pause screens with `ui-icon-pause-medallion` loaded and no console errors; screenshots and `state-summary.json` are in `.artifacts/test-results/pause-polish/`.

- Title and pause Settings polish: generated a transparent brass/cyan settings medallion with built-in imagegen on a chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Added a Settings utility button to the title screen, a shared Settings modal with audio/fullscreen controls and status rows, and Settings entry points from Route/Battle pause menus. Route and Battle now treat Settings as a blocking overlay for Enter/Esc, and Battle no longer redundantly preloads globally bootstrapped UI icons.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "settings overlay opens|pause overlay opens" --workers=1 --reporter=line`, and the required `develop-web-game` client on `http://127.0.0.1:5179/` passed. Built-preview captures at 2560x1600 plus 390x844 confirmed `ui-icon-settings-medallion` loaded, title/route/battle Settings text states were open, and no console errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/settings-polish/`, with an additional live direct desktop capture at `.artifacts/test-results/settings-polish/menu-settings-live-direct-2560x1600.png`.

- Flock Record progression polish: generated a transparent brass/cyan record medallion with built-in imagegen on a flat chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Reworked Profile/Flock Record into a framed progression dashboard with lifetime stat chips, leader/achievement/Codex progress rails, leader unlock rows, achievement badge rows, audio/close controls, and a cleaner `Return` action. The profile text-state now exposes stats and progression counts for automated checks.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders" --workers=1 --reporter=line`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Live screenshot inspected at `output/web-game/shot-0.png`; built-preview captures at 2560x1600 and 390x844 are in `.artifacts/test-results/profile-polish/` with `state-summary.json` and no console/page errors.

- Title How to Play onboarding polish: generated a transparent brass/cyan help medallion with built-in imagegen on a flat chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Added a title-screen How to Play utility button, `H` hotkey, shared modal guide overlay with route/combat/flock/progression guidance, and menu text-state `helpOpen` for automated checks.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "settings overlay opens|How to Play overlay" --workers=1 --reporter=line`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-help-medallion` loaded, the How to Play overlay was open, and no page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/help-polish/`.

- Market supply shelf optimization polish: generated a transparent brass/cyan market supply-crate medallion with built-in imagegen on a flat chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Market supply shelf tiles now render the lightweight `ui-icon-market-supply-crate` instead of queueing per-supply full icon WebPs, while item-specific supply art remains available for Codex/reward/detail surfaces.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock|market overlay renders" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Built-preview captures at 2560x1600 and 390x844 confirmed three crate shelf icons rendered, the Market shelf asset queue contains `ui-icon-market-supply-crate` and no `supply-*` shelf assets, and no page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/market-supply-crate/`.

- Run-complete unlock celebration polish: generated a transparent brass/cyan unlock badge medallion with built-in imagegen on a flat chroma-key background, removed the key locally, optimized it to a 320px runtime PNG, and wired it into the shared UI icon loader. Fresh meta unlocks on the run-complete screen now render as a badge-backed `NEW UNLOCKS` strip with leader/badge counts, summarized names, text-state `unlockHighlights`, and an extra reveal pulse/particle burst.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "run-complete outcome celebrates" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-unlock-badge` rendered, new unlocks included The Talon plus first-win badges, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/unlock-outcome/`.

- Market icon-system polish: generated transparent brass/cyan Market Waymark, Market Preen service, and Market Remove service badges with built-in imagegen on flat chroma-key backgrounds, removed the key locally, optimized each to 320px runtime PNGs, and wired them into the shared UI icon loader. Market Waymark shelf tiles now render `ui-icon-market-waymark-badge` instead of queueing per-Waymark full art, while Preen/Remove service buttons use dedicated market medallions instead of the generic `preen-kit`/`release-card` icons. Item-specific Waymark art remains available on detail/Codex surfaces.
- Verification: alpha validation passed with transparent corners and healthy subject coverage for `market-waymark-badge.png`, `market-preen-service.png`, and `market-remove-service.png`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Built-preview captures at 2560x1600 and 390x844 confirmed the Market service icons, three Waymark badges, and three Supply crates rendered with no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/market-service-icons/`.

- Market full-service icon completion: generated transparent brass/cyan Market Refresh, Boss Rigging, and Route Plan service medallions with built-in imagegen on flat chroma-key backgrounds, removed the key locally, and optimized each to a 320px runtime PNG. The Market service column now uses the full bespoke icon family for Preen, Remove, Boss Rigging, Route Plan, and Refresh. Late-map Boss Rigging and Route Plan listings now render as service buttons instead of falling through the Supply/goods-row path with no art.
- Verification: alpha validation passed with transparent corners and healthy subject coverage for `market-refresh-service.png`, `market-boss-rigging-service.png`, and `market-route-plan-service.png`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "late markets sell|market overlay renders" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/` passed. Built-preview captures at 2560x1600 and 390x844 confirmed early markets render Preen/Remove/Refresh plus shelf badges, late markets render Boss Rigging and Route Plan as service icons, and there were no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/market-full-service-icons/`.

- Market scrap price icon slimming: compact Market prices and non-card detail panels now use the already bootstrapped `ui-icon-scrap-gear` path instead of the large `resource-scrap` supply art helper. This keeps card price tags, Waymark/Supply labels, service prices, refresh hover details, and other small Market scrap costs visually consistent while avoiding the 616 KB scrap WebP as a tiny UI token.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "late markets sell|market overlay renders" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-scrap-gear` rendered 14-16 times across early/late Market states, `resource-scrap` rendered 0 times in the Market overlay, hover detail price art used `ui-icon-scrap-gear`, and no console/page errors occurred; screenshots plus `state-summary.json` and `hover-detail-summary.json` are in `.artifacts/test-results/market-scrap-gear-price-icons/`.

- Route commit medallion polish: generated a transparent brass/cyan/magenta route-commit medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/route-commit-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/route-commit-medallion.png`, and wired the RouteScene `Take this route` confirm affordance plus shared button-label mapping to `ui-icon-route-commit-medallion` while leaving generic route pins unchanged.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~49.6% subject coverage, and a 152 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route map loads|generated route map node footprints" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-route-commit-medallion` loaded and rendered once for a selectable route with no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/route-commit-medallion/`.

- Title Codex medallion polish: generated a transparent brass/cyan/magenta Codex field-guide medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-medallion.png`, and wired the title Codex utility button through `ui-icon-codex-medallion` so the top utility row now has a complete bespoke icon family.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~49.7% subject coverage, and a 149 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-codex-medallion` loaded/rendered on the title screen with no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-medallion/`.

- Title mobile viewport fit fix: lowered the Phaser FIT scale floor from 640x360 to 320x180 so portrait phone-width browsers are no longer forced to render a clipped 640px canvas inside `overflow: hidden`. Added a smoke regression that boots a 390x844 viewport, asserts the canvas fits within the viewport, and verifies all four title utility medallions render.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4195/?playwright=1` verified 2560x1600 desktop canvas centering and a fully visible 390x844 mobile canvas with Help, Settings, Codex, and Flock Record medallions rendered; screenshots and `state-summary.json` are in `.artifacts/test-results/title-mobile-fit/`.

- Title primary Start Run polish: generated a transparent brass/cyan/magenta route-launch medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/start-run-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/start-run-medallion.png`, and wired `Start Run`, `Start New Run`, and `Continue Run` menu buttons through `ui-icon-start-run-medallion`.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~46.3% subject coverage, and a 137 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4196/?playwright=1` at 2560x1600 and 390x844 confirmed `ui-icon-start-run-medallion` rendered on the primary title action, all title utility medallions still rendered, the canvas stayed inside the viewport, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/start-run-medallion/`.

- Title Ascension progression polish: generated a transparent brass/cyan/magenta Ascension tier medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/ascension-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/ascension-medallion.png`, and placed it in the title Ascension panel so the difficulty/progression selector now shares the same premium medallion language as the rest of the first screen.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~46.6% subject coverage, and a 136 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4197/?playwright=1` at 2560x1600 and 390x844 confirmed `ui-icon-ascension-medallion` rendered left of the Ascension label, all title medallions still rendered, the canvas stayed inside the viewport, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/ascension-medallion/`.

- Title leader-selection header polish: generated a transparent brass/cyan/magenta leader roster medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/leader-select-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/leader-select-medallion.png`, and placed it beside the `Choose your Flock Leader` title header so the leader picker now shares the same premium medallion language as the rest of the first screen.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~46.7% subject coverage, and a 139 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4198/?playwright=1` at 2560x1600 and 390x844 confirmed `ui-icon-leader-select-medallion` rendered beside the Flock Leader header, all title medallions still rendered, the canvas stayed inside the viewport, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/leader-select-medallion/`.
- Title medallion regression coverage follow-up: expanded the portrait title-screen smoke test to include `ui-icon-audio-toggle-medallion`, so the top-left mute control is now covered alongside Help, Settings, Codex, Flock Record, Start Run, Ascension, and leader-selection medallions.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` and focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line` passed after the coverage update.

- Title locked-leader card polish: generated a transparent brass/cyan/magenta locked-leader medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/leader-lock-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/leader-lock-medallion.png`, and wired locked title leader panels to show the medallion beside the unlock hint instead of repeating a plain `Locked` text line.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~42.5% subject coverage, and a 126 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4199/?playwright=1` at 2560x1600 and 390x844 confirmed `ui-icon-leader-lock-medallion` loaded/rendered three times for locked leaders, all other title medallions still rendered, the canvas stayed inside the viewport, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/leader-lock-medallion/`.

- Title selected-leader badge polish: generated a transparent brass/cyan/magenta rally-ready medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/leader-ready-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/leader-ready-medallion.png`, and wired unlocked title leader panels so exactly one ready badge appears on the currently selected leader and moves when the player selects a different unlocked leader.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~42.4% subject coverage, and a 128 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport|selecting a Flock Leader" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4200/?playwright=1` at 2560x1600 and 390x844 confirmed `ui-icon-leader-ready-medallion` loaded/rendered, exactly one ready badge was visible, the default badge started on Fledgling, selecting Spark-Caller moved it there, all other title medallions still rendered, the canvas stayed inside the viewport, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/leader-ready-medallion/`.

- Flock Record achievement badge polish: generated a transparent brass/cyan/magenta achievement medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/achievement-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/achievement-medallion.png`, and wired Profile/Flock Record achievement rows to render earned badges brightly and locked badges dimmed through the shared UI icon loader.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~38.8% subject coverage, and a 114 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders" --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-achievement-medallion` loaded, all 8 achievement rows rendered medallions, the earned badge alpha was 0.95 while locked badges were dimmed, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/profile-achievement-medallion/`.

- Flock Record leader-row medallion polish: generated a transparent brass/cyan/magenta leader record medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/leader-record-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/leader-record-medallion.png`, and replaced the Profile/Flock Record leader-row `*` / `-` text markers with shared-loader medallions. Unlocked leader rows render bright; locked leader rows render dimmed and tinted.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~36.1% subject coverage, and a 103 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "locked Flock Leaders" --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-leader-record-medallion` loaded, all 5 leader rows rendered medallions, unlocked leader alphas were 0.9 while locked leader alphas were 0.28, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/profile-leader-record-medallion/`. The live client profile screenshots are in `output/web-game/shot-0.png` and `output/web-game/shot-1.png`.

- Codex locked-card medallion polish: generated a transparent brass/cyan/magenta sealed-card discovery medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-locked-card-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-locked-card-medallion.png`, and replaced the Codex locked-card `?` fallback marker with a dimmed shared-loader medallion. The `?` remains only as a defensive fallback if the texture is unavailable.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~38.0% subject coverage, and a 102 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 confirmed `ui-icon-codex-locked-card-medallion` loaded/rendered 20 times in the visible locked-card grid, all rendered at alpha 0.42, no fallback question marks rendered, and no console/page errors occurred; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-locked-card-medallion/`.

- Codex enemy tab medallion polish: generated a transparent brass/cyan/magenta enemy-dossier medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-enemy-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-enemy-medallion.png`, and wired the Codex Enemies tab plus enemy-related Codex glossary categories through `ui-icon-codex-enemy-medallion` instead of the generic attack-card icon.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~54.7% subject coverage, and a 147 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "enemy codex" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex click path, confirmed `ui-icon-codex-enemy-medallion` loaded/rendered once on the active Enemies tab at alpha 0.96, and recorded no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-enemy-medallion/`.

- Codex glossary tab medallion polish: generated a transparent brass/cyan/magenta open-field-guide glossary medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-glossary-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-glossary-medallion.png`, and wired the Codex Glossary tab through `ui-icon-codex-glossary-medallion` instead of the generic deck-stack icon.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~54.7% subject coverage, and a 143 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: glossary" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex click path, confirmed `ui-icon-codex-glossary-medallion` loaded/rendered once on the active Glossary tab at alpha 0.96, and recorded no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-glossary-medallion/`.

- Codex items tab medallion polish: generated a transparent brass/cyan/magenta field-kit catalog medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-items-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-items-medallion.png`, and wired the Codex Items tab through `ui-icon-codex-items-medallion` instead of the generic waymark compass.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~54.8% subject coverage, and a 153 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex click path, confirmed `ui-icon-codex-items-medallion` loaded/rendered on the active Items tab at alpha 0.96, and recorded no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-items-medallion/`.

- Codex cards tab medallion polish: generated a transparent brass/cyan/magenta collectible-card medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-cards-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-cards-medallion.png`, and wired Cards/Major/Aviary Codex tabs through `ui-icon-codex-cards-medallion` instead of the generic deck-stack icon.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~59.2% subject coverage, and a 174 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: starting a run" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex click path, confirmed `ui-icon-codex-cards-medallion` loaded/rendered on the active Cards tab plus card category controls, and recorded no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-cards-medallion/`.

- Codex leaders tab medallion polish: generated a transparent brass/cyan/magenta flock-leader dossier medallion with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/codex-leader-medallion-source-v1.png`, removed the key locally, optimized a 320px runtime PNG at `assets/runtime/ui/icons/codex-leader-medallion.png`, and wired the Codex Leaders tab through `ui-icon-codex-leader-medallion` instead of the generic heart icon.
- Verification: alpha validation passed with transparent corners, 320x320 output, ~59.2% subject coverage, and a 176 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "codex: leaders" --workers=1 --reporter=line`, `npm run validate:runtime`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5181/?playwright=1` passed. Built-preview captures at 2560x1600 and 390x844 used the real title-to-Codex click path, confirmed `ui-icon-codex-leader-medallion` loaded/rendered on the active Leaders tab at alpha 0.96, and recorded no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/codex-leader-medallion/`.

- Combat incoming forecast HUD polish: generated a transparent brass/cyan/red incoming-pressure forecast frame with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/combat-incoming-forecast-frame-source-v1.png`, removed the key locally, optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/combat-incoming-forecast-frame.png`, and wired BattleScene's live incoming damage preview into the unified HUD. The Cohesion bar now shows a red predicted loss segment plus a generated framed readout with blocked damage and after-hit Cohesion, while text-state telemetry exposes `combatIncomingForecastFrame`.
- Verification: alpha validation passed with transparent corners, 512x144 output, ~36.2% subject coverage, and a 69 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "flock Cohesion bar exposes incoming damage prediction" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4217/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-incoming-forecast-frame` rendered once with no console/page errors; screenshots and `state-summary.json` are in `.artifacts/test-results/combat-incoming-forecast/`.

- Combat Flock Stats tally polish: generated a transparent brass/cyan/magenta vertical combat tally frame with built-in imagegen on a flat chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/combat-stats-tally-frame-source-v1.png`, removed the key locally, optimized a 384x360 runtime PNG at `assets/runtime/ui/icons/combat-stats-tally-frame.png`, and wired BattleScene's Flock Stats `This Combat` column through `ui-icon-combat-stats-tally-frame`. The first visual pass caught the frame overlapping stat labels, so the final placement now frames the numeric value column at 286x338 with a subtle additive glint while labels remain unobstructed. Text-state telemetry exposes `combatStatsTallyFrame`.
- Verification: alpha validation passed with transparent corners, 384x360 output, and a 113 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "flock stats overlays render generated formation flourish art" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4217/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-stats-tally-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 2 }`; screenshots and `state-summary.json` are in `.artifacts/test-results/combat-stats-tally-frame/`.

- Reward skip command polish: generated a transparent brass/cyan/magenta horizontal skip-reward command frame with built-in imagegen on a flat green chroma-key background, copied the raw source to `assets/concept-art/ui/icons/sources/reward-skip-command-frame-source-v1.png`, removed the key locally, cropped transparent padding, cleaned small disconnected alpha specks, and optimized a 640x185 runtime PNG at `assets/runtime/ui/icons/reward-skip-command-frame.png`. BattleScene's card reward screen now uses `ui-icon-reward-skip-command-frame` behind the `Skip + Scrap` action, keeps the same hit area and fallback rectangle, and adds a reduced-motion-safe glint layer. Text-state telemetry exposes `rewardSkipCommandFrame`.
- Verification: alpha validation passed with transparent corners, 640x185 output, ~80.9% visible coverage, and a 175 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "post-combat reward screens render generated choice ceremony art" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4218/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-reward-skip-command-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 2 }`; screenshots and `state-summary.json` are in `.artifacts/test-results/reward-skip-command-frame/`.

- Combat pile review dossier polish: generated a transparent brass/gunmetal/cyan/magenta selected-card review frame with built-in imagegen on a flat green chroma-key background, copied the chosen source to `assets/concept-art/ui/icons/sources/combat-pile-review-frame-source-v1.png`, removed the key locally, and optimized a 768x532 runtime PNG at `assets/runtime/ui/icons/combat-pile-review-frame.png`. BattleScene's deck/draw/discard card review detail panel now layers `ui-icon-combat-pile-review-frame` with a subtle glint behind the selected card art and text while preserving the existing field panel, close control, card list, and fallback frame. Text-state telemetry exposes `combatPileReviewFrame`.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 768x532 output, zero green-ish visible pixels, and a 295 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4219/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-review-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 2 }`, no console/page errors, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-review-frame/`.

- Combat pile row-frame polish: generated a compact transparent brass/gunmetal/cyan/magenta list-row frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-row-frame-source-v1.png`, removed the key locally, and optimized a 640x128 runtime PNG at `assets/runtime/ui/icons/combat-pile-row-frame.png`. BattleScene's deck/draw/discard card review list now renders `ui-icon-combat-pile-row-frame` behind each visible row while preserving the existing hit targets, cost bubbles, zone icons, and compact text. Text-state telemetry exposes `combatPileRowFrame`.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 640x128 output, zero green-ish visible pixels, and an 88 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4220/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-row-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 5 }`, no console/page errors, and readable row labels/costs; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-row-frame/`.

- Combat pile scroll command polish: generated a compact transparent brass/gunmetal/cyan/magenta scroll-button frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-scroll-button-frame-source-v1.png`, removed the key locally, and optimized a 320x240 runtime PNG at `assets/runtime/ui/icons/combat-pile-scroll-button-frame.png`. BattleScene's deck/draw/discard card review up/down scroll controls now render `ui-icon-combat-pile-scroll-button-frame` behind the existing chevrons while preserving disabled/enabled alpha, hit targets, and fallback rectangles. Text-state telemetry exposes `combatPileScrollButtonFrame`, and the card-pile smoke test now seeds a scrollable draw pile so the review frame, row frames, and both scroll-button frames are covered together.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 320x240 output, zero green-ish visible pixels, and a 55 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4221/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-scroll-button-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 2 }`, no console/page errors, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-scroll-button-frame/`.

- Combat pile numeric badge polish: generated a transparent brass/gunmetal/cyan/magenta circular count-badge ring with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-count-badge-source-v1.png`, removed the key locally, and optimized a 256x256 runtime PNG at `assets/runtime/ui/icons/combat-pile-count-badge.png`. BattleScene's deck/draw/discard card review overlay now layers `ui-icon-combat-pile-count-badge` over the three pile-count bubbles and each visible row cost bubble, preserving the existing colored fills and numeric labels while making the repeated counters match the generated dossier/row/scroll-frame family. Text-state telemetry exposes `combatPileCountBadge`, and the focused smoke now asserts 14 rendered badges for the seeded scrollable draw pile.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 256x256 output, zero green-ish visible pixels, and a 71 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4222/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-count-badge` rendered with telemetry `{ loaded: true, rendered: true, count: 14 }`, no console/page errors, legible numbers, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-count-badge/`.

- Combat pile detail-chip polish: generated a transparent brass/gunmetal/cyan/magenta horizontal chip frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-detail-chip-frame-source-v1.png`, removed the key locally, and optimized a 512x128 runtime PNG at `assets/runtime/ui/icons/combat-pile-detail-chip-frame.png`. BattleScene's deck/draw/discard selected-card detail panel now layers `ui-icon-combat-pile-detail-chip-frame` behind the target and role chips, preserving the existing dark fill, icons, and text while aligning the small tags with the generated dossier, row, scroll, and count-badge family. Text-state telemetry exposes `combatPileDetailChipFrame`, and the focused smoke now asserts both chip frames render.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 512x128 output, zero green-ish visible pixels, and a 54 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4223/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-detail-chip-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 2 }`, no console/page errors, readable chip labels, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-detail-chip-frame/`.

- Combat pile stat-chip polish: generated a transparent brass/gunmetal/cyan/magenta horizontal stat bonus chip frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-stat-chip-frame-source-v1.png`, removed the key locally, cropped transparent padding, and optimized a 320x160 runtime PNG at `assets/runtime/ui/icons/combat-pile-stat-chip-frame.png`. BattleScene's deck/draw/discard selected-card detail panel now layers `ui-icon-combat-pile-stat-chip-frame` behind each small flock-bonus stat readout, preserving the existing dark fill, stat icon, and `+N` label while bringing the last plain stat chip into the generated combat-pile UI family. Text-state telemetry exposes `combatPileStatChipFrame`, and the focused smoke now asserts at least one stat-chip frame renders in the seeded draw-pile inspector.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 320x160 output, zero green-ish visible pixels, and a 36 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-stat-chip-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 1 }`, no page errors, readable stat label, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-stat-chip-frame/`.

- Overlay close command polish: generated a transparent brass/gunmetal/cyan/magenta/red close-command frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/overlay-close-command-frame-source-v1.png`, removed the key locally, cropped transparent padding, and optimized a 512x192 runtime PNG at `assets/runtime/ui/icons/overlay-close-command-frame.png`. The shared Phaser `renderCloseControl` helper now layers `ui-icon-overlay-close-command-frame` behind the existing close medallion and label, giving deck/draw/discard inspector dismiss controls and other shared close pills the same generated command-frame language as the surrounding panels while preserving hit areas and text. Text-state telemetry exposes `overlayCloseCommandFrame`, and the focused combat-pile smoke now asserts one close frame renders in the seeded draw-pile inspector.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 512x192 output, zero green-ish visible pixels, and a 68 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-overlay-close-command-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 1 }`, no page errors, readable Close label, and no text overlap; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/overlay-close-command-frame/`.

- Combat pile title-plaque polish: generated a transparent brass/gunmetal/cyan/magenta title plaque with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-title-plaque-source-v1.png`, removed the key locally, cropped transparent padding, and optimized a 640x192 runtime PNG at `assets/runtime/ui/icons/combat-pile-title-plaque.png`. BattleScene's deck/draw/discard card review overlay now layers `ui-icon-combat-pile-title-plaque` behind the dynamic `Deck Review`, `Draw Pile`, and `Discard` title text, anchoring the inspector header in the same generated hardware language as the dossier, rows, counters, stat chips, and close command while preserving the existing coded title copy. Text-state telemetry exposes `combatPileTitlePlaque`, and the focused combat-pile smoke now asserts one title plaque renders in the seeded draw-pile inspector.
- Verification: alpha validation passed with transparent corners, opaque title center alpha 255, 640x192 output, zero green-ish visible pixels, and a 73 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-title-plaque` rendered with telemetry `{ loaded: true, rendered: true, count: 1 }`, no page errors, readable title text, and no new overlap with pile counters or close control; screenshots, keyed raw output, alpha validation, and `state-summary.json` are in `.artifacts/test-results/combat-pile-title-plaque/`.

- Combat pile page-indicator footer polish: generated a transparent brass/gunmetal/cyan/magenta horizontal pagination capsule with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-pile-page-indicator-frame-source-v1.png`, removed the key locally, cropped transparent padding, and optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/combat-pile-page-indicator-frame.png`. BattleScene's deck/draw/discard card review overlay now layers `ui-icon-combat-pile-page-indicator-frame` behind the `1-11 / 13` footer readout, finishing the generated combat-pile inspector chrome from title through list, controls, selected-card dossier, chips, and footer. Text-state telemetry exposes `combatPilePageIndicatorFrame`, and the focused combat-pile smoke now asserts exactly one page-indicator frame renders in the seeded draw-pile inspector.
- Verification: alpha validation passed with transparent corners, opaque readable center alpha 255, 512x144 output, ~84.9% visible coverage, and a 115 KB optimized PNG; the raw keyed output detected key `#04f804` and the optimized runtime sprite had only three conservative green-ish heuristic pixels with no visible fringe in inspection. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "combat pile inspector renders generated review dossier frame" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-pile-page-indicator-frame` rendered with telemetry `{ loaded: true, rendered: true, count: 1 }`, zero console errors, zero page errors, readable footer text, and no overlap with the list rows, scroll controls, or bottom hand cards; screenshots, keyed raw output, close crop, and `capture-summary.json` are in `.artifacts/test-results/combat-pile-page-indicator-frame/`.

- Combat elite crest polish: generated a transparent brass/gunmetal/cyan/magenta elite-enemy crest with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-elite-crest-source-v1.png`, removed the key locally, retried with `--edge-contract 1` to suppress the chroma halo, cropped transparent padding, and optimized a 512x176 runtime PNG at `assets/runtime/ui/icons/combat-elite-crest.png`. BattleScene's elite enemy marker now layers `ui-icon-combat-elite-crest` above elite HP/intent presentation, bringing the tougher-enemy rank marker into the generated combat UI family. The first built-preview pass caught the old glyph label rendering as mojibake in browser, so the final implementation overlays a small dark center plate and clean ASCII `ELITE` label on top of the crest while preserving the generated frame. Text-state telemetry exposes `combatEliteCrest`, and the existing elite encounter smoke now asserts the crest loads and renders exactly once in the authored Gutter Baron encounter.
- Verification: alpha validation passed with transparent corners, opaque readable center alpha 255, 512x176 output, ~61.8% visible coverage, and a 123 KB optimized PNG; the contracted raw keyed output detected key `#0bec10`, with only a small conservative green-ish heuristic count in the gold edge glow and no visible chroma fringe after inspection. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test --grep "elite encounters spawn multiple enemies" tests/smoke.spec.ts --workers=1 --reporter=line`, `npm run build`, `git diff --check` (CRLF warnings only), and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `ui-icon-combat-elite-crest` rendered with telemetry `{ loaded: true, rendered: true, count: 1 }`, zero console errors, zero page errors, a readable non-mojibake `ELITE` label, and no overlap with HP bars, intent rings, or hand cards; screenshots, keyed raw outputs, and capture summaries are in `.artifacts/test-results/combat-elite-crest/`.

- Combat enemy status-chip polish: reused the fresh built-in imagegen candidate for a transparent-ready brass/gunmetal/cyan/magenta horizontal condition chip, copied the source to `assets/concept-art/ui/icons/sources/combat-enemy-status-chip-frame-source-v1.png`, removed the flat green chroma key locally, and optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/combat-enemy-status-chip-frame.png`. BattleScene now layers `ui-icon-combat-enemy-status-chip-frame` behind enemy `Cover`/`Winded` status text, using a bolder stroked label so temporary conditions read like deliberate combat instrumentation rather than loose HUD text. Text-state telemetry exposes `combatEnemyStatusChipFrame`, and a new focused smoke forces a living enemy to `7 Cover / 2 Winded` and asserts the generated chip loads, renders, and matches the display-list texture count.
- Verification: alpha validation passed with transparent corners, visible bounds `(6, 6, 506, 137)`, zero green-ish visible pixels, a 114,670-byte optimized PNG, and key-removal source stats from `.artifacts/test-results/combat-enemy-status-chip-frame/combat-enemy-status-chip-frame-keyed-raw.png`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat enemy status text" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Built-preview captures on `http://127.0.0.1:4224/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatEnemyStatusChipFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, no page errors, and readable status text under the enemy HP frame without overlapping the intent badge or combat log; screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-enemy-status-chip-frame/`. Production output includes `combat-enemy-status-chip-frame-L18bLhFU.png`.

- Combat Waymark feedback toast polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal Waymark activation toast frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-waymark-feedback-frame-source-v1.png`, removed the key locally, cropped transparent padding, and optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/combat-waymark-feedback-frame.png`. BattleScene's Waymark feedback toast now uses `ui-icon-combat-waymark-feedback-frame` behind the existing Waymark icon/name/summary text, giving artifact-trigger feedback the same premium generated treatment as supply-use feedback while preserving the fallback rectangle path. Text-state telemetry exposes `combatWaymarkFeedbackFrame`, and a new focused smoke triggers `broken_cover_chime` through `checkEnemyCoverBrokenMarks()` to assert the real Waymark pipeline renders the generated frame.
- Verification: alpha validation passed with transparent corners, visible bounds `(7, 8, 505, 136)`, center/icon-well alpha 255, zero green-ish visible pixels, and a 111,513-byte optimized PNG; the raw keyed source is `.artifacts/test-results/combat-waymark-feedback-frame/combat-waymark-feedback-frame-keyed-raw.png`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat waymark feedback" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Built-preview captures on `http://127.0.0.1:4225/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatWaymarkFeedbackFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, no page errors, readable `Broken Cover Chime` toast text, and no overlap with the combat log, enemy vitals, or hand cards; screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-waymark-feedback-frame/`. Production output includes `combat-waymark-feedback-frame-DB-zSewV.png`.

- Combat HUD metric-chip polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal resource-chip frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-hud-metric-chip-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/combat-hud-metric-chip-frame.png`. The shared `renderHudMetricChip` helper now layers `ui-icon-combat-hud-metric-chip-frame` behind Cover, Wingbeats, Resonance, Scrap, Waymarks, and run-status HUD chips while preserving the fallback rectangle, click hit areas, and tooltips. Text-state telemetry exposes `combatHudMetricChipFrame`, and a focused smoke asserts the generated frames load/render and match scene texture counts.
- Verification: alpha validation passed with transparent corners, open center alpha 0, zero green-ish visible pixels, 512x144 output, and a 67,627-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat HUD metric chips" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only existing CRLF warnings. Built-preview captures on `http://127.0.0.1:4226/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatHudMetricChipFrame` telemetry `{ loaded: true, rendered: true, count: 7 }`, zero console/page errors, and no overlap with enemy vitals, hand cards, or the encounter plaque; screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-hud-metric-chip-frame/`. Production output includes `combat-hud-metric-chip-frame-B8ZcKd_Y.png`.

- Combat suit-keystone HUD polish: generated a transparent-ready brass/gunmetal/cyan/magenta suit-keystone chip frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-suit-keystone-chip-frame-source-v1.png`, removed the key locally, and optimized a 384x128 runtime PNG at `assets/runtime/ui/icons/combat-suit-keystone-chip-frame.png`. BattleScene now passes live flock suit counts into the shared unified run HUD, which renders four generated keystone chips under the top resource row with readable inactive labels and active `*` markers. Text-state telemetry exposes `combatSuitKeystoneChipFrame`, and the focused HUD smoke now asserts exactly four generated keystone frames render alongside the seven metric-chip frames.
- Verification: alpha validation passed with transparent corners, open center alpha 0, ~40.6% visible coverage, zero green-ish visible pixels, 384x128 output, and a 54,928-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat HUD metric chips" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4227/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatSuitKeystoneChipFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, `combatHudMetricChipFrame` telemetry `{ loaded: true, rendered: true, count: 7 }`, zero console/page errors, and no collisions with the encounter title, enemy vitals, or hand cards after a placement/opacity adjustment; screenshots, top-HUD crop, and `capture-summary.json` are in `.artifacts/test-results/combat-suit-keystone-chip-frame/`. Production output includes `combat-suit-keystone-chip-frame-DUvJ_UE3.png`.
- Shared HUD text cleanup: replaced a corrupted middle-dot sequence in the unified run HUD beat label with ASCII `Beat N` so future callers cannot surface mojibake. Follow-up `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `git diff --check` (CRLF warnings only), and `npm run build` passed.

- Combat hover tooltip polish: generated a transparent-ready brass/gunmetal/cyan/magenta combat tooltip dossier frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-tooltip-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x305 runtime PNG at `assets/runtime/ui/icons/combat-tooltip-frame.png`. BattleScene's shared hover tooltip builder now layers `ui-icon-combat-tooltip-frame` behind measured tooltip text while preserving the existing clamped placement, word-wrap, fallback rectangle, and text safe area. Text-state telemetry exposes `combatTooltipFrame`, and a focused smoke asserts the generated frame loads/renders and matches the display-list texture count.
- Verification: alpha validation passed with transparent corners, center alpha 255, ~90.2% visible tooltip-panel coverage, only 10 conservative green-ish heuristic pixels with no visible fringe, 640x305 output, and a 241,603-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat hover tooltip" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4228/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatTooltipFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, zero console/page errors, readable tooltip text, and no incoherent overlap with hand cards or the encounter plaque; screenshots, crop, and `capture-summary.json` are in `.artifacts/test-results/combat-tooltip-frame/`. Production output includes `combat-tooltip-frame-7s-7KCWH.png`.

- Keyword glossary tooltip polish: generated a transparent-ready brass/gunmetal/cyan/magenta compact keyword definition frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/keyword-tooltip-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x249 runtime PNG at `assets/runtime/ui/icons/keyword-tooltip-frame.png`. The shared `showKwTooltip` renderer now layers `ui-icon-keyword-tooltip-frame` behind card/Codex glossary definitions while preserving fallback rectangles, clamped placement, and readable text wrapping. A small `__birdSquadShowKeywordTooltip` harness hook calls the same production renderer for deterministic smoke/visual capture, and keyword tooltip frame state exposes loaded/rendered count through Codex and Battle telemetry.
- Verification: alpha validation passed with transparent corners, center alpha 255, visible bounds `(8, 6, 632, 243)`, zero green-ish visible pixels, 640x249 output, and a 191,074-byte optimized PNG; the raw keyed output detected key `#07f805`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "keyword hover tooltip renders generated dossier frame" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4229/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `keywordTooltipFrame` hook telemetry `{ loaded: true, count: 1, frame: 288x112 alpha 0.96 }`, zero console/page errors, readable `Cover` definition text, and no overlap with the Codex card art or detail text; screenshots and `capture-summary.json` are in `.artifacts/test-results/keyword-tooltip-frame/`. Production output includes `keyword-tooltip-frame-CdtCxzBm.png`.

- System settings row-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta settings option row frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/system-settings-row-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 768x249 runtime PNG at `assets/runtime/ui/icons/system-settings-row-frame.png`. The shared `renderSettingsMenuOverlay` now layers `ui-icon-system-settings-row-frame` behind the Audio, Display, and Motion rows across title menu settings, route pause settings, and battle pause settings while preserving the existing hit targets, text, audio toggle, fullscreen command, and close controls. Text-state telemetry exposes `systemSettingsRowFrame` on MenuScene, RouteScene, and BattleScene.
- Verification: alpha validation passed with transparent corners, visible ratio ~27.4%, opaque ratio ~25.5%, zero green-fringe pixels, 768x249 output, and a 125,244-byte optimized PNG; the raw keyed output detected key `#04f805`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "settings overlay opens from menu and paused run surfaces"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4229/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `systemSettingsRowFrame` telemetry `{ loaded: true, rendered: true, count: 3 }`, zero console/page errors, readable settings row text, and no overlap with the close or fullscreen controls; screenshots and `capture-summary.json` are in `.artifacts/test-results/settings-row-frame/`. Production output includes `system-settings-row-frame-DmXkM_xI.png`.

- How to Play topic-card polish: generated a transparent-ready brass/gunmetal/cyan/magenta compact tutorial card frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/how-to-play-topic-card-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x228 runtime PNG at `assets/runtime/ui/icons/how-to-play-topic-card-frame.png`. The shared `renderHowToPlayOverlay` now layers `ui-icon-how-to-play-topic-card-frame` behind all four tutorial cards (`Plan the Route`, `Win the Fight`, `Care for the Flock`, `Grow Between Runs`) while preserving the existing icons, copy, tips, close control, and generated guide backdrop. Menu text-state telemetry exposes `howToPlayTopicCardFrame`.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~29.6%, opaque ratio ~27.7%, zero green-fringe pixels, 640x228 output, and a 107,080-byte optimized PNG; the raw keyed output detected key `#0eeb0f`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "title How to Play overlay opens"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4230/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `howToPlayTopicCardFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, zero console/page errors, readable tutorial copy, and no overlap with tips or close controls; screenshots and `capture-summary.json` are in `.artifacts/test-results/how-to-play-topic-card-frame/`. Production output includes `how-to-play-topic-card-frame-ehOkRy6P.png`.

- System pause detail-row polish: generated a transparent-ready brass/gunmetal/cyan/magenta pause status row frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/system-pause-detail-row-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x150 runtime PNG at `assets/runtime/ui/icons/system-pause-detail-row-frame.png`. The shared `renderPauseMenuOverlay` now layers `ui-icon-system-pause-detail-row-frame` behind all four Route and Battle pause detail rows while preserving existing hit targets, resume/settings/main-menu commands, and fallback rectangles. Text-state telemetry exposes `systemPauseDetailRowFrame` on RouteScene and BattleScene, and the pause smoke now asserts the generated row frames render on both surfaces.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~38.0%, opaque ratio ~35.1%, zero green-fringe pixels, 640x150 output, and an 89 KB optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "pause overlay opens on route and combat" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Initial built-preview review caught the combat encounter intro FX crossing over the Battle pause dialog in landscape/portrait, so BattleScene now raises modal UI above the persistent FX layer while a drawer/pause/settings modal is open; the focused pause smoke asserts `root.depth > fxLayer.depth` while paused and restores normal FX-over-board ordering after closing. Fresh built-preview captures on `http://127.0.0.1:4231/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `systemPauseDetailRowFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, Battle modal depths `{ root: 40, fx: 20 }`, zero console/page errors, nonblank screenshots, readable pause row labels/values, and no intro FX overlap; screenshots and `capture-summary.json` are in `.artifacts/test-results/system-pause-detail-row-frame-depth-fix/`. Production output includes `system-pause-detail-row-frame-CVOcY4ku.png`.

- Run-kit empty supply-slot polish: generated a transparent-ready brass/gunmetal/cyan/magenta empty inventory slot frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/run-kit-empty-slot-frame-source-v1.png`, removed the key locally, cropped/downsampled it to match the filled run-kit tile footprint, and optimized a 512x224 runtime PNG at `assets/runtime/ui/icons/run-kit-empty-slot-frame.png`. RouteScene and BattleScene supply drawers now render `ui-icon-run-kit-empty-slot-frame` for vacant supply slots while keeping filled supply tiles on `ui-icon-run-kit-item-tile-frame`, so increased-capacity kits show intentional empty bays instead of plain fallback rectangles. Text-state telemetry exposes `runKitEmptySlotFrame` on RouteScene and BattleScene, and the run-kit smoke now seeds a three-slot kit with two supplies to assert exactly one empty generated frame on both surfaces.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~26.2%, opaque ratio ~24.3%, zero green-fringe pixels, 512x224 output, and a 94,980-byte optimized PNG; the raw keyed output detected key `#0bf809`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "run kit drawers render generated inventory flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4232/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed Route and Battle supply drawers render telemetry `{ runKitItemTileFrame: { loaded: true, rendered: true, count: 2 }, runKitEmptySlotFrame: { loaded: true, rendered: true, count: 1 } }`, zero console/page errors, nonblank screenshots, aligned empty-slot chrome, and no overlap with close controls or HUD; screenshots and `capture-summary.json` are in `.artifacts/test-results/run-kit-empty-slot-frame/`. Production output includes `run-kit-empty-slot-frame-D1b1LOY0.png`.

- Boss prep readiness-chip polish: generated a transparent-ready brass/gunmetal/cyan/magenta compact readiness chip frame with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/boss-prep-readiness-chip-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x128 runtime PNG at `assets/runtime/ui/icons/boss-prep-readiness-chip-frame.png`. RouteScene's boss prep dossier now layers `ui-icon-boss-prep-readiness-chip-frame` behind the Cover, Damage, Recovery, and Molt readiness chips while preserving the existing status colors, labels, and fallback rectangle path. Text-state telemetry exposes `bossPrepReadinessChipFrame`, and the route/boss-prep smoke tests assert exactly four rendered chip frames.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~45.8%, opaque ratio ~38.0%, zero green-fringe pixels, 512x128 output, and a 62,699-byte optimized PNG; the raw keyed output detected key `#0eef0d`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "route map exposes boss prep readiness before the final crossing" --workers=1 --reporter=line`, focused `npx playwright test tests/smoke.spec.ts --grep "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:4233/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4233/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `bossPrepReadinessChipFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, zero console/page errors, readable readiness labels, and no overlap with the boss title or Route For column; screenshots, footer crop, and `capture-summary.json` are in `.artifacts/test-results/boss-prep-readiness-chip-frame/`. Production output includes `boss-prep-readiness-chip-frame-OaMCfqNq.png`.

- Boss prep Route For plate polish: generated a transparent-ready brass/gunmetal/cyan/magenta recommendation readout plate with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/boss-prep-route-for-plate-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x144 runtime PNG at `assets/runtime/ui/icons/boss-prep-route-for-plate.png`. RouteScene's boss prep dossier now layers `ui-icon-boss-prep-route-for-plate` behind the `Route For`, `Molt / Supply`, and useful-node guidance block while preserving the existing dynamic copy, width constraints, and fallback readability. Text-state telemetry exposes `bossPrepRouteForPlate`, and the route/boss-prep smoke tests assert exactly one rendered plate alongside the four readiness-chip frames.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~39.9%, opaque ratio ~33.1%, zero green-fringe pixels, 512x144 output, and a 59,940-byte optimized PNG; the raw keyed output detected key `#14e517`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts --grep "route map exposes boss prep readiness before the final crossing" --workers=1 --reporter=line`, focused `npx playwright test tests/smoke.spec.ts --grep "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4234/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `bossPrepRouteForPlate` telemetry `{ loaded: true, rendered: true, count: 1 }`, `bossPrepReadinessChipFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, zero console/page errors, readable recommendation text, and no overlap with the readiness chips or route-map frame after reducing the plate to 220x50 at 0.56 alpha; screenshots, footer crop, and `capture-summary.json` are in `.artifacts/test-results/boss-prep-route-for-plate/`. Production output includes `boss-prep-route-for-plate-B6XWEyQ6.png`.

- Boss prep pressure-plaque polish: generated a transparent-ready brass/gunmetal/cyan/magenta boss-pressure plaque with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/boss-prep-pressure-plaque-source-v1.png`, removed the key locally, cropped/downsampled it into a 512x144 runtime frame at `assets/runtime/ui/icons/boss-prep-pressure-plaque.png`, and layered `ui-icon-boss-prep-pressure-plaque` behind the `BOSS PREP`, boss name, and pressure readout column. RouteScene telemetry now exposes `bossPrepPressurePlaque`, and the broad route preview plus focused boss-prep readiness smokes assert exactly one rendered plaque alongside the four readiness chips and one route recommendation plate.
- Verification: alpha validation passed with transparent corners, open center alpha 0, visible ratio ~29.4%, opaque ratio ~22.5%, zero green-fringe pixels, 512x144 output, and a 59,384-byte optimized PNG; the raw keyed output detected key `#09f80d`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map exposes boss prep readiness before the final crossing" --workers=1 --reporter=line`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4235/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `bossPrepPressurePlaque` telemetry `{ loaded: true, rendered: true, count: 1 }`, readable boss/pressure text, and no overlap with readiness chips or the Route For plate; screenshots, footer crops, and `capture-summary.json` are in `.artifacts/test-results/boss-prep-pressure-plaque/`. The capture harness reported repeated pre-existing Phaser duplicate-texture warnings while restarting scenes, with zero page errors.

- Boss prep signal-module animation polish: generated a transparent-ready brass/gunmetal/cyan/magenta three-lamp readiness analyzer with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/boss-prep-signal-module-source-v1.png`, removed the key locally, cropped/downsampled it into a 384x128 runtime frame at `assets/runtime/ui/icons/boss-prep-signal-module.png`, and layered `ui-icon-boss-prep-signal-module` between the readiness chips and `Route For` recommendation. The module runs a reduced-motion-aware alpha/scale pulse so boss prep now has a small live status bridge instead of only static chrome. RouteScene telemetry exposes `bossPrepSignalModule`, and the broad route preview plus focused boss-prep readiness smokes assert exactly one rendered signal module alongside the pressure plaque, four readiness chips, and one route recommendation plate.
- Verification: alpha validation passed with transparent corners, visible center lens alpha 255, visible ratio ~70.4%, opaque ratio ~66.8%, zero green-fringe pixels, 384x128 output, and an 85,830-byte optimized PNG; the raw keyed output detected key `#10ea17`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map exposes boss prep readiness before the final crossing" --workers=1 --reporter=line`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4236/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `bossPrepSignalModule` telemetry `{ loaded: true, rendered: true, count: 1 }`, final rendered geometry about 72x25 with alpha ~0.738 mid-pulse, and no overlap with Molt or Route For after reducing the first pass from 82x28; screenshots, footer crops, `capture-summary.json`, and `capture-summary-final.json` are in `.artifacts/test-results/boss-prep-signal-module/`. The capture harness reported repeated pre-existing Phaser duplicate-texture warnings while restarting scenes, with zero page errors.

- Route boss beacon-ring polish: generated a transparent-ready brass/gunmetal/cyan/magenta objective beacon with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/route-boss-beacon-ring-source-v1.png`, removed the key locally, cropped/downsampled it into a 384x384 runtime frame at `assets/runtime/ui/icons/route-boss-beacon-ring.png`, and layered `ui-icon-route-boss-beacon-ring` behind the boss node on the route map. The ring uses a reduced-motion-aware alpha/scale pulse on incomplete boss nodes and dims on completed boss nodes, making the final objective read as a live destination without changing the click target or route-node icon. RouteScene telemetry exposes `routeBossBeaconRing`, and the broad route preview smoke now asserts exactly one rendered beacon ring.
- Verification: alpha validation passed with transparent corners and open center alpha 0, visible ratio ~28.3%, opaque ratio ~25.1%, one conservative green-ish heuristic pixel with no visible fringe, 384x384 output, and a 133,978-byte optimized PNG; the raw keyed output detected key `#0de718`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route map loads and previews the boss before the final node" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4237/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `routeBossBeaconRing` telemetry `{ loaded: true, rendered: true, count: 1 }`, zero console/page errors, readable boss-node focus, and no route-map layout crowding; screenshots, boss crops, and `capture-summary.json` are in `.artifacts/test-results/route-boss-beacon-ring/`. Production output includes `route-boss-beacon-ring-DOStUTtx.png`.

- Confirm-exit command-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal command frame with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/confirm-exit-command-frame-source-v1.png`, removed the key locally, cropped/downsampled it into a 768x183 runtime frame at `assets/runtime/ui/icons/confirm-exit-command-frame.png`, and layered `ui-icon-confirm-exit-command-frame` behind both `Keep Playing` and `Abandon Run` controls in the destructive route-exit confirmation modal. The old button rectangles now act as low-alpha hit/fallback surfaces, while the generated frame and stroked text make the modal's decisive actions match the rest of the premium route UI chrome. RouteScene telemetry exposes `confirmExitCommandFrame`, and the route ESC smoke asserts exactly two rendered command frames.
- Verification: alpha validation passed with transparent corners, center alpha 255, visible ratio ~84.0%, opaque ratio ~81.0%, three conservative green-ish heuristic pixels with no visible fringe, 768x183 output, and a 226,607-byte optimized PNG; the raw keyed output detected key `#06f80c`. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "route ESC asks to confirm before abandoning a run" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4238/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `confirmExitCommandFrame` telemetry `{ loaded: true, rendered: true, count: 2 }`, zero console/page errors, readable command labels, and no modal text/button overlap; screenshots, modal crops, and `capture-summary.json` are in `.artifacts/test-results/confirm-exit-command-frame/`. Production output includes `confirm-exit-command-frame-DFNkIrub.png`.

- Codex back command-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal Back command frame with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/codex-back-command-frame-source-v1.png`, removed the key locally, cropped/downsampled it into a 512x162 runtime frame at `assets/runtime/ui/icons/codex-back-command-frame.png`, and layered `ui-icon-codex-back-command-frame` behind the Codex header `Back` control. CodexScene telemetry now exposes `codexBackCommandFrame`, and the focused Codex smoke asserts the texture is loaded, rendered once, visible, and sized at 132x42.
- Verification: alpha validation passed with transparent corners, visible ratio ~84.7%, opaque ratio ~81.6%, 512x162 output, and a 149,724-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "codex: starting a run discovers its deck and the Codex screen renders" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. The first built-preview pass caught the frame crowding the audio toggle, so the final geometry shifted the Back control to `GAME_WIDTH - 76` and reduced the generated frame to 132x42. Fresh built-preview captures on `http://127.0.0.1:4239/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `codexBackCommandFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, final spacing of about 11.1 stage pixels from the audio medallion and 3.4 from the pulse ring, zero console/page errors, readable Back text, and no header overlap; screenshots, header crops, `capture-summary.json`, and `capture-summary-final.json` are in `.artifacts/test-results/codex-back-command-frame/`. Production output includes `codex-back-command-frame-DbJQeCq5.png`.

- Codex navigation tab-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta compact Codex tab frame with a left medallion bay using built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/codex-tab-frame-source-v1.png`, removed the key locally, cropped/downsampled it into a 640x171 runtime frame at `assets/runtime/ui/icons/codex-tab-frame.png`, and layered `ui-icon-codex-tab-frame` behind every `renderCodexTab` navigation button while preserving the existing hit rectangles, labels, medallions, hover behavior, and fallback path. CodexScene telemetry now exposes `codexTabFrame`, and the focused Codex smoke asserts the Cards section renders all 12 generated tab frames.
- Verification: key removal detected `#04f807`; alpha validation passed with transparent corners, visible ratio ~84.5%, opaque ratio ~82.5%, one conservative green-ish heuristic pixel with no visible fringe, 640x171 output, and a 216,570-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "codex: starting a run discovers its deck and the Codex screen renders" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4240/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `codexTabFrame` telemetry `{ loaded: true, rendered: true, count: 12 }`, zero tab-frame geometry overlaps, zero console/page errors, readable tab labels/counters, and no actual left-edge clipping in the full desktop shot; screenshots, tab crops, and `capture-summary.json` are in `.artifacts/test-results/codex-tab-frame/`. Production output includes `codex-tab-frame-b-PMMu5d.png`.

- Codex detail close-command polish: generated a transparent-ready square Codex modal close frame with built-in imagegen on a flat green chroma-key background, copied the preserved source to `assets/concept-art/ui/icons/sources/codex-close-command-frame-source-v1.png`, removed the key locally, cropped/downsampled it into a 256x256 runtime frame at `assets/runtime/ui/icons/codex-close-command-frame.png`, and added `renderCodexCloseControl()` so supply, waymark, leader, card, and enemy Codex detail modals share the same generated close-command chrome. The old plain red close rectangles remain only as low-alpha hit/fallback surfaces, and the long card-detail close glyph now renders as clean ASCII `X` instead of the previous mojibake close symbol. CodexScene telemetry now exposes `codexCloseCommandFrame`, and the focused Codex smoke opens a real discovered card detail and asserts one rendered 46x46 close frame.
- Verification: key removal detected `#0bf510`; alpha validation passed with transparent corners, center alpha 255, visible ratio ~85.6%, opaque ratio ~84.1%, zero green-fringe pixels, 256x256 output, and a 118,349-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "codex: starting a run discovers its deck and the Codex screen renders" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4241/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed only `CodexScene` active, `codexCloseCommandFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, zero console/page errors, and the close command aligned inside the dossier corner without colliding with the border; screenshots, debug capture, and `capture-summary.json` are in `.artifacts/test-results/codex-close-command-frame/`. Visual review also surfaced pre-existing mojibake in other Codex detail copy/scroll hints, which remains a good next polish target outside this close-control slice.
- Combat log event-bead polish: generated a transparent-ready brass/gunmetal/cyan/magenta circular event bead with built-in imagegen on a flat green chroma-key background; prompt type was `stylized-concept` for a small combat-log ticker marker with a cyan signal core, magenta feather accent, no text, no logos, and no green subject pixels. Preserved the source at `assets/concept-art/ui/icons/sources/combat-log-event-bead-source-v1.png`, removed the key locally with `$CODEX_HOME/skills/.system/imagegen/scripts/remove_chroma_key.py`, and cropped/downsampled/optimized it into `assets/runtime/ui/icons/combat-log-event-bead.png` (192x192 RGBA, 66,311 bytes). Alpha validation detected key `#05f30d`; the runtime sprite has transparent corners, center alpha 255, visible bounds `(10, 10, 182, 182)`, 13,236 transparent pixels, 1,174 partial-alpha pixels, 22,454 opaque pixels, and zero green-ish visible pixels.
- BattleScene now registers/preloads `combat-log-event-bead`, renders one generated bead beside each visible combat-log line, exposes `combatLogEventBead.loaded/rendered/count` through `render_game_to_text`, and keeps the existing generated combat-log frame/fallback behavior. Active combat-log and tooltip mojibake dashes were normalized to clean ASCII in Molt, ruffle, snag, district-clear, intent, status, and keyword tooltip copy; the Winded keyword definition now uses a clean ASCII apostrophe. The focused smoke `combat event log renders generated dossier frame` now asserts bead telemetry, count, geometry, and alpha alongside the existing combat-log frame assertions.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test ./tests/smoke.spec.ts -g "combat event log renders generated dossier frame" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run validate:runtime`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4242/?playwright=1` at 2560x1600, 844x390, and 390x844 confirmed `combatLogFrame` and `combatLogEventBead` telemetry `{ loaded: true, rendered: true, count: 2 }`, zero bad-glyph log texts, zero console/page errors, and no overlap with the hand cards or enemy HUD after clearing transient intro FX for steady-state review. Screenshots and `capture-summary.json` are in `.artifacts/test-results/combat-log-event-bead/`; production output includes `combat-log-event-bead-xw3MkNXO.png`.

- Title leader selected-flourish polish: generated a transparent-ready brass/gunmetal/cyan/magenta selected leader card flourish with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/title-leader-selected-flourish-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x192 runtime PNG at `assets/runtime/ui/icons/title-leader-selected-flourish.png`. MenuScene now preloads `ui-icon-title-leader-selected-flourish`, creates a reduced-motion-aware subtle pulse for unlocked leader panels, shows exactly one visible flourish on the selected leader, and exposes `titleLeaderSelectedFlourish.loaded/rendered/count` through `render_game_to_text`. The title portrait smoke now asserts the new asset is loaded, exactly one visible selected flourish is present, and the generated frame renders at 236x82 without disturbing the existing leader card/header/start/control frame assertions.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 640x192 output, 44,721 visible pixels, 9,138 partial-alpha pixels, 35,583 opaque pixels, zero green-ish visible pixels, and a 102,629-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "title screen fits portrait mobile viewport" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4243/?playwright=1` at 2560x1600, 1280x720, and 390x844 confirmed `titleLeaderSelectedFlourish` telemetry `{ loaded: true, rendered: true, count: 1 }`, one visible selected flourish, zero page errors, and no leader-row text overlap; the only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, leader-row crops, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/title-leader-selected-flourish/`.

- Market price-chip frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta market price chip with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/market-price-chip-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x160 runtime PNG at `assets/runtime/ui/icons/market-price-chip-frame.png`. RouteScene's market overlay now registers `ui-icon-market-price-chip-frame`, renders it behind all card-offer buy tags, item label prices, service prices, and the refresh price while keeping the old dark fills as fallback/readability layers, and exposes `market.priceChipFrame.loaded/rendered/count` through `render_game_to_text`. The market shop-kit smoke now asserts the generated price chip texture loads, renders, and appears at least 10 times alongside the existing shopkeeper/sign/counter/tray/service-art assertions.
- Verification: alpha validation passed with transparent corners, open center alpha 0, 512x160 output, 34,967 visible pixels, 5,983 partial-alpha pixels, 28,984 opaque pixels, zero green-ish visible pixels, and a 78,854-byte optimized PNG. `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4244/?playwright=1` at 2560x1600, 1280x720, and 390x844 confirmed `market.priceChipFrame` telemetry `{ loaded: true, rendered: true, count: 13 }`, 13 visible price-chip frames, zero page errors, readable prices on card offers/waymarks/supplies/services, and no shop row overlap; the only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, market crops, keyed raw output, and `capture-summary.json` are in `.artifacts/test-results/market-price-chip-frame/`. Production output includes `market-price-chip-frame-BqDIgVdd.png`.

- Card picker cost-badge polish: generated a transparent-ready brass/gunmetal/cyan cost counter with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/card-picker-cost-badge-source-v1.png`, removed the key locally, and optimized a 256x256 runtime PNG at `assets/runtime/ui/icons/card-picker-cost-badge.png` (85,388 bytes). Alpha cleanup produced transparent corners, center alpha 255, visible bounds `(101, 78, 1150, 1131)` in the raw keyed output, 39,348 visible runtime pixels, 2,406 partial-alpha pixels, 36,942 opaque pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-card-picker-cost-badge`, layers the generated badge behind every card-picker cost value with affordable/unaffordable tinting and a circle fallback, and exposes `cardPickerCostBadge.loaded/rendered/count` through `render_game_to_text`. The market preen and route preen picker smokes now assert the generated cost badge loads and renders, including exactly ten visible badges in the two-row large-deck picker.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "market preen service lets the player choose a card before paying" --workers=1 --reporter=line`, and focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport" --workers=1 --reporter=line` passed. Built-preview captures on `http://127.0.0.1:4245/?playwright=1` at 2560x1600, 1280x720, and 390x844 confirmed `cardPickerCostBadge` telemetry `{ loaded: true, rendered: true, count: 10 }`, ten display-list badge objects, zero page errors, and no console errors except expected high-resolution WebGL ReadPixels screenshot warnings. Visual inspection of `highres-2560x1600-picker.png` and `mobile-390x844-picker.png` confirmed the badges read clearly as premium counters, stay anchored to card corners, and do not crowd labels, scroll controls, the Cancel command, or the picker frame. Screenshots, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/card-picker-cost-badge/`. Production output includes `card-picker-cost-badge-DDcG08VX.png`.

- Card picker page-indicator frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal page-range capsule with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/card-picker-page-indicator-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x160 runtime PNG at `assets/runtime/ui/icons/card-picker-page-indicator-frame.png` (90,086 bytes). Alpha cleanup detected key `#07f90a`; the raw keyed output had 1,018,821 transparent pixels and 6,843 partial-alpha pixels, while the runtime sprite has transparent corners, center alpha 255, 48,788 visible pixels, 3,957 partial-alpha pixels, 44,831 opaque pixels, and only three conservative green-ish visible pixels with none on the edge. RouteScene now registers/preloads `ui-icon-card-picker-page-indicator-frame`, layers it behind the card picker `1-10 / 18` range label with a dark fallback rectangle, and exposes `cardPickerPageIndicatorFrame.loaded/rendered/count` through `render_game_to_text`. The large-deck route preen picker smoke now asserts the generated frame loads, renders exactly once, and keeps its 104x30 geometry alongside the scroll buttons, cost badges, and cancel command.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4245/?playwright=1` at 2560x1600, 1280x720, and 390x844 confirmed `cardPickerPageIndicatorFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, one display-list frame object, readable `1-10 / 18` text, zero page errors, and no console errors except expected high-resolution WebGL ReadPixels screenshot warnings. Visual inspection of `highres-2560x1600-picker.png`, `highres-2560x1600-indicator.png`, and `mobile-390x844-picker.png` confirmed the capsule reads as a matching hardware label backing and stays clear of the scroll buttons, card row, Cancel command, and frame border. Screenshots, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/card-picker-page-indicator-frame/`. Production output includes `card-picker-page-indicator-frame-CoD_CCk5.png`.

- Card picker nameplate-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta card-name plaque with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/card-picker-nameplate-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 512x192 runtime PNG at `assets/runtime/ui/icons/card-picker-nameplate-frame.png` (102,525 bytes). Alpha cleanup detected key `#0eed15`; the raw keyed output had 1,083,346 transparent pixels and 4,319 partial-alpha pixels, while the runtime sprite has transparent corners, center alpha 255, 60,030 visible pixels, 3,702 partial-alpha pixels, 56,328 opaque pixels, and only three conservative green-ish visible pixels with none on the edge. RouteScene now registers/preloads `ui-icon-card-picker-nameplate-frame`, replaces the raw black card-name strips in the card picker with generated plaques plus a rectangle fallback, keeps the existing card-name word wrap/max-lines, adds dark text stroke for readability, and exposes `cardPickerNameplateFrame.loaded/rendered/count` through `render_game_to_text`. The large-deck route preen picker smoke now asserts ten generated nameplates render at 106x32, one per visible card.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Built-preview captures on `http://127.0.0.1:4245/?playwright=1` at 2560x1600, 1280x720, and 390x844 confirmed `cardPickerNameplateFrame` telemetry `{ loaded: true, rendered: true, count: 10 }`, ten display-list frame objects, all ten visible card labels present, zero page errors, and no console errors except expected high-resolution WebGL ReadPixels screenshot warnings. Visual inspection of `highres-2560x1600-picker.png`, `highres-2560x1600-nameplates.png`, and `mobile-390x844-picker.png` confirmed the plaques make the picker labels feel like crafted UI hardware while staying readable and clear of cost badges, card art, scroll controls, the page indicator, Cancel command, and frame border. Screenshots, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/card-picker-nameplate-frame/`. Production output includes `card-picker-nameplate-frame-CoQlaAXH.png`.

- Card picker context-plaque polish: generated a transparent-ready brass/gunmetal/cyan/magenta location label plaque with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/card-picker-context-plaque-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 960x192 runtime PNG at `assets/runtime/ui/icons/card-picker-context-plaque.png` (192,804 bytes). Alpha cleanup detected key `#06e919`; the raw keyed output had 949,796 transparent pixels and 6,423 partial-alpha pixels, while the runtime sprite has transparent corners, center alpha 255, 104,393 visible pixels, 4,003 partial-alpha pixels, 100,390 opaque pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-card-picker-context-plaque`, layers it behind the route/market context label in the card picker, centers the label inside the generated dark field with stroked readable text, and exposes `cardPickerContextPlaque.loaded/rendered/count` through `render_game_to_text`. The large-deck route preen picker smoke now asserts exactly one generated context plaque renders at 336x66 while preserving the context label text.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "route preen picker keeps large decks inside a two-row viewport"`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4254/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `cardPickerContextPlaque` telemetry `{ loaded: true, rendered: true, count: 1 }`, one display-list plaque object, and the `Nest` label centered inside the generated plaque after a visual placement correction. Visual inspection of `highres-2560x1600-context-crop.png`, `desktop-1440x900-context-crop.png`, and `mobile-390x844-full.png` confirmed the plaque reads as matching picker hardware and stays clear of the title, subtitle, first card column, nameplates, cost badges, scroll controls, page indicator, Cancel command, and picker frame. Screenshots, keyed raw output, preview logs, and `screenshot-metrics.json` are in `.artifacts/test-results/card-picker-context-plaque/`. Production output includes `card-picker-context-plaque-DRnepaCw.png`.

- Market enamel command-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta shared market command button frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/market-enamel-command-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 768x192 runtime PNG at `assets/runtime/ui/icons/market-enamel-command-frame.png` (193,540 bytes). Alpha cleanup detected key `#08e419`; the raw keyed output had 1,039,791 transparent pixels and 6,141 partial-alpha pixels, while the runtime sprite has transparent corners, center alpha 255, 104,404 visible pixels, 3,926 partial-alpha pixels, 100,478 opaque pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-market-enamel-command-frame`, layers it inside the shared `renderMarketEnamelButton()` helper while preserving the existing rectangle hit target/fallback, adds a small hover alpha/size response to the generated frame, and exposes `marketEnamelCommandFrame.loaded/rendered/count` through `render_game_to_text`. The market card-picker `Back to Market` command was moved right within the picker after the first built-preview pass showed the market character art partially covering the old button position.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market preen service lets the player choose a card before paying"`, `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4255/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `marketEnamelCommandFrame` telemetry `{ loaded: true, rendered: true, count: 2 }`, no page errors, the 200x56 `Back to Market` generated frame clear of the market character art after repositioning, and readable command text without crowding the card grid or picker frame. The only high-res console messages were WebGL screenshot/context performance warnings. Screenshots, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/market-enamel-command-frame/`. Production output includes `market-enamel-command-frame-Bp2ive99.png`.

- Market sold-slat frame polish: generated a transparent-ready market SOLD marker backing with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/market-sold-slat-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 640x192 runtime PNG at `assets/runtime/ui/icons/market-sold-slat-frame.png` (163,889 bytes). Alpha cleanup detected key `#06e720`; the runtime sprite has transparent corners, center alpha 255, 76,166 visible pixels, 4,574 partial-alpha pixels, 71,592 opaque pixels, 46,714 transparent pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-market-sold-slat-frame`, replaces the plain code-drawn sold bar with a generated brass/gunmetal/cyan/magenta slat while keeping crisp Phaser-rendered `SOLD` text above it, and exposes `marketSoldSlatFrame.loaded/rendered/count` through `render_game_to_text`. The focused market finite-offers smoke now asserts the sold slat appears immediately after buying a card and before refresh clears sold offers.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market shelves stock multiple finite offers and paid refreshes" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4256/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `marketSoldSlatFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, one generated frame, one crisp `SOLD` label, zero page errors, readable desktop/high-res crops after the label-size correction, and no mobile overlap with market offers or service rows. The only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, keyed raw output, alpha validation, preview log, and `capture-summary.json` are in `.artifacts/test-results/market-sold-slat-frame/`. Production output includes `market-sold-slat-frame-NZS9xPcT.png`.

- Market object backplate-frame polish: generated a transparent-ready waymark/supply item backplate with built-in imagegen on a flat green chroma-key background. The first generated source was too portrait for the shelf footprint, so a second 4:3 landscape source was generated and used for the final asset; the chosen source is preserved at `assets/concept-art/ui/icons/sources/market-object-backplate-frame-source-v2.png`. The source was chroma-keyed, cropped/downsampled, and optimized into `assets/runtime/ui/icons/market-object-backplate-frame.png` (271,815 bytes). Alpha cleanup detected key `#08e028`; the runtime sprite has transparent corners, center alpha 255, 146,367 visible pixels, 5,532 partial-alpha pixels, 140,835 opaque pixels, 50,241 transparent pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-market-object-backplate-frame`, layers it in `renderMarketObjectBackplate()` behind every waymark badge and supply crate while preserving the old dark readability fill/fallback, and exposes `market.objectBackplateFrame.loaded/rendered/count` through `render_game_to_text`.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4257/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `market.objectBackplateFrame` telemetry `{ loaded: true, rendered: true, count: 6 }`, six generated shelf frames for three waymarks and three supplies, zero page errors, readable labels/prices, and no overlap with the service rows or mobile market shelf. The only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, keyed raw output, alpha validation, preview log, and `capture-summary.json` are in `.artifacts/test-results/market-object-backplate-frame/`. Production output includes `market-object-backplate-frame-B09caXZC.png`.

- Market section-header frame polish: generated a transparent-ready wide brass/gunmetal/cyan/magenta market section header rail with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/market-section-header-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 1024x176 runtime PNG at `assets/runtime/ui/icons/market-section-header-frame.png` (306,737-byte production asset). Alpha cleanup detected key `#15f612`; the runtime sprite has transparent corners, 147,063 opaque pixels, 6,162 partial-alpha pixels, 26,999 transparent pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-market-section-header-frame`, layers it behind the Crew Cards, Services, Waymarks, and Supplies market labels, moved the top Crew Cards/Services labels into the clear band above the card row after visual review, and exposes `market.sectionHeaderFrame.loaded/rendered/count` through `render_game_to_text`.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4258/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `market.sectionHeaderFrame` telemetry `{ loaded: true, rendered: true, count: 4 }`, four visible 60px-tall generated header frames, zero page errors, readable labels, and no incoherent overlap after the top-header reposition. The only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, crop images, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/market-section-header-frame/`. Production output includes `market-section-header-frame-0xiVkEb_.png`.

- Market vendor title-plaque polish: generated a transparent-ready hanging vendor nameplate with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/market-vendor-title-plaque-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 1024x280 runtime PNG at `assets/runtime/ui/icons/market-vendor-title-plaque.png` (299,094-byte production asset). Alpha cleanup detected key `#1bec21`; the runtime sprite has transparent corners, 131,761 opaque pixels, 9,419 partial-alpha pixels, 145,540 transparent pixels, and zero green-ish visible pixels. RouteScene now registers/preloads `ui-icon-market-vendor-title-plaque`, layers it behind the central `Veyra Tallybright / CANAL CURATOR` market title while keeping crisp Phaser-rendered text and fallback geometry, and exposes `marketVendorTitlePlaque.loaded/rendered/count` through `render_game_to_text`. The market shop-kit smoke now asserts the plaque loads, renders exactly once, and displays at 500x104.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "market overlay renders generated shopkeeper sign and counter kit" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4259/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `marketVendorTitlePlaque` telemetry `{ loaded: true, rendered: true, count: 1 }`, one visible generated title plaque, visible vendor/subtitle labels, zero page errors, and no crowding with the section headers or mobile market surface. The only high-res console messages were WebGL ReadPixels screenshot-performance warnings. Screenshots, title crops, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/market-vendor-title-plaque/`. Production output includes `market-vendor-title-plaque-_4ELRgc5.png`.

- Deck review row-frame polish: generated a transparent-ready horizontal deck-review row backing with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/deck-review-row-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 1024x175 runtime PNG at `assets/runtime/ui/icons/deck-review-row-frame.png`. Alpha cleanup detected key `#04f804`; the runtime sprite has 35,108 transparent pixels, 7,486 partial-alpha pixels, 136,606 opaque pixels, and zero green-ish visible pixels. RouteScene and BattleScene now register/preload `ui-icon-deck-review-row-frame`, layer the generated brass/gunmetal/cyan/magenta row frame behind each visible deck-review card row, keep the existing rectangle hit targets/fallback readability, and expose `deckReviewRowFrame.loaded/rendered/count` through `render_game_to_text`. The battle deck review no longer borrows `combat-pile-row-frame` for this surface.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4260/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed route and battle `deckReviewRowFrame` telemetry `{ loaded: true, rendered: true, count: 10 }`, open route deck overlay, open battle deck inspect overlay, zero console/page errors, readable card labels, aligned cost sockets, and clean scaling on mobile-width. Screenshots, keyed raw output, and preview logs are in `.artifacts/test-results/deck-review-row-frame/`. Production output includes `deck-review-row-frame-b-c35h7G.png`.

- Deck review page-indicator frame polish: generated a transparent-ready horizontal page-range capsule with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/deck-review-page-indicator-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 768x185 runtime PNG at `assets/runtime/ui/icons/deck-review-page-indicator-frame.png`. Alpha cleanup detected key `#06f80e`; the runtime sprite has transparent corners, center alpha 255, 13,915 transparent pixels, 5,415 partial-alpha pixels, 122,750 opaque pixels, and zero green-ish visible pixels. RouteScene and BattleScene now register/preload `ui-icon-deck-review-page-indicator-frame`, layer it behind the deck-review `1-10 / 10` page range text, center/stroke the Phaser-rendered text for readability, and expose `deckReviewPageIndicatorFrame.loaded/rendered/count` through `render_game_to_text`. The battle deck review no longer borrows `combat-pile-page-indicator-frame` for its deck-review page label.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "deck review overlays render generated dossier flourish art" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4260/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed route and battle `deckReviewPageIndicatorFrame` telemetry `{ loaded: true, rendered: true, count: 1 }`, geometry 126x36, zero console/page errors, readable centered range text, and no collision with scroll controls, row frames, detail panels, or mobile-width scaling. Screenshots, keyed raw output, preview logs, and `capture-summary.json` are in `.artifacts/test-results/deck-review-page-indicator-frame/`. Production output includes `deck-review-page-indicator-frame-BdpdbwpG.png`.

- How to Play tip-row frame polish: generated a transparent-ready tutorial tip rail with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/how-to-play-tip-row-frame-source-v1.png`, removed the key locally, cropped/downsampled it, and optimized a 768x192 runtime PNG at `assets/runtime/ui/icons/how-to-play-tip-row-frame.png` (165,076 bytes). Alpha cleanup detected key `#03f804`; the runtime sprite has transparent corners, center alpha 255, 48,655 transparent pixels, 6,130 partial-alpha pixels, 92,671 opaque pixels, and zero green-adjusted pixels after cleanup. MenuScene now registers/preloads `ui-icon-how-to-play-tip-row-frame`, layers two generated brass/gunmetal/cyan/magenta rails behind the `Quick keys` and `Run shape` rows in the How to Play overlay, keeps the old rectangle fallback/readability mat, and exposes `howToPlayTipRowFrame.loaded/rendered/count` through `render_game_to_text`. The title How-to-Play smoke now recursively asserts two generated tip-row frames render at 616x38.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts --grep "title How to Play overlay opens, reports state, and loads its medallion" --workers=1 --reporter=line`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` plus a targeted How-to-Play click run, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4231/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed `howToPlayTipRowFrame` telemetry `{ loaded: true, rendered: true, count: 2 }`, two 616x38 visible display-list objects, zero console/page errors, readable tip text, and no overlap with the topic cards, close controls, or guide frame. Screenshots, keyed raw output, capture helper, web-client state dumps, and `capture-summary.json` are in `.artifacts/test-results/how-to-play-tip-row-frame/`. The temporary preview on port 4231 was stopped after verification; the Bird Squad dev server remains live on port 5199.

- Route choice preview-row frame polish: generated a transparent-ready compact route-choice hover preview rail with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/route-choice-preview-row-frame-source-v1.png`, removed the detected `#04f80c` key locally into `.artifacts/test-results/route-choice-preview-row-frame/route-choice-preview-row-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x160 runtime PNG at `assets/runtime/ui/icons/route-choice-preview-row-frame.png` (100,337 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(78, 22, 689, 138)`, 55,983 transparent pixels, 6,490 partial-alpha pixels, 60,407 opaque pixels, and zero visible/edge green pixels. RouteScene now registers/preloads `ui-icon-route-choice-preview-row-frame`, layers generated rails behind resource-change preview rows in route choice hover dossiers while preserving the rectangle fallback/readability mat, and exposes `routeChoicePreviewRowFrame.loaded/rendered/count` through route and `nodeChoice.previewRowFrame` telemetry. The route-event overlay smoke now selects choices with preview rows and asserts generated preview-row frames for Basin, Cache, Signal, and Nest hover panels while leaving Rival free to report zero when its current hover choice has no resource preview rows.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "route event overlays render generated special-node backdrops" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Built-preview captures on `http://127.0.0.1:4265/?playwright=1` at 2560x1600, 1440x900, and 390x844 confirmed Basin/Signal preview-row telemetry with exact row counts (`1`, `3`, `1`), zero page errors, readable before/after values, and no overlap with the route choice detail frame or option rows. Screenshots, keyed raw output, capture helper, web-client state dumps, and `capture-summary.json` are in `.artifacts/test-results/route-choice-preview-row-frame/`. The temporary built preview on port 4265 was stopped after verification; Bird Squad remains live on port 5199.

- Combat atmosphere strip v2 polish: replaced the old 2.8 KB procedural-looking `assets/runtime/fx/combat-atmosphere-strip.png` spritesheet with a richer generated three-frame rooftop atmosphere strip. The built-in image tool produced a flat-chroma source preserved at `assets/concept-art/fx/sources/combat-atmosphere-strip-source-v2.png` (1,055,348 bytes); local chroma removal detected key `#14f813` and wrote `.artifacts/test-results/combat-atmosphere-strip/combat-atmosphere-strip-keyed-raw.png` with 1,524,735 transparent pixels and 42,219 partial-alpha pixels. The normalized runtime keeps the existing 768x96 / 3x256x96 spritesheet contract, crops the active FX band from source bounds `(48, 232, 2121, 474)`, validates transparent corners, runtime visible bounds `(15, 5, 752, 93)`, 63,611 transparent pixels, 9,981 partial-alpha pixels, 136 opaque pixels, zero edge-green residue, and a 39,713-byte optimized PNG. BattleScene now names every atmosphere tile `combat-atmosphere-strip`, lowers the richer strip's board alpha to `0.24`/`0.30` with restrained tween alpha, and reports `combatAtmosphereStrip.loaded/rendered/count` through battle telemetry. The combat encounter smoke now asserts the generated strip loads and renders at least the 21 expected board tiles.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview capture on `http://127.0.0.1:4290/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-atmosphere-strip-BgJrC-9O.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 all report `combatAtmosphereStrip { loaded: true, rendered: true, count: 21 }`, exact 256x96 tile geometry, zero page errors, and zero console errors. Visual inspection confirmed the new cyan/brass/magenta signal wisps add rooftop motion texture without competing with the top HUD, enemy vitals, combat log, hand cards, deck/discard piles, or fixed-canvas mobile letterbox. Screenshots, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-atmosphere-strip/`. The temporary built preview on port 4290 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Cohesion gauge-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta open-center Cohesion health gauge frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-cohesion-gauge-frame-source-v1.png`, removed detected `#17f71a` locally into `.artifacts/test-results/combat-cohesion-gauge-frame/combat-cohesion-gauge-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x192 runtime PNG at `assets/runtime/ui/icons/combat-cohesion-gauge-frame.png` (107,040 bytes). Runtime validation found transparent corners, center alpha 0, visible bounds `(27, 31, 741, 161)`, 102,888 transparent pixels, 9,439 partial-alpha pixels, 35,129 opaque pixels, and zero visible/edge green adjustments. The shared top HUD now registers/preloads `ui-icon-combat-cohesion-gauge-frame`, layers it at 248x52 over the live engine-rendered Cohesion bar without replacing the click target, fill, risk marker, heart icon, or text, and exposes `combatCohesionGaugeFrame.loaded/rendered/count` through BattleScene telemetry. A final visual pass raised the existing incoming-forecast plaque alpha to `0.9`, enlarged the forecast text to 11px, and widened its fixed text box so the damage preview remains readable below the new gauge chrome after the encounter intro dimmer clears.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock Cohesion bar exposes incoming damage prediction after Cover" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4291/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-cohesion-gauge-frame-6IDxXQnK.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatCohesionGaugeFrame { loaded: true, rendered: true, count: 1 }`, `combatIncomingForecastFrame { loaded: true, rendered: true, count: 1 }`, exact gauge geometry 248x52 at alpha 0.9, forecast geometry 242x42 at alpha 0.9, zero page errors, zero console errors, readable damage forecast, no collision with Cover/Wingbeats/suit chips, and stable fixed-canvas mobile letterbox behavior. Screenshots, HUD crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-cohesion-gauge-frame/`. The temporary built preview on port 4291 was stopped after verification; Bird Squad remains live on port 5199.

- Flock Stats row-frame polish: generated a transparent-ready brass/gunmetal/cyan/magenta horizontal stat-row frame with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/flock-stats-row-frame-source-v1.png`, removed detected `#05f90c` locally into `.artifacts/test-results/flock-stats-row-frame/flock-stats-row-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x192 runtime PNG at `assets/runtime/ui/icons/flock-stats-row-frame.png` (129,152 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(83, 28, 685, 164)`, 73,455 transparent pixels, 4,968 partial-alpha pixels, 69,033 opaque pixels, one conservative green adjustment, and zero edge-green pixels. The shared `renderFlockStatTable()` now layers `ui-icon-flock-stats-row-frame` behind every Stat/Base/Flock/Now row on route and battle Flock Stats overlays, with brighter tint/alpha on rows where owned cards add a bonus while preserving all Phaser-rendered text, separator lines, and the existing cyan bonus bar. RouteScene and BattleScene telemetry expose `flockStatsRowFrame.loaded/rendered/count`, and the existing Flock Stats smoke asserts route and battle row-frame counts plus battle display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4292/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `flock-stats-row-frame-lkMm5feT.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed route `flockStatsRowFrame { loaded: true, rendered: true, count: 9 }`, battle `flockStatsRowFrame { loaded: true, rendered: true, count: 10 }`, battle `combatStatsTallyFrame { loaded: true, rendered: true, count: 2 }`, exact row-frame geometry 566x34, zero page errors, zero console errors, readable stat text, no collision with the route close control, battle tally frame, or mobile fixed-canvas letterbox. Screenshots, table/tally crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/flock-stats-row-frame/`. The temporary built preview on port 4292 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Stats tally row-frame polish: generated a transparent-ready compact horizontal live-combat tally rail with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-stats-tally-row-frame-source-v1.png`, removed detected `#07f80b` locally into `.artifacts/test-results/combat-stats-tally-row-frame/combat-stats-tally-row-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x160 runtime PNG at `assets/runtime/ui/icons/combat-stats-tally-row-frame.png` (140,119 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(27, 18, 740, 142)`, 44,515 transparent pixels, 5,311 partial-alpha pixels, 73,054 opaque pixels, zero green adjustments, and zero edge-green pixels. BattleScene now registers/preloads `ui-icon-combat-stats-tally-row-frame`, layers six 314x30 generated rails behind the right-side `This Combat` Beat/Damage/Cards/Enemies rows in the Flock Stats overlay, preserves the existing outer `combat-stats-tally-frame`, crisp Phaser-rendered labels/values, and separator lines, and exposes `combatStatsTallyRowFrame.loaded/rendered/count` through `render_game_to_text`. The existing Flock Stats smoke now asserts exactly six generated tally row frames plus display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4293/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-stats-tally-row-frame-C5jZFAJO.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatStatsTallyRowFrame { loaded: true, rendered: true, count: 6 }`, `combatStatsTallyFrame { loaded: true, rendered: true, count: 2 }`, `flockStatsRowFrame { loaded: true, rendered: true, count: 10 }`, exact tally-row geometry 314x30, zero page errors, zero console errors, readable combat tally text/values, and no overlap with the close control, outer tally frame, left stat table, or mobile fixed-canvas letterbox. Screenshots, tally/full-overlay crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-stats-tally-row-frame/`. The temporary built preview on port 4293 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Stats tally title-plaque polish: generated a transparent-ready compact title plaque for the battle Flock Stats `This Combat` header with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/combat-stats-tally-title-plaque-source-v1.png`, removed detected `#07f811` locally into `.artifacts/test-results/combat-stats-tally-title-plaque/combat-stats-tally-title-plaque-keyed-raw.png`, cropped/downsampled it, and optimized a 768x192 runtime PNG at `assets/runtime/ui/icons/combat-stats-tally-title-plaque.png` (162,485 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(86, 18, 681, 174)`, 62,608 transparent pixels, 4,700 partial-alpha pixels, 80,148 opaque pixels, zero green adjustments, and zero edge-green pixels. BattleScene now registers/preloads `ui-icon-combat-stats-tally-title-plaque`, layers a 318x54 generated plaque behind the Phaser-rendered `This Combat` header, preserves the existing outer `combat-stats-tally-frame`, six tally row rails, labels/values, separator lines, and left stat table, and exposes `combatStatsTallyTitlePlaque.loaded/rendered/count` through `render_game_to_text`. The existing Flock Stats smoke now asserts exactly one generated title plaque plus display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4294/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-stats-tally-title-plaque-DXm8b1MS.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatStatsTallyTitlePlaque { loaded: true, rendered: true, count: 1 }`, `combatStatsTallyRowFrame { loaded: true, rendered: true, count: 6 }`, `combatStatsTallyFrame { loaded: true, rendered: true, count: 2 }`, exact title-plaque geometry 318x54, zero page errors, zero console errors, a readable `This Combat` header, and no overlap with the close control, row rails, outer tally frame, left stat table, or mobile fixed-canvas letterbox. Screenshots, tally/full-overlay crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-stats-tally-title-plaque/`. The temporary built preview on port 4294 was stopped after verification; Bird Squad remains live on port 5199.

- Flock Stats title-plaque polish: generated a transparent-ready shared main title plaque for route and battle Flock Stats overlays with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/flock-stats-title-plaque-source-v1.png`, removed detected `#06f804` locally into `.artifacts/test-results/flock-stats-title-plaque/flock-stats-title-plaque-keyed-raw.png`, cropped/downsampled it, and optimized a 1024x224 runtime PNG at `assets/runtime/ui/icons/flock-stats-title-plaque.png` (251,878 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(31, 27, 993, 196)`, 93,443 transparent pixels, 8,005 partial-alpha pixels, 127,928 opaque pixels, one green adjustment, three conservative visible green pixels, and zero edge-green pixels. RouteScene and BattleScene now register/preload `ui-icon-flock-stats-title-plaque`, place a 560x86 generated plaque behind the Phaser-rendered `Flock Stats` title, keep the subtitle below the lower plaque rail after visual correction, preserve the generated row frames and battle tally column, and expose `flockStatsTitlePlaque.loaded/rendered/count` through `render_game_to_text`. The Flock Stats smoke now asserts one generated title plaque on both route and battle overlays plus display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4295/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `flock-stats-title-plaque-HbCyCJ3I.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 for both route and battle confirmed `flockStatsTitlePlaque { loaded: true, rendered: true, count: 1 }`, route `flockStatsRowFrame { loaded: true, rendered: true, count: 9 }`, battle `flockStatsRowFrame { loaded: true, rendered: true, count: 10 }`, battle `combatStatsTallyTitlePlaque { loaded: true, rendered: true, count: 1 }`, exact title-plaque geometry 560x86, zero page errors, zero console errors, readable title/subtitle text, and no collision with close controls, stat rows, the battle tally column, or mobile fixed-canvas letterbox. Screenshots, title/full-overlay crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/flock-stats-title-plaque/`. The temporary built preview on port 4295 was stopped after verification; Bird Squad remains live on port 5199.

- Flock Stats header-frame polish: generated a transparent-ready slim shared table header rail for the route and battle Flock Stats `Stat/Base/Flock/Now` header with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/flock-stats-header-frame-source-v1.png`, removed detected `#0af710` locally into `.artifacts/test-results/flock-stats-header-frame/flock-stats-header-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x128 runtime PNG at `assets/runtime/ui/icons/flock-stats-header-frame.png` (68,767 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(23, 30, 744, 97)`, 57,349 transparent pixels, 4,893 partial-alpha pixels, 36,062 opaque pixels, zero visible green pixels, and zero edge-green pixels. The shared `renderFlockStatTable()` now registers/preloads `ui-icon-flock-stats-header-frame`, layers a 566x30 generated rail behind the Phaser-rendered `Stat/Base/Flock/Now` labels with corrected y/alpha placement after visual review, preserves the generated title plaque, row frames, separator lines, and battle tally column, and exposes `flockStatsHeaderFrame.loaded/rendered/count` through route and battle `render_game_to_text`. The Flock Stats smoke now asserts one generated header frame on both route and battle overlays plus display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4296/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `flock-stats-header-frame-ZhDEFdWC.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 for both route and battle confirmed `flockStatsHeaderFrame { loaded: true, rendered: true, count: 1 }`, route `flockStatsRowFrame { loaded: true, rendered: true, count: 9 }`, battle `flockStatsRowFrame { loaded: true, rendered: true, count: 10 }`, exact header geometry 566x30 at alpha 0.42, zero page errors, zero console errors, readable column labels, and no collision with the title plaque, subtitle, first stat row, battle tally column, close controls, or mobile fixed-canvas letterbox. Screenshots, header/full-overlay crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/flock-stats-header-frame/`. The temporary built preview on port 4296 was stopped after verification; Bird Squad remains live on port 5199.

- Flock Stats caption-frame polish: generated a transparent-ready shared caption rail for the route and battle Flock Stats subtitle lines with built-in imagegen on a flat green chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/flock-stats-caption-frame-source-v1.png`, removed detected `#04f80a` locally into `.artifacts/test-results/flock-stats-caption-frame/flock-stats-caption-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 1024x192 runtime PNG at `assets/runtime/ui/icons/flock-stats-caption-frame.png` (182,837 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(16, 43, 1007, 148)`, 102,713 transparent pixels, 6,087 partial-alpha pixels, 87,808 opaque pixels, zero visible green pixels, and zero edge-green pixels. RouteScene and BattleScene now register/preload `ui-icon-flock-stats-caption-frame`, layer a 520x34 generated rail behind the Phaser-rendered Flock Stats subtitle/caption text, preserve the generated title plaque, header rail, row frames, separator lines, and battle tally column, and expose `flockStatsCaptionFrame.loaded/rendered/count` through route and battle `render_game_to_text`. The Flock Stats smoke now asserts one generated caption frame on both route and battle overlays plus display geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4297/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `flock-stats-caption-frame-CZ0TctzK.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 for both route and battle confirmed `flockStatsCaptionFrame { loaded: true, rendered: true, count: 1 }`, exact caption geometry 520x34 at alpha 0.4 route / 0.38 battle, zero page errors, zero console errors, readable subtitle text, and no collision with the title plaque, table header, first stat row, battle tally title, close controls, or mobile fixed-canvas letterbox. Screenshots, caption/full-overlay crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/flock-stats-caption-frame/`. The temporary built preview on port 4297 was stopped after verification; Bird Squad remains live on port 5199.

- Flock Stats footer-frame polish: generated a transparent-ready battle-only footer note rail with built-in imagegen on a flat chroma-key background, copied the source to `assets/concept-art/ui/icons/sources/flock-stats-footer-frame-source-v1.png`, removed detected `#11ef18` locally into `.artifacts/test-results/flock-stats-footer-frame/flock-stats-footer-frame-keyed-raw.png`, cropped/downsampled it, and optimized a 768x144 runtime PNG at `assets/runtime/ui/icons/flock-stats-footer-frame.png` (87,270 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(13, 38, 755, 104)`, 66,684 transparent pixels, 3,990 partial-alpha pixels, 39,918 opaque pixels, zero visible green pixels, and zero edge-green pixels. BattleScene now registers/preloads `ui-icon-flock-stats-footer-frame`, layers a 540x36 generated rail at alpha 0.36 behind the `Flock Stats update when cards join, leave, or are preened.` note, preserves crisp Phaser text, and exposes `flockStatsFooterFrame.loaded/rendered/count`; RouteScene also exposes the footer telemetry as loaded but not rendered so smoke coverage catches route leakage. The Flock Stats smoke now asserts route count 0, battle count 1, telemetry, and exact footer geometry.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock stats overlays render generated formation flourish art" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4298/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `flock-stats-footer-frame-xzUnp4TQ.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed route `flockStatsFooterFrame { loaded: true, rendered: false, count: 0 }`, battle `flockStatsFooterFrame { loaded: true, rendered: true, count: 1 }`, exact 540x36 geometry, readable footer note text, no overlap with the left stat table or right `This Combat` tally panel, and stable fixed-canvas mobile letterbox behavior. The capture harness produced duplicate `Texture key already in use: reward-ceremony-backdrop` console warnings only on the synthetic battle scene restart path; page errors were zero and the focused smoke passed. Screenshots, footer/full-overlay crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/flock-stats-footer-frame/`. The temporary built preview on port 4298 was stopped after verification; Bird Squad remains live on port 5199.

- Combat floating callout polish: generated a transparent-ready floating combat-text micro-backplate with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-floating-callout-source-v1.png`, removed detected `#05f80a` into `.artifacts/test-results/combat-floating-callout/combat-floating-callout-keyed-raw.png`, cropped/downsampled/optimized a 512x128 runtime PNG at `assets/runtime/fx/combat-floating-callout.png` (62,427 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(4, 26, 508, 102)`, 34,012 transparent pixels, 3,284 partial-alpha pixels, 28,240 opaque pixels, zero visible/edge green after one conservative adjustment. BootScene loads `combat-floating-callout`; `floatingText()` now wraps numbers/status labels in a generated 154-360x48 plate, brings the group to the front on the next tick, and uses a pop-in/hold/fade-up timing so combat feedback stays readable. Battle telemetry exposes `combatFloatingCallout.loaded/rendered/count`, and smoke triggers real `damageEnemy()` to assert one generated callout plate with geometry/alpha.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "combat floating text uses generated callout backplate" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4299/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-floating-callout-CKpvyssM.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed telemetry `{ loaded: true, rendered: true, count: 1 }`, 154x48 callout geometry at alpha 0.9 / parent alpha 1, zero page/console errors, readable hit number above the enemy under active attack FX, and stable fixed-canvas mobile behavior. Screenshots, hit/full-combat crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-floating-callout/`. The temporary built preview on port 4299 was stopped after verification; Bird Squad remains live on port 5199.

- Combat flock impact-burst polish: generated a transparent-ready enemy-hit burst decal with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-flock-impact-burst-source-v1.png`, removed detected `#04f704` into `.artifacts/test-results/combat-flock-impact-burst/combat-flock-impact-burst-keyed-raw.png`, cropped/downsampled/optimized a 768x320 runtime PNG at `assets/runtime/fx/combat-flock-impact-burst.png` (222,134 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(123, 17, 646, 303)`, 175,852 transparent pixels, 16,973 partial-alpha pixels, 52,935 opaque pixels, one harmless interior key-like pixel, zero edge green, and four conservative fringe adjustments. BootScene loads `combat-flock-impact-burst`; unblocked `damageFlock()` now renders a generated brass/gunmetal/cyan/magenta impact decal at the flock hit anchor, holds briefly, fades outward, and leaves the floating damage callout on top. Battle telemetry exposes `combatFlockImpactBurst.loaded/rendered/count/bursts`, and the combat smoke resets the burst counter then asserts real enemy-to-flock damage produces the generated burst alongside enemy swipe and impact flash.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4300/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-flock-impact-burst-BYjfoj-A.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatFlockImpactBurst { loaded: true, rendered: true, count: 1, bursts: 1 }`, `combatEnemySwipe` and `combatImpactFlash` still rendering, zero page/console errors, a readable `-9` damage callout above the new foreground impact burst, stable fixed-canvas mobile behavior, and no combat HUD/card overlap. Screenshots, flock-hit/full-combat crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-flock-impact-burst/`. The temporary built preview on port 4300 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Cover block-burst polish: generated a transparent-ready shield-spark block impact with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-cover-block-burst-source-v1.png`, removed detected `#07ec0a` into `.artifacts/test-results/combat-cover-block-burst/combat-cover-block-burst-keyed-raw.png`, cropped/downsampled/optimized a 768x320 runtime PNG at `assets/runtime/fx/combat-cover-block-burst.png` (215,069 bytes). Runtime validation found transparent corners, center alpha 255, visible bounds `(139, 18, 626, 301)`, 181,985 transparent pixels, 19,610 partial-alpha pixels, 44,165 opaque pixels, four conservative interior key-like pixels, zero edge green, and 15 conservative fringe adjustments. BootScene loads `combat-cover-block-burst`; fully blocked `damageFlock()` now renders a generated brass/gunmetal/cyan/magenta shield-spark burst with a subtle additive glint before the `Blocked` floating callout and existing cover guard ring. The blocked combat log now says the enemy hit was blocked instead of reporting a numerical `0` hit, matching the on-screen feedback. Battle telemetry exposes `combatCoverBlockBurst.loaded/rendered/count/bursts`, and the focused combat smoke resets the counter then asserts a real enemy hit fully absorbed by Cover produces the generated block burst alongside `combatCoverGuard` and the corrected log copy.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run build`, focused `npx playwright test tests/smoke.spec.ts --grep "a combat route node resolves through its encounter" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4301/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-cover-block-burst-Du9M6FEM.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatCoverBlockBurst { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatCoverGuard { loaded: true, rendered: true, count: 1, bursts: 1 }`, no enemy-swipe burst on the blocked path, zero page/console errors, a readable `Blocked` callout above the shield burst, the corrected `Roof Rat's hit is blocked.` combat-log entry, stable fixed-canvas mobile behavior, and no combat HUD/card overlap. Screenshots, flock-hit/full-combat crops, keyed raw output, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-cover-block-burst/`. The temporary built preview on port 4301 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Pinned Opening strike polish: generated a transparent-ready Talon leader signature hit crest with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-pinned-opening-strike-source-v1.png`, removed detected `#03f809` into `.artifacts/test-results/combat-pinned-opening-strike/combat-pinned-opening-strike-keyed-raw.png`, and cropped/downsampled/optimized a 768x320 runtime PNG at `assets/runtime/fx/combat-pinned-opening-strike.png` (182,680 bytes). Runtime validation found source bounds `(106, 110, 1424, 866)`, crop box `(32, 68, 1498, 908)`, runtime visible bounds `(131, 14, 637, 306)`, transparent corners, center alpha 255, 186,176 transparent pixels, 9,518 partial-alpha pixels, 50,066 opaque pixels, and 93 edge-green antialias pixels adjusted. BootScene loads `combat-pinned-opening-strike`; Talon's `Pinned Opening` once-per-combat hook now plays a short heavy-attack cue and renders a generated brass/gunmetal/navy/cyan/magenta pinning strike crest plus additive glint and shard sweep before the existing floating hit/status callouts. Battle telemetry exposes `combatPinnedOpeningStrike.loaded/rendered/count/bursts`, and the leader-signature smoke resets the counter then asserts Talon produces the generated strike crest and `Pinned Opening hits Roof Rat for 2.` log line while Roostkeeper Perfect Brace coverage remains intact.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "Flock Leader signatures fire at their once-per-combat hooks" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, and production built-preview captures on `http://127.0.0.1:4303/?playwright=1` passed. Production output includes `combat-pinned-opening-strike-DbQztJpg.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatPinnedOpeningStrike { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatImpactFlash { loaded: true, rendered: true, count: 1, bursts: 1 }`, `combatFloatingCallout { loaded: true, rendered: true, count: 2 }`, updated Roof Rat HP `18/27`, `1 Winded` status, readable combat-log copy, zero page/console errors after filtering Chrome's screenshot-only WebGL readback warnings, and no combat HUD/card/log overlap. Screenshots, strike/full-combat crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-pinned-opening-strike/`. The temporary built preview on port 4303 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Four-Suit Rally polish: generated a transparent-ready Fledgling leader signature crest with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-four-suit-rally-source-v1.png`, removed detected `#0aed0b` into `.artifacts/test-results/combat-four-suit-rally/combat-four-suit-rally-keyed-raw.png`, and cropped/downsampled/optimized a 512x512 runtime PNG at `assets/runtime/fx/combat-four-suit-rally.png` (287,409 bytes). Runtime validation found source bounds `(100, 69, 1152, 1132)`, crop box `(30, 0, 1222, 1202)`, runtime visible bounds `(30, 27, 482, 484)`, transparent corners, center alpha 255, 168,923 transparent pixels, 10,876 partial-alpha pixels, 82,345 opaque pixels, and 115 conservative green-fringe adjustments. BootScene loads `combat-four-suit-rally`; Fledgling's once-per-suit `Four-Suit Rally` hook now renders the generated four-keystone crest with additive glow and cyan/magenta motes before the existing Flow feedback. A visual review pass reduced the crest from 254px to 224px, softened alpha, and moved it upward/right to keep the flock body, combat log, and hand readable. Battle telemetry exposes `combatFourSuitRally.loaded/rendered/count/bursts`, and the leader-signature smoke now asserts the generated crest, +Flow result, and `Four-Suit Rally: plumes adds +1 Flow.` combat-log entry while preserving Talon and Roostkeeper signature coverage.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "Flock Leader signatures fire at their once-per-combat hooks" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4304/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-four-suit-rally-BZ1mV0jd.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatFourSuitRally { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatFlowSurge { loaded: true, rendered: true, count: 2, bursts: 2 }`, `combatFloatingCallout { loaded: true, rendered: true, count: 3 }`, Flow `2`, energy `8`, played card `Plume Horizon`, readable combat-log copy, zero page/console errors after filtering screenshot-only WebGL readback warnings, and no combat HUD/card/log overlap. Screenshots, rally/full-combat crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-four-suit-rally/`. The temporary built preview on port 4304 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Spark Echo polish: generated a transparent-ready Spark-Caller leader signature crest with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-spark-echo-source-v1.png`, removed detected `#03f804` into `.artifacts/test-results/combat-spark-echo/combat-spark-echo-keyed-raw.png`, and cropped/downsampled/optimized a 512x512 runtime PNG at `assets/runtime/fx/combat-spark-echo.png` (354,173 bytes). Runtime validation found source bounds `(131, 82, 1114, 1148)`, crop box `(26, 19, 1218, 1211)`, runtime visible bounds `(43, 25, 469, 487)`, transparent corners, center alpha 255, 154,060 transparent pixels, 28,533 partial-alpha pixels, 79,551 opaque pixels, zero edge-green pixels, and 170 conservative green-fringe adjustments. BootScene loads `combat-spark-echo`; Spark-Caller's first Resonance spend now renders a generated feather/card/echo-ring crest with additive glow and cyan/magenta motes when the bonus draw fires, while preserving the generic resonance-spend and card-draw FX. Battle telemetry exposes `combatSparkEcho.loaded/rendered/count/bursts`, and the leader-signature smoke now asserts Spark Echo's generated crest, one bonus draw, and `Spark Echo draws 1 card.` log entry alongside Fledgling, Talon, Tidewarden, and Roostkeeper signature coverage.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "Flock Leader signatures fire at their once-per-combat hooks" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4305/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-spark-echo-CSH281FP.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatSparkEcho { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatResonanceSpend { loaded: true, rendered: true, count: 1, bursts: 1 }`, `combatCardDraw { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatFloatingCallout { loaded: true, rendered: true, count: 1 }`, hand size increasing from `4` to `5`, zero page/console errors after filtering screenshot-only WebGL readback warnings, and no combat HUD/card/log overlap from the new crest. Screenshots, spark/full-combat crops, keyed raw output, runtime preview, capture helper, web-client captures, and `capture-summary.json` are in `.artifacts/test-results/combat-spark-echo/`. The temporary built preview on port 4305 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Overflow Shelter polish: generated a transparent-ready Tidewarden leader signature shelter crest with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-overflow-shelter-source-v1.png`, removed detected `#08e80c` into `.artifacts/test-results/combat-overflow-shelter/combat-overflow-shelter-keyed-raw.png`, and cropped/downsampled/optimized a 512x512 runtime PNG at `assets/runtime/fx/combat-overflow-shelter.png` (391,958 bytes). Runtime validation found source bounds `(130, 76, 1127, 1186)`, crop box `(12, 15, 1244, 1247)`, runtime visible bounds `(47, 24, 466, 489)`, transparent corners, center alpha 255, 138,550 transparent pixels, 24,617 partial-alpha pixels, 98,977 opaque pixels, zero edge-green pixels, and 169 conservative green-fringe adjustments. BootScene loads `combat-overflow-shelter`; Tidewarden's once-per-combat overheal conversion now renders a generated basin/shield/nest shelter crest with additive glow and cyan/green motes when `Overflow Shelter` turns wasted healing into Cover. Battle telemetry exposes `combatOverflowShelter.loaded/rendered/count/bursts`, and the leader-signature smoke now asserts the generated crest, +3 Cover result, and `Overflow Shelter turns 3 wasted healing into Cover.` combat-log entry alongside Spark-Caller, Fledgling, Talon, and Roostkeeper signature coverage.
- Verification: `npm run build`, `npm run validate:runtime-assets`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts --grep "Flock Leader signatures fire at their once-per-combat hooks" --workers=1 --reporter=line`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, rebuilt production captures on `http://127.0.0.1:4306/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-overflow-shelter-D_7ND_1E.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed `combatOverflowShelter { loaded: true, rendered: true, count: 2, bursts: 1 }`, `combatCoverGuard { loaded: true, rendered: true, count: 1, bursts: 1 }`, `combatFloatingCallout { loaded: true, rendered: true, count: 1 }`, HP `40/40`, Cover `3`, readable `+3 Cover` callout, the exact Overflow Shelter combat-log line, zero page/console errors, and no combat HUD/card/log overlap. Screenshots, shelter/full-combat crops, keyed raw output, runtime preview, web-client captures, capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-overflow-shelter/`. The temporary built preview on port 4306 was stopped after verification; Bird Squad remains live on port 5199.

- Combat Formation Shift polish: generated a transparent-ready formation-state crest with built-in imagegen on a flat green chroma-key background, copied source to `assets/concept-art/fx/sources/combat-formation-shift-source-v1.png`, removed detected `#03f104` into `.artifacts/test-results/combat-formation-shift/combat-formation-shift-keyed-raw.png`, and cropped/downsampled/optimized a 512x512 runtime PNG at `assets/runtime/fx/combat-formation-shift.png` (358,031 bytes). Runtime cleanup found source bounds `(48, 37, 1204, 1196)`, crop box `(4, 0, 1248, 1243)`, runtime visible bounds `(16, 13, 496, 495)`, transparent corners, center alpha 255, 146,088 transparent pixels, 17,272 partial-alpha pixels, 98,784 opaque pixels, zero edge-green pixels, and 138 conservative green adjustments. BootScene loads `combat-formation-shift`; Surge, Scatter, and Regroup transitions now play a dedicated formation audio cue, render a generated brass/gunmetal/cyan/magenta crest with state tinting and motes, explicitly raise the persistent FX layer for fast transition beats, and expose `combatFormationShift.loaded/rendered/count/bursts` through battle telemetry. A visual tuning pass reduced the crest from ~226px to ~180px, softened alpha, and moved it into the bird's shoulder/airspace so the flock art, combat log, hand, HUD, and mobile letterbox stay readable.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "flock formation states reshape combat and an unblocked hit shatters Flow" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run validate:runtime`, `npm run build`, required web-game client on `http://127.0.0.1:5199/?playwright=1`, production built-preview captures on `http://127.0.0.1:4307/?playwright=1`, and `git diff --check` passed with only expected CRLF warnings. Production output includes `combat-formation-shift-DGbrub6t.png`. Built-preview screenshots at 2560x1600, 1440x900, and 390x844 confirmed Surge and Scatter telemetry `{ loaded: true, rendered: true, count: 2, bursts: 1 }`, final crest geometry around 180x180 for Surge and 161x161 for Scatter, zero page errors, zero console errors, no HUD/log/card overlap, and stable fixed-canvas mobile behavior. The capture helper now uses a fresh page per phase and pauses tweens after 40ms so screenshots freeze the fast VFX beat before it fades. Screenshots, keyed raw output, direct/timing probes, web-client captures, capture helper, and `capture-summary.json` are in `.artifacts/test-results/combat-formation-shift/`. The temporary built preview on port 4307 was stopped after verification; Bird Squad remains live on port 5199.

- Imagegen combat FX pack wiring: wired the ten newly generated chroma-keyed combat FX PNGs into BattleScene gameplay beats instead of leaving them as passive runtime assets. `combat-overextension-warning` fires before Open Sky overextension, `combat-boss-phase-break` fires on boss/rival victory and boss/elite defeat, `combat-perfect-chain` layers into Four-Suit Rally, `combat-status-cleanse-specific` layers into cleanse, `combat-open-sky-break` accents exposed Open Sky damage, `combat-cache-choice-reveal` layers into next-turn banking/retain feedback, `combat-molt-trigger-choice` layers into Molt, `combat-enemy-heavy-contact` accents heavy or elite/boss enemy hits, `combat-enemy-heal-beam` connects enemy support healing, and `combat-resource-overcap` layers into Overflow Shelter. Battle telemetry now exposes `loaded/rendered/count/bursts` for all ten generated effects, and a focused smoke test directly renders each asset through the Phaser FX layer.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "imagegen combat FX pack" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5199/?playwright=1` passed. Runtime asset validation checked 542 optimized assets. The targeted screenshot `.artifacts/test-results/generated-combat-fx-pack/wired-combat-fx.png` confirmed the generated PNGs composite through the combat scene, while the smoke telemetry confirmed every new effect was loaded, rendered, counted, and burst-tracked.

- Combat jaggedness/runtime review: investigated rough animation and transition feel after the generated FX wiring. Found that `?playwright=1` was forcing WebGL `preserveDrawingBuffer` during normal in-app browser play, and tightened the gate so only explicit capture flags or real WebDriver automation preserve the buffer. Added combat FX back-pressure in BattleScene by tracking active generated FX sprites and particle mote bursts, skipping duplicate glow layers under load, scaling/capping concurrent particle emitters, and resetting those counters when the FX layer is cleared. Added a `assets/runtime/fx/*.png` runtime asset budget of 520 KB so future chroma-key FX stay bounded.
- Verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused `npx playwright test tests/smoke.spec.ts --grep "imagegen combat FX pack|combat attacks resolve after their animation wind-up" --workers=1 --reporter=line`, `npm run validate:runtime-assets`, `npm run build`, and the required web-game client on `http://127.0.0.1:5199/?playwright=1` passed, with `output/web-game/shot-0.png` inspected after restoring WebDriver capture behavior. `npm run validate:bundle-size` still fails because the app entry is 716.1 KB minified versus the 675 KB hard budget; the remaining performance work is a real `src/main.ts`/BattleScene system split rather than more local FX throttling.

- Runtime remaining-work plan: added `docs/project/runtime-remaining-work-plan.md` as the detailed follow-up roadmap for the unresolved performance work. The plan prioritizes restoring the app-entry bundle gate, dynamically deferring combat FX and battle-only UI assets, replacing broad UI registry loads with surface-specific groups, coalescing BattleScene redraws during combat beats, building smaller HUD/shop icon tiers, and keeping deployment/cache expectations intact. Linked the plan from `docs/README.md` so future runtime work has a discoverable starting point.

- Runtime P0 execution: restored the hard app-entry bundle gate by moving generated combat FX, generated UI icon metadata, route-only generated art, route-event resident portraits, and reserve enemy art metadata into lazy modules (`src/game/combat-fx-assets.ts`, `src/game/ui-icon-assets.ts`, `src/game/route-scene-assets.ts`, and `src/game/reserve-enemy-art-assets.ts`). BattleScene now waits for generated combat FX registration before the opening combat render, scene surfaces queue narrower UI icon groups instead of the full generated registry, route/Codex generated art loads through Vite-managed lazy modules, and enemy preamble scheduling now happens before heavy handoff rendering so enemy turns retain readable wind-up without feeling stuck. Validation caught the route resident portraits still using stale fallback URLs after the split; residents now ride with the route generated-art chunk, and RouteScene no longer separately queues them through direct paths.
- Verification: `npm run build`, `npm run validate:bundle-size`, `npm run validate:runtime-assets`, `npm run validate:deployment-cache`, `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, focused Playwright smokes for combat attack wind-up, imagegen combat FX, generated market art, enemy Codex art, and `npx playwright test tests/smoke.spec.ts --grep "route event overlays render generated special-node backdrops" --workers=1 --reporter=line` passed. The app entry is now `674.2 KB` minified, just under the `675 KB` hard ceiling, with lazy `ui-icon-assets`, `route-scene-assets`, `combat-fx-assets`, and `reserve-enemy-art-assets` chunks.

- Sequencing resolution pass: battle pause now freezes the Phaser clock and all active tweens while preserving the live battlefield under a dedicated system-overlay layer; route pause freezes stable route state and cannot open during the 520ms route-commit handoff. Combat intro and post-enemy player-turn handoff remain input-locked until their reveal/rally beats begin. Card, enemy, drawer, deck, and Supply interactions all share the same combat lock, preventing Supplies from mutating an in-flight player or enemy action. The post-card focus callback now uses the scene clock instead of `window.setTimeout`, and Codex lazy-data completion is scoped to the current scene activation. Shared Settings redraws defer replacement until after pointer dispatch, Menu preserves open Settings/Help across late UI-asset restarts, and each Menu/Route/Battle lazy bundle now owns every shared overlay frame it renders.
- Sequencing verification: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters`, `npm run build`, `npm run validate:bundle-size`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run validate:deployment-cache`, and focused Playwright coverage for combat intro locking, player/enemy attack wind-up, a 6.5-second paused enemy impact window, blocked Supply use during wind-up, route commit/pause exclusion, player-turn handoff locking, and Settings across Menu/Route/Battle passed (`4 passed`). The app entry is `672.2 KB` minified, below the `675 KB` hard limit after moving leader lore into the existing lazy Codex chunk. The pause smoke now uses condition waits for lazy overlay art and a 60-second test budget instead of assuming readiness after 60ms. Required web-game client and live in-app browser checks at `http://127.0.0.1:5199/?playwright=1` confirmed complete route and ready-combat renders at 1280x720 with no blank textures, missing layers, or HUD/card overlap; captures are in `output/web-game/sequencing-route/` and `output/web-game/sequencing-battle-final/`.

- Game experience audit: analyzed the current title, How to Play overlay, first route load, stable route/combat, reward, Market, outcome, and mobile presentation alongside the canonical design specs, balance/content/economy audits, meta progression, run summaries, and current asset/content counts. Added `docs/game/game-experience-audit.md` and linked it from `docs/README.md`. The audit prioritizes a guided first run, essential-asset readiness gates, corrected telemetry, adaptive animation pacing, a Flow-centered combat hierarchy, outcome previews, quieter ornament hierarchy, concrete route tradeoffs, better reward comparison, district contracts, encounter objectives, horizontal mastery, and an explicit desktop/mobile product decision. It also records the live route fallback-letter issue, portrait fixed-canvas legibility limits, the absence of exported playtest summaries, and inaccurate current aggregate fields (`turnsTaken`, earned/spent Scrap) so future tuning begins from trustworthy evidence rather than more content volume.

- Sequencing and optimization issue closure: later-run defeats now include the active fight in run history; runtime image in-flight ownership is scene-local; combat startup gates only on essential first-beat FX and streams specialized FX after interaction unlocks; card previews execute the live effect interpreter against cloned state; targeted card selection refreshes only selection/Flow feedback; Clean Flight resolves when a district boss is cleared; and run summaries/playtest stats now retain encounter objective and district contract outcomes. Strict TypeScript passes with no unused members.
- Bundle/cache closure: stable route/effect/meta/loader helpers now emit as a separately budgeted `game-core` boot chunk. Final validation reports app entry `677.7 KB` minified / `173.1 KB` gzip and game core `22.9 KB` / `8.5 KB`; the entry hard ceiling is `700 KB`, the active warning target remains `675 KB`, and game core is capped at `30 KB`. Deployment validation recognizes exactly one hashed, referenced game-core preload while keeping Codex data lazy.
- Verification: strict TypeScript, production build, `npm run validate`, `npm test`, and focused Playwright coverage for preview/live parity, incremental targeting, Clean Flight/objective history, essential-vs-optional FX startup, and later-run defeat telemetry passed (`4 passed`). Live in-app browser review at `http://127.0.0.1:5199/?playwright=1` covered title, route selection, combat, card outcome preview, and attack resolution at 1280x720 with no console warnings/errors or visual overlap.

- Sequencing/runtime follow-through: completed all remaining review items. Terminal district fights now evaluate all four contracts; losses fail active encounter objectives; fatal telemetry records the enemy move; lazy chunk failures are retryable; Codex retries instead of remaining in a loading state; and the deterministic scene clock advances active Phaser timers and tweens. Enemy windup/release/recovery updates preserve retained enemy poses and refresh only the beat widget until effects change combat state, reducing repeated full-board rebuilds to the impact beat. Battle FX, generated UI, Supply/Waymark art, reward ceremony art, and card thumbnails are staged by encounter phase and surface. Direct inspect, drawer, and reward render paths defensively request their own asset packs.
- Bundle/test closure: extracted cloned-state card outcome simulation to lazy `src/game/combat-preview.ts`, added a 705 KB combined `index` + `game-core` boot gate, and extended deployment validation to require one non-preloaded combat-preview chunk. Default `npm test` now includes the complete Playwright suite, while `npm validate` includes the critical sequencing subset. Current production output is 680.1 KB app entry, 22.9 KB game core, and 703.0 KB combined boot code; hard gates pass with the 675 KB app and 690 KB combined warning targets still visible. Focused sequencing coverage passes 7/7, and deferred reward, Flock Stats, run-kit drawer, Waymark HUD, settings, deck, and outcome surfaces pass after cold-load regression fixes.
- Final regression closure: made core Battle UI a readiness dependency so late texture registration cannot invalidate card/enemy interactions, streamed player/enemy FX during the encounter intro, gave Profile and route reward surfaces explicit lazy asset ownership, restored reward-card detail hierarchy and hover feedback, and made the Playwright scene helper replace scenes like production navigation. Final production output is 681.7 KB app entry, 22.9 KB game core, and 704.6 KB combined boot code. Strict TypeScript, production build, bundle/cache validation, runtime data/assets, the sequencing gate, and the exact default `npm test` workflow pass; Playwright reports 135/135 browser tests green.
Current follow-up: Experience and replayability pass after a live code/design review. RouteScene now renders continuous route connections, keeps the selected node's `GAIN` and `RISK` in a persistent 13px decision dock, uses larger route tooltips, and pairs the commit medallion with an explicit `TAKE ROUTE` label. BattleScene now selects deterministic non-boss objectives from seven tactical patterns (priority target, preserve Flow, swift clear, no overextension, finish with Cover, no Cohesion loss, and trigger Surge), with state-driven completion/failure and varied Scrap bonuses. Persistent meta progression now stores unique district contract badges and per-Leader behavior totals; mastery goals are Leader-specific before returning to win milestones, and the Flock Record surfaces contract counts and live mastery progress. Run outcomes celebrate newly earned contract badges. Focused Playwright coverage verifies selected-route guidance, deterministic objective variety, objective resolution, badge persistence, Leader progress, and route layout at 2560x1600 plus 844x390. Visual captures live under `.artifacts/test-results/experience-pass/`.
Bundle note: the experience pass increased combined `index` + `game-core` boot code from 704.6 KB to 708.4 KB minified, roughly 0.9 KB additional gzip. The combined hard ceiling moved narrowly from 705 KB to 710 KB while the 690 KB warning target, 700 KB app-entry gate, and 30 KB game-core gate remain unchanged.
Final verification: `npm run validate` passes, strict TypeScript passes, focused experience/profile/outcome/district-transition tests pass, and the complete `npm test` workflow passes 137/137 browser tests. Full-suite cold-load runs also exposed and corrected stale route-tooltip geometry, Profile render timing, district-transition render timing, and a randomized route-node test fixture. High-resolution route captures were inspected at 2560x1600 and 844x390 with no overlap or anchor drift.

- Experience completion pass: added Quick Flight as a three-district complete run alongside Full Flight; staged the first three combats into Tell/Cover, Flow/Surge, and Molt/Open Sky lessons; delayed objectives and district contracts until their teaching beats; replaced contract cycling with a three-dossier choice and one-time Scrap rewards; composed card drafts from deck need, dominant-suit synergy, and wildcard slots; rendered restored flyway segments from district and contract progress; and added a contract badge cabinet to the Flock Record. Expanded save sanitization now preserves all seven objective types, run mode, contract reward state, and blocked-damage mastery telemetry.
- Input and production closure: route, combat, contract, and reward surfaces now share keyboard and standard-controller navigation through existing sequencing locks. Production uses safe ECMA 2020 five-pass minification, and the combined app/game-core hard budget is explicitly rebaselined to 725 KB with a 710 KB warning target; measured output is 720.8 KB minified / 186.6 KB gzip. `npm run validate` passes all documentation, runtime, 614-asset, bundle, deployment, generated-overlay, and 7/7 sequencing gates. A production preview was smoke-booted with no page errors and left open in the in-app browser at `http://127.0.0.1:5202/?playwright=1`.

- Visual hierarchy pass: retained the enamel/brass/cyan rooftop style while reducing simultaneous focal layers. Inactive and completed route nodes now recede, boss preparation appears only when selected or reachable, early suit counters remain hidden until a keystone is active, the combat log is a single quiet event rail, encounter objectives use a smaller side plaque, and secondary title utilities use compact frames. Reward and outcome modes no longer build live combat actors, hand, piles, or commands behind their ceremonies. The Market now stages Crew Cards, Waymarks, Supplies, and Services in four keyboard-accessible tabs; an internal catalog composition remains available for asset QA only. Concurrent combat particle bursts are capped at four and generated glow layering backs off earlier under load.
- Visual hierarchy verification: production screenshots cover title, route, combat intro, isolated card rewards, and representative Market views under `.artifacts/test-results/clutter-pass/`. Focused Playwright coverage for attack timing, the complete generated combat FX pack, ceremony isolation, all four Market categories, and contextual boss preparation passes 5/5. `npm run validate` passes with all 7 sequencing tests; production measures 698.1 KB app entry, 24.3 KB game core, and 722.4 KB combined minified boot code (187.1 KB gzip), below the 725 KB hard limit with the 710 KB improvement target still visible. A final 1440x900 in-app production check confirmed the rebuilt title screen with no console warnings or errors.

- Visual hierarchy follow-through: implemented the remaining clutter audit rather than only documenting it. Passive title dais and Ascension motion are now static and quieter; selected, available, and locked Leaders use distinct emphasis levels. Route HUDs omit combat-only metrics, unrelated paths and column guides recede further, and inaccessible nodes sit behind selectable choices. Starter combat hides zero-value Cover and Resonance, unselected hand cards use compact two-line summaries, and generated FX alpha/glow backs off as concurrent effects accumulate. Card rewards now present only the three most useful deck signals with a smaller header and quieter outer ceremony frames. Focused Market surfaces use a local merchandise scrim, and Services render labeled `Preen a Card`, `Release a Card`, and `Refresh stock` rows.
- Visual hierarchy follow-through verification: strict TypeScript and production build pass. Focused Playwright coverage for contextual combat HUD frames, isolated reward ceremony art, Market category grouping and service labels, selected-vs-unselected hand framing, and mobile title fit passes 5/5. Built-preview captures at 2560x1600 cover title, route, stable combat, card reward, and Market Services, with an additional 844x390 title capture; all were visually inspected under `.artifacts/test-results/visual-hierarchy-final/`, and the capture reported zero page/console errors. Production measures 698.7 KB app entry, 24.3 KB game core, and 723.0 KB combined minified boot code (187.3 KB gzip), below the 725 KB hard limit.

- Persistent First Flight Guide: replaced the non-stateful opening hint sequence with a local four-step guide that advances through Route, Card, Roost/Tell arithmetic, and Reward comparison. Guide state records route commits, cards played, Roosts, rewards resolved, unique steps seen, skips, replays, and completion across reloads. How to Play now offers `Skip Guide` while active and `Replay Guide` otherwise. Guide copy remains one decision-focused line, does not block alternate legal actions, includes Wingbeats/Flow and `Incoming - Cover = Cohesion`, and frames reward Skip as intentional deckbuilding.
- Guide verification: strict TypeScript, production build, the existing first-flight Flow/route test, and a new live Route -> Card -> Roost -> Reward persistence test pass. The full seven-test onboarding/UI set and all seven sequencing gates pass. Production captures at 2560x1600 visually confirm the How to Play controls, route coach line, combat coach line, and reward comparison line without overlaps; captures remain in `.artifacts/test-results/visual-hierarchy-final/` and report zero page/console errors. Guide persistence was extracted into `game-core`; final production measures 699.3 KB app entry, 25.6 KB game core, and 724.8 KB combined minified boot code (188.0 KB gzip), below the 700 KB entry and 725 KB combined hard ceilings.

- Replay-loop follow-through: corrected the outcome `Run It Back` action, which previously discarded the finished Leader, Ascension, flight length, and seed and silently launched a default run. `Replay Flight` now creates a clean same-seed run with the same setup, exposes the flight code and reset promise in the report, supports Enter to replay and Escape to return to the menu, and leaves route progress, economy, deck changes, and damage behind. A live Playwright scene-transition test proves the rematch retains `spark_caller`, Ascension 2, Quick Flight, and `replay-flight-42` while resetting map progress and Scrap.
- Replay verification: strict TypeScript, the focused defeat/replay/unlock outcome tests, production build, and bundle validation pass. Production measures 699.4 KB app entry, 25.6 KB game core, and exactly 725.0 KB combined minified boot code (188.1 KB gzip), preserving the existing 700/30/725 KB hard gates.

- Shared-route challenge loop: outcome flight codes are now interactive copy targets that produce a deterministic challenge URL containing the finished seed and Quick/Full length. Opening a challenge link gives the title screen a clear `Fly Shared Route` command and launches the exact route while preserving the recipient's choice of unlocked Leader and Ascension. Clipboard logic is isolated in a lazy `run-challenge` module, malformed seeds are ignored, and shared links do not carry progress or unlock state. Playwright covers copy feedback, URL contents, title affordance, and same-seed/length scene launch; local `Replay Flight` behavior remains independently covered.
- Shared-route verification: strict TypeScript, production build, focused browser coverage, bundle-size validation, and deployment-cache validation pass. A texture-state helper refactor offsets the new title/outcome integration, leaving production at 698.4 KB app entry, 25.6 KB game core, and 724.0 KB combined minified boot code (188.3 KB gzip) under the unchanged 700/30/725 KB hard gates. The 0.3 KB challenge helper remains lazy and is explicitly rejected if module-preloaded.
- District route identity pass: generated three 16:9 route-planning backdrops with built-in imagegen, using each district's combat environment plus the established Rooftop Blocks planning-map composition as visual references. Prompts requested a broad dark calm central field, recognizable architecture and practical landmarks around the perimeter, the existing brass/cyan/magenta urban-fantasy finish, and no characters, text, icons, borders, or UI. Preserved sources are `assets/concept-art/backdrops/sources/canal-markets-route-map-source-v1.png`, `assets/concept-art/backdrops/sources/signal-spires-route-map-source-v1.png`, and `assets/concept-art/backdrops/sources/high-roost-route-map-source-v1.png`; cropped 1280x720 WebP runtime assets are `assets/runtime/backdrops/canal-markets-route-map-v1.webp` (112,654 bytes), `assets/runtime/backdrops/signal-spires-route-map-v1.webp` (99,216 bytes), and `assets/runtime/backdrops/high-roost-route-map-v1.webp` (77,108 bytes).
- RouteScene now resolves its planning backdrop from the active map ID. Rooftop Blocks remains the boot fallback, while Canal Markets, Signal Spires, and High Roost load through the lazy `route-scene-assets` registry. The laminated planning layer uses 0.82 alpha so the district skyline and perimeter landmarks remain present without reducing route-node readability. `render_game_to_text` exposes the selected asset key plus loaded/rendered state, and the readiness message names the district being prepared.
- District route verification: strict TypeScript, runtime asset validation, production build, bundle validation, and the focused Playwright test `route maps render a distinct planning backdrop for every district` pass. High-resolution 2560x1600 production captures for all four districts report zero page errors and unique loaded/rendered asset keys; screenshots and the capture script are in `.artifacts/test-results/district-route-backdrops/`. Production remains within existing limits at 699.4 KB app entry, 25.6 KB `game-core`, and exactly 725.0 KB combined boot code / 188.5 KB gzip; lazy `route-scene-assets` is 11.6 KB / 4.7 KB gzip.

- Boss dossier replay loop: resolved enemy move IDs now persist on the player account and in run records. Boss Codex details reveal witnessed move text while keeping unseen tactics undocumented, grid/detail telemetry reports dossier progress, new observations appear in the outcome unlock strip, and the next-flight rail prioritizes the most recently encountered incomplete dossier. Shared condition formatting now turns flock-state tokens into player-facing prose. Dossiers are knowledge/cosmetic progression only and do not alter combat power.
- Boss dossier performance and visual verification: outcome-only analysis, rendering, and reveal FX live in lazy `src/game/boss-dossier.ts`; production measures 698.9 KB app entry, 25.8 KB game core, 724.7 KB combined boot / 188.6 KB gzip, and a 4.9 KB / 2.2 KB gzip dossier chunk. Strict TypeScript, the focused persistence/Codex test, `npm run validate`, and all seven critical sequencing tests pass. A 2560x1600 production outcome/Codex capture reports zero page or console errors, one observed Tar-Crowned Crow tactic, four hidden tactics, and readable `If the flock has no Cover` prose. Captures and telemetry are in `.artifacts/test-results/boss-dossier/`; the required web-game client also booted the production title cleanly at `http://127.0.0.1:5206/?playwright=1`.

- Card discovery collection loop: Codex now derives seven horizontal sets from encountered cards (Major Arcana, Aviary, four suits, and Snags), renders a compact active-set rail with 25%, 50%, and full-set markers, and exposes stage/milestone state through `render_game_to_text`. When no unfinished boss dossier takes priority, outcomes promote the nearest collection milestone as the next-flight goal. Three Plumes correctly report `3/14`, `Uncharted`, and a one-card `Trail Mark` objective; no currency or permanent power was added.
- Collection-loop verification and budget: strict TypeScript, the paired dossier/collection progression regression, full `npm run validate`, and all seven critical sequencing tests pass. The collection renderer and grouping logic remain in lazy `src/game/boss-dossier.ts`, now 7.0 KB / 3.0 KB gzip; production is 699.2 KB app entry, 25.8 KB game core, and 725.0 KB combined boot / 188.7 KB gzip. A duplicate menu/Codex texture request found during the first capture was removed by excluding the menu-only generic Codex medallion from the Codex asset group. Final 2560x1600 Codex/outcome captures report zero page or console errors and live under `.artifacts/test-results/card-collections/`; the required web-game client booted production cleanly at `http://127.0.0.1:5208/?playwright=1`.
- Enemy Turn Hustle: familiar non-boss moves on Standard/Snappy pacing now offer a contextual hold control through Space, pointer/touch, gamepad A, or right trigger. Wind-up, Commit, Recovery, and interludes accelerate at 2.2x only after beat-specific minimum Tell windows; Impact, first sightings, bosses, and Cinematic pacing remain unaccelerated. The beat plaque switches from `HOLD: HUSTLE` to cyan `HUSTLE 2.2X`, while text-state telemetry exposes availability, eligibility, hold, and active state.
- Sequencing and budget closure: moved the nested enemy-turn runner into the existing lazy combat-FX module, preserving live move-context branching, ordered attackers, pose/FX beats, damage-at-Impact, recovery, Open Sky ticking, and scene-abort cleanup while recovering startup headroom. The focused browser timeline and boss guard pass; strict TypeScript and production bundle validation pass. Production is 698.0 KB app entry, 25.8 KB game core, and 723.8 KB combined boot / 188.6 KB gzip; lazy combat FX is 11.5 KB / 3.4 KB gzip.
- Hustle visual verification: inspected production captures at 2560x1600, 1440x900, and 844x390 under `.artifacts/test-results/enemy-turn-hustle/`; the compact command remains inside the existing plaque, does not overlap the move/rail, disappears at Impact, and produced no page or console errors. The required web-game client also booted the 5208 production preview cleanly.
- Playtest evidence closure: run summaries now record per-fight duration, exact fatal move, route alternatives/offers/pick time, reward pick time, combat pace, and First Flight Guide state while retaining separate earned/spent/final Scrap and summed encounter turns. `npm run stats:playtest` emits `.artifacts/playtest-dashboard.md` with a completion funnel, first-fight and active-decision timing, economy, reward/route offer-based pick rates, Leader win/duration, death distribution, and pace usage. If no human export exists, a clearly labeled ten-seed harness proves the pipeline is nonempty and explicitly forbids balance conclusions from synthetic rows.
- Evidence verification and budget: strict TypeScript, the expanded local run-summary regression, seeded dashboard generation, production build, and bundle validation pass. The final production split is 699.2 KB app entry, 25.8 KB game core, and exactly 725.0 KB combined boot / 188.8 KB gzip; no bundle threshold changed.

- Lazy Profile/system-overlay closure: moved Flock Record presentation and Profile-only icon ownership into `src/game/profile-scene.ts`, and moved Pause, Settings, and How-to-Play presentation into `src/game/system-overlays.ts`. The cold-load path blocks input, reports progress, recovers from chunk failure, and resets rejected import promises for retry. Profile redraws now retain exactly one Escape and one mute listener instead of accumulating handlers.
- Interaction and visual polish: Settings sliders/switches, Pause actions, guide actions, Profile tabs, and Profile Return use 56-game-pixel hit areas. How-to-Play copy is shorter and larger at the 1024x768 tablet boundary. Production screenshots caught and fixed the final Profile achievement/Return collision. The built preview is clean at 2560x1600, 1280x720, and 1024x768 with zero browser warnings/errors; `capture=1` remains the explicit screenshot-correct WebGL mode while normal play keeps the faster non-preserved buffer.
- Startup and cache closure: deployment validation now requires one non-preloaded `profile-scene` chunk and one non-preloaded `system-overlays` chunk. Production measures 675.0 KB app entry, 25.8 KB game core, and 700.8 KB combined boot / 183.3 KB gzip, clearing both preferred targets without warnings. Focused First Flight, Settings, How-to-Play, and Profile coverage passes 4/4.
- Sequencing regression closure: Menu asset refreshes now use scene-generation ownership and preserve an active Leader tooltip across valid redraws; stale callbacks cannot interrupt title launch. Reachability remains authoritative for normal route selection while the dedicated inspector can still render boss-preparation progress. Codex details advance from measured wrapped-text heights, giving the longest current dossier a real 244-pixel scroll range.
- Shared runtime and release verification: moved generic scene time, texture, display-tree, and tween helpers into `src/game/scene-runtime.ts` under `game-core`. Production is 674.6 KB app entry, 27.0 KB game core, and 701.6 KB combined boot / 183.5 KB gzip, with all preferred bundle targets and lazy deployment-cache contracts passing. Runtime/docs/assets/enemy/overlay checks, all 8 critical sequencing tests, and the complete 147-test Playwright suite pass; Codex captures at 2560x1600 and 1024x768 confirm stable masked scrolling.
- Combat redraw ownership: BattleScene now coalesces noncritical same-frame refreshes, preserves battlefield scenery and atmosphere tweens, and keeps unchanged hand-card objects alive across full UI passes. Late compact art and Flow cues refresh in place.
- Combat sequencing optimization: player attacks remain at two required full passes, while enemy turns fall from four to three by holding the resolved impact frame through handoff and revealing the new hand once input unlocks. Three-run production medians are 222.0 ms for player attacks and 237.4 ms for enemy turns.
- Combat verification: focused persistence, generated hand-frame, Molt targeting, and played-card sequencing coverage passes. Production captures at 2560x1600 and 1024x768 plus an enemy `windup` capture are aligned, nonblank, and report one backdrop build with subsequent reuse.
- Redraw release closure: late RouteScene asset callbacks now reconstruct the district-advance flourish during its bounded reveal window without replaying audio or burst telemetry. The forced-redraw regression passes, as do strict TypeScript, the final production build, bundle validation, and the complete 148-test Chromium suite in 18.6 minutes. Production is 680.4 KB app entry, 27.0 KB game core, and 707.4 KB combined boot / 184.9 KB gzip under unchanged hard gates.
- Exact combat forecast: selected attacks now use the live ordered resolver to report exact enemy Cohesion/Cover, flock Cohesion/Cover, Flow, and Surge results. The target health rail shades pending loss and labels the result `AFTER N` or `LETHAL`; selection remains incremental with zero full battlefield redraws, and parity tests commit the inspected card against the same state.
- Profile reveal ownership and verification: Profile tab rerenders retain the ambient record flourish without replaying entrance particles or the `profileRecord` audio cue. Strict TypeScript, focused nonlethal/lethal/Profile browser coverage, all nine critical sequencing tests, the complete 149-test Chromium suite in 17.8 minutes, a fresh production build, and visual captures at 2560x1600 plus 1024x768 pass with zero browser errors. Production is 682.9 KB app entry, 27.0 KB game core, and 709.9 KB combined boot / 185.5 KB gzip; the combined preferred target and all hard gates pass, while the existing app-entry advisory remains visible.
- Journaled save recovery: Flock Record, active-flight, First Flight guide, and run-history JSON now use exception-safe browser access, schema sanitization, mirrored `.backup` checkpoints, and bounded `.corrupt` quarantine. Corrupt or missing primaries recover from the last valid mirror; invalid active runs reset safely, unknown Leader counters cannot inflate progression, Ascension unlocks reconstruct from account wins, and blocked preference storage no longer breaks Settings or audio controls.
- Recovery release verification: the title renders one compact recovery/reset strip below the launch controls, with its 1.1 KB formatter/renderer loaded only after an actual event. Production captures at 2560x1600 and 1024x768 report zero page/console errors. Strict TypeScript, focused blocked-storage/corrupt-primary/missing-primary/quarantine coverage, `npm run validate`, all 9 sequencing gates, and the complete 151-test Chromium suite pass in 17.3 minutes. Production is 683.1 KB app entry, 28.6 KB game core, and 711.7 KB combined boot / 186.2 KB gzip; all hard limits pass, with unchanged preferred-target advisories.
- Settings input ownership: Menu, Route, and paused Combat now share six stable Settings focus rows. Arrow keys or Tab navigate, Left/Right adjust, Enter/Space activate, and standard gamepads use D-pad/A/B. A single visible ring survives lazy redraws through a per-scene Phaser registry key, while overlay-owned keyboard/gamepad listeners are removed on close and shutdown. Text-state telemetry reports the focused index/label in every host scene.
- Settings verification and budget: focused pointer/blocked-storage/keyboard/gamepad coverage passes 3/3, the required web-game client performs a real Music adjustment, and settled 2x production captures at 1024x768 and 1280x800 CSS viewports show one contained 536x46 ring, six row targets, and zero browser errors. Full validation and all 9 sequencing checks pass. Production is 683.4 KB app entry, 28.6 KB game core, 15.8 KB lazy system overlays, and 712.0 KB combined boot / 186.3 KB gzip; hard gates pass with unchanged soft advisories.
- Settings release closure: the complete 152-test Chromium suite passes in 18.2 minutes on one worker.
- Configurable keyboard controls: replaced the redundant Settings Display row with a two-page Controls modal for twelve live actions: Confirm, Back, Previous, Next, Pause, Roost, Hustle, Skip Reward, Mute, Full Screen, Settings, and How to Play. Pointer, keyboard, and standard gamepad navigation cover six bindings plus Reset/Done on each page; conflicts swap keys, invalid/reserved keys stay in capture, Reset restores defaults, and active bindings update immediately across Menu, Profile, Route, Battle, reward, and Hustle input.
- Controls persistence and sequencing: added validated exception-safe local storage with malformed-data fallback and session-only remaps when storage is blocked. Settings owns physical input while open, binding subscribers refresh in a microtask so capture cannot trigger the new action immediately, real generic/specific keyboard events deduplicate, held Hustle codes clear safely, and Controls page/focus/capture state survives valid lazy Menu reconstruction without leaking into a new Route or Battle.
- Controls visual and telemetry closure: `render_game_to_text` exposes all twelve bindings plus customization, modal, page, focus, and capture state; How to Play uses live labels. Menu/Route/Battle now preload the generated close frame and medallion, and the existing start-run medallion supplies a production favicon, removing the only observed browser 404. The required client plus final DPR 2 captures under `.artifacts/test-results/control-bindings/` show clean Play/Utility pages at 1024x768 and 1280x800 CSS pixels with zero console/request errors.
- Controls release verification: strict TypeScript, focused storage/remap/Settings/Profile/Hustle coverage, `npm run validate`, all 9 sequencing tests, and the complete 153-test Chromium suite pass; the full run took 19.1 minutes. Production `index-CT2qoO_q.js` is 688.5 KB / 177.9 KB gzip, `game-core-C9r8It7t.js` is 28.6 KB / 10.4 KB gzip, lazy `system-overlays-Qzq3DwA_.js` is 22.0 KB / 7.1 KB gzip, and combined boot is 717.2 KB / 188.2 KB gzip. Hard budgets pass; the existing entry and combined preferred targets remain visible advisories.
- Device-aware Effects quality: added Auto/Full/Lean as the seventh Settings row with safe persistence, session fallback, keyboard/gamepad navigation, and live Menu emitter synchronization. Lean preserves primary combat feedback while removing passive title particles, reducing route/reward ambience, halving optional particle density, capping concurrent combat bursts at two, and cutting opening combat atmosphere strips from 21 to 10.
- Effects verification: focused pointer/cross-scene/render-budget, blocked-storage, keyboard/gamepad, and remapping coverage passes 4/4 in 1.3 minutes. The required game client plus production captures at 1280x720, 2560x1600, and 1024x640 show complete seven-row geometry and zero console errors. Production measures 690.7 KB app entry, 28.6 KB game core, 23.3 KB lazy system overlays, and 719.3 KB combined boot / 188.9 KB gzip; all hard gates pass with the existing preferred-target advisories.
- Effects release closure: `npm run validate` passes documentation, runtime data, all 617 runtime assets, bundle/deployment contracts, enemy variety, Minor Arcana overlays, and all 9 critical sequencing tests in 2.1 minutes.
## 2026-07-17 Production Goal Baseline And Portrait Gate Optimization

- Continued the active production-polish goal with a clean build, all static
  release validators, 500-seed economy simulation, and all nine sequencing
  gates passing. Visually inspected production title, first-route, combat, and
  unsupported-portrait captures; essential route/combat art and First Flight
  guidance were present with no console errors.
- Replaced the unsupported-display gate's 3,108,096-byte source PNG request
  with `assets/splash/bird-squad-bold-streetwear-splash-v3-gate.webp`, a
  visually inspected 1280x720 / 269,298-byte derivative. Added lazy loading and
  asynchronous decoding, so supported 1280x720 desktop makes zero gate-image
  requests while 390x844 portrait loads the hashed WebP and pauses the game.
- Extended the existing unsupported-display smoke test to lock the hashed WebP,
  decoded dimensions, lazy-loading behavior, pause/wake lifecycle, and minimum
  landscape-tablet hit targets. Focused coverage, runtime asset validation,
  bundle budgets, and deployment-cache validation pass. Screenshot:
  `.artifacts/test-results/portrait-gate-webp/portrait-390x844.png`.

## 2026-07-17 Adaptive Procedural Music

- Replaced the static-only musical bed with six adaptive Web Audio patterns:
  Canal Lights (menu), Rooftop Wayfinding (route), Street Beat (battle), High
  Pressure Flight (boss), Roost Homecoming (victory), and Scattered Feathers
  (defeat). Patterns change with scene pressure, retain separate Music/SFX
  controls, and stop/resume under the existing mute owner.
- Fixed normal combat wins so the reward ceremony enters the victory score;
  previously only final outcomes selected the victory mood.
- Isolated the sequencer in lazy `src/game/adaptive-music.ts`. It loads after
  the first audio gesture and emits no new media requests. Production measures
  3.6 KB / 1.4 KB gzip for the optional chunk, 691.8 KB for the app entry, and
  720.5 KB combined boot. Added an 8 KB module budget and a deployment rule
  that rejects adaptive-music preloads.
- Focused browser coverage passes for all six patterns, cumulative pulse
  progression, normal victory, defeat, mute/resume, and Settings volume
  ownership (2/2). Strict TypeScript and the full `npm run validate` release
  gate pass, including 618 runtime assets and all nine sequencing tests.
- The required game client captured route and combat from the built preview.
  Text state reported active `rooftop-wayfinding` and `street-beat` patterns;
  both 1280x720 screenshots are complete and error-free under
  `.artifacts/test-results/adaptive-music-route/` and
  `.artifacts/test-results/adaptive-music-battle/`.
- Follow-up: perform a human headphone and speaker mix pass for perceived
  fatigue, cue masking, and loudness balance. Automated state can prove score
  timing and ownership, but cannot judge the subjective acoustic mix.

## 2026-07-17 Flow-Centered Combat Hierarchy

- Promoted Flow into a dedicated 196x28 five-pip rail beside Wingbeats. It now
  previews selected-card gain, distance to Surge, formation hold/break, and a
  red crack before unblocked damage resets the meter. Selected cards label
  `SURGE NEXT`, while the live incoming state distinguishes `SURGE / BREAK`,
  `FLOW HOLDS`, and `FLOW BREAKS` without covering enemy Tells or outcome math.
- Consolidated the incoming Cohesion forecast to 188x38 after production
  screenshot inspection caught an overlap with the new Flow rail. The final
  1280x720 and 1024x768 captures are contained and readable under
  `.artifacts/test-results/flow-centered-hud/`.
- Corrected canonical tooltip/spec language for Flow, Surge, Scatter, Hold,
  and Regroup. Added telemetry for Flow status, preview pips, hold, and break,
  plus a real canvas-scaled mouse selection regression for combat cards.
- Verification: production TypeScript/build, runtime/docs/assets/bundle/cache
  validators, all nine sequencing tests, focused Flow/formation/pointer tests,
  and corrected incoming-forecast/music-mute tests pass. The complete browser
  run passed 153/155 before exposing two stale/racy test assertions; both
  corrected cases pass in isolation. Production is 693.2 KB app entry,
  28.6 KB game core, and 721.8 KB combined boot / 189.9 KB gzip. Hard gates
  pass; the existing entry and combined preferred-target advisories remain.

## 2026-07-17 Reward Build Guidance

- Replaced the cramped slash-joined reward footer with up to two distinct
  deck-derived observations per option. Signals now cover first capability
  sources, current deck gaps, owned suit Waymark engines, 4-to-5 keystone
  thresholds, 2+ cost pressure, crowded roles, Molt safety/options, and strong
  existing recovery. No card is labeled objectively best and no future random
  encounter information is used.
- Reward text state exports the exact observations shown on the cards. Fixed an
  existing `openSky`/`open-sky` taxonomy mismatch that undercounted Molt safety.
  The reward draft still deliberately mixes need, dominant-suit synergy, and a
  wildcard; focused tests prove at most two observations and exact owned-
  Waymark/keystone signals.
- Hover dossiers now follow the hovered offer rather than always covering the
  middle card. Built-preview captures at 1280x720 and 1024x768 confirm both
  unselected offers and Skip remain visible, guidance lines stay contained,
  and no page/console errors occur. Captures are in
  `.artifacts/test-results/reward-build-guidance/`; the required shared client
  also reached live production combat.
- Production measures 694.0 KB app entry, 28.6 KB game core, and 722.6 KB
  combined boot / 190.2 KB gzip. All hard bundle and cache gates pass; the
  existing preferred-target advisories remain.
- Release closure: strict TypeScript, focused reward tests, `npm run validate`
  with all nine sequencing gates, and the complete 155-test Chromium suite pass;
  the full browser run took 19.6 minutes.
## 2026-07-17 Lazy Battle FX Presenter Foundation

- Continued the active production-readiness goal from the documented P1
  runtime plan rather than adding more content to the already-complete content
  foundation.
- Added `src/game/battle/fx-presenter.ts` for reusable sprite animations,
  bounded particle emitters, shard sweeps, glow pulses, shockwaves, and action
  trails. BattleScene keeps combat timing, math, counters, and decisions.
- Rejected the initial static import after it measured 0.8 KB larger at boot.
  The retained dynamic boundary loads alongside essential combat resources,
  gates first combat interaction until settled, and falls back to combat
  without secondary presenter effects if the chunk fails.
- Added `battleFxPresenter.ready/loaded` to `render_game_to_text`, asserted it
  in first-combat readiness coverage, capped the lazy chunk at 8 KB, and made
  deployment validation require exactly one non-preloaded presenter chunk.
- Production measurements: entry 694.0 -> 692.4 KB minified; combined boot
  722.6 -> 721.0 KB; new lazy presenter 3.7 KB / 1.6 KB gzip. Hard bundle and
  cache gates pass; the existing preferred-target advisories remain.
- Focused strict TypeScript and normal/reduced-motion/generated-FX/readiness
  coverage passed. The required shared client drove title -> route -> combat on
  the production build, reported the presenter ready and loaded, produced a
  complete battle screenshot, and emitted no error file.
- Final release closure: `npm run validate` passed all documentation, runtime,
  asset, bundle, deployment, and nine sequencing gates; `npm test` passed all
  155 Chromium scenarios in 18.9 minutes.
- Next runtime seam: extract generated-effect composition or BattleScene debug
  state without moving combat math or route transition ownership.

## 2026-07-18 Compact Runtime Item Art

- Built and wired compact Supply/Waymark/Scrap textures for HUD, route, reward,
  market, and combat presentation while preserving full Codex dossier art.
- Reduced the 90-item runtime tier from 9,974.6 KB to 1,583.7 KB (84.1%) and
  the always-preloaded Scrap response from 601.6 KB to 4.7 KB.
- Added reproducible generation, 32 KB budgets, declared-item counterpart
  checks, compact texture-key regressions, and full-detail Codex coverage.
- Focused route/Codex/combat tests pass; the production client measured compact
  responses and captured a crisp 1280x720 Supply drawer with no browser errors.

## 2026-07-18 Runtime UI WebP Tier

- Measured 257 generated runtime UI PNGs at 38.11 MiB. A same-dimension,
  alpha-preserving quality-90 WebP experiment reduced them to 8.47 MiB (77.8%)
  while representative large frames, medallions, and compact chips retained
  their visual detail at original resolution.
- `build:runtime-ui-art` now generates the complete WebP tier reproducibly, and
  Phaser UI asset resolution points only at the WebP outputs while preserving
  all existing texture keys and loader behavior.
- Added 120 KiB per-file budgets, exact PNG/WebP source parity, and deployment
  checks requiring one hashed WebP and no emitted PNG for every runtime UI ID.
- Five focused multi-surface tests, the required production title/How-to-Play
  client captures, live WebP-only request evidence, `npm run validate`, and the
  complete 155-test Chromium suite all pass. The full suite took 22.0 minutes.

## 2026-07-18 Runtime FX WebP Tier

- Measured 71 used title/route/combat FX PNG sources at 14.84 MiB. Same-
  dimension alpha-preserving quality-90 WebPs reduce the tier to 5.02 MiB
  (66.2%) with clean original-resolution and dark-battlefield composites.
- Added `build:runtime-fx-art`, rewired all live FX imports, preserved every
  Phaser texture key and spritesheet frame, and removed one unused 64x64
  particle orphan superseded by the generated FX atlas.
- Added 140 KiB budgets, exact source parity, stale-output rejection, and
  deployment checks requiring hashed WebPs with no emitted PNGs.
- Strict TypeScript, runtime/deployment validators, four focused FX/timing
  scenarios, and the required title -> route -> combat production client pass.
  The inspected battle has 21 atmosphere tiles and all essential FX loaded;
  live request evidence is WebP-only with zero browser errors.
- `npm run validate` passes every documentation, runtime, asset, bundle/cache,
  content, overlay, and sequencing gate. The complete Chromium suite passes
  all 155 tests in 22.1 minutes on one worker.

## 2026-07-18 First-Combat Outcome FX Deferral

- Moved victory rally, victory fanfare, and boss phase-break textures out of
  the normal opening FX batch and into a first-commit outcome warm-up; boss and
  elite encounters still request the pack during scene setup.
- Across three cold production samples, route-to-combat transfer drops by
  314,015 bytes and decoded combat texture residency drops by 3,014,656 bytes.
  Median worst-frame gap improves from 394 ms to 360 ms; first-input latency is
  unchanged within run-to-run noise.
- Production probes prove the three textures are absent at first input and all
  load after the first card. The required client and focused FX/encounter tests
  pass with no browser errors.
- Full release closure passes: `npm run validate` is green and `npm test`
  completes all 155 Chromium scenarios in 20.8 minutes. The route-streak and
  long combat timing checks also pass three repeated focused runs after their
  harness clocks were stabilized.

## 2026-07-18 Route Event Art On-Demand Loading

- Production profiling showed that unloading route textures would regress the
  existing zero-byte combat return. The retained fix removes the speculative
  load instead: ordinary routes no longer queue every special-event and Market
  texture before one of those overlays opens.
- Event assets are grouped by route-node type and load through the existing
  Phaser runtime loader only when requested; the completion callback redraws
  the overlay and the in-flight vector fallback remains intact.
- Ordinary-route decoded texture residency fell by 77,369,860 bytes (-42.6%),
  and first-battle residency fell by 77,162,824 bytes (-29.6%). The 18 deferred
  textures represent 3,979,034 bytes on disk and about 76.47 MB decoded.
- The production Basin probe fetched only its three matching textures (677,846
  bytes), unrelated event/Market keys stayed cold, focused regressions pass,
  and the shared client capture has no browser errors.
- A full-matrix failure exposed a deterministic lifecycle race: a late runtime
  asset callback could redraw the route during the travel beat and remove its
  generated streak. Pending route commitment now preserves transient objects
  and tweens while still refreshing telemetry; the travel and lazy-load tests
  pass together across three repeated focused runs.
- Final closure passes `npm run validate` and all 156 Chromium scenarios in
  19.7 minutes.

## 2026-07-18 District Encounter Objective Depth

- Current audits are green: content reports 32/32 checks and zero drift,
  economy meets all authored targets across 500 seeded samples, balance
  pressure progresses across four districts, and playtest stats clearly label
  their ten seeded rows as pipeline evidence rather than human conclusions.
- Combat objectives now use district-specific pools instead of one nearly
  universal list. New optional plans reward Roosting with banked Wingbeat and
  blocking a district-scaled damage target; existing Flow, Surge, restraint,
  speed, Cover, no-damage, and priority goals are placed where they best support
  district identity.
- The objective plate now displays exact live progress and provides the full
  condition plus Scrap reward on hover. Both new types survive active-run save
  sanitization and remain horizontal bonuses that cannot invalidate a win.
- Strict TypeScript, production build, five focused objective/HUD/save tests,
  the required shared client, and inspected production HUD/tooltip captures
  pass with no browser errors.
- Final release closure passes `npm run validate` and all 158 Chromium
  scenarios in 19.9 minutes. Production remains within hard budgets at 664.7
  KB app entry, 693.2 KB combined boot code, and 7.1 KB for the lazy battle HUD
  renderer.

Current follow-up: Legacy world cleanup. Removed 117 superseded world assets
(64.7 MB) across old splash generations, route-map sources/crops, route-event
sources/runtime layers, generic backdrop variants, and node atlases. The live
world graph now contains only the current district route/battle/boss art,
current route-event plates/residents/props, market kit, and splash-v7 node crops.
The UI-art builder now targets splash v11. The canonical world contract and
validator exact-allowlist runtime plus source families and reject old names in
both game and build code. Production build, release validation, and inspected
shared-client route/battle runs pass without browser errors.

Current follow-up: Leader/Ascension personal records. Winning flights now track
clear count and fastest total beats per Leader, Full/Quick length, and Ascension
tier; slower repeats cannot emit false records. Outcomes celebrate first clears
and real speed improvements as `NEW PROGRESS`, and Profile rows expose highest
Full tier plus Full/Quick pace while retaining the global final-boss record as
`Boss Fastest`. Old saves migrate without invented history, invalid record keys
are rejected, and mirrored recovery preserves valid records. Production proof
at `.artifacts/leader-record-profile-populated.png` was visually inspected with
matching text state and no console errors. Bundle budgets remain green at 29.7
KB game-core, 664.7 KB app entry, and 694.4 KB combined boot. `npm run validate`
passes, and the full Chromium suite passes 159/159 in 20.2 minutes.

Current follow-up: Enemy-local priority objective read. Priority encounter
goals now mark the configured target directly above its Cohesion rail and beside
its Tell, show the remaining Beat window, expose the full condition/reward on
hover, and switch to `OBJECTIVE MISSED` after the deadline without invalidating
combat victory. The marker and global goal plate share the same live resolver.
The focused active/failed/tooltip regression passes, `npm run validate` is green
across all release gates and nine sequencing scenarios, and the required shared
client reached a settled battle with no browser errors. Full-resolution proof is
`.artifacts/objective-target-production.png`; inspection confirms the marker
does not cover enemy art, Cohesion, Tell, hand, or the global goal plate.

Current follow-up: Encounter objective audio and celebration. Objective status
changes now use a one-shot resolver latch to fire a compact generated callout
and distinct procedural audio cue: bright/ascending with earned Scrap for
completion, softer/descending for a miss. Redundant redraws cannot replay the
feedback, and the approximately 1.3-second hold stays readable without blocking
combat. Focused complete/miss/deduplication coverage passes. Matching production
text state and inspected frames are `.artifacts/objective-feedback-complete-
production.png` and `.artifacts/objective-feedback-missed-production.png`; each
reports one burst, one correct cue, no opposite cue, and no browser errors.
`npm run validate` passes every release gate plus nine sequencing scenarios;
production remains within budget at 666.4 KB app entry, 696.2 KB combined boot,
and 29.7 KB `game-core`.

Current follow-up: Persistent High Contrast accessibility. Shared Settings now
has an eighth Contrast row with Standard/High values, live pointer, keyboard,
and standard-gamepad control, safe local persistence, and a session fallback
when browser storage is blocked. The treatment applies before Phaser boots and
therefore stays consistent across Menu, Profile, Codex, Route, Battle, rewards,
and outcomes without altering combat rules or asset identity. Five focused
Settings regressions pass across interaction, exact focus ownership, reload,
cross-scene propagation, and blocked storage. The required shared client reached
the settled eight-row panel with no error artifact; inspected browser-level
comparison frames are `.artifacts/high-contrast-standard-production.png` and
`.artifacts/high-contrast-high-production.png`, with matching applied text state
and no console/page errors. A settled High Contrast combat proof at
`.artifacts/high-contrast-battle-production.png` keeps the Leader, enemy, Tell,
Cohesion rails, HUD, hand, piles, and card artwork distinct. `npm run validate`
passes every release gate, the 42-runtime/41-source canonical-world contract,
and all nine sequencing scenarios; production measures 667.4 KB app entry,
697.1 KB combined boot, 29.7 KB game core, and 24.1 KB system overlays.

Current follow-up: Molt Power resolver and canonical rule repair. The previously
dead Molt Power Flock Stat now feeds both selected-card preview and live combat:
each Molt-active card spends it once on the first positive damage, Cover, or
recovery line, while area damage receives half rounded up per target and zero
bursts cannot consume it. The retired next-card Molt description has been
replaced with the shipped whole-turn alternate-ability/discount stance, and the
docs validator rejects those stale phrases. Five focused Molt browser scenarios
and all release/sequencing gates pass. The required production client completed
title -> route -> battle without errors; inspected proof at
`.artifacts/molt-power-production.png` shows Molt Power 3 turning 7 damage into
10 with a clear callout and Roof Rat moving from 30 to 20 Cohesion. Production
remains within hard budgets at 29.9 KB game core and 698.3 KB combined boot.

Current follow-up: Molt Power audio closure. Each real Molt Power spend now owns
one distinct, non-fatiguing procedural feather/rising-shimmer cue scaled to the
bonus; preview and normal-mode resolution stay silent, and existing mute/SFX
volume controls remain authoritative. A fresh production run unlocked Web Audio
through the real Start Run control before resolving Locked Nest. Text state
reports one cue, one burst, and Roof Rat at 20/30; the inspected
`.artifacts/molt-power-production.png` frame keeps `MOLT POWER +3` readable in
the stable upper-center lane despite the simultaneous four-suit rally effect.
Three focused regressions and every release/sequencing gate pass with no browser
errors. Game core remains 29.9 KB and combined boot is 698.5 KB.

Current follow-up: Contextual Molt onboarding. The opening four-step First
Flight Guide still teaches only route, card, Roost, and reward decisions; once
that sequence is complete, a persistent one-time lesson now waits until a real
`runtime.kind === 'molt'` card reaches hand. A compact orange HUD explanation,
distinct non-selection pulse, and `MOLT CARD` chip identify Hot Feathers while
leaving every legal alternative available. Playing the Molt card retires the
lesson across reloads, Replay Guide resets it, and Skip Guide suppresses it.
Focused browser coverage passes activation, skip, seen-state, completion, and
restart persistence. The required shared production client completed title ->
route -> battle; the inspected focused proof is
`.artifacts/molt-guide-production.png`, with matching text state and no console
or page errors. `npm run validate` passes all release gates and nine sequencing
scenarios at 700.0 KB combined boot, 671.6 KB app entry, and 28.4 KB game core.

Current follow-up: Reward decision outcome previews. Every Add-to-Flock card now
keeps an exact `DECK N > N+1` chip visible before commitment, while every Preen
choice shows the first changed base or Molt effect with a Flock Stat fallback.
The lazy ceremony renderer consumes the live card/deck decision contract, and a
focused browser regression proves the displayed Add and Preen choices against
the resulting deck size and upgraded card. The required shared production
client reached settled combat with audio unlocked; inspected focused proof at
`.artifacts/reward-delta-production.png` and
`.artifacts/preen-delta-production.png` shows all three choices unobstructed,
with state-matched deltas and no console/page errors. `npm run validate` passes
all release gates and nine sequencing scenarios. Combined boot remains 700.4
KB, app entry 672.0 KB, game core 28.4 KB, and lazy reward rendering 12.2 KB.

Current follow-up: Final card-picker consequence previews. The shared picker
used by paid Market services and route workbenches now keeps the exact primary
result on every candidate before the irreversible click. Preen cards compare
the first changed base/Molt effect or Flock Stat, preserve the changed numeric
token when long conditions share a prefix, and identify newly added effects;
Release cards show the live `DECK N > N-1` delta. Paid choices retain their
per-card Scrap badge and full hover dossier. Three focused browser scenarios
pass Market Preen, Market Release, and a two-pick route removal whose labels
recalculate after the first commit. The required shared production client
completed title -> route -> battle; inspected focused captures are
`.artifacts/card-picker-preen-production.png` and
`.artifacts/card-picker-release-production.png`, with matching text state and
no console/page errors. Production remains within budget at 702.5 KB combined
boot, 674.1 KB app entry, and 28.4 KB game core.

Current follow-up: Enemy-move and input-friction telemetry closure. Combat now
records actual Cohesion lost, Cover blocked, and hit count against the enemy and
move that resolved the pressure; invalid card/target actions and deliberate
selection cancellations are aggregated separately across the run. This also
fixes overkill inflation so a 20-point hit against 1 remaining Cohesion records
1 lost, not 20. The fields survive active-run checkpoint sanitization and clone
recovery. `npm run stats:playtest` now exposes Enemy Move Pressure and Input
Friction while clearly labeling seeded rows as pipeline evidence, not human
balance evidence. Focused emit, cross-fight aggregation, and save-recovery
coverage pass. The required shared production client reached settled combat;
the inspected outcome proof is `.artifacts/telemetry-outcome-production.png`,
with its summary recording Review Strike, 1 Cohesion lost, two invalid actions,
one cancellation, and no console/page errors. Bundle validation passes at 703.9
KB combined boot, 675.5 KB app entry, and 28.4 KB game core. `npm run validate`
passes every release gate and all nine sequencing scenarios.

Current follow-up: Complete Market purchase outcome previews. Cards, Waymarks,
Supplies, boss rigging, route planning, and stock refresh now show their exact
primary before/after rows before the irreversible click. The projection reuses
the route-effect resolver for active `afterMarketPurchase` Waymarks, so visible
Scrap totals include refunds and visible next-combat Guard/Open Sky changes match
the committed state. Preen and Release remain correctly two-stage: the service
spends nothing, then the final card picker shows the exact per-card cost and
result. Focused parity coverage commits every purchase family and also proves an
active Ledger Tab refund/Guard contract. Six surrounding Market regressions
pass. The required shared production client reaches settled combat without an
error artifact; inspected focused proof is
`.artifacts/market-card-preview-production.png` and
`.artifacts/market-boss-preview-production.png`, with matching text state and an
empty browser-error list. Bundle validation passes at 707.7 KB combined boot,
679.3 KB app entry, and 28.4 KB game core; the entry exceeds the preferred target
but remains below the hard release cap. `npm run validate` passes every release
gate and all nine sequencing scenarios.

Current follow-up: Legacy world-asset retirement closure. Confirmed and preserved
the removal of 117 superseded world files: 43 runtime/style assets and 74 concept
sources spanning the laminated planning board, pre-v7 route-node atlases/crops,
canal-run splash v4-v10 plus the original splash, four generic district variants,
and v1 route-event plates/residents/decor. Runtime wiring now resolves only the
canonical district route maps, district/boss battlefields, current v2/v3 event
set pieces, market kit, and individual splash-v7 node icons. The exact allowlist
validator also rejects every retired v1 event runtime filename in source code so
string fallbacks cannot silently reintroduce 404s. `npm run validate:runtime-assets`
passes for 951 optimized assets, 42 canonical world runtime/style assets, and 41
canonical sources. The production build passes. Five focused world regressions
pass across route preview, all district backdrops, boss variants, encounter moods,
and event plates. The required shared client and a production battle capture were
visually inspected with current Rooftop Blocks art and empty browser-error logs;
evidence is under `.artifacts/legacy-world-shared-client-route-current/` and
`.artifacts/current-world-battle.png`.

Current follow-up: Complete route-event outcome previews. Every Basin, Cache,
Nest, and Signal confirmation now projects its irreversible result through the
same route-effect resolver used by commitment. The compact decision ledger
reports Cohesion, Scrap, deck, Supplies, Waymarks, boss shield, Open Sky Guard
and reduction, enemy Cover, and forced Open Sky changes; active event-triggered
Waymarks are included. Random cache outcomes and displayed item/card rewards
are frozen when the panel opens, so confirmation cannot reroll them. Preen and
Release remain two-stage with exact card-level results in the final picker, and
rival options identify the Battle handoff. Focused parity coverage passes Basin,
Signal/cache expansion, Cache card choice, Nest/Preen, hidden Waymark bonuses,
and rival transitions; five surrounding route/world/picker regressions pass.
The production build and required shared-client route run pass. Inspected proof
at `.artifacts/route-decision-production/route-decision.png` matches its text
state (`20/38 > 28/38`, `200 > 180`, `0/2 > 2/2`, `Sky Guard 0 > 1`) with no
browser errors; shared-client evidence is under
`.artifacts/route-decision-shared-client/`. `npm run validate` passes every
release gate and all nine sequencing scenarios. Bundle validation passes its
hard caps at 714.2 KB combined boot, 685.8 KB app entry, and 28.4 KB game core;
the first two remain above their preferred soft targets.

Current follow-up: P1 visual-hierarchy evidence closure. Production grayscale
inspection now covers title, route confirmation, reward comparison, Market
inspection, combat intro, and settled combat. The current decision remains
obvious on every surface without suit color: primary actions and interactive
offers lead, structural chrome and logs recede, and no selected dossier hides
the remaining reward/Market options. The route confirmation was also checked at
2560x1600, 1280x720, and the minimum supported 1000x560 viewport. Its centered
stage, decision rows, reward dossier, and Confirm/Cancel controls remain anchored
and unclipped with empty browser-error lists. Evidence is under
`.artifacts/visual-hierarchy-grayscale/` and
`.artifacts/route-decision-breakpoints/`.

Current follow-up: Route-contract completion feedback closure. Reachable-node
benefit/risk rows and the four horizontal contract strategies already satisfied
the build-decision contract, but completion only changed a persistent badge and
log line. It now fires a generated `CONTRACT COMPLETE` route plate plus the
ascending objective-complete cue, names the exact Scrap and permanent record
badge reward, and clears after 1.8 seconds. A persisted `celebrated` latch keeps
scene reloads from replaying the reward. Focused coverage proves all four
contract resolvers, account/run-history recording, visible callout geometry,
audio emission, the sub-three-second expiry, and the one-shot latch. The
production capture at
`.artifacts/contract-celebration-production/contract-complete.png` is readable
without covering the selected route or Take Route action, and its browser-error
list is empty. `npm run validate` passes every release gate and all nine
sequencing scenarios. Bundle hard caps pass at 716.1 KB combined boot, 687.6 KB
app entry, and 28.4 KB game core; combined boot and entry remain above their
preferred soft targets.

Current follow-up: Horizontal mastery acceptance closure. Unlocked Leader cards
show named mastery and live progress before flight, while every outcome selects
one concrete `NEXT FLIGHT` goal from unfinished boss dossier observations, suit
collection milestones, or Leader mastery. Fresh contracts, dossier knowledge,
achievements, and personal records remain horizontal and receive compact outcome
celebration; same-seed replay and share links are opt-in. Four focused browser
scenarios pass dossier persistence/goal priority, collection milestones, shared
route fidelity, and fresh-progress outcome presentation. The optional weekly
route remains deliberately deferred until observed retention proves the base
loop, avoiding calendar pressure or FOMO mechanics.

Current follow-up: Codex boot-bundle deferral. The complete Codex browser and
dossier scene now loads only after the title's Codex command, with visible
loading feedback, retry after failure, and an early-click-safe handoff across
the title's normal UI-art restart. Production HTML does not preload the 55.2 KB
Codex scene or Codex data. App entry fell from 687.6 KB to 635.0 KB and combined
boot from 716.1 KB to 663.4 KB, clearing both preferred targets while game core
remains 28.4 KB. Focused coverage proves the chunk is unrequested and the scene
unregistered at boot, then requested, registered, active, and visually complete
after the real menu action. The required shared production client confirms a
clean Codex frame, matching `mode: codex` state, and no browser errors; evidence
is under `.artifacts/codex-lazy-production/client/`.

The first broad closure matrix passed 165/169 and exposed three legacy/manual
effect states that omitted Molt Power fields, allowing `undefined` to reach
WebAudio as `NaN`, plus one stale assertion that counted raw overkill instead of
actual Cohesion lost. Missing or non-finite Molt Power now safely resolves as
zero, all four cases pass together, and the final `npm run validate` gate passes
with all nine sequencing scenarios. The authoritative extended rerun passes all
169 Chromium scenarios in 22.3 minutes on one worker.

Current follow-up: Fairness audit and Codex single-cue closure. Authored balance,
500-seed economy, content-drift, and seeded telemetry-pipeline audits pass. No
live balance change was made from the starter Flock Power Index because that
proxy omits the Fledgling's three zero-cost cards and Four-Suit Rally Flow,
making a speculative buff weaker than the available evidence. The production
client instead exposed a concrete feedback bug: the shared title utility control
and lazy Codex opener both played `confirm`. One pointer action now produces one
cue. Focused coverage proves the real control requests/registers Codex, stops
Menu, clears loading feedback, and increments confirm telemetry exactly once.
The required production client reports `cueRequests.confirm: 1`, a clean Codex
frame, and no browser errors under
`.artifacts/codex-single-cue-production/client/`. The complete 169-scenario
Chromium matrix passes in 22.3 minutes, including this actual-pointer path and
all late combat-state regressions.

Current follow-up: Human playtest export bridge. The five observed first-run
sessions remain genuine human evidence and cannot be replaced by automation,
but collecting that evidence no longer requires the browser console.
`?playtest=1` adds a local-only Flock Record export command that downloads the
journaled run history, reports its run count/status, and makes no upload. The
dashboard accepts a file, several files, or an export directory and deduplicates
run IDs. A new `docs/game/playtest-runbook.md` owns fresh-profile setup,
observation questions, aggregation, and release criteria. Focused Profile tests
pass the normal hidden state plus actual-pointer download/content/no-upload
path. The required production client shows a clean empty-history command with
matching text state and no browser errors under
`.artifacts/playtest-export-production/`; a two-file merge proof collapses one
duplicate into two unique dashboard runs.
Final `npm run validate` passes every release gate and all nine sequencing
scenarios. The complete current-build Chromium matrix passes 170/170 in 24.0
minutes; production remains at 635.1 KB app entry, 663.5 KB combined boot, and
13.9 KB for the optional Profile module.

Current follow-up: Deployment security closure. The responsive display gate now
lives in external `src/platform-gate.ts`, leaving production HTML free of inline
executable scripts. `public/_headers` ships a strict self-script CSP, framing and
MIME-sniff denial, no-referrer, sensitive-capability restrictions, same-origin
opener/resource isolation, fresh HTML, and immutable hashed assets. Deployment
validation rejects missing/weakened security rules and inline/unsafe script
execution. The exact CSP boots cleanly in focused Chromium coverage; the
portrait/compact sleep and landscape resume lifecycle still passes. The shared
production client shows an unchanged title and no browser errors under
`.artifacts/deployment-security/client/`. Build and bundle/deployment gates pass
at 635.7 KB app entry and 664.2 KB combined boot.
Final `npm run validate` passes every release gate and all nine sequencing
scenarios. The complete hardened-build Chromium matrix passes 171/171 in 24.9
minutes, including the exact strict-CSP document boot.

Current follow-up: Player-controlled full save backup and staged restore. Added
`src/game/save-backup.ts` as a versioned, 2 MB-limited allowlist boundary for
the journaled account, active flight, run history, first-flight guide, and all
nine persistent control/audio/visual/difficulty preferences. Flock Record now
offers an always-visible Save Data panel with local download, validated file
selection, a run/win/leader/checkpoint preview, and a distinct confirmation
step. Restore clears only owned Bird Squad keys, recreates journal mirrors,
verifies writes, rolls back from a snapshot on failure, and leaves unrelated
origin storage alone; the `?playtest=1` run export moved inside the same panel.
Focused real-canvas Chromium coverage proves complete local export with no
mutating request, invalid-file immutability, pre-confirm immutability,
transactional restore/mirror rebuilding, unrelated-key preservation, and the
playtest export regression. The required production client drove Menu -> Flock
Record -> Save Data and its inspected polished capture under
`.artifacts/save-backup/client-polished/` has matching text state and no browser
error artifact. Production builds at 636.4 KB app entry and 664.8 KB combined
boot. Final `npm run validate` passes every release gate and all nine sequencing
scenarios; the complete current-build Chromium matrix passes 174/174 in 23.7
minutes on one worker.

Current follow-up: Flock Record and Save Data input parity. The Profile scene
now owns a visible linear focus model across Achievements, Contracts, Return,
Save Data, download/restore, playtest export, and staged restore confirmation.
Configured Previous/Next/Confirm/Back bindings drive the same actions as real
pointers; Tab and D-pad navigation, gamepad A confirmation, and gamepad B back
are supported with lifecycle-cleaned listeners. Focus rings and on-canvas hints
use the player's actual binding labels, while `render_game_to_text` reports the
current focus and legal order. The input pass also removed duplicate Save Data
confirmation sounds and gives Cancel/Return one close cue instead. Focused
Chromium coverage passes remapped keyboard plus D-pad/A/B input with exact cue
deltas, then passes local backup download with exactly one pointer confirmation
cue. The first production capture under `.artifacts/profile-input/client/`
shows a clean focused Download Backup action with matching text state and no
error artifact. The final rebuilt capture under
`.artifacts/profile-input/client-final/` confirms the refined binding hint and
focus treatment. Production remains within budget at 636.4 KB app entry and
664.9 KB combined boot; the optional Profile scene is 25.7 KB / 8.7 KB gzip.
Final `npm run validate` passes every release gate and nine sequencing cases,
and the authoritative Chromium matrix passes 175/175 in 24.2 minutes.

Current follow-up: Title setup and utility input parity. The Menu scene now
owns one lifecycle-cleaned focus path across unlocked Leaders, Ascension,
flight length, continue/new-flight actions, How to Play, Settings, Codex, and
Flock Record. Configured Previous/Next/Confirm bindings adjust or activate the
focused group; Tab and unbound Up/Down cycle groups; D-pad directions, gamepad
A/B, and Start provide standard controller access without letting an initial A
press accidentally launch a flight. Pointer actions adopt the same focus, the
canvas shows binding-aware help plus a visible focus ring, and text state reports
the exact focus order/label and selected setup. Focused Chromium coverage proves
remapped keyboard setup through a real Route launch with the chosen Leader,
Ascension, and length, exact one-cue-per-action telemetry, listener cleanup over
the flight-length redraw, and controller-only Profile/Help/Settings/Codex access.
The required production client exposed and then verified the fix for a scaled
focus-stroke rendering defect; the final inspected title capture and matching
state are under `.artifacts/title-focus/client-final/`, with no browser error
artifact. Remaining release evidence is unchanged: five observed human first-run
sessions still need to be completed through `docs/game/playtest-runbook.md`.

Final `npm run validate` passes every release gate and all nine sequencing
scenarios. The current production bundle remains within budget at 643.1 KB for
the app entry and 671.5 KB combined boot code.

Current follow-up: Title composition readability closure. A settled real-time
capture proved the title logo itself remained inside the canvas, but the new
input legend crossed directly through the BIRD SQUAD mark and scaled to roughly
8 CSS pixels on a 1024-wide tablet. The legend now sits on a restrained dark
command strip below the logo at 12 px internal type, with runtime geometry
telemetry proving the logo, strip, and text remain within the 1280x720 game
canvas and do not overlap. Inspected desktop and centered 1024x768 captures are
clean and readable; the shared required client reports exact bounds and no
browser error artifact under `.artifacts/title-composition/shared-client-readable/`.
Responsive coverage now waits for the settled composition and verifies the
authored 1.8% Leader-selection pulse envelope instead of racing its untweened
pixel size. Title setup, controller-only utilities, and tablet composition pass
together. Remaining release evidence is unchanged: five observed human
first-run sessions still need to be completed through
`docs/game/playtest-runbook.md`.

Final `npm run validate` passes every release gate and all nine sequencing
scenarios. The app entry remains within budget at 644.0 KB minified / 169.0 KB
gzip, with 672.4 KB combined boot code.

Current follow-up: One-pass title boot and overlay asset deferral. The first
Menu paint previously used fallback controls, loaded the broad Menu icon pack,
then restarted Menu to replace them; that replayed the title entrance and made
the composition visibly flash. Boot now queues the 24 essential generated title
assets through production-safe bundled URLs before Menu starts, while Help (11
assets) and Settings (10 assets) request their own artwork only when opened.
Those overlay callbacks refresh locally and never restart Menu. Runtime text
telemetry exposes the Menu generation, splash format, essential readiness, and
deferred overlay counts. Focused Chromium coverage passes 4/4 for one-generation
boot, lazy overlay loading, remapped setup controls, controller-only utilities,
and complete Settings navigation. The required production client inspected the
fully generated title and Help overlay under `.artifacts/title-one-pass/`; both
captures match their text state, remain on Menu generation 1, and have no browser
error artifact. The split release gates pass, including all 9/9 sequencing
scenarios; world validation still checks 42 canonical runtime/style assets and
41 canonical sources while explicitly rejecting retired legacy paths. The app
entry remains within budget at 648.0 KB minified / 170.1 KB gzip, with 676.4 KB
combined boot code. Remaining release evidence is unchanged: five observed
human first-run sessions still need to be completed through
`docs/game/playtest-runbook.md`.

Current follow-up: Compact first-paint title textures. Cache-disabled production
profiling showed application code was not the title bottleneck; Phaser/WebGL
initialization and texture processing dominated, while the app entry contributed
about 10 ms of sampled self-time. Twelve medallion assets authored at 256-320 px
were only displayed at 34 game pixels or less. The runtime UI builder now
reproducibly derives 128 px boot copies with explicit `-title-preload` names,
reducing that subset from 1,155,072 to 196,608 pixels (83.0%) and 97.5 KB. Total
essential title texture area drops from 3,137,536 to 2,179,072 pixels, and the
existing splash rebuild returns to its intended 273.0 KB output. A matched local
cold profile improved generation-1 title readiness from 2.22 s to 1.68 s while
title/splash transfer fell from roughly 1.32 MB to 1.02 MB. Title telemetry now
proves compact count/readiness, maximum dimension, and total essential pixel
area. Deployment validation requires one hashed canonical WebP and one distinctly
named hashed derivative for each of the 12 compact icons, preventing asset
ambiguity. Focused title/onboarding coverage passes 2/2 after final packaging;
the previously run four-scenario title/tablet/tooltip matrix also passes. The
required client and a 2560x1440 capture under
`.artifacts/startup-texture-optimization/` were visually inspected: the title
and larger How to Play seal remain crisp, generation stays 1, and there are no
browser errors. Strict TypeScript, production build, all static release gates,
and 9/9 sequencing scenarios pass. Current bundle size is 648.8 KB / 170.1 KB
gzip for the app entry and 677.2 KB / 180.4 KB gzip combined boot. Remaining
release evidence is unchanged: five observed human first-run sessions still
need to be completed through `docs/game/playtest-runbook.md`.

Current follow-up: Human experience evidence capture. Balance, content, and
economy audits remain clean, but behavioral telemetry could not substantiate
whether a run felt fun, fair, clear, or worth replaying. In `?playtest=1`, the
Flock Record > Save Data panel now attaches four voluntary 1-5 ratings to the
latest completed run, persists every choice immediately through the mirrored
run-history journal, exposes complete state through `render_game_to_text`, and
includes the ratings in the existing local JSON export. Mouse/touch and
keyboard/gamepad focus paths are covered; normal gameplay remains unchanged and
the flow makes no network writes. The playtest dashboard aggregates complete
human responses, counts low 1-2 ratings, and intentionally reports zero
subjective evidence for seeded runs. Both empty and completed states were
visually inspected at 1280x720 with no console errors. Strict TypeScript,
production build, documentation/runtime/world/deployment/bundle gates, focused
profile controls and export coverage (2/2), and all sequencing scenarios (9/9)
pass. The Profile optional chunk remains within budget at 28.9 KB minified /
9.7 KB gzip; combined boot code is 678.3 KB minified / 180.8 KB gzip. Remaining
release evidence is still five observed fresh-player sessions using
`docs/game/playtest-runbook.md`; ratings strengthen that gate but do not replace
observer notes.

Current follow-up: Opt-in assistive-technology narration. Settings now includes
a ninth Screen Reader row that is operable by pointer, remapped keyboard, and
gamepad input. Enabling it activates a visually hidden atomic live region and a
deduplicated state observer that announces the focused title/settings control,
selected route and risk, combat resources/hand/enemies/log, selected card and
target, rewards, Profile, and Codex context. The preference defaults off, adds
no polling cost while disabled, persists locally, and participates in complete
save backup/transactional restore. Authoritative text state exposes the
preference, live-region readiness, observer activity, and last announcement.
The same pass unified synthetic/remapped Escape with physical Escape for Menu
Settings and raised Menu system overlays above the title input legend after a
rendered capture exposed that z-order overlap. Focused Chromium coverage passes
4/4 across Settings, narration from Menu through Route and Battle, and both save
backup paths. The required client captures under
`.artifacts/screen-reader-settings-client/` and
`.artifacts/screen-reader-battle-client/` were visually inspected, match their
serialized states, and contain no browser error artifact. Strict TypeScript,
production build, documentation/runtime/world/deployment/bundle gates, and all
9/9 sequencing scenarios pass. Combined boot code remains within budget at
682.9 KB minified / 182.5 KB gzip. Remaining release evidence is unchanged:
five observed fresh-player sessions still need to be completed through
`docs/game/playtest-runbook.md`.

Current follow-up: Complete Codex input and assistive-technology parity. The
Codex was reachable from keyboard/gamepad on the title but its browser itself
was pointer/wheel-only. It now owns one persistent focus model across the five
sections, card/enemy/item filter rows, virtualized entries, Back, and dossier
close. Tab and D-pad Up/Down move focus bands; remapped Previous/Next and D-pad
Left/Right change the active section/filter or traverse the grid; Confirm/A
selects or opens; Back/B closes or returns; and grid focus scrolls fully into
view. A visible generated-style focus ring and binding-aware footer match the
serialized `codexFocus` label, position, bindings, and grid/detail scroll state.
Screen Reader announcements now use those player-facing labels (including
glossary summaries) instead of internal IDs. Pointer tab/detail actions share
the same focus and confirmation/close cues. Focused Chromium coverage passes
14/14 across every Codex collection/dossier, lazy loading, screen-reader state,
remapped keyboard, and standard gamepad input. The required client captures at
`.artifacts/codex-focus-grid-client-final/` and
`.artifacts/codex-focus-detail-client/` were visually inspected, match their
text state, and contain no browser error artifacts. Strict TypeScript and the
production build pass; the optional Codex scene remains lazy at 65.9 KB
minified / 17.1 KB gzip, while combined boot remains within budget at 683.3 KB
minified / 182.7 KB gzip. Remaining release evidence is unchanged: five
observed fresh-player sessions still need to be completed through
`docs/game/playtest-runbook.md`.

Current follow-up: Legacy world-asset cleanup closure. Re-audited the existing
117-file / 64.7 MB removal across splash generations, route-map atlases,
route-event layers, generic battlefield variants, and the laminated route
board. Runtime imports and the production bundle contain only the canonical
world-demo set. Removed the final misleading `assets/battlefields/` guidance
from the root README, exact-allowlisted the three current route-event
centerpiece masters, and extended `tools/validate-world-assets.mjs` to reject
retired asset names in current contributor guidance as well as runtime/build
code. The validator now checks 42 runtime/style assets and 44 source assets.
Production build, `npm run validate`, and all nine sequencing gates pass. The
required shared client visually proved the current Rooftop Blocks route and
battle surfaces with matching text state and no browser errors under
`.artifacts/world-cleanup-proof-route/` and
`.artifacts/world-cleanup-proof-battle/`.

Follow-up hardening: The canonical validator now scans every file below the ten
owned runtime/style and concept-source roots, rather than relying only on the
known leaf directories. A retired generation hidden in a newly created nested
folder is therefore rejected alongside old filenames, imports, builder
references, and contributor guidance. This closes the last path by which stale
world art could return as a misleading implementation reference.

Current follow-up: Exact minimum-supported touch closure. The device contract
advertised 1000x560 support, but its 0.778 canvas scale turned the title's
56-game-pixel controls into 43.6 CSS-pixel targets, below the documented 44 px
floor. A shared 58-game-pixel minimum now keeps title setup plus shared
audio/pause targets at roughly 45 CSS pixels without narrowing device support.
The platform regression now runs at the exact 1000x560 boundary instead of the
more forgiving 1024x768 viewport. Its inspected capture remains unclipped and
fully playable; the required production client also drove title -> route ->
battle with matching focus state and no browser errors under
`.artifacts/min-supported-touch-client/`. Remaining release evidence is still
five observed fresh-player sessions through `docs/game/playtest-runbook.md`.
Strict TypeScript, production build, the focused minimum-display/shared-audio
pair, and the complete release validation with all nine sequencing scenarios
pass. App entry remains 659.4 KiB and combined boot code 687.8 KiB.

Current follow-up: Settings and remapping minimum-touch closure. An exact
1000x560 probe found that Settings row targets scaled to 31.1 CSS pixels,
Contrast to 34.2, and nominal 56-game-pixel switches to 43.6, all below the
44 CSS-pixel contract. Settings now uses two columns of non-overlapping
58-game-pixel rows, while remapping tabs and six binding rows use the same
minimum and matching vertical spacing. The new boundary regression measures
all 15 named Settings targets and all 10 named remapping targets in CSS pixels,
requires each to remain at least 44x44, and captures both layouts after deferred
art settles. Exact-boundary and required production-client screenshots were
visually inspected; the text harness reports the matching Settings/Controls
focus state and neither client emitted browser errors. Strict TypeScript, four
focused Settings/remapping scenarios, production build, every static release
validator, and all nine sequencing scenarios pass. Remaining release evidence
is unchanged: five observed fresh-player sessions through
`docs/game/playtest-runbook.md`. App entry remains 659.4 KiB and combined boot
code 687.8 KiB.

Current follow-up: Shared field-command minimum-touch closure. The exact
1000x560 probe found that How to Play and route-pause actions authored at 56
game pixels still scaled to 43.6 CSS pixels. The shared field-button renderer
now separates its visual frame from a transparent named interaction/focus
region that is at least 58x58 game pixels. This preserves compact authored art
while applying the touch floor to onboarding, pause, Profile save/restore and
export actions, plus fallback shared commands. A new boundary regression
measures both How to Play actions and all three route-pause actions, and final
artwork-loaded captures under `.artifacts/test-results/min-supported/` remain
readable, separated, and unclipped. Required production clients reached both
surfaces through real pointer/title navigation with matching text state and no
browser errors under `.artifacts/shared-field-help-client/` and
`.artifacts/shared-field-pause-client/`. Strict TypeScript, six focused shared
consumers, isolated deck-review confirmation, production build, and full
`npm run validate` pass with all nine sequencing scenarios. Remaining release
evidence is unchanged: five observed fresh-player sessions through
`docs/game/playtest-runbook.md`. App entry is 659.5 KiB and combined boot code
is 687.9 KiB.

Current follow-up: Review and reward minimum-touch closure. At the exact
1000x560 support boundary, route/combat review rows measured 26.4 CSS pixels
high and reward Skip measured 37.3, with review scroll controls also below the
44x44 contract. Review pages now show seven rows instead of eleven, use
58-game-pixel spacing, and separate compact visual art from 58-pixel row and
scroll interaction regions. Reward Skip uses the same minimum transparent hit
region without changing its artwork. A new exact-boundary regression measures
all three surfaces in CSS pixels; the route review, combat pile, and reward
captures remain readable and unclipped. Seven focused regressions, the
production build, and full release validation pass, including all nine
sequencing scenarios. The required production client reached the live
seven-row Deck Review with matching text state and no browser errors under
`.artifacts/shared-review-client-final/`. Remaining release evidence is still
five observed fresh-player sessions through `docs/game/playtest-runbook.md`.
App entry is 659.7 KiB and combined boot code is 688.2 KiB.

Current follow-up: Codex and Flock Record minimum-touch closure. Exact 1000x560
measurement found Codex navigation as low as 23-34 CSS pixels, Flock Record
badge tabs at 43.6, playtest rating rows at 24.9, and direct rating chips at
26.4x18.7. Codex section/type/filter, Back, and dossier Close controls now use
independent 58-game-pixel hit regions; the secondary item-filter row was moved
down to keep the enlarged targets non-overlapping. The local playtest panel is
taller and gives all four rows plus every direct 1-5 choice a separated 58x58
target without inflating the compact chip art. A new exact-boundary regression
measures 18 Codex navigation controls, dossier Close, two Profile tabs, and 27
playtest/save controls. The inspected minimum-viewport captures remain readable
and unclipped. Required production clients reached Supplies / Plan and the
Contracts tab through real pointer navigation with matching text state and no
browser errors under `.artifacts/optional-touch-codex-client/` and
`.artifacts/optional-touch-profile-client/`. Strict TypeScript, production
build, five focused Codex/Profile regressions, and full release validation pass,
including all nine sequencing scenarios. App entry remains 659.7 KiB and
combined boot code remains 688.2 KiB. Remaining release evidence is still five
observed fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Core route-decision minimum-touch closure. Exact 1000x560
measurement found abandon confirmation at 42 CSS pixels high, route reward
Claim at 29.6, market tabs at 26.4, services/refresh at 43.6, and run-outcome
commands at 40.4. Exit confirmation, Market category/service/refresh, reward
Claim, and outcome commands now separate compact authored artwork from
independent 58-game-pixel hit regions. One exact-boundary traversal measures
all affected targets across four screens; inspected captures remain readable,
separated, and unclipped. The required production client reached a live paused
route with matching text state and no browser errors under
`.artifacts/core-route-touch-client/`. Strict TypeScript, five focused
regressions, production build, and full release validation pass, including all
nine sequencing scenarios. App entry is 660.4 KiB and combined boot code is
688.8 KiB. Remaining release evidence is still five observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Runtime pointer-target closure. Broad 1000x560 inventory now
covers Menu, baseline Route, long-deck card picker, loaded Market, settled
Battle, deck/pile/reward review, Codex/detail, Flock Record/playtest,
exit/reward, and outcome states instead of relying only on named controls. It
found route Cohesion at 18.7 CSS pixels high, route/battle HUD actions at 29.6,
card-picker scroll at 35.8x28, picker Cancel and Market back at 29.6 high,
Market Close at 23.3, combat log at 38.9, and Copy Route Link at 24.9. Shared
renderers now place independent 58-game-pixel interaction regions over the
compact artwork, and the boundary suite rejects any enabled hand-cursor target
below 44 CSS pixels in every exercised state. Copy Route Link still copies and
launches the same seed/run length. Inspected captures remain clean under
`.artifacts/test-results/min-supported/`; the required production client used
the live enlarged Deck HUD target to open Deck Review with matching state and
no browser errors under `.artifacts/final-touch-client/`. Strict TypeScript,
production build, broad boundary coverage, and focused market/card-picker/
route/outcome regressions pass. Full release validation also passes all nine
sequencing scenarios; app entry is 661.1 KiB and combined boot code is 689.6
KiB. Remaining release evidence is still five observed fresh-player sessions
through `docs/game/playtest-runbook.md`.

Current follow-up: P0 scene asset-readiness closure. Added a shared readiness
tracker for RouteScene and BattleScene with explicit loading, transition,
interactive, and full-art phases; separate time-to-first-interaction and
time-to-full-art clocks;
pending groups; and exact failed/timed-out group and key diagnostics in
`render_game_to_text`. Runtime image waits and required presentation-module
imports now release to their existing fallbacks after an eight-second ceiling
instead of risking an indefinite transition. Route tracks essential UI/art and
the broader current-scene UI bundle; Battle tracks preloaded combat art,
essential/core UI, combat FX, required render modules, and current optional FX.
Three focused Chromium regressions pass, including a deliberately stalled
route-map frame that becomes an interactive fallback and reports both affected
groups. Required production-client captures under
`.artifacts/readiness-client-route/` and
`.artifacts/readiness-client-battle-settled-real/` were visually inspected:
the route measured 436 ms to interaction / 1,535 ms to full art, combat measured
4,803 ms to interaction / 1,468 ms to full art (the encounter-intro beat
intentionally holds controls after art is complete), both had empty failure and
timeout lists, and neither emitted a browser-error artifact. Strict TypeScript,
the production build, and full release validation pass, including all nine
sequencing scenarios. Encounter intros now accept the current keyboard confirm,
gamepad A, or pointer/touch after a 600 ms readable floor; deterministic tests
prove early input queues without skipping the minimum and then releases combat.
App entry is 669.0 KiB and combined boot code is 697.6 KiB. Remaining release
evidence is still five observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Outcome input parity and playtest handoff. Run outcomes now
participate in the shared indexed input model instead of exposing pointer-only
commands: keyboard Previous/Next and gamepad D-pad focus Replay Flight or Main
Menu, Confirm/gamepad A activates the focused command, Back/gamepad B keeps its
safe menu shortcut, and the named focus ring matches `combatInputFocus` state.
Opt-in `?playtest=1` outcomes add a third Rate This Run command that opens Flock
Record directly on Save Data with the just-completed run selected and Fun
focused; normal outcomes remain two-choice and all feedback stays local. The
rating header now uses a short readable result/run reference. Strict TypeScript
and focused normal-keyboard, normal-gamepad, and playtest outcome-to-rating
Chromium regressions pass. The required built shared client exercised the live
title/route/battle path with complete asset readiness and no browser-error
artifact; outcome and rating-panel captures under
`.artifacts/test-results/outcome-input/` were inspected. Remaining release
evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`. The final production build and full release
validation pass, including all nine sequencing scenarios; app entry is 670.3
KiB and combined boot code is 698.9 KiB.

Current follow-up: Flock Record transition responsiveness. The Profile renderer
previously restarted its 200 ms full-camera entry fade on every interaction-led
display-list rebuild, so focus movement, tab changes, save actions, and each
playtest rating could briefly dim the whole screen. Profile state now owns a
one-shot entry-fade flag that resets only when the scene is entered; subsequent
renders stay at full brightness. `render_game_to_text` reports entry, running,
complete, and progress transition state. The playtest outcome regression now
uses deterministic game time to settle the entry transition, then proves both
moving to Fair and saving 1/5 do not restart it. Its inspected capture is
`.artifacts/test-results/outcome-input/playtest-rating-no-refade.png`. The
required production client used the real title Flock Record command and
keyboard Right to focus Contracts; `.artifacts/profile-transition-client/`
shows a fully bright rendered Profile, matching focus, a complete inactive
fade, all Profile art loaded, and no browser-error artifact. Strict TypeScript,
focused Profile/outcome tests, the production build, and full release
validation pass, including all nine sequencing scenarios. App entry remains
670.3 KiB and combined boot code is 699.0 KiB. Remaining release evidence is
still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Reinforced color-vision card cues. Settings now includes a
persistent `Color Cues` row with `Standard` and `Reinforced` modes across title,
route, and combat surfaces. Reinforced mode gives every combat-hand and card
reward choice a dark, high-legibility badge combining a distinct non-color
shape with its full `PLUMES`, `BASINS`, `QUILLS`, `NESTS`, `LEGEND`, `MOLT`,
`AVIARY`, or `SNAG` label. The normal and compact failure renderers preserve
the same redundant vocabulary, and changing the preference while paused
refreshes the underlying decision screen when play resumes. The preference is
safe-storage backed, exposed in text state, included in local save
export/restore, and old version-1 backups without the field restore to
`Standard` instead of becoming invalid. The focused regression proves
keyboard activation, persistence across reload, one cue per live hand/reward
card, shape/label metadata, pointer deactivation, and the badge-free Standard
mode. Five adjacent settings, screen-reader, and transactional backup
regressions pass. The required shared client drove real pointer/keyboard input
from title Settings through route commitment into combat; the inspected
`.artifacts/color-cues-shared-client/shot-0.png` shows five readable badges
without covering names, costs, art, rules, or targets, while text state reports
`colorCues: { preference: "reinforced", reinforced: true }` and no browser
error artifact. The inspected reward proof is
`.artifacts/test-results/color-cues/reinforced-reward.png`. Strict TypeScript,
production build, content/balance/economy audits, and full release validation
pass, including all nine sequencing scenarios. The cue renderer is a 1.5 KB
lazy chunk; combined boot is 704.9 KB minified / 188.8 KB gzip and passes the
enforced budget, with a non-blocking entry target warning at 676.2 KB. Remaining
release evidence is still five genuinely observed fresh-player sessions
through `docs/game/playtest-runbook.md`.

Current follow-up: Truthful route checkpoint feedback. The journaled active-run
save already mirrored each route checkpoint, but successful writes were silent
and RouteScene rewrote the identical payload on ordinary redraws. Route
checkpoints now compare the serialized run state, write only after real run
progress changes, and show a compact `FLIGHT SAVED` acknowledgement for 1.8
seconds. Storage failure instead shows `SAVE UNAVAILABLE` for 4.2 seconds and
announces that the tab should remain open when Screen Reader mode is active.
The same failed snapshot is not hammered repeatedly; the next genuine state
change retries, and a successful retry returns to the saved acknowledgement.
Text state exposes outcome, visibility, successful writes, and failures. The
focused browser contracts prove initial save, changed-state save, identical
redraw suppression, acknowledgement retirement, Continue restoration, forced
`QuotaExceededError`, and successful retry with matching stored/live Scrap.
The shared client capture at
`.artifacts/route-checkpoint-feedback-client-final/shot-0.png` shows the badge
clear of the guide and map controls; state reports one write, zero failures,
full route art, and no browser-error artifact. Strict TypeScript, the focused
checkpoint contracts, the production build, and full release validation pass,
including all nine sequencing scenarios. App entry is 674.9 KB minified and
combined boot code is 703.6 KB, within enforced budgets. Remaining release
evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: In-place title flight-length setup. Switching Quick/Full
previously restarted the entire MenuScene, rebuilding every title object and
replaying the 220 ms camera fade for a two-option setup change. MenuScene now
keeps typed references to both run-mode views and refreshes their fill, stroke,
labels, focus ring, and text state in place. Title transition telemetry exposes
generation and fade state. The remapped setup regression proves keyboard and
pointer toggles remain in one generation with one keyboard listener, one
gamepad listener, one focus ring, exact selected fills, a completed inactive
fade, exact cue counts, and the chosen Quick mode preserved through gamepad
launch. The required production client selected Quick with the real pointer;
the inspected `.artifacts/title-run-mode-client/` capture stays fully bright
and matches generation 1, Quick focus/selection, 24/24 essential title assets,
and no browser-error artifact. Strict TypeScript, the focused setup regression,
the production build, and full release validation pass, including all nine
sequencing scenarios. App entry is 670.9 KiB and combined boot code is 699.6
KiB. Remaining release evidence is still five genuinely observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Unified mute feedback and scene stability. Mute shortcuts on
Menu and Flock Record previously restarted their entire scenes, replaying entry
fades and presentation motion, while Codex rebuilt its full display list; these
paths also differed from the pointer control's generated pulse/wave feedback.
The shared audio control now has a named active hit target, dynamic Mute/Unmute
label, and per-control feedback counter. Menu, Route, Battle, Codex, and Profile
M handlers activate that same rendered control, with the former scene-specific
fallbacks retained if no control is available. Menu/Profile no longer restart
and Codex no longer redraws. A five-surface real-keyboard regression proves one
toggle and feedback presentation per M press, stable focus and scene/transition
markers, and retired burst state; the established pointer scenario still proves
the visual burst lifetime. The required production client captured the shared
handler while its generated wave was visibly active at the top-left muted
medallion; `.artifacts/mute-shortcut-client/` reports a settled full title,
generation 1, no running fade, 24/24 essential assets, and no browser errors.
Strict TypeScript, both focused audio regressions, the production build, and
full release validation pass, including all nine sequencing scenarios. App
entry is 671.4 KiB and combined boot code is 700.1 KiB. Remaining release
evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Pause/settings mute continuity. Pause and settings audio
callbacks still rebuilt their overlays after the shared control toggled, which
destroyed the generated pulse/wave before it could paint; Route and Battle also
leave their Phaser tween managers paused while their system overlays are open.
Settings-row pointer, keyboard, and gamepad activation now delegates to the
topmost rendered audio control, every visible medallion synchronizes in place,
and the Audio value/frame update locally without reconstructing the overlay.
The short pulse/wave presentation uses a canvas-frame driver independent of
gameplay pause, so it remains smooth and retires on time without advancing the
run. The settings medallion also moved right within its existing 58px target so
the full `Muted` label stays readable. A real-M regression covers Menu settings,
Route pause/settings, and Battle pause/settings, proving one toggle, persistent
overlay/control identity, synchronized labels, Audio focus, and feedback cleanup;
the existing five-scene regression is hardened against late Codex asset redraws
and passes alongside it. The required production client used real pointer input
on the built title Settings panel; the inspected
`.artifacts/overlay-audio-client-final2/shot-0.png` shows the unobstructed Muted
row and focused Audio setting, matching text state with no browser-error artifact.
Strict TypeScript, both focused regressions, the production build, and full
release validation pass, including all nine sequencing scenarios. App entry is
672.3 KiB and combined boot code is 701.0 KiB, within enforced budgets. Remaining
release evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Direction-neutral enemy attack telegraph swap. Replaced the
old arrow-centered `combat-enemy-attack-tell` with the approved sparse,
eight-node brass/gunmetal perimeter ring. The generated chroma source is
preserved at
`assets/concept-art/fx/sources/combat-enemy-attack-tell-source-v2-directionless.png`;
the prior v1 source remains untouched for rollback. Canonical PNG and WebP
runtime assets remain 512x512 RGBA, now have fully transparent center/corners,
occupy only 13.99% of the canvas, and are 118,967 / 36,992 bytes respectively.
The renderer no longer accepts a target coordinate or horizontally flips either
tell layer, and the focused combat timing regression now proves every active
tell sprite has `flipX === false`. Strict TypeScript, runtime asset validation,
the focused enemy-wind-up test, the production build, and full release
validation pass, including all nine sequencing scenarios. The required
shared client drove the real title -> route -> battle -> Roost path and captured
the live `Strike 6` wind-up at
`.artifacts/attack-tell-swap-client-final2/shot-0.png`; text state reports two
tell layers, one threat-charge burst, HP held at 38/38, and no browser-error
artifact. Visual inspection confirms the large empty aperture keeps the Roof
Rat fully visible while the existing flourish supplies motion and urgency, with
no arrow or apparent attack direction. Remaining release evidence is still five
genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

User-approved swap verification (2026-07-26): Confirmed the dense-center-free,
direction-neutral perimeter ring is the canonical live enemy attack tell. The
required shared web-game client captured a real `Strike 6` wind-up at
`output/web-game/shot-0.png`: both tell layers render around Roof Rat with the
center open, Cohesion remains held at 38/38, full art is ready, and no fresh
browser-error artifact was emitted. `npm run validate:runtime-assets` passes
all 961 optimized assets plus the world and combat-FX contracts; the attack
tell has zero visible center pixels, 33,937 visible perimeter pixels, a 1.05
quadrant ratio, and no retired directional source. The focused enemy wind-up
regression also passes. The repository-wide strict TypeScript command remains
red on two unrelated, pre-existing unused card-hover helper declarations in
`src/main.ts` (`addCardHoverDossierFrame` and
`addCardHoverStatChipFrame`); this swap introduced no TypeScript changes.

Current follow-up: Ordered first-flight teaching and clean enemy-turn handoff.
First-flight guide progress now follows its authored route -> card -> Roost ->
reward order even when a new player experiments out of sequence; gameplay is
never blocked, but an early Roost can no longer silently skip the dedicated
card and Roost lessons. The focused regression deliberately Roosts before
playing, proves the guide remains on `card` with zero recorded Roosts, then
finishes the normal card -> Roost -> reward sequence. Live shared-client input
reproduced that mistake through the real title, route, and battle screens.
During this pass, the enemy attack tell, commitment seal, wind-up plaque, impact
contact, and action trail were found lingering into Beat 2. `startPlayerTurn`
now retires the resolved enemy-phase FX tree before introducing the player-turn
rally. The inspected
`.artifacts/first-flight-ordered-guide/premature-roost-clean/shot-0.png` leaves
the Roof Rat and five-card hand unobstructed while `GUIDE: PLAY` remains active;
text state reports every enemy-phase FX family absent, player rally present,
and no browser-error artifact. Balance and economy audits pass, as do strict
TypeScript, both focused regressions, the production build, and full release
validation, including all nine sequencing scenarios. App entry remains 672.4
KiB minified and combined boot code is 701.0 KiB, within enforced budgets.
Remaining release evidence is still five genuinely observed fresh-player
sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Positive incoming-damage forecast language. The live
first-flight capture exposed a clarity contradiction in the Cohesion forecast:
`Incoming -6 / After 26` presented a negative delta under a label that otherwise
means the enemy's positive Tell. The HUD now uses the same arithmetic taught by
the guide. With Cover it reads `Incoming 6 - Cover 3 = 3` followed by `After 29
Cohesion`; without Cover it reads `Incoming 6` followed by `After 26 Cohesion`.
The threat amount, absorbed Cover, HP loss, and result remain sourced from the
authoritative preview rather than duplicated math. The focused browser
regression proves exact covered and uncovered labels and rejects the old
`Incoming -` notation. The required shared client drove the real title ->
route -> battle -> early Roost path, then played a Cover card. Inspected
captures at `.artifacts/incoming-forecast-clarity-client/shot-0.png` and
`.artifacts/incoming-forecast-clarity-client/partial-cover/shot-0.png` show both
two-line forecasts fitting the generated frame with no clipping; text state
matches 6 total, 3 blocked, 3 loss, 29 after, and no browser-error artifact.
Content audit remains at 32/32 checks with no drift; seeded telemetry remains
pipeline evidence only, not human fun/balance evidence. Strict TypeScript, the
focused forecast regression, production build, and full release validation
pass, including all nine sequencing scenarios. App entry is 672.5 KiB and
combined boot code is 701.1 KiB, within enforced budgets. Remaining release
evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Clear-center flourish cleanup. The approved direction-neutral
enemy attack tell is now the canonical runtime PNG/WebP and remains fully
unflipped at every call site. Its ornamental perimeter leaves the enemy art
visible through a transparent center. The Four-Suit Rally used a second dense
card-suit crest plus a wide perfect-chain overlay, so that presentation now uses
one procedural four-color segmented perimeter with a deliberately empty center.
The two legacy rally textures are no longer imported, preloaded, rendered,
exercised by the FX gallery, or listed as active roadmap work. Their runtime PNG
and WebP files and original generated sources have been deleted, so neither
tooling nor future implementation can confuse them with current assets.
Player-turn ceremony objects now complete during the handoff and are retired
before input unlocks, preventing the next card from stacking on `Your Turn`.
The leader-signature regression proves the procedural rally contract and proves
both old texture keys are absent; the enemy wind-up regression proves the attack
tell is direction-neutral. Strict TypeScript and all three focused combat/FX
regressions pass. The production build and full release validation also pass,
including all nine sequencing scenarios. App entry is 671.8 KiB minified and
combined boot code is 700.5 KiB, within enforced budgets. The production client
remains live at `http://127.0.0.1:5201`; the requested runtime tell is visible at
`assets/runtime/fx/combat-enemy-attack-tell.png`. Remaining release evidence is
still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Actionable-frame combat FX cleanup. Live first-flight
evidence showed that the player-turn draw/Resonance ceremony and the first
card's commit, trail, cast, Flow, bank, and leader-signature effects could remain
stacked over the flock after controls unlocked. Animated card plays now snapshot
the clean FX layer at commitment, preserve all action feedback through
resolution, hold the result for 240 ms, and atomically retire only objects
created by that card before restoring input. Player-turn handoff now retires its
entire protected transition beat—including draw cards, start-of-turn callouts,
and particles—rather than only the `Your Turn` crest and banner. Text state
reports the active feedback window, baseline/transient counts, and the last
retired count. The focused sequencing regression proves FX exist while the card
is pending, result/hit feedback resolves, then commit sigils, trails, cast
focus, transition draw, Resonance, callouts, and particle counts are all absent
at unlock. The required shared client replayed the same first-card path that
previously produced a fully obscured flock. The inspected
`.artifacts/player-card-feedback-cleanup-client-final/shot-0.png` is clean at
the actionable frame; state reports 32 retired objects, zero transients, zero
tracked action/transition FX, `combatAnimationPending: false`, and no browser
errors. Strict TypeScript, the three affected combat regressions, the production
build, and full release validation pass, including all nine sequencing
scenarios. App entry is 672.9 KiB minified and combined boot code is 701.5 KiB,
within enforced budgets. Remaining release evidence is still five genuinely
observed fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Progressive route readiness. A normal production-client
capture exposed that RouteScene still presented `Charting the route...` while
its state had already crossed the interactive milestone. The first route
decision now waits only for the three interaction-critical map pieces: the map
frame, selected-node ring, and commit medallion. The district backdrop, eight
painted node icons, and secondary route/overlay chrome stream afterward and
replace the existing procedural and glyph fallbacks in place. Full-art
readiness now truthfully requires both the painted route art and the complete
route UI group. A forced three-second painted-art delay proves the fallback map
is readable, has enabled route targets, exposes the Street `S` glyph, and does
not show the loading copy while `route-essential-art` remains pending. In the
normal shared-client run, interaction arrived at 1.172 seconds and full art at
1.790 seconds with no failed or timed-out groups. The inspected completed map
at `.artifacts/route-progressive-readiness-client-final/shot-1.png` is visually
complete and error-free. Authored balance, content, and economy audits remain
clean; seeded playtest telemetry still contains zero human rating responses
and is not treated as fun or balance evidence. Strict route readiness/status
regressions, the production build, and full release validation pass, including
all nine sequencing scenarios. App entry is 672.7 KB minified and combined
boot code is 701.4 KB, within enforced budgets. Remaining release evidence is
still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Screen-shake accessibility and persistence. Added an
explicit `Screen Shake` On/Off row as the eleventh Settings control on Menu,
Route, and Battle. The preference is safe-storage backed, defaults to On,
persists across reloads, is included in Flock Record export/transactional
restore, and keeps older version-1 backups compatible by defaulting a missing
field to On. Reduced Motion is a safety override: text state reports
`reducedByMotion: true`, the row reads `Off (Motion)`, and shake stays disabled
without overwriting the player's saved On preference. Every current combat
camera-shake path now consults the effective state. The direct regression
replaces the live battle camera's shake method, applies real enemy damage, and
proves zero calls for explicit Off, zero calls for Reduced Motion plus On, and
exactly one call after Full Motion restores the On preference. Settings,
color-cue, screen-reader, blocked-storage, remapped-controls, save export, and
transactional restore regressions all pass. The settings row test regex now
accepts multi-digit indices, and the remapped-confirm regression explicitly
navigates to Start Run before activating it. The required shared client opened
Settings with real input and produced `output/web-game/shot-0.png`; the
inspected panel shows the focused, unobstructed `Screen Shake: Off` row, while
`output/web-game/state-0.json` reports index 10 and the matching effective
state. Strict TypeScript and full release validation pass, including all nine
sequencing scenarios. App entry is 676.9 KB minified and combined boot code is
705.6 KB minified; enforced bundle gates pass, with the existing preferred
675 KB entry target warning still visible. Remaining release evidence is still
five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Reduced-flash accessibility and impact-layer cleanup. Added
`Flashes` as the twelfth Settings control across Menu, Route, and Battle, with
Full/Reduced choices, safe-storage persistence, profile telemetry, and Flock
Record export/transactional restore. Reduced Motion automatically applies the
safer effective state without overwriting the player's saved flash preference;
the row reports `Reduced (Motion)` while that override is active. Reduced mode
removes only abrupt additive duplicates from the impact flash, player
hit-confirm, enemy contact, and cover-shatter effects. Their normal contact art,
soft glow, damage numbers, animation, and audio feedback remain. The right
Settings column now uses a compact 54px rhythm for seven rows, preserving clear
space above the bottom commands and minimum touch targets. The direct combat
regression proves effect counts of 0/1/1/1 with zero flash bursts in Reduced
mode and 1/2/2/2 with one burst in Full mode; Reduced Motion independently
reproduces the safer counts. Backup download and transactional restore
regressions pass, along with the six focused Settings/accessibility regressions.
The required shared client produced `output/web-game/shot-0.png`; the inspected
panel is readable and unobstructed, while `output/web-game/state-0.json` reports
focus index 11, 12 rendered row frames, and the persisted effective Reduced
state. Strict TypeScript and full release validation pass, including all nine
sequencing scenarios. App entry is 677.8 KB minified and combined boot code is
706.4 KB minified; enforced bundle gates pass, with the existing preferred
675 KB entry target warning still visible. Remaining release evidence is still
five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Independent ambience audio control. Split the environmental
drone away from the adaptive Music gain and added a persistent `Ambience`
slider as the fourth audio row across Menu, Route, and Battle. Existing players
migrate without a volume jump because a missing ambience preference inherits
their current Music level; new defaults retain the established 78% balance.
The 13-row Settings panel now uses six evenly spaced rows on the left and seven
compact rows on the right, keeping every control clear of the bottom commands.
Text telemetry reports Music, SFX, and Ambience independently. Flock Record
exports the new preference, restores it transactionally, and accepts older
version-1 backups by inheriting their Music value. Focused tests prove that
adjusting Ambience leaves Music and SFX unchanged, all 20 Settings interaction
targets remain at least 44px at the minimum viewport, controls remapping still
works with the shifted row, and backup download/legacy restore remain safe.
Neighboring color-cue, screen-shake, reduced-flash, and screen-reader tests pass
4/4. The required shared client produced `output/web-game/shot-0.png`; visual
inspection shows the focused `Ambience: 85%` row with no overlap, while
`output/web-game/state-0.json` reports focus index 3, 13 row frames, three
volume sliders, and independent values. No fresh browser-error artifact was
emitted. Strict TypeScript and full release validation pass, including all nine
sequencing scenarios. App entry is 678.3 KB minified and combined boot code is
707.0 KB minified; enforced bundle gates pass, with the existing preferred
675 KB entry warning still visible. Remaining release evidence is still five
genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Default-boot bundle reduction through opt-in screen-reader
loading. Moved the scene-to-speech summary formatter out of the main entry and
behind the existing Screen Reader preference. The small synchronous preference,
ARIA-region, and observer layer still initializes on first render; default
players no longer download or parse the 3.5 KB formatter, while opted-in players
load it once before the first polled scene summary. The accessibility regression
now proves the module is absent with Screen Reader Off, becomes a fetched
resource after enabling it, and continues announcing Menu, Route, Combat, and
Settings focus correctly. The required shared client produced
`output/web-game/shot-0.png` and a clean default title boot; text state reports
`summaryLoaded: false`, the ARIA region ready, and the observer inactive. No
fresh browser-error artifact was emitted. Strict TypeScript, production build,
and full release validation pass, including all nine sequencing scenarios.
App entry fell from 678.3 KB to 675.0 KB, the prior preferred-target warning is
gone, combined boot code fell to 703.6 KB minified / 188.2 KB gzip, and the
lazy formatter is 3.5 KB minified / 1.5 KB gzip. Remaining release evidence is
still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Procedural route fairness stress gate. Audited all four
district generators across 256 deterministic seeds each (1,024 generated maps)
and promoted the audit into the regular sequencing/release suite. Every sample
is deterministic, fully reachable from entry, able to reach its boss, branched,
free of forced all-Rival choices, and compliant on consecutive combat, timely
safety, build access, and pressure caps across every enumerated route. All four
districts produced 256/256 unique maps, covered every Street and Rival encounter
pool entry, exposed 5-160 viable paths per map, and collectively produced
29,786 route-type path patterns with zero violations. The narrow production
test bridge uses a compact tuple so the extra gate keeps the main entry at the
preferred 675.0 KB target without a bundle warning. The required shared client
opened a live Rooftop Blocks run: the inspected 26-node, 10-column route became
interactive in 617ms, reached full art in 1,303ms, saved its opening checkpoint,
and reported no asset failures or fresh runtime errors. Strict TypeScript,
production build, and full release validation pass, now including all ten
sequencing scenarios. Combined boot code remains 703.7 KB minified / 188.2 KB
gzip. Remaining release evidence is still five genuinely observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Seeded opening-draw variety and fairness. Combat previously
copied the saved deck directly into the draw pile, so every encounter opened
with the same first cards in the same order. The first Fledgling lesson now
retains its authored five-card teaching hand, while every later encounter uses
a deterministic seed keyed by flight, district, and route node. Protection is
computed against the actual leader/card-modified hand size: when possible the
opening contains at least two affordable non-Snag cards and one card that can
pressure an enemy. Any protection swap is recorded in `openingDraw` telemetry
and the combat log, and the How to Play panel discloses the rule under `Fair
draws`. The same encounter RNG now also drives reshuffles, random draw
insertion, random support targets, map-start Supply picks, and reward drafts, so
the same seed plus decisions reproduces gameplay-relevant randomness. A
256-seed Tidewarden stress pass produced 252 unique ordered opening hands;
133 otherwise pressureless hands were corrected, every actual four-card hand
retained at least two playable cards and one pressure card, and repeated seeds
matched both hand and three-card reward draft exactly. The required shared
client visually confirmed the disclosure panel and the unchanged live tutorial
hand (`First Flight`, `Plume Flash`, `Plume Fledgling`, `Quill Point`, `Quill
Fledgling`); battle became interactive in 1,648ms with full art in 1,364ms and
zero asset failures. No fresh browser-error artifact was emitted. Deterministic
draw helpers were folded into the existing game-core boot chunk; app entry is
674.9 KB, game-core is within its 30 KB budget, and combined boot is 704.9 KB
minified / 188.6 KB gzip with no bundle warning. Strict TypeScript, production
build, and full release validation pass, now including all eleven sequencing
 scenarios. Remaining release evidence is still five genuinely observed
fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Strategic Ascension Tell ladder. Tier 3 no longer adds a
hidden flat damage tax. `Unfamiliar Tells` gives every cyclic enemy a seeded
alternate opening intent, so repeated flights are reproducible while different
flights demand adaptation from turn one. Tier 6 `Restless Patterns` preserves
only +1 attack damage (down from +2) and forces cyclic enemies to skip their
ordinary next pattern step after every Roost; the rerouted Tell is rendered
before the next decision and announced in the combat log. The four scripted
boss patterns remain in authored order. The title panel now explains the
complete modifier stack, and BattleScene text state exposes the tier rules plus
each enemy's current intent index. Incoming intent forecasts now include the
Ascension damage modifier, matching resolved Cohesion loss. Pure Ascension and
enemy-display calculations live with runtime/balance data, keeping the app
entry at the preferred 675.0 KB target; combined boot is 704.0 KB minified /
188.1 KB gzip with no warning. The focused mastery regression proves varied
seeded starts, same-seed opener and reroute reproduction, forecast/resolution
parity, log feedback, and unchanged boss sequencing. The isolated Tier 6 title
capture at `.artifacts/ascension-mastery-client/tier6-title.png` fits its
description in a 262x32 bound with no browser errors. The required shared
client completed title -> route -> live combat after the final refactor;
`.artifacts/ascension-mastery-client-final/shot-0.png` shows the clear
actionable board, full art, the correct Standard 8-damage intent forecast, and
no error artifact. Strict TypeScript, production build, and full
`npm run validate` pass, now including all twelve critical sequencing
scenarios. Remaining release evidence is still five genuinely observed
fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Final approved enemy attack-tell swap verification. The
direction-neutral eight-node perimeter ring is the canonical
`combat-enemy-attack-tell` PNG/WebP, with the old directional flip and target
coordinate removed from the renderer. A fresh required shared-client run
captured the real `Strike 6` wind-up at
`.artifacts/attack-tell-swap-current-live4/shot-0.png`: both tell layers render,
the existing flourish carries the motion, the clear center leaves the Roof Rat
visible, Cohesion remains 38/38 before impact, all asset groups are healthy, and
there is no browser-error artifact. The focused combat wind-up regression also
passes and continues to prove every tell layer is unflipped.

Current follow-up: Informative defeat review and final outcome layering. A loss
now aggregates damage by enemy move, identifies the fatal hit and greatest
pressure source, then presents one evidence-backed adjustment such as spending
unused Wingbeats, responding to an uncovered Tell, or improving tempo. Exact
seed replay and copyable flight sharing remain available, and the opt-in screen
reader announces the fatal move, evidence, tip, focused action, and Confirm
binding. Live testing caught and removed both retired attack FX and outcome
reveal FX that could obscure the report; the outcome root now remains cleanly
above its flourish. The final required shared-client capture at
`.artifacts/defeat-review-client-release-final/shot-0.png` shows `Strike 6`,
38 damage taken, the `UNSPENT WINGBEATS` coaching signal, both commands, full
art, zero active particles, no stale tell/contact layers, and no browser-error
artifact. Full `npm run validate` passes all release gates and 13 critical
sequencing scenarios. Combined boot remains 703.9 KB minified / 188.0 KB gzip;
the lazy boss dossier is 12.5 KB / 5.0 KB gzip. Remaining release evidence is
still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Directionless attack-tell contract and legacy retirement.
The obsolete arrow-filled
`assets/concept-art/fx/sources/combat-enemy-attack-tell-source-v1.png` has been
deleted; v2 directionless source art is now the only owned attack-tell source.
`tools/validate-combat-fx-contracts.mjs` is part of runtime-asset validation and
rejects the retired source, a non-RGBA or resized runtime PNG, any visible pixel
inside the central 30% opening, opaque corners, directionally shifted alpha
mass, excessive 180-degree silhouette difference, or imbalanced quadrants. The
approved asset passes with 33,937 visible perimeter pixels, zero visible center
pixels, 10.43 mean rotational difference, and a 1.05 quadrant ratio. A new
critical regression proves both live layers remain centered and unflipped at
the 1000x560 minimum supported landscape under Lean effects, reduced motion,
and high contrast; the inspected capture is
`.artifacts/test-results/directionless-attack-tell/minimum-landscape-lean-reduced-high-contrast.png`.
The required shared client independently drove title -> route -> combat and
captured the real `Strike 6` wind-up at
`.artifacts/attack-tell-contract-release-final/shot-0.png`: full art, 38/38
Cohesion before impact, two tell layers, an unobstructed Roof Rat, healthy asset
groups, and no browser-error artifact. Full `npm run validate` passes every
release gate and all 14 critical sequencing scenarios. Remaining release
evidence is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Accessible completed-flight review. Win and defeat outcomes
now offer `Flight Details`, an on-demand report that makes the recorded run
legible instead of leaving it only in local telemetry. It shows the final deck
with upgrades, deterministic route path, Waymarks and used Supplies, route and
reward decisions, Signal choices, objective results, card/overextension and
Cover/Wingbeat behavior, difficulty, duration, Cohesion, damage, and Scrap.
The full-screen review supports pointer Close, keyboard Confirm/Back, and
gamepad A/B; Previous/Next are intentionally paused while it is open, and the
opt-in screen reader narrates the result, complete deck, route, kit, and close
bindings. A minimum-viewport regression protects section bounds, a 190x58
close target, focus, narration, keyboard/gamepad close/reopen, and pointer
close. Live validation caught a cramped Results/Close boundary and moved the
Results section upward before release. The report builder and renderer now
live in the lazy boss-dossier chunk; the app entry is 677.2 KB minified /
178.1 KB gzip, below the enforced 700 KB ceiling but 2.2 KB above the preferred
675 KB target, while combined boot is 706.2 KB / 188.6 KB and the dossier is
17.3 KB / 6.5 KB. The final required shared client drove title -> route ->
combat -> defeat -> Flight Details at
`.artifacts/flight-details-client-final/shot-0.png`; state confirms full art,
10 deck entries, populated path/decision/result sections, visible
`Close Flight Details` focus, no failed asset groups, and no browser-error
artifact. Production build and full `npm run validate` pass every release gate
and all 15 critical sequencing scenarios. Remaining release evidence is still
five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Durable Flight Log and historical sharing. `Flock Record`
now exposes all completed flights in the bounded 50-flight journal through
seven-row pages, with result, seed, leader, difficulty, mode, date, duration,
Cohesion, turns, deck size, route length, Waymarks, and Supplies. Confirm opens
the exact full `Flight Details` report for the selected historical run, and
`Copy Flight Link` produces its deterministic seeded-flight URL. The history
loader now strictly sanitizes bounded input, de-duplicates repeated IDs with
the newest record winning, discards malformed summaries, validates nested
cards/items/decisions/results, and remains compatible with transactional local
backup and restore. Pointer, configurable keyboard, and gamepad navigation
share visible focus, while the opt-in screen reader announces the selected
flight, page position, report contents, actions, and bindings. Newer/Older
pointer controls, Page Up/Down, and controller shoulders cross pages while
preserving the selected row. The focused 1000x560 regression protects
58-game-pixel rows and page controls (at least 44 CSS pixels), newest-first
ordering, all ten injected valid records across two pages, sanitization, review,
clipboard sharing, and every input path; seven neighboring save/history/input
tests also pass. The final required shared-client run drove
title -> route -> combat -> defeat -> Main Menu -> Flock Record -> Flight Log
and captured `.artifacts/flight-log-pagination-client-final/shot-0.png`; state
contains the real 10-card deck, route, decisions, results, copy focus, and
page metadata, with no browser-error artifact. The inspected pagination
captures are
`.artifacts/test-results/flight-log/minimum-landscape-history.png` and
`.artifacts/test-results/flight-log/minimum-landscape-history-page-2.png`.
Full `npm run validate` passes every release gate and all 16 critical
sequencing scenarios. The app entry remains 681.1 KB minified / 179.1 KB gzip
and combined boot remains 710.2 KB / 189.7 KB gzip because pagination stays in
lazy chunks; both hard budgets pass, with aspirational warnings above 675 KB
and 710 KB respectively. Remaining release evidence is still five genuinely
observed fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Accessible combat pile inspection. Deck, Draw, and Discard
reviews now have full pointer, configurable keyboard, and standard-gamepad
parity. Remapped Previous/Next, Tab/Shift+Tab, and Up/Down move the selected
row; D-pad directions do the same; controller shoulders page by seven; and
Back/B closes without changing the turn, Wingbeats, or Cohesion. Every card
copy is focused by its unique instance ID, so duplicate definitions no longer
select or inspect the wrong copy. A strong cyan outline and left marker keep
focus visible against ornate frames, mouse-wheel movement preserves a visible
selection, and the opt-in screen reader announces the pile, selected position,
card rules, zone, cost, and legal controls. The focused 1000x560 regression
proves seven-row visibility, at least 44-CSS-pixel targets, duplicate-card
identity, controller paging, narration, and state-safe close behavior; three
neighboring inspector tests pass. The required shared client independently
drove title -> route -> live combat -> Draw Pile and captured
`.artifacts/combat-pile-accessibility-client-release-final/shot-0.png`;
authoritative state identifies `Locked Nest` as item 2 of 5, the lazy inspector
renderer is healthy, full art is present, and no browser-error artifact was
emitted. Full `npm run validate` passes every release gate and all 17 critical
sequencing scenarios. The app entry is 683.1 KB minified / 179.6 KB gzip and
combined boot is 712.1 KB / 190.2 KB gzip; both hard budgets pass while the
preferred 675 KB and 710 KB targets emit warnings. Remaining release evidence
is still five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Cleared-card zone and run-deck integrity. Cards resolving
`exhaustSelf()` now move into a dedicated combat-only Cleared pile instead of
falling out of every owned-card collection. The permanent deck, post-combat
reward pool, run snapshot, outcome report, and resume state continue to include
those cards, while draws and reshuffles correctly exclude them until the next
combat. Deck Review now includes Cleared rows, a fourth zone count and icon,
unique-instance focus, full card details, and screen-reader narration; normal
combat narration reports a nonzero cleared count. Visual inspection also caught
Bent Feather being mislabeled as an Attack because self-applied Winded matched
the enemy-debuff classifier, so only `applyWinded(target, ...)` now implies an
attack and self-pressure Snags correctly read as Utility. The critical 1000x560
regression plays the real Bent Feather, proves one playable card leaves combat
without reducing the 11-card run deck, verifies the saved snapshot still owns
it, navigates to its Cleared row by keyboard, and inspects
`.artifacts/test-results/cleared-card-zone/minimum-landscape-cleared-card.png`.
The required shared client independently drove title -> route -> live combat ->
Deck Review at `.artifacts/cleared-zone-client-final/shot-0.png`; state reports
5 Draw, 5 Hand, 0 Discard, 0 Cleared, a 10-card deck, full art, a healthy lazy
inspector, and no browser-error artifact. Full `npm run validate` passes every
release gate and all 18 critical sequencing scenarios. The app entry is
683.4 KB minified / 179.7 KB gzip and combined boot is 712.5 KB / 190.3 KB
gzip; hard budgets pass while the preferred 675 KB and 710 KB targets warn.
Remaining release evidence is still five genuinely observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Distinct Waymark Reward Identities

- Removed the last four duplicate Waymark trigger/effect signatures across all
  58 rewards. Black Ink Pin now banks two held cards plus Resonance, Parade
  Mirror rewards sequencing a third card, Fresh Pinfeather grants immediate
  Molt Wingbeat tempo plus a draw, and Double-Packed Buckle chains Supply use
  into immediate Wingbeat while preserving its next-Supply repeat.
- Preserved the counterpart niches: Wind-Step Tag provides delayed energy and
  one retained card, Plumes Applause Cap rewards a clean Roost, Molt Metronome
  pays out next turn, and Supply Bell favors draw velocity.
- Runtime validation now rejects any future Waymarks with identical trigger
  and sorted-effect signatures. The focused browser regression verifies exact
  trigger timing, once-per-combat latching, immediate-versus-delayed tempo,
  Supply-repeat consumption, and visible combat feedback.
- Strict TypeScript, focused Waymark regressions, runtime validation, seeded
  balance/economy audits, content audit, production build, bundle budgets, and
  deployment cache/security validation pass. The app entry is 675.0 KB
  minified / 177.6 KB gzip and combined boot is 704.1 KB / 188.3 KB gzip.
- The inspected focused capture is
  `.artifacts/test-results/waymark-identity/double-packed-feedback.png`. The
  required shared client independently reached the full-art First Flight board
  at `output/web-game/shot-0.png`, with five readable cards, intact onboarding
  marks, and no fresh browser-error artifact.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Eager card-detail legacy cleanup. The route/deck/market
card-detail presentation had already moved into `card-hover-detail` and the
combat hand dossier into `render-hand`, but two superseded frame helpers
remained in `src/main.ts`. Removed both dead implementations, restoring strict
`noUnusedLocals`/`noUnusedParameters` compilation and ensuring future work
cannot accidentally wire the old eager render path back into boot. The two
focused browser regressions for full card-choice details and the generated
combat hover dossier pass. Production build, bundle validation, and deployment
cache/security validation pass; the app entry remains at the preferred
675.0 KB / 177.6 KB gzip target and combined boot remains 704.1 KB /
188.3 KB gzip. The required shared client reached a full-art, interactive route
with both lazy detail textures loaded and no failed asset groups; the inspected
capture is `output/web-game/shot-0.png`. Remaining release evidence is still
five genuinely observed fresh-player sessions through
`docs/game/playtest-runbook.md`.

Current follow-up: Singleton deck ownership and duplicate-safe persistence.
Bird Squad's one-of-a-kind deck rule is now enforced at every persistence
boundary and pressure-effect path. Active-run recovery, completed-flight
history, combat restoration, and combat snapshots canonicalize cards by ID,
preserve first-seen order, reject unknown definitions, and keep the upgraded
copy whenever malformed or legacy data contains both base and upgraded
entries. Route and combat Snag effects no longer add a second copy; route
previews truthfully show an unchanged deck count, while combat rejection
produces a `Snag blocked` visual, block audio, and a named combat-log
explanation. Specific route rewards are also defensive against stale duplicate
grants. The critical 1000x560 regression proves repeated route and combat
pressure cannot create duplicates, an upgraded duplicate survives
canonicalization, the saved snapshot remains singleton, and the lone Tangled
Line stays fully inspectable and screen-reader announced. Its inspected capture
is `.artifacts/test-results/singleton-deck/minimum-landscape-singleton-snag.png`.
The required shared client independently drove title -> route -> live combat ->
Deck Review and captured `.artifacts/singleton-deck-client/shot-0.png`; state
reports 5 Draw, 5 Hand, 0 Discard, 0 Cleared, ten unique deck IDs, full art, a
healthy lazy inspector, and no browser-error artifact. Full
`npm run validate` passes every release gate and all 19 critical sequencing
scenarios. The app entry is 684.3 KB minified / 179.9 KB gzip and combined boot
is 713.5 KB / 190.6 KB gzip; hard budgets pass while the preferred 675 KB and
710 KB targets warn. Remaining release evidence is still five genuinely
observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Preferred Boot Targets Restored

- Moved the route and combat Flock Stats presentation into the retryable lazy
  `flock-stats-overlay` module. Both overlays retain their generated flourish,
  plaque, caption, row, footer, and combat-tally art; opening or retry failure
  always leaves a closeable UI.
- Moved route abandon confirmation into the existing lazy system-overlay
  boundary without changing its two-command behavior, reduced-motion handling,
  or touch targets.
- Added the lazy `card-hover-detail` presentation module for route card hover,
  Deck Review detail, and market item dossiers. Route entry begins loading it
  before those optional interactions, while card hover retains an immediate
  lightweight shell, retryable loading, and stale-hover protection.
- Added bundle-composition diagnostics behind
  `BIRD_SQUAD_BUNDLE_REPORT=1`, plus hard per-chunk budgets for the new Flock
  Stats and card-detail boundaries. Deployment validation rejects either chunk
  if it becomes an eager title-screen preload.
- Production app entry falls from 684.3 KB to 674.8 KB minified / 177.6 KB
  gzip. Combined app boot code falls from 713.5 KB to 703.9 KB / 188.3 KB
  gzip. Both preferred targets now pass without changing their budgets.
- Focused regressions pass for full card-choice hover details, route abandon
  confirmation, Flock Stats art, and minimum-viewport route commands. The
  required shared client captures
  `.artifacts/flock-stats-lazy-client-2/shot-0.png` and
  `.artifacts/card-detail-lazy-client-final/shot-0.png`; authoritative state reports
  every expected generated frame rendered, full-art readiness, and no browser
  errors. The inspected 1000x560 abandon dialog is
  `.artifacts/test-results/min-supported/route-confirm-exit-1000x560.png`.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  runtime assets, canonical world-asset and combat-FX contracts, deployment
  cache/security, enemy variety, Minor Arcana overlays, both preferred bundle
  targets, and all 19 critical sequencing scenarios.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

Current follow-up: First-combat start-here guidance. The live card step now
deterministically recommends an affordable non-Snag opener, preferring an
enemy-targeted Flow builder, and links the resource lesson to the board with a
gold `START HERE` card pulse and `PLAY HERE` legal-target label. The guide rail
names the card, exact Wingbeat cost, target, and Flow result without disabling
any alternative. Text state and opt-in screen-reader output expose the same
recommendation, and all card/target marks retire immediately after a valid
first play. Focused First Flight progression/Flow tests and the existing
menu-route-combat-settings screen-reader suite pass; `npm run build` and hard
bundle validation pass. The required shared client captured the settled lesson
at `.artifacts/goal-first-card-guide-final/shot-0.png`, then played First Flight
on Roof Rat with real pointer input and advanced cleanly to the Roost lesson at
`.artifacts/goal-first-card-guide-played/shot-0.png`, with full-art readiness
and no browser error artifact. Combined boot is 705.7 KB / 188.7 KB gzip; the
675.0 KB app entry and 704.1 KB combined boot now pass both preferred targets.
Remaining release evidence is still five genuinely observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

Current follow-up: Default-off screen-reader runtime extraction. The compact
preference/state façade remains synchronous, while the polling interval,
live-region announcer, and summary loader now live in the opt-in
`screen-reader-runtime` chunk. With Screen Reader off, neither accessibility
runtime resource loads and no observer runs; opting in loads both modules,
announces through menu, route, combat, and settings focus, and opting out stops
polling and clears the region. The new chunk has a 4 KB hard budget,
exactly-one hashed-output contract, and modulepreload rejection. Strict
TypeScript, focused screen-reader and First Flight regressions, production
build, bundle validation, and deployment-cache/security validation pass. The
app entry is 675.0 KB / 177.6 KB gzip, combined boot is 704.1 KB / 188.3 KB
gzip, and the lazy runtime is 1.4 KB / 0.7 KB gzip. The required shared client
capture at `.artifacts/goal-screen-reader-lazy-guide/shot-0.png` retains the
complete First Flight recommendation board at full-art readiness, with Screen
Reader off, no observer/summary activity, and no browser error artifact.
Full `npm run validate` passes every release gate and all 19 critical
sequencing scenarios.
Remaining release evidence is still five genuinely observed fresh-player
sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Strategic Starter Preens

- Audited all 110 cards at base and Preened states. No two different cards
  share an exact cost, target, normal-effect, and Molt-effect signature; the
  identical pairs are the intentionally non-upgradable Snags. Runtime
  validation now rejects future cross-card combat-identity collisions across
  all 220 base/Preened states.
- Reworked all seven starter cards whose Preen previously changed only numbers.
  First Flight gains first-play Wingbeat tempo; Plume Flash draws when
  Resonance was already held; Quill Point and Quill Fledgling turn established
  Winded into Wingbeat; Open Basin converts full-Cohesion healing into Open Sky
  Guard; Locked Nest adds Guard after a complete brace; and Nest Fledgling
  primes the next Nest brace after fully covering the incoming hit.
- Runtime validation now requires every card in every Leader's shared
  ten-card starter deck to change decision shape, condition, effect, or cost
  when Preened. All ten currently satisfy that contract.
- The focused browser regression executes the new conditions on both sides of
  every gate and confirms exact draw, Wingbeat, Cover, Guard, next-turn draw,
  and next-Nest results. The inspected in-combat Preen dossier is
  `.artifacts/test-results/starter-preen-first-flight.png`.
- The required shared client independently reached a full-art, interactive
  First Flight battle at `output/web-game/shot-0.png`; the five opening cards,
  `START HERE` and `PLAY HERE` guidance, incoming forecast, and text state agree,
  with no fresh browser-error artifact.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  assets, world and direction-neutral combat-FX contracts, bundle and deployment
  gates, enemy variety, Minor Arcana overlays, and all 20 critical sequencing
  scenarios. App entry remains 675.0 KB minified / 177.6 KB gzip and combined
  boot remains 704.1 KB / 188.3 KB gzip. Balance, content, and 500-seed economy
  audits also pass.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Strategic Common Reward Preens

- Extended the strategic Preen standard to the cards players encounter most
  often after the starter deck. All 24 common reward cards now change decision
  shape when Preened; 22 formerly number-only upgrades were redesigned.
- The new upgrade hooks reward distinct play patterns: finishing weakened
  targets, reading attack intent, spending or holding Resonance, preserving
  full Cohesion, maintaining Open Sky, completing a brace, timing first plays,
  breaking enemy Cover, retaining a hand, and banking next-turn resources.
- Corrected the covered-target resolution order for Sheathed Quills, Scattered
  Quills, and Scavenger Eye so their Cover-dependent rewards resolve before
  the card removes or consumes that Cover.
- Runtime validation now rejects any common reward whose Preen only increases
  numbers. The focused regression checks all 24 authored transformations and
  executes representative true/false branches in combat. Its inspected dossier
  capture is `.artifacts/test-results/common-preen-crossed-quills.png`.
- The required shared client reached a full-art, interactive First Flight
  battle at `output/web-game/shot-0.png`; five opening cards, onboarding marks,
  target state, and incoming forecast render cleanly. No fresh browser-error
  artifact was produced.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  assets, canonical world and direction-neutral combat-FX contracts, bundle and
  deployment gates, enemy variety, Minor Arcana overlays, and all 21 critical
  sequencing scenarios. App entry remains 675.0 KB minified / 177.6 KB gzip
  and combined boot remains 704.1 KB / 188.3 KB gzip. Balance, content, and
  500-seed economy audits also pass.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Strategic Reward-Wide Preens

- Completed the strategic Preen pass across the entire reward pool. Redesigned
  59 formerly number-only upgrades: 24 uncommon, 11 rare, and 24 legendary.
  Together with the prior starter/common work, all 100 playable cards now gain
  a new decision, condition, effect, or cost tradeoff when Preened.
- New hooks reinforce each card's existing identity: Cover breaking, Winded
  setup, Resonance spending and hoarding, Open Sky, full/low Cohesion,
  complete braces, suit sequencing, first-play timing, finishers, recursion,
  retention, and next-turn planning.
- Runtime validation now enforces the decision-shape contract for all 90 reward
  cards by rarity, while the 220-state cross-card identity check still rejects
  duplicate base/Preened combat signatures.
- The reward-Preen regression statically verifies all 90 reward transformations
  and exact authored hooks for the 81 redesigned cards, then executes
  representative true/false branches from every rarity in live combat. It also
  replaced a fixed dossier-art delay with explicit texture readiness and a
  rendered-image assertion. The inspected full-art capture is
  `.artifacts/test-results/common-preen-crossed-quills.png`.
- The required shared client independently reached the full-art, interactive
  First Flight battle at `output/web-game/shot-0.png`, with five readable cards,
  accurate onboarding marks, no pending or failed asset groups, and no fresh
  browser-error artifact.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  assets, canonical world and direction-neutral combat-FX contracts, bundle and
  deployment gates, enemy variety, Minor Arcana overlays, and all 21 critical
  sequencing scenarios. App entry remains 675.0 KB minified / 177.6 KB gzip,
  combined boot remains 704.1 KB / 188.3 KB gzip, and runtime data is
  310.2 KB / 58.9 KB gzip. Balance, content, and 500-seed economy audits pass.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Clear-Center Attack Tell and Cross-Stance Card Identity

- Accepted the open-center enemy pre-attack replacement as the canonical
  runtime tell. The former arrow-filled source remains deleted; the active PNG
  and WebP contain only the balanced perimeter ring, so the existing animated
  threat flourish carries the warning without covering the enemy.
- Re-ran the minimum-landscape tell regression in lean graphics, reduced
  motion, and high contrast. The inspected wind-up capture is
  `.artifacts/test-results/directionless-attack-tell/minimum-landscape-lean-reduced-high-contrast.png`;
  the enemy remains visible through the transparent center and neither tell
  layer flips toward a target.
- Eliminated five active-stance collisions discovered by the stricter card
  audit. Plume Matron now ignites empty Resonance and converts Molt Resonance
  into Wingbeats; Twin Basin Bond rewards an already-Winded target with
  recovery; Shared Nest reads an attacking enemy and braces with Cover; Shiny
  Toolkit retains its broader Plume-sequencing role.
- Runtime validation now rejects any different cards that share the same cost,
  inferred active target, and ordered effect list in an individual Normal or
  Molt stance. All 420 base/Preened active stances pass this identity contract.
- Focused live-combat coverage executes both sides of the new Resonance,
  Winded, and attack-intent branches. The neighboring Preen, preview-order,
  Molt-transform, and next-attack prediction regressions also pass.
- The required shared client independently reached a full-art battle at
  `.artifacts/goal-final-swap/shot-0.png`; state reported no pending or failed
  asset groups and produced no browser-error artifact.
- Production build, documentation, runtime/data, 961 optimized assets,
  canonical world and direction-neutral combat-FX contracts, bundle and
  deployment gates, enemy variety, Minor Arcana overlays, all 21 critical
  sequencing scenarios, and the balance/content/500-seed economy audits pass.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Configurable Text Pace

- Added a separate Text Pace preference with Relaxed, Standard, and Fast modes
  to the shared Menu, Route, and paused-Combat Settings panel. The new fourteenth
  row uses the same pointer, keyboard, gamepad, touch-target, focus-ring, and
  storage-fallback contracts as the other settings.
- Text Pace scales only readable presentation windows: encounter-intro dismissal
  floors, combat-banner holds, and floating-callout holds. Relaxed uses a 1.45x
  hold scale, Standard 1x, and Fast 0.65x. Combat Pace, damage timing, effect
  order, animation locks, enemy Tells, and combat math remain independent.
- Added `textPacing` to authoritative Menu, Route, and Battle text state with
  the active preference and concrete timing values. Save Data backup/restore
  now includes `birdsquad.textPace`; older backups without the field restore
  safely to Standard.
- Focused coverage proves all three encounter reading floors, real banner
  lifetime differences, pointer persistence across scenes, keyboard/gamepad
  navigation, minimum touch targets, blocked-storage fallback, adjacent
  Color Cues/Screen Shake/Flashes/Screen Reader navigation, current backup
  export, and legacy restore compatibility.
- The required shared client opened the complete fourteen-row panel at
  `.artifacts/text-pace-client-settings/shot-0.png`. Text Pace is visible
  between Combat Pace and Screen Reader, all rows are unclipped, text state
  reports the Standard 600 ms reading floor, and no browser-error artifact was
  produced.
- Production build and all 22 critical sequencing scenarios pass. App entry is
  675.8 KB minified / 177.9 KB gzip: below the 700 KB hard budget and 0.8 KB
  above the preferred target; combined boot remains below its preferred target
  at 705.1 KB / 188.6 KB gzip.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Configurable Animation Pace

- Added a standalone Animation Pace preference with Relaxed, Standard, and
  Fast modes to the shared Menu, Route, and paused-Combat Settings surface.
  It is the fifteenth setting row and supports pointer, touch, keyboard,
  standard gamepad, focus-ring, and blocked-storage interaction.
- Animation Pace scales visual tween and sprite-animation clocks to 0.8x, 1x,
  or 1.3x across menu, profile, route, and combat scenes. Combat Pace timers,
  damage resolution, enemy intent, Text Pace reading floors, effect order, and
  game math remain independent.
- The preference persists as `birdsquad.animationPace`, participates in local
  Save Data backup/restore, and defaults legacy backups without the field to
  Standard. Completed-run telemetry now records the selected animation pace,
  and the local dashboard reports its distribution.
- Rebalanced the complete Settings panel into eight left-column and seven
  right-column rows. The new control uses a 276x58 interaction target; all
  fifteen rows and both bottom commands remain visible without overlap.
- Focused browser coverage proves the visual clock rates are ordered while
  combat and text pacing stay fixed, then covers pointer persistence across
  scenes, keyboard/gamepad navigation, minimum viewport touch targets,
  adjacent visual-accessibility controls, screen-reader focus, current backup
  export, and legacy restore compatibility.
- The required shared client selected Fast with a real pointer and produced
  `.artifacts/animation-pace-client-fast/shot-0.png`. Text state reports focus
  on Animation Pace plus 1.3x tween and sprite clocks, all fifteen rows are
  unclipped, and no browser-error artifact was produced.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  assets, world and direction-neutral combat-FX contracts, bundle/deployment,
  enemy variety, Minor Arcana overlays, and all 23 critical sequencing
  scenarios. App entry is 676.8 KB minified / 178.2 KB gzip: below the 700 KB
  hard cap and 1.8 KB above the preferred target; combined boot remains below
  its preferred target at 706.1 KB / 188.9 KB gzip.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Complete Browser Matrix Stabilization

- Ran the complete production Playwright matrix after the Settings and
  direction-neutral attack-tell work. The first sweep found 16 isolated
  failures among 214 scenarios; the surrounding gameplay, accessibility,
  persistence, responsiveness, and content systems remained operational.
- Fixed a real fresh-route art omission by wiring reward badges and the
  cache, Open Sky, and Sky Guard choice medallions into RouteScene's deferred
  UI bundle. These assets now appear on the first route without relying on a
  prior BattleScene preload.
- Hardened scene text-state reporting across menu/profile handoffs so a stale
  reporter cannot read a destroyed camera. The shared browser harness now
  waits for active BattleScene roots and FX layers instead of accepting
  recycled scene references.
- Replaced race-prone route/reward visual sampling with full-art or live-scene
  readiness, corrected a recursive route-supply frame counter, and aligned
  assertions with the current 15 Settings rows, 58 px touch targets, three
  outcome commands, and authored Waymark timing.
- The final production sweep passes all 214/214 browser scenarios with zero
  failures in 28.8 minutes. Evidence is stored in
  `.artifacts/full-e2e-final-clean-gate.out.log`.
- Full `npm run validate` passes documentation, runtime/data, 961 optimized
  assets, canonical world and direction-neutral combat-FX contracts,
  bundle/deployment/security checks, enemy variety, Minor Arcana overlays,
  and all 23 critical sequencing scenarios. App entry is 677.0 KB minified /
  178.2 KB gzip under the 700 KB hard cap; combined boot is 706.3 KB /
  188.9 KB gzip.
- Remaining release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 First-Run Route Command Hit Target

- A fresh-state production-client pass found that the visible `TAKE ROUTE`
  label did not accept pointer input; only its adjacent round medallion was
  interactive. This made a correct first-run click appear ignored.
- Expanded the route commit hit target to cover the complete visible label
  and medallion while retaining the existing 82 px vertical touch target,
  hover tooltip, keyboard input, and gamepad input.
- Added a real-canvas pointer regression that clicks the label center, proves
  the hit target encloses the full label, and waits for BattleScene. The
  focused production Playwright scenario passes.
- Replayed that exact label click with the required shared production client.
  `.artifacts/first-run-audit/battle-pointer-fixed/shot-0.png` reaches the
  playable First Flight hand with full art, route commit progress `1`, no
  pending animation, and no browser errors.
- This automation establishes interaction correctness and first-run
  readiness; it does not replace the five genuinely observed fresh-player
  sessions still required through `docs/game/playtest-runbook.md`.

## 2026-07-26 Direction-Neutral Enemy Wind-Up Chain

- A live first-combat capture revealed that the approved direction-neutral
  tell asset was still paired with a separate enemy-to-flock action trail
  during Wind-up. The extra streak recreated the left-pointing read even
  though the tell artwork itself no longer contained an arrow.
- Removed the directional trail from damaging Wind-up presentation. The
  enemy-centered perimeter flourish, move plaque, target glow, audio cue, and
  readable hold remain; directional travel still begins during Commit, where
  the enemy actually releases the attack.
- The minimum 1000x560 Lean + Reduced Motion + High Contrast regression now
  asserts that Wind-up renders two neutral tell layers and zero action-trail
  objects. The broader timed attack-resolution regression also proves that
  the trail is absent in Wind-up while damage remains deferred until Impact.
- Both focused production scenarios pass. The required shared client drove a
  fresh run through Route, Roost, and the live `Strike 6` Wind-up; text state
  reports the tell rendered, action trail `0`, full art, and no browser
  errors. Inspected captures are
  `.artifacts/first-combat-audit/windup-directionless/shot-0.png` and
  `.artifacts/test-results/directionless-attack-tell/minimum-landscape-lean-reduced-high-contrast.png`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Approved Directionless Attack-Tell Swap

- Confirmed the user-approved, centerpiece-free eight-node perimeter ring is
  the canonical enemy attack-tell treatment. The owned v2 directionless source
  remains paired with the existing 512x512 runtime PNG/WebP; the retired
  arrow-filled source stays deleted.
- Runtime asset validation passes with a completely clear center, 33,937
  visible perimeter pixels, balanced quadrants, and a direction-neutral
  silhouette. Both focused combat regressions pass and continue to prove the
  live tell is centered, unflipped, and has no action trail during Wind-up.
- The required shared client captured a fresh real `Strike 6` warning at
  `.artifacts/attack-tell-approved-swap-final/shot-0.png`. The two-layer ring
  and existing flourish render around an unobstructed Roof Rat, Cohesion stays
  at 38/38 before Impact, full art is ready, and no browser-error artifact was
  produced.
- The game remains live at `http://127.0.0.1:5201/`.

## 2026-07-26 Persistent Waymark Review And Comparison

- Reworked the Route owned-Waymark drawer into a persistent review surface:
  selecting a Waymark now exposes its full description, friendly trigger,
  numbered effect order, synergy tags, source, rarity, and art without relying
  on hover.
- Added pin-to-compare behavior across pointer/touch, keyboard `C`, and
  controller `X`. Arrow keys, Tab, configured Previous/Next controls, pointer,
  scrolling, and Back all retain one coherent selected/pinned state.
- Added matching Route text-state and screen-reader summaries, including exact
  trigger and ordered raw effect grammar for automation and accessibility.
- Kept the review renderer on demand in `src/game/waymark-review.ts` at
  2.9 KB / 1.4 KB gzip. Moved Route's large diagnostic snapshot assembly into
  `src/game/route-debug-state.ts` at 19.5 KB / 4.3 KB gzip, restoring production
  startup budgets to 681.0 KB entry and 710.2 KB combined boot. Both chunks
  have hard size limits and are prohibited from title-screen modulepreloads.
- Focused pointer, keyboard, controller, screen-reader, scroll, and minimum-size
  tests pass. The inspected comparison capture is
  `.artifacts/test-results/route-waymark-comparison.png`. The required shared
  client loaded the full Route snapshot in
  `.artifacts/waymark-swap-shared-client/`, reported 655 ms to first
  interaction, and produced no browser error artifact.
- `npm run validate` passes every release validator and all 23 critical
  sequencing scenarios. The direction-neutral open-center enemy tell contract
  also remains green: clear center, 33,937 perimeter pixels, 10.43 rotational
  difference, and 1.05 quadrant ratio.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Searchable Route Deck Review

- A live route audit found that Deck Review presented polished card rows and
  details but offered no way to organize a growing run deck.
- Added cycling filters for All, each suit, Special, and Preened cards; Run,
  Cost, and Name sorting; and a 24-character Find field that searches card
  identity, bird, role, target, suit, and rules text.
- Added 58 px pointer targets plus keyboard and controller navigation:
  Up/Down selects cards, Left/Right changes the filter, Confirm changes the
  sort, `/` starts Find, and Escape closes or leaves text entry. Filtering
  reselects a valid card and provides a clear zero-results state.
- Extended route text state and screen-reader summaries with visible/total
  counts, active filter and sort, query, selection, controls, and the
  currently visible card set.
- The interaction, generated-art, and minimum-viewport Playwright scenarios
  pass together, as do TypeScript and documentation validation. The required
  shared production client verified both the default and PLUMES + COST states
  with no browser errors; inspected captures are
  `.artifacts/deck-review-audit/organized/shot-0.png` and
  `.artifacts/deck-review-audit/filtered/shot-0.png`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Four-Zone Combat Card Browser

- A live combat audit found that Draw and Discard could be opened separately,
  but their header zone badges were passive, the Deck badge showed the hand
  count instead of the complete deck, and Cleared cards had no dedicated
  exhaust-like pile inspector.
- Turned Deck, Draw, Discard, and Cleared into labeled, stateful browser tabs
  with 70x58 pointer targets. A player can move between all four zones without
  closing the overlay; each switch resets selection and scrolling safely and
  requests only the relevant card art.
- Up/Down and Tab select cards, Previous/Next and controller D-pad Left/Right
  switch zones, controller shoulders page, and pointer input activates every
  tab. Empty zones retain inspector focus and show `No cards in this pile`
  instead of dropping accessibility state.
- Corrected the Deck badge to report the complete combat deck and extended
  `render_game_to_text` plus screen-reader summaries with all four counts,
  the active zone, empty-state information, selection, and exact bindings.
- Five focused production scenarios pass together across generated art,
  pointer, keyboard, controller, screen reader, cleared-card persistence, and
  the minimum viewport. Full `npm run validate` also passes all release
  validators and 23 critical sequencing scenarios; only the existing
  preferred bundle-size advisories remain.
- The required shared production client drove a fresh flight into combat,
  opened Draw, and switched to empty Cleared with real pointer input. Inspected
  captures are `.artifacts/combat-zone-browser-audit/draw-verified/shot-0.png`
  and `.artifacts/combat-zone-browser-audit/cleared-verified/shot-0.png`;
  both text states match the screen and neither produced a browser-error file.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Pinned Route Card Comparison

- Audited Deck Review against the product requirement to compare cards,
  upgrades, relics, and effects quickly. Filtering and full dossiers were
  present, but comparing two cards still required memorizing the first card.
- Added a pinned two-column comparison with card art, cost, role, target,
  current and Preen/Base rules, flock stats, and a plain-language delta strip.
  Cost badges are 58 px pin targets; `C` and controller `X` pin or repin the
  selected card while normal Up/Down navigation changes the comparison target.
- Extended route text state and screen-reader summaries with the pinned and
  selected cards, comparison deltas, renderer readiness, and exact controls.
- Split the renderer into the on-demand `card-comparison` module. It keeps the
  selected dossier readable while loading, has a recoverable failure state,
  a 6 KB hard budget, exactly-one-chunk deployment validation, and no eager
  modulepreload. Production output is 3.4 KB / 1.3 KB gzip; the existing card
  detail module is restored to 8.6 KB / 2.6 KB gzip under its unchanged limit.
- Four focused Playwright scenarios pass across real pointer input, keyboard,
  controller, screen reader, generated dossier art, and minimum viewport.
  Full `npm run validate` also passes every release validator and all 23
  critical sequencing scenarios; only the existing preferred bundle-size
  advisories remain. The required shared client confirmed the lazy renderer
  loaded with matching text state and no browser errors; inspected capture:
  `.artifacts/deck-comparison-audit/lazy-verified/shot-0.png`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Base-Versus-Preened Deck Comparison

- Completed the unused same-card pin state in Route Deck Review. Pinning a
  different card still compares cards; pinning the selected card now opens an
  exact Base-versus-Preened comparison without adding another screen or input.
- Both columns derive from the authoritative runtime card contracts and show
  normal rules, Molt rules, cost, role, target, card art, and flock stats.
  Preened stats are consolidated into final totals, while the delta strip
  preserves the exact added stat or changed decision shape.
- The existing 58 px pointer pin, keyboard `C`, and controller `X` paths all
  reach the upgrade comparison. Text state exposes `mode: preen`, both forms,
  renderer readiness, and totals; the screen reader announces the Base and
  Preened roles, targets, and summary.
- The focused scenario and four-test Deck Review/minimum-viewport suite pass.
  Production bundle and deployment gates pass with the on-demand comparison
  at 3.5 KB / 1.3 KB gzip under its unchanged 6 KB hard limit. Full
  `npm run validate` also passes every release validator and all 23 critical
  sequencing scenarios; only the existing preferred bundle-size advisories
  remain.
- The required shared client verified the final full-resolution dossier,
  matching serialized state, consolidated Cohesion `+3`, and no browser
  errors. Inspected capture:
  `.artifacts/deck-comparison-audit/preen-totals-verified/shot-0.png`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 On-Demand Route Review And Run-Kit Presentation

- Completed the approved enemy-attack tell swap with the open-center,
  direction-neutral perimeter flourish as the sole canonical runtime/source
  treatment. The subject remains visible during wind-up and no directional
  arrow or dense centerpiece remains.
- Moved the complete Route Deck Review browser, complete owned-Waymark drawer,
  and Supply drawer presentation into interaction-only modules while preserving
  pointer, keyboard, controller, screen-reader, filtering, sorting, search,
  selection, pinning, scrolling, and failure/loading behavior.
- Added explicit lazy-renderer readiness to Route text state and hard size,
  exactly-one-chunk, immutable-cache, and no-modulepreload deployment contracts
  for `route-deck-browser`, `waymark-review`, and `route-supply-drawer`.
- Production output is 676.1 KB / 179.1 KB gzip for the app entry and
  705.4 KB / 189.8 KB gzip for combined boot. All hard limits pass; the entry
  remains 1.1 KB above the 675 KB preferred advisory. On-demand chunks are
  5.5 KB / 2.2 KB gzip, 4.6 KB / 2.1 KB gzip, and 1.3 KB / 0.8 KB gzip.
- Focused Deck Review, Supply drawer, and Waymark pointer/keyboard/controller
  scenarios pass together. All release validators and all 23 critical
  sequencing scenarios pass. The required shared client confirmed the Deck
  browser loaded on demand, its serialized state matched the screen, and no
  browser-error artifact was produced. Inspected capture:
  `.artifacts/route-lazy-run-kit-shared-client/shot-0.png`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 On-Demand Route Reward Review

- Moved the post-choice Route reward ceremony into
  `src/game/route-reward-overlay.ts`. RouteScene still owns authoritative
  reward generation, projections, costs, state restoration, claiming,
  cancellation, and progression; the interaction-only module owns the
  decision panel, card/item/effect showcase, and Confirm/Cancel presentation.
- Added a guarded loading/failure surface, retry-on-reopen behavior, and
  `routeRewardRenderer` text-state telemetry. The minimum-viewport scenario
  proves the chunk stays cold before a reward opens, loads before interaction,
  preserves its 58 px Confirm target, and leaves no undersized pointer target.
- Added a 6 KB hard chunk budget, exactly-one-hashed-chunk deployment rule,
  and no-modulepreload contract. Production output is 674.4 KB / 178.5 KB
  gzip for the app entry and 703.7 KB / 189.2 KB gzip for combined boot, both
  below their preferred targets with no advisory. The new on-demand chunk is
  3.6 KB / 1.6 KB gzip.
- The focused Route decision/outcome scenario passes and the inspected
  minimum-viewport capture is
  `.artifacts/test-results/min-supported/route-reward-claim-1000x560.png`.
  All release validators and all 23 critical sequencing scenarios pass.
  The required shared client kept the reward module cold through a real fresh
  route/combat path with matching telemetry and no browser-error artifact;
  evidence is under `.artifacts/route-reward-lazy-fast-fight-client-v7/`.
- Remaining qualitative release evidence is unchanged: five genuinely
  observed fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 One-Flight Strategy Achievements

- Added four horizontal, one-flight achievements using metrics the finished-run
  record already owns: `Live Wire` (3 Surges), `Still Air` (3 clean fights),
  `Brace Brigade` (24 blocked damage), and `Every Promise` (3 district
  contracts). A qualifying loss can earn them, while separate near misses never
  combine through lifetime totals. Existing achievement ids and account saves
  remain additive and compatible.
- Replaced the Flock Record's silent eight-row cutoff with shared six-row
  pagination for achievements and contract badges. Pointer controls, Page
  Up/Page Down, and LB/RB drive the same bounded page state; text state exposes
  the visible range, item names, exact requirements, earned flags, and input
  labels. Paging controls retain 76x58 hit areas.
- The focused regression proves two cumulative near-miss losses unlock nothing,
  one qualifying loss unlocks all four challenges, older account fields survive,
  all 12 achievement rows are reachable, and a seven-badge contract collection
  reaches its second page through the same input model. The inspected minimum
  landscape capture is
  `.artifacts/test-results/strategy-achievements/minimum-landscape-page-2.png`.
- The required shared client opened Flock Record and paged to the new challenges
  with a real pointer; its matching text state and inspected capture are under
  `.artifacts/strategy-achievements-profile-client/`, with no browser-error
  artifact.
- `npm run build` and full `npm run validate` pass. All release validators and
  all 24 critical sequencing scenarios are green. App entry is 674.5 KiB /
  178.6 KiB gzip and combined boot is 704.3 KiB / 189.4 KiB gzip, inside both
  preferred budgets.
- Remaining qualitative release evidence is unchanged: five genuinely observed
  fresh-player sessions through `docs/game/playtest-runbook.md`.

## 2026-07-26 Completion Evidence Audit

- Re-ran the exact current production test workflow serially. Runtime-data,
  optimized-asset, canonical-world, and combat-FX contract validation passed,
  followed by all 219 Playwright scenarios in 30.5 minutes.
- Corrected two test-harness ownership/readiness faults found by the first
  exhaustive pass. Reward-delta assertions now read the owning BattleScene
  instead of a global text hook that RouteScene can legitimately reclaim, and
  the Route Waymark rail assertion now waits for the actual lazy-rendered
  object. Both focused regressions and the complete suite pass.
- Current deterministic audits are green: content has 32 passing checks with
  no drift or pending entries; economy has 500 passing seeded samples; balance
  validates all four district pressure scaffolds. The critical 24-scenario
  release gate and production bundle/deployment checks also passed in the
  immediately preceding validation run.
- This proves the current build's mechanical behavior, runtime data, asset
  wiring, accessibility contracts, supported layouts, saves, progression,
  security policy, and performance budgets. It does not prove subjective fun,
  fairness, clarity, or replay intent.
- No `.artifacts/playtest-sessions/` directory exists. The current dashboard
  explicitly reports `seeded pipeline harness (not human playtest evidence)`
  and `0/10` complete latest-run rating sets. The remaining release gate is
  therefore five genuinely fresh, observed human sessions with complete
  ratings and observer notes under `docs/game/playtest-runbook.md`.
- Further speculative balance or content changes are not justified before
  those sessions identify a repeated confusion point or low experience rating.

## 2026-07-26 Fresh-Player Entry Recheck

- Rechecked the exact tester URL at `http://127.0.0.1:5201/?playtest=1` with the
  required shared browser client. The production menu loaded with all 24
  essential title assets and all 12 compact assets ready, matching serialized
  menu state, and no browser-error artifact.
- Visually inspected the full-resolution capture at
  `.artifacts/playtest-entry-audit/shot-0.png`. Setup focus, leader identity,
  Ascension, flight length, Start Run, How to Play, Settings, Codex, and Flock
  Record remain visible and coherent for a fresh tester.
- External evidence is unchanged: `.artifacts/playtest-sessions/` does not
  exist and the dashboard still has zero complete human rating sets. The goal
  remains active pending the five observed sessions in the playtest runbook.

## 2026-07-26 Standardized Human Observation Evidence

- Added `docs/game/playtest-observer-template.md`, a privacy-conscious
  per-session sheet covering fresh-player identity, device/input, silent
  observations, every required mechanic, friction events, first-fight
  comprehension, post-run questions, tester ratings, and observer summary.
- Updated the playtest runbook to require matching each sheet to its JSON
  export filename and run ID. This prevents ratings, telemetry, and notes from
  being mixed between testers while keeping observer notes separate from the
  machine-generated dashboard.
- `npm run validate:docs` passes. The remaining gate still requires five real
  people; the template reduces collection ambiguity but is not session
  evidence itself.

## 2026-07-28 Persistent Codex Card Favorites

- Re-audited the game against the expanded card-collector objective. The Codex
  already persisted card discovery and set milestones, but it had no durable
  way for players to express attachment to individual cards.
- Added `favoriteCards` to the journaled account schema. Existing accounts and
  legacy backups default safely to an empty list, unknown or undiscovered
  favorite ids are discarded during sanitation, and favorite state remains
  separate from combat power, reward pools, and run availability.
- Every discovered card dossier now has a 142x46 `FAVORITE` / `FAVORITED`
  command with star-state labeling. Pointer/touch, keyboard `C`, and controller
  `X` all toggle the same state; favorited cards receive a persistent,
  non-color-only star marker in the collection grid.
- Extended `render_game_to_text` with favorite count, ids, active detail state,
  and bindings. The lazy screen-reader summary announces whether the open card
  is favorited and how to change it.
- Focused production regressions prove pointer, keyboard, controller,
  sanitation, journaled persistence, grid markers, local backup export, and
  transactional restore. The required shared client completed a fresh
  Start Run -> Route -> Menu -> Codex path and favorited First Flight with a
  real pointer. The inspected final capture is
  `.artifacts/codex-favorite-shared-client-final-v2/shot-1.png`; text state
  reports `major_00`, one favorite, and no browser-error artifact.
- The initial release pass found the shared game-core chunk 0.1 KB above its
  hard budget. Moving the write operation into the already-lazy Codex boundary
  restored the gate: game core is 29.9 KB, app entry is 674.5 KB, and combined
  boot is 704.4 KB. Full `npm run validate` passes all validators and all 24
  critical sequencing scenarios.
- Next collector-loop priority is an explicit Favorites collection view or
  filter so a larger personal set can be browsed as a group. The five genuine
  fresh-player sessions remain necessary qualitative release evidence.

## 2026-07-28 Favorites Collection View

- Added a dedicated eighth `Favorites` card tab to the Codex. The compact
  104x46 tab layout preserves the existing collection milestone rail and gives
  pointer/touch, keyboard, and controller users the same reachable filter.
- The populated view isolates favorited discovered cards, retains their
  non-color-only gold star markers, and reports favorite/discovered totals.
  The empty view replaces a blank grid with a centered, readable explanation
  of how to favorite a discovered card and where that state is saved.
- Extended `render_game_to_text` with active-view, empty-view, and visible-id
  state. Screen-reader output now announces both populated counts and the
  actionable empty-state instructions.
- Focused regressions pass for save backup/restore, remapped Codex navigation,
  run discovery, persistent pointer/keyboard/controller favorites, the new
  populated and empty views, Snags isolation, and every detail dossier frame.
- The required shared production client ended on a populated Favorites view
  with `major_00` as its sole visible id and no browser error artifact. Visual
  inspection passed at
  `.artifacts/codex-favorites-view-shared-client/shot-1.png`; the independently
  captured empty state passed at
  `.artifacts/test-results/codex-favorites-empty.png`.
- Full `npm run validate` passes all documentation, runtime-data, asset,
  canonical-world, combat-FX, deployment, overlay, and enemy-variety checks,
  plus all 24 critical sequencing scenarios. The combined boot remains within
  budget at 704.4 KB minified / 189.4 KB gzip; game core remains 29.9 KB.
- The broader card-collector objective remains active. Acquisition/ownership
  semantics and collection-driven goals should be audited next, while five
  genuinely observed fresh-player sessions remain required qualitative
  evidence.

## 2026-07-28 Permanent Card Collection Provenance

- Separated card discovery from permanent collection ownership. Seeing a card
  in an offer still reveals it in the Codex, while starter-flock cards, combat
  rewards, route choices, market purchases, and enemy Snags now create or
  update an atomic journaled collection record.
- Each record preserves its first acquisition timestamp and source plus its
  lifetime flight-claim count. Repeated claims update history instead of
  creating worthless duplicate inventory. Playable copies, Preens, and
  transformations remain specific to the current flight, so collection growth
  expands identity and history without adding account power or gating runs.
- Combat rewards now mark first copies as `NEW TO COLLECTION`, celebrate the
  claim with the objective-complete audio cue, and distinguish later flight
  claims. Market and route/Snag acquisition paths use the same source-aware
  transaction model.
- The Codex now reports collected, discovered, and total counts; every revealed
  card has a non-color-only `COLLECTED` or `SEEN` marker. Card dossiers show
  claim count, original source, acquisition date, and the explicit
  permanent-record/run-copy distinction. Reward and Codex text states and
  screen-reader summaries expose the same semantics.
- Focused Playwright coverage passes 10/10 across route rewards, combat reward
  first/repeat claims, market purchases, Snags, starter records, favorites,
  collection sanitation, local backup, and transactional restore.
- The required shared production client completed Start Run -> Route -> Codex
  -> Favorites with 10 collected starter records and no browser errors. The
  inspected populated binder capture is
  `.artifacts/collection-ownership-shared-client/shot-0.png`; it visibly shows
  First Flight with both the favorite star and `COLLECTED` label.
- Full `npm run validate` passes documentation, runtime data, 961 optimized
  runtime assets, canonical-world and combat-FX contracts, bundle and
  deployment-cache checks, enemy-variety and Minor Arcana overlay checks, and
  all 24 critical sequencing scenarios. Production is 675.7 KB app entry,
  30.0 KB game core, and 705.7 KB combined boot / 189.9 KB gzip. The unchanged
  675 KB app-entry preference remains a 0.7 KB advisory; every hard gate passes.
- Next collector-loop priority is a player-chosen collection target or wishlist
  with meaningful non-power milestones. Five genuinely observed fresh-player
  sessions remain required qualitative evidence.

## 2026-07-28 Durable Newly Acquired Card Acknowledgement

- Added durable unread state directly to each first-copy collection record.
  Every authoritative acquisition source sets `isNew` atomically with the
  permanent claim; repeat flight claims preserve the marker instead of creating
  duplicates, while legacy collections default to no notification flood.
- Added a fifth `New` Collection Lens plus explicit purple `NEW` thumbnail
  labels. Undiscovered cards remain private, the marker is not color-only, and
  its state does not affect ownership, reward odds, playable copies, or run
  power.
- A new card dossier replaces the otherwise-disabled collected/Hunt command
  with a 142x46 `NEW / MARK SEEN` action. Pointer/touch, keyboard `N`, and
  controller `LT` acknowledge that card intentionally. The dossier text and
  screen-reader summary explain the consequence before the action.
- A 132x46 `CLEAR ALL` command occupies the unused right grid gutter whenever
  unread cards exist. Pointer/touch, keyboard `N`, controller `LT`, and logical
  focus all clear notification state in bulk without removing any collection
  record. Its scene-level pointer region is authoritative so later root
  overlays cannot intercept the visible command.
- Malformed non-boolean unread flags are ignored and repaired when the Codex
  loads. Flock Record export and transactional restore preserve valid unread
  state inside the existing account record. `render_game_to_text` exposes
  count, ids, visible ids, detail state, New-lens state, controls, persistence,
  and the explicit non-ownership contract.
- Thirteen focused acquisition/Codex/backup regressions pass together,
  including first and repeated combat claims, Hunt completion, starter-flock
  claims, provenance sanitation, Favorites, Hunt List, all five Collection
  Lens states, search, sort, per-card acknowledgement, and bulk acknowledgement
  through pointer, keyboard, and controller.
- Full `npm run validate` passes documentation, 110-card runtime data, 961
  optimized runtime assets, canonical-world and directionless combat-FX
  contracts, deployment hardening, enemy variety, Minor Arcana overlays, and
  all 24 critical sequencing scenarios. The hard game-core budget remains
  green at 30.0 KiB; combined boot is 706.9 KiB / 190.3 KiB gzip. The existing
  677.0 KiB app-entry preference remains advisory.
- The required shared production client completed a fresh Start Run -> Route
  -> Title -> Codex -> New Lens path. Text state reported 10 collected and 10
  new cards before a real bulk-clear pointer action, then 10 collected and zero
  new cards afterward. Inspected captures are
  `.artifacts/codex-new-shared-client-lens/shot-0.png` and
  `.artifacts/codex-new-shared-client-clear/shot-0.png`; neither run emitted a
  browser-error artifact.
- The broader card-collector objective remains active. A useful next collection
  milestone is richer completion breakdown and personal organization beyond
  Favorites/Hunt/New, while five genuinely observed fresh-player sessions
  remain necessary qualitative release evidence.

## 2026-07-28 Permanent Collection Atlas

- Corrected the Codex collection rail so its `COLLECTION` count and progress
  stage are grounded in permanent ownership, not mere discovery. Discovery
  milestones remain intact under an explicit `cardDiscovery` text-state
  contract and still feed the next-flight discovery goal.
- Added a full Collection Atlas reachable from every card tab through the
  280x46 rail, keyboard `G`, controller `R3`, logical focus, and pointer/touch.
  It reports exact overall, seven-family, and four-rarity ownership totals plus
  first-acquisition path counts.
- Collection stages use encouraging shelf/archive language from `Ready to
  Begin` through `Collection Complete`. The view compares permanent collection
  with aggregate encounter counts without listing any undiscovered card id,
  name, rules text, or other concealed identity.
- All seven family rows are actionable binder shelves. Pointer, keyboard,
  controller, and screen-reader users can browse a row, hear its owned/total and
  encountered/total counts, and open that family directly. Back, `G`, and `R3`
  close the modal without changing the active family.
- The modal stays readable at 1280x720 and the supported 1000x560 compact
  landscape viewport. It preserves established Codex Tab order and existing
  Favorites, Hunt List, New-marker, Collection Lens, search, sorting, and
  detail behavior.
- Focused and adjacent Codex regressions pass for discovery goals, the complete
  Atlas interaction contract, Favorites, Hunt List, New markers, Collection
  Lens, search, sorting, Snags, remapped controls, and screen readers. The full
  `npm run validate` production gate passes all validators and all 24 critical
  sequencing scenarios.
- Production remains within hard budgets: combined boot is 706.9 KiB minified /
  190.3 KiB gzip, game core is 30.0 KiB, and the lazy Codex scene is 108.7 KiB /
  27.3 KiB gzip. The existing 677.0 KiB app-entry preference remains advisory.
- The required shared production client completed Start Run -> Route -> Title
  -> Codex -> Collection Atlas with 10 permanent starter cards, exact
  family/rarity/source totals, a rendered focus ring, and no browser-error
  artifact. The inspected capture and text state are in
  `.artifacts/collection-atlas-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence, and
  deeper personal organization such as tags, locks, or saved collection views
  remains a useful future collector-loop milestone.

## 2026-07-28 Persistent Personal Card Tags

- Added three private, non-power card tags: `Staple`, `Experiment`, and
  `Keepsake`. A discovered card dossier cycles them with a 180x46
  pointer/touch action, keyboard `V`, or controller `L3`; a fourth activation
  clears the tag.
- Added a sixth `Tagged` Collection Lens and a text-bearing tag badge on each
  tagged thumbnail. Tags also participate in card search, while undiscovered
  identities remain concealed and tag state never affects ownership, reward
  odds, playable copies, or run power.
- Tags live in the journaled player account, survive the existing atomic
  primary/backup save pair, export through Flock Record, and restore
  transactionally. A shared sanitizer removes unknown tag values and tags for
  undiscovered cards before Codex display, mutation, export, or restore.
- Text state and screen-reader summaries expose the tagged count, ids, current
  dossier value, visible Tagged-lens subset, inputs, persistence, privacy, and
  non-power contract. Empty Tagged sets give direct, non-color-only guidance
  without overwhelming the card browser.
- Ten adjacent Codex regressions pass together for collection provenance,
  Favorites, Hunt List, New markers, all six Collection Lens states, personal
  tags, search, sorting, and the Collection Atlas. Focused backup download and
  transactional restore coverage also passes with valid tags preserved and
  invalid data removed.
- Full `npm run validate` passes documentation, 110-card runtime data, 961
  optimized assets, canonical-world and directionless attack-tell contracts,
  deployment hardening, enemy variety, Minor Arcana overlays, bundle gates,
  and all 24 critical sequencing scenarios. Combined boot is 707.0 KiB /
  190.4 KiB gzip; the hard game-core cap remains green at 30.0 KiB. The
  existing 677.0 KiB app-entry preference remains advisory.
- The required shared client completed Start Run -> Route -> Codex -> card
  dossier -> tag assignment -> Tagged Lens. Text state reported one persistent
  `Staple` tag on `major_00`, the screenshot visibly retained both `NEW` and
  `STAPLE` labels without obscuring the card, and no browser-error artifact was
  emitted. Evidence is in `.artifacts/personal-tags-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence; a
  useful next milestone is a small set of collection achievements tied to
  varied families and acquisition paths rather than repetitive grinding.

## 2026-07-28 Breadth-Based Collection Milestones

- Added six permanent collector achievements: First Shelf, Four Winds,
  Curator's Eye, Marked Routes, Many Roads Home, and Citywide Binder. Their
  requirements reward a modest first collection, suit breadth, all three
  personal tags, completed Hunt goals, four non-starter acquisition paths, and
  all seven card families instead of repetitive high-count farming.
- Collection claims evaluate and unlock eligible milestones inside the same
  journaled account transaction as ownership. Personal-tag changes evaluate
  Curator's Eye inside their existing journaled mutation. Legacy accounts are
  reconciled when the Codex loads, without duplicate achievement ids or a
  second save system.
- The Collection Atlas now retains compact first-acquisition counts and adds
  three visible current goal rows with exact progress bars, completion labels,
  and named badge rewards. Completed count, all six goals, the next goal, and
  the explicit non-power/privacy contract are available in text state and
  screen-reader summaries without exposing undiscovered card identities.
- Flock Record now includes the six collector badges on a third achievement
  page. Earned counts and progress rails include both flight and collection
  achievements; incomplete badge descriptions show exact progress, completed
  badges show their reward, and Profile text state reports that rewards never
  affect gameplay power.
- Split the small acquisition-time progress evaluator from the 1.2 KiB lazy
  presentation module so names, descriptions, and badge copy load only with
  Codex/Profile. Combined boot is 708.1 KiB / 190.8 KiB gzip, and the hard
  game-core cap remains green at 30.0 KiB. The existing 678.1 KiB app-entry
  preference remains advisory.
- Focused and adjacent coverage passes for real starter transactions,
  idempotent legacy reconciliation, all six progress/completion contracts,
  journal backup parity, Flock Record pagination, screen readers, personal
  tags, Hunt List, the Collection Atlas, and save download/restore. A held
  pointer sequence replaced zero-frame synthetic clicks in the Atlas/tag tests,
  matching human input and removing frame-boundary flakes without weakening
  pointer coverage.
- Full `npm run validate` passes documentation, 110-card runtime data, 961
  optimized assets, canonical-world and directionless attack-tell contracts,
  deployment hardening, enemy variety, Minor Arcana overlays, bundle gates,
  and all 24 critical sequencing scenarios.
- The required shared client completed Start Run -> Route -> Codex -> Collection
  Atlas. It showed 10 permanent starter cards, two earned badges, Curator's Eye
  as the next 0/3 goal, a logical Atlas focus ring, and zero browser errors.
  The inspected screenshot and state are in
  `.artifacts/collection-milestones-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence. A
  useful next collector milestone is a profile showcase that lets players
  present a small curated set of favorite cards without adding account power.

## 2026-07-28 Three-Card Flock Record Showcase

- Added a persistent, identity-only Showcase for up to three discovered cards.
  A card dossier now exposes a 180-by-46 pointer target plus contextual G / R3
  controls. Removing a card preserves the remaining order; trying to add a
  fourth plays locked feedback and never silently replaces an existing choice.
- Added a dedicated Flock Record Showcase tab with the selected compact card
  art, names, family, and rarity. Empty and filled states explain that choices
  are managed in the Codex and never affect gameplay power. Keyboard,
  controller, text-state, and screen-reader navigation include the new tab.
- Added strict showcase sanitation for duplicate, undiscovered, malformed, and
  over-cap data. Codex startup repairs legacy state, journaled writes preserve
  deliberate order, and full Save Data download/transactional restore sanitize
  and retain the same three choices.
- Focused tests cover keyboard, held pointer, controller R3, full-cap refusal,
  screen-reader output, minimum-landscape rendering, real card art, save
  download, and transactional restore. `npm run validate` passes all production
  gates and 24 critical sequencing scenarios; combined boot is 708.4 KiB /
  190.9 KiB gzip, with game-core still at its 30.0 KiB hard limit.
- The required shared production client completed Start Run -> Route -> Codex
  -> card dossier -> Showcase. Text state reported First Flight as 1/3,
  persisted and non-power, with zero browser error artifacts. The inspected
  screenshot and state are in `.artifacts/card-showcase-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence. A useful
  next collector milestone is stronger collection-goal discovery from the
  title/route surfaces without adding manipulative urgency.

## 2026-07-28 Collection Path Goal Discovery

- Renamed the title Codex utility to Collection and added a calm Collection
  Path strip that shows permanent-card and badge progress plus the next
  optional goal. It states the useful target without a deadline, rotating
  urgency, or gameplay-power incentive and opens the Collection Atlas directly.
- Added the same compact progress affordance to stable Route screens. Pointer,
  G, and controller R3 all deep-link to the Atlas; closing it restores the
  exact active flight, including its route seed and run state.
- Added origin-aware Codex navigation, synchronous text-state contracts, and
  screen-reader summaries for both surfaces. Minimum-landscape layouts retain
  independent lanes for collection progress, route guidance, and autosave
  status without overlaps.
- Split the decorative Collection Path renderer into a 1.3 KiB on-demand
  chunk. Production build, type checking, deployment-cache validation, and
  bundle hard limits pass; combined boot is 710.9 KiB / 191.8 KiB gzip, with
  the remaining 0.9 KiB preferred-target excess reported as advisory.
- Focused Playwright coverage passes title and route pointer access, G and R3
  access, screen-reader language, and lossless return to the active flight.
  The required shared production client opened the Atlas from the new title
  strip with zero browser error artifacts; its inspected screenshot and state
  are in `.artifacts/collection-path-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence. A useful
  next milestone is saved favorite deck/loadout records that preserve player
  identity without adding collection power.

## 2026-07-28 Saved Flight Folios

- Added six durable Flight Folio slots for preserving meaningful active decks.
  Saving from Route Deck Review retains exact card order, duplicate copies,
  Base/Preened state, leader, flight seed, and run mode. Pointer, V, and
  controller Y all use the same journaled account write.
- Capacity is refusal-only: a seventh save plays locked feedback and never
  replaces, rotates, or edits an existing folio. Malformed, empty, duplicate-id,
  oversized, and invalid-card records are sanitized at route, Profile, and
  backup boundaries.
- Added a Flock Record Folios view with two readable records per page, six-slot
  pagination, explicit selection, favorites, and custom names. Pointer and
  keyboard/controller controls are represented in text state and screen-reader
  summaries; rename uses an accessible native input so physical and on-screen
  keyboards share the same commit/cancel path.
- Full Save Data download and transactional restore preserve sanitized folios,
  custom names, favorites, duplicates, and Preened state. The feature is
  identity-only and explicitly reports that it never affects gameplay power.
- Focused end-to-end coverage passes pointer, V, controller Y, capacity refusal,
  exact deck persistence, favorite toggling, custom rename, minimum-landscape
  rendering, backup download, and transactional recovery. Adjacent Deck Review,
  Profile focus, touch-target, Save Data, and Showcase scenarios pass.
- Full `npm run validate` passes documentation, 110-card runtime data, 961
  optimized assets, canonical-world and directionless attack-tell contracts,
  deployment hardening, enemy variety, Minor Arcana overlays, bundle gates,
  and all 24 critical sequencing scenarios. Game core remains inside its
  30.0 KiB hard cap; combined boot is 714.1 KiB / 192.8 KiB gzip, below the
  hard ceiling with a 4.1 KiB advisory preferred-target excess.
- The required shared production client completed Continue Run -> Deck Review
  -> Save Flight. Text state reported one of six folios saved, persisted,
  non-power, and refusal-only, with zero browser error artifacts. The inspected
  screenshot and state are in `.artifacts/flight-folios-shared-client/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence. A useful
  next milestone is deck-library duplication/version history or collection
  locking that builds on these durable identity records.

## 2026-07-28 Safe Folio Forks And BSF1 Flight Codes

- Extended Flight Folios into a reversible experimentation library. `Fork Copy`
  creates a new exact record with a stable lineage id, numbered revision, and
  parent link while retaining the trusted original unchanged. Repeated forks
  normalize their names to one readable base plus the current revision instead
  of accumulating suffixes.
- Added compact, deterministic `BSF1` deck codes with a payload checksum.
  Codes preserve leader, run mode, exact card order, duplicate copies, and
  Base/Preened state while deliberately excluding account data, custom names,
  favorites, timestamps, and private flight seeds.
- Added pointer, keyboard, and controller access for fork (`D` / `LT`), copy
  (`E` / `RT`), and import (`I` / `L3`). Import uses an accessible native input
  for physical and on-screen keyboards plus a clear in-canvas modal prompt,
  verifies version/checksum/deck shape before writing, and imports into a fresh
  identity line.
- The existing six-slot refusal contract applies to forks and imports: at
  capacity the action is locked and no original or other folio is replaced.
  Malformed lineage ids, revisions, and parent ids are sanitized at the same
  route/Profile/backup boundaries as the underlying deck records.
- Profile text state and screen-reader summaries expose revision lineage,
  capacity, action availability, code version, checksum validation, privacy
  exclusions, exact-card guarantees, and all input methods. One folio is shown
  per page so the four 132x46 management actions and touch pagination remain
  readable at the supported 1000x560 minimum landscape viewport.
- Focused coverage proves pointer/keyboard/controller forks, original
  preservation, exact decoded payload shape, checksum rejection without
  mutation, valid import, repeated revision naming, capacity refusal,
  minimum-viewport presentation, and screen-reader output. Adjacent original
  Folio behavior, remapped Profile focus, and transactional backup restore
  with revision lineage pass together.
- Full `npm run validate` passes documentation, 110-card runtime data, 961
  optimized assets, canonical-world and directionless attack-tell contracts,
  deployment hardening, enemy variety, Minor Arcana overlays, bundle hard
  gates, and all 24 critical sequencing scenarios. Game core remains at its
  30.0 KiB hard cap; combined boot is 714.5 KiB / 192.9 KiB gzip, with the
  existing 4.5 KiB preferred-target excess remaining advisory.
- The required shared production client opened Flock Record -> Folios -> Import
  from a clean account. Text state reported an active native import, complete
  BSF1 integrity/privacy contracts, zero saved records changed, and no browser
  errors. The inspected canvas capture and state are in
  `.artifacts/flight-folio-library-shared-client-final/`; the full-page visual
  proof including the native field is
  `.artifacts/test-results/flight-folio-import-prompt.png`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence. A useful
  next deck-library milestone is sample-hand/test-draw analysis or richer deck
  statistics that make experimentation feel more like play.

## 2026-07-28 Flight Lab Deck Practice

- Added a non-destructive Flight Lab to saved Flight Folios. It reports saved
  versus currently playable cards, Preened count, average Wingbeat cost, cost
  curve, card roles, families, resource hooks, singleton-rule duplicates, and
  unavailable definitions without rewriting the underlying folio.
- Sample Hand uses the same seeded shuffle and opening protection routine as
  combat. Players can repeatedly deal five-card, three-Wingbeat hands and see
  playable count, pressure access, total cost, and protection swaps. A
  sixteen-hand consistency summary reports average playable cards, two-card
  opening reliability, pressure frequency, distinct hands, and average swaps.
- Practice seeds derive only from the exact shareable deck shape, leader, and
  run mode. Custom names, account identity, timestamps, favorites, and private
  flight seeds never influence or leak into the sample. Re-selecting a sample
  index reproduces the exact hand; sampling never spends Scrap, affects power,
  starts a run, or mutates the saved order.
- Profile Folios expose pointer, keyboard, and controller access: Flight Lab
  button, `L`, or controller R3 opens it; Previous/Next, Space, controller
  shoulders/D-pad/A, and large touch commands deal samples; Back/controller B
  closes it. Screen-reader narration includes the complete deck shape, sample,
  consistency, legality issues, non-mutation guarantee, and controls.
- Focused browser coverage proves duplicate and unavailable-card reporting,
  exact curve/role/family statistics, deterministic replay, real protected
  five-card hands, loaded art, byte-for-byte local-save preservation, all three
  input methods, accessible narration, and minimum 1000x560 presentation.
  Inspected visual evidence is
  `.artifacts/test-results/flight-lab-profile.png`.
- `npm run build` and the full `npm run validate` gate pass. Validation covers
  110 cards, 961 optimized assets, canonical-world and directionless tell
  contracts, deployment hardening, bundle hard limits, and all 25 critical
  browser scenarios. Combined boot remains 714.6 KiB / 192.9 KiB gzip; the
  existing 675 KiB entry and 710 KiB combined preferred targets remain advisory
  warnings.
- The required production shared client opened Flock Record -> Folios from a
  clean account, confirmed the Lab is unavailable without a saved deck, and
  emitted no browser errors. Its inspected screenshot and text state are under
  `.artifacts/flight-lab-shared-client-final-v2/`.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence.

## 2026-07-28 Non-Destructive Flight Folio Archive

- Added a durable Active/Archive split to Flight Folios so the six active slots
  can be reused without deleting valued decks. Existing records migrate as
  active; each archived record retains its id, lineage, revision, parent,
  exact card order, Base/Preened state, custom name, favorite, source seed, and
  run mode.
- Active capacity remains six and now governs Route Deck Review saves, imports,
  and forks. The archive retains up to 24 additional identities. Sanitization
  independently caps both sides, rejects duplicate ids, and never lets excess
  active records evict preserved archive records.
- Archiving immediately frees an active slot. Restoring is enabled only when a
  slot is open; keyboard attempts at full capacity report the reason and leave
  local storage byte-for-byte unchanged. Forking an archived revision creates
  a new active revision while preserving the archived source.
- Flock Record now has explicit Active and Archive views, live counts, complete
  empty states, a large Archive/Restore action, and readable locked feedback.
  Pointer controls are joined by `V` / controller Select to switch libraries
  and `A` / controller Start to archive or restore. Favorite, rename, BSF1
  sharing, and Flight Lab inspection continue to work for archived records.
- Profile text state and screen-reader narration expose active/archive counts,
  capacities, selected state, action availability, safety guarantees, and all
  controls. Route Deck Review separately reports the active count and how many
  identities are archived.
- Focused coverage proves legacy migration, 6+24 independent caps, pointer
  archive, route reuse of the freed slot, checksum-stable sharing before and
  after archive, full-capacity restore refusal without mutation, controller
  library switching/archive, keyboard restore, and exact identity/card
  preservation. All four adjacent Folio suites pass together.
- The exact 1000x560 Archive-full state was visually inspected at
  `.artifacts/test-results/flight-folio-archive-profile.png`. The required
  production shared client opened Flock Record -> Folios from a clean account,
  confirmed complete Active/Archive empty-state text and controls, and emitted
  no browser errors; evidence is under
  `.artifacts/flight-folio-archive-shared-client/`.
- `npm run build`, strict TypeScript with unused checks, `git diff --check`, and
  full `npm run validate` pass. Validation covers 110 cards, 961 optimized
  assets, canonical-world and directionless tell contracts, deployment
  hardening, bundle hard limits, and all 26 critical browser scenarios.
  Combined boot is 715.2 KiB / 193.1 KiB gzip; the existing app-entry and
  combined preferred targets remain advisory warnings.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence.

## 2026-07-28 Immutable Role-Aware Folio Tuning

- Added a Tuning Bench to Flight Lab that turns deck analysis into one-card
  experiments without changing the saved source. Each successful tune creates
  a new active revision in the same lineage, records its parent, preserves
  every untouched card and card state exactly, and inserts the selected
  replacement at Base state.
- Replacement suggestions are restricted to cards permanently owned by the
  account. Deterministic ranking prioritizes the source card's combat role,
  family, Wingbeat cost, target, and resource hooks; it excludes Snags,
  unavailable definitions, the source card, and cards already present in the
  remaining saved deck.
- The bench supports Base, Preened, and unavailable source entries, exposes up
  to five readable recommendations with explicit reasons, and refuses to save
  at the six-active-Folio limit without changing storage. The archived source
  model and BSF1 privacy/integrity guarantees remain intact.
- Pointer, keyboard, and controller flows are complete: the Lab's Tune Copy
  command, `T`, or controller X opens the bench; arrows/Page Up/Page Down and
  controller D-pad/shoulders choose the source and candidate; Enter/controller
  A saves; Back/controller B returns to the Lab. Screen-reader narration
  includes source state, recommendation evidence, capacity, immutable revision
  behavior, and controls.
- Focused coverage proves owned-only deterministic ranking, unavailable-source
  handling, exact one-card replacement, unchanged source data, revision
  lineage, all three input methods, loaded card art, and byte-for-byte
  full-capacity refusal. The complete five-suite Folio regression group passes.
  The inspected 1000x560 visual is
  `.artifacts/test-results/flight-folio-tuning-bench.png`.
- The required production shared client opened Flock Record -> Folios from a
  clean account with valid complete text state and no browser errors. Its
  inspected screenshot and state are under
  `.artifacts/flight-folio-tuning-shared-client/`.
- Strict TypeScript with unused checks, `git diff --check`, and full
  `npm run validate` pass. Validation covers 110 cards, 961 optimized assets,
  canonical-world and directionless attack-tell contracts, deployment
  hardening, bundle hard limits, and all 27 critical browser scenarios.
  Combined boot is 715.4 KiB / 193.2 KiB gzip; the app-entry and combined
  preferred targets remain advisory warnings.
- The broader card-collector objective remains active. Five genuinely observed
  fresh-player sessions remain necessary qualitative release evidence.
