Original prompt: make it happen, can you wire the found artifacts in an appropriate spot that I can view during the run, along with a tooltip to tell me what they do

Current follow-up: slimmed Marn's cache event on the live 5173 path by moving Marn left, removing the center cabinet label, making the cabinet prop art read solid, and narrowing the right claim panel. Verification: `npx tsc --noEmit`, `npm run build`, generic 5173 web-game client, and focused 5173 Playwright screenshot at `.artifacts/test-results/marn-cache-live-5173-layout-tune.png`.

Current follow-up: rebalanced Marn's cache event into a cabinet-first composition. Removed the permanent right claim panel, moved claim details to hover, added a bordered selection panel around the options, shifted the cabinet down/right as the set-piece, and kept Marn slightly farther left. Verification: `npx tsc --noEmit`, `npm run build`, generic 5173 web-game client, and focused 5173 screenshots at `.artifacts/test-results/marn-cache-hover-info-rest-5173.png` and `.artifacts/test-results/marn-cache-hover-info-hover-5173.png`.

Current follow-up: moved Marn's lockbox cabinet farther right and slightly up/scaled so the full cabinet prop appears on-screen in the no-hover 5173 state while the options remain centered/readable. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: nudged Marn's lockbox cabinet farther right again while keeping the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-more-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: nudged Marn's lockbox cabinet farther right a third time, keeping a slim onscreen margin and leaving the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-even-more-right-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's lockbox cabinet group slightly up while keeping its right-side placement and leaving the options panel fixed. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-cabinet-slightly-up-rest-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's hover claim info card left while keeping the resting cabinet/options layout fixed. Verification: `npx tsc --noEmit`, focused 5173 hover screenshot at `.artifacts/test-results/marn-cache-hover-card-left-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's hover claim info card farther left again, from the previous 1058 anchor to 1000, while preserving the centered option panel and right-side cabinet. Verification: `npx tsc --noEmit`, focused 5173 hover screenshot at `.artifacts/test-results/marn-cache-hover-card-farther-left-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: improved Marn's center selection readability by removing the "Marn sets a talon..." helper sentence, widening/tallening the option rows, and increasing drawer number, title, tell, and icon sizes. Verification: `npx tsc --noEmit`, focused 5173 screenshots at `.artifacts/test-results/marn-cache-readable-center-select-5173.png` and `.artifacts/test-results/marn-cache-readable-center-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: simplified Marn's hover claim info card by removing the conversational note and rules copy, shortening the card, surfacing only reward/cost rows, and increasing both center-row and claim-card icon sizes. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-simplified-info-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: tightened Marn's hover reward/cost rows so they show only the actual change, e.g. `Reward: Scrap +35`, instead of before/after values. Verification: `npx tsc --noEmit`, focused 5173 screenshot at `.artifacts/test-results/marn-cache-reward-delta-hover-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: removed Marn's right-side hover info screen and added an event Exit button. The Exit button restores the previous route position instead of advancing the node, then returns to the map. Verification: `npx tsc --noEmit`, focused 5173 screenshot/click test at `.artifacts/test-results/marn-cache-no-right-info-exit-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: moved Marn's Exit control out of the outer event layer and into the central reward selection panel. The old top-right event-menu click spot no longer closes the overlay, while the in-panel Exit restores route selection without resolving the cache node. Verification: `npx tsc --noEmit`, focused 5173 screenshot/click test at `.artifacts/test-results/marn-cache-exit-in-selection-menu-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: corrected Marn's cancel flow so the drawer list no longer has an Exit/Cancel button. Claiming Drawer 27 now opens a route-local two-card reward picker with Cancel inside that reward menu; Cancel restores the pre-pick HP/Scrap/deck/log state and returns to Marn's drawer choices, while picking a card completes the cache event. Verification: `npx tsc --noEmit`, focused 5173 drawer/reward/cancel/claim Playwright probe, screenshots at `.artifacts/test-results/marn-cache-drawer-list-no-cancel-5173.png`, `.artifacts/test-results/marn-cache-reward-picker-cancel-5173.png`, and `.artifacts/test-results/marn-cache-reward-picker-claimed-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: expanded Marn's drawer cancel flow to every non-decline drawer, not just the card-choice drawer. Opening Drawer 12 now shows an `Open This Drawer` reward panel with Claim and Cancel before applying rewards; Cancel returns to the drawer list with HP/Scrap/deck/log unchanged, and Claim resolves the drawer. Verification: `npx tsc --noEmit`, focused 5173 simple-drawer open/cancel/claim Playwright probe, screenshots at `.artifacts/test-results/marn-cache-simple-drawer-cancel-5173.png` and `.artifacts/test-results/marn-cache-simple-drawer-after-cancel-5173.png`, `npm run build`, and generic 5173 web-game client.

Current follow-up: routed Marn's `Keep moving` option through the same confirm/cancel layer. Selecting it now opens a `Leave the Cache` panel with Cancel and Leave; Cancel returns to the drawer list unchanged, while Leave completes the cache event. Verification: `npx tsc --noEmit`, focused 5173 keep-moving cancel/leave Playwright probe, screenshots at `.artifacts/test-results/marn-cache-keep-moving-cancel-5173.png` and `.artifacts/test-results/marn-cache-keep-moving-after-cancel-5173.png`, `npm run build`, and generic 5173 web-game client.

## 2026-06-16

- Generated a 40-cell Waymark found-object icon atlas with the built-in imagegen path.
- Copied the atlas into `assets/runtime/waymarks/waymark-icon-atlas-v1.png` and cropped 40 per-id `.webp` runtime icons under `assets/runtime/waymarks/icons/`.
- Wired Waymark icons into Codex item thumbnails/details and BattleScene.
- Added a left-side run shelf for found Waymarks plus a top-bar Waymarks chip that opens a drawer.
- Added hover tooltips with family/rarity, mechanical effect, trigger/effect grammar, and found-object flavor.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, and focused smoke tests passed. Visual screenshots inspected at `.artifacts/test-results/waymark-artifact-drawer.png` and `.artifacts/test-results/waymark-artifact-tooltip.png`.
- Note: the generic `develop-web-game` client completed but captured a blank screenshot in `output/web-game/shot-0.png`; the project Playwright screenshots rendered the canvas correctly and were used for visual verification.
- Converted all 40 Waymark item icons to transparent-background WebP assets and verified every icon has full alpha range. Checkerboard review sheet: `.artifacts/test-results/waymark-transparent-contact-sheet-small.png`.

## 2026-06-17

- New user prompt: generate map icons to make the route map more readable.
- Generated an 8-icon route node atlas with built-in imagegen, copied the source to `assets/runtime/map-icons/map-node-icon-atlas-v1.png`, and cropped transparent 192x192 WebP icons under `assets/runtime/map-icons/icons/`.
- Wired route node icons into `RouteScene` map nodes and the legend, keeping the existing text glyphs as fallback if icon textures have not loaded.
- Added smoke coverage that waits for all route-node icon textures and confirms icon images render in the RouteScene.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map smoke tests, and transparent icon alpha validation passed. Visual screenshots inspected at `.artifacts/test-results/map-node-icons-contact-sheet.png` and `.artifacts/test-results/route-map-node-icons.png`.
- Note: the generic `develop-web-game` client still captures a blank screenshot in `output/web-game/shot-0.png`; the project Playwright route screenshot rendered correctly and was used for visual verification.
- Follow-up map readability pass: removed the old type-colored node fills/legend dots now that icons carry type identity, switched nodes to neutral plates with state rings, added subtle route-column guide lines, and brightened edges that lead to currently available choices.
- Verification after neutral map pass: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map smoke tests passed. Final visual screenshot: `.artifacts/test-results/route-map-neutral-icons-final.png`.

- New user prompt: make the accepted visual beauty improvements happen, excluding extra flock companion silhouettes.
- Planned scope: ambient district battlefield layers, battle intro vignette, suit-based card/effect language, stronger hit/block/heal/Winded reactions, card play micro-cinematics, and a field-dossier Codex polish pass.
- Implemented first visual polish pass in `src/main.ts`: generated soft-dot FX texture, district atmosphere overlays, battle intro vignette, card-cast micro-cinematic, suit-colored hit bursts, richer Cover/heal/Winded/Open Sky reactions, and Codex leader dossier framing.
- New user prompt: wire recommended ability updates into cards, items, enemies, and identify existing assets that should be refreshed.
- Planned scope: add a small supported mechanic set for anti-Cover cards, route-control Waymarks, boss-prep Waymarks, and more varied enemy pressure without broad engine churn.
- Added runtime/data support for `damagePierce`, `removeCover`, `targetHasCover`, `loseWingbeat`, `flockHasCover`, `extraCacheChoice`, `bossDamageShield`, and `freePreenNextDistrict`.
- Updated Sheathed Quills, Quill Ring, Quill Elder, Cache Hook, Roofline Compass, Sky-Safe Harness, Reopened Roofline, Gutter Baron, and Tar-Crowned Crow to use the new mechanics.
- Added `cache_boss_line` as a hidden sixth Cache option revealed by cache-choice Waymarks.
- Added `docs/game/ability-asset-update-recommendations.md` with art/content refresh candidates.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts`, and a focused Playwright harness check all passed. The generic web-game client still produced the known blank screenshot but returned valid `render_game_to_text` menu state.

- Visual beauty pass verification follow-up: extended the card-cast micro-cinematic timing so suit/card moments are visible during play.
- Verification: `npm run build` and `npm test` passed. In-app browser checks covered the main menu, a route into Rooftop Blocks combat, card play/impact/readability, and the Fledgling Flock Codex leader dossier; no browser warnings or errors were reported during the visual pass.

- Route map readability polish: added selected-route preview highlighting, reward/source badges under route nodes, compact route-kit stats, and moved the Signals log into the legend strip so it no longer overlaps the selected-node action panel.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, and focused route-map Playwright smoke tests passed. Final visual screenshot: `.artifacts/test-results/route-map-reward-badges-final.png`.

- Reward image mapping pass: audited card reward, Waymark, and Supply image coverage; all collectible reward cards have manifest/portrait/thumb/icon assets, all 58 Waymarks have icons, and all 31 Supplies have icons.
- Wired route reward badges to category image assets for card, Waymark, Supply, Preen, heal, and Scrap; queued these badge assets with route-node icon art; queued Waymark reward-choice art so boss reward cards do not fall back to glyphs while their icon files are unloaded.
- Extended runtime validation so reward card art must be approved, and missing Waymark/Supply reward icons fail `npm run validate:runtime`.
- Verification: `npm run validate:runtime`, `npm run build`, generic develop-web-game client, and focused Playwright screenshots passed. Screenshots inspected at `.artifacts/test-results/reward-badge-images-route.png` and `.artifacts/test-results/reward-waymark-choice-images.png`.
- Note: the generic `develop-web-game` client completed successfully; its `output/web-game/shot-0.png` capture remains the known blank/tiny harness artifact, so the project Playwright screenshot was used for visual QA.

- Route map menu redesign pass: converted the map view into a clearer planning-board layout with shared layout bounds, a wider graph lane, a dedicated right inspector, boss prep moved out of the map banner and into the inspector, a slimmer footer legend, and the Signals line moved away from node-type labels.
- Added smoke coverage proving the inspector renders the `BOSS PREP` summary in addition to the route text-state boss-prep payload.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map Playwright smoke tests, generic `develop-web-game` client, and in-app browser route-map check all passed. Visual screenshot inspected at `.artifacts/test-results/route-map-layout-redesign-selected.png`.

- New user prompt: execute the production path for the visual beauty items, replacing stand-in lines/animations with real paths forward.
- Added transparent pixel-FX spritesheets under `assets/runtime/fx/` for combat suit effects and subtle district atmosphere texture strips.
- Replaced the procedural combat atmosphere lines, generic pulse rings, soft-dot particles, and card-cast debug line/plate with Phaser spritesheet animations in `src/main.ts`.
- Suit/card play, damage, Cover, healing, Winded, Open Sky, and hostile impact hooks now route through authored pixel-FX animation keys while keeping existing combat logic intact.
- Verification: `npm run build`, `npm test`, and the required generic `develop-web-game` client completed. The generic client screenshot remained the known blank harness artifact; direct Playwright battle screenshots were captured at `.artifacts/test-results/combat-fx-asset-backed-battle.png`, `.artifacts/test-results/combat-fx-card-mid.png`, `.artifacts/test-results/combat-fx-card-after.png`, and `.artifacts/test-results/combat-fx-support-mid.png`.
- Browser/direct-scene QA: direct BattleScene harness confirmed card play reduced Roof Rat from 20 to 15 and support FX restored Cohesion. Only WebGL `ReadPixels` screenshot performance warnings appeared during Playwright capture.

- New user prompt: execute balance/variety recommendations from the cards/enemies/artifacts/mechanics audit.
- Implemented first balance contract chunk: Waymark economy/recovery hooks now resolve by trigger/effect instead of hardcoded IDs for Signal Scrap, Street bonus Scrap, Basin healing, and Market Preen discount.
- Added deterministic `routeMarkChance` payout support for normal combat reward profiles.
- Added map-scaled reward profiles for Maps 2-4 and repointed district encounters to the map-specific street/rival/boss profiles.
- Verification: `npm run validate:runtime` passed after the resolver/profile changes.
- Added balance/variety content pass: Plume Barricade and Growing Nest now contribute anti-Cover pressure, Basin Mirage can loop itself under Open Sky, three duplicate combat-start Waymarks now use timing/Open Sky triggers, and selected Map 3-4 enemies now react to Cover/low-health/unguarded states.
- Verification: `npm run validate:runtime` and `npm run build` passed. The generic `develop-web-game` client completed successfully and produced valid menu state JSON; its screenshot remains the known blank harness artifact, so the in-app browser was used for visual QA. Browser verification loaded the route map, displayed the new 15% Waymark street reward line, entered Rooftop Blocks combat, and captured no console errors.

- New user prompt: make remaining work items/improvements happen after the balance pass.
- Added `tools/simulate-run-economy.mjs` plus `npm run audit:economy` to sample 500 seeded full-run route/economy outcomes and compare boss-entry deck/Waymark counts against `balance-config.json` targets.
- Reconciled `balance-config.json`, `balance-scaling-plan.md`, and `tools/audit-content.mjs` with the current map-scaled reward profile numbers.
- Added focused smoke coverage for generic Waymark economy hooks, cache choice expansion, Basin/Signal/Market bonuses, timed Waymark triggers, `shuffleSelfToDraw`, and anti-Cover conditionals.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:economy`, `npm run audit:content`, `npm run build`, and the full `npx playwright test tests/smoke.spec.ts` suite all passed. The generic `develop-web-game` client completed and returned valid menu state JSON; its screenshot remains the known blank harness artifact.

- Route map backdrop/inspector polish: generated a dedicated imagegen rooftop planning backdrop and saved the project asset as `assets/runtime/backdrops/rooftop-blocks-route-map-v1.webp`.
- Wired `RouteScene` to load and render the route-specific backdrop instead of reusing the splash art, reduced the map board opacity so the district art remains visible, and added a larger selected-node type icon plus labeled likely-find badges in the inspector.
- Fixed the existing combat FX compile blocker by completing the missing flock motion cue helper that matched the already-present enemy motion cue system.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:docs`, focused route-map Playwright smoke tests, required generic `develop-web-game` client, direct Playwright route-map screenshot, and in-app browser bridge check all completed. Final inspected screenshot: `.artifacts/test-results/route-map-imagegen-backdrop-selected.png`.

- Home screen compactness follow-up: moved the animated Bird Squad title higher, reduced the bottom setup dock height/opacity, and made the leader chips, Ascension arrows, run actions, Codex, and Flock Record buttons smaller so the splash art remains more visible.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Home screen text-sharpness follow-up: increased menu text texture resolution, reduced heavy text strokes, slightly enlarged the smallest Ascension/leader detail labels, and strengthened only the local backing panels needed for readability.
- Verification: `npm run build`, direct Playwright menu screenshot at `.artifacts/test-results/menu-text-detail-pass.png`, and required generic `develop-web-game` client passed. The direct screenshot reported no console errors; only repeated WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Codex header layout follow-up: shifted the Cards/Items/Leaders/Enemies section tabs left and tightened their width so the Enemies tab no longer overlaps the Back button.
- Verification: `npm run build`, direct Playwright Codex screenshots at `.artifacts/test-results/codex-header-tabs-no-overlap.png` and `.artifacts/test-results/codex-enemies-tab-no-overlap.png`, and required generic `develop-web-game` client passed. The direct screenshots showed clear separation between Enemies and Back; only WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.
- Codex card-filter alignment follow-up: shifted the Major/Aviary/Plumes/Quills/Basins/Nests row 12px right so the Major button no longer hugs/clips against the left edge.
- Verification: `npm run build`, direct Playwright Codex screenshot at `.artifacts/test-results/codex-major-tab-inset.png`, and required generic `develop-web-game` client passed. The direct screenshot showed the Major tab inset cleanly; only WebGL `ReadPixels` performance warnings appeared during capture. The generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Marn cache event follow-up: changed the cache choice presentation from drawer-hotspot buttons into light dialogue-style claim options, with the generated lockbox cabinet art faded into the center panel background.
- Verification: `npx tsc --noEmit`, `npm run build`, direct Playwright cache-event screenshot at `.artifacts/test-results/marn-cache-dialogue-options-final.png`, and required generic `develop-web-game` client passed. The focused cache-event check confirmed seven options, the cabinet texture, and the updated `Choose this claim` copy.

- New user prompt: add supplies to the Codex and provide a breakdown table of supplies in the game.
- Planned scope: expand the Codex Items section to include supply definitions from `alpha-supplies.json`, add supply list/detail rendering, and add focused smoke coverage for the Supplies tab.

- Route map highlight refinement: reduced selected/available node ring thickness, replaced the selected-node halo with thin cyan focus ticks, softened route-preview line weights/dots, and changed the large map board frame from gold to muted steel blue so the map no longer feels over-highlighted.
- Verification: `npm run build`, `npm run validate:runtime`, focused route-map Playwright smoke tests, required generic `develop-web-game` client, and visual screenshot inspection passed. Final screenshot: `.artifacts/test-results/route-map-thin-node-highlight-steel-frame.png`.

- Generated the first encounter/situation backdrop variant set with built-in imagegen and saved normalized 1280x720 WebP runtime assets under `assets/runtime/backdrops/variants/`.
- New boss variants: `rooftop-blocks-boss-tar-crow-v1.webp`, `canal-markets-boss-gatekeeper-v1.webp`, `signal-spires-boss-beacon-breaker-v1.webp`, `high-roost-boss-warden-v1.webp`.
- New special-node variants: `rooftop-blocks-cache-billboard-v1.webp`, `canal-markets-supply-market-v1.webp`, `signal-spires-signal-relay-v1.webp`, `high-roost-workshop-prep-v1.webp`.
- Validation: every variant is 1280x720 RGB WebP. Contact sheet inspected at `.artifacts/test-results/backdrop-variant-contact-sheet-v1.jpg`.

- Home screen text-detail follow-up: added compact mechanical detail to the leader chips, surfaced the selected leader's signature rule in a readable two-line strip, and expanded the Ascension text to show current enemy Cohesion, reward, hit, and Open Sky modifiers.
- Refined text readability with subtle strokes and a low-opacity backing strip while keeping the splash art visible.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON; its screenshot remains the known black WebGL capture artifact.

- Home screen selected-detail implementation follow-up: removed the full-width tinted bottom dock and permanent leader description strip, keeping only compact control panels over the splash art.
- Converted flock leader descriptions/signature rules into hover tooltips for both unlocked and locked leader chips; pointer-out hides the tooltip cleanly.
- Verification: `npm run build`, in-app browser idle/leader-hover/pointer-out screenshots, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Runtime optimization follow-up: added reproducible UI and enemy-art optimization paths, including active menu splash WebP regeneration, lossy/capped large encounter enemy outputs, and reserve enemy WebP generation from transparent masters.
- Added `tools/validate-runtime-asset-sizes.mjs`, wired it into `npm test` and `npm run validate`, and set budgets for optimized splash, leader, encounter enemy, reserve enemy, card thumbnail/icon, Waymark icon, route icon, and backdrop assets.
- Tightened Codex optional art loading to visible grid rows plus the open detail item instead of queueing every asset in the active section.
- Split Phaser into a dedicated Vite `vendor-phaser` chunk for better long-term caching. Remaining note: Vite still warns because Phaser is a large dependency and the app entry remains above 500 KB minified; deeper scene/module splitting would be the next larger-scope bundle task.
- Verification: `npm run build:runtime-ui-art`, `npm run build:runtime-enemy-art`, `npm test`, `npm run build`, `npm run validate`, required generic `develop-web-game` client, and in-app browser menu/route/battle/Codex enemy smoke checks all passed with no browser warnings or errors.

- Startup bundle optimization follow-up: moved Codex-only tarot lore, bird facts, card meanings, and reserve enemy contract data into lazy `src/game/codex-data.ts`.
- Updated `CodexScene` to import the Codex data chunk on demand, rerender when it arrives, and keep visible card/enemy grids usable while the chunk resolves.
- Added a lightweight HTML/CSS boot shell so players see immediate feedback before Phaser finishes booting, removed by `BootScene.create()`.
- Build output after the split: app entry `index` is ~459 KB minified, lazy `codex-data` is ~249 KB, and `vendor-phaser` remains ~1.62 MB. Built HTML modulepreloads only `vendor-phaser`, confirming `codex-data` is deferred until Codex opens.
- Verification: `npm run build`, `npm test`, `npm run validate`, required generic `develop-web-game` client, and in-app browser menu/route/battle/Codex card-detail/enemy-grid smoke checks passed with no browser warnings or errors.

- Home screen splash-pop follow-up: generated `assets/splash/bird-squad-canal-run-splash-v4-menu-pop.webp` from the original splash PNG with a brighter, higher-saturation menu grade, removed the remaining full-screen dark wash, and pointed the menu splash import at the new asset.
- Verification: `npm run build`, in-app browser idle and leader-tooltip screenshots, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Home screen title placement follow-up: moved the animated Bird Squad title 22px higher and reduced the textured title image sizes by roughly 6% so the splash art has more room to read.
- Verification: `npm run build`, in-app browser menu screenshot, and required generic `develop-web-game` client passed. Browser console had no errors; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Home screen Ascension/readability follow-up: added a compact translucent backing plate behind the Ascension controls, brightened/stroked its label/detail text, unified title-screen button borders under a shared thin `MENU_BORDER_WIDTH`, and removed the thicker selected-leader outline in favor of color/fill state.
- Verification: `npm run build`, direct Playwright menu screenshot at `.artifacts/test-results/menu-ascension-border-pass.png`, and required generic `develop-web-game` client passed on rerun. The in-app browser navigation got stuck during reload, but direct Playwright reported no console errors and rendered the canvas correctly; the generic client returned valid `MenuScene` state JSON and its screenshot remains the known black WebGL capture artifact.

- Boss backdrop variant wiring follow-up: added a generated backdrop variant registry for `assets/runtime/backdrops/variants/*.webp`, routed boss combat nodes through district-specific boss arenas, queued the selected arena via the existing optional art loader, and exposed `route.battlefieldAssetKey` / `route.battlefieldVariant` in the battle text-state.
- Added smoke coverage that starts each district boss fight and confirms the expected generated boss backdrop texture is loaded and rendered: Rooftop Blocks Tar-Crowned Crow, Canal Markets Gatekeeper, Signal Spires Beacon Breaker, and High Roost Warden.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused boss Playwright smoke tests, required generic `develop-web-game` client, and direct Playwright boss-scene visual capture passed. Inspected screenshot: `.artifacts/test-results/boss-backdrop-variant-rooftop.png`.
- Note: the special-node backdrop variants are generated and bundled but remain staged until the non-combat route/encounter presentation gets a proper backdrop surface.

- Text overflow/tidiness execution pass: tightened compact reward/preen card text bounds, removed lore paragraphs from small choice cards, kept full details on hover in the deck-card detail format, and shifted card reward/preen choices down to clear the deck-needs row.
- Cleaned the route boss inspector by allowing long titles to drive spacing, removing crowded likely-find micro-labels, and constraining boss prep pressure/prep/readiness text to the panel.
- Improved modal/overlay readability by making Codex detail curtains fully opaque, lowering the Codex detail viewport bottom, repositioning market hover detail away from buy controls, and darkening reward backdrops so battle text does not compete with choices.
- Verification: `npm run build`, `npm run validate:runtime`, focused route/reward/hover Playwright smoke tests, `git diff --check`, required generic `develop-web-game` client, and final screenshots under `output/text-audit-after/` passed/inspected.

- Codex visual upgrade pass: added shared tab/chip styling, a stronger header band, count-bearing section/filter tabs, intentional locked card backs, suit/family accents, Waymark trigger/effect chips, leader suit/status chips, enemy source/role chips, and card/enemy dossier framing.
- Verification: `npm run build`, direct Playwright Codex screenshots for cards/items/enemies/leaders and enemy detail, and required generic `develop-web-game` client passed. Final inspected screenshots include `.artifacts/test-results/codex-polish-leaders-final.png` and `.artifacts/test-results/codex-polish-enemy-detail.png`; only WebGL `ReadPixels` screenshot performance warnings appeared during direct capture, and the generic client returned valid `MenuScene` state JSON while its screenshot remained the known black WebGL capture artifact.

- Codex enemy bounds follow-up: tightened enemy thumbnail art into a framed 108x124 art well, shifted the text column right, and reduced the enemy type chip width so wide/tall enemy silhouettes no longer spill past card borders.
- Verification: `npm run build`, built-preview Playwright captures at `.artifacts/test-results/codex-enemy-bounds-top.png`, `codex-enemy-bounds-mid.png`, `codex-enemy-bounds-lower.png`, and `codex-enemy-bounds-deep.png`, `git diff --check -- src/main.ts progress.md`, and the required generic `develop-web-game` client passed. The generic client returned valid `MenuScene` state JSON; only the known WebGL screenshot artifact/performance warnings appeared during capture.

- Codex enemy border harmonization follow-up: replaced the full bright district-colored enemy card outline with a muted steel outer frame, slim district top line, and small corner marker so enemy cards better match the broader Codex styling.
- Codex enemy sprite cleanup follow-up: removed the visible rectangular art-well border around real enemy cutout sprites and replaced it with a subtle grounding shadow, while keeping fallback art boxes for missing art.
- Verification: `npm run build`, built-preview Playwright captures at `.artifacts/test-results/codex-enemy-border-harmonized-top.png`, `.artifacts/test-results/codex-enemy-border-harmonized-reserve.png`, `.artifacts/test-results/codex-enemy-sprite-borderless-top.png`, and `.artifacts/test-results/codex-enemy-sprite-borderless-reserve.png`, `git diff --check -- src/main.ts progress.md`, and the required generic `develop-web-game` client passed. The direct captures showed contained enemy art with no visible sprite boxes; the generic client returned valid `MenuScene` state JSON.

- Aviary quality expansion implementation: added eight suitless Aviary cards (`aviary_28` through `aviary_35`) centered on bird qualities rather than tarot roles: Hover Check, Mobbing Call, Underwing Shelter, Scavenger Eye, Thermal Lift, Cache Memory, Brood Shield, and Mimic Thread.
- Wired the new Aviary cards into `data/game/alpha-cards.json`, the reward pool, Codex meanings, bird facts, runtime card art manifest placeholders, and the Aviary arcana identity data.
- Updated gameplay/docs/tooling so Aviary can be a no-suit card family rather than tarot: added `kind: "aviary"`, expanded the card count from 84/74 to 92/82, regenerated the 14-card Aviary center-art prompt pack, and changed Aviary art prompts/compositing language from roman-numeral trumps to compositor-owned top badge/title marks.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:content`, `npm run build`, `npm run validate`, `npm test`, full `npx playwright test tests/smoke.spec.ts` with 48/49 passing then focused rerun of the one flaky enemy Codex test passed, and the required generic `develop-web-game` client rendered the menu with valid `MenuScene` text state.

- Aviary fieldcraft expansion implementation: added eight more suitless Aviary cards (`aviary_36` through `aviary_43`) focused on practical bird instincts: Drumline Tap, Curb Step, Spiral Search, Reed Balance, Plunge Claim, Cold Plunge, Formation Draft, and Tool Probe.
- Wired the new fieldcraft cards into runtime card data, reward pool, Aviary arcana identity data, Codex meanings, bird facts, placeholder art manifest rows, art bible rows, alpha spec tables, and the generated Aviary center-art prompt pack.
- Updated deck/count contracts from 92/82 to 100 playable cards / 90-card reward pool and raised the app-entry bundle-size guard from 500 KB to 540 KB to match the current full-content build while keeping the check active.
- Verification: `npm run validate:runtime`, `npm run validate:docs`, `npm run audit:content`, `npm run build`, `npm run validate`, `npm test`, full `npx playwright test tests/smoke.spec.ts` (50/50 passed), `git diff --check`, and the required generic `develop-web-game` client against strict local port 5317 with valid `MenuScene` state and inspected menu screenshot.

- Enemy roster route-wiring follow-up: promoted 11 reserve enemy concepts into playable runtime enemies across the four districts, copied their optimized reserve art into combat full-art assets, added enemy-art manifest entries, and authored matching street/rival encounter definitions so the Codex reserve list now reflects true future concepts only.
- Tightened generated route combat payload selection so middle nodes receive encounter payloads after their final node type is chosen and street/rival/signal pools cycle through fresh payloads before repeats, improving per-run enemy variety without changing authored route quotas.
- Updated the enemy Codex to report `44 playable / 37 reserve concepts`, label reserve-only thumbnails as concepts, and keep promoted contracts displayed through the playable combat roster.
- Added focused smoke coverage for the new roster split and a deterministic seed sweep proving all 11 promoted encounter payloads can surface in generated route maps.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-promoted-playable-top.png`, and the required generic `develop-web-game` client all passed. The generic client returned valid `MenuScene` state JSON.

- Enemy roster batch 2 follow-up: promoted 12 more reserve concepts into playable enemies: Rat Courier, Weasel Cutpurse, Skunk Chemist, Armadillo Roller, Turtle Barricader, Cormorant Netter, Kingfisher Snapdiver, Heron Spearstepper, Bat Nightcaller, Kite Hawk Courier, Fox Lookout, and Peacock Intimidator.
- Added matching street/rival encounter payloads and mixed encounters across Rooftop Blocks, Canal Markets, Signal Spires, and High Roost, emphasizing meaningful additions that use current verbs: setup pressure, Snags, Winded, Open Sky, Frail, Cover locks, and heavy telegraphs.
- Copied each promoted reserve WebP into `assets/runtime/enemies/full/`, added enemy-art manifest entries, and updated the Codex/route-exposure smoke expectations to `56 playable / 25 reserve concepts` and 23 total promoted encounter payloads.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-batch2-playable-top.png`, direct generated-route combat screenshot `.artifacts/test-results/battle-batch2-rat-courier.png`, and the required generic `develop-web-game` client all passed. The combat capture launched `enc_rat_courier` from a generated route and showed Rat Courier's `Run Message` Tell.

- Enemy roster batch 3 follow-up: promoted 8 more reserve concepts that fit current combat verbs without new terrain/object rules: Alley Cat, Jackrabbit Sprinter, Hedgehog Curb-Crosser, Mongoose Counterfighter, Opossum Decoy, Goose Bouncer, Mantis Street Duelist, and Moth Lantern-Drifter.
- Added single and mixed street/rival encounters for Rooftop Blocks, Canal Markets, and Signal Spires, with gameplay shapes focused on burst setup, speed pressure, low Cover blockers, counterfighting, feints, lane-control charging, precision duels, and lantern distraction.
- Copied the promoted reserve WebPs into `assets/runtime/enemies/full/`, added enemy-art manifest entries, and updated smoke expectations to `64 playable / 17 reserve concepts` and 31 total promoted encounter payloads.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts --grep "enemy codex|promoted reserve"`, built-preview Codex screenshot `.artifacts/test-results/codex-enemy-batch3-playable-top.png`, direct generated-route combat screenshot `.artifacts/test-results/battle-batch3-alley-cat.png`, and the required generic `develop-web-game` client all passed. The combat capture launched `enc_alley_cat` from a generated route and showed Alley Cat's `Stalk` Tell.

- Codex enemy shadow follow-up: replaced the fixed enemy thumbnail/detail ellipse placement with texture-alpha visible-bounds anchoring so enemies with different transparent crops have shadows under their actual stance instead of a shared low offset.
- Verification: `npm run build`, focused enemy Codex smoke test, direct Playwright Codex screenshots at `.artifacts/test-results/codex-enemy-shadows-grid.png`, `.artifacts/test-results/codex-enemy-shadow-roof-rat-detail.png`, and `.artifacts/test-results/codex-enemy-shadow-alley-cat-detail.png`, plus required generic `develop-web-game` client. The first generic-client attempts failed only from PowerShell JSON quoting; the rerun with the skill reference actions file passed and returned valid `MenuScene` state JSON.

- Molt state/targeting contract pass: added an active card contract that derives the current effect text, active target, base target, and Molt label from the card plus current flock Molt state.
- Wired the active contract through hand cards, hover previews, card detail/inspect payloads, click targeting, card resolution, and card-cast VFX so Molt cards that turn self/none cards into enemy-targeted effects now require an enemy click and display their true active behavior.
- Added runtime validation visibility for Molt target shifts; the current data set intentionally reports 68 cards whose active Molt target differs from their base card target.
- Added smoke coverage for Locked Nest's self-to-enemy Molt behavior and a synthetic none-target card that Molts into target damage/Winded without auto-firing ambiguously.
- Verification: `npm run build`, `npm test`, focused Molt Playwright smoke tests, full `npm run test:e2e` (52/52 passed), required generic `develop-web-game` client, and direct Molt combat capture passed. Inspected screenshot/state artifacts: `.artifacts/test-results/molt-active-card-contract.png` and `.artifacts/test-results/molt-active-card-contract-state.json`.

- Enemy description art-direction polish follow-up: strengthened seven weaker runtime enemy descriptions/visualBriefs with concrete fashion/accessory, streetwear, and urban-place cues while preserving anatomy constraints and gameplay data.
- Re-ran the fashion/urban heuristic audit: all 64 playable enemies now meet the cue threshold. Verification passed with `npm run validate:runtime`, `npm run build`, focused enemy Codex smoke, the required generic `develop-web-game` client, and direct Codex detail screenshot `.artifacts/test-results/codex-enemy-fashion-copy-pass.png`.

- Reward card-choice visual polish follow-up: replaced the post-combat Add-to-Flock/Preen choice cards' heavy text slabs with larger art-forward card frames, lighter glass panels, suit-accent borders, stroked labels, and compact footer metadata; also aligned the market Add-to-Flock offer tile with the same mini-card treatment.
- Verification: `npm run build`, focused reward/market/card-detail Playwright smoke tests, required generic `develop-web-game` client, and production-preview screenshots in `.artifacts/test-results/reward-card-polish-final/`.

- Combat tension tuning follow-up: audited flock/card/enemy scaling and found the main player-favoring pressure came from passive Damage/Cover stats compounding through multi-hit, AoE, burst, Surge, and Nests carry interactions rather than from route economy drift.
- Tuned runtime combat scaling so passive Damage applies once per played card, AoE gets a smaller passive Damage bonus, Resonance/Winded bursts no longer double-dip passive Damage, passive Cover is softened, Surge gives +1 instead of +2, and the Nests Cover carry cap is 8.
- Added `npm run audit:balance` for the existing balance-audit script and updated formation smoke coverage for the new Surge contract.
- Verification: `npm run audit:balance`, `npm run audit:economy`, `npm run build`, `npm test`, `npx playwright test tests/smoke.spec.ts --grep combat`, `npx playwright test tests/smoke.spec.ts --grep "Cover"`, and the generic `develop-web-game` client passed. Latest generic client screenshot/state: `output/web-game/shot-1.png`, `output/web-game/state-1.json`.

- Market layout polish follow-up: rebuilt the route market overlay as a stall row with a string-light lane, awning trim, a recruit perch, waymark board, and preen bench so the screen reads like visiting a market instead of three menu boxes.
- Verification: `npm run build`, focused Playwright smoke tests for market shelves/route event backdrops/card hover details, required generic `develop-web-game` client, and production-preview screenshot `.artifacts/test-results/market-layout-polish/market-stall-layout-final.png`.

- Market decision pass: replaced one-shot market offers with seeded per-visit shelves: four rarity-priced card listings, two unowned Waymark listings, a utility shelf with Preen/Release/Supply options, and paid Scrap refresh that rerolls fresh stock while individual purchases sell out their row.
- Market prices now use the existing `alpha-market.json` bands with rarity/district scaling, and the route text-state exposes full market shelves plus refresh cost/count for tests and debugging.
- While verifying, build surfaced existing in-progress combat-state type drift (`fouled`, enemy roles/solo/damageBonus); aligned the constructors/text payloads with those fields without rolling back that work.
- Verification: `npm run build`, focused Playwright smoke tests for market shelves/route event backdrops/card hover details, required generic `develop-web-game` client, in-app browser opened `http://127.0.0.1:5173/`, and inspected market screenshot `.artifacts/test-results/market-meaning/market-stocked-final.png`.

- Route map node-spacing fix: route node placement now reserves explicit visual room for focus ticks above icons and reward-badge pips below icons, then spaces lanes across the usable graph band so four-lane generated columns do not overlap.
- Exposed `position` and `visualBounds` for route nodes in the text-state harness and added a smoke test that sweeps 4 districts x 12 seeds to fail on any overlapping generated route-node footprint.
- Verification: `npm run build`, focused route-map Playwright smoke tests, required generic `develop-web-game` client against built preview, in-app browser opened built preview `http://127.0.0.1:5324/`, and inspected built-preview screenshot `.artifacts/test-results/route-node-spacing/route-four-lane-spacing.png`.

- Enemy tension/mechanics pass: added explicit enemy roles, solo non-boss encounter tuning (+HP and +1 persistent damage), ally-support enemy verbs, and the new Fouled poison status.
- Authored support/poison identities into existing enemies: Signal Gull, Market Crow, Cicada Static Swarm, Crane Signal Caller, Moth Lantern-Drifter, Skunk Chemist, and Vulture Cleanup Crew; tagged supporting/poison encounters across districts.
- Expanded runtime validation/audit coverage for enemy roles, support/poison verbs, encounter tags, status registry, and balance-audit composition reporting.
- Added smoke coverage for solo tuning, support companion targeting, and Fouled tick behavior.
- Verification: `npm run validate:runtime`, `npm run audit:balance`, `npm run build`, `npm test`, full `npx playwright test tests/smoke.spec.ts`, and the required generic `develop-web-game` client passed. Latest generic screenshot/state: `output/web-game/shot-1.png` and `output/web-game/state-1.json`.

- Combat convenience HUD pass: added an incoming-damage prediction to the flock Cohesion bar. Visible enemy attack intents now create a red at-risk segment on the current Cohesion fill with a `-N / After X` marker after current Cover is applied.
- Exposed the same prediction in the combat text-state as `flock.incoming` (`total`, `blocked`, `hpLoss`, `afterHp`, `attackers`) and added smoke coverage for the math.
- Verification: `npm run build`, focused Playwright smoke test `flock Cohesion bar exposes incoming damage prediction after Cover`, `npm test`, required generic `develop-web-game` client, and inspected combat screenshot `.artifacts/test-results/combat-incoming-health-preview.png`.

- Support encounter composition follow-up: converted every solo support encounter into a paired fight with at least one non-support threat so support enemies create target-priority decisions instead of low-pressure stall turns.
- Updated Signal Gull, Market Crow, Cicada Static Swarm, Crane Signal Caller, and Vulture Cleanup Crew encounters with companion enemies and `multi`/`support` tags where needed.
- Added a runtime data validation guard that fails any support-only encounter composition, including future support-only pairs.
- Verification: focused support-composition audit script, `npm run validate:runtime`, `npm run audit:balance`, `npm run build`, `npm test`, required generic `develop-web-game` client, and inspected `output/web-game/shot-1.png`.

- Market shop-scene redesign follow-up: researched Slay the Spire and Monster Train shop presentation, then rebuilt the market overlay from a row-list layout into a place-like shop scene with a vendor, awning/string lights, card rack, waymark tray, service signs, object-mounted price tags, refresh sign, and sold slats.
- Kept the existing randomized shelves, rarity-priced offers, paid refresh, finite purchases, and hover details intact while making the visual grouping follow goods/services/currency rather than three menu boxes.
- Verification: `npm run build`, focused market/hover/event Playwright smoke tests, required generic `develop-web-game` client against built preview, and inspected `.artifacts/test-results/market-shop-redesign/market-shop-scene-final.png`.

- Aviary art production pass: generated center art for `aviary_28` through `aviary_43` using the Minor Arcana center-art workflow, strengthened Aviary prompt language toward the title splash's bold streetwear silhouette, and composited the full 22-card Aviary set under the shared `master.png` raster border.
- Promoted the 16 new Aviary final cards into `.generated/imagegen/tarot/selected`, approved their runtime manifest entries, and rebuilt optimized portrait/thumb/icon WebP assets for the game.
- Verification: Aviary composite validation, center/final contact sheets, `npm run build:runtime-card-art`, `npm run validate`, `npm run build`, `npm test`, full `npx playwright test tests/smoke.spec.ts` (60/60 passed), and a local Playwright menu smoke against Vite dev server passed. QA outputs live under `tmp/qa/aviary/2026-06-17-aviary-master-border-expansion-v1/`.
- Aviary art anatomy correction: replaced `aviary_33` / Cache Memory because the first Pinyon Jay art had a hand-like lifted limb near the cache. The corrected card keeps both feet planted below the body and holds the cache only in the beak, then was recomposited, promoted, and rebuilt into runtime WebP assets. Verification: `npm run validate:aviary-composites`, `npm run validate:runtime-assets`, `npm run build`, and `npm run validate`.
- Aviary art two-wing correction: regenerated `aviary_30` / Underwing Shelter, `aviary_39` / Reed Balance, and `aviary_43` / Tool Probe with clearer two-wing silhouettes and no wing-hand prop interactions. The corrected centers were recomposited under the shared `master.png` border, promoted to selected art, and rebuilt into portrait/thumb/icon WebP runtime assets.
- Market art composition pass: generated runtime-sized market-kit assets from the concept outputs, wired the market node to use the new landmark backdrop, and composed the in-game shop with the transparent starling shopkeeper, hanging sign, and counter/wares layers while preserving existing purchase interactions. Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:runtime-assets`, `npm run validate:bundle-size`, focused market Playwright smoke tests, the required web-game client, and screenshot review at `.artifacts/test-results/market-kit-in-game.png`.

- Market art-first presentation pass: reduced the shop offer UI from heavy framed blocks into art-forward goods on the landmark scene. Card offers now show the full card thumbnail with only a tiny rarity chip and price tag, Waymarks/artifacts sit as counter objects with stats moved to hover, and supply/service offers use object tokens plus invisible hit areas instead of boxed menu rows.
- Added smoke coverage for non-card market item detail panels so Waymark/supply-style offers keep their stats available through hover while the scene stays visible.
- Verification: `npm run build`, focused market Playwright smoke tests (`market shelves`, generated market kit, card hover details, non-card item detail), required `develop-web-game` client with inspected `output/web-game/shot-1.png`, in-app browser opened `http://127.0.0.1:5273/`, and inspected market screenshot `.artifacts/test-results/market-art-first-in-game.png`.

- Market scenic open-frame pass: removed the generic `Landmark Visit` modal panel from the market and let the generated shop background fill the full viewport. The market now uses only subtle hairline guides, small floating Scrap/Refresh/Close controls, lighter scene masks, and a softer vendor nameplate so the shop reads as a place instead of a bordered menu.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client with inspected menu screenshot, dev-server market screenshot `.artifacts/test-results/market-scenic-open-in-game.png`, and built-preview market screenshot `.artifacts/test-results/market-scenic-open-built-preview.png`.

- Market opaque-readability pass: replaced the remaining transparent market guide borders and glassy control plaques with compact opaque enamel tags, physical shelf rails, solid hover-detail backs, explicit Waymark art loading, and a decorated Waymark fallback. This keeps price/scrap/service text readable while leaving the generated landmark scene more visible.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-opaque-readable-built-preview.png`.

- Market display polish pass: made the shop offers feel more physically staged by adding card hanger pins, small card stand feet, shaped opaque price tickets, individual Waymark display pads, and slimmer service perches. The final tuning kept the decorative hardware around the goods while avoiding the large opaque trays/signboards that made the first take feel too heavy.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-display-polish-final-built-preview.png`.

- Market asset-led cleanup pass: removed decorative shelf rails, card mounts, rarity chips, Waymark pads, service perches, and constructed supply symbols so the generated cards, shopkeeper, counter wares, and Waymark assets carry the scene with only subtle shadows and compact opaque price tags.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-assets-standalone-final-built-preview.png`.

- Codex supplies pass: expanded the Codex Items section to include the 5 authored supplies from `data/game/alpha-supplies.json` alongside the 40 Waymarks, with a Supplies filter tab, supply cards, and supply detail panels.
- Added focused smoke coverage for the Codex Supplies tab and Seed Packet detail panel.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts -g "codex: supplies are listed"`, required `develop-web-game` client, and visual screenshot inspection at `.artifacts/test-results/codex-supplies-tab.png` and `.artifacts/test-results/codex-supply-detail.png`.

- Expanded supplies from 5 to 17 entries with distinct tactical roles: pierce damage, anti-Cover, hand fixing, cleansing, Molt entry, burst turns, Open Sky safety, and high-cover defense.
- Added `visualBrief` art-generation descriptions to every supply, tuned toward the Bird Squad title splash art's neon rooftop streetwear/salvage style, and surfaced those briefs in the Codex supply detail panel.
- Wired expanded supply verbs in combat (`damage`, `damagePierce`, `damageAll`, `removeCover`, `applyWinded`, `cleanseFlock`, `enterMolt`, `discard`, `loseCohesion`, `nextTurnDraw`) and extended runtime validation/type answer sets.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Codex supplies and expanded supply verbs, required `develop-web-game` client, and visual screenshot inspection at `.artifacts/test-results/codex-expanded-supplies-tab.png` and `.artifacts/test-results/codex-bottlecap-popper-brief.png`.

- Market title-button/readability pass: matched Scrap, Refresh, Close, and price tags to the title screen's dark enamel button style, set all market-scene art images to full opacity, enlarged the shopkeeper/sign/counter/cards/Waymark goods, and gave service labels compact opaque backplates.
- Replaced repeated counter-wares supply placeholders with dedicated large supply icons where available, falling back to proxy Waymark/object art only for supplies without runtime icons yet.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-title-style-buttons-supply-art-built-preview.png`.

- Supply art completion pass: generated transparent runtime icons for the 12 expanded supplies that did not yet have art (`bottlecap_popper`, `tar_solvent`, `feather_splint`, `mirror_shard`, `rooftop_decoy`, `molt_pin`, `wire_snips`, `spare_harness`, `signal_kite`, `storm_lantern`, `cache_key`, `rain_cape`) using the supply visual briefs and title-splash neon rooftop salvage style.
- Added matching chroma source PNGs under `assets/concept-art/supplies/sources`, kept the 5 existing supply icons intact, and wired battle supply slots to preload and display supply art instead of text-only labels.
- Verification: transparent-alpha audit for all 17 supply icons, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, and visual screenshot inspection at `tmp/qa/supplies/supply-icons-contact-sheet.png` and `tmp/qa/supplies/codex-supply-art.png`.

- High-resolution pixel-art supply pass: used the image-generation workflow against the title splash style, then replaced all 17 runtime supply icons with 512x512 transparent high-res pixel-art cutouts authored from the supply descriptions and scaled with nearest-neighbor crisp edges.
- Saved new pixel chroma source PNGs as `*-pixel-chroma-v1.png` under `assets/concept-art/supplies/sources`, replacing the runtime WebP icons the Codex, market, and battle scenes already load.
- Verification: 17-icon alpha/dimension audit, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, and visual inspection at `tmp/qa/supplies/pixel-art/supply-icons-pixel-contact-sheet.png` and `tmp/qa/supplies/pixel-art/codex-supply-pixel-art.png`.

- Market selection agency pass: renamed the player-facing Nest removal option to `Remove a Card`, changed route/market removal copy from Release to Remove, and converted market Preen/Remove services from predetermined hidden targets into player-selected deck actions that charge Scrap only after the chosen card is picked.
- Enlarged market card-sale art and upgraded the Preen/Remove picker from a text list into visible deck card thumbnails with compact price chips, readable name backplates, and hover details for full stats.
- Verification: `npm run build`, focused market/card-picker Playwright smoke tests, required `develop-web-game` client against built preview, pixel sanity checks, and screenshot review at `.artifacts/test-results/market-selection-main-built-preview.png` and `.artifacts/test-results/market-selection-preen-picker-built-preview.png`.

- Market item cleanup pass: removed dark shadow pads from Waymark and utility/supply items, replaced split item label + separate price tags with one bordered box per non-card item, and removed the extra offset shadow from non-card hover detail panels.
- Kept price/name/action text inside fixed bordered boxes with constrained wrapping so long names like `Chalk Wingmark` and supplies such as `Tar Solvent` stay contained while the art remains readable.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, pixel sanity checks, and built-preview screenshots `.artifacts/test-results/market-clean-single-box-built-preview.png` and `.artifacts/test-results/market-clean-single-box-hover-built-preview.png`.

- Market service glyph cleanup: removed the floating `P` and `R` fallback letters from Preen/Remove service offers because the bordered service buttons now communicate those actions clearly enough on their own.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client, and built-preview screenshot `.artifacts/test-results/market-no-floating-service-glyphs-built-preview.png`.

- Pixel-art supply wiring pass: added a shared supply-art image helper that applies Phaser's per-texture `NEAREST` filter before rendering supply icons, preserving crisp pixel-art edges while leaving the rest of the painterly game art on smooth filtering.
- Confirmed supply art renders through Codex item cards/detail panels, market utility offers, and battle supply slots using the existing `supply-*` runtime keys.
- Verification: `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client, runtime probe showing all 17 visible `supply-*` images in Codex and `supply-bottlecap_popper` filter mode `1` (`NEAREST`), plus `npm run validate:runtime`.

- Imagegen sheet processing pass: split the supplied green-background 17-icon sheet from `C:\Users\Will\Desktop\download.png` into individual transparent 512x512 runtime WebP supply icons, preserving high-resolution pixel-art edges and replacing the prior generated runtime cutouts.
- Saved the original sheet and per-supply source crops under `assets/concept-art/supplies/sources`, with transparent QA PNGs and contact sheets under `tmp/qa/supplies/imagegen-sheet`; corrected the Molt Pin and Rain Cape crop boxes after visual QA caught row-overlap artifacts.
- Verification: 17-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, `npm run build`, focused Playwright supply smoke tests, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex probes/screenshots confirming all 17 supply textures load after grid scroll with `NEAREST` filter mode.

- Codex supply presentation pass: removed the circular supply icon backplates from Codex supply cards and removed the boxed art frame from supply detail modals so the transparent pixel-art items stand on their own.
- Verification: `npm run build`, focused Playwright Codex supply smoke test, required `develop-web-game` client on `http://127.0.0.1:5173`, and visual screenshot review at `tmp/qa/supplies/codex-supply-standalone-icons.png` plus `tmp/qa/supplies/codex-supply-standalone-detail.png`.

- Flock leader Codex art pass: generated transparent forward-facing 3/4 runtime Codex art for Fledgling, Spark-Caller, Tidewarden, and Roostkeeper, matching the already-wired Talon treatment while preserving each leader's species, outfit, gear, and color identity.
- Wired the Codex leader art override table so all five flock leaders now request `front-3q` WebP assets in leader thumbnails/details, leaving combat/menu art on the original leader art paths.
- Verification: `npm run build`, required `develop-web-game` client against the built preview, and focused Playwright Codex screenshots at `.artifacts/fledgling-codex-front-art-check.png`, `.artifacts/spark-caller-codex-front-art-check.png`, `.artifacts/tidewarden-codex-front-art-check.png`, and `.artifacts/roostkeeper-codex-front-art-check.png`. Final request log showed all five leader assets using `front-3q`.

- Market best-next presentation pass: replaced the wide bottom market narration slab with a compact opaque Veyra bark tucked onto the vendor side, split Preen/Remove into a small service-board area, and staged Supplies as larger physical counter objects with one compact purchase ticket each.
- This pass keeps hover details and the randomized purchase logic intact while clearing more of the landmark scene and making the Supply objects read as actual goods instead of menu rows.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against the built preview, `git diff --check`, and visual screenshot inspection at `.artifacts/test-results/market-best-next-built-preview.png`.

- Tidewarden anatomy correction: replaced the forward-facing Tidewarden Codex asset with a no-arms variant so the rain jacket drapes over folded wings instead of reading as humanlike sleeves/hands.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and focused Codex screenshot `.artifacts/tidewarden-codex-no-arms-check.png` confirmed the built game requests `tidewarden-combat-front-3q-TtOWaJQn.webp`.

- Tidewarden sleeved back-facing pass: generated a replacement `tidewarden-combat-back-ne` asset to match the sleeved front-facing Tidewarden identity while preserving the back/NE combat orientation. The replacement keeps the teal jacket sleeves, hood, crossed back straps, olive medic satchel, red-cross tag, heron head profile, and long-legged silhouette.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and direct BattleScene screenshot `.artifacts/tidewarden-back-ne-sleeved-battle-check.png` confirmed the built game requests `tidewarden-combat-back-ne-B567ol0E.webp`.

- Waymark imagegen art pass: generated four 10-icon high-resolution pixel-art sheets for all 40 Waymarks, matching the supply sheet's neon rooftop salvage style and preserving standalone object silhouettes on transparent runtime backgrounds.
- Extracted and saved the imagegen sheets plus per-Waymark source crops under `assets/concept-art/waymarks/sources`, then split/chroma-cleaned them into 512x512 transparent runtime WebP icons in `assets/runtime/waymarks/icons`.
- Updated Waymark rendering to use nearest-neighbor pixel-art filtering and removed Codex Waymark thumbnail/detail art backplates so the cutouts stand on their own like the supplies; reward, market, and combat Waymark image calls now use the same helper path.
- Verification: 40-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex probe confirming 40 Waymarks cataloged, visible Waymark textures loaded, and `waymark-chalk_wingmark` using `NEAREST` filter mode. QA outputs live under `tmp/qa/waymarks/imagegen-sheets/`.

- Build-around item expansion pass: added 6 supplies (`Return Ticket`, `Nest Clamp`, `Smoke Thread`, `Windcatcher Spool`, `Resonance Battery`, `Oath Ledger`) and 8 Waymarks (`Flock Counterweight`, `Supply Bell`, `Molt Metronome`, `Eave Bloom`, `Plumes Busker Patch`, `Quills Pressure Gauge`, `Basin Overflow Cup`, `Nest Ratchet`), bringing Codex Items to 71 total (48 Waymarks / 23 Supplies).
- Wired new runtime hooks for Waymarks that trigger on Supply use, first Molt entry, first suit card played each turn, and healing restored each turn; added richer mark effects for Waymark-count Cover scaling, Resonance gain, all-enemy ping damage, next-turn draw/Wingbeat, and next Nest Cover bonuses.
- Extended supply execution with `returnDiscard`, `nextCoverBonus`, `gainEnergyNextTurn`, `gainScrap`, and `resonanceBurst`, then added temporary transparent pixel-art placeholder icons for the new supplies and Waymarks under the runtime icon folders plus matching concept source PNGs.
- Updated validation/smoke coverage for the new item counts, new supply assets, and the build-around gameplay hooks. Raised the Waymark icon budget to match the intentional 512px high-resolution runtime format instead of the old small-icon budget.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Codex supplies/expanded supply verbs/new build-around hooks, required `develop-web-game` client on `http://127.0.0.1:5173`, live Codex screenshot at `tmp/qa/items-expansion/codex-items-71-new-supplies-waymarks.png`, and `npm test`.

- Market larger-card/no-overlap pass: increased market card offer art from 164x234 to 176x252, widened the card hit/display rack, and moved the Waymark lane lower so card price tickets and Waymark objects no longer crowd each other.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against the built preview, `git diff --check`, and screenshot inspection at `.artifacts/test-results/market-larger-cards-no-overlap-built-preview.png`.

- Market Veyra/title readability pass: removed Veyra's speech bubble and lower nameplate, moved `Veyra Tallybright / CANAL CURATOR` into a compact top title tag, and added small opaque backing plates behind Waymark and supply objects so their transparent art reads against the landmark scene without rebuilding the whole shop into menu boxes.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-veyra-title-object-backs-built-preview.png`.

- Market shopkeeper scale/no-overlap pass: enlarged Veyra's shopkeeper art and moved her further left while keeping the first card lane clear; shifted the utility/supply lane right and lowered supply label boxes so object backplates, labels, and the fourth card no longer crowd each other.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` for `src/main.ts`/`progress.md` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-large-left-shopkeeper-no-overlap-built-preview.png`.

- Market larger-shopkeeper hard-spacing pass: scaled Veyra up again and anchored her below the bottom edge so she reads as a foreground shopkeeper, then narrowed and re-spaced the card rack and right utility/supply lane with explicit horizontal and vertical gaps to remove remaining item overlaps.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-shopkeeper-bigger-bottom-hard-spacing-built-preview.png`.

- Market composition nudge: lowered Veyra's bottom anchor so her feet sit below the viewport, moved her slightly right, and lifted the card rack while preserving the hard-spaced card and goods lanes. After screenshot review, dropped the shopkeeper anchor further so the feet are actually clipped below the screen edge.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-shopkeeper-feet-under-cards-up-built-preview.png`.

- Tidewarden back-facing sleeve correction: replaced the back/NE asset again so the teal sleeve fabric drapes over folded wings instead of forming arm-like tubes; preserved the sleeved front identity, hood, crossed back straps, satchel, red-cross tag, heron profile, and long-legged silhouette.
- Verification: alpha/chroma processing passed, `npm run build`, required `develop-web-game` client, and direct BattleScene screenshot `.artifacts/tidewarden-back-ne-draped-wing-battle-check.png` confirmed the built game requests `tidewarden-combat-back-ne-z9XIXyYe.webp`.

- Flock leader starter deck refresh: rebuilt all five 10-card singleton starter decks around the expanded card pool. Fledgling now teaches the four new suit Fledgling crew cards; Spark-Caller, Talon, Tidewarden, and Roostkeeper each open with a six-card suit keystone package plus newer off-suit support cards that reinforce their signatures.
- Updated the mirrored `alpha-cards.json` global starter/reward partition for the Fledgling deck, keeping 10 starter cards, 90 reward cards, and no starter/reward overlap.
- Verification: `node tools/balance-audit.mjs`, starter/reward overlap audit, all-leader deck validity check, `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for leader starter selection / Plumes keystone / Codex deck discovery, and required `develop-web-game` client on `http://127.0.0.1:5623/`.

- New item imagegen art pass: generated dedicated high-resolution pixel-art sheets for the 6 new supplies and 8 new Waymarks from the build-around item expansion, matching the title-splash/neon rooftop salvage style and avoiding border circles or backplates so each item stands alone.
- Chroma-cleaned and split the sheets into transparent 512x512 crops, replacing the temporary runtime placeholders in `assets/runtime/supplies/icons` and `assets/runtime/waymarks/icons`; saved source sheets/crops under `assets/concept-art/supplies/sources` and `assets/concept-art/waymarks/sources`.
- Verification: 14-icon alpha/dimension/green-fringe audit, `npm run validate:runtime`, runtime asset-size validation, `npm run build`, focused Playwright Codex/item smoke tests, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex screenshots/probe under `tmp/qa/items-imagegen/new-items/` confirming `supply-return_ticket` and `waymark-flock_counterweight` load with `NEAREST` filtering.

- Next item pass brief: added `docs/game/item-expansion-next-pass.md` with current item coverage gaps, next-pass design targets, proposed route/either supplies, conditional Waymarks, new runtime trigger/verb candidates, art-generation sheet guidance, and test targets for the next implementation pass.

- Market item-row alignment pass: moved Supply offers down to share the Waymarker display line, renamed compact item tags from `PIN` to `WAYMARKER` and from `PACK` to `SUPPLY`, and nudged Veyra slightly right while scaling her up so the shopkeeper remains a stronger foreground landmark figure.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-supplies-waymarker-level-labels-built-preview.png`.

- Market stock/cluster pass: moved the card rack higher, shifted Veyra farther right toward the lower goods area, tightened the Waymarker spacing, moved the Supply row left into the same goods cluster, and expanded market stock to offer 3 Waymarkers plus 3 Supplies.
- Follow-up overlap fix: moved the Preen/Remove service boxes to the far-right side so they no longer sit over the fourth card art after the card rack was lifted.
- Verification: `npm run validate:runtime`, `npm run build`, focused market Playwright smoke tests with 3 Waymarker / 3 Supply assertions, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-three-and-three-cluster-service-clear-built-preview.png`.

- Market service-stack and goods nudge: bumped Waymarker/Supply objects and labels upward, shifted the lower goods cluster slightly left, removed the oversized top Refresh sign, and rendered Refresh as a third compact service row under Remove with the same size/style as Preen and Remove.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, `git diff --check` (line-ending warnings only), and screenshot inspection at `.artifacts/test-results/market-refresh-service-stack-final-built-preview.png`.

- Market goods parity pass: moved the Preen/Remove/Refresh stack upward to sit more centered beside the cards, and standardized Waymarker/Supply item presentation to matching art plates, art size, label size, hit area, and spacing rhythm.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-service-centered-goods-parity-built-preview.png`.

- Market title banner pass: moved Veyra Tallybright's banner upward, widened it into a centered title plaque, and centered/scaled both the shopkeeper name and Canal Curator subtitle so the header reads as part of the landmark scene.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-centered-title-banner-built-preview.png`.

- Market goods row alignment pass: moved the Waymarker/Supply cluster upward together and put both offer types on a shared row baseline with matching art centers, label centers, sold slats, and hit areas.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-goods-shared-row-built-preview.png`.

- Market goods spacing fix: shifted the Waymarker shelf left so the rightmost Waymarker label no longer overlaps the leftmost Supply label while preserving the shared lower-row baseline.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-waymarkers-shifted-left-built-preview.png`.

- Market scrap badge polish: simplified the Scrap counter into a tighter opaque enamel badge with a cleaner gold border, centered value alignment, and stronger text contrast.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and screenshot inspection at `.artifacts/test-results/market-scrap-badge-polish-built-preview.png`.

- Market shopkeeper idle pass: added a subtle foot-anchored breathing tween to Veyra's shopkeeper art and a timed single-eye blink overlay while keeping the shopkeeper texture as a top-level rendered image for existing market art probes.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, animation probe confirming shopkeeper scale and blink alpha changes, and screenshot inspection at `.artifacts/test-results/market-shopkeeper-idle-built-preview.png`.

- Market shopkeeper blink visibility fix: replaced the too-subtle shape overlay with a closed-eye shopkeeper art variant (`assets/runtime/market-kit/starling-shopkeeper-blink-v1.webp`) and crossfaded it over the base shopkeeper during idle blinks so the blink is visible in the rendered market scene.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and closed-frame blink probe/screenshot inspection at `.artifacts/test-results/market-shopkeeper-blink-visible-built-preview.png` plus head crop `.artifacts/test-results/market-shopkeeper-blink-head-crop.png`.

- Market shopkeeper TravQuest-style blink revision: removed the full-body closed-eye variant and switched Veyra to a tiny low-alpha eyelid line over the existing shopkeeper art, timed as a brief idle blink while keeping breathing separate.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against built preview, and closed-frame screenshot inspection at `.artifacts/test-results/market-shopkeeper-travquest-blink-line-final-built-preview.png` plus head crop `.artifacts/test-results/market-shopkeeper-travquest-blink-line-final-head-crop.png`.

- Market shopkeeper blink placement correction: moved the blink from the face feathers onto the rendered eye, replaced the too-subtle line with a small eyelid patch container above the shopkeeper art, and shortened the repeat delay so the blink is noticeable during normal market viewing.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against `http://127.0.0.1:5636/`, and open-vs-closed eye comparison at `.artifacts/test-results/market-shopkeeper-eye-open-vs-closed-patched-crop.png`.

- Market shopkeeper blink removal: removed the eyelid patch and blink alpha tween entirely after visual review showed the mechanic did not suit the shopkeeper art; Veyra keeps only the subtle breathing idle tween.
- Verification: `npm run build`, focused market Playwright smoke tests, required `develop-web-game` client against `http://127.0.0.1:5636/`, runtime probe confirming zero blink-named objects and one shopkeeper idle tween, and screenshot inspection at `.artifacts/test-results/market-shopkeeper-no-blink-built-preview.png`.

- Placeholder/load flash audit: identified live fallback renderers for battle backdrops, flock leaders, enemies, route node glyphs, route-event backdrops, market kit art, card offers, Waymarkers, and Supplies; these are still useful load-failure guards, but normal scene entry was sometimes rendering before optional art finished.
- Added preload-time runtime art queuing for RouteScene and BattleScene so first render waits for route node icons, market kit/backdrop art, route deck cards, battlefield art, current flock leader art, active waymarks/supplies, current battle deck, and current enemies. Battle's follow-up optional art queue now requests only active run assets instead of every item icon globally.
- Safety call: runtime item icons referenced by supply/Waymark data are not safe to remove while their data entries remain live; fallback drawing branches should stay as failure guards. Any old concept-source placeholder PNGs are provenance-only once replaced by final runtime art.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5623/`, BattleScene preload texture probe/screenshot at `.artifacts/placeholder-preload-battle-check.png`, RouteScene/market preload probes, and market screenshot at `.artifacts/placeholder-preload-market-check.png`.

- Route map edge-line refinement: reduced route board connection stroke widths so inactive/secondary/available/lit edges read as thinner map lines without changing node rings, icons, labels, or column guides.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and route screenshot inspection at `.artifacts/route-map-thin-edge-lines-check.png`.

- Route map dotted-path refinement: replaced straight edge segments with deterministic quadratic dotted paths. Each route connection now curves between nodes with small stamped dots, preserving the existing color/alpha hierarchy for inactive, secondary preview, available, and primary/lit routes.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and route screenshot inspection at `.artifacts/route-map-curved-dotted-paths-check.png`.

- Route map breathing-room pass: widened the map board, moved the route description inspector farther right, expanded graph bounds, spread node lanes slightly farther apart, and added a soft halo to highlighted dotted paths so reachable/preview edges remain readable after the dotted-path styling.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with two reachable nodes, and screenshot inspection at `.artifacts/route-map-wide-breathing-highlight-check.png`.

- Next item pass implementation: wired 8 supplies (`Map Sticker Strip`, `Thermos Lid`, `Bus Token Cache`, `Chalk Dust Pouch`, `Market IOU`, `Sky Sugar`, `Anchor Threader`, `Spare Pocket`) and 10 Waymarks (`Broken Cover Chime`, `Spark Ground Clip`, `Quiet Roost Token`, `Double-Packed Buckle`, `Ledger Tab`, `Basin Safety Pin`, `Nest Measure Line`, `Plumes Applause Cap`, `Molt Shadow Tag`, `Cache Divining Hook`), bringing Codex Items to 89 total (58 Waymarks / 31 Supplies).
- Added route supply slots and route-side supply use, plus runtime hooks/effects for route previews, permanent supply capacity, enemy Cover-break draw, Resonance-spend Cover, no-damage-turn healing, next-supply repeat, heal cleanse, Nests hand retention, Plumes fourth-card payoff, Molt Open Sky guard, market rebates, and cache supply picks.
- Generated matching high-resolution pixel-art sheets for the 18 new items, chroma-cleaned them into transparent 512x512 runtime icons under `assets/runtime/supplies/icons` and `assets/runtime/waymarks/icons`, and saved imagegen source sheets/crops under the concept-art source folders.
- Verification: `npm run validate:runtime`, runtime asset-size validation, `npx tsc --noEmit`, focused Playwright smoke tests for Codex/item hooks, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173`.

- Next item supply art repair: replaced the 8 next-pass Supply runtime icons with individually generated high-resolution pixel-art cutouts instead of grid crops, removing sheet-boundary clipping and fixing Anchor Threader's green tape alpha damage by switching to a magenta-key workflow with edge despill.
- Verification: final supply contact sheet at `tmp/qa/items-imagegen/next-pass/next-pass-supplies-transparent-contact-v3.png`, residue/corner/edge audit, runtime asset-size validation, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and live Codex detail screenshot at `tmp/qa/items-imagegen/next-pass/codex-anchor-threader-repair.png`.
- Thermos Lid steam cleanup: neutralized remaining magenta-key contamination in the Thermos Lid steam curl and edge specks, updating `assets/runtime/supplies/icons/thermos_lid.webp` plus source crops through `thermos_lid-imagegen-crop-v7.png`.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, final contact sheet `tmp/qa/items-imagegen/next-pass/next-pass-supplies-transparent-contact-v7.png`, and live Codex screenshot `tmp/qa/items-imagegen/next-pass/codex-thermos-lid-steam-repair.png`.

- Route map highlighted-dot pullback: reduced lit/primary/available dotted-path radii and softened the highlight halo so reachable paths remain visible without clustering into thick bead chains.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with two reachable nodes, and screenshot inspection at `.artifacts/route-map-highlight-dots-thinner-check.png`.

- Route map highlighted-dot extra-thin pass: took the active/preview/available dotted paths down one more notch and reduced the halo spread so highlighted routes feel path-like without clumping.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, focused progressed-route probe with four reachable nodes, and screenshot inspection at `.artifacts/route-map-highlight-dots-extra-thin-check.png`.

- Route board image-gen backing correction: replaced the procedural route-board texture with the attached generated map-paper image, preserving the original PNG source at `assets/concept-art/ui/route-map/route-board-laminated-plan-v1.png` and optimizing a cropped 1200x693 runtime WebP (~73 KB) at `assets/runtime/backdrops/route-board-laminated-plan-v1.webp`.
- Wired the texture through BootScene preload and RouteScene rendering as a clearer screen-blended layer inside the map border so nodes, dotted paths, and inspector highlights remain readable while the generated map art is visible.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, temporary built-preview route probe confirming `route-board-laminated-plan` loaded with 20 nodes, and screenshot inspection at `.artifacts/route-map-imagegen-plan-backing-built-preview-canvas.png`.

- Route map backing brightness/fit pass: settled on `route map backing` as the code-facing name for the generated paper layer, expanded it to the full inner route-board panel, and reduced the dark wash so it reads as the actual map surface behind the route icons.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview screenshot inspection at `.artifacts/route-map-backing-expanded-brightened-built-preview-canvas.png`.

- Route map backing full-color/full-bleed correction: removed the remaining backing inset, tint, alpha fade, and dark wash so the generated map image fills the whole route-board backing at full color; redrew only the thin frame accents above the image.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview screenshot inspection at `.artifacts/route-map-backing-full-color-full-bleed-built-preview-canvas.png`.

- Route node icon contrast pass: refreshed the eight route node runtime icons from the existing imagegen route atlas using the item-icon processing style: stronger dark silhouettes, boosted color/contrast, cleaned transparent edges, and small detached-alpha cleanup so Street/Rival/Boss/Basin/Nest/Market/Signal/Cache read on the new full-color map backing.
- Saved versioned source outputs under `assets/concept-art/ui/route-map/route-node-icons-contrast-v2/` plus `assets/concept-art/ui/route-map/route-node-icon-atlas-contrast-v2.png` and `assets/runtime/map-icons/map-node-icon-atlas-contrast-v2.png`; replaced the existing runtime WebP icon filenames under `assets/runtime/map-icons/icons/` so RouteScene picks them up without code changes.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-contrast-v2/route-node-icons-contrast-v2-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5623/`, and built-preview route screenshot `.artifacts/route-map-icons-contrast-v2-built-preview-canvas.png`.

- Title screen floating-particle bump: increased the home-screen mote emitter frequency, lifespan, drift, alpha, scale, and live-particle cap so the floating particles read a little richer without obscuring the splash art or menu controls.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, direct Playwright MenuScene probe confirming the `menu-soft-mote` emitter at 34/36 live particles, and screenshot inspection at `.artifacts/test-results/title-particles-bumped-menu.png`.

- Title logo shimmer pass: added a reduced-motion-aware additive gold sheen over the transparent Bird Squad title images, triggered after the title impact and repeated on a long delay so the logo catches light without adding stray streaks over the splash art.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, direct Playwright MenuScene probe confirming both shimmer overlay images active at alpha `0.2`, and screenshot inspection at `.artifacts/test-results/title-logo-shimmer-final.png`.

- Title shine visibility follow-up: strengthened the title shimmer with brighter additive title sheen and three logo-anchored twinkles so the effect is plainly visible on first view.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5173`, and a direct Playwright title-screen probe confirming sheen/twinkle alpha with screenshot output at `.artifacts/test-results/title-logo-shine-visible.png`.

- Title polish follow-up: removed the visible logo twinkle flares, reduced the looping title sheen to a slow low-alpha warm lift, and softened the entrance gold burst into sparse tiny dust flecks so the title treatment feels less flashy.
- Verification: `npm run build`, fresh Vite dev server on `http://127.0.0.1:5634`, required `develop-web-game` Playwright client against that clean port, and screenshot inspection at `.artifacts/test-results/title-polished-final-client/shot-0.png`.

- Title feathers-only rollback: removed the delayed logo sheen/glint overlay entirely so the title screen returns to the simpler feather/particle-only treatment while keeping the title entrance intact.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-feathers-only-client/shot-0.png`.

- Title effects full rollback: restored the original home-screen particle emitter settings and original 42-fleck title impact burst, with the shimmer/glint overlay still removed.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-original-effects-client/shot-0.png`.

- Title particle preference follow-up: restored the boosted ambient home-screen particle emitter values while keeping the logo shimmer/glint removed and leaving the original title impact flecks in place.
- Verification: `npm run build`, required `develop-web-game` Playwright client against `http://127.0.0.1:5634`, and screenshot inspection at `.artifacts/test-results/title-boosted-particles-client/shot-0.png`.

- Codex Items filter pass: split the Items browser into a type row (`All`, `Waymarks`, `Supplies`) plus contextual filters. Waymarks now filter by family (`Shelter`, `Tempo`, `Economy`, `Suit`, `Molt`, `Boss`); Supplies filter by timing and tactical role (`Combat`, `Route`, `Flexible`, `Defense`, `Recovery`, `Momentum`, `Pressure`, `Intel`). The Items grid gets extra header height only in that section, and legacy smoke/debug `activeItemTab` values still map to the expected old Supplies/Waymarks selections.
- Verification: `npm run build`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "codex: supplies are listed as items"`, required `develop-web-game` client on `http://127.0.0.1:5634`, and built-preview screenshots inspected at `.artifacts/test-results/codex-items-two-row-all.png`, `.artifacts/test-results/codex-items-waymark-filters.png`, and `.artifacts/test-results/codex-items-supply-filters.png`.

- Supplies/Waymark tuning pass: strengthened underperforming supplies with clearer tactical hooks (`Shade Cloth`, `Signal Kite`, `Cache Key`, `Oath Ledger`, `Map Sticker Strip`, `Bus Token Cache`, `Market IOU`, `Anchor Threader`, `Spare Pocket`, and clarified `Thermos Lid`) and corrected `Molt Shadow Tag` to use the Open Sky Guard effect its text promised.
- Added conditional combat-supply support for `if visitedNodeType(cache) then ...`, letting `Cache Key` reward players who opened a rooftop Cache earlier in the district.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173`.

- Waymark trigger tuning pass: moved several older flat-start Waymarks into more expressive triggers: `Chalk Wingmark` now rewards breaking enemy Cover, `Stage Pin` keys off Plumes, `Quill Notch` keys off Quills, `Feather Tape` keys off Basins, `Workshop Stamp` keys off Nests, and `Rooftop Relay Bell` keys off Supply use.
- Route-memory supply follow-up: added conditional route-effect support for `visitedNodeType(...)`, then gave `Market IOU` a Market-visit Cohesion refund and `Bus Token Cache` a Signal-visit Scrap kicker.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "route marks are real relics|codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Route node icon splash-style integration: cleaned the provided green-screen atlas `C:/Users/Will/Desktop/dfg.png` into transparent `Street`, `Rival Crew`, `Boss`, `Basin`, `Nest`, `Market`, `Signal`, and `Cache` source crops under `assets/concept-art/ui/route-map/route-node-icons-splash-v3/`, preserved the full source atlas as `assets/concept-art/ui/route-map/route-node-icon-atlas-splash-v3.png`, and replaced the existing runtime WebP filenames under `assets/runtime/map-icons/icons/` so RouteScene picks them up without code changes.
- Exported the runtime icons as 192px transparent quality-86 WebPs, all under the 32 KB route-node icon budget, and verified the real RouteScene map/legend rendering on preview port `5641`.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-splash-v3/route-node-icons-splash-v3-runtime-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and live route screenshot `.artifacts/route-icons-splash-v3-client-route/shot-3.png`.

- Route map icon scale-up: increased RouteScene node icon display sizes from 66/84 to 82/106 for normal/boss nodes, with matching node radii and focus padding so the splash-style map icons read larger and more detailed while preserving hit targets, visual bounds, reward pips, and inspector thumbnails.
- Verification: `npm run build`, `node tools/validate-runtime-asset-sizes.mjs`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and visual inspection of `.artifacts/route-icons-large-client/shot-3.png`.

- Route map path contrast pass: shifted dotted route edges from low-alpha pale tones to higher-contrast inked dots with a small dark underlay, using warmer amber for primary/reachable previews, cyan for available routes, green for chosen routes, and darker blue-gray for inactive/secondary paths so the dots read clearly on the full-color map backing without becoming thick.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and visual inspection of `.artifacts/route-path-contrast-client/shot-3.png`.

- Route node icon splash-style v5 follow-up: replaced the weak Street and oversized Signal concepts with a combat-forward street marker built from crossed hook/crowbar waymark art over the street plate and a compact windcatcher-spool signal marker with no radio-wave arcs; preserved the other six v4 icons and re-exported the runtime set as sharper 256px high-quality transparent WebPs under the existing `assets/runtime/map-icons/icons/*.webp` filenames.
- Increased route node icon display sizes to 90px normal / 114px boss with matching radii, and tightened Street/Signal export padding so the art fills the node better without edge clipping.
- Verification: contact sheet inspection at `tmp/qa/route-map-icons-splash-v5/route-node-icons-splash-v5-runtime-contact.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client against `http://127.0.0.1:5641/`, and live route screenshot `.artifacts/route-icons-splash-v5-client-2/shot-4.png`.

- Rare/Boss Waymark identity pass: moved `Parade Mirror`, `Harbor Bead Strand`, `Fresh Pinfeather`, `Rooftop Nest Lining`, and `Crowbar Debt` away from flat startup stats into build-defining trigger payoffs, and increased `Sky-Safe Harness` boss prep from 10 to 14 Cover.
- Basin/Nest route-memory supply pass: `Seed Packet` now heals extra after a Basin visit, and `Nest Clamp` retains a card after a Nest visit.
- Verification: `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts --grep "route marks are real relics|new balance verbs|codex: supplies are listed|expanded supplies resolve tactical combat verbs|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Non-battle set-piece kickoff: added a cast art brief that keeps each personality on a distinct bird species and fashion silhouette from Veyra, then converted Basin visits into the first `Lantern Roost` landmark screen with Sella Warmwick, a generated shelter backdrop, the roostkeeper resident art, compact title/status plates, and opaque choice tickets.
- Route-event art loading now supports generated route-event runtime WebPs alongside the existing market and battlefield variant backdrops, and the smoke coverage verifies the new Lantern Roost backdrop loads and renders.
- Verification: `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route event overlays|signals/basins/cache|route choice and market"`, required `develop-web-game` client on `http://127.0.0.1:5637/`, direct Lantern Roost Playwright probe, and screenshot inspection of `.artifacts/lantern-roost-set-piece.png`.

- Non-battle set-piece expansion: generalized the Lantern Roost overlay into a reusable personality-led route-event set piece for Basin, Cache, Signal, and Nest, with top title/species plaque, compact resource tag, resident art, landmark note, and opaque decision tickets that keep the backdrop visible.
- Added resident art mapping for Marn Valeclip (crow), Ivo Tallymast (kestrel), and Oren Shearbright (woodpecker), using existing approved runtime art as the first playable pass while the updated brief tracks future custom cutout targets.
- Verification: `npm run build`, focused `npx playwright test tests/smoke.spec.ts -g "route event overlays|signals/basins/cache|route choice and market"`, required `develop-web-game` client on built preview `http://127.0.0.1:5638/`, direct built-preview Playwright probes for Basin/Cache/Signal/Nest with no offscreen text, and screenshot contact-sheet inspection at `.artifacts/nonbattle-setpieces-built/contact-sheet.png`.

- Non-battle set-piece decomposition correction: replaced the borrowed resident/backdrop wiring with route-event-specific assets. Each planned personality now has a flat green chroma-source PNG, an alpha-source PNG, and a transparent runtime WebP under the route-event asset pipeline; each set piece now has its own 16:9 scene plate under `assets/runtime/route-events/`.
- Prepared decomposed assets for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and future Rival Wager. Basin/Cache/Signal/Nest now render from the route-event assets directly; Rival assets are staged for a later route-choice conversion.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused route-event and route-choice Playwright tests run individually after the combined dev-server run hit a transient port handoff, required `develop-web-game` client on built preview `http://127.0.0.1:5638/`, built-preview probes confirming all four active set pieces render their expected scene/resident keys with no offscreen text, and screenshot inspection of `.artifacts/nonbattle-setpieces-decomposed-built/contact-sheet.png` plus chroma/scene asset contact sheets under `.artifacts/route-event-decomposed-assets/`.

- Boss reward identity follow-up: added the `districtStartKit(1)` Waymark effect and upgraded `Reopened Roofline` so the next district starts with both a free Preen and a packed random Supply when there is room, making the boss trophy feel more run-shaping than a single prep coupon.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|route marks are real relics|codex: supplies are listed|next item pass supplies"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `output/web-game/shot-0.png`.

- Item communication polish pass: added derived build/synergy tags for Supplies and Waymarks, then surfaced them on Codex item thumbnails, Codex item detail panels, boss Waymark reward cards, and the Waymark reward debug state. Tags call out hooks like Route Memory, Supply Combo, Next District, Preen, Anti-Cover, Recovery, Cover, Economy, and Boss so players can understand why an item matters before they pick it.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|codex: supplies are listed"` plus the four-test item mechanics group, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, Codex/detail screenshot inspection at `.artifacts/test-results/item-tags/codex-supply-tags.png`, boss Waymark reward screenshot inspection at `.artifacts/test-results/item-tags/boss-waymark-tags.png`, and final menu screenshot inspection of `output/web-game/shot-0.png`.

- Waymark trigger feedback pass: added combat feedback for Waymarks when they fire, including a short animated toast near the Waymark rail, a persistent `LAST TRIGGER` strip under the Waymark shelf, and `waymarkFeedback` in the render-state payload for smoke coverage. The feedback uses the current Waymark art and effect summary so triggers like `Crowbar Debt` read as concrete payoffs instead of hidden stat changes.
- Fixed combat Waymark art loading for marks added after scene start by queueing current carried Waymark art from the shelf renderer, making the drawer and feedback deterministic even when tests or runtime rewards mutate the carried list mid-scene.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "boss Waymark reward choices|route marks are real relics|waymark artifact drawer|codex: supplies are listed"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, trigger-feedback screenshot inspection at `.artifacts/test-results/waymark-feedback/crowbar-feedback.png`, and final menu screenshot inspection of `output/web-game/shot-0.png`.

- Lantern Roost first-scene completion: rebuilt the Basin visit as the first finished non-battle set piece, using a v2 landmark scene plate, Sella Warmwick resident art, and separate heated hearth/sign prop art so the encounter reads as a visited shelter instead of a menu overlay.
- Wired the Lantern Roost kit through RouteScene with a dedicated Basin renderer, centered title plaque, compact scrap/cohesion tag, large resident placement, opaque choice tickets, and screenshot/test coverage that confirms the scene, resident, hearth, and sign textures all render together with no offscreen text.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on `http://127.0.0.1:5639/`, and direct first-scene probe screenshot `.artifacts/lantern-roost-first-scene-complete.png`.

- Lantern Roost prop-kit completion: generated the remaining choice-specific pieces for the first scene: `lantern-roost-rain-pipe-v1`, `lantern-roost-awning-v1`, and `lantern-roost-wrapped-snack-v1`, each with alpha-source PNG, chroma-source PNG, and optimized transparent runtime WebP files.
- Wired the new props into the dedicated Basin renderer beside their matching choice tickets without adding new text boxes or borders, then expanded the route-event smoke assertion so it verifies the full Lantern Roost kit: backdrop, resident, hearth, sign, rain pipe, awning, and wrapped snack.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on `http://127.0.0.1:5644/`, prop contact sheet `.artifacts/lantern-roost-prop-kit-v1-contact.png`, and in-scene screenshot `.artifacts/lantern-roost-full-prop-kit-screen.png`.

- Route-event centerpiece decomposition pass: made the remaining planned centerpieces into first-class assets instead of metadata-only labels. Added high-resolution pixel-art-style alpha/chroma/runtime assets for Rooftop Cache's sealed pulley cache cabinet, Signal Landmark's illuminated route switchboard, Featherwright Studio's grooming chair and feather press, and Rival Wager's contract case/prize board.
- Wired Cache, Signal, and Nest set-piece overlays to queue and render their centerpiece assets independently over the scene plate, and extended the route-event smoke test so it verifies each active centerpiece texture is loaded and rendered. Rival's centerpiece is staged in the same asset pipeline, but remains unused until Rival gets a route-choice flow.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, built-preview route-event probes on `http://127.0.0.1:5646/`, required `develop-web-game` client, source contact sheet `.artifacts/route-event-centerpiece-kit-v1-contact.png`, and built-preview screenshot sheet `.artifacts/route-centerpieces-built-contact.jpg`.

- Supply use feedback pass: added `supplyFeedback` state for route and combat supplies, visible `LAST SUPPLY` strips, combat supply toasts, and render-state payload coverage so players can see what a consumed supply just did instead of inferring from stat deltas. Route supply feedback now renders above the route inspector without hiding four-slot supply shelves; combat feedback stacks with Waymark trigger toasts.
- Improved effect formatting for supply-packing verbs (`gainSupply`, `gainSupplyChoice`) so item summaries read as player-facing actions instead of raw runtime tokens.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused `npx playwright test tests/smoke.spec.ts --grep "next item pass supplies and Waymarks execute route and combat hooks|supplies are carried into combat and usable for an effect"`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173`, and screenshot inspection of `.artifacts/test-results/supply-feedback/route-supply-feedback.png`, `.artifacts/test-results/supply-feedback/combat-supply-feedback.png`, and `output/web-game/shot-0.png`.

- Route node icon simplification pass: processed the simplified green-screen atlas `C:/Users/Will/Desktop/32131.png` into transparent source crops under `assets/concept-art/ui/route-map/route-node-icons-splash-v7/`, preserved the atlas as `assets/concept-art/ui/route-map/route-node-icon-atlas-splash-v7.png`, and replaced the existing runtime `assets/runtime/map-icons/icons/{street,rival,boss,basin,nest,market,signal,cache}.webp` files so RouteScene picks up the clearer map emblems without code changes.
- Exported all eight runtime route icons as 384px transparent WebPs under the 32 KB route-icon budget; source QA contact sheet: `tmp/qa/route-map-icons-splash-v7/route-node-icons-splash-v7-runtime-contact.png`.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5648/`, RouteScene state payload confirming `routeSelection`, and screenshot inspection at `.artifacts/route-icons-splash-v7-client/shot-5.png`.

- Route path dot color pass: shifted dotted route edges to a more dockside palette with lantern amber selected previews, sea-glass teal available routes, muted rope green chosen routes, and harbor-slate inactive/secondary trails, plus a stronger ink under-dot for contrast against the map backing.
- Verification: `npm run validate:runtime`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5649/`, RouteScene state payload confirming `routeSelection`, and screenshot inspection at `.artifacts/route-dock-dot-colors-client/shot-5.png`.

- Route path highlight darkening pass: reduced selected, preview, available, and secondary route-dot brightness to deeper rust, teal, green, and slate values so highlights contrast the full-color map backing without clustering into bright lanes.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on built preview `http://127.0.0.1:5652/`, and screenshot inspection at `.artifacts/route-darker-highlight-client/shot-5.png`.

- Route path light-fill follow-up: brightened only the interior route-dot fills one step while preserving the darker ink backing circles, giving the highlighted paths clearer readability without returning to the earlier overly bright clustered look.
- Verification: `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on built preview `http://127.0.0.1:5653/`, and screenshot inspection at `.artifacts/route-light-fill-highlight-client-route/shot-11.png`.

- Route-event imagegen source-reference pass: generated imagegen-directed scene/personality briefs for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager, then persisted a project-local source-reference package with scene plates, chroma resident sources, alpha resident review files, and optimized runtime WebPs. The built-in imagegen outputs were not exposed as filesystem files in this environment, so the committed assets are the art-bible-aligned source-reference pass derived from the same specs rather than a final animation-ready package.
- Wired the new v2/v3 route-event scene and resident assets into RouteScene while keeping stable texture keys, with Rival Wager also staged for route-choice conversion. Contact sheets: `.artifacts/route-event-scenes-imagegen-pass-contact.jpg`, `.artifacts/route-event-residents-imagegen-pass-contact.jpg`, and built-preview render sheet `.artifacts/route-event-render-contact.jpg`.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5647/`, direct built-preview probes for Basin/Cache/Signal/Nest/Rival, and visual inspection of the source and render contact sheets.

- Rival Wager route-choice integration: wired Caldra Pinion's generated scene/personality assets into actual play instead of leaving them staged. Rival nodes now open a pre-battle set piece with `Take Caldra's wager` and `Do not get involved` choices; taking the wager starts the existing rival BattleScene reward path, while declining marks the node complete without combat.
- Flattened the generated scene plates to opaque runtime WebPs so non-market set pieces no longer show the route map beneath their landmark art; resident and centerpiece assets remain transparent overlays.
- Verification: `npm run build`, `npm run validate:runtime`, `node tools/validate-runtime-asset-sizes.mjs`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5650/`, and Rival set-piece screenshot inspection at `.artifacts/rival-wager-wired-preview-final.png`.

- Scrap icon integration pass: added a reusable transparent high-resolution pixel-art Scrap source PNG and runtime WebP, then wired the Scrap texture into RouteScene and BattleScene resource UI. The icon now appears in the route Scrap rail, route reward badges, market Scrap badge, market price/service tags, route-event resource tags, and combat Scrap chip while keeping text labels for readability.
- Verification: `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and direct Playwright screenshots inspected at `.artifacts/test-results/scrap-icon-ui/route-scrap-icon.png`, `.artifacts/test-results/scrap-icon-ui/market-scrap-icon.png`, and `.artifacts/test-results/scrap-icon-ui/combat-scrap-icon.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon scale follow-up: increased the Scrap icon display sizes across route rail, reward badges, market tags, route-event resource tags, and combat HUD, with small text offsets to keep labels and numbers clear.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and screenshot inspection at `.artifacts/test-results/scrap-icon-larger-ui/route-scrap-icon-larger.png`, `.artifacts/test-results/scrap-icon-larger-ui/market-scrap-icon-larger.png`, and `.artifacts/test-results/scrap-icon-larger-ui/combat-scrap-icon-larger.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon simplification pass: generated a clearer imagegen direction for Scrap around one dominant brass washer/token silhouette with only a few supporting metal and copper-wire details, then kept the already-wired runtime source focused on that simpler high-contrast read instead of the earlier busy scrap pile. The built-in imagegen result rendered in-thread but was not exposed as a usable filesystem export in this environment.
- Verification: `npm run validate:runtime-assets`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5651/`, and screenshot inspection at `.artifacts/test-results/scrap-icon-simple-ui/route-scrap-icon-simple.png`, `.artifacts/test-results/scrap-icon-simple-ui/market-scrap-icon-simple.png`, and `.artifacts/test-results/scrap-icon-simple-ui/combat-scrap-icon-simple.png`. Only WebGL `ReadPixels` screenshot performance warnings appeared.

- Scrap icon high-detail reference pass: used the supplied washer/shard/copper-wire Scrap icon as the reference silhouette, generated a higher-detail imagegen edit direction, and replaced the project Scrap art with a transparent 4096px source plus a 1024px runtime WebP that keeps the same composition while adding richer brass bevels, pitting, shard facets, cyan rim light, magenta chips, and copper-wire highlights. Preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference.png`.
- Verification: visual inspection of `assets/concept-art/supplies/sources/scrap-icon-source-v4.png` and `assets/runtime/supplies/icons/scrap.webp`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with screenshot `.artifacts/test-results/scrap-icon-detailed-client/shot-0.png`.

- Scrap icon green-screen cleanup pass: used `C:/Users/Will/Desktop/scrap.png` as the final Scrap source, removed the flat green background with the imagegen chroma-key helper, centered the transparent cutout on a larger square canvas, and exported a 4096px source plus a larger 1536px runtime WebP under the existing Scrap texture path. Preserved the supplied green-screen file at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v2.png`.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-cleanup-client/shot-1.png`.

- Actual route-event imagegen asset replacement: replaced the earlier playable source-reference route-event scene/resident pass with the supplied Desktop image-generated PNG exports for Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager. The full scene sources are preserved under `assets/concept-art/route-events/**`, resident chroma sources and transparent alpha review files are preserved beside them, and the runtime `assets/runtime/route-events/*-v2/v3.webp` files now come from those supplied images.
- Contact sheets for the actual supplied imagegen assets: `.artifacts/route-event-scenes-actual-imagegen-contact.jpg` and `.artifacts/route-event-residents-actual-imagegen-contact.jpg`.

- Route-event composition simplification: removed the separate in-scene set-piece item overlays from Lantern Roost, Rooftop Cache, Signal Landmark, Featherwright Studio, and Rival Wager so each visit is now composed from the full background scene plate plus the personality cutout only. Resident art now fits inside its placement bounds while preserving the generated image's natural aspect ratio instead of stretching every bird into the same rectangle.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event and route-choice tests, required `develop-web-game` client on built preview `http://127.0.0.1:5655/`, and visual inspection of `.artifacts/route-events-bg-personality-only/route-events-bg-personality-only-contact.jpg`.

- Marn Valeclip art replacement: updated Rooftop Cache's resident art from the supplied `C:\Users\Will\Desktop\2-2.png`, preserving the chroma source, regenerating the transparent alpha review PNG, and replacing `assets/runtime/route-events/marn-valeclip-v2.webp`. The cleanup pass tightened the green-screen matte and despill while preserving the cyan rim-light and natural tall character ratio.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5656/`, alpha preview `.artifacts/marn-valeclip-v2-cleanup-preview.png`, and in-game Rooftop Cache screenshot `.artifacts/marn-valeclip-v2-update/rooftop-cache-marn-v2-update.png`.

- Oren Shearbright art replacement and layout polish: replaced Featherwright Studio's resident source with the supplied cleaned `C:\Users\Will\Desktop\4-2.png`, regenerated the chroma source, transparent alpha review PNG, and `assets/runtime/route-events/oren-shearbright-v2.webp`, then gave Nest events a larger resident frame while preserving Oren's natural tall ratio. Sella Warmwick's Lantern Roost resident placement also shifted right to sit better in the shelter composition.
- Verification: `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5657/`, alpha preview `.artifacts/oren-shearbright-v3-cleanup-preview.png`, and in-game screenshots `.artifacts/route-event-qa/oren-shearbright-cleaned-larger.png` plus `.artifacts/route-event-qa/sella-warmwick-shifted-right.png`.

- Sella Warmwick mirror pass: horizontally mirrored the Lantern Roost v2 resident source set (`sella-warmwick-v2-chroma-source.png`, `sella-warmwick-v2-alpha-source.png`) and regenerated `assets/runtime/route-events/sella-warmwick-v2.webp` so Sella faces right in the Basin set piece without changing her placement or natural aspect ratio.
- Verification: visual matte preview `.artifacts/sella-warmwick-v2-mirrored-preview.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5658/`, and in-game screenshot `.artifacts/route-event-qa/sella-warmwick-mirrored.png`.

- Oren Shearbright mirror pass: horizontally mirrored the Featherwright Studio v2 resident source set (`oren-shearbright-v2-chroma-source.png`, `oren-shearbright-v2-alpha-source.png`) and regenerated `assets/runtime/route-events/oren-shearbright-v2.webp` so Oren faces right into the Nest set piece while keeping the larger natural-ratio placement.
- Verification: visual matte preview `.artifacts/oren-shearbright-v2-mirrored-preview.png`, `node tools/validate-runtime-asset-sizes.mjs`, `npm run validate:runtime`, `npm run build`, focused Playwright route-event test, required `develop-web-game` client on built preview `http://127.0.0.1:5659/`, and in-game screenshot `.artifacts/route-event-qa/oren-shearbright-mirrored.png`.

- New user prompt: review the game's balance because it feels too easy and card spam makes decisions less meaningful.
- First combat-balance pass: lowered base hand target from 5 to 4, added an overextension pressure valve where playing 4+ cards before Roost puts the flock into Open Sky for the enemy phase, and reduced automatic post-combat recovery from +3 to +1 Cohesion. Intent: preserve card identities while making the fourth card and route sustain choices matter more.
- Added smoke coverage for the overextension breakpoint and updated alpha/core design docs to document the new hand target, anti-spam rule, and lighter passive recovery.

- Scrap icon gear readability pass: replaced the busier washer-pile runtime with the supplied `C:/Users/Will/Desktop/scrap-2.png` gear silhouette, removed the green screen with the imagegen chroma-key helper, exported a transparent 4096px source and 768px runtime WebP, and preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v3.png`. The gear ring reads more clearly in the 30-34px Scrap HUD slots while retaining the metal plate and copper-wire scrap cues.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, simulated 32px preview `.artifacts/test-results/scrap-icon-gear-readable/scrap-gear-32x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-gear-client/shot-1.png`.

- Scrap icon solid-token replacement: replaced the gear/scrap-pile concept with a single chunky brass salvage token so Scrap reads as a more solid currency mark at HUD scale. Exported a transparent 4096px source at `assets/concept-art/supplies/sources/scrap-icon-source-v8-solid-token.png`, refreshed the stable source path, and replaced the 768px runtime WebP under the existing `resource-scrap` texture path so all already-wired UI locations pick it up without code changes.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, small-size preview `.artifacts/test-results/scrap-solid-token/scrap-solid-token-34x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-solid-token-client/shot-1.png`.

- Scrap icon nut-token reference replacement: used the supplied green-screen nut/gear Scrap icon from `C:/Users/Will/AppData/Local/Temp/codex-clipboard-cafd78d5-a8a8-4a7f-ad9b-e1528493668a.png`, removed the green background with the imagegen chroma-key helper, and replaced the runtime Scrap texture with the cleaned transparent version. Preserved the supplied reference at `assets/concept-art/supplies/sources/scrap-icon-user-reference-v4.png` and the transparent high-resolution source at `assets/concept-art/supplies/sources/scrap-icon-source-v9-nut-token.png`.
- Verification: visual inspection of `assets/runtime/supplies/icons/scrap.webp`, small-size preview `.artifacts/test-results/scrap-nut-reference/scrap-nut-34x.png`, `npm run validate:runtime-assets`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-nut-reference-client/shot-1.png`.

- Scrap icon label-free UI pass: removed redundant all-caps `SCRAP` captions from compact Scrap icon/count treatments in the route rail, market Scrap badge, route-event resource tags, market service price labels, and combat resource chip while preserving full Scrap wording in explanatory sentences and tooltips.
- Verification: `npm run build`, `rg -n SCRAP src/main.ts` confirms only constants/internal references remain, and required `develop-web-game` client on `http://127.0.0.1:5651/` with route HUD screenshot `.artifacts/test-results/scrap-icon-label-free-client/shot-1.png`.

- HUD standardization first pass: audited the current run HUD surfaces and used the combat resource chip as the baseline pattern. Added a shared metric-chip renderer, moved the combat top resource chips onto it, split the route map rail into matching Cohesion/Scrap/Deck/Waymarks/Supplies chips, and replaced bespoke Scrap/Cohesion tags in route choices, market, Lantern Roost, and route set-piece overlays.
- Verification: `npm run build`, built preview on `http://127.0.0.1:5173/`, required `develop-web-game` route screenshot `.artifacts/test-results/hud-consolidation-route-preview-fixed/shot-1.png`, and combat screenshot `.artifacts/test-results/hud-consolidation-combat-preview/shot-0.png`.

- HUD combat-source standardization pass: pivoted the shared run HUD to use the combat top bar as the canonical layout. Route, market, and route-event surfaces now preserve the combat-shaped Cohesion/status/Flow/Cover/suit/resource frame through the run, while combat gained Deck and Supplies in the same resource lane so the map-only information is no longer missing in fights.
- Verification: `npm run build`, required steady-state Playwright captures on `http://127.0.0.1:5173/`, route screenshot `.artifacts/test-results/combat-source-hud-steady-final/route.png`, and combat screenshot `.artifacts/test-results/combat-source-hud-steady-final/combat.png`.

- Route map declutter pass: removed permanent instructional copy from the map footer, removed the route log line from the route map, suppressed route-only HUD title/subtitle/status labels, and removed the inspector's `Route Board` eyebrow so the map relies more on icons, selection, and node hover tooltips.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, final route screenshot `.artifacts/test-results/route-map-declutter-client-final/shot-0.png`.

- Route map icon-first follow-up: shortened the shared flock HP bar from 300px to 240px, changed its visible text from `Cohesion current/max` to just `current/max`, removed the route map legend render, and deleted the now-unused legend renderer so the map relies on node icons, risk pips, selection, and hover tooltips.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-bar-map-no-legend-route/shot-0.png`, required combat capture `.artifacts/test-results/hud-bar-map-no-legend-combat/shot-0.png`, plus steady combat screenshot `.artifacts/test-results/hud-bar-map-no-legend-combat-steady/combat.png`.

- Route map HUD-control consolidation: removed the standalone Back/Flock/Deck route buttons, made the shared HUD HP bar open the Flock overlay, and made the Deck HUD chip open the deck overlay so map controls live in the unified run HUD instead of a second button row.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/map-hud-controls-route/shot-0.png`, deck-chip interaction capture `.artifacts/test-results/map-hud-controls-deck/shot-0.png` with `deckOverlayOpen: true`, and HP-bar interaction capture `.artifacts/test-results/map-hud-controls-flock/shot-0.png` with `flockOverlayOpen: true`.

- HUD declutter follow-up: removed the permanent Plumes/Quills/Basins/Nests suit-count row from the shared run HUD and stopped feeding route-progress numbers into the top-right HUD slot, leaving the top bar focused on Cohesion, Cover, Resonance, Scrap, Deck, Waymarks, and Supplies.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-no-suits-route/shot-0.png`, and required combat capture `.artifacts/test-results/hud-no-suits-combat/shot-0.png`.

- HUD icon/pip cleanup: shifted the flock Cohesion bar left, removed the decorative Flow/status pips from the shared HUD, removed the generic colored dot pips from non-Scrap HUD chips, and replaced the bordered `Cover` text chip with an icon-only shield marker that keeps the numeric cover value.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/hud-cover-icon-route/shot-0.png`, and required combat capture `.artifacts/test-results/hud-cover-icon-combat/shot-0.png`.

- Route map inspector removal: removed the right-hand selected-node info panel, widened the route map and graph into that space, and kept `Take this route` as a compact floating control on the map.
- Verification: `npm run build`, restarted Vite on `http://127.0.0.1:5173/`, required `develop-web-game` route capture `.artifacts/test-results/map-expanded-no-inspector-fresh/shot-0.png`, and required route-confirm flow `.artifacts/test-results/map-expanded-no-inspector-fresh-combat-flow/state-0.json` reaching `mode: battle`.

- Route map vertical stretch: expanded the map frame upward/downward, stretched the route graph vertically, and moved the `Take this route` button down into the lower-right of the larger map frame.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-taller-button-lower/shot-0.png`, and required route-confirm flow `.artifacts/test-results/route-map-taller-button-lower-flow/state-0.json` reaching `mode: battle`.

- Route map extra-column polish: increased procedural route generation from 6-7 middle columns to 7-8 middle columns for every district and added deterministic, clamped middle-node position drift so route icons feel hand-placed instead of grid-aligned while keeping entry/boss anchors stable.
- Verification: `npm run validate:runtime`, `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-extra-column-offset-final/shot-0.png` showing 10 generated columns for the alpha seed, and required route-confirm flow `.artifacts/test-results/route-map-extra-column-offset-flow/state-0.json` reaching `mode: battle`.

- New user prompt: review sluggishness/load slowness and continue investigating optimization issues beyond the runtime-optimizer checklist.
- Runtime/load pass: stopped `codex-data` from importing at app module evaluation time; Codex extended data now loads only when `CodexScene` needs it.
- Card-art load pass: added thumbnail runtime asset records and switched routine card surfaces (Codex grid, route/market cards, battle hand cards, reward choices, and battle preload queues) to thumbnail WebPs while preserving full portraits for large detail/preview panels.
- Runtime sluggishness follow-up: added recursive tween cleanup before Codex, Route, and Battle redraws destroy their UI trees, preventing repeating idle/breathing/atmosphere tweens from accumulating across `renderAll()` rebuilds.
- Bundle validation update: app entry remains above the old 590 KB target, so `validate-bundle-size` now warns above 590 KB but hard-fails above a temporary 675 KB ceiling. `docs/project/performance-notes.md` records the measured chunks and the remaining need to split `src/main.ts` into lazy scene/system modules.
- Verification: `npm run build`, `npm run validate:bundle-size`, `npm test`, `npm run validate:docs`, required `develop-web-game` client on built preview `http://127.0.0.1:4175/`, and direct route-to-combat Playwright smoke on `http://127.0.0.1:4176/` all completed. Screenshots inspected: `output/web-game/shot-1.png` and `.artifacts/test-results/perf-after-confirm-smoke.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared; no failed requests in the direct smoke.

- Route map icon-pip removal: removed the small colored risk pips attached to combat route icons, leaving risk available through hover tooltips and keeping the map icon field cleaner.
- Verification: `npm run build`, required `develop-web-game` route capture `.artifacts/test-results/route-map-no-icon-pips/shot-0.png`, and required route-confirm flow `.artifacts/test-results/route-map-no-icon-pips-flow/state-0.json` reaching `mode: battle`.

- Controlled route pacing pass: replaced purely shuffled procedural node assignment with authored route-generation beats plus a deterministic path repair pass. Each generated district now has an opening choice, early recovery/build access, pressure beats with non-combat alternatives, boss-prep safety, max-consecutive-combat caps, early safety requirements, build/economy requirements, rival-alternative repair, and district-specific pressure-score caps.
- Added smoke coverage for generated route pacing across 16 seeds per district, checking all entry-to-boss paths for no 3-combat chains, early safety, build/economy access, pressure cap compliance, and no forced Rival-only exits.
- Verification: `npm run validate:runtime`, `npm run build`, `npx playwright test tests/smoke.spec.ts -g "controlled pacing"`, and required `develop-web-game` client on `http://127.0.0.1:5173/`. Visual screenshot inspected at `output/web-game/shot-0.png`; state JSON confirmed a controlled route sequence.

- Combat flock-label cleanup: removed the permanent Flock Leader name from the combat leader art label and removed the leader/map combined title from the battle intro, leaving district context only.
- Verification: `npm run build` passed; direct Playwright combat text scan on `http://127.0.0.1:5173/` confirmed no visible leader panel or intro leader-name text remained. Screenshot inspected at `.artifacts/test-results/combat-leader-name-removed.png`.

- New user prompt: every card click in combat feels like it freezes the game.
- Combat click responsiveness fix: profiled BattleScene card clicks and found target-card selection was doing a full `renderAll()` rebuild, with one select taking roughly 170 ms. Target-card selection now updates the existing hand-card border in place instead of rebuilding the whole combat board. Card pointer handlers now stop propagation, and recursive tween cleanup now only traverses real Phaser Containers.
- Verification: direct Playwright combat click smoke on `http://127.0.0.1:4177/` confirmed target selection no longer calls `renderAll()` and dropped to ~0.1 ms; a real support-card play still updates hand/piles/log correctly. `npm run build`, `npm run validate`, and the required `develop-web-game` client completed. Screenshots inspected: `.artifacts/test-results/card-click-after-selection-optimized.png` and `output/web-game/shot-0.png`.

- UI icon crop correction: regenerated the current 21 runtime UI icon PNGs from alpha-component bounds instead of fixed sheet cells, grouping detached sparkle/fragment components with `preen-kit` and `release-card` so icons no longer clip or include neighboring artwork.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, direct Playwright route-to-battle smoke, and transparent-margin validation across `assets/runtime/ui/icons/*.png` all passed. Screenshots inspected: `output/icon-segmentation/corrected-icons-contact.png`, `output/icon-segmentation/manual-playwright/01-after-start-click.png`, and `output/icon-segmentation/deeper-playwright/02-after-take-route.png`.

- UI icon size bump: increased the global in-game UI icon preview scale from `2` to `2.2` and moved the unified HUD context label lower inside the top rail so the enlarged Supplies/Waymarks/HUD icons do not visually collide with route text.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route-to-battle smoke completed without page errors. Screenshots inspected: `output/icon-size-bump/final-playwright/01-route.png` and `output/icon-size-bump/final-playwright/02-battle.png`.

- Flock Stats scrap removal: removed Scrap from the route Flock Stats overlay summary strip so the surface only presents Flock-related stats/context (`Cohesion` and `Waymarks` above the passive stat table). Scrap remains in the run HUD and economy surfaces.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route overlay capture completed without page errors. Screenshot inspected: `output/flock-stats-no-scrap/final-route-overlay/route-flock-stats.png`.

- Completed route node pip removal: removed the small top-right completion pip from route map nodes that are already complete. Completed nodes still read as complete through the existing dimmed icon state.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright completed-node route capture completed without page errors. Screenshot inspected: `output/completed-map-no-pip/final-playwright/completed-route-map.png`.

- Route Flock Stats resource cleanup: removed the remaining Cohesion and Waymarks route-resource readouts from the route Flock Stats overlay, and filtered the route overlay's Cohesion row so the page now focuses on passive flock/build stats only.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route Flock Stats capture completed without page errors. Screenshot inspected: `output/flock-stats-no-resource-summary/final-playwright/route-flock-stats-final.png`.

- Boss size bump: increased route-map boss node/icon sizing and made combat bosses render larger in both solo and helper-supported boss fights while preserving separate helper placement.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route boss/combat boss captures completed without page errors. Screenshots inspected: `output/boss-size-bump/final-playwright/route-boss-node.png` and `output/boss-size-bump/final-playwright/boss-combat.png`.

- Codex card thumbnail load fix: corrected the Codex card grid renderer to use the same compact/thumbnail art key that the Codex grid queues for visible discovered cards, so card images appear without needing to open the detail modal first.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and clean Playwright Codex capture with seeded discovered cards completed without card-detail clicks. Screenshot inspected: `output/codex-card-thumb-load/final-playwright/codex-cards-clean-before-detail-click.png`.

- HUD chip width trim: shortened the shared top-HUD Resonance and Waymarks metric chip frames so their borders better match their compact icon/value contents without disturbing Scrap, Deck, or Supplies spacing.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route/combat HUD captures completed without page errors. Screenshots inspected: `output/hud-chip-width-trim/final-playwright/route-hud.png` and `output/hud-chip-width-trim/final-playwright/combat-hud.png`.

- Flock HP bar compact pass: shortened the shared top-HUD Flock/Cohesion bar from 300px to 190px while preserving the left-edge heart anchor, returning top-rail space before the cover shield and resource chips.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright route/combat HUD captures completed without page errors. Screenshots inspected: `output/flock-hp-bar-shorter/final-playwright/route-hud.png` and `output/flock-hp-bar-shorter/final-playwright/combat-hud.png`.

- Combat HUD level-name removal: removed the far-right level/encounter context label from the unified top HUD so combat no longer repeats the map/encounter name in the HUD rail.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat HUD capture completed without page errors. Screenshot inspected: `output/combat-hud-no-level-name/final-playwright/combat-hud.png`.

- Combat pile/Waymarks declutter: removed visible Draw/Discard pile labels from combat pile stacks, stopped rendering the left-side Waymarks shelf/icons during combat, and made the top-HUD Waymarks chip open the Found Waymarks drawer.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat captures completed without page errors. Screenshots inspected: `output/combat-waymarks-hud-menu/final-playwright-cachebust/combat-cleaned-hud.png` and `output/combat-waymarks-hud-menu/final-playwright-cachebust/combat-waymarks-menu.png`.

- Combat left-side Waymark text cleanup: stopped rendering the Waymark trigger feedback strip in combat so no Waymark-related text appears on the left side; Waymark state remains available through the top-HUD Waymarks menu.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat capture with seeded Waymark feedback completed without page errors. Screenshot inspected: `output/combat-no-left-waymark-text/final-playwright/combat-no-left-waymark-text.png`.

- Combat pile rectangle removal: removed the visible rectangular card-stack frames and diamond backplate from the draw/deck and discard pile controls, leaving icon art plus count badges with an invisible click target for pile inspection.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright combat captures completed without page errors; clicking the icon-only draw pile still opened Deck Review. Screenshots inspected: `output/combat-pile-no-rectangles/final-playwright/combat-piles-icon-only.png` and `output/combat-pile-no-rectangles/final-playwright/combat-draw-pile-overlay.png`.

- Dynamic card-effect role pass: extended the active card contract so the current role is inferred from the effects actually being cast, including Molt effects, instead of always presenting the card's base role. Combat hand/detail text state now reports both `role` and `activeRole`, and the combat deck/detail panel uses the active role next to the active target.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright Molt inspection confirmed `Open Basin` changes from base `skill/self/heal` to active `attack/enemy/damage` while in Molt. Screenshot inspected: `output/dynamic-molt-card-contract/final-playwright/molt-active-card-detail.png`.

- Boss Waymark reward pool pass: Waymark rewards now always offer 3 choices when awarded. Boss combat rewards draw from a high-tier pool that mixes boss-source Waymarks with rare/boss Waymarks, anchors at least one boss-source option when available, and falls back to the broader unowned pool only if ownership would shrink the offer. The reward payload now includes Waymark rarity, and post-combat rewards clear transient combat FX so old intro slashes do not overlay reward cards.
- Verification: `npm run build`, `npx playwright test tests/smoke.spec.ts -g "boss Waymark reward choices mix"`, required `develop-web-game` client on built preview `http://127.0.0.1:4173/`, and direct Playwright boss reward capture completed without page errors. Screenshot inspected: `output/boss-waymark-pool/final-playwright-clean/boss-waymark-reward-mixed-pool-clean.png`.

- Card reward rarity rate increase: raised the shared card rarity weights from common/uncommon/rare/legendary `100/45/16/5` to `80/75/32/10`, affecting post-combat card rewards and the market's weighted card slot. In a 100k simulated 3-card offer sample, non-common appearance rose from ~78.7% to ~93.4%, rare-or-legendary appearance rose from ~24.1% to ~38.5%, and offered-card share moved from about `60/31/5/3` to `41/44/9/6`.
- Verification: `npm run validate`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:4178/`, and screenshot inspection of `output/web-game/shot-0.png`.

- Multi-enemy encounter rate/strength increase: procedural combat payload selection now weights 2-enemy encounters at `8x` and 3+ enemy encounters at `12x` while preserving the existing no-repeat-until-exhausted variety behavior. Non-boss multi-enemy encounters now receive `+12%` max HP per enemy and `+1` damage bonus, matching the existing solo-tuning path without escalating boss-helper fights.
- Verification: `npx playwright test tests/smoke.spec.ts -g "encounter composition supports solo tuning and support companions|generated combat payloads favor multi-enemy encounters"`, `npm run validate`, and `npm run build` passed. Required `develop-web-game` client ran on `http://127.0.0.1:5187/`; route screenshot inspected at `.artifacts/test-results/multi-enemy-client/shot-0.png`. A follow-up combat-client coordinate burst did not activate the route confirm button in the generic client, but the focused Playwright smoke entered combat and verified multi-enemy HP/damage state.

- Combat enemy size bump: increased combat enemy stage scales across solo, multi-enemy, solo boss, and boss-with-helper layouts so enemy art reads larger in battle while preserving the shared HP/intent/FX placement model.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5188/`, and direct Playwright battle captures passed without browser errors. Screenshots inspected: `.artifacts/test-results/enemy-size-direct/single-enemy.png` and `.artifacts/test-results/enemy-size-direct/multi-enemy.png`.

- Sella Warmwick/Lantern Roost plaque cleanup: removed the lower-left "HEATED BRASS ROOST HEARTH" info plaque from `renderLanternRoostOverlay` so the event art is less cluttered while keeping the resident title and choices.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Sella route-event text scan/screenshot at `.artifacts/test-results/sella-warmwick-no-hearth-plaque.png`. Existing route-event smoke has a separate fixed-seed rival assumption issue after route pacing changes.

- Marn Valeclip/Rooftop Cache plaque cleanup: skipped the lower-left set-piece descriptor plaque for cache events so "SEALED PULLEY CACHE CABINET" no longer appears while Marn's title and reward choices remain.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Marn route-event text scan/screenshot at `.artifacts/test-results/marn-valeclip-no-cache-cabinet-plaque.png`.

- Ivo Tallymast/Signal plaque cleanup: skipped the lower-left set-piece descriptor plaque for signal events so "ILLUMINATED SWITCHBOARD ROUTE MAP" no longer appears while Ivo's title and signal choices remain.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright Ivo route-event text scan/screenshot at `.artifacts/test-results/ivo-tallymast-no-switchboard-plaque.png`.

- Route HUD Waymarks menu: wired the route-map shared HUD Waymarks chip to a route-side Found Waymarks drawer, including close/Esc behavior, owned Waymark art loading, readable family/rarity/effect rows, and text-state reporting via `waymarkDrawerOpen`.
- Verification: `npm run build`, `npx playwright test tests/smoke.spec.ts -g "route HUD Waymarks chip opens"`, required `develop-web-game` client on built preview `http://127.0.0.1:5189/`, and screenshots inspected at `.artifacts/test-results/route-hud-waymarks-drawer.png` and `.artifacts/test-results/route-waymarks-client/shot-0.png`.

- HUD cover icon size trim: reduced the shared top-HUD Cover shield icon from `38` to `28` in `drawCoverHudIcon`, reduced the cover count text from `13px` to `12px`, and scaled the fallback vector shield to match so the cover marker no longer dominates the space between Cohesion and the resource chips.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5190/`, route HUD screenshot inspected at `.artifacts/test-results/cover-icon-small-client/shot-0.png`, and direct Playwright combat HUD screenshot with `12` Cover inspected at `.artifacts/test-results/cover-icon-small-direct/combat-cover-hud.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared.

- HUD metric chip uniformity pass: converted Cover from a standalone shield/count marker into the same shared metric-chip component used by Resonance, with icon on the left and label/value text to the right. Removed the special Scrap-only chip branch so Scrap now uses the same label/value formatting as Cover, Resonance, Deck, Waymarks, and Supplies.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5191/`, route HUD screenshot inspected at `.artifacts/test-results/hud-uniform-chips-client/shot-0.png`, and direct Playwright combat HUD screenshot with `12` Cover inspected at `.artifacts/test-results/hud-uniform-chips-direct/combat-hud-uniform-cover.png`. Only headless WebGL `ReadPixels` screenshot warnings appeared.

- Combat enemy grounding pass: combat enemy art now measures each enemy texture's visible alpha bounds and places the visible bottom edge on the combat shadow, with shadow width/height and the invisible hitbox following the corrected visible sprite placement. This replaces the fixed center-offset art placement that could make shadows feel detached from padded enemy PNGs.
- Verification: `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5192/`, direct Playwright combat capture, and post-card-click responsiveness smoke completed without browser/page errors. Screenshots inspected: `.artifacts/test-results/enemy-grounding-client/shot-0.png`, `.artifacts/test-results/enemy-grounding-direct/single-combat-canvas.png`, and `.artifacts/test-results/enemy-grounding-direct/post-card-click-canvas.png`.

- Battle pressure tuning pass: increased generated encounter weighting toward multi-enemy fights (`2-enemy/multi` from `8x` to `12x`, `3+ enemy` from `12x` to `20x`), raised non-boss solo scaling to `135%` HP and `+2` damage, raised non-boss multi scaling to `120%` HP and `+2` damage, and converted enemy self-`gainCover(N)` turns into `+N` next-attack damage wind-ups so those turns create real pressure instead of disposable enemy Cover. Ally/all-enemy Cover support effects still create Cover for target-priority gameplay.
- Verification: `npm run validate:runtime`, `npx playwright test tests/smoke.spec.ts -g "encounter composition supports solo tuning and support companions|generated combat payloads favor multi-enemy encounters"`, `npm run build`, required `develop-web-game` client on built preview `http://127.0.0.1:5193/`, direct forced multi-encounter Playwright capture, and `npm run validate` all passed. Screenshots/state inspected: `.artifacts/test-results/battle-pressure-client/shot-0.png`, `.artifacts/test-results/battle-pressure-client/state-0.json`, `.artifacts/test-results/battle-pressure-direct/multi-encounter-pressure.png`, and `.artifacts/test-results/battle-pressure-direct/state.json`. Bundle validation passed with the existing app-entry warning.

- New user prompt: make the Waymark/Supply meaningfulness updates from the review.
- Added backwards-compatible multi-effect Waymark support across runtime types, route/combat Waymark interpreters, route recovery/economy helpers, validation, Codex/tooltips, and the run economy simulator.
- Tuned weak/narrow Waymarks and Supplies: boss Waymarks now have stronger baseline/payoff effects, cache/market Waymarks have fallback value, duplicate Open Sky/combat-start marks were differentiated, low-value recovery/suit marks were raised, and `Map Sticker Strip`, `Shade Cloth`, and `Oath Ledger` were strengthened.
- Verification: `npm run validate:runtime`, `npm run build`, focused Playwright smoke tests for Waymark economy/combat hooks, boss Waymark rewards, expanded Supplies, build-around Waymarks, and next-item route/combat hooks, plus the required generic `develop-web-game` client on `http://127.0.0.1:5193/` all passed. Generic client screenshot inspected at `output/web-game/shot-1.png` with menu text-state JSON.

- HUD Waymarks overflow scrolling: added row-based mouse-wheel scrolling to the route and combat Found Waymarks drawers, reset scroll on fresh opens, and added compact scrollbar thumbs when carried Waymarks exceed the visible rows. Route now shows three rows to avoid panel bleed; combat shows four rows.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright overflow captures with all 58 Waymarks carried. Screenshots inspected: `.artifacts/test-results/route-waymark-drawer-overflow-scrolled-fixed.png` and `.artifacts/test-results/combat-waymark-drawer-overflow-scrolled.png`.

- HUD Deck/Waymarks menu alignment: added a shared `HUD_MENU_PANEL` frame/anchor definition and applied it to route Deck, route Waymarks, combat Deck, and combat Waymarks menus so the border size, header icon position, and close button position match across the run. Combat modal menus now clear transient FX toasts before rendering so combat-start feedback cannot overlap the standardized panels.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright route/combat captures. Screenshots inspected: `.artifacts/test-results/hud-menu-align-route-deck.png`, `.artifacts/test-results/hud-menu-align-route-waymarks.png`, `.artifacts/test-results/hud-menu-align-combat-deck.png`, and `.artifacts/test-results/hud-menu-align-combat-waymarks.png`.

- HUD Deck Review detail placement: added a shared `DECK_DETAIL_LAYOUT` for route and combat Deck Review detail panels. The detail frame now starts below the Close control, and the selected card art plus cost pip sit inside the bordered panel instead of spilling past the left edge.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright captures inspected at `.artifacts/test-results/hud-deck-detail-route-fixed.png` and `.artifacts/test-results/hud-deck-detail-combat-fixed.png`.

- HUD Supplies menu: wired the unified HUD Supplies chip on route and combat into a same-size Packed Supplies menu using the shared `HUD_MENU_PANEL` frame, header icon anchor, and Close control used by Deck and Waymarks. Supply tiles show carried Supply art, category/rarity/timing, descriptions, empty slots, and route/combat use availability.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright captures inspected at `.artifacts/test-results/hud-supply-menu-route.png` and `.artifacts/test-results/hud-supply-menu-combat.png`.

- HUD Resonance cluster: moved the shared Resonance chip out of the right-side resource row and into the left status cluster next to Cohesion and Cover. Scrap, Deck, Waymarks, and Supplies now remain grouped as the run inventory/navigation chips.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, and direct Playwright route/combat captures inspected at `.artifacts/test-results/hud-resonance-cluster-route.png` and `.artifacts/test-results/hud-resonance-cluster-combat.png`.

- Combat end-turn declutter: removed the bottom-right safe-play/Open Sky helper line and replaced the red `Roost / End Turn` command box with an icon-only end-turn control above the discard pile. The control keeps a hover tooltip and the hidden hit area still advances combat turns.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright combat capture inspected at `.artifacts/test-results/combat-roost-cleanup-before-click.png`, and icon-only end-turn click verified by state advancing from turn 1 to turn 2.

- Roost icon hover stability: removed the size-changing hover state from the icon-only combat end-turn control so the Roost icon keeps a fixed `74x74` display size at rest and on hover. Hover feedback is now alpha-only plus the existing tooltip.
- Verification: `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright combat captures inspected at `.artifacts/test-results/roost-fixed-size-rest.png` and `.artifacts/test-results/roost-fixed-size-hover.png`, and icon click verified by state advancing from turn 1 to turn 2.

- Waymark/Supply/card meaningfulness follow-up: made `returnDiscard(nonMolt, +1)` actually return a non-Molt card and draw 1, fixed Canal Shelter's Molt suit gate from stale `cups` to live `basins`, strengthened underperforming Waymarks/Supplies, and gave low-impact recursion/discard/Cover cards extra payoff so their effects are visible in play.
- Also resolved the existing RouteScene/BattleScene supply drawer compile blockers by keeping the shared HUD Supplies chip connected to usable route/combat supply drawers and text-state flags.
- Verification: `npm run validate:runtime`, `npm run build`, focused smoke tests for Waymark economy/combat hooks, recursion/Basins gate, expanded Supplies, build-around Waymarks, next-item route/combat hooks, and new balance verbs all passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5174/`; screenshot/state inspected at `output/web-game/shot-1.png` and `output/web-game/state-1.json`.

- New user prompt: implement the comprehensive balance plan to make gameplay more challenging and reduce card-spam dominance.
- Added combat decision telemetry to run summaries and `render_game_to_text`: cards played, overextensions, low-card Roosts, held cards, unspent Wingbeat, active Waymarks, and boss-entry Cohesion/Scrap.
- Added restraint Waymark triggers (`onRoostWithWingbeat`, `onRoostWithCardsInHand`, `onLowCardTurn(N)`, `onTurnEndNoOverextension`) and converted several old Nth-card spam payoffs into clean-Roost/low-card rewards.
- Added a 6 non-boss Waymark active cap with boss Waymarks exempt; route/combat rewards and the economy simulator now enforce the same cap.
- Tuned late economy down for Maps 3-4, nudged generated Map 4 basin access upward, reduced Spark-Caller’s free opening hand inflation, softened Talon’s opening Flow, and made Roostkeeper’s Perfect Brace counterstrike.
- Updated playtest stats to report the new decision metrics and added focused smoke coverage for restraint triggers and Waymark cap behavior.
- Verification: `npm run validate:runtime`, `npm test`, `npm run audit:balance`, `npm run audit:economy`, `node tools/simulate-run-economy.mjs --json --seeds=1000`, `npm run build`, and focused Playwright smoke tests passed. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5194/`; screenshot inspected at `output/web-game/shot-1.png`. Direct battle screenshot inspected at `.artifacts/test-results/balance-overextension-risk-hint.png`.
- Follow-up: resolved the bundle-size gate by switching production minification to Terser and collapsing repeated UI style literals behind shared constants. Full `npm run validate` now passes; app entry is 674.4 KB against the 675 KB hard cap, with the existing 590 KB target warning still reported.
- Remaining test-harness work: a full `npx playwright test tests/smoke.spec.ts` pass currently has broader async scene-start/fx-layer failures in older tests that directly call `g.scene.start(...)` and scene internals before the next Phaser tick. Focused balance/HUD smoke coverage passes; the broad harness should be updated to await scene readiness before direct assertions.

- Selected UI/UX implementation pass: converted route/combat Waymark and Supply drawers to a shared compact item-tile renderer, replaced long/truncated drawer and Codex item copy with player-facing effect summaries, added missing plain-language effect formatting for Supply/Waymark verbs, removed the redundant combat left-side Supply shelf, separated resource and inventory HUD groups with a divider, kept Waymarks in the familiar HUD click zone, and added lightweight Market section rails plus a small Scrap label in market item details.
- Verification: `npm run build`, `npm test`, and focused Playwright smoke tests for route HUD Waymarks, market item detail, and Codex Supplies passed. Required `develop-web-game` local client ran on `http://127.0.0.1:5174/`; screenshots inspected at `.artifacts/ui-selected/route-waymark-drawer.png`, `.artifacts/ui-selected/market-hierarchy.png`, `.artifacts/ui-selected/combat-supply-drawer.png`, and `.artifacts/ui-selected/codex-items.png`.

- Selected UI/UX follow-up pass: polished card detail surfaces so combat, reward, and preen previews show the active card contract, including Molt-active target/role/effect text and the alternate normal/preened line. Reward/preen choice cards now use compact effect copy plus a Molt marker, Codex Supply/Waymark detail modals include player-facing effect summaries above the raw rules, keyword/stat hover tooltips are scene-safe after combat, and card detail art falls back to already-loaded compact art instead of showing blank panels.
- Entry-size cleanup: removed unused legacy route supply/status rail renderers and old combat supply/Waymark shelf renderers superseded by the HUD menu/drawer flow. Bundle validation still passes with the app entry warning: `index-Dd7IwM0_.js` is 672.7 KB minified / 165.6 KB gzip, so a true scene/module split remains the next performance step.
- Verification: `npx tsc --noEmit`, focused Playwright smoke tests for card choice hovers, Codex Supplies, Molt active card contracts, and preened Molt/Codex notes, `npm run build`, `npm test`, and `npm run validate:bundle-size` passed. Required `develop-web-game` client ran on `http://127.0.0.1:5174/`. Screenshots inspected at `.artifacts/ui-selected-next/combat-molt-card-detail.png`, `.artifacts/ui-selected-next/reward-choice-card-hover.png`, `.artifacts/ui-selected-next/preen-choice-card-hover.png`, `.artifacts/ui-selected-next/codex-supply-detail.png`, and `.artifacts/ui-selected-next/codex-waymark-detail.png`.

- Broad smoke harness closure: replaced direct same-scene restarts with `window.__birdSquadStartScene`, which stops an active scene before restart and waits for seeded RouteScene readiness / BattleScene FX readiness. Updated stale route/Codex expectations to assert current HUD text-state contracts, generated-route availability, lazy-loaded Codex notes, and current combat damage tuning.
- Verification: focused route/Codex smoke tests passed, then full `npx playwright test tests/smoke.spec.ts` passed with 73/73 tests. `npm run validate`, `npm test`, `npm run build`, `npm run audit:balance`, and `npm run audit:economy` passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5273/`; screenshot/state inspected at `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- Remaining note: no blocking work items are left from the balance implementation. The app entry still exceeds the softer 590 KB bundle target while passing the 675 KB hard cap, so a future scene/module split is still the clean performance follow-up.

- Balance challenge follow-up: reduced unconditional zero-cost draw/refund loops on key Plumes, Quills, Basins, Lantern Roost, Eclipse Watch, and Fledgling cards; added post-Roost card-volume telemetry so enemies can punish 4+ card turns, repeated zero-cost plays, and empty-hand Roosts; converted more Waymarks from combat-start or Nth-card rewards into clean-Roost/held-card/unspent-Wingbeat timing rewards; added late Basin/Nest/Market Scrap sinks for boss prep and route scouting; and added non-boss overextension repair costs to post-combat Scrap rewards.
- Enemy and economy tuning: Map 2-4 enemies now include targeted anti-spam conditional effects, late markets sell Boss Rigging and Scout Roofline services, Basin offers Emergency Perch, Nest offers Boss-Ready Tune-Up, and the economy simulator now samples live Basin/Nest choices plus premium market prep services.
- Verification: `npm run validate`, `npm run build`, `npm run validate:bundle-size`, `npm run audit:balance`, `npm run audit:economy`, `npm test -- --runInBand`, and focused Playwright smoke tests for premium market services, enemy Roost punishers, conditional free draw, updated Basin/cache route choices, and reward-screen input safety passed. A full `npx playwright test tests/smoke.spec.ts --reporter=line` run reached 74/76 before two stale expectations were corrected; the corrected two-test subset then passed. Required browser/client check ran on `http://127.0.0.1:5173/`; screenshot inspected at `.artifacts/balance-pass-screenshot.png`.
- Remaining note: the app entry is 674.8 KB against the 675 KB hard cap after trimming dead market utility card plumbing and increasing Terser compression passes; it still exceeds the soft 590 KB target, so scene/module splitting remains the clean performance follow-up.

- Combat log removal UX pass: removed the player-facing combat event strip, `Log >` affordance, recent-signals drawer, and dormant route log footer. Supply feedback remains as immediate feedback, but the ledger-like `LAST SUPPLY` label was removed and replaced with the supply icon treatment.
- Verification: `npx tsc --noEmit` passed, required `develop-web-game` client ran against `http://127.0.0.1:5173/`, and direct Playwright combat screenshot/text scan confirmed no visible `Log`, `LAST SUPPLY`, or recent-signals strings at `.artifacts/test-results/combat-no-log-ui.png`. `npm run build` is currently blocked by a Windows lock on generated `.artifacts/build/assets/map_sticker_strip-U61G5Fsa.webp` from existing local preview/dev/test processes, after TypeScript had completed.

- Iconography/simplification pass for selected recommendations 1, 3, 4, 5, 6, and 7: HUD chips now read as icon + value, hand cards use compact rule grammar, route confirmation is icon-only with a hover tooltip, route node hover uses icon/risk/reward chips, Waymark/Supply run drawers prioritize icon + name + one-line effect grammar, and deck review uses icon counts plus target/role/stat chips.
- Verification: `npx tsc --noEmit`, required `develop-web-game` client on `http://127.0.0.1:5173/`, direct Playwright screenshots inspected at `.artifacts/test-results/icon-pass-route-confirm.png`, `icon-pass-route-tooltip.png`, `icon-pass-combat-hand.png`, `icon-pass-deck-review-fixed.png`, `icon-pass-waymarks-drawer-filled.png`, and `icon-pass-supplies-drawer-filled.png`; fresh browser console check had no errors. `npm run build` passed with the existing large chunk warning.

- Card/Supply/Waymark meaningfulness pass: removed exact Waymark duplicates and made several low-interest cards/items ask for clearer decisions. Plume Flash now rewards banked Resonance before generating more; Quill Slip now needs Winded setup for its payoff; Basin/Nest Fledglings convert into Open Sky/next-brace planning instead of generic free value; Hover Check and Spiral Search now care about Open Sky/retention. Zip Tie Roll, Spare Harness, Rooftop Decoy, Bottlecap Popper, Tar Solvent, Emergency Call, and Rain Cape now occupy more distinct tactical jobs.
- Runtime support: card `retainHand` is now resolved like the existing Supply/Waymark retain effect, and Signal Waymarks now resolve all authored route effects instead of only reading `gainScrap`, allowing Wire Map to provide route preview without no-oping. Added the missing `BattleScene.logDrawerOpen` field that was already referenced by existing drawer UI and blocked TypeScript compilation.
- Verification: duplicate scan reports zero exact card/Supply/Waymark effect duplicates. `npm run validate:runtime`, `npm run validate`, `npm run audit:balance`, `npm run audit:economy`, `npm test`, `git diff --check`, and `npm run build` passed. Required generic `develop-web-game` client ran on `http://127.0.0.1:5173/`; menu and route screenshots inspected under `output/web-game-balance-pass-start/`. Manual Playwright route-to-combat smoke completed with no console/page errors; combat screenshot/state inspected at `output/web-game-balance-pass-manual/03-after-route-click.png` and `output/web-game-balance-pass-manual/03-after-route-click-state.json`.
- Remaining note: no blocking work items from this pass. Existing bundle soft-target warning remains; the app still passes the hard bundle-size gate.

- Runtime continuation: added `tools/validate-deployment-cache.mjs`, wired `validate:deployment-cache` into `npm run validate`, and documented the hashed-asset/cache-header build contract in `docs/deployment-cache.md`.
- Smoke harness cleanup: moved Playwright webServer execution to `npm run build` plus `vite preview` on a smoke port, disabled smoke HMR/watch churn, and added boot retry logic so broad smoke coverage runs against production output instead of a reload-prone dev server.
- Bundle cleanup: shared optional runtime/preload image loader bookkeeping in `src/game/runtime-images.ts`, moved Codex glossary data into the lazy Codex data chunk, and tightened small Codex text-state/header strings. The app entry now passes the 675 KB hard cap again at `index-DQbnt2gm.js` 674.7 KB minified / 166.0 KB gzip; the intentional 590 KB target warning remains.
- Smoke expectation refresh: updated stale assertions for Tar Solvent's authored `removeCover(12)` effect and current Codex subtitle copy.
- Verification: `npm run build`, `npm run validate`, `npm test`, focused smoke for balance/supplies/next-item hooks, and full `npx playwright test tests/smoke.spec.ts --workers=1 --reporter=line` all passed (77/77). Required `develop-web-game` client ran against built preview `http://127.0.0.1:5391/`; screenshot and state inspected at `.artifacts/runtime-continue-client/shot-1.png` and `.artifacts/runtime-continue-client/state-1.json`.
- Remaining note: no blocking harness/cache work remains. The next meaningful performance item is still a real scene/module split to move the app entry toward the 590 KB soft target; local string/dead-code trimming is now down to tiny margins.

- New user prompt: add a glossary of keywords to the Codex.
- Added a `Glossary` Codex section backed by lazy `codex-data` terms, with 25 core gameplay, run-item, and route-node keywords including Wingbeat, Molt, Open Sky, Overextension, Waymark, Supply, Scrap, Basin, Nest, Signal, and Cache.
- Rendered glossary entries as compact two-column Codex tiles with category chips and short gameplay summaries; `render_game_to_text` now exposes the glossary term list when the Glossary section is active.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:bundle-size`, focused Codex smoke tests, and required `develop-web-game` client passed. Visual screenshot inspected at `output/web-game-glossary/shot-0.png`; text state reported `section: glossary` with 25 terms.

- Codex glossary tab placement follow-up: moved the top-level Glossary section tab to the right-most Codex tab slot after Enemies.
- Verification: `npm run build`, required generic `develop-web-game` client on `http://127.0.0.1:4173/`, direct built-preview Codex probe confirmed section labels render left-to-right as Cards / Items / Leaders / Enemies / Glossary, and screenshot inspected at `.artifacts/test-results/codex-glossary-rightmost.png`. Only the usual WebGL `ReadPixels` screenshot warnings appeared.

- Combat Roost button size stability: fixed the icon-only combat Roost control so it is created at the same fixed `74x74` display size used by its hover/out handlers, preventing the global UI icon preview scale from making it start oversized and then snap smaller after pointer-out.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5195/`; focused direct Playwright combat probe measured the Roost icon at `74x74` at rest, on hover, after pointer-out, and after click/turn advance. Screenshots inspected at `.artifacts/test-results/roost-button-fixed/client/shot-0.png`, `.artifacts/test-results/roost-button-fixed/direct/roost-rest.png`, `.artifacts/test-results/roost-button-fixed/direct/roost-hover.png`, and `.artifacts/test-results/roost-button-fixed/direct/roost-after-click.png`.

- Wingbeats icon generation: referenced `docs/art/art-bible.md`, `docs/art/ui-iconography-workshop.md`, and existing UI/Supply/Waymark icons, then generated a high-resolution pixel-painterly brass/teal wingbeat charm with built-in image generation. Processed the chroma-key result to transparent PNG and saved the runtime asset at `assets/runtime/ui/icons/wingbeats.png`.
- Wingbeats HUD integration: added the `wingbeats` UI icon id and rendered it as a fixed `34x34` badge attached to the combat Wingbeats orb, preserving the existing numeric value and label.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5196/`; direct combat Playwright probe confirmed `ui-icon-wingbeats` renders at `34x34` in `BattleScene`. Screenshots inspected at `.artifacts/imagegen/wingbeats/wingbeats-runtime-check.png`, `.artifacts/test-results/wingbeats-icon/direct/combat-wingbeats-icon.png`, and `.artifacts/test-results/wingbeats-icon/direct/wingbeats-orb-crop-3x.png`.

- Wingbeats shared-chip integration: moved the combat Wingbeats meter from its one-off orb/badge treatment to the same `renderHudMetricChip` path used by Cover and Resonance, with `hudIconForLabel` and `statIconForKey` resolving Wingbeat/Wingbeats/Energy to the generated `wingbeats` UI icon.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5197/`; direct combat Playwright probe confirmed one `ui-icon-wingbeats` image inside a `126x44` BattleScene metric chip. Screenshot inspected at `.artifacts/test-results/wingbeats-icon-shared/direct/combat-wingbeats-shared-chip.png`.

- Wingbeats HUD placement correction: moved Wingbeats into the top combat HUD beside Cover and Resonance, removed the lower-left duplicate meter, and kept the Wingbeats icon on the exact same shared HUD icon display sizing path as the adjacent Cover/Resonance icons.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5198/`; direct BattleScene probe measured Cover, Wingbeats, and Resonance icons at matching `61.6x61.6` display size, with no lower Wingbeats icon remaining. Screenshot inspected at `.artifacts/test-results/wingbeats-hud-top/direct/combat-wingbeats-top-hud-crop.png`.

- Flock/Roost icon integration: replaced the runtime `flock-heart` icon with the clearer health-heart Cohesion concept and replaced `roost-nest` with the Roost End Turn concept. Both were normalized to 256x256 alpha PNGs for predictable HUD/button scaling; previous runtime icon files were preserved under `.artifacts/imagegen/flock-roost-icon-concepts/previous-runtime-icons/`.
- Verification: `npm run build` passed with the existing large-chunk warning. Required `develop-web-game` client ran on built preview `http://127.0.0.1:5199/`; direct BattleScene probe confirmed `ui-icon-flock-heart` in the top HUD and `ui-icon-roost-nest` on the end-turn control at the expected display sizes. Screenshot inspected at `.artifacts/test-results/flock-roost-icons-integrated/direct/combat-flock-roost-icons.png`.

- Flock/deck HUD correction: moved the Flock health icon above the HP bar, kept the lower-left draw pile count/open action scoped to cards left in the draw pile, and made the top HUD Deck chip show/open the full combat deck instead of only the remaining draw pile.
- Verification: `npx tsc --noEmit`, isolated `npx vite build --outDir .artifacts/build-hud-deck --emptyOutDir true`, required `develop-web-game` client on `http://127.0.0.1:5200/`, and direct Playwright probes passed. Direct state measured draw pile `5`, hand `5`, discard `0`, and deck `10`; lower-left draw opened `Draw Pile (5)`, while the HUD deck chip opened `Deck Review (10)`. Screenshots inspected at `.artifacts/test-results/hud-deck-counts/direct-final/combat-hud-deck-counts.png`, `.artifacts/test-results/hud-deck-counts/direct-final/draw-overlay.png`, and `.artifacts/test-results/hud-deck-counts/direct-final/deck-overlay.png`.

- Flock HUD icon alignment follow-up: resized the Flock health icon to the same shared HUD icon display size as Cover, Wingbeats, and Resonance, moved it onto the same top-HUD centerline, and drew it after the HP bar fill so it visibly sits above the bar layer.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5201/`, and direct Playwright HUD probe passed. The probe measured Flock, Cover, Wingbeats, and Resonance icons at `61.6x61.6`; Flock centered at `y=45` and rendered after the HP bar rectangles. Screenshot inspected at `.artifacts/test-results/hud-flock-icon-level/direct/combat-flock-icon-level.png`.

- Non-combat event visual pass: upgraded Basin/Nest/Cache/Signal event overlays with physical object-style choice cards, route-effect outcome token strips, stronger resident/place plaques, subtle ambient glow/motion, and bespoke Cache/Signal compositions. Cache now presents as a pulley cache cabinet with drawer choices; Signal now presents as a switchboard route map with live wire paths; Basin/Nest reuse the richer object-choice treatment while preserving their existing resident/backdrop art.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5202/`, and direct built-preview screenshots for Cache, Signal, Nest, and Basin passed. After a second layout tightening pass, direct captures on `http://127.0.0.1:5203/` confirmed no bottom text overflow. Final screenshots inspected at `.artifacts/test-results/noncombat-events-visual-pass/direct-final/cache-event.png`, `.artifacts/test-results/noncombat-events-visual-pass/direct-final/signal-event.png`, `.artifacts/test-results/noncombat-events-visual-pass/direct-final/nest-event.png`, and `.artifacts/test-results/noncombat-events-visual-pass/direct-final/basin-event.png`.

- Non-combat event UX follow-up: added hover detail panels to route event choice cards with before/after previews for Cohesion, Scrap, Deck, Supplies, Waymarks, and next-combat setup; locked choices now still explain their requirement on hover. Basin gained a dedicated roost hearth service prop with recovery/shelter/supply stations and animated steam, while Nest gained a bespoke Featherwright deck bench with Preen/Sleeve/Remove tool stations instead of using the generic set-piece fallback.
- Verification: `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5204/`, and direct built-preview screenshots for Basin, Nest, Cache, and Signal hover states passed. Screenshots inspected at `.artifacts/test-results/noncombat-events-ux-pass/direct/basin-hover.png`, `.artifacts/test-results/noncombat-events-ux-pass/direct/nest-hover.png`, `.artifacts/test-results/noncombat-events-ux-pass/direct/cache-hover.png`, and `.artifacts/test-results/noncombat-events-ux-pass/direct/signal-hover-centered.png`.

- Enemy battlefield placement pass: lowered and enlarged boss anchors without letting Tar-Crowned Crow flow into the HUD, staggered boss helpers lower/wider on the battlefield, aligned enemy art by visible alpha bounds so tall cutout art stands on its shadow, widened boss HP bars/shortened boss labels, and pushed intent badges outward in boss fights so boss/helper badges no longer overlap.
- Bundle/performance follow-up: split runtime game data into a `runtime-data` production chunk in `vite.config.ts`, bringing the app entry well under the 675 KB hard cap while preserving the existing lazy Codex chunk.
- Verification: `npm run build`, `npm run validate:runtime`, `npm run validate:bundle-size`, focused boss Playwright smoke tests, and `git diff --check` passed. Built-preview screenshot inspected at `.artifacts/test-results/tar-crowned-crow-placement-final.png`; measured Tar-Crowned Crow visible top at `y=135.5` with HUD bottom around `y=92` and hand top at `y=467`.

- Card/item grade improvement implementation: upgraded low-identity defensive/healing cards into timing or build-around choices. Locked Nest now trades raw cover for retain and full-block next-turn draw, Basin Chorus and Closed Basin reward full Cohesion/Open Sky planning, Twin Basin Bond and Cold Nest reward restraint/full-block states, Shared Nest sets up the next Nest brace, and Spilled Basin now plays as Open Sky mitigation instead of generic healing.
- Waymark/Supply identity pass: Patched Harness moved from passive Preen discount to after-purchase Scrap refund plus next-fight Open Sky Guard; Patched Shoulder Wrap now rewards a clean enemy turn with Cover and retention; Borrowed Raincoat, Flock Counterweight, Basin Overflow Cup, Loose Change Tin, Supply Bell, and Chalk Wingmark now ask for more specific timing. Feather Splint, Mirror Shard, and Thermos Lid gained route-context bonuses so they care about where the run has been.
- Test/metadata follow-up: removed Patched Harness from the market Preen discount metadata, updated smoke expectations for the new Waymark timings and Supply Bell repeat synergy, and fixed a stale `playUiSound('click')` type mismatch by using the existing confirm sound variant.
- Verification: `npm run validate:runtime`, `npm run build`, `npm run validate:bundle-size`, `npm test`, and focused smoke tests for Waymark economy/combat hooks, build-around Supplies/Waymarks, and Locked Nest Molt behavior all passed. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; route screenshot/state inspected at `.artifacts/test-results/balance-meaningfulness-client/shot-0.png` and combat screenshot/state inspected at `.artifacts/test-results/balance-meaningfulness-combat-client/shot-0.png`.
- Remaining note: no blocking work items from this pass. The repository still contains broad pre-existing balance/Codex/UI changes in the dirty worktree, and the existing large-chunk warning remains a future performance follow-up rather than a blocker.

- Remaining iconography cleanup pass: converted enemy intent labels from visible move-name text to icon/value badges with hover tooltips, made deck review scroll controls icon-only, shortened card detail language to `Now` / `Preen` / `Base` with Molt shown only when active, quieted empty Waymark/Supply drawer states, and removed repeated Market item category labels plus the last visible `SCRAP` price label in favor of the scrap icon.
- Codex wording cleanup: renamed player-facing item detail sections from build/prompt language to shorter `TAGS`, `SUMMARY`, `ROLE`, `TRIGGER`, and `OBJECT` labels, and removed the visual/prompt brief from Supply detail modals.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client passed on `http://127.0.0.1:5173/`. Direct Playwright screenshots inspected at `.artifacts/test-results/icon-final-combat-intent.png`, `.artifacts/test-results/icon-final-deck-detail.png`, `.artifacts/test-results/icon-final-market-labels.png`, `.artifacts/test-results/icon-final-waymark-drawer.png`, `.artifacts/test-results/icon-final-supply-drawer.png`, and `.artifacts/test-results/icon-final-codex-supply-detail.png`.

- Creativity/meaningfulness balance pass: improved low-grade cards, Supplies, Waymarks, and early enemies identified in the latest audit. Curb Step now rewards Open Sky and perfect-block timing, Scattered Quills became an anti-Cover AOE setup card, Tangled Line and Bad Directions now create distinct nuisance/loop decisions, Signal Flare gained route-memory payoff, and six underperforming Waymarks now pair their trigger with a second tactical hook.
- Enemy identity pass: Roof Rat now punishes empty-hand Roosts and coverless turns, Wire Hawk now pressures 4+ card turns with Open Sky/bonus attack setup, and Pigeon Enforcer now interacts with the Flock's Cover instead of acting as a plain stat check.
- Test follow-up: smoke coverage now asserts Ledger Tab's carried Open Sky Guard, Broken Cover Chime's Resonance, and Plumes Applause Cap's next-turn draw in addition to their prior effects.
- Verification: focused Playwright smoke tests for enemy effects, expanded Supplies, build-around Waymarks, next-item route/combat hooks, enemy Codex, and new balance verbs passed 7/7; `npm test`, `npm run build`, `npm run validate:bundle-size`, and `git diff --check` passed aside from existing CRLF warnings. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; combat screenshot/state inspected at `.artifacts/test-results/creativity-meaningfulness-client/shot-0.png` and `.artifacts/test-results/creativity-meaningfulness-client/state-0.json`.

- Low-grade lift follow-up: raised the post-audit floor by giving weak cards setup clauses, making snags more distinct burdens, and turning the weakest single-effect Waymarks into two- or three-part tactical hooks. Basin heal Waymarks now fire route rider effects after Basin recovery, after-street Waymarks can carry next-combat prep, and combat Waymarks now honor `gainScrap`.
- Specific tuning: Quill Point, Nest Juggle, Twin Plume Lookout, Underwing Shelter, Scavenger Eye, Quill Outrider, Tangled Line, Wet Feathers, and Bad Directions all gained clearer setup/payoff texture. Rain Gutter, Blue Cup Token, Rooftop Shortcut, Feather Tape, Courier Thread, Bright Bottlecap, Dawn Ribbon, Quill Notch, Harbor Bead Strand, Workshop Stamp, Molt Metronome, Eave Bloom, Plumes Busker Patch, Fresh Pinfeather, Shed Feather Case, Stage Pin, Ledger Tab, Patched Harness, Rooftop Relay Bell, Market Tally String, Rooftop Nest Lining, Razor Kite Tail, Spare Harness, and Windcatcher Spool gained meaningful rider effects or runtime support.
- Enemy floor lift: Rat Courier, Rooftop Raccoon, and Alley Cat now punish overextension, empty-hand Roosts, or coverless play more clearly without simply raising base damage.
- Audit result after the pass: cards overall A- with floor B, enemies overall A- with floor B+, items overall B- with floor C+; remaining C+ items are mostly passive economy/recovery pieces that are useful but intentionally simpler.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused Playwright smoke tests for route economy, combat Waymarks, Supplies, next-item hooks, and new balance verbs passed; `npm test`, `npm run build`, and `npm run validate:bundle-size` passed. One focused smoke boot timed out once, then passed on isolated rerun. Required `develop-web-game` client ran on `http://127.0.0.1:5173/`; screenshot/state inspected at `.artifacts/test-results/low-grade-lift-client/shot-0.png` and `.artifacts/test-results/low-grade-lift-client/state-0.json`.

- Item floor lift: raised the remaining low-interest item floor from C+ to B- by giving simple economy/safety tools a second decision axis. Loose Change Tin now rewards clean non-overextended Roosts with retention, Rain Gutter previews the next route after Basin healing, Bright Bottlecap and Stage Pin add next-turn tempo, Courier's Ledger and Ledger Tab soften future Open Sky pressure, Blue Cup Token and Spare Pocket create Supply-pack planning, and Shed Feather Case now turns Molt entry into next-turn Wingbeat. Quiet Landing, Nest Ratchet, Spark Ground Clip, Resonance Battery, Chalk Dust Pouch, Spare Harness, and Windcatcher Spool also gained route, Winded, Open Sky, or next-turn riders so they no longer read as single-note coupons.
- Audit result after the item pass: cards overall A- with floor B, items overall B with floor B-, and enemies overall A with floor A-. Targeted improved items now range from B- to A-, with Windcatcher Spool, Stage Pin, Spare Harness, and Bright Bottlecap leading the item spotlight.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, focused Playwright smoke tests for route economy/combat Waymarks/Supplies/new balance verbs passed 6/6, `npm test`, `npm run build`, and `npm run validate:bundle-size` passed. `git diff --check` reported only the existing CRLF warnings. Required `develop-web-game` client ran on `http://127.0.0.1:5173/`; combat screenshot/state inspected at `.artifacts/test-results/item-floor-lift-client/shot-0.png` and `.artifacts/test-results/item-floor-lift-client/state-0.json`.

- Remaining work closure: resolved the outstanding bundle/performance warning, UI cleanup, market polish, and broad regression items. Vite's generic warning threshold now matches the deliberate Phaser vendor budget, deployment-cache validation understands the runtime-data boot chunk, and the performance note documents the current production chunk profile and why Codex remains lazy.
- Cleaner UI follow-up: market service controls now use icon + Scrap price instead of permanent service labels, route choices use icons for enter/locked state, reward/picker copy is shorter, stale Codex image-brief expectations were removed, and compact item summaries now format `gainEnergyNextTurn` as `next Wingbeat +N` instead of leaking internal effect grammar.
- Verification: `npx tsc --noEmit`, `npm run validate`, focused Playwright smoke slices for market details and Waymark drawers, and the full `npx playwright test tests/smoke.spec.ts --workers=1 --reporter=line` passed 77/77 after the cleanup. Required `develop-web-game` client ran against `http://127.0.0.1:5173/`; visual screenshots inspected at `.artifacts/test-results/remaining-market-cleanup.png`, `.artifacts/test-results/remaining-waymark-drawer-cleanup.png`, and `.artifacts/test-results/remaining-card-reward-cleanup.png`.

- Snag card art pipeline: added a dedicated transparent Snag tarot border from imagegen output, prepared from the raw chroma-key template with connected chroma removal only (no rectangular image-window wipe), and composed Tangled Line, Wet Feathers, and Bad Directions as `center art + alpha border overlay + deterministic title text`.
- Snag Codex integration: added approved runtime portrait/thumb/icon entries for the three Snag cards, a rightmost `Snags` card tab in the Codex, Snag-specific labels/dossier accenting, and discovery when enemies insert a Snag into draw/discard.
- Snag title polish: centered the Snag title compositor vertically in the cartouche and rebuilt the three card masters plus runtime derivatives.
- Verification: visually inspected the three composed Snag cards; `npx tsc --noEmit`, `npm run validate:runtime`, `npm run validate:runtime-assets`, and focused `npx playwright test tests/smoke.spec.ts -g "codex: snag cards" --workers=1 --reporter=line` passed.

- Non-combat event maturity follow-up: non-picker Basin/Cache/Signal/Nest choices now pause on a `RESOLVED` outcome ledger instead of immediately snapping back to the route map. The ledger reuses the event backdrop/icon art, records the chosen action and resolved effect text, then lists actual before/after changes for Cohesion, Scrap, Deck, Supplies, Waymarks, and next-combat setup before `Continue` returns to route flow.
- Layout/copy polish: expanded the result panel so multi-row outcomes do not crowd the Continue button, separated the chosen action from the resolved effect line, and replaced the non-ASCII route-effect separator with ` / ` so multi-effect summaries render cleanly on the canvas.
- Verification: `npx tsc --noEmit`, `npm run build`, `git diff --check -- src/main.ts progress.md`, required `develop-web-game` client on `http://127.0.0.1:5205/`, and focused Playwright route-event probes passed. Screenshots inspected at `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/basin-result-ledger.png`, `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/cache-result-ledger.png`, `.artifacts/test-results/noncombat-events-resolution-pass/direct-final/signal-result-ledger.png`, and `.artifacts/test-results/current-dev-5173-resolution.png`. Restarted `5173` fresh and verified the ledger there too.

- Marn/Rooftop Cache maturity pass: reframed the cache event as Marn Valeclip's numbered lockbox appraisal instead of a flat reward list. Cache options now resolve as drawer claims with drawer numbers, seal/tell language, Marn notes, icons, and risk tags; the central cabinet and appraisal plaque teach clean seals, warm latches, and bad tags while preserving the existing reward mechanics.
- Contract cleanup: added the optional card-level `heldEffects` field to `RuntimeCard` so the current card data and `createCardTemplate` compile under TypeScript.
- Verification: `npm run validate:runtime`, `npx tsc --noEmit`, `npm run build`, required `develop-web-game` client on `http://127.0.0.1:5206/`, and focused Playwright cache probes passed. Screenshots inspected at `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-appraisal.png`, `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-hover.png`, `.artifacts/test-results/marn-cache-appraisal/direct-final-6/marn-cache-result-ledger.png`, and `.artifacts/test-results/current-dev-5173-marn-cache-appraisal.png`. Restarted `5173` fresh and verified the Marn drawer appraisal there.

- Marn cleanup pass: simplified the Rooftop Cache event from a duplicated cabinet-plus-choice-list layout into a single interaction loop. The cabinet drawers are now the clickable choices, the right side is one `CLAIM FILE` panel that updates on drawer hover, and the extra appraisal plaque/full-card stack was removed so the event reads as inspect drawer -> claim drawer -> resolve ledger.
- Verification: `npx tsc --noEmit`, `npm run build`, `npm run validate:runtime`, required `develop-web-game` client on `http://127.0.0.1:5207/`, and focused Playwright cache probes passed, including a real canvas click on Drawer 12. Screenshots inspected at `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-open.png`, `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-hover.png`, `.artifacts/test-results/marn-cache-cleanup/direct-final/marn-cache-resolved.png`, and `.artifacts/test-results/current-dev-5173-marn-cache-cleanup.png`. Restarted `5173` fresh and verified the cleaned event there.

- Snag gameplay integration: Snag cards now have explicit `heldEffects` that resolve at Roost if the player leaves them in hand, turning them from passive dead cards into cleanup pressure. Tangled Line drains a Wingbeat and loops back into the draw pile, Wet Feathers leaves the flock Winded, and Bad Directions spends Resonance when available before looping back into the draw pile.
- Runtime support: combat now resolves held Snag penalties before normal Roost discard/retain cleanup, displays a small `Snag!` cue, supports `applyWinded(flock, N)`, validates `heldEffects` in card data, and includes Roost penalty text in card descriptions.
- Verification: `npx tsc --noEmit`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish" --workers=1 --reporter=line`, `npm run build`, and `npm run validate:runtime-assets` passed.

- Snag expansion pass: added Loose Shingle, Static Burst, and Bent Feather as new Snag cards with distinct play-vs-Roost decisions. Loose Shingle trades away Cover and punishes no-Cover Roosts, Static Burst drains Resonance or Wingbeat and buffs the next enemy attack if held, and Bent Feather applies Winded but can be played to clear itself for the combat.
- Integration sources: Roof Rat now creates Loose Shingle, Wire Hawk creates Tangled Line on overextension, Signal Gull creates Bad Directions when ignored, Relay Starling creates Static Burst, Tower Pigeon can seed Bent Feather, and rooftop Signal choices now add Static Burst/Loose Shingle in risk-reward paths.
- Art pipeline: generated borderless imagegen center art for the three new Snags, composed all six Snag tarot cards with the existing transparent Snag border, rebuilt runtime portrait/thumb/icon assets, and expanded the Codex Snags tab/test coverage to six cards.
- Verification: visually inspected the three final composed Snag cards; `npx tsc --noEmit`, `npm run validate:runtime`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish|new snag cards|codex: snag cards" --workers=1 --reporter=line`, `npm run build`, and `npm run validate:runtime-assets` passed.

- Snag source audit follow-up: confirmed all six Snags now have both combat enemy injection sources and event/risk exchange sources. Bent Feather was the only gap, so the `rooftop_chalk_smudge` dig choice now trades `gainScrap(38)` for `addSnagToDraw(bent_feather)`.
- Verification: direct Snag-source audit script, `npm run validate:runtime`, and `npx tsc --noEmit` passed.

- Snag expansion follow-up: added four more Snags to cover the remaining pressure gaps. Glass Gap creates Open Sky exposure, Jammed Strap pressures hand/discard cleanup, Barricade Scrap gives enemies Cover, and Smeared Map reduces next-turn draw. The new Snags now enter through readable enemy identities and event exchanges: Wire Hawk/Aerial Kite and Open Sky choices seed Glass Gap, Weasel Cutpurse/tangled gear seeds Jammed Strap, Signal Gull/lookout choices seed Barricade Scrap, and route-scouting choices plus Rat Courier seed Smeared Map.
- Art pipeline: generated high-resolution pixel center art for Glass Gap, Jammed Strap, Barricade Scrap, and Smeared Map with image gen, copied the sources into `assets/concept-art/snag-cards/sources/`, composed all ten Snag tarot cards through `compose:snag-art`, and rebuilt portrait/thumb/icon runtime assets for the approved manifest.
- Runtime polish: added support/formatting for the new Snag effects, including `applyOpenSky`, `enemyGainCover`, and clearer negative `nextTurnDraw(-1)` wording in card text and combat logs. Codex Snag coverage now expects ten cards.
- Verification: visually inspected the four final composed Snag cards; `npm run compose:snag-art -- --strict`, `npm run build:runtime-card-art`, `npx tsc --noEmit`, `npm run validate:runtime`, `npm run validate:runtime-assets`, focused `npx playwright test tests/smoke.spec.ts -g "snag cards punish|new snag cards|codex: snag cards" --workers=1 --reporter=line`, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Marn Nest Note reward picker follow-up: card reward choices are now cached for the lifetime of the opened route event, so Cancel returns to Marn's drawer menu without rerolling the two options. The reward picker also renders fallback mini-card options with cost, name, suit/role, and effect text when compact card art has not been loaded into the route scene.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe for Nest Note Cancel/reopen static choices, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-nest-note-static-cards-5173.png`; the probe confirmed the same two card IDs after Cancel/reopen and no state reward claim on Cancel.

- Marn Nest Note card-art follow-up: the reward picker now queues runtime compact card art for the exact two static reward choices and redraws the picker when those textures load, so the options display as actual card faces with their illustration art rather than text-only fallback cards.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed `card-thumb-*` textures were loaded and displayed for both reward options while Cancel/reopen kept the same IDs, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-nest-note-actual-card-art-5173.png`.

- Marn Supply/Waymark drawer art follow-up: cache drawers that resolve to Supplies or Waymarks now pick and cache the concrete item when the confirmation panel opens, queue that item's runtime art, and award that exact previewed item on Claim. Snack Tin and Chalk Tag now show actual Supply/Waymark art, name, type/rarity, and compact effect text instead of generic reward text.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probes confirmed `supply-*` and `waymark-*` textures were loaded/displayed and Cancel/reopen kept the same item ID, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/marn-cache-supply-confirm-actual-art-5173.png` and `.artifacts/test-results/marn-cache-waymark-confirm-actual-art-5173.png`.

- Marn Chalk Tag consequence follow-up: the `Gain a Waymark / Add a Snag` confirmation now shows both halves of the drawer outcome side by side. The pending reward extracts deterministic Snag cards from `addSnagToDiscard`/`addSnagToDraw`, queues their card art, and the Chalk Tag panel renders the actual Waymark art next to the Bad Directions Snag card face.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed a stable Waymark ID plus `bad_directions` after Cancel/reopen and verified both `waymark-*` and `card-thumb-bad_directions` textures were displayed, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-waymark-snag-confirm-art-5173.png`.

- Marn Supply drawer copy follow-up: the Supply drawer confirmation text now uses the exact cached Supply preview, so the Snack Tin line reads like `Gain Mirror Shard / -2 Cohesion` instead of generic `Choose a Supply / -2 Cohesion` while still showing the matching actual Supply art.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe confirmed exact Supply name text plus matching `supply-*` art on the confirmation panel, `npm run build`, and required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/marn-cache-supply-confirm-actual-name-5173.png`.

- Marn reward preview hover follow-up: the Supply drawer preview and Chalk Tag Waymark/Snag split preview now expose the same hover details used elsewhere. Hovering the Supply or Waymark opens the floating item detail with description/effects, while hovering the Snag card opens the standard full card hover detail.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright probe with real pointer movement over the Supply preview, Waymark preview, and Bad Directions card preview passed; screenshots inspected at `.artifacts/test-results/marn-cache-supply-hover-detail-5173.png`, `.artifacts/test-results/marn-cache-waymark-hover-detail-5173.png`, and `.artifacts/test-results/marn-cache-snag-card-hover-detail-5173.png`. `npm run build` and required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Basin/Signal/Nest cache-format event pass: Basin, Signal, and Nest now reuse the cache-style event structure with the resident on the left, centered choice tiles, and the event centerpiece staged off to the right. Selecting a choice opens a reward drilldown before resolving, with concrete Supply, Waymark, Snag, and card visuals when available plus a compact visual reward board for generic effects.
- Runtime flow: Signal choices still record `signalChoices` and fire signal Waymark triggers from the pending confirmation, Cache still fires cache Waymark triggers, and Nest Preen/Remove confirmations now hand off to the existing card picker instead of resolving immediately.
- Verification: `npx tsc --noEmit`, `npm run build`, focused 5173 Playwright probes for Basin/Supply, Signal/card reward, and Nest/Waymark drilldowns, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-basin-cache-format-menu-5173.png`, `.artifacts/test-results/event-basin-cache-format-confirm-5173.png`, `.artifacts/test-results/event-signal-cache-format-menu-5173.png`, `.artifacts/test-results/event-signal-cache-format-confirm-5173.png`, `.artifacts/test-results/event-nest-cache-format-menu-5173.png`, and `.artifacts/test-results/event-nest-cache-format-confirm-5173.png`.

- Imagegen centerpiece prop pass: generated dedicated transparent prop-style centerpieces for Basin, Signal, and Nest so they now match Marn's cabinet-like physical focal point instead of relying on procedural placeholder boxes. Source PNGs live under `assets/concept-art/route-event-centerpieces/sources/`; alpha runtime WebPs are `assets/runtime/route-events/props/basin-hearth-cart-v1.webp`, `assets/runtime/route-events/props/signal-route-switchboard-v1.webp`, and `assets/runtime/route-events/props/nest-featherwright-bench-v1.webp`.
- Integration: `RouteScene` now queues the matching prop asset for Basin/Signal/Nest route events, renders the generated prop with subtle glow/shadow motion, and preserves the old procedural centerpiece drawings as texture-load fallbacks.
- Verification: `npx tsc --noEmit`, alpha coverage inspection, focused 5173 Playwright prop probes, `npm run build`, `npm run validate:runtime-assets`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-basin-generated-centerpiece-5173.png`, `.artifacts/test-results/event-signal-generated-centerpiece-5173.png`, and `.artifacts/test-results/event-nest-generated-centerpiece-5173.png`.

- Route event outcome-screen removal: removed the redundant post-confirm outcome receipt for Cache, Basin, Signal, and Nest route events. Confirming or claiming the reward now completes the route node, clears reward/card hover state, and returns directly to route selection; card-pick paths still complete through the existing picker flow.
- Cleanup: deleted the unused `routeEventResolution` render state plus the snapshot/delta outcome helpers so the extra screen cannot reappear through stale code.
- Verification: `npx tsc --noEmit`, focused 5173 Playwright click-through for Cache/Basin/Signal/Nest no-outcome completion, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-no-outcome-5173.png`, `.artifacts/test-results/event-basin-no-outcome-5173.png`, `.artifacts/test-results/event-signal-no-outcome-5173.png`, and `.artifacts/test-results/event-nest-no-outcome-5173.png`.

- Route event static centerpiece pass: removed the breathing/float tween from generated event prop centerpieces and made the cache glint, basin hearth/steam, signal jack lights, and nest lamp fallback elements static as well. Cache, Basin, Signal, and Nest centerpieces now read as planted physical props instead of animated UI ornaments.
- Verification: `npx tsc --noEmit`, focused 5173 event-menu captures with a delay for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-static-centerpiece-5173.png`, `.artifacts/test-results/event-basin-static-centerpiece-5173.png`, `.artifacts/test-results/event-signal-static-centerpiece-5173.png`, and `.artifacts/test-results/event-nest-static-centerpiece-5173.png`.

- Route event centerpiece text-box removal: removed the descriptive plaque box under route-event centerpieces so Cache, Basin, Signal, and Nest rely on the prop art and choice list instead of an extra explanatory panel.
- Verification: `npx tsc --noEmit`, focused 5173 menu captures for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-no-centerpiece-textbox-5173.png`, `.artifacts/test-results/event-basin-no-centerpiece-textbox-5173.png`, `.artifacts/test-results/event-signal-no-centerpiece-textbox-5173.png`, and `.artifacts/test-results/event-nest-no-centerpiece-textbox-5173.png`.

- Unified HUD tightening: reduced the shared route/combat HUD frame height from 76px to 62px, moved the lower rail up, and shortened the vertical divider so the bottom padding no longer reads as wasted space while preserving the existing icon and chip sizes.
- Verification: `npx tsc --noEmit`, focused 5173 route/event/combat HUD captures, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/hud-thinner-route-5173.png`, `.artifacts/test-results/hud-thinner-event-5173.png`, and `.artifacts/test-results/hud-thinner-combat-5173.png`.

- Route event selection icon scale pass: enlarged the choice-row icons in Cache, Basin, Signal, and Nest select screens from 17-18px to 25px so rewards/costs read more clearly without changing row height or text positions.
- Verification: `npx tsc --noEmit`, focused 5173 menu captures for Cache/Basin/Signal/Nest, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-larger-select-icons-5173.png`, `.artifacts/test-results/event-basin-larger-select-icons-5173.png`, `.artifacts/test-results/event-signal-larger-select-icons-5173.png`, and `.artifacts/test-results/event-nest-larger-select-icons-5173.png`.

- Sella Warmwick centerpiece cleanup: the Basin/Sella generated hearth-cart centerpiece now opts out of the shared prop glow circle and accent underline, leaving only the art and its grounded shadow while Signal and Nest keep their existing prop framing.
- Verification: `npx tsc --noEmit`, focused 5173 Basin event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-basin-sella-centerpiece-no-circle-line-5173.png`.

- Oren Shearbright centerpiece cleanup: the Nest/Oren generated workbench centerpiece now opts out of the shared prop glow circle and accent underline, and Oren's resident art no longer renders with the extra glow circle behind them.
- Verification: `npx tsc --noEmit`, focused 5173 Nest event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-nest-oren-centerpiece-no-circles-line-5173.png`.

- Marn Valeclip resident cleanup: the Cache/Marn event now renders Marn's resident art without the extra circular glow behind him, while leaving the lockbox cabinet and choice layout unchanged.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-no-resident-circle-5173.png`.

- Sella Warmwick placement cleanup: the Basin/Sella event now renders Sella without the resident glow circle, and the generated hearth-cart centerpiece is larger and lower so it sits visually grounded just above the bottom screen rail.
- Verification: `npx tsc --noEmit`, focused 5173 Basin event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-basin-sella-larger-lower-no-resident-circle-5173.png`.

- Ivo Tallymast Signal placement cleanup: the Signal/Ivo event now renders Ivo without the resident glow circle, removes the shared prop glow circle and underline from the generated switchboard, and lowers the switchboard so it sits near the bottom screen rail.
- Verification: `npx tsc --noEmit`, focused 5173 Signal event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-signal-ivo-lower-no-circles-line-5173.png`.

- Oren Shearbright workbench placement cleanup: lowered the Nest/Oren generated featherwright workbench so the desk sits visually near the bottom screen rail while preserving the existing no-circle/no-underline treatment.
- Verification: `npx tsc --noEmit`, focused 5173 Nest event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-nest-oren-workbench-lower-5173.png`.

- Ivo Tallymast Signal alignment pass: removed the shared transparent bottom backdrop band from the Signal event, moved Ivo's title plaque to Sella's vertical level, and enlarged the generated switchboard while keeping it grounded near the bottom rail.
- Verification: `npx tsc --noEmit`, focused 5173 Signal event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-signal-align-title-bigger-no-bottom-band-5173.png`.

- Marn Valeclip Cache title alignment pass: moved Marn's title plaque down to the same vertical level used by Sella's event so the named resident header is consistent across the set-piece events.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-title-sella-level-5173.png`.

- Marn Valeclip Cache selection padding pass: moved the drawer selection rows down inside the existing border panel so the first choice no longer crowds the panel's top edge while the cabinet, resident, and header framing stay anchored.
- Verification: `npx tsc --noEmit`, focused 5173 Cache event capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-marn-selection-padding-5173.png`.

- Route reward menu icon scale pass: enlarged the effect-token tiles inside the drilled-in route reward menu and switched scrap reward/cost tokens to the same HUD scrap-gear icon so the Cache scrap drawer reads consistently after clicking into it.
- Verification: `npx tsc --noEmit`, focused 5173 Cache reward-menu capture, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshot inspected at `.artifacts/test-results/event-cache-reward-menu-large-hud-scrap-icon-5173.png`.

- Marn Cache reward spacing fix: increased the cache drawer row height to fit the large drawer icons without crossing neighboring borders, and gave the clicked-in reward token rows more vertical room with centered labels so Scrap and Shelter Key no longer crowd their tile borders.
- Verification: `npx tsc --noEmit`, focused 5173 captures for the outer Cache menu plus the Scrap, Shelter Key, and Leave states, screenshot inspection, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Screenshots inspected at `.artifacts/test-results/event-cache-marn-reward-menu-spacing-outer-5173.png`, `.artifacts/test-results/event-cache-marn-reward-menu-spacing-scrap-5173.png`, `.artifacts/test-results/event-cache-marn-reward-menu-spacing-shelter-key-5173.png`, and `.artifacts/test-results/event-cache-marn-reward-menu-spacing-leave-5173.png`.

- Route event icon audit follow-up: Basin/Nest/Signal event rows and compact effect chips now use the same HUD `scrap-gear` UI icon for scrap costs/rewards instead of the older supply-art scrap helper. This keeps scrap visually sized through the same `addUiIconImage` path as flock-heart, cover-shield, supply-pouch, waymark-compass, preen-kit, release-card, and deck-stack.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Fresh 5173 screenshots inspected for Basin outer/reward and Nest reward at `.artifacts/test-results/icon-audit-basin-outer-5173.png`, `.artifacts/test-results/icon-audit-basin-refill-reward-5173.png`, and `.artifacts/test-results/icon-audit-nest-preen-reward-5173.png`; a later clean outer recapture retry hit a transient Windows browser launch access error after the successful captures.

- Rain Pipe reward-token overlap fix: reduced clicked-in route reward token icons from the oversized 22px UI-icon input to 18px, moved the icon slightly right, and increased label padding so single-effect choices like Basin's `Drink at the Rain Pipe` no longer let the flock-heart icon ride the token border or crowd the text.
- Verification: reproduced the issue at `.artifacts/test-results/rain-pipe-reward-before-5173.png`, then verified the fix at `.artifacts/test-results/rain-pipe-reward-after-5173.png`; spot-checked the two-row Nest Preen reward at `.artifacts/test-results/nest-preen-reward-icons-after-5173.png`. `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed.

- Sella reward-token rail spacing fix: moved the clicked-in route reward token stack lower inside the reward preview panel so the panel's top accent rail no longer overlaps the top edge of the token boxes. This specifically fixes Sella's `Drink at the Rain Pipe`, `Hide Under the Awning`, and `Keep moving` reward-selection screens.
- Verification: `npx tsc --noEmit`, `npm run build`, and the required `develop-web-game` client on `http://127.0.0.1:5173/` passed. Fresh 5173 screenshots inspected at `.artifacts/test-results/sella-reward-rail-fix-5173/drink-rain-pipe.png`, `.artifacts/test-results/sella-reward-rail-fix-5173/hide-under-awning.png`, and `.artifacts/test-results/sella-reward-rail-fix-5173/keep-moving.png`.
